---

| [⬅️ Fargate](./Fargate.md) | [📑 Day 1 목차](./README.md) | [🏠 Week 4](../README.md) | [App Runner ➡️](./App-Runner.md) |

---

# ECR 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** ECR는 Docker 컨테이너 이미지를 안전하게 저장하고 관리하기 위한 AWS의 컨테이너 레지스트리 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 개발자들이 로컬에서 생성한 Docker 이미지를 공유하거나 배포할 때 보안 문제가 발생합니다. 예를 들어, 외부에서 악의적인 이미지가 배포될 수 있는 위험이 있습니다.  
- **문제 2:** 다양한 환경(예: 개발/생산)에서 이미지를 사용할 때 이미지 버전 관리가 복잡해집니다.  
- **문제 3:** 컨테이너 이미지의 의존성이나 버전을 관리하지 않으면, 배포 시 예상치 못한 오류가 발생할 수 있습니다.  

**ECR로 해결:**
- **해결 1:** ECR은 IAM 정책을 통해 이미지 접근을 제어하고, 암호화 및 접근 제어를 통해 보안을 강화합니다.  
- **해결 2:** ECR은 이미지 태깅을 통해 다양한 환경에서 특정 버전의 이미지를 사용할 수 있도록 지원합니다.  
- **해결 3:** ECR은 이미지의 의존성과 버전을 명확히 관리해, 배포 시 예측 가능한 환경을 제공합니다.  

### 비유로 이해하기
ECR은 도서관이 아닐까 합니다. 개발자는 자신이 만든 책(컨테이너 이미지)을 도서관에 등록하고, 다른 개발자나 시스템은 필요할 때 해당 책을 안전하게 빌릴 수 있습니다. 도서관은 책의 종류(이미지 태깅), 보안(접근 제어), 그리고 책의 상태(이미지 버전)를 관리해 주죠.  

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 마이크로서비스 아키텍처에서 서비스 간 이미지 공유 | Netflix가 ECR을 사용해 여러 서비스 간 이미지를 공유 |
| 시나리오 2 | CI/CD 파이프라인에서 이미지 빌드 및 배포 | GitHub Actions에서 ECR에 이미지 저장 후 ECS로 배포 |
| 시나리오 3 | 하이브리드 클라우드 환경에서 로컬과 클라우드 간 이미지 동기화 | AWS ECS Anywhere를 사용해 로컬 서버에 ECR 이미지 배포 |

**이럴 때 ECR를 선택하세요:**
- ✅ **상황 1:** 이미지 버전 관리와 보안이 중요한 배포 환경  
- ✅ **상황 2:** AWS 서비스(예: ECS, EKS)와의 원활한 통합이 필요한 경우  
- ✅ **상황 3:** 컨테이너 이미지를 여러 환경에서 재사용해야 할 때  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **상황 → 대안 서비스 추천:** Docker Hub 사용 시 보안 위험 증가 → ECR으로 대체  
- ❌ **상황 → 대안 서비스 추천:** 이미지 관리가 필요 없는 간단한 서버 환경 → EC2 사용  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **ECS** | ECR 이미지를 기반으로 태스크를 실행 | `ECR → ECS 클러스터 → Fargate` |
| **EKS** | ECR 이미지를 Kubernetes 클러스터에 배포 | `ECR → EKS 노드 그룹 → Kubernetes` |
| **Fargate** | ECR 이미지를 자동 확장 가능한 환경에서 실행 | `ECR → Fargate 서비스 → 로드 밸런서` |
| **App Runner** | ECR 이미지를 빌드 없이 배포 | `ECR → App Runner 서비스 → HTTP 엔드포인트` |
| **ECS Anywhere** | 로컬 서버에서 ECR 이미지 사용 | `ECR → ECS Anywhere 호스트 → 로컬 애플리케이션` |

**자주 사용되는 아키텍처 패턴:**
```
[사용자] → [CloudFront] → [ECR] → [ECS 클러스터] → [Fargate] → [HTTP 엔드포인트]
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **이미지 저장 및 추출** | $0.024/GB/월 | 월 5GB 무료 |
| **데이터 전송** | $0.09/GB | 12개월 무료 (10GB) |
| **추가 기능 (예: 로그, 태깅)** | $0.0015/1000개 | 항상 무료 |

**비용 최적화 팁:**
1. 💡 **팁 1:** 프리티어 기간 동안 이미지 저장량을 극대화해 비용 절감  
2. 💡 **팁 2:** 이미지 태깅을 통해 불필요한 버전을 제거하고, 이미지 크기를 최소화  
3. 💡 **팁 3:** 로그 및 메트릭을 사용해 과다한 데이터 전송을 방지  

> **⚠️ 비용 주의:** 대규모 이미지 저장 시 데이터 전송 비용이 급격히 증가할 수 있으니, 전송량을 모니터링하고 필요 시 로컬 스토리지와 결합해 사용하세요.

## 📚 핵심 개념

### 개념 1: ECR의 컨테이너 이미지 관리 및 보안
ECR(Amazon Elastic Container Registry)는 Docker 이미지를 저장하고 관리하는 클라우드 서비스로, AWS 컨테이너 서비스(예: ECS, EKS, Fargate)와 원활하게 통합됩니다. ECR은 이미지의 보안을 강화하기 위해 IAM 정책, 암호화, 이미지 스캔 기능 등을 제공합니다. 또한, ECR은 빌드된 이미지를 저장소에 보관하고, 필요 시 빠르게 배포할 수 있도록 최적화된 스토리지 구조를 갖추고 있습니다. 이는 개발자가 컨테이너 이미지를 안전하게 관리하고, 다른 서비스와의 협업을 원활하게 만듭니다.

#### 왜 중요한가?
- **보안 강화**: IAM 정책을 통해 접근 제어를 세분화하고, 이미지 스캔을 통해 악성 코드를 탐지합니다.  
- **서비스 통합**: ECS, EKS, Fargate 등 다양한 컨테이너 서비스와의 호환성을 통해 배포 프로세스를 간소화합니다.  

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| 이미지 저장소 | Docker 이미지를 저장하고 관리하는 저장소 | `aws ecr get-login-password` 명령어로 로그인 후 이미지 업로드 |
| IAM 정책 | 사용자 및 서비스에 대한 접근 권한 설정 | `aws iam attach-role-policy`로 ECR 접근 권한 부여 |
| 이미지 스캔 | 악성 코드 및 취약점을 자동 검출 | `aws ecr describe-images`로 스캔 결과 확인 |

> **💡 Tip:** ECR에서 이미지 보안을 강화하려면 `--scan-on-push` 옵션을 사용해 빌드 시 자동 스캔을 설정하세요.  

---

### 개념 2: EKS 및 Fargate의 자동 확장과 리소스 관리
EKS(Elastic Kubernetes Service)는 Kubernetes 클러스터를 관리하는 서비스로, Fargate는 AWS가 관리하는 Kubernetes 노드를 제공합니다. Fargate는 자동 확장 기능을 통해 트래픽 증가 시 리소스를 자동으로 확장하고, 필요 시 리소스를 축소해 비용을 절감합니다. EKS 컨트롤 플레인은 클러스터의 핵심 구성 요소로, 노드 그룹을 관리하며, Fargate는 이를 통해 가상 노드를 제공합니다. 이는 높은 유연성과 확장성을 제공합니다.

#### 작동 원리
1. **클러스터 생성**: EKS를 통해 Kubernetes 클러스터를 생성하고, 컨트롤 플레인을 설정합니다.  
2. **Fargate 통합**: Fargate를 사용해 노드 그룹을 생성하고, 작업 태스크를 자동으로 배포합니다.  
3. **리소스 확장**: 트래픽 증가 시 Fargate가 자동으로 노드를 확장하고, 트래픽 감소 시 리소스를 축소합니다.  

> **💡 Tip:** Fargate를 사용할 때는 `aws eks update-kubeconfig`로 Kubernetes CLI를 설정하고, `kubectl` 명령어로 작업을 관리하세요.  

---

### 개념 3: ECS Anywhere의 하이브리드 배포
ECS Anywhere는 AWS 클라우드와 온프레미스 서버 간에 컨테이너 작업을 실행할 수 있는 기능입니다. 이는 하이브리드 클라우드 환경에서 높은 유연성을 제공하며, 네트워크 연결이 필요한 경우에도 배포가 가능합니다. ECS Anywhere는 ECR과 연동되어 이미지 저장소에서 이미지를 가져와 실행하며, ECS 클러스터에 직접 연결해 작업을 관리합니다. 이는 기존 인프라와의 호환성을 높이고, 비용을 절감할 수 있습니다.

#### 주요 특징
1. **하이브리드 아키텍처**: AWS 클라우드와 온프레미스 서버를 단일 클러스터로 통합합니다.  
2. **비용 효율성**: 기존 서버 인프라를 재사용해 추가 비용을 줄일 수 있습니다.  
3. **네트워크 유연성**: AWS VPC와 온프레미스 네트워크 간의 연결을 통해 작업을 실행합니다.  

> **💡 Tip:** ECS Anywhere를 사용할 때는 `aws ecs register-task-definition`로 작업 정의를 설정하고, `aws ecs run-task`로 작업을 실행하세요.

## 🖥️ AWS 콘솔에서 ECR 사용하기

### Step 1: ECR 서비스 접속
1. AWS Management Console에 로그인합니다  
   - URL: [https://console.aws.amazon.com](https://console.aws.amazon.com)  
   - AWS 계정이 없으신가요? [AWS 계정 생성](https://aws.amazon.com/launch/)을 참고하세요.  
2. 상단 검색창에 **"ECR"**를 입력하고, 검색 결과에서 **"ECR"**를 클릭합니다.  
3. **"ECR"** 메뉴로 이동하면, ECR 대시보드가 표시됩니다.  

> **📸 화면 확인:** ECR 대시보드가 표시되면 정상입니다.  
> **💡 Tip:** ECR은 Docker 이미지를 저장소로 관리하는 서비스입니다. 클러스터 및 태스크 배포 전에 이미지를 ECR에 업로드해야 합니다.

---

### Step 2: [주요 작업 1 - 리소스 생성] ECR 저장소 만들기
1. **"Repositories"** 메뉴를 클릭합니다.  
   - ECR 대시보드에서 좌측 메뉴의 **"Repositories"**를 선택합니다.  
2. **"Create repository"** 버튼을 클릭합니다.  
   - 저장소 이름을 입력합니다.  
     - 예: `my-app-repo`  
   - **"Image tag"** 필드는 기본값 `latest`로 자동 입력됩니다.  
3. **"Create"** 버튼을 클릭하여 저장소를 생성합니다.  

> **📸 화면 확인:** 저장소 목록에 `my-app-repo`가 추가된 것을 확인하세요.  
> **⚠️ 주의:** 저장소 이름은 대/소문자 구분이 없으며, 최대 255자까지 입력할 수 있습니다.

---

### Step 3: [주요 작업 2 - 설정/구성] ECS 클러스터 및 Fargate 태스크 설정
1. **"ECS"** 서비스로 이동합니다.  
   - AWS Console에서 상단 검색창에 **"ECS"**를 입력하고, **"ECS"**를 클릭합니다.  
2. **"Clusters"** 메뉴에서 **"Create cluster"**를 클릭합니다.  
   - 클러스터 이름을 입력합니다.  
     - 예: `my-fargate-cluster`  
   - **Cluster type**에서 **"Fargate"**를 선택합니다.  
   - **VPC**는 기본값을 선택하거나, 필요한 경우 직접 선택합니다.  
3. **"Create"** 버튼을 클릭하여 클러스터를 생성합니다.  
4. 클러스터 생성 후, **"Task definitions"** 메뉴에서 **"Create"**를 클릭합니다.  
   - 태스크 정의 이름을 입력합니다.  
     - 예: `my-fargate-task`  
   - **Container definitions**에서 다음을 설정합니다:  
     - **Container name**: `my-container`  
     - **Image**: `AWS_ACCOUNT_ID.dkr.ecr.REGION.amazonaws.com/my-app-repo:latest`  
       - AWS_ACCOUNT_ID는 [IAM 계정 ID](https://console.aws.amazon.com/iamv2/home#/security_credentials)를 확인하세요.  
     - **Memory**와 **CPU**를 적절히 설정합니다.  
5. **"Create"** 버튼을 클릭하여 태스크 정의를 저장합니다.  

> **📸 화면 확인:** 태스크 정의 목록에 `my-fargate-task`가 추가된 것을 확인하세요.  
> **⚠️ 주의:** 이미지 주소는 ECR 리포지토리의 URL을 사용해야 합니다.  

---

### Step 4: 설정 확인 및 테스트
1. **"Services"** 메뉴에서 **"Create"**를 클릭합니다.  
   - 서비스 이름을 입력합니다.  
     - 예: `my-fargate-service`  
   - **Cluster**에서 생성한 `my-fargate-cluster`를 선택합니다.  
   - **Task definition**에서 `my-fargate-task`를 선택합니다.  
   - **Desired count**를 `1`로 설정합니다.  
2. **"Launch type"**에서 **"Fargate"**를 선택합니다.  
3. **"Create"** 버튼을 클릭하여 서비스를 배포합니다.  
4. 배포 후, **"Services"** 메뉴에서 서비스 상태를 확인합니다.  
   - **"Running"** 상태가 되면 정상적으로 실행 중입니다.  
5. **"Tasks"** 메뉴에서 태스크 ID를 클릭해 로그를 확인합니다.  
   - 로그가 정상적으로 출력되면 배포가 성공했습니다.  

> **📸 화면 확인:** 서비스 상태가 `Running`이 되고, 로그가 출력된 것을 확인하세요.  
> **💡 Tip:** Fargate 태스크는 자동으로 네트워크를 구성하므로, 별도의 네트워크 설정이 필요 없습니다.  

---

### 🧾 요약 및 확인 사항
- **ECR 저장소 생성**: `my-app-repo`  
- **ECS 클러스터 생성**: `my-fargate-cluster`  
- **Fargate 태스크 정의**: `my-fargate-task`  
- **서비스 배포**: `my-fargate-service`  
- **확인 사항**:  
  1. ECR 저장소가 생성되었는지 확인합니다.  
  2. ECS 클러스터와 태스크 정의가 정상적으로 생성되었는지 확인합니다.  
  3. 서비스가 `Running` 상태인지 확인하고, 로그를 확인합니다.  

> **⚠️ 주의:** ECR은 데이터 저장소이므로, 이미지 버전 관리에 주의해야 합니다. `latest` 태그는 안정성을 보장하지 않으므로, 버전 관리가 필요한 경우 `v1.0.0`과 같은 형식을 사용하세요.

## ⌨️ AWS CLI로 ECR 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**💡 Tip:**  
AWS CLI를 사용하기 전에 [AWS CLI 설치 가이드](https://docs.aws.amazon.com/cli/latest/userguide/cli-install.html)를 참고해 설치하고, `aws configure` 명령어로 액세스 키와 리전을 설정해야 합니다.  
프리티어로 ECR 사용 시, 5GB의 리포지토리 저장소와 100MB의 이미지 저장 공간이 제공됩니다.

---

### 예제 1: ECR 리소스 조회
```bash
# ECR 리포지토리 목록 조회
aws ecr list-registries --query '[].registryId' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 결과 필터링 (리포지토리 ID 추출) | `[].registryId` |
| `--output` | 출력 형식 (table, json, text) | `table` |

**예상 출력:**
```
REGISTRY ID
123456789012
```

> **💡 Tip:** `list-registries`는 현재 계정에 속한 ECR 리전을 조회합니다. `describe-repositories` 명령어로 특정 리포지토리의 상세 정보를 확인할 수 있습니다.

---

### 예제 2: ECR 리소스 생성
```bash
# ECR 리포지토리 생성
aws ecr create-repository \
    --repository-name "my-ecr-repo" \
    --tags Key=Environment,Value=Production
```

**필수 옵션:**
- `--repository-name`: 생성할 리포지토리 이름 (예: `my-ecr-repo`)
- `--tags`: 태그 키-값 쌍 (선택 사항)

**예상 출력:**
```json
{
    "repository": {
        "repositoryName": "my-ecr-repo",
        "repositoryArn": "arn:aws:ecr:region:account-id:repository/my-ecr-repo",
        "createdAt": "2023-10-05T12:34:56Z"
    }
}
```

> **💡 Tip:** 리포지토리 생성 시 `--repository-name`은 유일해야 하며, 이미 존재하는 이름은 에러가 발생합니다.

---

### 예제 3: ECR 리소스 수정
```bash
# ECR 리포지토리 태그 추가
aws ecr put-image --repository-name "my-ecr-repo" --image-tag "v1.0.0"
```

**⚠️ 주의:**  
ECR은 직접 리포지토리 수정 명령어가 없으므로, 이미지 태그를 통해 이미지 버전을 관리하거나 `tag` 명령어로 리포지토리의 태그를 업데이트해야 합니다.

---

### 예제 4: ECR 리소스 삭제
```bash
# ECR 리포지토리 삭제
aws ecr delete-repository --repository-name "my-ecr-repo" --force

# 삭제 확인
aws ecr describe-repositories --repository-names "my-ecr-repo"
```

**⚠️ 주의:**  
`--force` 옵션 없이 삭제 시 이미지가 포함된 리포지토리는 삭제 불가능합니다. 삭제 후에는 `describe-repositories` 명령어로 상태를 확인할 수 있습니다.

---

### 자주 사용하는 명령어 정리
```bash
# 리포지토리 조회
aws ecr describe-repositories --repository-names "my-ecr-repo"

# 리포지토리 생성
aws ecr create-repository --repository-name "my-ecr-repo"

# 이미지 업로드
aws ecr put-image --repository-name "my-ecr-repo" --image-tag "v1.0.0"

# 리포지토리 삭제
aws ecr delete-repository --repository-name "my-ecr-repo" --force
```

> **💡 Tip:** ECR은 AWS CLI를 통해 이미지 관리, 리포지토리 생성/삭제, 태그 관리가 가능하며, ECS/Fargate와 연동 시 `--repository-uri`를 사용해 이미지 주소를 지정해야 합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 ECR 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **ECR 이미지 보안 및 IAM 정책 관리**  
   - 설명: ECR은 Docker 이미지의 보안성을 확보하기 위해 IAM 정책을 통해 접근 제어를 설정해야 합니다. 시험에서 ECR 이미지의 암호화, 퍼미션 설정, 정책 최소화 원칙 등이 자주 출제됩니다.  
   - 키워드: `IAM 정책`, `이미지 보안`, `VPC 통신`

2. **ECR과 ECS의 연동 구조**  
   - 설명: ECR은 ECS( Elastic Container Service)와 연동되어 컨테이너 이미지를 저장하고 배포합니다. ECS 태스크에서 ECR 이미지를 사용하는 방식, 리포지토리 설정, 태스크 정의 파일 구성 등이 핵심입니다.  
   - 키워드: `ECS 연동`, `리포지토리 설정`, `태스크 정의 파일`

3. **ECR의 네트워크 구성 및 VPC 통신**  
   - 설명: ECR 리포지토리에 접근하려면 VPC 내에서의 네트워크 설정이 필요합니다. VPC Endpoints를 통해 ECR에 대한 직접 통신을 설정하거나, NAT 게이트웨이를 사용하는 경우가 시험에서 자주 다룹니다.  
   - 키워드: `VPC 통신`, `NAT 게이트웨이`, `VPC Endpoint`

4. **ECR의 비용 구조 및 프리티어 한계**  
   - 설명: ECR은 이미지 저장소 크기, 데이터 전송량, 보안 설정 등에 따라 비용이 달라집니다. 프리티어 한계(5GB, 100MB/일)를 넘어서면 추가 비용이 발생하므로, 비용 최적화 전략이 시험에서 중요합니다.  
   - 키워드: `프리티어`, `비용 계산`, `데이터 전송량`

5. **ECR의 리소스 관리 및 스케일링**  
   - 설명: ECR은 이미지의 버전 관리, 다중 리포지토리 구조, 태스크 실행 시 자동 스케일링과의 연동 등이 시험에 자주 등장합니다. 특히 Fargate와의 연동 시 리소스 제한 설정이 핵심입니다.  
   - 키워드: `리소스 제한`, `스케일링`, `버전 관리`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "ECR은 AWS CodeBuild와 동일한 기능을 가진다"는 문장이 주어졌을 때, ECR과 CodeBuild의 차이를 혼동할 수 있습니다. | ECR은 이미지 저장소, CodeBuild는 CI/CD 도구이므로 기능이 다릅니다. |
| 함정 2 | "ECR 리포지토리 생성 시 자동으로 VPC 통신이 활성화된다"는 문장이 주어졌을 때, VPC 통신 설정이 수동으로 필요하다는 점을 간과할 수 있습니다. | VPC 통신은 수동으로 설정해야 하며, 기본적으로 활성화되지 않습니다. |
| 함정 3 | "ECR은 EKS와 같은 클러스터 관리 서비스와 연동할 수 없다"는 문장이 주어졌을 때, ECR과 EKS의 연동 방식을 혼동할 수 있습니다. | ECR은 EKS와 연동 가능하며, Kubernetes에서 이미지를 사용할 수 있습니다. |

#### 🔄 ECR vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | ECR | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | Docker 이미지 저장소 및 배포 | ECR Public (공개 리포지토리), CodeBuild (CI/CD 도구), CodeArtifact (코드 관리) | 리포지토리 접근 제어가 필요할 때 |
| 확장성 | 리포지토리 및 이미지 버전 관리가 유연 | CodeBuild는 CI/CD에 특화, CodeArtifact는 코드 관리에 특화 | 이미지 저장소로 사용해야 할 때 |
| 비용 | 저장소 크기, 데이터 전송량에 따라 비용 발생 | CodeBuild는 실행 시간에 따라 비용 | 내부 이미지 저장소로 사용할 때 |
| 지연시간 | VPC 통신 시 지연 최소화 | CodeBuild는 실행 시간이 긴 경우 지연 발생 | 내부 네트워크 통신이 필요한 경우 |

#### 📝 시험 대비 체크리스트
- [ ] ECR의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  > "ECR은 AWS에서 Docker 이미지를 저장하고 관리하는 리포지토리 서비스입니다."
- [ ] ECR를 선택해야 하는 시나리오를 알고 있는가?  
  > "내부 이미지 저장소가 필요하거나, 보안이 중요한 환경에서 사용해야 할 때"
- [ ] ECR의 제한사항/한계를 알고 있는가?  
  > "프리티어 한계(5GB, 100MB/일), VPC 통신 설정 필요, 외부 네트워크에 의존하는 경우"
- [ ] ECR와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  > "ECR은 이미지 저장소, CodeBuild는 CI/CD 도구, CodeArtifact는 코드 관리 도구"
- [ ] ECR의 비용 구조를 이해하고 있는가?  
  > "이미지 저장소 크기, 데이터 전송량, 보안 설정에 따라 비용이 발생하며, 프리티어 한계가 있음"

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 ECR를 떠올리세요:  
> - **이미지 보안**  
> - **ECS 연동**  
> - **VPC 통신**  
> - **프리티어 한계**  
> - **리소스 제한**

---

| [⬅️ Fargate](./Fargate.md) | [📑 Day 1 목차](./README.md) | [🏠 Week 4](../README.md) | [App Runner ➡️](./App-Runner.md) |

---
