---

| [⬅️ App Runner](./App-Runner.md) | [📑 Day 1 목차](./README.md) | [🏠 Week 4](../README.md) | [다음 Day ➡️](../day2/README.md) |

---

> **한 줄 정의:** Copilot는 컨테이너화된 애플리케이션을 자동화하고 관리하기 위한 AWS의 클라우드 애플리케이션 개발 도구입니다.

### 이 서비스가 해결하는 문제  
**기존의 문제점:**  
- **문제 1:** ECS 클러스터 생성 시 수동으로 네트워크 설정, 스토리지, 보안 그룹을 구성해야 하며, 이 과정이 시간과 기술적 노하우가 필요합니다.  
- **문제 2:** EKS 클러스터를 구성할 때 컨트롤 플레인 및 노드 그룹 설정이 복잡해, 초보자에게 접근성이 낮습니다.  
- **문제 3:** Fargate 태스크 실행 시 자동 확장, 리소스 할당, 모니터링을 수동으로 관리해야 하며, 실시간 최적화가 어렵습니다.  

**Copilot로 해결:**  
- **해결 1:** Copilot은 ECS 클러스터 생성 시 네트워크, 보안, 스토리지 설정을 자동화하여 수동 작업을 최소화합니다.  
- **해결 2:** EKS 컨트롤 플레인과 노드 그룹 설정을 단계별로 안내하며, 클러스터 관리의 복잡성을 줄입니다.  
- **해결 3:** Fargate 태스크에 자동 확장 및 리소스 관리를 통합해, 애플리케이션 성능 최적화를 실시간으로 지원합니다.  

### 비유로 이해하기  
Copilot은 개발자의 프로젝트 매니저 역할을 합니다. 예를 들어, 새로운 마이크로서비스 애플리케이션을 배포할 때, Copilot이 ECS 클러스터 구성, EKS 클러스터 생성, Fargate 태스크 실행을 자동으로 처리해 주므로, 개발자는 코드 작성에 집중할 수 있습니다. 이처럼 Copilot은 컨테이너 관리의 번거로움을 해소해 생산성을 높입니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)  

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 마이크로서비스 아키텍처 구축 시 ECS 클러스터 자동화 | 회사 A가 10개 이상의 마이크로서비스를 ECS로 관리하며 Copilot로 클러스터 생성 |
| 시나리오 2 | 기업 규모의 EKS 클러스터 관리 | 회사 B가 EKS를 사용해 기업 내부 애플리케이션 운영 및 보안 정책 적용 |
| 시나리오 3 | Fargate 기반의 무.serverless 애플리케이션 배포 | 스타트업 C가 Fargate로 라우팅 서비스를 운영하며 자동 확장 기능 활용 |

**이럴 때 Copilot를 선택하세요:**  
- ✅ **수동 클러스터 설정 시간 절약**  
- ✅ **EKS/EC2 클러스터 관리 복잡성 감소**  
- ✅ **Fargate 자동 확장 및 리소스 최적화**  

**이럴 때는 다른 서비스를 고려하세요:**  
- ❌ **EKS 커스터마이징이 필수적일 경우** → **EKS 관리 포털** 또는 **Terraform**  
- ❌ **ECS Anywhere 하이브리드 배포 필요 시** → **EC2 + ECS Anywhere**  
- ❌ **ECR 이미지 관리가 주요 목표일 경우** → **ECR + AWS CodeBuild**  

---

## 🔗 연관 서비스 (Used Together With)  

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **ECR (Elastic Container Registry)** | 컨테이너 이미지 저장 및 보안 관리 | Copilot → ECR → ECS/Fargate |
| **CloudFormation** | 인프라 as Code로 자동화된 클러스터 구성 | Copilot + CloudFormation → ECS 클러스터 생성 |
| **AWS CodePipeline** | CI/CD 파이프라인 통합 | CodePipeline → Copilot → 배포 자동화 |

**자주 사용되는 아키텍처 패턴:**  
```
User → AWS CodePipeline → Copilot → ECR → ECS/Fargate → Load Balancer → End User
``

---

## 💰 비용 구조 (Pricing)  

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **Copilot 클러스터 생성** | $0.10/클러스터(월 기준) | 월 10개 클러스터 무료 |
| **Fargate 태스크 실행** | $0.015/시간(2023 기준) | 12개월 무료 (초기 100시간) |
| **EKS 클러스터 관리** | $0.25/클러스터(월 기준) | 월 1개 클러스터 무료 |

**비용 최적화 팁:**  
1. 💡 **프리티어 활용**: 초기 100시간 Fargate 사용 또는 10개 클러스터 무료 기간을 활용해 비용 절감  
2. 💡 **리소스 모니터링**: CloudWatch로 리소스 사용량을 실시간으로 추적해 과다 사용 방지  
3. 💡 **ECR 이미지 최소화**: 필요 없는 이미지 태그 제거 및 캐시 최적화로 저장소 비용 절감  

> **⚠️ 비용 주의:** Fargate 태스크 실행 시간이 급증하거나 EKS 클러스터 수가 늘어날 경우, 예상치 못한 비용이 발생할 수 있습니다. AWS Cost Explorer를 사용해 정기적으로 비용 분석을 수행해야 합니다.

## 📚 핵심 개념

### 개념 1: ECS 클러스터 구성 및 관리  
ECS( Elastic Container Service) 클러스터는 컨테이너 작업을 실행하고 관리하는 AWS 서비스로, 작업(task)을 실행할 수 있는 호스팅 환경을 제공합니다. Copilot은 ECS 클러스터를 자동으로 생성하고, 서비스(service)와 태스크(task)를 관리해 애플리케이션 배포를 간소화합니다. 클러스터는 작업을 실행할 수 있는 호스트(에이전트)를 포함하며, Copilot은 이 클러스터를 구성하고, 작업 실행 방식을 정의하는 태스크 정의(task definition)를 생성합니다.  

#### 왜 중요한가?  
- **작업 자동화**: Copilot은 수동으로 클러스터 설정을 할 필요 없이, 단일 명령어로 클러스터 및 서비스를 생성합니다.  
- **확장성 최적화**: 작업 수에 따라 자동으로 리소스를 확장해 성능을 유지합니다.  
- **모니터링 지원**: AWS CloudWatch와 통합해 작업 상태를 실시간으로 모니터링할 수 있습니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| 클러스터 생성 | ECS 클러스터를 생성하고, 작업 실행 환경을 설정 | `aws ecs create-cluster --cluster-name my-cluster` |
| 태스크 정의 | 작업 실행 방식을 정의하는 YAML 파일 | `task-definition.yaml` 파일에 CPU, 메모리, 이미지 설정 |
| 서비스 관리 | 태스크를 실행하는 서비스를 생성 및 관리 | `aws ecs create-service --cluster my-cluster --service-name my-service` |

> **💡 Tip:** Copilot CLI를 사용하면 `copilot deploy` 명령어로 클러스터 및 서비스를 한 번에 배포할 수 있습니다.  

---

### 개념 2: EKS 컨트롤 플레인 및 노드 그룹  
EKS(Elastic Kubernetes Service)는 Kubernetes 클러스터를 AWS에서 관리하는 서비스로, 컨트롤 플레인(관리 서버)과 노드 그룹(worker node)으로 구성됩니다. Copilot은 EKS 클러스터를 생성하고, 노드 그룹을 자동으로 설정해 애플리케이션을 Kubernetes 환경에서 실행할 수 있도록 합니다.  

#### 작동 원리  
1. **클러스터 생성**: Copilot은 EKS 컨트롤 플레인을 생성하고, AWS Managed Node Group을 자동으로 설정합니다.  
2. **노드 그룹 확장**: 작업량에 따라 노드 그룹을 자동으로 확장해 리소스를 효율적으로 관리합니다.  
3. **워크로드 배포**: 생성된 클러스터에 Kubernetes 애플리케이션을 배포하고, 자동으로 스케일링을 적용합니다.  

> **💡 Tip:** EKS Managed Node Group을 사용하면 노드 관리 비용을 절감할 수 있습니다.  

---

### 개념 3: Fargate 자동 확장 및 리소스 관리  
Fargate는 ECS에서 컨테이너 실행을 위한 레이어로, 인프라 관리를 걷어내고 자동 확장을 지원합니다. Copilot은 Fargate를 사용해 작업을 실행하고, 작업량에 따라 자동으로 컴퓨팅 리소스를 확장해 성능을 최적화합니다.  

#### 주요 특징  
1. **자동 확장**: 작업량이 증가할 때 Fargate는 자동으로 CPU/메모리 리소스를 증가시켜 처리 능력을 높입니다.  
2. **리소스 최적화**: 작업이 종료되면 자동으로 리소스를 해제해 비용을 절약합니다.  
3. **모니터링 통합**: AWS CloudWatch와 연동해 작업 상태를 실시간으로 추적할 수 있습니다.  

> **💡 Tip:** Fargate는 리소스 관리에 대한 걱정을 덜어주며, 작업 비용을 정확히 추적할 수 있습니다.  

---

### 개념 4: ECR 이미지 보안 및 IAM 정책  
ECR(Elastic Container Registry)는 Docker 이미지를 저장하고 관리하는 서비스로, Copilot은 ECR에서 이미지를 안전하게 저장하고, IAM 정책을 통해 접근 권한을 제어합니다. 이는 이미지의 보안성과 액세스 제어를 강화합니다.  

#### 왜 중요한가?  
- **이미지 보안**: ECR은 암호화 및 스캔 기능을 통해 이미지의 취약점을 탐지합니다.  
- **IAM 정책**: 특정 사용자나 서비스에만 이미지 접근 권한을 부여해 보안 위험을 줄입니다.  
- **CI/CD 통합**: Copilot은 ECR 이미지를 빌드 후 자동으로 등록해 배포 프로세스를 간소화합니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| 이미지 스캔 | ECR에서 이미지의 보안 취약점을 자동으로 검출 | `aws ecr describe-images` |
| IAM 정책 | 특정 사용자나 서비스에 이미지 접근 권한을 부여 | `ecr:Push`, `ecr:GetAuthorizationToken` 권한 부여 |
| 이미지 등록 | 빌드된 이미지를 ECR에 등록 | `aws ecr put-image --repository-name my-repo` |

> **💡 Tip:** ECR에서 IAM 정책을 설정하면 이미지 접근 권한을 정밀하게 제어할 수 있습니다.  

---

### 개념 5: ECS Anywhere 하이브리드 배포  
ECS Anywhere는 AWS 클라우드와 온프레미스 환경을 연결해, AWS에서 호스팅되는 ECS 클러스터에 온프레미스 서버를 연결하는 기능입니다. Copilot은 이 기능을 통해 하이브리드 아키텍처를 구축해, 혼합 환경에서 애플리케이션을 실행할 수 있도록 합니다.  

#### 주요 특징  
1. **하이브리드 인프라**: AWS 클라우드와 온프레미스 서버를 연동해 리소스를 효율적으로 분배합니다.  
2. **보안 강화**: AWS VPC와 통합해 네트워크 보안을 강화합니다.  
3. **비용 최적화**: 필요한 만큼의 리소스를 사용해 비용을 줄일 수 있습니다.  

> **💡 Tip:** ECS Anywhere는 하이브리드 아키텍처를 구축할 때 특히 유용하며, Copilot을 통해 쉽게 설정할 수 있습니다.

## 🖥️ AWS 콘솔에서 Copilot 사용하기

### Step 1: Copilot 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - **AWS 계정 ID**와 **IAM 사용자 권한**이 활성화되어 있어야 합니다.  
2. 상단 검색창에서 **"Copilot"**을 입력하고, 검색 결과에서 **"Copilot"**을 클릭합니다.  
   - **Copilot 대시보드**가 열리면 정상입니다.  

> **📸 화면 확인:** Copilot 대시보드에서 **"Applications"** 탭이 표시되면 정상입니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **ECS 클러스터 생성**  
   - **"Create application"** 버튼을 클릭하여 새 애플리케이션을 생성합니다.  
   - **Application name**을 입력하고, **Service type**에서 **"ECS"**를 선택합니다.  
   - **Cluster type**에서 **"Fargate"**를 선택하여 Fargate 기반 클러스터를 설정합니다.  
2. **ECR 리포지토리 설정**  
   - **"ECR repository"** 필드에 리포지토리 이름을 입력합니다.  
   - **"Image tag"** 필드에서 `latest` 또는 커스터마이징한 태그를 입력합니다.  
3. **리소스 확인**  
   - 생성된 **ECS 클러스터**와 **ECR 리포지토리**가 **"Applications"** 탭에서 표시됩니다.  

> **📸 화면 확인:** "Applications" 탭에서 생성된 애플리케이션 이름과 ECS 클러스터 상태가 표시됩니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **Fargate 태스크 구성**  
   - 애플리케이션을 클릭한 후, **"Tasks"** 탭에서 **"Create task"**를 선택합니다.  
   - **Task name**을 입력하고, **Container image**를 ECR 리포지토리 URL로 설정합니다.  
   - **CPU/内存** 값을 조정하여 Fargate 태스크의 자원 할당량을 설정합니다.  
2. **네트워크 설정**  
   - **"Network"** 섹션에서 **VPC**와 **Security Group**을 선택합니다.  
   - **Public IP**를 활성화할지 여부를 결정합니다.  
3. **자동 스케일 설정**  
   - **"Autoscaling"** 옵션에서 **Minimum/Maximum** 태스크 수를 설정합니다.  

> **⚠️ 주의:** Fargate 태스크 생성 시 **ECS 클러스터**와 **ECR 리포지토리**가 이미 생성되어 있어야 합니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **리소스 확인 방법**  
   - **"Applications"** 탭에서 애플리케이션 이름을 클릭해 **"Services"** 탭에서 ECS 클러스터 상태를 확인합니다.  
   - **"Tasks"** 탭에서 태스크 상태(예: `RUNNING`, `STOPPED`)를 확인합니다.  
2. **정상 동작 테스트**  
   - **"Actions"** 메뉴에서 **"Start task"**를 선택해 태스크를 실행합니다.  
   - **"Logs"** 탭에서 컨테이너 로그를 확인하여 오류를 점검합니다.  

> **💡 Tip:** Copilot은 자동으로 ECR 이미지 빌드 및 ECS 태스크 배포를 수행하므로, **"Build"** 및 **"Deploy"** 단계를 별도로 진행할 필요가 없습니다.  

---

### 📊 서비스 비교 및 비용 요약  
| 서비스         | 설명                             | Free Tier 지원 | 비용 주의사항                     |  
|----------------|----------------------------------|----------------|----------------------------------|  
| **ECS Fargate** | Fargate 기반 클러스터 및 태스크   | ✅              | 태스크 실행 시간/수에 따라 요금 발생 |  
| **ECR**        | Docker 이미지 저장소             | ✅              | 리포지토리 및 이미지 수 제한 있음 |  
| **App Runner** | 코드로 자동 배포 지원            | ✅              | 요청 수에 따라 요금 발생          |  
| **EKS**        | Kubernetes 기반 클러스터          | ❌              | 클러스터 생성 및 관리 비용 고 |  

> **💰 비용 최적화 팁:** Fargate 태스크는 **1분 단위로 요금이 청구**되므로, 비용 절감을 위해 **태스크 수**와 **실행 시간**을 제어하세요. AWS Free Tier는 **ECS Fargate** 및 **ECR**에서 750시간까지 제공됩니다.

## ⌨️ AWS CLI로 Copilot 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**설명:**  
AWS CLI를 사용하기 전에 기본 설정을 확인하세요. `aws --version`으로 CLI 버전을 확인하고, `sts get-caller-identity`로 AWS 계정 정보를 확인합니다. 리전은 `configure get region`으로 확인할 수 있으며, 리전이 설정되지 않았다면 기본 리전(예: `us-east-1`)이 사용됩니다.  

> **💡 Tip:** AWS CLI는 `~/.aws/config` 파일에 설정이 저장됩니다. 필요 시 `aws configure set region us-east-1`로 리전을 변경할 수 있습니다.

---

### 예제 1: Copilot 리소스 조회  
```bash
# ECS 클러스터 목록 조회
aws ecs list-clusters --query 'clusterArns' --output text
```

**옵션 설명:**  
| 옵션 | 설명 | 예시 값 |
|-----|-----|--------|
| `--query` | 결과 필터링 (예: `clusterArns` 필드 선택) | `'clusterArns'` |
| `--output` | 출력 형식 (JSON, 테이블, 텍스트) | `text` |

**예상 출력:**  
```
arn:aws:ecs:us-east-1:123456789012:cluster/my-cluster
```

**설명:**  
`list-clusters` 명령어는 ECS 클러스터 목록을 조회합니다. `--query`를 사용해 특정 필드만 출력할 수 있으며, `--output text`로 텍스트 형식으로 결과를 받을 수 있습니다.  

> **⚠️ 주의:** Copilot은 AWS CLI를 직접 사용하지 않지만, ECS, ECR 등 AWS 서비스를 CLI로 관리할 수 있습니다.

---

### 예제 2: Copilot 리소스 생성  
```bash
# ECS 클러스터 생성
aws ecs create-cluster --cluster-name my-cluster --region us-east-1
```

**필수 옵션:**  
- `--cluster-name`: 클러스터 이름 (예: `my-cluster`)  
- `--region`: 리전 (예: `us-east-1`)  

**예상 출력:**  
```json
{
    "cluster": {
        "clusterArn": "arn:aws:ecs:us-east-1:123456789012:cluster/my-cluster",
        "clusterName": "my-cluster",
        "status": "ACTIVE"
    }
}
```

**설명:**  
`create-cluster` 명령어는 ECS 클러스터를 생성합니다. 리전은 `--region` 옵션으로 지정해야 하며, 생성된 클러스터의 ARN과 상태를 확인할 수 있습니다. Copilot은 이 클러스터를 기반으로 서비스를 배포할 수 있습니다.  

> **💡 Tip:** 클러스터 생성 후 `describe-clusters` 명령어로 상태를 확인할 수 있습니다.

---

### 예제 3: Copilot 리소스 수정  
```bash
# ECS 클러스터 구성 변경 (예: 태스크 실행 정책 변경)
aws ecs update-cluster-configuration --cluster my-cluster --region us-east-1
```

**옵션 설명:**  
- `--cluster`: 클러스터 이름  
- `--region`: 리전  

**설명:**  
`update-cluster-configuration` 명령어는 ECS 클러스터의 구성(예: 태스크 실행 정책, 리소스 제한)을 수정합니다. Copilot은 이 구성에 따라 자동으로 태스크를 관리할 수 있습니다.  

> **⚠️ 주의:** 구성 변경 시 기존 작업이 중단될 수 있으므로, 변경 전에 서비스 상태를 점검하세요.

---

### 예제 4: Copilot 리소스 삭제  
```bash
# ECS 클러스터 삭제
aws ecs delete-cluster --cluster my-cluster --region us-east-1

# 삭제 확인
aws ecs describe-clusters --clusters my-cluster --region us-east-1
```

**예상 출력:**  
```json
{
    "clusters": [
        {
            "clusterArn": "arn:aws:ecs:us-east-1:123456789012:cluster/my-cluster",
            "status": "DELETED"
        }
    ]
}
```

**설명:**  
`delete-cluster` 명령어는 ECS 클러스터를 삭제합니다. 삭제 후 `describe-clusters`로 상태를 확인할 수 있으며, 삭제는 되돌릴 수 없습니다. Copilot이 관리하는 리소스는 이 CLI 명령어로 제어할 수 있습니다.  

> **⚠️ 주의:** 삭제 전에 관련된 태스크, 서비스, ECR 리포지토리 등을 확인하세요.

---

### 자주 사용하는 명령어 정리  
```bash
# 조회
aws ecs list-clusters
aws ecs describe-clusters --clusters my-cluster
aws ecr describe-repositories

# 생성
aws ecs create-cluster --cluster-name my-cluster
aws ecr create-repository --repository-name my-repo

# 수정
aws ecs update-cluster-configuration --cluster my-cluster

# 삭제
aws ecs delete-cluster --cluster my-cluster
aws ecr delete-repository --repository-name my-repo
```

**설명:**  
- `list-clusters`로 ECS 클러스터 목록을 확인하고, `describe-clusters`로 세부 정보를 확인할 수 있습니다.  
- `create-cluster`는 클러스터 생성, `create-repository`는 ECR 리포지토리 생성을 담당합니다.  
- `delete` 명령어는 리소스를 완전히 제거합니다.  

> **💡 Tip:** Copilot은 AWS CLI로 직접 리소스를 관리하지 않지만, ECS, ECR, Fargate 등의 AWS 서비스를 CLI로 조작할 수 있습니다.  

---

### 실습 예제: ECS 클러스터 생성 및 Fargate 태스크 배포  
1. **ECS 클러스터 생성**  
   ```bash
   aws ecs create-cluster --cluster-name my-fargate-cluster --region us-east-1
   ```

2. **ECR 리포지토리 생성**  
   ```bash
   aws ecr create-repository --repository-name my-app-image --region us-east-1
   ```

3. **Docker 이미지 푸시 (예: `my-app:latest`)**  
   ```bash
   docker tag my-app:latest <AWS_ACCOUNT_ID>.dkr.ecr.us-east-1.amazonaws.com/my-app-image:latest
   docker push <AWS_ACCOUNT_ID>.dkr.ecr.us-east-1.amazonaws.com/my-app-image:latest
   ```

4. **Fargate 태스크 정의 생성**  
   ```bash
   aws ecs register-task-definition --cli-input-json file://task-definition.json
   ```

   `task-definition.json` 예시:  
   ```json
   {
     "family": "my-fargate-task",
     "containerDefinitions": [
       {
         "name": "my-container",
         "image": "<AWS_ACCOUNT_ID>.dkr.ecr.us-east-1.amazonaws.com/my-app-image:latest",
         "memory": 256,
         "cpu": 128,
         "essential": true,
         "logConfiguration": {
           "logDriver": "awslogs",
           "options": {
             "awslogs-group": "/aws/lambda/my-app",
             "awslogs-region": "us-east-1",
             "awslogs-stream-prefix": "my-app"
           }
         }
       }
     ],
     "requiresCompatibilities": ["FARGATE"],
     "networkMode": "host"
   }
   ```

5. **Fargate 태스크 실행**  
   ```bash
   aws ecs run-task --cluster my-fargate-cluster --task-definition my-fargate-task --region us-east-1
   ```

**비용 주의사항:**  
- ECS 클러스터와 Fargate 태스크는 리전별로 요금이 부과됩니다.  
- ECR 리포지토리 사용 시, 이미지 저장소 비용이 발생할 수 있습니다.  
- AWS Free Tier 사용 시, 1년간 특정 서비스(예: 1개 ECS 클러스터, 1개 Fargate 태스크) 무료입니다.  

> **💡 Tip:** Copilot CLI를 사용하면 이 과정을 자동화할 수 있지만, AWS CLI는 기본적인 리소스 관리에 적합합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Copilot 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **포인트 1**: **Copilot의 핵심 목적과 기능**  
   - 설명: Copilot은 컨테이너화된 애플리케이션을 자동화하여 AWS ECS/EKS에 배포하는 도구로, DevOps 팀이 리소스 관리, 서비스 구성, 배포 프로세스를 단순화합니다. 시험에서 Copilot의 "컨테이너 관리 및 자동화" 역할이 주요 시험 항목입니다.  
   - 키워드: `컨테이너 관리`, `자동화`, `DevOps 통합`

2. **포인트 2**: **Copilot의 사용 시나리오 및 제한 사항**  
   - 설명: Copilot는 하이브리드 클라우드(예: ECS Anywhere), 마이크로서비스 아키텍처, 자동 확장이 필요한 애플리케이션에 적합하지만, 상태 있는 애플리케이션(예: DB)은 지원하지 않습니다. 시험에서 "Copilot의 적합한/비적합한 시나리오"를 구분하는 문제가 자주 출제됩니다.  
   - 키워드: `하이브리드 클라우드`, `마이크로서비스`, `상태 있는 애플리케이션`

3. **포인트 3**: **Copilot과 ECS/EKS의 통합 방식**  
   - 설명: Copilot는 ECS와 EKS 클러스터에 직접 통합되어 서비스 배포를 자동화합니다. 예를 들어, `eksctl` 또는 `ecs-cli`를 사용해 클러스터 생성 후 Copilot이 서비스 구성 및 관리를 담당합니다. 시험에서 "서비스 통합 방식"은 핵심 개념입니다.  
   - 키워드: `ECS 통합`, `EKS 통합`, `eksctl`

4. **포인트 4**: **Copilot의 비용 구조 및 프리티어 활용**  
   - 설명: Copilot는 리소스 사용 기반 요금제를 사용하며, 프리티어는 1년간 100만 개의 컨테이너 실행을 제공합니다. 시험에서는 "비용 최적화 전략"과 "프리티어의 한계"를 구분하는 문제가 등장합니다.  
   - 키워드: `리소스 기반 요금제`, `프리티어`, `비용 최적화`

5. **포인트 5**: **Copilot의 자동 확장 및 리소스 관리**  
   - 설명: Fargate와 연동해 태스크 수에 따라 자동 확장이 가능하며, 리소스 사용량을 모니터링해 비용을 절감합니다. 시험에서 "자동 확장 메커니즘"과 "리소스 관리 전략"이 핵심입니다.  
   - 키워드: `Fargate 연동`, `자동 확장`, `리소스 모니터링`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | Copilot이 EKS 클러스터 생성을 직접 수행한다고 오인 | Copilot은 EKS 클러스터 생성은 **아니고**, `eksctl` 등 도구와 연동해 서비스 구성만 담당 |
| 함정 2 | Copilot이 상태 있는 애플리케이션도 지원한다고 오인 | Copilot은 상태 없는 애플리케이션에만 적합, 상태 있는 애플리케이션은 EKS/K8s에서 직접 관리 |
| 함정 3 | Copilot의 비용이 항상 저렴하다고 오인 | Copilot은 리소스 사용량에 따라 비용이 변동, 프리티어를 활용해 비용을 최적화해야 함 |

#### 🔄 Copilot vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | Copilot | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 컨테이너 서비스 자동화 | ECS/EKS 직접 관리 | DevOps 자동화가 필요한 경우 |
| 확장성 | Fargate 자동 확장 지원 | ECS 기반 수동 확장 | 자동 확장이 필수적일 때 |
| 비용 | 리소스 사용량 기반 요금제 | EKS/ECS의 고정 비용 | 리소스 사용량이 변동적일 때 |
| 지연시간 | Fargate의 저지연 성능 | ECS의 고정 리소스 사용 | 실시간 처리가 필요한 경우 |

#### 📝 시험 대비 체크리스트
- [ ] Copilot의 핵심 목적을 한 문장으로 설명할 수 있는가?  
- [ ] Copilot를 선택해야 하는 시나리오를 알고 있는가?  
- [ ] Copilot의 제한사항/한계를 알고 있는가?  
- [ ] Copilot와 비슷한 서비스의 차이점을 설명할 수 있는가?  
- [ ] Copilot의 비용 구조를 이해하고 있는가?

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Copilot를 떠올리세요:  
> - `컨테이너 자동화`  
> - `하이브리드 클라우드`  
> - `Fargate 연동`  
> - `비용 최적화`  
> - `상태 없는 애플리케이션`

---

| [⬅️ App Runner](./App-Runner.md) | [📑 Day 1 목차](./README.md) | [🏠 Week 4](../README.md) | [다음 Day ➡️](../day2/README.md) |

---
