---

| [⬅️ ECS](./ECS.md) | [📑 Day 1 목차](./README.md) | [🏠 Week 4](../README.md) | [Fargate ➡️](./Fargate.md) |

---

# EKS 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** EKS는 Kubernetes 클러스터를 쉽게 관리하고 확장할 수 있는 AWS의 관리형 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** Kubernetes 클러스터를 직접 운영할 때, 노드 관리, 업데이트, 보안 설정 등 복잡한 작업이 필요해 시간과 비용이 많이 소요되었습니다.  
- **문제 2:** 자동 확장 및 리소스 최적화 없이 수작업으로 관리할 경우, 성능 저하와 비용 과다 지출이 발생했습니다.  
- **문제 3:** 컨테이너 이미지 보안, IAM 정책 설정 등 복잡한 보안 관리가 어렵고 실수로 인한 취약점이 발생할 수 있었습니다.

**EKS로 해결:**
- **해결 1:** AWS가 클러스터 컨트롤 플레인을 관리해 운영 부담을 줄이고, 자동 업데이트 및 보안 패치를 제공합니다.  
- **해결 2:** Fargate 자동 확장과 노드 그룹 관리 기능으로 리소스 사용량을 최적화하고 비용을 절감합니다.  
- **해결 3:** ECR과 연동해 이미지 보안 및 IAM 정책 설정을 간편하게 수행할 수 있으며, 네트워크 보안 정책도 강화됩니다.

### 비유로 이해하기
EKS는 집안의 스마트 홈 시스템을 관리하는 전문가입니다. 기존에는 모든 장치를 직접 설정하고 관리해야 했지만, EKS는 이 모든 과정을 자동화해 사용자가 단순히 앱으로 조작만 하면 됩니다. 예를 들어, 냉방 시스템(노드)을 자동으로 조절해 에너지 소비를 줄이는 것처럼, EKS는 클러스터 자원을 최적화해 비용을 절감합니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 마이크로서비스 아키텍처 구축 | AWS 기업 고객 A가 100개 이상의 마이크로서비스를 EKS 클러스터에 배포 |
| 시나리오 2 | 높은 트래픽 대응 및 자동 확장 | 온라인 쇼핑몰 B가 프로모션 기간 중 Fargate로 5배 확장해 트래픽 처리 |
| 시나리오 3 | 엄격한 보안 정책 준수 | 금융 기관 C가 ECR과 IAM 정책으로 컨테이너 이미지 보안을 강화 |

**이럴 때 EKS를 선택하세요:**
- ✅ 상황 1: Kubernetes 기반의 유연한 배포가 필요한 경우  
- ✅ 상황 2: 자동 확장과 리소스 관리를 원하는 경우  
- ✅ 상황 3: 보안 및 규정 준수를 강화해야 할 경우  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ 상황 → 대안 서비스 추천 (이유 포함): **Fargate** → 서버리스 환경에서 단순한 태스크 배포 시  
- ❌ 상황 → 대안 서비스 추천 (이유 포함): **ECS** → 리소스를 직접 관리하고 싶은 경우  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **ECR** | 컨테이너 이미지 저장 및 관리 | EKS → ECR → 클러스터 노드 |
| **CloudFront** | 빠른 배포 및 글로벌 분배 | EKS → CloudFront → 사용자 |
| **S3** | 정적 자산 저장 및 배포 | EKS → S3 → CloudFront |

**자주 사용되는 아키텍처 패턴:**
```
User → CloudFront → S3
         ↓
         EKS (Fargate)
         ↓
         ECR
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| 클러스터 관리 | $0.10/시간 | 월 750시간 무료 |
| Fargate 실행 | $0.010/시간 | 12개월 무료 |
| ECR 저장소 | $0.004/GB/월 | 5GB 무료 |

**비용 최적화 팁:**
1. 💡 팁 1: **Spot Instance 사용** → 비용 절감 (단, 작업 중단 가능)  
2. 💡 팁 2: **자동 스케일링 설정** → 트래픽 변동에 따라 리소스 자동 조절  
3. 💡 팁 3: **태그 관리** → 리소스 분류 후 요금 세부 분석 가능  

> **⚠️ 비용 주의:** Fargate의 **데이터 전송 비용** 또는 **클러스터 관리 요금**이 예상치 못하게 누적될 수 있으니, 사용량 모니터링 필수.

## 📚 핵심 개념

### 개념 1: ECS 클러스터 구성 및 관리
ECS 클러스터는 Docker 컨테이너를 실행하고 관리하는 AWS 서비스로, 작업(_task_)과 서비스(_service_)를 통해 애플리케이션을 배포합니다. 클러스터는 리소스를 효율적으로 분배하고, 작업을 자동으로 확장하여 트래픽 증가 시 성능을 유지합니다. ECS는 컨테이너 오케스트레이션을 통해 CPU, 메모리, 네트워크를 관리하며, 스팟 인스턴스나 온디맨드 인스턴스를 사용해 비용을 최적화합니다. 클러스터는 작업을 실행할 노드를 포함하며, 작업은 서비스에 의해 관리되며, 서비스는 트래픽을 균형 있게 분배합니다. ECS는 다양한 환경에서 작동하며, 로컬 서버와 클라우드를 통합할 수 있는 유연성을 제공합니다.

#### 왜 중요한가?
- **스케일링 효율성**: 트래픽 증가 시 자동으로 리소스를 확장해 성능을 유지합니다.  
- **비용 최적화**: 스팟 인스턴스나 온디맨드 인스턴스를 조합해 비용을 줄일 수 있습니다.  
- **리소스 관리**: CPU, 메모리, 네트워크 자원을 효율적으로 분배해 애플리케이션의 안정성을 높입니다.

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|------|---|
| 클러스터 | 컨테이너 작업을 관리하는 최상위 단위 | `my-cluster` |
| 작업 | 특정 작업을 실행하는 컨테이너 그룹 | `web-task` |
| 서비스 | 작업을 관리하고 트래픽을 분배하는 로드 밸런서 | `web-service` |
| 태스크 정의 | 작업의 구성 요소(이미지, 환경 변수 등) | `web-task-definition.json` |

> **💡 Tip:** 클러스터는 작업과 서비스를 연결해 애플리케이션을 실행하는 핵심 요소로, 리소스 최적화를 위해 `spot` 인스턴스를 활용하는 것이 좋습니다.

---

### 개념 2: EKS 컨트롤 플레인 및 노드 그룹
EKS는 AWS의 Kubernetes 클러스터 관리 서비스로, 컨트롤 플레인과 노드 그룹을 통해 클러스터를 운영합니다. 컨트롤 플레인은 API 서버, etcd, 컨트롤러 매니저, 스케줄러 등으로 구성되어 클러스터의 상태를 관리합니다. 노드 그룹은 Kubernetes 노드를 포함하고, 자동 확장 기능을 통해 트래픽 증가 시 노드를 추가합니다. EKS는 클라우드 네이티브 환경에서 안정적인 클러스터 운영을 지원하며, 사용자는 컨트롤 플레인을 직접 관리하지 않아도 됩니다. 노드 그룹은 AWS EC2 인스턴스나 Fargate를 기반으로 구성할 수 있습니다.

#### 작동 원리
1. **클러스터 생성**: 사용자가 EKS를 통해 클러스터를 생성하면, AWS가 컨트롤 플레인을 설정합니다.  
2. **노드 그룹 추가**: 사용자는 EC2 노드 그룹 또는 Fargate를 통해 노드를 추가해 작업을 실행합니다.  
3. **자동 확장**: 트래픽 증가 시 노드 그룹이 자동으로 확장되어 처리 능력을 높입니다.  

> **💡 Tip:** 노드 그룹을 사용해 자동 확장 기능을 활성화하면, 트래픽 변동에 유연하게 대응할 수 있습니다.

---

### 개념 3: Fargate 자동 확장 및 리소스 관리
Fargate는 EKS에서 사용할 수 있는 관리형 Kubernetes 노드로, 사용자가 인프라를 직접 관리하지 않아도 애플리케이션을 실행할 수 있습니다. Fargate는 자동 확장 기능을 통해 트래픽 증가 시 자동으로 리소스를 확장하며, 작업이 종료되면 리소스를 해제해 비용을 절감합니다. Fargate는 리소스 관리에 초점을 맞추어 CPU, 메모리, 네트워크를 효율적으로 분배하고, 작업 실행 시 필요한 리소스만 할당합니다. 이로 인해 사용자는 인프라 관리 부담을 줄이고, 클라우드 환경에서 유연한 애플리케이션 배포가 가능합니다.

#### 주요 특징
1. **자동 확장**: 트래픽 증가 시 자동으로 노드를 추가해 처리 능력을 높입니다.  
2. **리소스 최적화**: 작업이 실행되는 동안 필요한 리소스만 할당해 비용을 절감합니다.  
3. **관리형 노드**: 사용자가 인프라를 직접 관리하지 않아도 애플리케이션을 실행할 수 있습니다.  

> **💡 Tip:** Fargate를 사용하면 인프라 관리 부담을 줄이고, 클라우드 환경에서 유연한 애플리케이션 배포가 가능합니다.

## 🖥️ AWS 콘솔에서 EKS 사용하기

### Step 1: EKS 서비스 접속  
1. AWS Management Console에 로그인합니다.  
   - URL: [https://console.aws.amazon.com](https://console.aws.amazon.com)  
2. 상단 검색창에 **"EKS"**를 입력한 후, **EKS** 서비스를 클릭합니다.  
   - 이 단계는 EKS 대시보드로 이동하여 서비스를 관리하는 시작점입니다.  

> **📸 화면 확인:** EKS 대시보드가 표시되면 "EKS Clusters" 탭이 활성화되어 있는지 확인합니다.  

---

### Step 2: [주요 작업 1 - EKS 클러스터 생성]  
1. **"Create cluster"** 버튼을 클릭합니다.  
   - **Cluster name** 필드에 `my-eks-cluster` 등 원하는 이름을 입력합니다.  
   - **Kubernetes version**은 최신 버전을 선택하는 것이 일반적입니다.  
   - **VPC 및 Subnet**은 기본값을 유지하거나, 사전에 설정한 VPC를 선택할 수 있습니다.  
2. **"Create"** 버튼을 클릭하여 클러스터를 생성합니다.  
   - 생성 과정은 약 10~15분 소요될 수 있습니다.  
3. 생성 후, **"Cluster details"** 섹션에서 클러스터 상태를 확인합니다.  
   - 상태가 **"ACTIVE"**로 변경되면 정상적으로 생성된 것입니다.  

> **📸 화면 확인:** "Cluster details" 탭에서 클러스터 상태가 `ACTIVE`로 표시되는지 확인합니다.  

---

### Step 3: [주요 작업 2 - Fargate 설정 및 태스크 배포]  
1. 생성된 클러스터를 클릭한 후, **"Compute"** 탭에서 **"Add node group"**을 클릭합니다.  
   - **"Fargate"** 옵션을 선택하여 Fargate를 사용하도록 설정합니다.  
   - Fargate는 관리형 컴퓨팅 플랫폼으로, EC2 인스턴스 관리가 필요 없습니다.  
2. **"Task definition"**을 생성하고, **"Service"**를 배포합니다.  
   - **"Create task definition"**에서 Docker 이미지를 선택하고, CPU/메모리 리소스를 설정합니다.  
   - 예: `nginx` 이미지를 사용하여 웹 서버를 배포할 수 있습니다.  
3. **"Deploy"** 버튼을 클릭하여 Fargate 태스크를 실행합니다.  
   - 배포 후, **"Services"** 탭에서 서비스 상태를 확인합니다.  

> **⚠️ 주의:** Fargate를 사용하려면 클러스터 생성 시 **"Fargate"** 옵션을 반드시 선택해야 합니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **"Services"** 탭에서 배포된 서비스를 클릭하여 상태를 확인합니다.  
   - 상태가 **"ACTIVE"**로 표시되면 정상적으로 작동하고 있습니다.  
2. **"Tasks"** 탭에서 태스크가 실행 중인지 확인합니다.  
   - 태스크 상태가 **"RUNNING"**이면 성공적으로 배포되었습니다.  
3. **"EC2 instances"** 탭에서 Fargate가 실행된 EC2 인스턴스를 확인할 수 있습니다.  
   - Fargate는 EC2 인스턴스를 자동으로 관리하므로, 수동으로 확인할 필요가 없습니다.  

> **💡 Tip:** Fargate는 비용 효율적인 솔루션으로, 기존 EC2 기반의 Kubernetes 클러스터 대비 관리 부담이 적습니다.  

---

### 📊 EKS와 Fargate 비교표  
| 항목              | EKS (EC2 기반)           | Fargate (관리형)                |  
|-------------------|--------------------------|----------------------------------|  
| 컴퓨팅 관리       | 수동 관리 (EC2 인스턴스) | 자동 관리 (AWS 관리)            |  
| 비용              | EC2 인스턴스 비용 발생    | Fargate 컴퓨팅 요금 적용        |  
| 설정 복잡도       | 높음                     | 낮음 (Fargate 옵션 선택)        |  
| 적합한 시나리오   | 대규모 워크로드, 커스터마이징 필요 | 단순 웹 서버, 빠른 프로토타이핑 |  

---

### 📌 비용 및 프리티어 활용 팁  
- **EKS 클러스터**: 기본적으로 1개의 클러스터 생성은 무료입니다.  
- **Fargate**: 프리티어는 최대 10시간의 Fargate 컴퓨팅 시간을 제공합니다.  
- **EC2 인스턴스**: 수동 관리 시, EC2 인스턴스 비용이 발생할 수 있습니다.  
- **ECR**: Docker 이미지 저장소로, 프리티어는 500MB의 저장 용량 제공.  

> **⚠️ 주의:** Fargate를 사용할 때는 "Compute platform"을 반드시 "Fargate"로 설정해야 합니다.  
> **💡 Tip:** 클러스터 생성 시 VPC 설정은 사전에 생성한 VPC를 선택하는 것이 안정적입니다.

## ⌨️ AWS CLI로 EKS 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

> **💡 Tip:** AWS CLI를 사용하기 전에 IAM 권한이 EKS 관련 작업을 수행할 수 있도록 설정해야 합니다. `iam:CreateRole`, `eks:CreateCluster` 등의 권한이 포함된 역할을 할당해야 합니다.

---

### 예제 1: EKS 클러스터 목록 조회
```bash
# [EKS 클러스터 목록 조회]
aws eks list-clusters --query 'clusters' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | JSON 결과 필터링 | `'clusters'` |
| `--output` | 출력 형식 | `json`, `table`, `text` |

**예상 출력:**
```
NAME       CREATED           STATUS    VERSION
my-cluster 2023-10-01T00:00:00  ACTIVE    1.21
```

> **⚠️ 주의:** `list-clusters` 명령어는 AWS CLI v1.20.0 이상에서 사용 가능합니다.

---

### 예제 2: EKS 클러스터 생성
```bash
# [EKS 클러스터 생성]
aws eks create-cluster \
    --name "my-cluster" \
    --role-arn "arn:aws:iam::123456789012:role/eks-role" \
    --resources-vpc-config subnetIds=subnet-12345678,subnet-87654321,securityGroupIds=sg-12345678
```

**필수 옵션:**
- `--name`: 클러스터 이름 (예: `my-cluster`)
- `--role-arn`: IAM 역할 ARN (EKS 클러스터 관리 역할)
- `--resources-vpc-config`: VPC 설정 (서브넷, 보안 그룹 등)

**예상 출력:**
```json
{
    "cluster": {
        "name": "my-cluster",
        "arn": "arn:aws:eks:us-west-2:123456789012:cluster/my-cluster",
        "createdAt": "2023-10-01T00:00:00",
        "status": "CREATE_IN_PROGRESS"
    }
}
```

> **💡 Tip:** `--role-arn`은 `aws iam get-role` 명령어로 확인할 수 있습니다.

---

### 예제 3: EKS 클러스터 수정
```bash
# [EKS 클러스터 수정]
aws eks update-cluster-config \
    --name "my-cluster" \
    --resources-vpc-config subnetIds=subnet-98765432,securityGroupIds=sg-98765432
```

**주의 사항:**
- `update-cluster-config`는 VPC 설정만 수정 가능합니다.
- 클러스터 상태가 `ACTIVE`여야 수정이 가능합니다.

---

### 예제 4: EKS 클러스터 삭제
```bash
# [EKS 클러스터 삭제]
aws eks delete-cluster --name "my-cluster"

# 삭제 확인
aws eks describe-cluster --name "my-cluster"
```

> **⚠️ 주의:** 클러스터 삭제는 되돌릴 수 없습니다. `--no-rollback` 옵션을 사용해 영구 삭제를 확인해야 합니다.

---

### 자주 사용하는 명령어 정리
```bash
# 클러스터 조회
aws eks list-clusters
aws eks describe-cluster --name "my-cluster"

# 클러스터 생성/수정/삭제
aws eks create-cluster --name "my-cluster" --role-arn "arn:aws:iam::123456789012:role/eks-role"
aws eks update-cluster-config --name "my-cluster" --resources-vpc-config subnetIds=subnet-12345678
aws eks delete-cluster --name "my-cluster"
```

> **💡 Tip:** `aws eks get-credentials` 명령어로 클러스터에 접근할 수 있는 kubeconfig 파일을 생성할 수 있습니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 EKS 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **EKS 컨트롤 플레인과 노드 그룹 구성**  
   - 설명: EKS는 AWS Managed Kubernetes 서비스로, 컨트롤 플레인(eksctl, AWS Managed Node Groups)과 자동화된 노드 관리가 핵심입니다. 시험에서는 클러스터 생성, 노드 그룹 확장, 리소스 할당 방식을 묻는 경우가 많습니다.  
   - 키워드: `AWS Managed`, `Node Groups`, `Auto Scaling`

2. **Fargate 자동 확장 및 리소스 관리**  
   - 설명: Fargate는 EKS에서 CPU/메모리 자동 확장, 무분별한 리소스 소비 방지가 핵심입니다. 시험에서는 Fargate 태스크의 리소스 제한, 비용 계산 방식, 태스크 생성 시 오류 원인을 묻는 경우가 많습니다.  
   - 키워드: `Fargate`, `Auto Scaling`, `Resource Limits`

3. **ECR 이미지 보안 및 IAM 정책 설정**  
   - 설명: EKS와 ECR의 연동은 이미지 보안 및 IAM 정책으로 구성됩니다. 시험에서는 ECR에서의 Dockerfile 최소권한 설정, IAM 역할 할당, 썸네일 정책 생성 방식을 묻는 경우가 많습니다.  
   - 키워드: `ECR`, `IAM Policies`, `Image Security`

4. **EKS Anywhere 하이브리드 배포**  
   - 설명: EKS Anywhere는 온프레미스 환경에서 Kubernetes 클러스터를 생성할 수 있는 기능입니다. 시험에서는 하이브리드 배포의 장점, vSphere/ESXi 기반 설치 절차, 리소스 할당 방식을 묻는 경우가 많습니다.  
   - 키워드: `EKS Anywhere`, `Hybrid Deployment`, `vSphere`

5. **EKS 비용 구조 및 프리티어 제한**  
   - 설명: EKS는 기본적으로 AWS Managed 서비스로, 프리티어는 25 노드까지 사용 가능합니다. 시험에서는 노드 그룹 생성 시 비용 계산 방식, Fargate 비용 구조, 비용 최적화 전략을 묻는 경우가 많습니다.  
   - 키워드: `Cost Structure`, `Free Tier`, `Node Groups`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | EKS와 EC2 클러스터 구분을 헷갈리게 하는 문제 | EKS는 AWS Managed Kubernetes 서비스로, EC2는 직접 관리하는 서버입니다. EKS에서 노드 그룹 생성 시 EC2 인스턴스 자동 생성이 특징입니다. |
| 함정 2 | Fargate와 ECS Fargate의 차이를 묻는 문제 | EKS Fargate는 AWS Managed 서비스로, ECS Fargate는 ECS 서비스에 종속됩니다. EKS Fargate는 리소스 자동 확장이 더 강력합니다. |
| 함정 3 | ECR에서의 IAM 정책 생성 시 허용 권한을 헷갈리게 하는 문제 | ECR에서 `ecr:Push` 권한은 `aws-ecr-push-access-policy` 정책에 포함되어 있으며, `ecr:GetAuthorizationToken`은 별도 정책이 필요합니다. |

#### 🔄 EKS vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | EKS | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| **용도** | Kubernetes 클러스터 관리, AWS Managed 서비스 | ECS (EC2/Fargate) | 리소스 자동 확장, 복잡한 네트워크 설정이 필요할 때 |
| **확장성** | 노드 그룹 자동 확장, Fargate 자동 리소스 할당 | ECS Fargate (수동 설정) | 고가용성 환경에서 자동화된 관리가 필요할 때 |
| **비용** | 노드 그룹 생성 시 AWS Managed 비용 발생 | ECS Fargate (리소스 수동 관리) | 리소스 사용량을 정밀 제어할 수 있는 경우 |
| **지연시간** | Fargate는 리소스 자동 할당으로 지연 최소화 | ECS EC2 (수동 설정) | 실시간 처리가 필수적인 시나리오 |

#### 📝 시험 대비 체크리스트
- [ ] EKS의 핵심 목적을 한 문장으로 설명할 수 있는가? ("AWS Managed Kubernetes 서비스로 클러스터를 자동화 관리합니다.")
- [ ] EKS를 선택해야 하는 시나리오를 알고 있는가? ("고가용성, 자동 확장, 복잡한 네트워크 설정이 필요한 환경")
- [ ] EKS의 제한사항/한계를 알고 있는가? ("프리티어는 25 노드까지, Fargate는 리소스 제한이 있음")
- [ ] EKS와 비슷한 서비스의 차이점을 설명할 수 있는가? ("ECS는 EC2/Fargate 기반, EKS는 Kubernetes 기반")
- [ ] EKS의 비용 구조를 이해하고 있는가? ("노드 그룹 생성 시 AWS Managed 비용, Fargate는 리소스 기반 요금")

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 EKS를 떠올리세요:  
> - **Kubernetes** (AWS Managed)  
> - **Node Groups** (자동 확장)  
> - **Fargate** (리소스 자동 관리)  
> - **ECR** (이미지 보안 연동)  
> - **Hybrid Deployment** (온프레미스 지원)

---

| [⬅️ ECS](./ECS.md) | [📑 Day 1 목차](./README.md) | [🏠 Week 4](../README.md) | [Fargate ➡️](./Fargate.md) |

---
