---

| [⬅️ EKS](./EKS.md) | [📑 Day 1 목차](./README.md) | [🏠 Week 4](../README.md) | [ECR ➡️](./ECR.md) |

---

# Fargate 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** Fargate는 컨테이너 애플리케이션을 실행하기 위한 AWS의 **서버리스 컨테이너 오케스트레이션** 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** EC2 인스턴스 관리가 복잡해졌습니다.  
  기존에는 EC2 인스턴스를 직접 관리해야 했고, CPU/메모리 리소스를 수동으로 할당/확장해야 했습니다.  
  예: "어떤 애플리케이션이 많은 트래픽을 받으면 인스턴스를 추가해야 했고, 이에 따라 네트워크 설정, 보안 그룹, 스토리지 연결 등 모든 리소스를 관리해야 했습니다."

- **문제 2:** 리소스 낭비가 발생했습니다.  
  기존 EC2 기반에서는 리소스를 과도하게 할당해 비용이 증가했거나, 리소스 부족으로 인해 애플리케이션 성능이 저하되었습니다.  
  예: "단일 인스턴스에 여러 애플리케이션을 배포해 CPU 사용률이 30%만 사용되더라도 고정 비용이 발생했습니다."

- **문제 3:** 확장성과 가용성이 낮았습니다.  
  트래픽 폭주 시 수동으로 인스턴스를 추가해야 했고, 인스턴스 다운 시간이 발생하면 서비스 중단이 발생할 수 있었습니다.  
  예: "수천 명의 사용자가 동시에 서비스에 접속하면 수동으로 인스턴스를 확장해야 했고, 이 과정에서 애플리케이션이 멈출 수 있었습니다."

**Fargate로 해결:**
- **해결 1:** 인프라 관리 부담을 제거했습니다.  
  Fargate는 EC2 인스턴스를 직접 관리할 필요 없으며, AWS가 자동으로 리소스를 할당/확장해 사용자에게 편리한 경험을 제공합니다.  
  예: "Fargate를 사용하면 'aws ecs run-task' 명령어로 단순히 태스크를 배포만 하면 됩니다."

- **해결 2:** 리소스 효율성을 극대화했습니다.  
  Fargate는 실제 사용량에 따라 자동으로 리소스를 할당해 비용을 최적화하고, 리소스 과다 할당을 방지합니다.  
  예: "단일 태스크에만 CPU 1vCPU를 할당해 비용을 절감할 수 있습니다."

- **해결 3:** 자동 확장 및 고가용성을 제공했습니다.  
  Fargate는 ECS 서비스의 자동 확장 기능을 활용해 트래픽 증가 시 실시간으로 태스크를 추가하고, 인스턴스 다운 시도 자동으로 복구합니다.  
  예: "1분 내에 100개의 태스크를 자동으로 생성해 서비스 가용성을 확보할 수 있습니다."

### 비유로 이해하기
Fargate는 **타ク시 서비스**에 비유할 수 있습니다.  
기존에는 자동차를 직접 구입해 운전해야 했지만, Fargate는 타고 싶은 차량을 호출만 하면 운전사가 자동으로 차량을 제공해 걱정할 필요가 없습니다.  
비유적으로, Fargate는 AWS가 자동으로 적절한 리소스(차량)를 제공해 애플리케이션을 실행해주는 서비스입니다.  
이처럼 Fargate는 **인프라 관리 부담을 완전히 제거**해 사용자는 애플리케이션 로직에 집중할 수 있습니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 마이크로서비스 아키텍처에서의 빠른 배포 | Netflix는 Fargate를 사용해 수천 개의 마이크로서비스를 자동으로 배포 및 확장 |
| 시나리오 2 | 일시적인 작업 처리 (예: 배치 처리) | AWS Lambda와 Fargate를 결합해 백업 작업을 자동으로 실행 |
| 시나리오 3 | CI/CD 파이프라인에서의 빌드/테스트 | GitHub Actions에서 Fargate를 사용해 빌드 결과를 즉시 테스트 |

**이럴 때 Fargate를 선택하세요:**
- ✅ **사용자 수가 급격히 증가할 수 있는 서비스**
- ✅ **리소스 관리와 확장성에 고민이 많은 팀**
- ✅ **컨테이너 기반 애플리케이션을 빠르게 배포하고 싶은 경우**

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **장기 실행 작업이 많은 경우 → EKS 또는 EC2 기반 ECS**  
  이유: Fargate는 단기 작업에 최적화되어 장기 실행 시 비용이 증가할 수 있습니다.
- ❌ **고성능 GPU 리소스가 필요한 경우 → EC2 GPU 인스턴스**  
  이유: Fargate는 GPU 리소스를 지원하지 않아 EC2를 선택해야 합니다.
- ❌ **미니멀한 리소스 할당이 필요한 경우 → AWS Lambda**  
  이유: Fargate는 최소 0.25 vCPU 이상의 리소스를 요구하는 반면, Lambda는 128MB 부터 시작합니다.

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **ECS** | Fargate가 ECS의 태스크 실행을 관리하는 컨테이너 오케스트레이션 서비스 | Fargate → ECS 서비스 → EC2/On-premises |
| **ECR** | 이미지 저장소로 Fargate 태스크에 필요한 컨테이너 이미지를 제공 | ECR → Fargate 태스크 |
| **App Runner** | Fargate와 연동해 간단한 웹 애플리케이션을 빠르게 배포 | App Runner → Fargate 태스크 |

**자주 사용되는 아키텍처 패턴:**
```
[사용자] → [CloudFront] → [Fargate 태스크] → [ECR 이미지] → [ECS 클러스터] → [S3 저장소]
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **태스크 실행 시간** | $0.075/GB-SEC | 월 120시간 무료 |
| **ECS 클러스터 관리** | $0.006/클러스터-시간 | 월 10개 클러스터 무료 |
| **리소스 할당 비용** | $0.10/GB-SEC | 항상 무료 |

**비용 최적화 팁:**
1. 💡 **타スク 정의 최적화:** CPU/메모리 리소스를 실제 사용량에 맞게 설정해 과다 할당을 방지하세요.  
   예: "vCPU 0.25로 실행 가능한 애플리케이션은 태스크 정의에 0.25로 설정해야 합니다."
2. 💡 **스팟 인스턴스 활용:** 비용을 절감할 수 있는 스포트 인스턴스를 Fargate에서 사용 가능합니다.  
   예: "ECS 클러스터에 스포트 인스턴스를 연결해 비용을 70% 절감할 수 있습니다."
3. 💡 **태스크 기간 단축:** 작업이 완료되면 자동으로 태스크를 종료해 비용을 줄이세요.  
   예: "배치 작업 후 5분 이내에 태스크를 종료하는 정책을 설정할 수 있습니다."

> **⚠️ 비용 주의:** 트래픽 폭주 시 자동 확장으로 인해 예상치 못한 비용이 발생할 수 있습니다.  
  예: "1시간 내 1,000개 이상의 태스크가 생성되면 비용이 급격히 증가할 수 있습니다."

---

## ✅ 실습 체크리스트

1. **ECS 클러스터 생성:** AWS 콘솔에서 ECS 클러스터를 생성하고, Fargate를 선택하세요.  
2. **ECR 이미지 생성:** Docker 이미지를 ECR에 푸시해 Fargate 태스크에 사용할 수 있도록 합니다.  
3. **태스크 정의 작성:** CPU/메모리 리소스를 설정해 태스크 정의 파일을 생성합니다.  
4. **Fargate 태스크 배포:** AWS CLI 또는 콘솔에서 태스크를 실행하고 로그를 확인하세요.  
5. **비용 모니터링:** AWS Cost Explorer를 사용해 Fargate 사용량을 추적하고 최적화 전략을 수립합니다.

## 📚 핵심 개념

### 개념 1: ECS 클러스터 구성 및 관리  
ECS 클러스터는 AWS에서 컨테이너 애플리케이션을 실행하는 가상 환경입니다. 클러스터는 **타스크**(Task)와 **서비스**(Service)로 구성되며, 태스크는 특정 컨테이너 이미지와 설정을 포함한 작업 단위입니다. 서비스는 태스크의 실행을 관리하고, 태스크가 실패할 경우 자동으로 복구하는 역할을 합니다. ECS 클러스터는 컨테이너 오케스트레이션의 핵심으로, CPU/메모리 리소스를 효율적으로 관리하며, AWS Fargate와 함께 사용될 수 있습니다.  

#### 왜 중요한가?  
- **자원 효율성**: 클러스터를 통해 여러 컨테이너를 단일 환경에서 관리해 리소스를 최적화합니다.  
- **확장성**: 서비스는 태스크 수를 자동으로 조절해 트래픽 증가 시 애플리케이션의 가용성을 보장합니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |  
|-----|-----|-----|  
| 클러스터 | 컨테이너 실행을 위한 가상 환경 | `my-cluster`라는 이름의 클러스터 생성 |  
| 태스크 | 특정 컨테이너 이미지와 설정을 포함한 작업 단위 | `web-app` 태스크 실행 |  
| 서비스 | 태스크의 실행을 관리하고 복구 기능 제공 | `web-service`가 태스크 실패 시 자동 복구 |  

> **💡 Tip:** ECS 클러스터는 Fargate와 함께 사용 시, 서버 관리 부담을 줄이고 클라우드 네이티브 애플리케이션 개발에 적합합니다.  

---

### 개념 2: EKS 컨트롤 플레인 및 노드 그룹  
EKS(Amazon Elastic Kubernetes Service)는 Kubernetes 클러스터를 AWS에서 실행할 수 있도록 제공하는 서비스입니다. **컨트롤 플레인**은 클러스터의 관리 기능을 담당하며, **노드 그룹**은 실제 컨테이너 작업을 실행하는 머신입니다. EKS는 Fargate와 차별화되며, Fargate는 Kubernetes가 아닌 AWS 관리형 컨테이너 실행 환경입니다.  

#### 작동 원리  
1. **클러스터 생성**: EKS 콘솔에서 클러스터를 생성하고, 컨트롤 플레인을 설정합니다.  
2. **노드 그룹 구성**: EC2 인스턴스나 Fargate를 사용해 노드 그룹을 구성합니다.  
3. **워크로드 배포**: Kubernetes의 `kubectl` 명령어로 애플리케이션을 배포하고, Fargate는 Kubernetes가 아닌 AWS 관리 환경에서 실행됩니다.  

> **💡 Tip:** EKS는 Kubernetes 기반 애플리케이션을 위한 선택지이며, Fargate는 서버리스 컨테이너 실행에 적합합니다.  

---

### 개념 3: Fargate 자동 확장 및 리소스 관리  
Fargate는 AWS가 리소스를 자동으로 관리하는 **서버리스 컨테이너 실행 환경**입니다. 사용자는 CPU/메모리 요구사항을 정의하고, Fargate는 필요 시 자동으로 스케일링하여 애플리케이션의 가용성을 보장합니다. 이는 전통적인 ECS 클러스터에 비해 관리 부담을 줄입니다.  

#### 주요 특징  
1. **자동 스케일링**: 트래픽 증가 시 Fargate가 자동으로 추가 리소스를 할당합니다.  
2. **리소스 최적화**: 사용자가 정의한 CPU/메모리 제한을 기준으로 자원을 효율적으로 사용합니다.  
3. **비용 효율성**: 서버 관리 없이도 리소스를 유연하게 조절해 비용을 절감합니다.  

> **💡 Tip:** Fargate는 예측 불가능한 트래픽이나 서버리스 애플리케이션에 적합하며, 프리티어로 25시간까지 무료로 사용할 수 있습니다.

## 🖥️ AWS 콘솔에서 Fargate 사용하기

### Step 1: Fargate 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - **💡 Tip:** AWS 계정이 없는 경우, [AWS 계정 생성](https://aws.amazon.com/zh_cn/sign-up/) 링크를 클릭하여 등록합니다.  
2. 상단 검색창에서 **"Fargate"**를 입력하고, 검색 결과에서 **"Fargate"**를 클릭합니다.  
3. **"Fargate"** 메뉴에서 **"Services"** > **"ECS"** > **"Clusters"**로 이동합니다.  

> **📸 화면 확인:** Fargate 대시보드가 표시되면 정상입니다. 클러스터 목록이 표시되며, "Create cluster" 버튼이 보일 수 있습니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
#### 1. ECS 클러스터 생성  
1. **"Create cluster"** 버튼을 클릭합니다.  
2. **"Cluster name"**을 입력하고, **"Cluster type"**에서 **"Fargate"**를 선택합니다.  
   - **💡 Tip:** Fargate는 EC2 인스턴스 없이 컨테이너를 실행할 수 있는 자동화된 클러스터입니다.  
3. **"VPC"**를 선택하고, **"Task role"** 및 **"Service role"**을 설정합니다.  
   - **⚠️ 주의:** IAM 역할은 컨테이너가 AWS 서비스에 접근할 수 있도록 설정해야 합니다.  

#### 2. Fargate 태스크 정의 생성  
1. **"Create task definition"**을 클릭합니다.  
2. **"Container"** 섹션에서 **"Image"**를 ECR(예: `123456789012.dkr.ecr.region.amazonaws.com/my-app:latest`)로 설정합니다.  
3. **"Port mappings"**에서 포트를 지정하고, **"Environment variables"**를 추가합니다.  
   - **💡 Tip:** ECR 이미지를 사용하려면 먼저 `aws ecr get-login-password` 명령어로 로그인해야 합니다.  

#### 3. 서비스 배포  
1. **"Create service"** 버튼을 클릭하고, **"Cluster"** 및 **"Task definition"**을 선택합니다.  
2. **"Launch type"**에서 **"Fargate"**를 선택하고, **"Task count"**를 1로 설정합니다.  
3. **"Network settings"**에서 VPC 및 보안 그룹을 선택합니다.  

> **📸 화면 확인:** 클러스터, 태스크 정의, 서비스가 생성되면 각 항목의 상태가 "ACTIVE"로 표시됩니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
#### 1. ECR 이미지 연결  
1. **"Elastic Container Registry (ECR)"** 메뉴에서 **"Repositories"**로 이동합니다.  
2. **"Create repository"**를 클릭하고, 이미지 이름을 입력합니다 (예: `my-app`).  
3. **"Build"** 버튼을 클릭하여 Dockerfile로 이미지를 빌드하고 ECR에 푸시합니다.  

#### 2. 네트워크 및 보안 설정  
1. **"VPC"** 메뉴에서 클러스터와 연결된 VPC를 확인합니다.  
2. **"Security Groups"**에서 서비스에 접근할 수 있는 IP 범위를 설정합니다.  
   - **⚠️ 주의:** 잘못 설정된 보안 그룹은 서비스가 작동하지 않을 수 있습니다.  

#### 3. 로깅 및 모니터링  
1. **"CloudWatch"** 메뉴에서 **"Logs"**를 선택하고, Fargate 태스크의 로그를 확인합니다.  
2. **"CloudWatch Metrics"**에서 CPU 및 메모리 사용량을 모니터링합니다.  

> **⚠️ 주의:** Fargate는 자동으로 리소스를 할당하므로, 명시적인 CPU/메모리 제한은 필요하지 않습니다.  

---

### Step 4: 설정 확인 및 테스트  
#### 1. 생성된 리소스 확인  
1. **"ECS"** > **"Clusters"**로 이동하여 클러스터 상태를 확인합니다.  
2. **"Services"**에서 서비스 이름을 클릭하고, **"Tasks"**를 확인하여 태스크가 실행 중인지 확인합니다.  

#### 2. 상태 확인  
1. **"CloudWatch"** > **"Metrics"**에서 Fargate 태스크의 상태를 모니터링합니다.  
2. **"ECS"** > **"Tasks"**에서 태스크 상태가 "RUNNING"인지 확인합니다.  

#### 3. 정상 동작 테스트  
1. **"ECS"** > **"Services"**에서 서비스의 **"Load balancer"**를 클릭하여 외부 IP를 확인합니다.  
2. 브라우저에서 IP 주소를 입력하여 애플리케이션이 정상적으로 실행되는지 테스트합니다.  

> **📸 화면 확인:** 로드 밸런서의 DNS 이름을 클릭하면 애플리케이션이 열리며, 상태가 "200 OK"로 표시되면 성공입니다.  

---

### 📋 체크리스트: Fargate 배포 확인  
- [ ] ECS 클러스터 생성 완료  
- [ ] ECR 이미지 생성 및 푸시 완료  
- [ ] Fargate 태스크 정의 및 서비스 배포 완료  
- [ ] 로그 및 메트릭 모니터링 설정 완료  
- [ ] 외부 접근 테스트 완료  

> **💡 Tip:** Fargate는 EC2 인스턴스 없이 컨테이너를 실행하므로, 네트워크 설정과 ECR 연동을 주의 깊게 확인하세요.  
> **⚠️ 주의:** Fargate는 사용량에 따라 비용이 발생하므로, 프리티어(3500달러)를 활용하여 실습을 진행하세요.

## ⌨️ AWS CLI로 Fargate 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

> **💡 Tip:** AWS CLI를 사용하기 전에 [AWS CLI 설치 가이드](https://docs.aws.amazon.com/cli/latest/userguide/cli-install.html)를 참고하여 설치 및 구성하세요.  
> **⚠️ 주의:** CLI 명령어는 리전 및 IAM 권한에 따라 실행 결과가 다를 수 있습니다.

---

### 예제 1: Fargate 리소스 조회
```bash
# ECS 클러스터 목록 조회
aws ecs list-clusters --query 'clusterArns' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | JSON 결과 필터링 | `'clusterArns'` |
| `--output` | 출력 형식 | `json`, `table`, `text` |

**예상 출력:**
```
clusterArns
------------------
arn:aws:ecs:us-east-1:123456789012:cluster/my-cluster
```

> **💡 Tip:** `list-tasks` 명령어로 Fargate 태스크 목록을 확인할 수 있습니다.  
> **예시:** `aws ecs list-tasks --cluster my-cluster --output table`

---

### 예제 2: Fargate 리소스 생성
```bash
# ECS 클러스터 생성
aws ecs create-cluster \
    --cluster-name my-fargate-cluster \
    --service-role-arn arn:aws:iam::123456789012:role/ecsTaskExecutionRole
```

**필수 옵션:**
- `--cluster-name`: 클러스터 이름 (Fargate 실행 환경)
- `--service-role-arn`: ECS 서비스 역할 ARN (IAM 역할 필요)

**예상 출력:**
```json
{
    "cluster": {
        "clusterArn": "arn:aws:ecs:us-east-1:123456789012:cluster/my-fargate-cluster",
        "status": "ACTIVE",
        "registeredContainerInstancesCount": 0,
        "runningTasksCount": 0,
        "pendingTasksCount": 0
    }
}
```

> **💡 Tip:** Fargate 클러스터 생성 시 `--cluster-type` 옵션으로 `FARGATE`를 명시할 수 있습니다.

---

### 예제 3: Fargate 리소스 수정
```bash
# ECS 서비스 수정 (Fargate 태스크 실행 환경 설정)
aws ecs update-service \
    --cluster my-fargate-cluster \
    --service my-fargate-service \
    --task-definition my-task-definition
```

**필수 옵션:**
- `--cluster`: Fargate 클러스터 이름
- `--service`: 서비스 이름 (Fargate 태스크 관리)
- `--task-definition`: 태스크 정의 파일명 (ECR 이미지 포함)

**예상 출력:**
```json
{
    "service": {
        "serviceArn": "arn:aws:ecs:us-east-1:123456789012:service/my-fargate-cluster/my-fargate-service",
        "status": "ACTIVE"
    }
}
```

> **💡 Tip:** `update-task-definition` 명령어로 태스크 정의를 수정할 수 있습니다.

---

### 예제 4: Fargate 리소스 삭제
```bash
# ECS 클러스터 삭제
aws ecs delete-cluster --cluster my-fargate-cluster

# 삭제 확인
aws ecs describe-clusters --clusters my-fargate-cluster
```

> **⚠️ 주의:** 클러스터 삭제 시 모든 태스크 및 서비스도 함께 삭제됩니다.  
> **💡 Tip:** `--force` 옵션을 사용해 강제 삭제할 수 있습니다.

---

### 자주 사용하는 명령어 정리
```bash
# 클러스터 관리
aws ecs create-cluster --cluster-name my-cluster
aws ecs describe-clusters --clusters my-cluster

# 태스크 정의 관리
aws ecs register-task-definition --cli-input-json file://task-definition.json
aws ecs describe-task-definition --task-definition my-task

# 서비스 관리 (Fargate)
aws ecs create-service --cluster my-cluster --service-name my-service --task-definition my-task
aws ecs update-service --cluster my-cluster --service my-service --task-definition my-task
aws ecs delete-service --cluster my-cluster --service my-service
```

> **💡 Tip:** `--launch-type FARGATE`를 명시해 Fargate 태스크를 실행하세요.  
> **⚠️ 주의:** Fargate 사용 시 `ECS_FARGATE` 리소스 타입을 반드시 설정해야 합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Fargate 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **Fargate의 서버리스 특성 및 자동 리소스 관리**  
   - 설명: Fargate는 사용자가 EC2 인스턴스를 직접 관리할 필요가 없고, AWS가 자동으로 CPU/메모리 리소스를 할당/확장해 주는 점이 시험에서 주요 포인트입니다. 이는 클라우드 기반 컨테이너 오케스트레이션의 핵심 이점으로, **Serverless**, **Auto Scaling**, **Resource Optimization** 키워드와 연결됩니다.  
   - 키워드: `Serverless`, `Auto Scaling`, `Resource Optimization`

2. **Fargate와 ECS의 통합 및 서비스 배포 방식**  
   - 설명: Fargate는 ECS 클러스터에 속하지만, EC2 기반 노드가 필요하지 않아 "ECS Anywhere"와 혼동할 수 있습니다. 시험에서는 Fargate 서비스를 생성할 때 ECS 클러스터와의 연동 방식, 서비스 정의 파일 작성 방법 등이 주요 시험 항목입니다.  
   - 키워드: `ECS Integration`, `Service Definition`, `ECS Anywhere`

3. **Fargate의 제한사항 및 사용 시나리오**  
   - 설명: Fargate는 고성능 워크로드나 특정 네트워크 설정이 필요한 경우 사용할 수 없습니다. 예를 들어, **GPU 지원**, **자체 라우팅**, **고성능 스토리지**가 필요한 경우 EC2 기반 컨테이너를 사용해야 합니다. 이는 Fargate의 한계를 묻는 시험 문제의 핵심입니다.  
   - 키워드: `Limitations`, `Use Cases`, `EC2 vs Fargate`

4. **Fargate의 비용 구조 및 프리티어 활용**  
   - 설명: Fargate는 **pay-as-you-go** 모델로, 실제 실행 시간에 따라 요금이 청구됩니다. 프리티어는 1년간 $250의 리소스로 Fargate 테스트가 가능하며, 시험에서는 비용 효율성과 프리티어 활용법이 주요 시험 항목입니다.  
   - 키워드: `Cost Model`, `Free Tier`, `Pay-as-you-go`

5. **Fargate vs EKS/ECS의 차이점**  
   - 설명: EKS는 Kubernetes 기반으로 사용자가 컨트롤 플레인을 관리해야 하며, ECS는 EC2 기반 노드가 필요합니다. Fargate는 이들의 복잡성을 줄여주는 서버리스 솔루션이므로, **Orchestration**, **Infrastructure Management**, **Serverless** 키워드가 시험에서 자주 등장합니다.  
   - 키워드: `Orchestration`, `Infrastructure Management`, `Serverless`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | Fargate가 EC2 기반 노드를 필요하다고 오해 | Fargate는 EC2 노드 없이도 작동 가능 (ECS 클러스터에만 연동) |
| 함정 2 | Fargate의 비용이 EC2보다 항상 낮다고 생각 | Fargate는 실행 시간에 따라 요금이 청구되며, CPU/메모리 사용량에 따라 비용이 변동 |
| 함정 3 | Fargate가 ECS Anywhere와 동일하다고 오해 | ECS Anywhere는 EC2 기반 하이브리드 배포, Fargate는 서버리스로 완전히 관리됨 |

#### 🔄 Fargate vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | Fargate | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 서버리스 컨테이너 실행 (EC2 없이) | ECS (EC2 기반), EKS (Kubernetes 기반) | **간단한 워크로드**, **리소스 관리 부담 없을 때** |
| 확장성 | 자동 확장, 탄력적 리소스 할당 | ECS (수동/자동 확장), EKS (Kubernetes 스케줄러) | **예측 불가능한 트래픽**, **리소스 최적화 필요 시** |
| 비용 | 실행 시간 기반, 프리티어 활용 가능 | ECS (인스턴스 비용), EKS (컨트롤 플레인 + 노드) | **비용 효율성**, **단기 테스트 시** |
| 지연시간 | AWS가 리소스 최적화로 빠름 | ECS (EC2 기반, 설정에 따라), EKS (Kubernetes 오버헤드) | **저지연 서비스**, **빠른 배포 필요 시** |

#### 📝 시험 대비 체크리스트
- [ ] Fargate의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  *예: "Fargate는 사용자가 EC2 인스턴스를 직접 관리하지 않고, AWS가 자동으로 리소스를 할당하여 컨테이너를 실행하는 서버리스 서비스입니다."*
- [ ] Fargate를 선택해야 하는 시나리오를 알고 있는가?  
  *예: "예측 불가능한 트래픽, 리소스 관리 부담이 적은 경우, 단기 테스트 시"*
- [ ] Fargate의 제한사항/한계를 알고 있는가?  
  *예: "GPU 지원, 고성능 스토리지, 복잡한 네트워크 설정이 필요한 경우 EC2를 사용해야 합니다."*
- [ ] Farg,ate와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  *예: "ECS는 EC2 기반, EKS는 Kubernetes 기반, Fargate는 서버리스로 EC2 없이 작동합니다."*
- [ ] Fargate의 비용 구조를 이해하고 있는가?  
  *예: "Fargate는 실행 시간 기반, 프리티어로 1년간 $250의 리소스를 제공합니다."*

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Fargate를 떠올리세요:  
> - **Serverless** (서버 관리 필요 없음)  
> - **Auto Scaling** (자동 리소스 확장)  
> - **Resource Optimization** (리소스 자동 최적화)  
> - **No Infrastructure Management** (인프라 직접 관리 필요 없음)  
> - **Cost Efficiency** (비용 효율성)

---

| [⬅️ EKS](./EKS.md) | [📑 Day 1 목차](./README.md) | [🏠 Week 4](../README.md) | [ECR ➡️](./ECR.md) |

---
