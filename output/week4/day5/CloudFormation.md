---

| [⬅️ 이전 Day](../day4/README.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 4](../README.md) | [CDK ➡️](./CDK.md) |

---

# CloudFormation 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** CloudFormation은 인프라스트럭처를 코드로 관리하기 위한 AWS의 자동화 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 매뉴얼로 AWS 리소스를 구성할 때 인스턴스 생성 시 오타나 설정 누락으로 인한 오류가 발생합니다. 예: EC2 인스턴스 생성 시 보안 그룹 설정 누락으로 인한 외부 접근 차단 문제
- **문제 2:** 개발/생산 환경에서 일관된 인프라 구성이 어려워 테스트 시 환경 불일치로 버그 발생 가능성이 높습니다. 예: 개발 환경에서 사용하지 않는 보안 그룹이 생산 환경에 자동 생성되는 문제
- **문제 3:** 스케일링 시 수동으로 리소스 추가해야 하며, 리소스 간 의존성 관리가 복잡합니다. 예: 로드 밸런서 추가 시 백엔드 서버 인스턴스가 동기화되지 않는 문제

**CloudFormation으로 해결:**
- **해결 1:** YAML/JSON 템플릿으로 리소스를 코드로 정의하여 자동 생성합니다. 예: `EC2.Instance` 리소스 생성 시 `SecurityGroup` 설정이 자동 포함됩니다.
- **해결 2:** 템플릿을 통해 모든 환경에서 동일한 인프라 구성이 가능합니다. 예: `VPC` 템플릿을 개발/생산 환경 모두에 적용하여 환경 불일치 방지
- **해결 3:** 단일 명령어로 스케일링 리소스를 자동 생성하고 의존성 관리를 통합합니다. 예: `AutoScalingGroup` 생성 시 로드 밸런서와의 연결이 자동 설정됩니다.

### 비유로 이해하기
CloudFormation은 건축가의 설계도처럼 모든 AWS 리소스를 코드로 정의해 일관된 인프라를 구축합니다. 예를 들어, 주택을 짓기 위해 지붕, 벽, 창문 등 모든 구조물을 설계도로 정리하면, CloudFormation은 이를 코드로 변환해 자동으로 집을 짓는 듯한 역할을 합니다. 이처럼 복잡한 AWS 환경에서도 코드로 정의된 템플릿을 통해 오류 없는 인프라 구축이 가능합니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 신규 애플리케이션 배포 시 일관된 인프라 구축 | AWS Marketplace에서 다운로드한 템플릿으로 EC2, RDS, Load Balancer 자동 배포 |
| 시나리오 2 | 멀티-테이터 기반 아키텍처 관리 | Nested Stack을 사용해 DB 서버, 웹 서버, 캐시 서버를 별도 템플릿으로 관리 |
| 시나리오 3 | CI/CD 파이프라인 자동화 | CodePipeline과 연동해 테스트 완료 시 자동으로 배포 환경 구성 |

**이럴 때 CloudFormation를 선택하세요:**
- ✅ **동일한 인프라 구성이 필요한 환경** (개발, 테스트, 프로덕션)
- ✅ **복잡한 리소스 의존성이 있는 시나리오**
- ✅ **자동화된 배포 및 회복이 필요한 시스템**

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **동적 리소스 생성이 필요한 경우** → **EC2 Auto Scaling** (동적 인스턴스 생성)
- ❌ **기존 리소스를 관리해야 하는 경우** → **AWS Management Console** (수동 리소스 관리)

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **S3** | 템플릿 저장소로 사용 | `CloudFormation → S3` (템플릿 파일 저장) |
| **Systems Manager** | 자동화된 구성 관리 | `CloudFormation → Systems Manager State Manager` (리소스 상태 유지) |
| **CDK** | 타 언어로 템플릿 작성 | `CDK (TypeScript/Python) → CloudFormation` (템플릿 생성) |

**자주 사용되는 아키텍처 패턴:**
```
[사용자] → [CodePipeline] → [CloudFormation] → [S3] → [EC2, RDS, Load Balancer]
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **리소스 생성** | $0.005/요청 | 월 10,000 무료 |
| **스택 업데이트** | $0.003/요청 | 12개월 10,000 무료 |
| **중첩 스택** | $0.001/요청 | 항상 무료 |

**비용 최적화 팁:**
1. 💡 **템플릿 최적화:** 중복 리소스 제거 및 최소한의 리소스만 포함해 템플릿 파일 크기 줄이기
2. 💡 **Reserved Instances 활용:** 장기 사용 리소스에 Reserved Instances 적용해 60% 이상 비용 절감
3. 💡 **태깅 정책 강제:** `Cost Center` 태깅으로 비용 분배 관리 및 리소스 사용자 식별

> **⚠️ 비용 주의:** 중첩 스택 사용 시 스택 수가 많아질 경우 비용이 증가할 수 있으며, `S3`에 템플릿 저장 시 S3 요금이 별도로 발생합니다. 프리티어 기간 내 사용 후 다른 서비스의 요금이 발생할 수 있으니 주의해야 합니다.

## 📚 핵심 개념

### 개념 1: 중첩 스택(Nested Stacks in CloudFormation)
CloudFormation은 복잡한 인프라를 관리하기 위해 중첩 스택을 지원합니다. 중첩 스택은 하나의 템플릿 안에서 다른 템플릿(자식 스택)을 포함하여 구성할 수 있는 기능으로, 대규모 시스템을 모듈화하고 재사용 가능하게 만듭니다. 이는 각 스택이 독립적으로 관리되고, 변경 사항이 영향을 주지 않도록 보장합니다. 또한, 중첩 스택은 템플릿의 복잡성을 줄이고, 팀 간 협업을 용이하게 합니다.  
#### 왜 중요한가?  
- **모듈화**: 복잡한 시스템을 작은 단위로 나누어 관리 가능  
- **재사용성**: 공통된 구성 요소를 다른 스택에서 재사용 가능  
- **업데이트 안정성**: 자식 스택의 변경이 부모 스택에 영향을 주지 않음  

#### 세부 요소  
| 요소 | 설명 | 예시 |
|-----|-|-|
| **부모 스택** | 중첩 스택을 포함하는 주 스택 | VPC 생성 후 EC2 인스턴스 배포를 담당하는 스택 |
| **자식 스택** | 부모 스택에서 참조되는 별도의 템플릿 | RDS 인스턴스 생성을 담당하는 스택 |
| **파라미터** | 부모 스택에서 자식 스택으로 전달되는 입력 값 | VPC ID를 자식 스택에 전달하여 RDS 인스턴스 생성 |
| **출력 값** | 자식 스택이 부모 스택으로 전달하는 결과 값 | 자식 스택의 RDS 엔드포인트를 부모 스택에서 사용 |

> **💡 Tip:** 중첩 스택은 대규모 인프라 구축 시 필수적입니다. 자식 스택은 별도의 템플릿 파일로 관리하고, 부모 스택에서 `AWS::CloudFormation::Stack` 리소스로 참조하세요.  

---

### 개념 2: AWS CDK Constructs and Declarative Infrastructure  
AWS CDK(Cloud Development Kit)는 TypeScript, Python, Java 등 프로그래밍 언어를 사용해 인프라를 정의할 수 있도록 하는 도구입니다. CDK는 코드로 인프라를 작성한 후, 이를 CloudFormation 템플릿으로 변환해 AWS에 배포합니다. 이는 기존 CloudFormation의 YAML/JSON 기반 템플릿 대신 코드를 사용해 인프라를 선언적으로 정의할 수 있는 장점을 제공합니다.  
#### 작동 원리  
1. **코드 작성**: 개발자는 프로그래밍 언어로 리소스(예: VPC, EC2)를 정의합니다.  
2. **템플릿 생성**: CDK는 코드를 CloudFormation 템플릿으로 변환합니다.  
3. **배포**: 생성된 템플릿을 AWS CLI 또는 AWS Console을 통해 배포합니다.  

> **💡 Tip:** CDK는 복잡한 인프라 구조나 동적 구성 요소를 처리할 때 유리합니다. 예를 들어, 동적 파라미터나 조건문이 필요한 경우 코드 기반 접근법이 더 유연합니다.  

---

### 개념 3: Systems Manager State Manager  
Systems Manager State Manager는 AWS 자원의 상태를 관리하고 일관성을 유지하는 기능입니다. 이는 EC2 인스턴스에 특정 소프트웨어를 설치하거나, 보안 패치를 적용하는 등 자동화된 구성 관리를 가능하게 합니다. State Manager는 원하는 상태를 정의하고, 실제 상태와 비교해 필요한 조치를 자동으로 수행합니다.  
#### 주요 특징  
1. **자동화된 구성 관리**: EC2 인스턴스에 필요한 소프트웨어 설치 및 설정을 자동화  
2. **보안 준수**: 정책 기반으로 보안 패치 적용 및 암호화 설정을 강제  
3. **모니터링**: 상태가 변경되었을 때 알림을 제공하고, 이력을 추적 가능  

> **💡 Tip:** State Manager는 클라우드 환경에서 일관된 상태를 유지하고, 인프라 보안을 강화하는 데 필수적입니다. 예를 들어, EC2 인스턴스의 특정 포트가 열려 있는 경우 자동으로 닫도록 설정할 수 있습니다.  

---

### 개념 4: Well-Architected 6 대 원칙  
AWS Well-Architected Framework는 시스템을 설계할 때 고려해야 할 6개의 핵심 원칙을 제공합니다. 이는 운영 효율성, 보안, 비용 최적화, 성능, 신뢰성, 지속 가능한 성능을 포함합니다. CloudFormation은 이 원칙을 구현하는 데 도움을 줍니다. 예를 들어, 자동화된 배포(운영 효율성), 보안 정책 적용(보안), 리소스 최적화(비용 최적화) 등을 통해 시스템을 안정적으로 운영할 수 있습니다.  
#### 주요 원칙  
1. **운영 효율성**: 자동화와 모니터링을 통해 운영을 효율화  
2. **보안**: 정책 기반 접근 제어와 보안 패치 적용  
3. **비용 최적화**: 리소스 사용을 최소화하고, 비용을 통제  
4. **성능**: 고가용성 및 확장성을 고려한 설계  
5. **신뢰성**: 장애 발생 시 복구를 위한 전략 수립  
6. **지속 가능성**: 에너지 효율성과 환경 영향을 최소화  

> **⚠️ 주의:** CloudFormation 템플릿은 Well-Architected 원칙을 준수하는 데 도움이 되지만, 추가적인 보안 및 모니터링 도구와 결합해 사용해야 합니다.  

---

### 개념 5: 비용 최적화 - Reserved Instances  
Reserved Instances(RI)는 AWS에서 특정 기간 동안 리소스(예: EC2 인스턴스)를 예약해 사용할 때 할인 혜택을 제공하는 서비스입니다. 이는 장기적인 리소스 사용을 고려할 때 비용을 최대 75% 절감할 수 있습니다. CloudFormation은 RI를 자동화해 배포할 수 있으며, 이는 비용 관리에 중요한 역할을 합니다.  
#### 주요 이점  
1. **비용 절감**: 연간 사용량을 고려해 예약할 경우, 단일 인스턴스 가격의 75% 할인  
2. **예측 가능**: 사용량을 미리 계획해 예산 관리 가능  
3. **스케일링**: 필요한 시기에만 리소스를 확보해 비용 효율성 유지  

> **💡 Tip:** Reserved Instances는 장기적인 리소스 사용 계획이 필요한 경우 필수입니다. 예를 들어, 월 100시간 사용하는 EC2 인스턴스를 1년간 예약하면, 월간 비용을 75% 절감할 수 있습니다.

## 🖥️ AWS 콘솔에서 CloudFormation 사용하기

### Step 1: CloudFormation 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - AWS 계정이 활성화된 상태에서 로그인해야 합니다.  
2. 상단 검색창에서 **"CloudFormation"**을 입력하고, 검색 결과에서 **"CloudFormation"** 서비스를 클릭합니다.  
   - **검색 결과**에서 서비스 이름이 정확히 표시되도록 주의해야 합니다.  
3. **CloudFormation 대시보드**가 열리면 정상적으로 접속한 것입니다.  

> **📸 화면 확인:**  
> - 상단에 **"CloudFormation"** 타이틀이 표시됩니다.  
> - 좌측 메뉴에서 **"Stacks"** 또는 **"Templates"** 탭이 보여야 합니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **"Create stack"** 버튼 클릭  
   - 메인 화면 우측 상단의 **"Create stack"** 버튼을 클릭합니다.  
   - 이 버튼을 누르면 **"Create stack"** 페이지가 열립니다.  
2. **템플릿 선택**  
   - **"Choose a template"** 섹션에서 다음 중 하나를 선택합니다:  
     - **"Upload a template file"**: 로컬 파일에서 템플릿을 업로드합니다.  
     - **"Specify template"**: 직접 YAML/JSON 형식의 템플릿을 입력합니다.  
     - **"URL"**: 외부 URL에서 템플릿을 가져옵니다.  
   - 예시: EC2 인스턴스 생성을 위한 기본 템플릿을 업로드합니다.  
3. **파라미터 입력**  
   - 템플릿에 필요한 파라미터(예: 인스턴스 이름, 스토리지 크기)를 입력합니다.  
   - 파라미터는 템플릿의 `Parameters` 섹션에 정의된 값이어야 합니다.  

> **📸 화면 확인:**  
> - **"Create stack"** 페이지에서 템플릿 선택과 파라미터 입력이 완료된 상태입니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **리소스 타입 및 속성 설정**  
   - 템플릿에서 정의된 리소스(예: EC2, RDS)의 속성을 확인하고, 필요 시 수정합니다.  
   - 예: EC2 인스턴스의 `InstanceType`을 `t2.micro`로 설정합니다.  
2. **IAM 역할 및 정책 설정**  
   - **"Create or update IAM role"** 옵션을 선택하여 CloudFormation이 리소스를 관리할 수 있는 권한을 부여합니다.  
   - 권한은 **"AWSCloudFormationReadOnlyAccess"** 또는 **"AWSCloudFormationFullAccess"**를 선택할 수 있습니다.  
3. **알림 및 정책 구성**  
   - **"Notifications"** 섹션에서 스택 상태 변경 시 알림을 설정합니다.  
   - **"Options"**에서 스택 생성 시 자동으로 생성되는 리소스를 관리할 수 있습니다.  

> **⚠️ 주의:**  
> - 파라미터와 리소스 속성의 값은 템플릿에 정의된 형식과 일치해야 합니다.  
> - IAM 역할 권한은 비용 및 보안에 따라 적절히 설정해야 합니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인**  
   - **"Stacks"** 탭으로 이동해 스택 상태를 확인합니다.  
   - 상태가 `CREATE_COMPLETE`로 표시되면 리소스가 성공적으로 생성되었습니다.  
2. **리소스 상태 및 로그 확인**  
   - 스택 상세 페이지에서 **"Events"** 탭을 통해 생성/삭제 과정의 로그를 확인합니다.  
   - 오류 발생 시 **"Outputs"** 탭에서 출력값을 확인해 문제를 진단합니다.  
3. **정상 동작 테스트**  
   - 생성된 EC2 인스턴스의 IP 주소를 확인하고 SSH 연결을 시도합니다.  
   - **Systems Manager**를 사용해 인스턴스 상태를 모니터링합니다.  

> **💡 Tip:**  
> - 비용 최적화를 위해 **"Cost Estimator"** 도구를 사용해 스택 비용을 예측합니다.  
> - DR 전략으로 **"Nested Stacks"**을 사용해 복잡한 환경을 분할 관리합니다.  

---

### 📊 CloudFormation 서비스 비교표  
| 기능 | CloudFormation | AWS CDK | Systems Manager |  
|------|----------------|---------|------------------|  
| 템플릿 형식 | YAML/JSON | TypeScript/Python | CLI/Agent |  
| 리소스 관리 | 자동화 | 코드로 정의 | 구성 관리 |  
| 비용 최적화 | 템플릿 최적화 | 코드 최적화 | 자동화 |  
| DR 전략 | Nested Stacks | CDK StackSets | Automation |  

> **📌 참고:**  
> - **Free Tier**는 7일간 100만 개의 리소스 생성이 가능합니다.  
> - **Stack Policy**를 설정해 리소스 변경을 제어할 수 있습니다.

## ⌨️ AWS CLI로 CloudFormation 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**설명:**  
CloudFormation CLI 명령어는 AWS CLI 버전 1.18.0 이상에서 실행 가능합니다. 먼저 CLI 버전을 확인하고, IAM 사용자 권한이 유효한지 확인하세요. 리전 설정은 리소스 생성 위치에 따라 중요합니다.

---

### 예제 1: CloudFormation 스택 목록 조회
```bash
# 모든 스택 목록 조회
aws cloudformation list-stacks --stack-status-filter CREATE_COMPLETE DELETE_COMPLETE --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--stack-status-filter` | 스택 상태 필터링 | `CREATE_COMPLETE`, `DELETE_COMPLETE` |
| `--output` | 출력 형식 | `json`, `table`, `text` |

**예상 출력:**
```
|  StackName    | StackId                             | 
|---------------|-------------------------------------|
| MyStack       | arn:aws:cloudformation:us-east-1:123456789012:stack/MyStack/abcd1234 |
```

**팁:** `--output table`은 명확한 테이블 형식으로 결과를 보여줍니다. `--stack-status-filter`로 특정 상태의 스택만 필터링할 수 있습니다.

---

### 예제 2: CloudFormation 스택 생성
```bash
# 스택 생성 (템플릿 파일 사용)
aws cloudformation create-stack \
    --stack-name "MyStack" \
    --template-body file://template.yaml \
    --parameters ParameterKey=InstanceType,ParameterValue=t2.micro \
    --capabilities CAPABILITY_IAM
```

**필수 옵션:**
- `--stack-name`: 스택 이름 (중복되지 않도록 설정)
- `--template-body`: YAML/JSON 템플릿 파일 경로 또는 URL
- `--parameters`: 파라미터 키-값 쌍 (IAM 역할 생성 시 `CAPABILITY_IAM` 필요)

**예상 출력:**
```json
{
    "StackId": "arn:aws:cloudformation:us-east-1:123456789012:stack/MyStack/abcd1234",
    "Status": "CREATE_IN_PROGRESS"
}
```

**경고:** `--capabilities`는 IAM 역할 생성 시 필수입니다. `CAPABILITY_IAM`을 생략하면 오류 발생.

---

### 예제 3: CloudFormation 스택 수정
```bash
# 스택 수정 (파라미터 변경)
aws cloudformation update-stack \
    --stack-name "MyStack" \
    --template-body file://updated-template.yaml \
    --parameters ParameterKey=InstanceType,ParameterValue=t3.small
```

**주의사항:**  
- 수정 시 기존 리소스는 자동으로 재구성됩니다.  
- `--template-body`로 새로운 템플릿을 제공해야 합니다.  
- 파라미터 변경은 `--parameters`로 전달해야 합니다.

---

### 예제 4: CloudFormation 스택 삭제
```bash
# 스택 삭제
aws cloudformation delete-stack --stack-name "MyStack"

# 삭제 상태 확인
aws cloudformation describe-stacks --stack-name "MyStack"
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. `--stack-name`을 정확히 입력하세요.  
> 삭제 후 `describe-stacks`는 `DELETE_IN_PROGRESS` 또는 `DELETE_COMPLETE` 상태를 반환합니다.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws cloudformation list-stacks --output table
aws cloudformation describe-stacks --stack-name "MyStack"

# 생성
aws cloudformation create-stack --stack-name "MyStack" --template-body file://template.yaml

# 수정
aws cloudformation update-stack --stack-name "MyStack" --template-body file://updated-template.yaml

# 삭제
aws cloudformation delete-stack --stack-name "MyStack"
```

**추가 팁:**  
- 템플릿 파일은 YAML 또는 JSON 형식으로 작성해야 합니다.  
- CLI 사용 시 `--output table`은 명령어 결과를 쉽게 파악할 수 있습니다.  
- 프리티어는 7일간 75개의 스택 생성이 가능합니다.  
- `aws cloudformation validate-template`로 템플릿 유효성 검증 가능합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 CloudFormation 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **Nested Stacks (중첩 스택)**:
   - 설명: 복잡한 인프라 구조를 관리하기 위한 유연한 설계 방식으로, 중첩 스택은 부모 스택과 자식 스택 간의 관계를 명확히 정의하여 리소스 의존성을 줄입니다. 시험에서는 중첩 스택의 구조, `AWS::CloudFormation::Stack` 리소스 사용법, 그리고 스택 간 상호작용에 대한 이해를 평가합니다.
   - 키워드: `Nested Stacks`, `AWS::CloudFormation::Stack`, `Stack Dependencies`

2. **AWS CDK Constructs (CDK 구조)**:
   - 설명: CDK는 TypeScript/Python/Java로 인프라를 코드로 작성할 수 있게 해주는 도구로, CloudFormation의 선언형 템플릿을 프로그래밍 방식으로 구현합니다. 시험에서는 CDK의 장점(코드 재사용성, 테스트 가능성)과 CloudFormation과의 차이점을 비교해 묻는 경우가 많습니다.
   - 키워드: `CDK Constructs`, `Declarative Infrastructure`, `Code-Based IaC`

3. **Systems Manager State Manager**:
   - 설명: 리소스 상태를 관리하고 자동화된 구성 변경을 수행하는 기능으로, 시험에서는 State Manager의 `associations`, `automation`, `patch management` 기능과 관련된 문제를 출제합니다. 특히, 리소스 상태 모니터링과 일관성 유지에 대한 이해가 중요합니다.
   - 키워드: `State Manager`, `Resource State`, `Patch Management`

4. **Well-Architected 6대 원칙**:
   - 설명: Cost Optimization, Operational Excellence, Security, Reliability, Performance Efficiency, Sustainability 등 6가지 원칙은 AWS Well-Architected Framework의 핵심입니다. 시험에서는 각 원칙에 해당하는 구체적인 사례(예: Reserved Instances로 비용 최적화)를 묻는 경우가 많습니다.
   - 키워드: `6 Pillars`, `Cost Optimization`, `Reserved Instances`

5. **비용 최적화 (Reserved Instances)**:
   - 설명: 장기적인 리소스 사용을 고려한 비용 절감 전략으로, 시험에서는 Reserved Instances의 구독 방식, 예약 리소스의 종류(예: EC2, RDS), 그리고 예약률 계산에 대한 이해를 평가합니다. 특히, 비용 최적화와 관련된 시나리오 문제를 자주 출제합니다.
   - 키워드: `Reserved Instances`, `Cost Optimization`, `Instance Reservation`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | 중첩 스택과 중첩 템플릿을 혼동해 문제 풀이에 오류 발생 | 중첩 스택은 `AWS::CloudFormation::Stack` 리소스로 명시적 구조를 정의하며, 중첩 템플릿은 템플릿 내부에 포함된 템플릿입니다. |
| 함정 2 | CDK와 CloudFormation의 차이를 오해해 잘못된 구현 설계 | CDK는 프로그래밍 방식으로 인프라를 작성하지만, 최종적으로 CloudFormation 템플릿으로 변환되어 실행됩니다. |
| 함정 3 | State Manager의 `associations`와 `automation` 기능을 혼동 | `associations`는 리소스와 정책을 연결하는 기능이고, `automation`은 자동화된 작업 실행을 위한 도구입니다. |

#### 🔄 CloudFormation vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | CloudFormation | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 선언형 템플릿으로 인프라 자동화 | CDK(코드 기반 IaC), Terraform(다중 클라우드) | 클라우드 전용 인프라 관리 시 |
| 확장성 | 중첩 스택으로 복잡한 구조 지원 | CDK는 코드 재사용성, Terraform는 모듈화 | 대규모 리소스 관리 시 |
| 비용 | 템플릿 작성 비용 낮음 | CDK는 개발 인프라 필요, Terraform는 클라우드 비용 추가 | 예산 제약 시 |
| 지연시간 | 템플릿 파싱 및 실행 시간 | CDK는 코드 컴파일 시간, Terraform는 리소스 생성 시간 | 빠른 배포가 필요한 경우 |

#### 📝 시험 대비 체크리스트
- [ ] CloudFormation의 핵심 목적을 한 문장으로 설명할 수 있는가? (인프라 자동화 및 일관성 유지)
- [ ] CloudFormation를 선택해야 하는 시나리오를 알고 있는가? (복잡한 리소스 관리, 팀 협업)
- [ ] CloudFormation의 제한사항/한계를 알고 있는가? (템플릿 복잡성, 리소스 종속성)
- [ ] CloudFormation와 비슷한 서비스의 차이점을 설명할 수 있는가? (CDK: 코드 기반, Terraform: 다중 클라우드)
- [ ] CloudFormation의 비용 구조를 이해하고 있는가? (템플릿 작성 비용, 리소스 사용 비용)

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 CloudFormation를 떠올리세요:  
> - **Infrastructure as Code** (IaC)  
> - **Nested Stacks**  
> - **Declarative Templates**  
> - **State Manager**  
> - **Reserved Instances**

---

| [⬅️ 이전 Day](../day4/README.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 4](../README.md) | [CDK ➡️](./CDK.md) |

---
