---

| [⬅️ Systems Manager](./Systems-Manager.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 4](../README.md) | 끝 ➡️ |

---

# Well-Architected Framework 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** Well-Architected Framework는 AWS 클라우드 환경에서 안전하고 성능이 우수하며 비용 효율적인 아키텍처 설계 및 운영을 위한 AWS의 통합적인 가이드라인 및 도구 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 클라우드 인프라를 수동으로 관리할 때 발생하는 리소스 중복, 구성 오류, 보안 취약점 등으로 인한 운영 비용 증가 및 안정성 저하  
- **문제 2:** 기존 시스템에서 일관된 보안 정책 적용이 어려워지는 경우, 예를 들어 암호화 미흡, 인증/허가 관리 부재 등으로 인한 데이터 유출 리스크  
- **문제 3:** 비용 관리가 제대로 되지 않아 예상치 못한 청구 발생, 리소스 과다 사용으로 인한 성능 저하  

**Well-Architected Framework로 해결:**
- **해결 1:** CloudFormation, AWS CDK 등으로 자동화된 인프라 구성으로 구성 오류 감소, 리소스 관리 효율화  
- **해결 2:** 6대 원칙(보안, 성능, 비용, 운영, 지속 가능성, 신뢰성)을 기반으로 일관된 보안 및 운영 정책을 적용 가능  
- **해결 3:** Reserved Instances, Spot Fleet 등 비용 최적화 도구를 통해 예측 가능한 비용 관리 및 리소스 효율성 향상  

### 비유로 이해하기
**아파트 건설을 생각해보세요.**  
기존 방식은 각 층을 독립적으로 설계해 건축하는 것이었지만, Well-Architected Framework는 전반적인 설계 지침(예: 전기 배선 방식, 방수 처리 기준)을 제공해 각 층이 동일한 기준을 따르도록 합니다. 이는 클라우드 아키텍처에서 보안 정책, 성능 최적화, 비용 관리 등을 일관되게 적용하는 데 비유할 수 있습니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 기존 온프레미스 시스템을 클라우드로 마이그레이션 시 | 금융 기관이 데이터 센터를 클라우드로 전환하며, 보안 및 성능 기준을 일관되게 적용 |
| 시나리오 2 | SaaS 플랫폼 개발 시 | 콘텐츠 플랫폼이 확장성과 지속 가능성 기준을 충족하는 인프라 설계 |
| 시나리오 3 | 대규모 e-commerce 서비스 운영 시 | 온라인 쇼핑몰이 고가용성과 비용 최적화를 동시에 달성 |

**이럴 때 Well-Architected Framework를 선택하세요:**
- ✅ 상황 1: 기존 시스템을 클라우드로 전환할 때 일관된 설계 기준이 필요  
- ✅ 상황 2: 보안, 성능, 비용 등 다중 요소를 고려한 아키텍처 설계가 필요  
- ✅ 상황 3: 운영 중 발생하는 리스크를 사전에 방지하고자 할 때  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ 상황: 단일 리소스 관리 → **AWS EC2, S3** 추천 (개별 리소스 관리에 적합)  
- ❌ 상황: 실시간 데이터 처리 → **Kinesis, Lambda** 추천 (데이터 파이프라인 최적화에 적합)  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **CloudFormation** | 자동화된 인프라 배포 및 관리 | `User → CloudFormation → EC2, RDS` |
| **AWS CDK** | 코드 기반 인프라 정의 및 배포 | `User → CDK (TypeScript) → CloudFormation → EC2, RDS` |
| **Systems Manager** | 자동화된 운영 및 보안 관리 | `User → SSM → EC2, Lambda` |

**자주 사용되는 아키텍처 패턴:**
```
User → CloudFront (CDN) → CloudFormation (인프라 정의) → EC2 (서버) + RDS (데이터베이스) + S3 (스토리지)
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **CloudFormation** | $0.00 / 요청 (사용량 기반) | 12개월 무료 |
| **AWS CDK** | $0.00 (프리티어 포함) | 항상 무료 |
| **Systems Manager** | $0.00 / 요청 (사용량 기반) | 12개월 무료 |

**비용 최적화 팁:**
1. 💡 팁 1: **Reserved Instances** 사용으로 인프라 비용 30~70% 절감 (예: 1년 기준 24시간 연중무휴 서버)  
2. 💡 팁 2: **Spot Fleet** 활용해 비용 민감형 작업(예: 배포, 분석) 실행  
3. 💡 팁 3: **Cost Explorer**를 사용해 비용 패턴 분석 후 리소스 최적화  

> **⚠️ 비용 주의:** 예상치 못한 비용이 발생할 수 있는 상황: **고사양 트래픽 시 Auto Scaling 기능으로 인한 EC2 인스턴스 추가** 또는 **SSM Agent 설치 시 추가 비용 발생** 가능성. 미리 **Cost Allocation Tag**로 비용 분류를 설정하세요.

## 📚 핵심 개념

### 개념 1: **Nested Stacks in CloudFormation**
CloudFormation은 AWS 자원을 일관되게 배포하기 위해 템플릿을 사용하는 도구입니다. **Nested Stacks**는 복잡한 환경을 관리하기 위해 여러 개의 CloudFormation 스택을 중첩하여 사용하는 방식입니다. 이는 큰 템플릿을 간소화하고, 재사용 가능한 구성 요소를 만들 수 있어 조직의 인프라 관리 효율성을 높입니다.  
Nested Stacks는 부모 스택에서 자식 스택을 참조하는 방식으로 작동하며, 각 스택은 독립적인 리소스를 관리할 수 있습니다. 이는 단일 템플릿 대신 여러 템플릿을 조합해 복잡한 시나리오를 처리할 수 있는 유연성을 제공합니다.  
**왜 중요한가?**  
- **구조화된 관리**: 큰 템플릿을 분할해 관리 가능한 단위로 나누어 복잡성을 줄입니다.  
- **재사용성**: 중첩 스택은 다른 프로젝트에서 재사용 가능하며, 팀 간 협업을 용이하게 합니다.  
- **모듈화**: 각 스택은 독립적인 책임을 가지므로, 유지보수 및 테스트가 쉬워집니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **부모 스택** | 자식 스택을 포함하는 스택 | VPC 설정 스택이 EC2 인스턴스 스택을 포함하는 경우 |
| **자식 스택** | 부모 스택에서 참조되는 독립적인 스택 | EC2 인스턴스 배포를 담당하는 스택 |
| **Output 값** | 자식 스택에서 부모 스택으로 전달되는 값 | 자식 스택의 VPC ID를 부모 스택에서 사용 |

> **💡 Tip:** 중첩 스택은 `AWS::CloudFormation::Stack` 리소스를 사용해 구현하며, `Outputs` 섹션에서 자식 스택의 값을 참조해야 합니다.  

---

### 개념 2: **AWS CDK Constructs and Declarative Infrastructure**  
AWS Cloud Development Kit (CDK)는 코드로 AWS 인프라를 정의할 수 있는 도구입니다. CDK는 **Declarative Infrastructure** 방식을 사용해, 필요한 자원을 명시적으로 선언하고 자동으로 배포합니다. 이는 Terraform과 유사하지만, TypeScript, Python, Java 등 다양한 언어를 지원하여 개발자 경험을 향상시킵니다.  
CDK는 **Constructs**라는 개념을 통해 재사용 가능한 컴포넌트를 제공합니다. Construct는 기본 리소스(예: S3 버킷, EC2 인스턴스)나 복잡한 구조(예: VPC, 로드 balancer)를 표현하는 클래스로, 이를 조합해 복잡한 인프라를 구축할 수 있습니다.  
**왜 중요한가?**  
- **코드 기반 관리**: 인프라를 코드로 관리해 버전 제어 및 테스트 가능성이 높아집니다.  
- **재사용성**: Constructs는 다른 프로젝트에서 재사용 가능해 팀 내 협업을 용이하게 합니다.  
- **개발자 친화적**: 개발 언어에 익숙한 개발자가 인프라를 작성할 수 있어 생산성 향상  

#### 작동 원리  
1. **템플릿 작성**: TypeScript/Python 등 언어로 인프라를 코드로 작성합니다.  
2. **CDK 배포**: `cdk deploy` 명령어로 템플릿을 AWS CloudFormation으로 변환해 배포합니다.  
3. **관리 및 업데이트**: 코드 변경 후 `cdk deploy`를 다시 실행해 인프라를 자동으로 업데이트합니다.  

> **💡 Tip:** CDK는 `cdk.json` 파일에서 프로젝트 설정을 정의하고, `lib` 폴더에 Construct를 구현합니다.  

---

### 개념 3: **Systems Manager State Manager 및 Well-Architected 6 Pillars**  
**Systems Manager State Manager**는 AWS 인프라의 상태를 유지하고, 구성 변경을 자동화하는 도구입니다. 이는 **Operational Excellence**, **Security**, **Reliability**, **Cost Management**, **Performance Efficiency**, **Sustainability** 등 6대 Well-Architected 원칙을 실천하는 데 핵심적인 역할을 합니다.  
State Manager는 **Automation**, **Monitoring**, **Compliance**를 통합해 인프라를 일관되게 유지하고, 보안 위험을 감지해 조치할 수 있습니다. 예를 들어, EC2 인스턴스의 패치 상태를 모니터링하거나, 특정 시스템에서만 허용되는 프로세스를 제어할 수 있습니다.  
**왜 중요한가?**  
- **운영 효율성**: 자동화로 수동 작업을 줄이고, 인프라의 일관성을 유지합니다.  
- **보안 강화**: 구성 변경을 모니터링해 보안 위협을 사전에 방지합니다.  
- **비용 최적화**: 비필수 리소스를 자동으로 종료해 비용을 절감합니다.  

#### 주요 특징  
1. ****Automation**: 자동화된 구성 관리로 수동 작업을 줄입니다.  
2. ****Monitoring**: 리소스 상태를 실시간으로 모니터링해 이슈를 즉시 탐지합니다.  
3. ****Compliance**: 정책 기반으로 리소스를 검증해 보안 및 규정 준수를 강화합니다.  

> **💡 Tip:** State Manager는 `AWS::SSM::Document`를 사용해 정책을 정의하고, `aws ssm send-command` CLI 명령어로 실행할 수 있습니다.  

--- 

### 개념 4: **Cost Optimization with Reserved Instances**  
**Reserved Instances (RI)**는 AWS에서 장기적으로 사용할 리소스를 미리 예약해, **예약 비용**을 지불하고 **할인된 요금**을 받는 방식입니다. 이는 단기적인 인스턴스 사용보다 비용을 70% 이상 절감할 수 있어, **Cost Management** 원칙에서 필수적인 전략입니다.  
RI는 **1년 또는 3년 기간** 동안 특정 리소스(예: EC2, RDS)를 예약하고, 해당 기간 동안 해당 리소스의 사용량을 감당합니다. 예를 들어, 월간 100시간 사용하는 EC2 인스턴스를 1년 기간의 RI로 예약하면, 1년간의 비용이 절감됩니다.  
**왜 중요한가?**  
- **비용 최적화**: 장기 사용 시 예약 비용을 통해 비용을 크게 절감합니다.  
- **예측 가능성**: 사용량을 예측해 예산 관리가 용이해집니다.  
- **성능 향상**: 예약된 리소스는 퍼포먼스 최적화에 기여합니다.  

#### 실무 예제  
```bash
# Reserved Instance 예약 확인
aws ec2 describe-reserved-instances
```

> **⚠️ 주의:** RI는 예약 기간 동안만 사용 가능하며, 중단 시 잔여 기간에 대한 환불이 불가능합니다.  
> **💡 Tip:** AWS Free Tier은 RI 예약을 지원하지 않으므로, 예약 전 비용 계산을 반드시 확인하세요.

## 🖥️ AWS 콘솔에서 Well-Architected Framework 사용하기

### Step 1: Well-Architected Framework 서비스 접속  
1. **AWS Management Console 로그인**  
   - [https://console.aws.amazon.com](https://console.aws.amazon.com)에 접속하여 AWS 계정으로 로그인합니다.  
   - **IAM 권한 확인**: Well-Architected Framework를 사용하려면 `AWSWellArchitectedAdministrator` 역할 또는 `AWSWellArchitectedReadOnly` 권한이 필요합니다.  

2. **서비스 검색 및 접근**  
   - 상단 메뉴바의 검색창에서 `Well-Architected Framework`를 입력하고, 검색 결과에서 해당 서비스를 클릭합니다.  
   - **대시보드 화면**: "Well-Architected Framework" 대시보드가 열리면 정상적으로 서비스에 접근한 것입니다.  

> **📸 화면 확인:** 대시보드 상단에 "Workloads" 탭이 표시되고, "Review" 및 "Tools" 섹션이 존재해야 합니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **CloudFormation 스택 생성**  
   - 왼쪽 메뉴에서 `CloudFormation` → `Stacks` → `Create stack`을 클릭합니다.  
   - **템플릿 선택**:  
     - "Upload a template file" 또는 "Use a template from Amazon S3"를 선택합니다.  
     - 예: `AWS::CloudFormation::Resource` 템플릿을 업로드하여 EC2 인스턴스 생성.  

   - **파라미터 입력**:  
     - `InstanceType` (e.g., `t2.micro`)  
     - `KeyName` (기존 EC2 키 쌍 이름)  
     - `SecurityGroup` (기존 보안 그룹 이름)  

2. **중첩 스택 생성 (Nested Stack)**  
   - "Create nested stack" 옵션을 선택하여 부모 스택 내에서 자식 스택을 생성합니다.  
   - 예: `ParentStack`에 `ChildStack`을 포함하여 리소스를 중첩 관리.  

3. **Systems Manager 설정**  
   - `Systems Manager` → `State Manager` → `State Configuration`에서 리소스 상태를 모니터링합니다.  
   - 예: EC2 인스턴스의 상태를 `Desired`로 설정하여 자동 정책 적용.  

> **📸 화면 확인:** CloudFormation 스택 생성 후, "Stacks" 탭에서 생성된 스택 이름과 상태(`CREATE_COMPLETE`)가 표시됩니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **6대 원칙 검토**  
   - `Well-Architected Tool` → `Review` 탭에서 6대 원칙(성능, 가용성, 보안 등)을 검토합니다.  
   - 예: "Cost Optimization" 항목에서 AWS Cost Explorer 추천을 확인합니다.  

2. **DR 전략 설정**  
   - `Well-Architected Tool` → `Review` → `Disaster Recovery` 섹션에서 DR 전략을 구성합니다.  
   - 예: `Multi-AZ Deployment` 및 `Cross-Region Replication` 옵션 활성화.  

3. **비용 최적화 설정**  
   - `Cost Explorer` → `Cost Optimization Recommendations`에서 비용 절감 제안을 확인합니다.  
   - 예: `Right-Size` 권장 사항을 기반으로 EC2 인스턴스 크기 조정.  

> **⚠️ 주의:** 비용 최적화 설정 시, `Free Tier` 사용 가능 기간(1년)을 확인하여 과도한 자원 사용을 방지해야 합니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **리소스 확인 방법**  
   - `CloudFormation` → `Stacks`에서 생성된 스택의 리소스 목록을 확인합니다.  
   - 예: `EC2 Instance`가 `CREATE_COMPLETE` 상태인지 확인.  

2. **상태 확인 방법**  
   - `CloudWatch` → `Metrics`에서 스택 리소스의 상태 및 성능 메트릭을 모니터링합니다.  
   - 예: CPU 사용률, 네트워크 트래픽을 실시간으로 추적.  

3. **정상 동작 테스트**  
   - `Systems Manager` → `Automation`에서 정책을 실행하여 리소스가 제대로 작동하는지 테스트합니다.  
   - 예: `Test` 작업을 실행하고, `Status`가 `SUCCEEDED`인지 확인.  

> **✅ 체크리스트:**  
- [ ] CloudFormation 스택 생성 완료  
- [ ] 6대 원칙 검토 및 수정 완료  
- [ ] DR 전략 및 비용 최적화 설정 완료  
- [ ] 리소스 상태 및 메트릭 확인 완료  

---

### 📌 AWS CLI 명령어 예시  
```bash
# CloudFormation 스택 생성
aws cloudformation create-stack --stack-name my-stack --template-body file://template.yaml --parameters ParameterKey=InstanceType,ParameterValue=t2.micro

# 스택 상태 확인
aws cloudformation describe-stacks --stack-name my-stack
```

### 📊 서비스 비교 표  
| 서비스                | 주요 기능                          | 활용 예시                             |  
|-----------------------|-----------------------------------|----------------------------------------|  
| **CloudFormation**    | 자동화된 인프라 배포              | EC2, RDS, VPC 자동 생성               |  
| **Systems Manager**   | 리소스 관리 및 모니터링          | 보안 그룹 설정, 정책 적용            |  
| **Well-Architected**  | 6대 원칙 검토 및 최적화          | 비용 절감, DR 전략 설정             |  

> **💡 Tip:** AWS Free Tier를 활용하여 1년간 무제한 사용 가능. 1년 후에는 `Cost Explorer`를 통해 자원 사용량을 모니터링하세요.

## ⌨️ AWS CLI로 Well-Architected Framework 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**필수 사전 조건:**
- AWS CLI가 설치 및 구성되어 있어야 합니다.  
- IAM 권한이 `cloudformation:CreateStack`, `cloudformation:DescribeStacks` 등에 대한 접근 권한이 있어야 합니다.  
- `aws configure`를 통해 AWS 액세스 키와 시크릿 키를 설정해야 합니다.  

---

### 예제 1: Well-Architected Framework 리소스 조회  
```bash
# CloudFormation 스택 목록 조회
aws cloudformation list-stacks --stack-status-filter CREATE_COMPLETE DELETE_COMPLETE --query 'StackSummaries[].StackName' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--stack-status-filter` | 필터링할 스택 상태 | `CREATE_COMPLETE`, `DELETE_COMPLETE` |
| `--query` | JSON 응답 필터링 | `'StackSummaries[].StackName'` |
| `--output` | 출력 형식 | `table` (테이블 형식) 또는 `json` (JSON 형식) |

**예상 출력:**
```
StackName
-------------------
MyProductionStack
MyTestStack
```

**⚠️ 주의:**  
- `--stack-status-filter`를 사용하지 않으면 모든 스택이 표시됩니다.  
- `--query`를 사용하지 않으면 JSON 형식의 전체 응답이 출력됩니다.  

---

### 예제 2: Well-Architected Framework 리소스 생성  
```bash
# CloudFormation 스택 생성
aws cloudformation create-stack \
    --stack-name "MyWellArchitectedStack" \
    --template-body file://template.yaml \
    --parameters ParameterKey=Environment,ParameterValue=Production \
    --capabilities CAPABILITY_IAM
```

**필수 옵션:**
- `--stack-name`: 스택 이름 (예: `MyWellArchitectedStack`)  
- `--template-body`: 템플릿 파일 경로 (예: `file://template.yaml`)  
- `--parameters`: 파라미터 값 (예: `Environment=Production`)  
- `--capabilities`: IAM 역할 생성 시 필요 (예: `CAPABILITY_IAM`)  

**예상 출력:**
```json
{
    "StackId": "arn:aws:cloudformation:us-east-1:123456789012:stack/MyWellArchitectedStack/abcd1234-5678-90ab-cdef-012345678901"
}
```

**💡 Tip:**  
- `template.yaml` 파일은 EC2 인스턴스, RDS 데이터베이스 등 Well-Architected Framework의 6대 원칙에 맞춘 리소스 정의를 포함해야 합니다.  
- `CAPABILITY_IAM`은 IAM 역할 생성 시 필요합니다.  

---

### 예제 3: Well-Architected Framework 리소스 수정  
```bash
# CloudFormation 스택 업데이트
aws cloudformation update-stack \
    --stack-name "MyWellArchitectedStack" \
    --template-body file://updated-template.yaml \
    --parameters ParameterKey=Environment,ParameterValue=Staging \
    --capabilities CAPABILITY_IAM
```

**필수 옵션:**
- `--stack-name`: 수정할 스택 이름  
- `--template-body`: 수정된 템플릿 파일 경로  
- `--parameters`: 변경된 파라미터 값  
- `--capabilities`: IAM 역할 생성 시 필요  

**⚠️ 주의:**  
- 스택 업데이트 시 기존 리소스가 삭제될 수 있으므로, `--retain-resources` 옵션을 사용해 리소스 보존이 필요합니다.  

---

### 예제 4: Well-Architected Framework 리소스 삭제  
```bash
# CloudFormation 스택 삭제
aws cloudformation delete-stack --stack-name "MyWellArchitectedStack"

# 삭제 완료 확인
aws cloudformation describe-stacks --stack-name "MyWellArchitectedStack"
```

**⚠️ 주의:**  
- 삭제는 되돌릴 수 없습니다. `--retain-resources`를 사용해 특정 리소스만 보존할 수 있습니다.  
- `describe-stacks` 명령어로 삭제 상태를 확인할 수 있습니다.  

---

### 자주 사용하는 명령어 정리  
```bash
# 조회
aws cloudformation list-stacks --stack-status-filter CREATE_COMPLETE
aws cloudformation describe-stacks --stack-name "MyStack"

# 생성
aws cloudformation create-stack --stack-name "MyStack" --template-body file://template.yaml

# 수정
aws cloudformation update-stack --stack-name "MyStack" --template-body file://updated-template.yaml

# 삭제
aws cloudformation delete-stack --stack-name "MyStack"
```

**💡 Tip:**  
- `--output table`을 사용하면 명확한 테이블 형식으로 결과를 확인할 수 있습니다.  
- `--query`를 사용해 필터링하여 특정 필드만 출력할 수 있습니다.  
- `aws cloudformation validate-template` 명령어로 템플릿 유효성을 점검할 수 있습니다.  

---

### 비용 최적화 및 DR 전략 CLI 활용  
```bash
# Cost Explorer 데이터 조회 (CloudWatch Cost Explorer CLI)
aws ce get-cost-and-usage --time-period Start=2023-01-01,End=2023-01-31 --granularity MONTHLY

# RDS 백업 상태 확인 (Systems Manager)
aws ssm get-command-invocation --command-id "command-id" --instance-id "instance-id"
```

**💡 Tip:**  
- **비용 최적화**: `aws ce` CLI로 비용 데이터를 분석하고, `AWS Budgets` CLI를 사용해 예산 경고를 설정할 수 있습니다.  
- **DR 전략**: `aws ssm` CLI로 시스템 상태를 모니터링하고, `aws ec2 describe-tags`로 리소스 태깅을 확인할 수 있습니다.  

---

### Well-Architected 6대 원칙 CLI 검증  
```bash
# Systems Manager로 리소스 상태 확인
aws ssm get-command-invocation --command-id "command-id" --instance-id "i-1234567890abcdef0"

# CloudWatch로 모니터링 설정 확인
aws cloudwatch get-alarms --alarm-name "HighCPUUtilization"
```

**💡 Tip:**  
- **Security**: `aws iam get-user`로 IAM 사용자 권한을 확인하고, `aws ec2 describe-security-groups`로 보안 그룹 설정을 점검합니다.  
- **Reliability**: `aws cloudwatch get-metric-alarm-history`로 알림 이력을 확인합니다.  
- **Performance**: `aws ec2 describe-instances`로 인스턴스 상태를 모니터링합니다.  

---

### CLI로 Well-Architected Framework 구현 점검  
| 점검 항목 | CLI 명령어 | 설명 |
|-----------|------------|------|
| **Cost Optimization** | `aws ce get-cost-and-usage` | 비용 데이터 분석 |
| **Security** | `aws iam get-user` | IAM 사용자 권한 확인 |
| **Reliability** | `aws cloudwatch get-metric-alarm-history` | 알림 이력 확인 |
| **Performance** | `aws ec2 describe-instances` | 인스턴스 상태 모니터링 |
| **Operational Excellence** | `aws ssm get-command-invocation` | 명령 실행 상태 확인 |
| **Architecture** | `aws cloudformation describe-stacks` | 스택 구성 확인 |

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Well-Architected Framework 포인트

#### 📌 핵심 출제 포인트 TOP 5  
1. **Well-Architected 6대 원칙 (6 Pillars)**  
   - 설명: 시험에서 가장 빈출된 주제로, **성능, 보안, 비용, 신뢰성, 운영, 지속 가능** 등 6개 원칙이 구체적으로 평가됩니다. 예: "비용 최적화는 Reserved Instances 사용 시 75% 이상의 할인을 제공합니다"와 같은 구체적 사례가 출제됩니다.  
   - 키워드: `6 Pillars`, `Cost Optimization`, `Reserved Instances`

2. **CloudFormation 중첩 스택 (Nested Stacks)**  
   - 설명: 복잡한 인프라 구조를 관리하기 위한 핵심 기법으로, 단일 스택이 아닌 다중 스택을 통합적으로 관리하는 방식입니다. 시험에서는 중첩 스택의 구조, 최대 스택 깊이(5개까지), 그리고 `AWS::CloudFormation::Stack` 리소스의 사용법이 테스트됩니다.  
   - 키워드: `Nested Stacks`, `CloudFormation`, `AWS::CloudFormation::Stack`

3. **AWS CDK의 Declarative Infrastructure**  
   - 설명: CDK는 TypeScript/Python/Java로 인프라를 코드로 작성하지만, **Declarative(선언형)** 방식을 사용하여 템플릿 기반(예: CloudFormation)과 차별화됩니다. 시험에서는 CDK의 `Constructs` 개념과 `synth` 명령어의 역할이 주요 포인트입니다.  
   - 키워드: `CDK Constructs`, `Declarative Infrastructure`, `synth`

4. **Systems Manager State Manager**  
   - 설명: 자동화된 시스템 관리 기능으로, **리눅스/윈도우 서버의 패치 관리, 구성 설정, 상태 모니터링** 등이 포함됩니다. 시험에서는 `State Manager`와 `Automation`의 차이점, 그리고 `Document`를 사용한 정책 적용 방식이 중요합니다.  
   - 키워드: `State Manager`, `Automation`, `Document`

5. **비용 최적화 전략 (Reserved Instances & Spot Instances)**  
   - 설명: 시험에서 비용 최적화는 **Reserved Instances**의 75% 할인, **Spot Instances**의 90% 할인, 그리고 **Right-Sizing** 전략이 핵심입니다. 예: "예약 인스턴스는 1년 이상 사용 시 75% 할인"과 같은 구체적 사례가 출제됩니다.  
   - 키워드: `Reserved Instances`, `Spot Instances`, `Right-Sizing`

#### ⚠️ 시험에서 자주 나오는 함정  
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "Well-Architected Framework는 AWS Config와 동일한 기능을 가진다"고 오인할 수 있다. | **잘못된**. Well-Architected Framework는 인프라 설계 가이드라인을 제공하는 반면, AWS Config는 자원 상태를 모니터링하는 도구입니다. |
| 함정 2 | "Nested Stacks는 CloudFormation의 최대 스택 수를 10개까지 늘린다"고 오인할 수 있다. | **잘못된**. 중첩 스택의 최대 깊이는 5개로 제한됩니다. |
| 함정 3 | "CDK는 Terraform과 동일하게 Infrastructure as Code를 제공한다"고 오인할 수 있다. | **잘못된**. CDK는 AWS 서비스를 코드로 작성하지만, Terraform은 인프라를 코드로 작성하는 일반적인 IaC 도구입니다. |

#### 🔄 Well-Architected Framework vs 비슷한 서비스 비교 (시험 단골!)  
| 비교 항목 | Well-Architected Framework | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 인프라 설계 가이드라인 제공 | AWS Config(모니터링), CloudTrail(로깅), Terraform(IaC) | **인프라 설계 최적화** 시 선택 |
| 확장성 | 다중 원칙(성능, 보안 등)을 통합적으로 검토 | Terraform: 인프라 정의 기능만 제공 | **복잡한 인프라 설계 시** 선택 |
| 비용 | Reserved Instances, Spot Instances 활용 | AWS Cost Explorer(비용 분석) | **비용 최적화 전략** 시 선택 |
| 지연시간 | State Manager의 자동화 기능 | AWS Lambda(서버리스) | **실시간 시스템 관리** 시 선택 |

#### 📝 시험 대비 체크리스트  
- [ ] Well-Architected Framework의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  → "AWS 인프라를 안정성, 비용, 보안, 운영 효율성 등을 고려해 설계하는 가이드라인 제공"  
- [ ] Well-Architected Framework를 선택해야 하는 시나리오를 알고 있는가?  
  → "복잡한 인프라 최적화, 비용 절감, 장애 복구 전략 설계 시"  
- [ ] Well-Architected Framework의 제한사항/한계를 알고 있는가?  
  → "인프라 설계 가이드라인 제공에 한정되며, 실제 구현은 다른 도구(예: CloudFormation)와 결합 필요"  
- [ ] Well-Architected Framework와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  → "AWS Config는 자원 모니터링, Terraform은 IaC 정의, CloudTrail은 로깅 기능을 제공"  
- [ ] Well-Architected Framework의 비용 구조를 이해하고 있는가?  
  → "예약 인스턴스(75% 할인), 스파트 인스턴스(90% 할인), Right-Sizing 전략 활용"  

#### 💡 시험 팁  
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Well-Architected Framework를 떠올리세요:  
> - `6 Pillars`  
> - `Cost Optimization`  
> - `Nested Stacks`  
> - `CDK Constructs`  
> - `State Manager`

---

| [⬅️ Systems Manager](./Systems-Manager.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 4](../README.md) | 끝 ➡️ |

---
