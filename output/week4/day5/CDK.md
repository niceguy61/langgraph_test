---

| [⬅️ CloudFormation](./CloudFormation.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 4](../README.md) | [Systems Manager ➡️](./Systems-Manager.md) |

---

# CDK 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** CDK는 AWS 클라우드 인프라를 코드로 정의하고 관리하기 위한 도구입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** CloudFormation 템플릿은 YAML/JSON 형식으로 복잡한 구조를 가진다. 인프라 구성 시 오타가 발생하면 전체 배포가 실패하고, 디버깅이 어렵다.  
- **문제 2:** 기존 템플릿은 재사용 가능한 구성 요소가 부족해, 반복적인 코드 작성과 유지보수가 필요하다.  
- **문제 3:** 중첩 스택(Nested Stacks)을 사용할 경우, 스택 간 의존성 관리와 배포 순서 설정이 복잡하다.  

**CDK로 해결:**
- **해결 1:** TypeScript/Python/Java 등의 프로그래밍 언어로 인프라를 코드로 정의해 읽기 쉽고 가독성이 높은 구조를 제공한다.  
- **해결 2:** Construct라는 고수준 추상화 개념을 통해 재사용 가능한 컴포넌트를 만들 수 있어 코드 중복을 줄인다.  
- **해결 3:** 중첩 스택을 명시적으로 정의해 스택 간 의존성을 명확히 하고, 자동화된 배포 순서 관리를 가능하게 한다.  

### 비유로 이해하기
CDK는 건설용 소프트웨어처럼 생각해보자. 기존 방식은 건설 시 일일이 설계도를 따라 집을 짓는 반면, CDK는 설계도를 코드로 작성해 소프트웨어로 집을 설계하고, 자동화된 도구로 완성한다. 이는 인프라 구성 시 오류를 줄이고, 효율적인 수정과 확장이 가능하게 한다.  

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 복잡한 인프라 구조 관리 | 대규모 마이크로서비스 아키텍처 구축 시 중첩 스택을 활용해 스택 간 의존성을 명확히 관리 |
| 시나리오 2 | DevOps 자동화 | CI/CD 파이프라인에 통합해 코드 변경 시 자동으로 인프라가 배포되도록 설정 |
| 시나리오 3 | 비용 최적화 | Reserved Instances를 활용해 장기 사용 시 비용을 절감하는 인프라 구성 |

**이럴 때 CDK를 선택하세요:**
- ✅ **복잡한 인프라 템플릿 관리**  
- ✅ **자동화된 DevOps 파이프라인 구축**  
- ✅ **장기 사용 시 비용 절감 전략 적용**  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **단순한 템플릿 배포** → **CloudFormation** 사용 (CDK보다 간단한 템플릿 관리에 적합)  
- ❌ **즉시 배포가 필요한 단일 리소스** → **AWS Management Console** 사용 (CDK의 코드 기반 접근 방식보다 빠름)  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **Systems Manager** | 인프라 상태 관리 및 패치 적용 | CDK로 생성된 인스턴스에 SSM State Manager를 통해 자동 패치 관리 |
| **CloudFormation** | 중첩 스택 관리 | CDK로 생성된 중첩 스택을 CloudFormation 템플릿과 연동해 배포 순서 제어 |
| **Well-Architected Framework** | 최적화된 인프라 설계 | CDK로 구축한 인프라가 Well-Architected의 6대 원칙을 준수하도록 설계 |

**자주 사용되는 아키텍처 패턴:**
```
User → CDK (인프라 정의) → CloudFormation (중첩 스택) → EC2/ELB/RDS (리소스)  
User → CDK (자동 배포) → SSM (패치 관리) → EC2 인스턴스  
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **CDK 도구 사용** | AWS 계정 기반 무료 | 프리티어 없음 (도구 자체 비용 없음) |
| **CloudFormation 리소스** | 리소스별 유료 (예: EC2 $0.01/시간) | 750시간 무료 (1년) |
| **SSM State Manager** | 사용량 기반 요금 (예: 100개 이하 무료) | 100개까지 무료 |

**비용 최적화 팁:**
1. 💡 **프리티어 활용:** EC2, RDS 등 리소스 사용량을 750시간 이내로 제어해 무료 기간 연장  
2. 💡 **Reserved Instances 사용:** 1년 이상 장기 사용 시 70% 이상 비용 절감  
3. 💡 **SSM State Manager 무료 한도 활용:** 100개 이하 인스턴스로 패치 관리 비용 절감  

> **⚠️ 비용 주의:** CDK 자체는 무료이지만, 생성된 리소스(예: EC2, RDS)는 별도 요금이 발생하므로, 프리티어 한도를 반드시 확인해야 합니다.

## 📚 핵심 개념

### 개념 1: **Nested Stacks in CloudFormation**
CloudFormation의 중첩 스택은 복잡한 인프라 구조를 관리하기 위해 대규모 템플릿을 작은 단위로 나누어 구조화하는 기법입니다. 중첩 스택은 부모 스택과 자식 스택으로 구성되며, 부모 스택은 자식 스택을 참조하여 자동으로 배포합니다. 이는 템플릿의 관리성을 높이고, 재사용 가능한 구성 요소를 만들 수 있는 장점을 제공합니다. 특히, 여러 팀이 협업하거나 대규모 애플리케이션을 관리할 때 중첩 스택은 효율적인 모듈화를 가능하게 합니다.

#### 왜 중요한가?
- **모듈화**: 복잡한 템플릿을 작은 단위로 나누어 관리성 향상  
- **재사용성**: 자식 스택은 다른 템플릿에서도 재사용 가능  
- **보안**: 자식 스택의 접근 권한을 제어하여 보안 위험 최소화  

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **부모 스택** | 자식 스택을 포함하는 스택 | VPC 생성 후 EC2 인스턴스 배포를 담당하는 스택 |
| **자식 스택** | 부모 스택에 의존하는 독립적인 스택 | 특정 서비스(예: DynamoDB)를 구성하는 스택 |
| **템플릿 링크** | 부모 스택에서 자식 스택의 템플릿 경로를 지정 | `AWS::CloudFormation::Stack` 리소스를 사용한 연결 |

> **💡 Tip:** 중첩 스택은 `AWS::CloudFormation::Stack` 리소스를 사용해 연결하고, `Outputs`로 자식 스택의 리소스를 참조하여 부모 스택에서 활용합니다.  

---

### 개념 2: **AWS CDK Constructs and Declarative Infrastructure**
AWS CDK(Cloud Development Kit)는 선언형 인프라 정의를 통해 코드로 AWS 리소스를 구성하는 도구입니다. CDK는 TypeScript, Python, Java 등의 코드로 인프라를 정의하고, 이를 CloudFormation 템플릿으로 변환하여 배포합니다. 이는 기존 CloudFormation의 YAML/JSON 기반 템플릿 대신 코드로 리소스를 정의함으로써 개발 생산성을 높입니다. CDK의 **Constructs**는 재사용 가능한 인프라 구성 요소를 의미하며, 이를 통해 복잡한 시스템을 단순화합니다.

#### 작동 원리
1. **Constructs 정의**: TypeScript나 Python 코드로 리소스(예: EC2, S3)를 선언  
2. **템플릿 생성**: CDK CLI를 통해 CloudFormation 템플릿으로 변환  
3. **배포**: AWS CloudFormation을 통해 실제 인프라 생성  

> **💡 Tip:** CDK는 `cdk deploy` 명령어로 배포하고, `cdk synth`로 템플릿 확인 가능. 코드 기반으로 인프라를 관리하면 버전 제어와 협업이 쉬워집니다.  

---

### 개념 3: **Systems Manager State Manager**
Systems Manager State Manager는 AWS 리소스의 상태를 유지하고 자동화된 관리를 제공하는 서비스입니다. 이는 EC2 인스턴스, 랩탑, 서버 등에서 특정 상태(예: 특정 패키지 설치, 설정 파일 적용)를 유지하도록 보장합니다. State Manager는 **Automation**, **Compliance**, **Inventory** 기능을 통해 리소스 상태를 모니터링하고, 필요한 경우 자동으로 수정합니다. 이는 인프라 일관성 유지와 운영 효율성 향상에 기여합니다.

#### 주요 특징
1. **Resource State Management**: 특정 상태를 유지하도록 정책 적용 (예: "Ubuntu 20.04 설치")  
2. **Automation**: 상태가 변경될 때 자동으로 조치 (예: 패키지 설치 후 재시작)  
3. **Compliance Checks**: 리소스 상태를 정기적으로 점검하고 보고 (예: 보안 패치 미적용 시 경고)  

> **💡 Tip:** State Manager는 `aws ssm create-association` 명령어로 정책을 정의하고, `aws ssm get-association`로 상태 확인 가능. 이는 운영 팀이 인프라 상태를 일관되게 관리하는 데 유용합니다.

## 🖥️ AWS 콘솔에서 CDK 사용하기

### Step 1: CDK 서비스 접속
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - **💡 Tip:** 프리티어 계정이 없으신 경우, [AWS Free Tier](https://aws.amazon.com/free/)에서 계정 생성 후 로그인하세요.  
2. 상단 검색창에서 **"CDK"**를 입력하고, **"Cloud Development Kit (CDK)"**를 클릭합니다.  
3. **"CDK" 대시보드**가 열리면, **"CloudFormation"** 탭을 선택해 스택 관리 화면으로 이동합니다.  

> **📸 화면 확인:** CDK 대시보드에서 **"CloudFormation"** 탭을 클릭하면, 생성된 스택 목록이 표시됩니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **CloudFormation 스택 생성**  
   - **"Create stack"** 버튼을 클릭합니다.  
   - **"Specify template"** 섹션에서 **"Upload a template file"**을 선택하고, 로컬 파일을 업로드합니다 (예: `template.yaml`).  
   - **"Parameters"** 섹션에서 필요한 값을 입력합니다 (예: `BucketName`).  

2. **리소스 설정 옵션**  
   - **"Options"** 섹션에서 **"Role ARN"**을 선택해 IAM 역할을 지정합니다.  
   - **"Advanced options"**에서 **"Tags"**를 추가해 리소스를 분류합니다 (예: `Environment=Production`).  

3. **확인 및 생성**  
   - **"Next"** → **"Create"** 버튼을 클릭해 스택을 생성합니다.  
   - 생성 중에는 **"Events"** 탭에서 진행 상황을 확인할 수 있습니다.  

> **📸 화면 확인:** **"Parameters"** 섹션에서 입력한 값이 정확하게 반영되었는지, **"Tags"**가 추가되었는지 확인합니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **스택 구성 변경**  
   - **"Stacks"** 탭에서 목록에서 스택을 선택하고, **"Edit"** 버튼을 클릭합니다.  
   - **"Update template"** 섹션에서 새로운 템플릿 파일을 업로드하거나, 기존 템플릿을 수정합니다.  

2. **리소스 태깅**  
   - **"Tags"** 섹션에서 추가 태그를 입력하거나, 기존 태그를 수정합니다.  
   - **"Save"** 버튼을 클릭해 변경 사항을 저장합니다.  

3. **IAM 역할 수정**  
   - **"Role ARN"**을 변경해 다른 IAM 역할을 사용하도록 설정합니다.  
   - **"Validate"** 버튼을 클릭해 역할이 유효한지 확인합니다.  

> **⚠️ 주의:** **"Parameters"** 값에 잘못된 값을 입력하면 스택 생성 실패가 발생할 수 있습니다. 반드시 템플릿의 `Parameters` 섹션을 확인하세요.  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인**  
   - **"CloudFormation"** → **"Stacks"** 탭에서 스택 상태가 **"CREATE_COMPLETE"**인지 확인합니다.  
   - **"Resources"** 탭에서 생성된 리소스 목록(예: S3 버킷, EC2 인스턴스)을 확인합니다.  

2. **리소스 상태 확인**  
   - **"Events"** 탭에서 스택 생성/수정 과정의 로그를 확인해 오류 여부를 파악합니다.  
   - **"Outputs"** 탭에서 생성된 리소스의 URL이나 ARN을 확인합니다 (예: S3 버킷 URL).  

3. **정상 동작 테스트**  
   - 생성된 S3 버킷에 파일을 업로드해 기능이 동작하는지 확인합니다.  
   - EC2 인스턴스의 SSH 접속을 시도해 인스턴스가 정상 작동하는지 검증합니다.  

> **💡 Tip:** **"Well-Architected Framework"**의 **"Cost Optimization"** 원칙을 지키기 위해, **"Tags"**를 활용해 리소스를 분류하고, **"Cost Explorer"**에서 비용을 모니터링하세요.

## ⌨️ AWS CLI로 CDK 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**필수 사전 조건:**  
- AWS CLI가 최신 버전으로 업데이트되어야 합니다.  
- CDK 애플리케이션을 배포한 후, AWS CLI를 사용해 생성된 리소스(예: CloudFormation 스택, S3 버킷 등)를 관리해야 합니다.  
- **프리티어 사용 시:** `us-east-1` 리전에서 1년간 무료로 사용 가능합니다.  

---

### 예제 1: CDK로 생성된 CloudFormation 스택 조회
```bash
# CloudFormation 스택 목록 조회
aws cloudformation list-stacks --stack-status-filter CREATE_COMPLETE --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--stack-status-filter` | 필터링할 스택 상태 | `CREATE_COMPLETE` |
| `--output` | 출력 형식 | `table` (표 형식) |

**예상 출력:**
```
TABLE OUTPUT
-------------------------------------
|        Name         |  StackId         |
+---------------------+------------------+
| MyCDKStack          | arn:aws:cloudformation:... |
| AnotherStack        | arn:aws:cloudformation:... |
-------------------------------------
```

**주의:** CDK로 배포한 스택은 `MyCDKStack`과 같은 이름으로 생성됩니다.  

---

### 예제 2: CDK로 생성된 리소스 생성 및 확인
```bash
# S3 버킷 생성 (CDK로 배포한 리소스 예시)
aws s3api create-bucket --bucket my-cdk-bucket --region us-east-1
```

**필수 옵션:**
- `--bucket`: 생성할 S3 버킷 이름  
- `--region`: 리전 (CDK 배포 시 자동으로 설정됨)  

**예상 출력:**
```json
{
    "Location": "https://my-cdk-bucket.s3.amazonaws.com/"
}
```

**확인 명령어:**
```bash
# 생성된 S3 버킷 목록 조회
aws s3api list-buckets --query "Buckets[].Name" --output text
```

---

### 예제 3: CDK로 생성된 리소스 수정
```bash
# EC2 인스턴스 태그 수정 (CDK로 생성된 리소스 예시)
aws ec2 create-tags --resources i-1234567890abcdef0 --tags Key=Environment,Value=Production
```

**필수 옵션:**
- `--resources`: 수정할 리소스 ID (예: EC2 인스턴스 ID)  
- `--tags`: 추가할 태그 (Key=Value 형식)  

**주의:** EC2 인스턴스 수정 시 **중단** 시도 시 **경고**가 발생할 수 있습니다.  

---

### 예제 4: CDK로 생성된 리소스 삭제
```bash
# S3 버킷 삭제 (CDK로 생성된 리소스 예시)
aws s3api delete-bucket --bucket my-cdk-bucket --region us-east-1
```

**삭제 확인:**
```bash
# 삭제 후 상태 확인
aws s3api list-buckets --query "Buckets[].Name" --output text
```

> **⚠️ 주의:** AWS CLI로 삭제한 리소스는 복구할 수 없습니다. CDK 배포 시 생성된 리소스는 **항상 삭제 전에 백업**을 확인하세요.  

---

### 자주 사용하는 명령어 정리
```bash
# CloudFormation 스택 관리
aws cloudformation describe-stacks --stack-name MyCDKStack
aws cloudformation delete-stack --stack-name MyCDKStack

# S3 리소스 관리
aws s3api list-buckets
aws s3api get-bucket-location --bucket my-cdk-bucket

# EC2 리소스 관리
aws ec2 describe-instances --instance-ids i-1234567890abcdef0
```

**Tip:** CDK로 배포한 리소스는 AWS Console에서 **"CloudFormation" > "Stacks"** 메뉴에서 관리할 수 있습니다.  

---

### CLI 사용 시 주의사항
| 항목 | 설명 |
|------|------|
| **비용 관리** | CDK로 생성된 리소스는 AWS 서비스 요금에 포함됩니다. 프리티어 사용 시 `us-east-1` 리전에서 1년간 무료입니다. |
| **DR 전략** | CDK로 배포한 스택은 **CloudFormation 기반**이므로, 스택 삭제 시 **모든 리소스가 함께 삭제됩니다**. |
| **Well-Architected 원칙** | CDK로 생성된 리소스는 **자동화**, **모니터링**, **비용 최적화** 원칙에 따라 설계됩니다. |

---

### 체크리스트: CLI로 CDK 리소스 관리
- [ ] AWS CLI 버전 확인 및 업데이트  
- [ ] CDK 애플리케이션 배포 후 생성된 리소스 확인  
- [ ] 생성된 리소스에 대한 `--output table` 사용  
- [ ] 삭제 전에 **백업** 및 **재사용 가능성** 검토  
- [ ] 프리티어 사용 시 `us-east-1` 리전 선택  

> **💡 Tip:** AWS CLI는 CDK로 생성된 리소스를 직접 관리하는 데 유용하지만, **CDK 자체는 CLI 기반 도구가 아닙니다**. CDK는 코드로 인프라를 정의하고, AWS CLI는 생성된 리소스를 관리하는 데 사용됩니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 CDK 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **CDK의 Declarative Infrastructure 및 Constructs 개념**  
   - 설명: CDK는 AWS 자원을 TypeScript/Python으로 정의하는 **Declarative** 방식을 사용하며, **Constructs**는 리소스를 모듈화하여 재사용 가능한 코드 구조를 제공합니다. 이는 CloudFormation의 JSON/YAML 기반 imperative 방식과 차별화되며, 코드로 인프라 정의를 관리하는 데 핵심입니다.  
   - 키워드: `Declarative`, `Constructs`, `Infrastructure as Code`

2. **CDK vs CloudFormation의 차이점**  
   - 설명: CDK는 CloudFormation의 복잡성을 줄이기 위해 **고수준 추상화**를 제공하며, TypeScript/Python 기반으로 개발자가 리소스를 코드로 정의할 수 있습니다. 반면 CloudFormation은 JSON/YAML 기반의 명령형 방식으로 복잡한 구조를 다루기 어렵습니다. 이는 시험에서 자주 비교되는 주제입니다.  
   - 키워드: `Abstraction Layer`, `Imperative vs Declarative`, `High-Level Constructs`

3. **CDK로 비용 최적화 전략 구현 (예: Reserved Instances)**  
   - 설명: CDK는 **Reserved Instances**와 같은 비용 최적화 전략을 코드로 자동화할 수 있어, 리소스 사용 패턴을 정의하고 예측 가능한 비용을 관리하는 데 유리합니다. 이는 AWS Well-Architected Framework의 Cost Optimization 원칙과 직접 연관됩니다.  
   - 키워드: `Reserved Instances`, `Cost Optimization`, `Cost Management`

4. **CDK의 Nested Stacks 활용**  
   - 설명: CDK는 CloudFormation의 중첩 스택 기능을 더 편리하게 활용할 수 있도록 지원합니다. 중첩 스택을 통해 복잡한 환경을 모듈화하고, 리소스 간 의존성을 명확히 관리할 수 있어, 대규모 아키텍처 설계 시 필수적인 개념입니다.  
   - 키워드: `Nested Stacks`, `Modular Infrastructure`, `Stack Dependencies`

5. **CDK의 Security 및 Compliance 자동화**  
   - 설명: CDK는 AWS IAM 정책, Security Groups, VPC 설정 등을 코드로 정의해 **보안 표준 준수**를 자동화할 수 있습니다. 이는 Well-Architected Framework의 Security 원칙과 직접 연결되며, 시험에서 자주 다루는 주제입니다.  
   - 키워드: `Security Compliance`, `IAM Policies`, `Automated Security`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "CDK는 CloudFormation의 JSON 기반 방식을 단순화한 도구"라고 오해하는 경우 | CDK는 **코드 기반**의 Declarative 방식으로, CloudFormation의 JSON/YAML을 대체하지 않고 보완합니다. |
| 함정 2 | "CDK는 모든 AWS 서비스를 지원하는 유일한 IaC 도구"라고 오인하는 경우 | CDK는 특정 서비스에 한정되며, CloudFormation, Terraform 등과 함께 IaC 도구로 사용됩니다. |
| 함정 3 | "CDK의 비용은 CloudFormation보다 항상 높다"고 잘못 인식하는 경우 | CDK는 코드 작성 시간 단축으로 **총 개발 비용**을 절감할 수 있으나, 실행 시 비용은 동일합니다. |

#### 🔄 CDK vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | CDK | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 코드 기반의 Declarative IaC | CloudFormation (JSON/YAML 기반) | 리소스 복잡도가 높거나 개발자 친화적인 환경이 필요한 경우 |
| 확장성 | 모듈화된 Constructs로 재사용 가능 | 중첩 스택이나 템플릿 재사용 | 대규모 아키텍처 설계 시 |
| 비용 | 코드 작성 시간 절감 → 총 개발 비용 절감 | CloudFormation (템플릿 작성 시간 단축) | 리소스 정의가 반복적인 경우 |
| 지연시간 | 코드 컴파일 및 배포 시 발생 | 템플릿 업로드 및 스택 배포 시 발생 | 실시간 배포가 필요한 경우 |

#### 📝 시험 대비 체크리스트
- [ ] CDK의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  (예: "AWS 자원을 코드로 정의하여 일관된 인프라를 관리하는 도구")
- [ ] CDK를 선택해야 하는 시나리오를 알고 있는가?  
  (예: 복잡한 중첩 스택, 보안 정책 자동화, 개발자 친화적 환경)
- [ ] CDK의 제한사항/한계를 알고 있는가?  
  (예: 특정 서비스 지원 제한, 코드 작성 필요, 실행 비용 동일)
- [ ] CDK와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  (예: CloudFormation은 JSON 기반, CDK는 코드 기반)
- [ ] CDK의 비용 구조를 이해하고 있는가?  
  (예: 코드 작성 시간 절감 → 총 개발 비용 절감, 실행 비용 동일)

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 CDK를 떠올리세요:  
> - `Declarative Infrastructure`  
> - `Constructs`  
> - `Infrastructure as Code`  
> - `Nested Stacks`  
> - `Cost Optimization`

---

| [⬅️ CloudFormation](./CloudFormation.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 4](../README.md) | [Systems Manager ➡️](./Systems-Manager.md) |

---
