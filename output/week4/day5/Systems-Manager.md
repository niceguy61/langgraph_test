---

| [⬅️ CDK](./CDK.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 4](../README.md) | [Well-Architected Framework ➡️](./Well-Architected-Framework.md) |

---

# Systems Manager 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** Systems Manager는 AWS 리소스를 관리하고 자동화를 위한 통합형 관리 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** EC2 인스턴스에서 패치 관리가 수동으로 이루어져 시간이 많이 소요되고, 일관성이 떨어짐.  
  *기존에는 각 인스턴스에 SSH 접속 후 패치 명령어를 직접 입력하거나, 별도의 도구를 사용해야 했습니다.*  
- **문제 2:** 여러 AWS 리소스(예: EC2, Lambda, RDS)의 상태를 중앙에서 모니터링할 수 없음.  
  *각 리소스별로 별도의 콘솔과 알림 시스템을 사용해야 하며, 통합 관리가 어려웠습니다.*  
- **문제 3:** 인프라 구성 변경 시 수동으로 코드를 수정해야 하며, 버전 관리가 어려움.  
  *예: EC2 인스턴스의 애플리케이션 설정 변경 시, 수동으로 파일을 업로드해야 했습니다.*

**Systems Manager로 해결:**
- **해결 1:** **State Manager**를 통해 일관된 구성 관리가 가능하며, 자동화된 패치 및 설정 변경이 가능.  
  *예: EC2 인스턴스의 패치 상태를 실시간으로 모니터링하고, 자동으로 패치를 실행합니다.*  
- **해결 2:** **Inventory** 기능으로 모든 AWS 리소스의 상태를 중앙에서 확인 가능.  
  *예: EC2, RDS, Lambda의 리소스 사용량, 운영 체제 버전 등을 한눈에 파악할 수 있습니다.*  
- **해결 3:** **Automation**을 통해 인프라 변경 작업을 코드로 정의하고, 버전 관리가 가능.  
  *예: EC2 인스턴스의 애플리케이션 설정을 JSON 파일로 정의하고, 자동으로 배포합니다.*

### 비유로 이해하기
Systems Manager는 집안의 스마트 홈 제어 패널에 비유할 수 있어요. 전등, 냉난방, 보안 카메라 등 모든 기기들을 하나의 플랫폼에서 제어하고 상태를 실시간으로 확인할 수 있습니다. 마찬가지로, Systems Manager는 AWS 리소스들을 중앙에서 관리하고, 자동화된 작업을 통해 효율적인 운영을 가능하게 해줍니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 다수의 EC2 인스턴스에서 일관된 설정 관리를 필요로 할 때 | 기업이 100대 이상의 EC2 인스턴스를 운영하고, OS 및 애플리케이션 설정을 일관되게 관리해야 할 때 |
| 시나리오 2 | 자동화된 패치 및 보안 업데이트를 수행해야 할 때 | AWS EC2 인스턴스의 보안 패치를 주기적으로 자동으로 실행하는 시나리오 |
| 시나리오 3 | 인프라 구성 변경을 코드로 정의하고 버전 관리할 때 | CloudFormation 또는 AWS CDK를 사용하여 인프라를 코드로 관리하는 경우 |

**이럴 때 Systems Manager를 선택하세요:**
- ✅ **다양한 AWS 리소스를 중앙에서 관리해야 할 때**
- ✅ **자동화된 작업(예: 패치, 모니터링)을 수행해야 할 때**
- ✅ **인프라 구성 변경을 코드로 관리하고 버전을 추적해야 할 때**

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **단일 리소스 관리에 집중할 때** → **EC2 Console** 또는 **RDS Console** 사용
- ❌ **고성능 배포가 필요한 경우** → **AWS Lambda** 또는 **Terraform** 사용

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **EC2** | Systems Manager는 EC2 인스턴스의 상태 모니터링 및 자동화 작업을 수행 | User → Systems Manager → EC2 |
| **Lambda** | 자동화 작업을 Lambda 함수로 실행하여 비용 최적화 | Systems Manager → Lambda → EC2 |
| **CloudFormation** | 인프라 구성 변경을 코드로 관리하고 버전 관리 | User → CloudFormation → Systems Manager → EC2 |

**자주 사용되는 아키텍처 패턴:**
```
User → CloudFormation → Systems Manager → EC2
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **State Manager 상태 모니터링** | $0.10/월 (인스턴스당) | 12개월 무료 |
| **Automation 작업 실행** | $0.05/작업 | 12개월 무료 |
| **Inventory 수집** | $0.02/월 (인스턴스당) | 12개월 무료 |

**비용 최적화 팁:**
1. 💡 **State Manager를 사용해 일관된 구성 관리**를 통해 수동 작업을 줄여 비용 절감.  
2. 💡 **Automation을 통해 반복 작업 자동화**하여 인력 비용을 절감.  
3. 💡 **Reserved Instances 활용**하여 장기 사용 시 단가를 낮추고, 비용 예측을 가능하게 함.

> **⚠️ 비용 주의:** **Automation 작업의 수**가 많아지면 비용이 급격히 증가할 수 있으므로, 작업을 정확히 정의하고 **CloudWatch 알림**을 설정해 비용을 모니터링해야 합니다.

## 📚 핵심 개념

### 개념 1: **Systems Manager State Manager**
Systems Manager State Manager는 AWS 환경에서 자동화된 구성 관리 및 보안 모니터링을 제공하는 핵심 서비스입니다. 이 서비스는 클라우드 리소스의 상태를 일관되게 유지하고, 보안 정책을 준수하도록 자동화된 작업을 수행합니다. 예를 들어, 서버의 패치 관리, 소프트웨어 설치, 구성 변경 등을 자동으로 처리할 수 있어 운영 효율성을 극대화합니다. State Manager는 AWS CloudFormation과 연동하여 자동화된 인프라 관리를 가능하게 하며, 복잡한 환경에서도 일관된 상태를 유지합니다.  

#### 왜 중요한가?
- **운영 효율성 향상**: 수동 작업을 최소화하여 인프라 관리 시간을 절감합니다.  
- **보안 강화**: 정책 기반으로 자동화된 보안 검사 및 패치 적용을 통해 취약점을 사전에 예방합니다.  
- **일관성 유지**: 다양한 리소스에 동일한 구성 규칙을 적용해 시스템의 안정성을 높입니다.  

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **Configuration Management** | 클라우드 리소스의 상태를 정의하고 자동으로 유지합니다. | EC2 인스턴스에 특정 소프트웨어를 설치하고 유지합니다. |
| **Compliance Monitoring** | 정책 기반으로 리소스의 준수 여부를 감시합니다. | IAM 정책을 준수하지 않는 리소스를 실시간으로 알림합니다. |
| **Automation** | 수동 작업을 자동화하여 운영을 효율화합니다. | 패치 적용 작업을 자동으로 실행하고 상태를 확인합니다. |

> **💡 Tip:** State Manager는 AWS Systems Manager Console에서 'State Manager' 탭에서 확인할 수 있으며, CLI로도 `aws ssm get-parameters`와 같은 명령어를 사용해 상태를 관리할 수 있습니다.

---

### 개념 2: **Well-Architected 6 Pillars**
Well-Architected Framework는 AWS에서 안정적이고 효율적인 클라우드 아키텍처를 설계하기 위한 6대 원칙을 제공합니다. 이 원칙들은 **운영 우수성(Operational Excellence)**, **보안(Security)**, **신뢰성(Reliability)**, **성능 효율성(Performance Efficiency)**, **비용 최적화(Cost Optimization)**, **서비스 관리(Service Management)**로 구성됩니다. 이 원칙들은 클라우드 환경에서 리스크를 줄이고, 지속적인 개선을 촉진합니다.  

#### 작동 원리
1. **운영 우수성**: 자동화 도구와 모니터링을 활용해 인프라를 지속적으로 관리합니다.  
2. **보안**: 정기적인 보안 검사와 접근 제어 정책을 통해 위협을 차단합니다.  
3. **신뢰성**: 고가용성 설계와 장애 대응 계획을 통해 서비스 중단을 최소화합니다.  
4. **성능 효율성**: 리소스를 최적화해 성능과 비용의 균형을 유지합니다.  
5. **비용 최적화**: Reserved Instances나 Spot Instances 등을 활용해 비용을 절감합니다.  
6. **서비스 관리**: 고객 피드백을 반영해 서비스를 지속적으로 개선합니다.  

> **💡 Tip:** AWS Well-Architected Tool을 사용해 아키텍처를 점검하고, 각 원칙에 대한 점수를 확인해 개선 방향을 설정할 수 있습니다.

---

### 개념 3: **비용 최적화 - Reserved Instances**
Reserved Instances(RI)는 AWS에서 특정 기간 동안 리소스 사용을 확약함으로써 **비용을 최대 75% 절감**할 수 있는 서비스입니다. 이는 EC2, RDS, DynamoDB 등 여러 서비스에 적용 가능하며, 장기적인 리소스 사용을 예측할 수 있을 때 유용합니다. 예를 들어, 월간 일정한 EC2 인스턴스 사용량이 있을 경우, RI를 통해 비용을 정확히 예측하고 절약할 수 있습니다.  

#### 주요 특징
1. **예측 가능한 비용**: 사용량을 확약함으로써 월별 비용을 안정적으로 관리할 수 있습니다.  
2. **고도화된 할인**: 1년 또는 3년 기간 동안 사용량을 확약하면 할인율이 높아집니다.  
3. **리소스 유형 지원**: EC2, RDS, DynamoDB 등 다양한 서비스에 적용 가능합니다.  

> **💡 Tip:** AWS Cost Explorer를 사용해 사용량 패턴을 분석하고, RI를 적절히 확약해 비용을 최적화하세요.  

--- 

> **⚠️ 주의:** Reserved Instances는 1년 이상의 기간 동안 사용해야 할인 혜택을 받을 수 있으므로, 단기 사용에는 적합하지 않습니다.

## 🖥️ AWS 콘솔에서 Systems Manager 사용하기

### Step 1: Systems Manager 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - IAM 사용자 권한이 부여된 계정으로 로그인해야 합니다  
2. 상단 검색창에 "Systems Manager"를 입력하고, 결과 목록에서 서비스를 선택합니다  
3. "Systems Manager" 클릭 후 대시보드가 열리면 정상적으로 접속되었습니다  

> **📸 화면 확인:** Systems Manager 대시보드에 "Resources", "Automation", "State Manager" 등의 탭이 표시되는지 확인합니다  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **CloudFormation 스택 생성**  
   - 메뉴 우측 상단의 **"Create stack"** 버튼 클릭  
   - "Create a new stack" 탭에서 템플릿을 선택합니다  
     - **AWS Managed Templates** 또는 **Upload a template** 옵션 사용 가능  
     - 예: `EC2 Instance` 템플릿 선택 시 EC2 인스턴스 생성  
2. **템플릿 파라미터 설정**  
   - 인스턴스 이름, 키 페어, 보안 그룹 등 필수 파라미터 입력  
   - **Parameter Options**에서 기본값을 설정하거나 필수 항목을 변경할 수 있습니다  
3. **리소스 확인**  
   - "Next" 버튼 클릭 후 "Create"를 선택하여 스택 생성  
   - 생성 완료 후 **"Stacks"** 탭에서 상태를 확인합니다  

> **📸 화면 확인:** "Create stack" 페이지에서 템플릿 선택 후 파라미터 입력 화면이 표시되는지 확인합니다  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **State Manager 구성**  
   - 메뉴에서 **"State Manager"** > **"State configurations"** 클릭  
   - "Create configuration" 버튼을 통해 인스턴스 상태를 정의합니다  
   - 예: "Ensure EC2 instance is running" 정책 설정  
2. **Automation 실행**  
   - **"Automation"** 탭에서 "Create automation"을 선택  
   - "Run command" 또는 "Start automation"을 통해 명령어 실행  
   - 예: `Run Command`를 사용해 EC2 인스턴스에 명령어 전송  
3. **IAM 권한 설정**  
   - 스택 생성 시 자동으로 생성되는 IAM 역할을 확인합니다  
   - **"Service roles"** 탭에서 역할 권한을 수정하거나 삭제할 수 있습니다  

> **⚠️ 주의:** State Manager 구성 시 "Resource Data" 권한이 부여된 IAM 역할을 사용해야 합니다  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인**  
   - "Stacks" 탭에서 스택 이름을 클릭해 출력값(Outputs)을 확인합니다  
   - 예: EC2 인스턴스의 public IP 주소 확인  
2. ** 상태 확인**  
   - "Systems Manager" > **"Inventory"**에서 리소스 상태를 실시간으로 모니터링합니다  
   - "Command" 탭에서 실행된 명령어의 결과를 확인할 수 있습니다  
3. **정상 동작 테스트**  
   - "Run Command"를 통해 `reboot` 명령어를 실행해 인스턴스 재부팅 테스트  
   - "Automation"에서 "Test" 옵션으로 자동화 작업 실행 여부 확인  

> **💡 Tip:** 스택 생성 후 5~10분이 지나야 리소스가 완전히 생성되는 경우가 있으므로, 상태를 꾸준히 확인하세요.

## ⌨️ AWS CLI로 Systems Manager 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**설명:**  
AWS CLI를 사용하기 전에 버전을 확인하고, IAM 자격 증명이 정상적으로 설정되었는지 확인하세요. 리전은 리소스 생성 시 지역에 따라 달라질 수 있으므로 반드시 확인해야 합니다.  
> **💡 Tip:** AWS CLI 설정은 `aws configure` 명령어로 `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_DEFAULT_REGION`을 설정할 수 있습니다.

---

### 예제 1: Systems Manager 리소스 조회
```bash
# [Systems Manager 관리 인스턴스 목록 조회]
aws ssm list-managed-instances --query 'ManagedInstances[].InstanceId' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 결과 필터링 | `'ManagedInstances[].InstanceId'` |
| `--output` | 출력 형식 | `json`, `table`, `text` |

**예상 출력:**
```
InstanceId
-------- 
i-1234567890abcdef0
i-0987654321fedcba0
```

**설명:**  
`list-managed-instances` 명령어는 Systems Manager에 등록된 EC2 인스턴스 목록을 조회합니다. `--query` 옵션으로 특정 필드만 추출할 수 있으며, `--output table`은 테이블 형식으로 결과를 보여줍니다.

---

### 예제 2: Systems Manager 리소스 생성
```bash
# [Systems Manager 문서 생성]
aws ssm create-document \
    --name "Example-Document" \
    --content '{"schemaVersion": "2.2", "description": "Example document", "targets": [{"platformType": "Windows"}], "documentType": "Command"}'
```

**필수 옵션:**
- `--name`: 문서 이름 (예: `Example-Document`)
- `--content`: 문서 내용 (JSON 형식)

**예상 출력:**
```json
{
    "DocumentDescription": {
        "DocumentName": "Example-Document",
        "DocumentVersion": "1",
        "DocumentArn": "arn:aws:ssm:region:account-id:document:Example-Document"
    }
}
```

**설명:**  
`create-document` 명령어는 Systems Manager에 명령어 문서를 생성합니다. `--content`는 JSON 형식으로 문서 구조를 정의해야 하며, `schemaVersion`, `targets`, `documentType` 필드는 필수적입니다.

---

### 예제 3: Systems Manager 리소스 수정
```bash
# [Systems Manager 문서 수정]
aws ssm update-document \
    --name "Example-Document" \
    --content '{"schemaVersion": "2.2", "description": "Updated example document", "targets": [{"platformType": "Linux"}], "documentType": "Command"}'
```

**설명:**  
`update-document` 명령어는 기존 문서의 내용을 수정합니다. `--name`은 수정할 문서 이름, `--content`는 새로운 내용을 입력해야 합니다.  
> **⚠️ 주의:** 문서 수정 시 이전 버전은 별도로 저장해야 하므로, 변경 사항을 체크리스트로 관리하는 것이 좋습니다.

---

### 예제 4: Systems Manager 리소스 삭제
```bash
# [Systems Manager 문서 삭제]
aws ssm delete-document --name "Example-Document"

# 삭제 확인
aws ssm list-documents --query 'Documents[].DocumentTitle' --output table
```

**예상 출력:**
```
DocumentTitle
------------
Example-Document
```

**설명:**  
`delete-document` 명령어는 문서를 영구 삭제합니다. 삭제 후에는 `list-documents` 명령어로 삭제 여부를 확인할 수 있습니다.  
> **⚠️ 주의:** 삭제는 되돌릴 수 없으므로, `--force` 옵션 없이 신중하게 실행하세요. 프리티어 사용 시 삭제 전에 리소스를 백업하는 것이 좋습니다.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws ssm list-managed-instances
aws ssm describe-association --association-id "id"

# 생성
aws ssm create-association --name "example-association" --targets "Key=InstanceIds,Values=i-1234567890abcdef0"

# 수정
aws ssm update-association --association-id "id" --parameters "Key=ParameterName,Values=Value1"

# 삭제
aws ssm delete-association --association-id "id"
```

**체크리스트:**
- [ ] AWS CLI 버전 확인
- [ ] IAM 권한 확인 (Systems Manager 관리자 역할 권한 필요)
- [ ] 리전 설정 확인
- [ ] 리소스 생성 시 `--targets` 옵션으로 대상 인스턴스 지정
- [ ] 삭제 전에 리소스 상태 확인

---

### 비용 최적화 팁
- **프리티어 사용:** Systems Manager는 프리티어로 1년간 250시간의 사용량이 제공됩니다.  
  > **💡 Tip:** `aws ssm get-command-invocation`을 사용해 명령어 실행 비용을 줄이는 것이 좋습니다.
- **리소스 제한:** `list-managed-instances`로 등록된 인스턴스 수를 확인하고, 필요 없는 인스턴스는 `deregister-target`로 제거하세요.  
- **자동화:** `create-association`로 반복 작업을 자동화해 수동 작업 비용을 절감하세요.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Systems Manager 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **State Manager의 자동화 기능**  
   - 설명: Systems Manager는 인프라 자동화, 패치 관리, 구성 관리 등을 통합적으로 제공합니다. 시험에서 자동화 기능의 구현 방식과 주요 구성 요소(예: State Manager, Automation)에 대한 이해가 필수적입니다.  
   - 키워드: `State Manager`, `Automation`, `Patch Management`

2. **CloudFormation 및 CDK 통합 활용**  
   - 설명: IaC(Infrastructure as Code) 구현 시 CloudFormation 또는 CDK를 사용해 리소스를 정의하고, Systems Manager를 통해 자동화 작업을 수행하는 사례가 시험에 자주 등장합니다.  
   - 키워드: `CloudFormation`, `CDK`, `IaC`

3. **비용 최적화 전략 (예: Reserved Instances)**  
   - 설명: Systems Manager는 리소스 사용 패턴을 분석해 비용 최적화를 지원합니다. Reserved Instances와 같은 결합 전략은 시험에서 비용 관리 관련 문제에서 자주 다룹니다.  
   - 키워드: `Cost Optimization`, `Reserved Instances`, `Billing Analysis`

4. **State Manager의 구성 관리 기능**  
   - 설명: 시스템의 구성 상태를 모니터링하고, 필요한 경우 자동으로 정규화하는 기능은 시험에서 자주 출제됩니다. 예: EC2 인스턴스의 OS 패치 상태 확인 및 자동 정정.  
   - 키워드: `Configuration Management`, `Compliance`, `Inventory`

5. **시스템 모니터링 및 보고 기능**  
   - 설명: Systems Manager는 리소스 상태를 실시간으로 모니터링하고, 보고서를 생성해 운영 팀에게 제공합니다. 이 기능은 시험에서 '모니터링' 또는 '보안 규정 준수'와 관련된 문제로 등장합니다.  
   - 키워드: `Monitoring`, `Reporting`, `Compliance`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | Systems Manager와 EC2 Auto Scaling의 차이를 혼동할 수 있음 | Systems Manager는 자동화/구성 관리를, EC2 Auto Scaling은 인스턴스 수를 조절하는 기능임 |
| 함정 2 | State Manager의 State Execution과 Automation의 차이를 헷갈릴 수 있음 | State Manager는 단일 작업 수행, Automation은 복잡한 워크플로우를 지원함 |
| 함정 3 | 비용 최적화 전략 중 Reserved Instances와 Spot Fleet의 혼동 | Reserved Instances는 예약 비용으로 고정 비용 절감, Spot Fleet는 유연한 비용 절감 |

#### 🔄 Systems Manager vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | Systems Manager | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 인프라 자동화, 구성 관리, 패치 관리 | EC2 Auto Scaling, Lambda | 리소스 상태 모니터링 및 정규화를 요구할 때 |
| 확장성 | 대규모 인프라에 적합 (예: 수만 개 인스턴스 관리) | Lambda는 단일 작업 처리에 적합 | 복잡한 자동화 흐름이 필요한 경우 |
| 비용 | 리소스 사용 패턴 분석을 통한 비용 최적화 | EC2 Spot Fleet | 예약된 리소스 사용 시 비용 절감 |
| 지연시간 | State Manager는 실시간 모니터링 가능 | Lambda는 함수 실행 시간에 따라 변동 | 즉시 상태 확인이 필요한 경우 |

#### 📝 시험 대비 체크리스트
- [ ] Systems Manager의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  (예: "리소스 상태를 모니터링하고 자동화 작업을 수행하여 운영 효율성을 높이는 도구")
- [ ] Systems Manager를 선택해야 하는 시나리오를 알고 있는가?  
  (예: "패치 관리, 구성 일관성 유지, 비용 분석이 필요한 환경")
- [ ] Systems Manager의 제한사항/한계를 알고 있는가?  
  (예: "리소스 수 제한, 특정 플랫폼 지원 제한, 복잡한 워크플로우 처리 어려움")
- [ ] Systems Manager와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  (예: "EC2 Auto Scaling은 인스턴스 수 조절, Systems Manager는 상태 관리")
- [ ] Systems Manager의 비용 구조를 이해하고 있는가?  
  (예: "예약된 인스턴스 사용 시 비용 절감, 리소스 사용량에 따른 요금")

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Systems Manager를 떠올리세요:  
> - **State Manager**  
> - **Patch Management**  
> - **Configuration Compliance**  
> - **Cost Optimization**  
> - **Inventory Reporting**

---

| [⬅️ CDK](./CDK.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 4](../README.md) | [Well-Architected Framework ➡️](./Well-Architected-Framework.md) |

---
