---

| [⬅️ CloudTrail](./CloudTrail.md) | [📑 Day 4 목차](./README.md) | [🏠 Week 4](../README.md) | [OpenSearch ➡️](./OpenSearch.md) |

---

# Athena 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** Athena는 AWS의 **서버리스 방식의 데이터 분석**을 위한 **S3에 저장된 데이터를 SQL로 쿼리**하는 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 데이터 저장소가 분산되어 있어 통합 분석이 어려웠습니다. 예를 들어, S3에 저장된 로그 파일을 분석하려면 별도의 ETL 프로세스가 필요했고, 이는 복잡한 인프라와 시간이 소요되었습니다.  
- **문제 2:** 데이터 분석을 위한 비용이 높아 작은 규모 프로젝트에서는 부담이었습니다. 전통적인 데이터 웨어하우스나 클러스터 기반 분석 도구는 초기 투자 비용이 컸습니다.  
- **문제 3:** 실시간 또는 거의 실시간의 데이터 분석이 불가능했습니다. 로그 분석이나 이벤트 트래킹은 대규모 스토리지와 복잡한 처리 로직을 요구했습니다.  

**Athena로 해결:**
- **해결 1:** S3에 저장된 데이터를 SQL 쿼리로 직접 분석할 수 있어 데이터 통합이 간소화됩니다. 예를 들어, 로그 파일을 직접 쿼리해 실시간으로 분석할 수 있습니다.  
- **해결 2:** 서버리스 구조로 인해 인프라 설정이 필요 없고, 쿼리당 요금제로 작은 규모 프로젝트도 부담없이 사용할 수 있습니다.  
- **해결 3:** 실시간 분석을 위해 CloudWatch Logs Insights와 연동해 이벤트 기반 분석이 가능하며, X-Ray로 성능 트레이스도 수행할 수 있습니다.  

### 비유로 이해하기
Athena는 도서관의 온라인 검색 시스템에 비유할 수 있습니다. 기존에는 도서관에 직접 가서 책을 찾아야 했지만, Athena는 S3에 저장된 데이터를 SQL 쿼리로 직접 검색할 수 있어, 도서관의 모든 책을 실시간으로 탐색할 수 있습니다. 이처럼 데이터를 직접 접근해 분석할 수 있어 효율적입니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 로그 분석 및 모니터링 | AWS CloudWatch Logs에서 발생한 에러 로그를 Athena로 쿼리해 사용자 행동 패턴 분석 |
| 시나리오 2 | 데이터 탐색 및 보고서 작성 | S3에 저장된 CSV/JSON 파일을 SQL로 분석해 실시간 보고서 생성 |
| 시나리오 3 | 실시간 분석 및 이벤트 트래킹 | X-Ray와 연동해 애플리케이션의 API 호출 트레이스를 분석해 성능 최적화 |

**이럴 때 Athena를 선택하세요:**
- ✅ **데이터 저장소가 S3에 분산되어 있는 경우**  
- ✅ **단기적인 분석 프로젝트나 예산이 제한된 경우**  
- ✅ **실시간 분석이 필요한 로그/이벤트 데이터 처리 시**  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **대규모 데이터 웨어하우스 분석** → **Redshift/BigQuery** (고성능 배치 처리 필요)  
- ❌ **지속적인 데이터 스트리밍 분석** → **Kinesis Firehose + Redshift** (실시간 처리 및 저장 필요)  
- ❌ **복잡한 데이터 웨어하우스 아키텍처** → **Snowflake** (데이터 모델링 및 스키마 관리 필요)  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **Amazon S3** | 데이터 저장소로 사용해 Athena에서 쿼리 가능 | User → Application → S3 → Athena |
| **CloudWatch Logs** | 로그 데이터를 S3에 저장해 Athena로 분석 | CloudWatch Logs → S3 → Athena |
| **X-Ray** | 쿼리 성능을 분석하고 트레이스 정보를 제공 | Athena → X-Ray → Dashboard |
| **OpenSearch** | 분석 결과를 시각화 및 검색 기능 제공 | Athena → OpenSearch → Kibana |

**자주 사용되는 아키텍처 패턴:**
```
User → Application → CloudFront → S3 → Athena → X-Ray → OpenSearch → Kibana
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **쿼리 비용** | $5.00 ~ $10.00 / 1GB 쿼리 | 월 5GB 무료 (12개월) |
| **데이터 스캔 비용** | $0.01 ~ $0.10 / GB | 1GB 무료 (12개월) |
| **X-Ray 연동 비용** | $0.01 ~ $0.05 / 1MB | 항상 무료 |

**비용 최적화 팁:**
1. 💡 **S3 버킷 정책으로 쿼리 권한 제한**: 특정 사용자만 데이터를 분석할 수 있도록 제어해 불필요한 스캔을 방지합니다.  
2. 💡 **쿼리 성능 최적화**: `SELECT *` 대신 필요한 컬럼만 선택하고, 파티셔닝된 데이터를 활용해 스캔량을 줄입니다.  
3. 💡 **프리티어 활용**: 초기 시범 운영 시 5GB 무료 쿼리 할당량을 사용해 비용을 절약합니다.  

> **⚠️ 비용 주의:** 대규모 데이터 스캔 시 쿼리 비용이 급증할 수 있으므로, `Caching` 및 `Partitioning`을 활용해 성능과 비용을 균형 있게 관리해야 합니다.

## 📚 핵심 개념

### 개념 1: **CloudWatch Metrics and Alarms**
CloudWatch는 AWS 서비스의 성능, 가용성, 리소스 사용량을 실시간으로 모니터링하는 기능입니다. Metrics는 CPU 사용률, 네트워크 트래픽, 데이터베이스 응답 시간 등 다양한 메트릭을 수집하고, Alarms는 특정 임계값을 초과할 때 알림을 보내는 기능입니다. 이는 시스템 이상을 사전에 감지하고, 자동화된 조치(예: EC2 인스턴스 스케일링)를 수행하는 데 필수적입니다.

#### 왜 중요한가?
- **실시간 모니터링**: 시스템 성능 향상과 오류 예방에 기여합니다.  
- **자원 최적화**: 비정상적인 리소스 사용을 감지해 비용 절감에 도움을 줍니다.  

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| Metrics | AWS 리소스의 수치적 성능 지표 | CPU 사용률(%) |
| Alarms | 특정 메트릭이 임계값을 넘을 때 알림 | CPU 사용률 90% 초과 시 알림 |
| Dashboards | 다양한 메트릭을 시각화한 대시보드 | EC2 인스턴스 성능 모니터링 대시보드 |

> **💡 Tip:** Athena에서 로그 분석 시 CloudWatch Metrics와 연동해 데이터 전송량을 모니터링하는 것이 좋습니다. 예: 로그 저장 비용이 급증할 경우 알림을 설정해 비용 절감을 도모할 수 있습니다.

---

### 개념 2: **CloudWatch Logs Insights Query Syntax**
CloudWatch Logs Insights는 로그 데이터를 분석하기 위한 고급 쿼리 언어입니다. 이는 정규표현식과 SQL 유사한 문법을 사용해 로그에서 특정 패턴, 통계, 시계열 데이터를 추출합니다. 예를 들어, 404 오류 발생 횟수나 특정 IP 주소의 요청 패턴을 분석할 수 있습니다.

#### 작동 원리
1. **로그 수집**: EC2, Lambda, CloudTrail 등의 로그를 S3에 저장합니다.  
2. **쿼리 작성**: `fields`, `filter`, `stats` 등의 명령어로 데이터 필터링 및 통계를 계산합니다.  
3. **결과 시각화**: 대시보드나 Athena에서 분석 결과를 활용해 시각화합니다.  

> **💡 Tip:** 로그 분석 시 `stats` 명령어를 사용해 특정 시간대의 요청 수를 집계하고, `filter`로 이상치를 제거해 정확한 분석을 수행하세요.

---

### 개념 3: **X-Ray Tracing and Sampling**
X-Ray는 분산 시스템에서 요청 흐름을 추적하는 도구입니다. 요청이 여러 AWS 서비스(예: EC2, Lambda, DynamoDB)를 거치는 경우, X-Ray는 요청의 경로와 각 서비스에서의 처리 시간, 오류 등을 추적해 디버깅과 성능 최적화에 도움을 줍니다. Sampling은 트래픽이 많을 때 일부 요청만 추적해 성능 영향을 줄입니다.

#### 주요 특징
1. **분산 추적**: 마이크로서비스 아키텍처에서 요청 흐름을 시각화합니다.  
2. **샘플링 정책**: 트래픽량에 따라 추적 대상 요청을 조절해 리소스 사용량을 최적화합니다.  
3. **서비스 통합**: AWS 서비스와 비-AWS 서비스 모두에서 추적을 지원합니다.  

> **💡 Tip:** Athena에서 로그 분석 시 X-Ray의 추적 ID를 활용해 특정 요청의 전체 흐름을 분석해 디버깅 효율을 높일 수 있습니다. 예: Lambda 함수에서 발생한 오류를 X-Ray 추적 ID로 연결해 원인을 파악합니다.

## 🖥️ AWS 콘솔에서 Athena 사용하기

### Step 1: Athena 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
2. 상단 검색창에 "Athena"를 입력하고, 결과에서 "Athena"를 클릭합니다  
3. "Athena" 메뉴에서 "Query editor"를 선택해 쿼리 편집기로 이동합니다  

> **📸 화면 확인:**  
> - **Athena 대시보드**에 "Query editor"가 표시되면 정상입니다.  
> - **"Query editor" 화면**에서 "Create query" 버튼이 보이는지 확인하세요.  

---

### Step 2: [주요 작업 1 - 로그 분석 환경 구성]  
1. **CloudWatch Logs 설정**  
   - **"Services" > "CloudWatch" > "Logs"**로 이동하여 로그 그룹 생성  
   - **Log group 이름**: `my-app-logs`  
   - **Log stream**: 애플리케이션 로그를 수집할 스트림을 설정  
2. **Athena 데이터 소스 연결**  
   - **Athena 콘솔 > "Data sources" > "Add data source"**  
   - **"CloudWatch Logs"**를 선택하고, 생성한 로그 그룹을 선택  
   - **"Create"** 버튼 클릭 후 데이터 소스 연결 완료  

> **📸 화면 확인:**  
> - **CloudWatch Logs**에서 로그 그룹이 생성되었는지 확인합니다.  
> - **Athena 데이터 소스** 목록에 `my-app-logs`가 추가된 상태인지 확인하세요.  

3. **X-Ray 통합 설정**  
   - **Athena 콘솔 > "Data sources" > "Add data source"**  
   - **"AWS X-Ray"**를 선택하고, **"Create"** 버튼 클릭  
   - X-Ray 데이터를 Athena 쿼리에 활용할 수 있도록 설정  

> **⚠️ 주의:**  
> - CloudWatch Logs와 X-Ray는 별도의 서비스 요금이 발생할 수 있으므로, **프리티어 사용 여부**를 확인하세요.  

---

### Step 3: [주요 작업 2 - CloudWatch 대시보드 및 경보 설정]  
1. **CloudWatch 대시보드 생성**  
   - **"Services" > "CloudWatch" > "Dashboards"**로 이동  
   - **"Create dashboard"** 클릭 후 이름 `my-app-metrics` 입력  
   - **"Add metric"** 버튼 클릭 후 **Athena 쿼리 결과**를 기반으로 메트릭 추가  
     - 예: `SELECT COUNT(*) FROM my-app-logs WHERE log_level = 'ERROR'`  
2. **경보 설정**  
   - **CloudWatch > Alarms > Create alarm**  
   - **"Metric"** 필드에서 **Athena 쿼리 결과**를 선택  
   - **"Threshold"** 설정: 예) 10개 이상의 오류 로그 발생 시 경보  
   - **"Notification"** 설정: SNS 토픽 또는 이메일 주소 입력  
3. **OpenSearch 연동**  
   - **Athena 쿼리 결과를 OpenSearch에 저장**  
   - **"Athena > Query results > Save results"**  
   - **"OpenSearch"**를 선택하고, 인덱스 이름 및 설정 입력  

> **📸 화면 확인:**  
> - **CloudWatch 대시보드**에 `my-app-metrics`가 생성되었는지 확인합니다.  
> - **경보 설정** 시 알림이 제대로 설정되었는지 확인하세요.  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인 방법**  
   - **Athena > Data sources**에서 `my-app-logs` 및 X-Ray 데이터 소스가 존재하는지 확인  
   - **CloudWatch > Dashboards**에서 `my-app-metrics` 대시보드가 표시되는지 확인  
2. **상태 확인 방법**  
   - **Athena > Query history**에서 쿼리 실행 결과를 확인  
   - **CloudWatch > Alarms**에서 경보 상태가 "OK" 또는 "ALERT"인지 확인  
3. **정상 동작 테스트**  
   - **Athena 쿼리 실행**:  
     ```sql
     SELECT COUNT(*) FROM my-app-logs WHERE log_level = 'ERROR';
     ```
   - **CloudWatch 경보 확인**:  
     - 오류 로그가 10개 이상 발생하면 경보가 트리거되는지 확인  
   - **OpenSearch 검색**:  
     - OpenSearch 콘솔에서 `my-app-logs` 인덱스를 검색해 결과가 저장되었는지 확인  

> **💡 Tip:**  
> - **비용 최적화**를 위해 Athena 쿼리 시 `LIMIT` 또는 `WHERE` 조건을 사용해 데이터량을 줄이세요.  
> - **프리티어 사용 시**, 100만 개 이하의 로그 분석 및 5GB 이하의 데이터 처리가 가능합니다.  

> **📌 실습 예시:**  
> 1. **오류 로그 분석**:  
>    ```sql
>    SELECT COUNT(*), log_level FROM my-app-logs GROUP BY log_level;
>    ```
> 2. **경보 설정 예시**:  
>    - 메트릭: `Athena query result`  
>    - 경보 조건: `COUNT(*) > 10`  
> 3. **X-Ray 추적**:  
>    - `SELECT * FROM xray_traces WHERE trace_id = '123456'`  

> **⚠️ 주의:**  
> - **CloudWatch Logs**는 로그 저장 비용이 발생하므로, **로그 보존 기간**을 설정해 비용을 관리하세요.  
> - **Athena 쿼리**는 스토리지 비용이 발생하므로, **데이터 분할** 및 **아카이브 전략**을 고려하세요.

## ⌨️ AWS CLI로 Athena 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**⚠️ 주의:** Athena CLI 명령어는 `aws athena` 명령어로 시작됩니다. CLI 버전이 1.16.300 이상이어야 Athena 기능 사용이 가능합니다.

---

### 예제 1: Athena 리소스 조회
```bash
# [Athena 작업 그룹 목록 조회]
aws athena list-workgroups --query 'WorkGroups[*].Name' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 결과 필터링 | `'WorkGroups[*].Name'` |
| `--output` | 출력 형식 | `json`, `table`, `text` |

**예상 출력:**
```
| Name       |
+------------+
| default    |
| analytics  |
| reporting  |
```

> **💡 Tip:** `list-workgroups`는 현재 리전의 작업 그룹을 조회합니다. `--region` 파라미터로 리전 변경 가능합니다.

---

### 예제 2: Athena 리소스 생성
```bash
# [Athena 작업 그룹 생성]
aws athena create-workgroup \
    --name "analytics-team" \
    --description "Analytics team's query workgroup" \
    --storage-location "s3://my-bucket/athena-results/" \
    --recursive-creation-enabled
```

**필수 옵션:**
- `--name`: 작업 그룹 이름 (1~128자, 특수문자 제외)
- `--storage-location`: 결과 저장소 S3 경로 (필수)

**예상 출력:**
```json
{
    "WorkGroup": {
        "Name": "analytics-team",
        "State": "ENABLED",
        "Description": "Analytics team's query workgroup",
        "StorageLocation": "s3://my-bucket/athena-results/",
        "RecursiveCreationEnabled": true
    }
}
```

> **💡 Tip:** `--recursive-creation-enabled` 옵션은 S3 경로 하위 폴더 자동 생성 기능입니다.

---

### 예제 3: Athena 리소스 수정
```bash
# [Athena 작업 그룹 업데이트]
aws athena update-workgroup \
    --name "analytics-team" \
    --description "Analytics team's query workgroup (updated)"
```

**수정 가능한 옵션:**
- `--description`: 작업 그룹 설명 수정
- `--storage-location`: 저장소 경로 변경 (이전 경로 삭제됨)

> **⚠️ 주의:** 저장소 경로 변경 시 이전 데이터는 삭제됩니다. 백업 필수입니다.

---

### 예제 4: Athena 리소스 삭제
```bash
# [Athena 작업 그룹 삭제]
aws athena delete-workgroup --name "analytics-team"

# 삭제 확인
aws athena describe-workgroup --name "analytics-team"
```

**삭제 후 출력 예시:**
```
{
    "WorkGroup": {
        "Name": "analytics-team",
        "State": "DELETING"
    }
}
```

> **⚠️ 주의:** 삭제는 즉시 완료되며, 복구 불가능합니다. `--force` 파라미터 없이 실행 시 확인 메시지 표시됩니다.

---

### 자주 사용하는 명령어 정리
```bash
# 작업 그룹 관리
aws athena list-workgroups
aws athena describe-workgroup --name "workgroup-name"
aws athena create-workgroup --name "new-workgroup"
aws athena update-workgroup --name "workgroup-name" --description "new-desc"
aws athena delete-workgroup --name "workgroup-name"

# 쿼리 실행 (CLI 직접 실행)
aws athena start-query-execution \
    --query-string "SELECT * FROM my_table LIMIT 10" \
    --result-configuration "OutputLocation=s3://my-bucket/athena-results/"
```

**💡 추가 팁:**  
- `aws athena get-query-execution --query-execution-id`로 쿼리 결과 확인 가능  
- `--region` 파라미터로 리전 변경 (예: `--region us-west-2`)  
- `--output text`으로 텍스트 형식 출력 가능 (파싱 용이)  

**🚨 비용 주의사항:**  
- Athena는 쿼리 실행 시 S3 저장소에 데이터 저장 비용 발생  
- `--storage-location` 설정 시 S3 버킷 생성 필수 (AWS 계정 기본 버킷 사용 불가)  
- 프리티어는 500MB 쿼리 결과 저장소 제공 (S3 버킷 생성 시 자동 할당)

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Athena 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **Athena와 S3의 통합 분석**  
   - 설명: Athena은 S3에 저장된 데이터를 SQL 쿼리로 분석할 수 있는 서버리스 서비스로, 로그 분석 및 실시간 데이터 처리 시 핵심 기술입니다. 시험에서는 S3에 저장된 로그 파일을 Athena으로 처리하는 구조, 쿼리 성능 최적화 방법, 데이터 스키마 정의 방식 등을 주요 내용으로 출제합니다.  
   - 키워드: `S3`, `서버리스`, `SQL 쿼리`, `로그 분석`

2. **비용 모델 및 비용 최적화 전략**  
   - 설명: Athena은 "pay-per-query" 모델로, 쿼리 실행 시 데이터 스캔량에 따라 비용이 발생합니다. 시험에서는 데이터 스캔 최소화 기법(예: 파티션 최적화, 데이터 형식 최적화), 쿼리 성능 개선을 통한 비용 절감, 프리티어 사용법 등이 주요 포인트입니다.  
   - 키워드: `pay-per-query`, `데이터 스캔`, `파티션 최적화`, `프리티어`

3. **Athena의 제한사항 및 한계**  
   - 설명: Athena은 실시간 처리, 복잡한 데이터 변환, 대규모 데이터 처리에 적합하지 않습니다. 시험에서는 "Athena가 적합하지 않은 시나리오"를 구분하는 문제(예: 실시간 분석, 데이터 워크로드 처리)가 자주 출제됩니다.  
   - 키워드: `실시간 처리`, `데이터 워크로드`, `복잡한 변환`, `성능 한계`

4. **Athena와 Redshift/EMR의 차이점**  
   - 설명: Athena은 서버리스로 클러스터 관리가 필요 없지만, Redshift나 EMR은 클러스터 기반의 관리형 데이터 워크스페이스입니다. 시험에서는 "데이터 분석 작업에 어떤 서비스를 선택해야 하는가"를 판단하는 문제에 집중합니다.  
   - 키워드: `서버리스 vs 클러스터`, `관리 부담`, `성능 vs 비용`, `데이터 스토리지`

5. **Athena의 쿼리 성능 최적화 방법**  
   - 설명: Athena은 쿼리 성능에 따라 데이터 스캔량이 달라지므로, 파티션 최적화, 데이터 형식 정의(예: Parquet), 메타데이터 관리 등을 통해 성능을 개선해야 합니다. 시험에서는 이러한 최적화 전략을 구체적으로 묻는 경우가 많습니다.  
   - 키워드: `파티션`, `데이터 형식`, `메타데이터`, `쿼리 성능`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "Athena는 실시간 데이터 처리에 적합한가?" → **아니요** (Athena는 대규모 데이터 분석에 적합, 실시간 처리에는 적합하지 않음) | **Athena는 대규모 데이터 분석에 적합** |
| 함정 2 | "Athena의 쿼리 비용은 데이터 저장량에 따라 결정되나?" → **아니요** (데이터 스캔량에 따라 비용이 발생) | **Athena는 데이터 스캔량에 따라 비용이 발생** |
| 함정 3 | "Athena는 데이터 저장소를 직접 제공하나?" → **아니요** (S3에 데이터를 저장하고 Athena가 쿼리 처리) | **Athena는 S3에 저장된 데이터를 쿼리** |

#### 🔄 Athena vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | Athena | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | S3에 저장된 데이터를 SQL로 분석하는 서버리스 서비스 | Redshift(클러스터 기반 관리형 데이터 워크스페이스), EMR(클러스터 기반 대규모 처리) | **실시간 분석이 필요하지 않은, 비용 효율적인 분석** |
| 확장성 | 자동 스케일링, 클러스터 관리 필요 없음 | Redshift/EMR은 클러스터 관리 필요 | **자원 관리 부담을 줄이고자 할 때** |
| 비용 | "pay-per-query" 모델 (데이터 스캔량에 따라 비용) | Redshift는 Reserved/On-Demand 클러스터 비용, EMR는 컴퓨팅 리소스 기반 비용 | **데이터 분석 비용을 최소화하고자 할 때** |
| 지연시간 | 쿼리 실행 시간은 데이터 크기와 관련 | Redshift/EMR은 클러스터 성능에 따라 변동 | **데이터 분석 결과의 지연 시간을 최소화해야 할 때** |

#### 📝 시험 대비 체크리스트
- [ ] Athena의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  (예: "S3에 저장된 데이터를 SQL 쿼리로 분석하는 서버리스 서비스")  
- [ ] Athena를 선택해야 하는 시나리오를 알고 있는가?  
  (예: "대규모 데이터 분석, 로그 분석, 비용 효율성")  
- [ ] Athena의 제한사항/한계를 알고 있는가?  
  (예: "실시간 처리, 복잡한 데이터 변환, 대규모 데이터 처리에 적합하지 않음")  
- [ ] Athena와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  (예: "Redshift는 클러스터 기반 관리형 서비스, Athena는 서버리스 서비스")  
- [ ] Athena의 비용 구조를 이해하고 있는가?  
  (예: "데이터 스캔량에 따라 비용, 프리티어 사용 가능")

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Athena를 떠올리세요:  
> - **S3** (Athena는 S3에 데이터를 저장하고 분석)  
> - **서버리스** (Athena는 관리되는 서비스로 클러스터 설정 필요 없음)  
> - **SQL 쿼리** (Athena는 SQL을 사용하여 데이터 분석)  
> - **로그 분석** (Athena는 로그 파일 분석에 널리 사용됨)  
> - **비용 효율성** (Athena는 "pay-per-query" 모델로 비용 최적화 가능)

---

| [⬅️ CloudTrail](./CloudTrail.md) | [📑 Day 4 목차](./README.md) | [🏠 Week 4](../README.md) | [OpenSearch ➡️](./OpenSearch.md) |

---
