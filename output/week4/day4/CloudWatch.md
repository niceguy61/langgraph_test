---

| [⬅️ 이전 Day](../day3/README.md) | [📑 Day 4 목차](./README.md) | [🏠 Week 4](../README.md) | [X-Ray ➡️](./X-Ray.md) |

---

# CloudWatch 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** CloudWatch는 시스템 및 애플리케이션의 성능, 가용성, 및 리소스 사용량을 모니터링하고 분석하기 위한 AWS의 통합 모니터링 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 전통적인 모니터링 도구는 별도의 인프라로 운영되며, 데이터 통합이 어려웠습니다. 예를 들어, EC2 로그는 CloudWatch, S3 로그는 다른 서비스로 관리해 데이터 분석이 복잡했습니다.
- **문제 2:** 실시간 성능 모니터링이 불가능해, 애플리케이션 지연이나 서버 과부하가 발생했을 때 대응이 지연되었습니다.
- **문제 3:** 로그 분석 시 SQL 같은 복잡한 쿼리가 필요해, 비전문가도 쉽게 분석할 수 없었습니다.

**CloudWatch로 해결:**
- **해결 1:** 통합된 메트릭, 로그, 경보 기능으로 단일 플랫폼에서 실시간 모니터링이 가능합니다. 예를 들어, EC2 CPU 사용량과 로그 데이터를 동시에 분석할 수 있습니다.
- **해결 2:** 경보 시스템을 통해 특정 임계값을 초과하면 자동 알림이 발송되어, 문제 발생 시 신속히 대응할 수 있습니다.
- **해결 3:** CloudWatch Logs Insights의 파이썬 기반 쿼리 언어로 복잡한 로그 분석이 가능해, 개발자와 운영팀이 쉽게 데이터를 해석할 수 있습니다.

### 비유로 이해하기
**CloudWatch는 집안의 스마트 홈 시스템입니다.** 집의 온도, 보안 카메라, 전력 소비량을 한눈에 모니터링할 수 있는 센서와 통신 장치가 연결되어 있습니다. 예를 들어, 온도가 너무 높으면 자동으로 에어컨을 가동시키거나, 불량한 장치가 작동하면 알림을 보내는 방식입니다. 이처럼 CloudWatch는 AWS 인프라의 모든 요소를 하나의 시스템으로 관리해 문제를 사전에 방지하고, 효율적인 운영을 도와줍니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | EC2 인스턴스의 CPU 사용량 및 네트워크 트래픽 모니터링 | AWS에서 운영하는 웹 서버의 성능 지표를 실시간으로 확인 |
| 시나리오 2 | 로그 분석을 통한 애플리케이션 오류 추적 | 사용자 요청에 따른 에러 로그를 CloudWatch Logs Insights로 분석해 원인 파악 |
| 시나리오 3 | 서비스 가용성 모니터링 및 자동 회복 | 랜덤한 서버 다운 시 경보를 발송해 운영자에게 알림 및 복구 조치 |

**이럴 때 CloudWatch를 선택하세요:**
- ✅ **실시간 성능 모니터링이 필요한 경우**
- ✅ **다양한 리소스의 통합 모니터링이 필요한 경우**
- ✅ **로그 분석을 통한 문제 진단이 필요한 경우**

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **단순한 네트워크 모니터링 → CloudWatch Agent 대신 **CloudWatch Synthetics** 사용**
- ❌ **대규모 로그 저장 → **S3 + CloudWatch Logs** 대신 **AWS Glue + Athena** 사용**
- ❌ **로그 검색 및 분석 → **OpenSearch Service** 대신 **CloudWatch Logs Insights** 사용**

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **S3** | 로그 데이터 저장소로 활용 | CloudWatch Logs → S3 버킷 → Athena 분석 |
| **X-Ray** | 애플리케이션 트레이스 및 성능 분석 | CloudWatch + X-Ray → 서비스 연동 |
| **Athena** | 로그 데이터의 SQL 기반 분석 | S3에 저장된 로그 → Athena 쿼리 |
| **OpenSearch** | 로그 검색 및 시각화 | CloudWatch Logs → OpenSearch 분석 |

**자주 사용되는 아키텍처 패턴:**
```
User → API Gateway → Lambda → S3 (로그 저장) → CloudWatch Logs Insights → OpenSearch (검색 및 분석)
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **CloudWatch Metrics** | $0.10/100,000 개수 | 12개월 무료 |
| **CloudWatch Logs** | $0.008/GB | 12개월 무료 |
| **CloudWatch Alarms** | $0.005/경보 | 항상 무료 |

**비용 최적화 팁:**
1. 💡 **프리티어를 활용해 초기 사용량을 최소화:** AWS 프리티어 기간 동안 무료로 사용할 수 있는 기능을 우선적으로 활용하세요.
2. 💡 **로그 데이터 압축 및 저장 기간 제한:** 로그를 S3에 저장할 때는 Gzip 압축을 사용하고, 저장 기간을 최소화해 비용 절감.
3. 💡 **경보 설정 시 요금 효율성 검토:** 필요 없는 경보를 제거하고, 임계값을 정확하게 설정해 과도한 알림 비용을 방지.

> **⚠️ 비용 주의:** 로그 데이터를 S3로 전송 시 데이터 전송 비용이 발생할 수 있으며, 사용량이 급증할 경우 CloudWatch 요금이 급증할 수 있습니다. AWS Cost Explorer를 사용해 사용량을 정기적으로 모니터링하세요.

## 📚 핵심 개념

### 개념 1: **CloudWatch Metrics and Alarms**
CloudWatch Metrics는 AWS 리소스의 성능, 가용성, 사용량 등을 실시간으로 추적하는 기능입니다. 이 메트릭은 CPU 사용률, 네트워크 트래픽, 디스크 I/O 등 다양한 파라미터를 포함하며, 메트릭 값을 기반으로 경보(Alarms)를 설정해 이상 징후를 자동 감지할 수 있습니다. 이는 시스템의 가용성과 성능을 모니터링하고, 사전에 문제를 예방하는 데 핵심적인 역할을 합니다. 

#### 왜 중요한가?
- **실시간 모니터링**: 리소스 상태를 실시간으로 추적해 문제가 발생하기 전에 대응 가능  
- **자동화된 대응**: 경보 트리거 시 SNS, Lambda 등과 연동해 자동화된 대응 프로세스 구축 가능  

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|------|---|
| **메트릭 유형** | CPU 사용률, 네트워크 전송량, 디스크 I/O 등 리소스 상태 추적 | EC2 인스턴스의 CPU 사용률 80% 이상 감시 |
| **경보 조건** | 메트릭 값이 특정 기준을 초과/미달할 때 트리거 | CPU 사용률 80% 이상 지속 시 경고 |
| **응답 액션** | SNS 알림, Lambda 함수 실행, Auto Scaling 조정 등 | 경고 발생 시 SNS로 알림 전송 |

> **💡 Tip:** 경보를 설정할 때는 **메트릭의 단위**와 **트리거 조건**을 정확히 설정해 오버/언더 측정을 방지하세요. 예: CPU 사용률은 `%` 단위로 설정해야 합니다.

---

### 개념 2: **CloudWatch Logs Insights Query Syntax**
CloudWatch Logs Insights는 로그 데이터를 분석하기 위한 강력한 쿼리 언어를 제공합니다. 이 언어는 `fields`, `filter`, `stats`, `sort` 등 다양한 기능을 통해 로그를 필터링, 집계, 시각화할 수 있도록 합니다. 특히, 대규모 로그 데이터에서 특정 패턴을 추출하거나 이상 징후를 탐지하는 데 유용합니다.

#### 작동 원리
1. **로그 수집**: CloudWatch Logs는 EC2, Lambda, Application Load Balancer 등에서 로그를 수집  
2. **쿼리 실행**: Insights 콘솔 또는 CLI로 쿼리 문을 입력해 데이터 필터링 및 분석  
3. **결과 시각화**: 쿼리 결과는 테이블, 그래프, 또는 스크린샷으로 출력 가능  

> **💡 Tip:** 복잡한 로그 분석 시 `filter`와 `stats`를 조합해 정확한 데이터를 추출하세요. 예: `filter @message like /error/`로 오류 메시지만 필터링.

---

### 개념 3: **X-Ray Tracing and Sampling**
X-Ray는 분산 시스템에서 요청의 흐름을 추적하는 도구로, 서비스 간 호출을 시각화해 성능 최적화와 문제 진단에 도움을 줍니다. **Sampling**은 트래스를 기록할 비율을 조절하는 기능으로, 모든 요청을 기록하면 성능 저하를 유발하므로, **1%~100%** 범위에서 조정할 수 있습니다.

#### 주요 특징
1. ****자동 트래스 생성****: Lambda, API Gateway, DynamoDB 등 자동으로 트래스 생성  
2. ****샘플링 전략****: 요청 수에 따라 조정 가능한 샘플링 비율 설정 (예: 1% 또는 100%)  
3. ****분산 추적****: 마이크로서비스 환경에서 요청의 전체 경로를 시각화해 문제 원인 파악  

> **⚠️ 주의:** 샘플링 비율이 낮으면 일부 트래스가 누락될 수 있으므로, **성능 분석 시 100%로 설정**해 전체 데이터를 확인하세요.

## 🖥️ AWS 콘솔에서 CloudWatch 사용하기

### Step 1: CloudWatch 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: [https://console.aws.amazon.com](https://console.aws.amazon.com)  
2. 상단 검색창에서 **"CloudWatch"**를 입력하고, 검색 결과에서 **"CloudWatch"**를 클릭합니다  
   - 메뉴 항목에서 **"Metrics"**, **"Logs"**, **"Alarms"**, **"Dashboards"** 등을 확인할 수 있습니다  

> **📸 화면 확인:** CloudWatch 대시보드가 표시되면 정상입니다.  
> - **메인 화면**에서 **"Metrics"** 또는 **"Dashboards"** 탭을 클릭해 해당 서비스로 이동합니다.  

---

### Step 2: [주요 작업 1 - 대시보드 생성]  
1. **대시보드 생성**  
   - 왼쪽 메뉴에서 **"Dashboards"** > **"Create dashboard"** 클릭  
   - **Dashboard name**에 `MyCustomDashboard` 입력 후 **"Create"** 클릭  
   - **Dashboard editor** 화면에서 **"Add widget"** > **"Metric widget"** 선택  
     - **Metric name**: `CPUUtilization`  
     - **Namespace**: `AWS/EC2`  
     - **Dimensions**: `InstanceId` 입력 (예: `i-1234567890abcdef0`)  
     - **Statistic**: `Average`  
     - **Period**: `5m`  
     - **Region**: `ap-northeast-2`  

2. **로그 기반 대시보드 추가**  
   - **"Add widget"** > **"Log search"** 선택  
   - **Log group**: `/var/log/myapp` 입력  
   - **Query**: `SELECT * FROM /var/log/myapp WHERE $message LIKE '%ERROR%'`  
   - **Time range**: `Last 1 hour`  
   - **Widget title**: `Error Logs`  

3. **확인 사항**  
   - 생성된 대시보드가 **"Dashboards"** 탭에서 표시되는지 확인  
   - 각 위젯이 올바르게 데이터를 표시하는지 확인  

> **📸 화면 확인:** 대시보드 생성 후, **"Dashboards"** 탭에서 `MyCustomDashboard`가 목록에 등록된 상태  

---

### Step 3: [주요 작업 2 - 경보 설정]  
1. **경보 생성**  
   - 왼쪽 메뉴에서 **"Alarms"** > **"Create alarm"** 클릭  
   - **Alarm name**: `CPUUtilizationAlarm` 입력  
   - **Metric**  
     - **Namespace**: `AWS/EC2`  
     - **Metric name**: `CPUUtilization`  
     - **Dimensions**: `InstanceId` 입력 (예: `i-1234567890abcdef0`)  
   - **Statistic**: `Average`  
   - **Threshold**: `80`  
   - **Period**: `5m`  
   - **Evaluation periods**: `1`  
   - **Alarm actions**  
     - **"Add action"** > **"SNS topic"** 선택  
     - **SNS topic**: `MyNotificationTopic` (미리 생성한 토픽)  

2. **경보 상태 확인**  
   - **"Alarms"** 탭에서 `CPUUtilizationAlarm`을 클릭  
   - **"History"** 탭에서 경보가 트리거되었는지 확인  
   - **"Actions"** 탭에서 알림이 전송되었는지 확인  

3. **경보 수정**  
   - **"Edit"** 버튼 클릭 후 **Threshold** 값을 `70`으로 변경  
   - **"Save"** 클릭 후 변경 사항이 반영되는지 확인  

> **⚠️ 주의:** 경보를 생성할 때는 **"Metric"** 선택 시 해당 리소스가 존재해야 합니다.  
> - EC2 인스턴스가 실행 중인지 확인하세요.  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인 방법**  
   - **"Dashboards"** 탭에서 `MyCustomDashboard` 클릭  
   - **"Alarms"** 탭에서 `CPUUtilizationAlarm` 클릭  
   - **"CloudWatch Metrics"** 탭에서 `AWS/EC2` > `CPUUtilization` 검색  

2. **상태 확인 방법**  
   - **"Alarms"** 탭에서 **"All alarms"** 필터링  
   - **"Status"** 열에서 **"OK"** 또는 **"ALARM"** 상태 확인  
   - **"History"** 탭에서 경보 발생 이력 확인  

3. **정상 동작 테스트**  
   - **EC2 인스턴스**에 CPU 부하 주입 (예: `stress-ng` 명령어 사용)  
   - **CloudWatch Metrics**에서 `CPUUtilization`이 80% 이상으로 상승하는지 확인  
   - **SNS 알림**이 전송되었는지 확인  

> **💡 Tip:** AWS Free Tier은 1년간 200시간의 CloudWatch 사용이 가능합니다.  
> - 경보 및 대시보드는 무료로 사용 가능합니다.  
> - **Cost Explorer**에서 비용을 모니터링하세요.

## ⌨️ AWS CLI로 CloudWatch 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

> **💡 Tip:** CLI를 사용하려면 먼저 [AWS CLI 설치 가이드](https://docs.aws.amazon.com/cli/latest/userguide/cli-install.html)를 참고해 설치하세요.  
> **⚠️ 주의:** CLI 명령어는 리전과 계정에 따라 결과가 달라질 수 있습니다. `aws configure set region`으로 리전을 명시적으로 설정하세요.

---

### 예제 1: CloudWatch 리소스 조회
```bash
# [CloudWatch 메트릭 목록 조회]
aws cloudwatch list-metrics \
    --namespace "AWS/EC2" \
    --query 'Metrics[*].MetricName' \
    --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--namespace` | 메트릭 네임스페이스 지정 | `"AWS/EC2"` |
| `--query` | 결과 필터링 | `'Metrics[*].MetricName'` |
| `--output` | 출력 형식 | `json`, `table`, `text` |

**예상 출력:**
```
| MetricName         |
|---------------------|
| CPUUtilization     |
| DiskReadBytes      |
| DiskWriteBytes     |
| NetworkIn          |
| NetworkOut         |
```

---

### 예제 2: CloudWatch 경보 생성
```bash
# [CPU 사용률 경보 생성]
aws cloudwatch put-metric-alarm \
    --alarm-name "HighCPUUtilization" \
    --metric-name "CPUUtilization" \
    --namespace "AWS/EC2" \
    --statistic "Average" \
    --period "300" \
    --threshold "80" \
    --comparison-operator "GreaterThanThreshold" \
    --evaluation-periods "1" \
    --alarm-actions "arn:aws:sns:region:account-id:example-sns-topic"
```

**필수 옵션:**
- `--alarm-name`: 경보 이름
- `--metric-name`: 모니터링할 메트릭 이름
- `--namespace`: 메트릭 네임스페이스
- `--threshold`: 경보 임계값
- `--comparison-operator`: 비교 연산자 (`GreaterThanThreshold`, `LessThanThreshold` 등)

**예상 출력:**
```json
{
    "AlarmArn": "arn:aws:cloudwatch:region:account-id:alarm:HighCPUUtilization",
    "AlarmName": "HighCPUUtilization",
    "StateValue": "OK",
    "StateReason": "Alarm of type CloudWatchAlarm is in OK state"
}
```

---

### 예제 3: CloudWatch 대시보드 생성
```bash
# [CloudWatch 대시보드 생성]
aws cloudwatch put-dashboard \
    --dashboard-name "MyCustomDashboard" \
    --dashboard-body '{
        "widgets": [
            {
                "type": "metric",
                "properties": {
                    "metrics": [
                        ["AWS/EC2", "CPUUtilization", "InstanceId", "i-1234567890abcdef0"]
                    ],
                    "view": "singleValue"
                }
            }
        ]
    }'
```

**필수 옵션:**
- `--dashboard-name`: 대시보드 이름
- `--dashboard-body`: JSON 형식으로 구성한 대시보드 구조

**예상 출력:**
```json
{
    "dashboardArn": "arn:aws:cloudwatch:region:account-id:dashboard/MyCustomDashboard",
    "dashboardName": "MyCustomDashboard"
}
```

---

### 예제 4: CloudWatch 경보 삭제
```bash
# [경보 삭제]
aws cloudwatch delete-alarm --alarm-name "HighCPUUtilization"

# 삭제 확인
aws cloudwatch describe-alarms --alarm-name "HighCPUUtilization"
```

> **⚠️ 주의:** 경보 삭제는 되돌릴 수 없습니다. `describe-alarms`로 삭제 전 상태를 확인하세요.

---

### 자주 사용하는 명령어 정리
```bash
# 메트릭 조회
aws cloudwatch list-metrics --namespace "AWS/EC2"
aws cloudwatch get-metric-statistics \
    --metric-name "CPUUtilization" \
    --namespace "AWS/EC2" \
    --start-time "2023-10-01T00:00:00Z" \
    --end-time "2023-10-01T01:00:00Z" \
    --period "300" \
    --statistic "Average"

# 경보 관리
aws cloudwatch put-metric-alarm ...  # 경보 생성
aws cloudwatch describe-alarms --alarm-name "AlarmName"  # 경보 조회
aws cloudwatch delete-alarm --alarm-name "AlarmName"  # 경보 삭제

# 대시보드 관리
aws cloudwatch put-dashboard ...  # 대시보드 생성
aws cloudwatch get-dashboard --dashboard-name "DashboardName"  # 대시보드 조회
```

---

### CLI 사용 팁
| 팁 | 설명 |
|-----|-----|
| **메트릭 필터링** | `--query`로 특정 메트릭만 필터링 (예: `Metrics[?MetricName=='CPUUtilization']`) |
| **시간 범위 조절** | `--start-time`과 `--end-time`으로 정확한 시간대를 지정 (UTC로 입력) |
| **경보 수정** | `update-alarm` 명령어로 기존 경보의 임계값/알림 대상 수정 |
| **대시보드 업데이트** | `put-dashboard`로 기존 대시보드의 위젯 구조 수정 |

---

### 비용 관련 주의사항
- **CloudWatch 메트릭**: 100개 이하 메트릭은 무료, 100개 이상은 0.10달러/개/월 (AWS Free Tier 포함)
- **CloudWatch Logs Insights**: 100MB/일 무료, 초과 시 0.0045달러/MB
- **경보 수 제한**: 500개 경보/월 (Free Tier 포함)

> **💡 Tip:** 프리티어 사용 시 `aws configure set region us-east-1`로 리전을 설정하고, `aws cloudwatch get-metric-statistics`로 메트릭을 무료로 확인하세요.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 CloudWatch 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **CloudWatch Metrics and Alarms**  
   - 설명: AWS 리소스의 성능 및 상태를 실시간으로 모니터링하고, 임계값을 초과할 때 경보를 발송하는 기능은 시험에서 반드시 출제됩니다. 예: CPU 사용률 90% 이상일 때 경보를 설정하는 구조와 알림 대상 설정 방법이 핵심입니다.  
   - 키워드: `Metrics`, `Alarms`, `Threshold`, `Notification`

2. **CloudWatch Logs Insights Query Syntax**  
   - 설명: 로그 데이터를 분석하기 위해 사용하는 `Filter`와 `Metric Filter`의 문법은 시험에서 빈도가 높습니다. 예: 로그에서 특정 오류 코드를 추출하거나, 특정 패턴을 기반으로 메트릭을 생성하는 방법이 중요합니다.  
   - 키워드: `Logs Insights`, `Query`, `Filter`, `Metric Filter`

3. **X-Ray Tracing and Sampling**  
   - 설명: 애플리케이션의 성능 분석을 위해 트레이스 데이터를 수집하는 X-Ray의 샘플링 전략과 트레이스 ID의 역할은 시험에서 자주 다룹니다. 예: 샘플링 비율 조절 및 트레이스 데이터의 분석 방법이 핵심입니다.  
   - 키워드: `X-Ray`, `Tracing`, `Sampling`, `Trace ID`

4. **Athena Log Analysis with S3**  
   - 설명: S3에 저장된 로그 데이터를 Athena로 분석하는 과정에서 사용하는 `Partitioning` 및 `Query Optimization` 기법은 시험에서 빈도가 높습니다. 예: 로그 파일의 경로 구조와 Athena 쿼리 최적화 전략이 핵심입니다.  
   - 키워드: `Athena`, `S3`, `Partitioning`, `Query Optimization`

5. **OpenSearch Log Search and Analytics**  
   - 설명: 실시간 로그 검색 및 분석을 위해 OpenSearch를 사용하는 시나리오에서의 `Indexing`, `Search Query`, `Analytics` 기능은 시험에서 자주 다룹니다. 예: 로그 데이터를 OpenSearch에 저장하고 실시간으로 분석하는 구조가 핵심입니다.  
   - 키워드: `OpenSearch`, `Search`, `Analytics`, `Indexing`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | CloudWatch Metrics와 Logs를 혼동해 경보 설정 시 오류 발생 | CloudWatch Metrics는 리소스의 성능 지표, Logs는 로그 데이터를 분석하는 기능으로 구분해야 함 |
| 함정 2 | X-Ray의 Sampling Rate 조절 시 기본값을 모르고 오류 발생 | X-Ray의 기본 샘플링 비율은 10%로, 사용자는 이를 조정해 트레이스 데이터량을 제어해야 함 |
| 함정 3 | Athena로 S3 로그 분석 시 Partitioning을 생략해 쿼리 성능 저하 | Partitioning을 통해 데이터 분할해 쿼리 속도를 향상시켜야 함 |

#### 🔄 CloudWatch vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | CloudWatch | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 리소스 성능 모니터링, 로그 수집 및 분석 | CloudTrail(API 로그), Athena(데이터 분석), OpenSearch(실시간 검색) | 실시간 모니터링 및 경보가 필요한 경우 |
| 확장성 | 리소스 수에 따라 자동 확장 | Athena는 S3 데이터 처리량에 따라 확장 | 대규모 로그 분석 시 Athena의 유연성 |
| 비용 | 메트릭/로그 수집 시 요금, 경보 기능 무료 | Athena는 쿼리 시간에 따라 요금, OpenSearch는 사용량 기반 요금 | 로그 분석 비용 최적화 시 Athena |
| 지연시간 | 실시간 모니터링 | OpenSearch는 실시간 검색, Athena는 쿼리 대기 시간 있음 | 즉시 분석이 필요한 경우 OpenSearch |

#### 📝 시험 대비 체크리스트
- [ ] CloudWatch의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  (예: "AWS 리소스의 성능 및 상태를 실시간으로 모니터링하고 경보를 발송하는 서비스")
- [ ] CloudWatch를 선택해야 하는 시나리오를 알고 있는가?  
  (예: "CPU 사용률 90% 이상일 때 경보를 설정해야 할 경우")
- [ ] CloudWatch의 제한사항/한계를 알고 있는가?  
  (예: "메트릭 수집 주기 최소 1분, 로그 분석 시 데이터 저장소 필요")
- [ ] CloudWatch와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  (예: "CloudTrail은 API 로그, Athena은 데이터 분석, OpenSearch는 실시간 검색")
- [ ] CloudWatch의 비용 구조를 이해하고 있는가?  
  (예: "메트릭/로그 수집 요금, 경보 기능 무료, 대시보드 생성 비용 포함")

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 CloudWatch를 떠올리세요:  
> - **Metrics** (성능 지표)  
> - **Alarms** (경보 설정)  
> - **Logs Insights** (로그 분석)  
> - **Tracing** (X-Ray 트레이스)  
> - **Real-time Monitoring** (실시간 모니터링)

---

| [⬅️ 이전 Day](../day3/README.md) | [📑 Day 4 목차](./README.md) | [🏠 Week 4](../README.md) | [X-Ray ➡️](./X-Ray.md) |

---
