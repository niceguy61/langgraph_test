---

| [⬅️ Athena](./Athena.md) | [📑 Day 4 목차](./README.md) | [🏠 Week 4](../README.md) | [다음 Day ➡️](../day5/README.md) |

---

# OpenSearch 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** OpenSearch는 검색 및 분석을 위한 AWS의 managed search 및 analytics 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 전통적인 온프레미스 검색 솔루션은 고비용의 하드웨어 구매와 유지보수, 확장성 제한으로 인해 비용이 높고 복잡합니다.  
- **문제 2:** 로그 분석을 위해 수동으로 파일을 처리하거나 외부 도구를 사용해야 하며, 실시간 분석이 불가능한 한계가 있습니다.  
- **문제 3:** 복잡한 트래픽 패턴이나 분석 요구사항에 대응하기 위해 개발자들이 직접 로직을 구현해야 하는 번거로움이 있습니다.

**OpenSearch로 해결:**
- **해결 1:** AWS가 관리하는 클라우드 기반 검색 서비스로, 확장성과 가용성을 보장하면서도 하드웨어 관리 부담을 덜어줍니다.  
- **해결 2:** CloudWatch Logs Insights와 Athena를 통해 로그를 실시간으로 분석하고, 지표를 시각화하여 문제를 신속히 탐지할 수 있습니다.  
- **해결 3:** X-Ray와 OpenSearch의 통합을 통해 요청 트래픽을 추적하고, 분석 결과를 기반으로 서비스 개선이 가능합니다.

### 비유로 이해하기
**오픈서치는 대형 도서관의 디지털 라이브러리 및 검색 시스템을 상상해보세요.**  
사람들이 책을 찾아야 할 때, 기존에는 책장에 직접 둘러서 찾아야 했지만, OpenSearch는 검색어를 입력하면 실시간으로 관련 책을 보여주고, 분석을 통해 인기 책을 추천해주는 기능을 제공합니다.  
또한, 로그를 분석하면 도서관의 방문 패턴을 파악해 인프라를 효율적으로 운영할 수 있습니다.  
이처럼 OpenSearch는 데이터를 빠르게 검색하고, 분석하여 의사결정을 지원하는 기술입니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | **실시간 검색 및 분석이 필요한 웹 애플리케이션** | 온라인 쇼핑몰의 상품 검색 기능 구현, 사용자 행동 분석 |
| 시나리오 2 | **클라우드 기반 로그 분석 및 모니터링** | 클라우드 서버의 로그를 실시간으로 분석해 오류를 감지하고 경보 설정 |
| 시나리오 3 | **분산 시스템의 트래픽 추적 및 성능 최적화** | 마이크로서비스 아키텍처에서 요청 흐름을 추적하고, 지연 요인을 분석 |

**이럴 때 OpenSearch를 선택하세요:**
- ✅ **실시간 검색 및 분석이 필수적인 시스템**
- ✅ **클라우드 기반 로그 모니터링을 원하는 경우**
- ✅ **분산 시스템에서 트래픽을 추적해야 할 때**

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **단순한 NoSQL 데이터 저장이 필요한 경우 → DynamoDB** (OpenSearch는 검색 및 분석에 특화됨)
- ❌ **정적 파일 저장 및 액세스가 필요한 경우 → S3** (OpenSearch는 데이터 분석에 집중됨)
- ❌ **데이터베이스의 복잡한 쿼리 처리가 필요한 경우 → RDS/Redshift** (OpenSearch는 검색 및 분석에 최적화됨)

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **CloudWatch** | **실시간 지표 모니터링 및 경보 설정** | OpenSearch → CloudWatch → 대시보드 |
| **Kinesis** | **고유한 로그 데이터 스트리밍** | Kinesis Firehose → S3 → OpenSearch |
| **Athena** | **S3에 저장된 로그 분석** | S3 → Athena → OpenSearch |

**자주 사용되는 아키텍처 패턴:**
```
User → CloudFront → S3 (로그 저장) → Kinesis Firehose → OpenSearch → CloudWatch (모니터링)
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **OpenSearch 도메인 생성** | $0.25/노드/시간 (10개 노드까지) | 월 10개 노드 무료 |
| **CloudWatch 로그 저장** | $0.03/GB/월 | 12개월 무료 (1GB) |
| **X-Ray 트래킹** | $0.004/요청 | 항상 무료 (기본 기능) |

**비용 최적화 팁:**
1. 💡 **프리티어 활용:** 10개 노드까지는 무료로 사용 가능하므로, 초기 시범 운영 시에 활용하세요.  
2. 💡 **데이터 저장 최소화:** 로그는 S3에 저장하고, 필요 시만 Athena로 분석해 비용을 절감하세요.  
3. 💡 **트래킹 샘플링 설정:** X-Ray에서 10% 샘플링으로 트래픽 부담을 줄이고, 성능을 유지하세요.

> **⚠️ 비용 주의:** 로그 저장 비용이 급격히 증가할 수 있으므로, S3 버킷의 로그 저장 기간을 제한하고, CloudWatch의 로그 보존 정책을 설정해주세요.

## 📚 핵심 개념

### 개념 1: OpenSearch의 로그 모니터링과 분석
OpenSearch는 클라우드 환경에서 로그 데이터를 실시간으로 모니터링하고 분석하는 데 필수적인 역할을 합니다. CloudWatch Metrics와 Logs Insights, Athena, X-Ray 등 다양한 AWS 서비스와 연동하여 시스템의 성능, 오류, 사용 패턴을 파악할 수 있습니다. OpenSearch는 로그 데이터를 저장하고 검색할 수 있는 강력한 검색 엔진으로, 실시간 분석과 대시보드를 통해 운영 효율성을 높입니다. 이는 애플리케이션의 가용성과 성능 최적화에 직접적인 영향을 미칩니다.

#### 왜 중요한가?
- **실시간 모니터링**: 시스템의 상태를 실시간으로 파악해 문제를 빠르게 대응할 수 있습니다.
- **데이터 통합**: 다양한 서비스와의 연동으로 통합된 분석을 가능하게 하여 운영 효율성을 극대화합니다.

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|------|---|
| CloudWatch Metrics | OpenSearch의 성능 지표를 추적 (CPU, 요청량 등) | CPU 사용량이 80%를 초과하면 알림 발생 |
| CloudWatch Alarms | 지표 이상 시 자동 알림 (이메일, SNS 등) | 요청 실패율이 5% 이상일 경우 알림 |
| OpenSearch Log Search | 로그 데이터를 검색 및 분석 (全文検索, 필터링) | "error" 키워드로 필터링한 로그 조회 |

> **💡 Tip:** OpenSearch는 로그 데이터를 저장하는 데 최적화되어 있으나, 실시간 분석에선 CloudWatch Logs Insights나 Athena를 더 적합하게 활용하세요.

---

### 개념 2: CloudWatch Logs Insights의 쿼리 구문
CloudWatch Logs Insights는 로그 데이터를 분석하기 위한 강력한 쿼리 언어를 제공합니다. 이 쿼리는 필터링, 집계, 패턴 분석 등 다양한 작업을 수행할 수 있어, 로그 데이터의 의미를 추출하고 시스템의 문제를 진단하는 데 필수적입니다. 특히 OpenSearch와 연동된 로그 분석 시, 이 쿼리는 실시간으로 복잡한 패턴을 탐지할 수 있습니다.

#### 작동 원리
1. **로그 데이터 수집**: CloudWatch에서 로그를 수집하고 S3 또는 OpenSearch에 저장합니다.
2. **쿼리 작성**: `fields`, `filter`, `stats` 등 함수를 사용해 데이터를 필터링하고 집계합니다.
3. **결과 시각화**: CloudWatch 대시보드나 Athena로 분석 결과를 시각화해 운영 팀이 쉽게 이해할 수 있도록 합니다.

> **💡 Tip:** 쿼리 성능을 최적화하려면 `fields`로 필요한 필드만 선택하고, `stats` 함수로 집계 연산을 줄이는 것이 좋습니다.

---

### 개념 3: X-Ray 추적과 샘플링
X-Ray는 OpenSearch의 요청 흐름을 추적해 서비스 간 상호작용을 분석하는 데 사용됩니다. 샘플링 기능은 트레이스 데이터를 제한적으로 수집해 성능을 유지하면서도 중요한 요청만 분석할 수 있도록 합니다. 이는 고부하 환경에서 자원 사용을 최소화하면서도 핵심 문제를 파악하는 데 유용합니다.

#### 주요 특징
1. **트레이스 추적**: OpenSearch 요청이 클라우드 서비스 간 이동할 때의 경로를 추적해 문제 원인을 분석합니다.
2. **샘플링 설정**: 요청 중 일부만 추적해 자원 사용을 줄이고, 중요한 요청만 집중 분석합니다.
3. **경로 분석**: 요청이 여러 서비스를 거치는 경우, 각 단계의 성능 및 오류를 시각화해 디버깅을 단순화합니다.

> **💡 Tip:** 샘플링 비율은 워크로드에 따라 조정해야 합니다. 예를 들어, 높은 트래픽 시 1% 샘플링을 설정하고, 문제 발생 시 샘플링 비율을 100%로 올려 상세 분석을 수행하세요.

## 🖥️ AWS 콘솔에서 OpenSearch 사용하기

### Step 1: OpenSearch 서비스 접속  
1. AWS Management Console에 로그인합니다.  
   - URL: https://console.aws.amazon.com  
2. 상단 검색창에서 **"OpenSearch"**를 입력하고, 검색 결과에서 **"OpenSearch Service"**를 클릭합니다.  
   - 이 서비스는 OpenSearch 클러스터를 생성 및 관리하는 주요 인터페이스입니다.  
3. "Create domain" 버튼을 클릭하여 새 클러스터를 생성하거나, 기존 클러스터 목록에서 관리할 수 있습니다.  

> **📸 화면 확인:** "OpenSearch Service" 메인 페이지에서 "Create domain" 버튼이 표시되면 정상입니다.  
> **💡 Tip:** OpenSearch 클러스터는 AWS Regions에서 생성해야 하며, 네트워크 설정과 보안 그룹을 사전에 확인하세요.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **"Create domain" 클릭 후**  
   - **Domain name**: 클러스터 이름 입력 (예: `my-opensearch-cluster`)  
   - **Cluster type**:  
     - **Single Availability Zone (AZ)**: 비용 효율적이지만 고가용성 요구 시 추천하지 않음  
     - **Multi-AZ**: 고가용성 및 장애 터널링을 위한 선택 (추천)  
   - **Instance type**:  
     - **t3.small.search**: 프리티어 기준으로 기본 제공 (1개 인스턴스)  
     - **m5.large.search**: 성능 요구 시 선택 (비용 증가)  
   - **VPC 설정**: 기존 VPC 선택 또는 새 VPC 생성 (보안 그룹, 서브넷 설정 필수)  

2. **Advanced settings**  
   - **EBS volume type**: SSD (io1) 또는 HDD (gp2) 선택 (성능과 비용 균형 고려)  
   - **Node-to-node encryption**: 데이터 전송 시 암호화 활성화 (보안 강화)  
   - **Integrate with AWS Identity Center**: IAM 역할 연결 (권한 관리 용이)  

3. **확인 사항**  
   - **Cost**: t3.small.search는 프리티어 750시간 제공 (비용 절감)  
   - **Security**: VPC 및 보안 그룹 설정 필수 (외부 접근 차단)  
   - **Region**: 데이터 정책에 따라 적절한 리전 선택 (예: `us-east-1`)  

> **📸 화면 확인:** "Create domain" 페이지에서 "Review" 버튼을 클릭 전에 모든 설정을 확인하세요.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **CloudWatch Metrics 활성화**  
   - OpenSearch 클러스터 생성 후, **CloudWatch** > **Metrics**로 이동합니다.  
   - **OpenSearch** 섹션에서 **CPU utilization**, **Request latency**, **Throughput** 등 메트릭 확인.  

2. **CloudWatch Alarms 설정**  
   - **CloudWatch** > **Alarms** > **Create alarm**  
   - **Metric**: `OpenSearch` > `CPU utilization`  
   - **Threshold**: 80% 이상 시 경보 발령 (예: 5분간 지속 시)  
   - **Notification**: SNS 토픽 연결 (경보 알림 수신)  

3. **X-Ray 통합 설정**  
   - OpenSearch 클러스터 설정에서 **X-Ray integration** 활성화.  
   - **AWS X-Ray** > **Service integrations**에서 OpenSearch 서비스 연결.  
   - **Trace ID**를 통해 요청 트래킹 및 성능 분석 가능.  

> **⚠️ 주의:** CloudWatch Alarms는 기본적으로 비활성화되어 있으므로, 필요 시 **"Enable metrics"** 옵션을 확인하세요.  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인 방법**  
   - **OpenSearch Console** > **Domains**에서 클러스터 상태 확인 (예: `Active`, `Creating`)  
   - **CloudWatch Metrics** > `OpenSearch` 섹션에서 실시간 메트릭 확인.  

2. **상태 확인 방법**  
   - **AWS Console** > **OpenSearch** > **Domains**에서 **Status** 필드 확인.  
   - **CloudWatch Alarms**에서 경보 상태 (OK/ALARM) 확인.  

3. **정상 동작 테스트**  
   - **OpenSearch Console** > **Indices** > **_cat/indices?v**로 인덱스 목록 확인.  
   - **Test data indexing**:  
     ```bash
     curl -X POST "https://<domain-endpoint>/_bulk" -H "Content-Type: application/json" -d'
     { "index" : { "_index" : "test-index", "_id" : "1" } }
     { "data" : "test value" }
     '
     ```
   - **Query test**:  
     ```bash
     curl -X GET "https://<domain-endpoint>/test-index/_search" -H "Content-Type: application/json" -d'
     { "query": { "match_all": {} } }
     '
     ```

> **📸 화면 확인:** OpenSearch Console에서 인덱스 생성 후, CloudWatch 대시보드에서 메트릭이 실시간으로 표시되는지 확인하세요.

## ⌨️ AWS CLI로 OpenSearch 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**필수 조건:**
- AWS CLI 버전 1.18.0 이상
- OpenSearch 서비스 권한이 포함된 IAM 역할
- CloudWatch, X-Ray, Athena 서비스 접근 권한

---

### 예제 1: OpenSearch 리소스 조회
```bash
# OpenSearch 도메인 목록 조회
aws opensearch list-domains --query "[].DomainName" --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| --query | 결과 필터링 | "[].DomainName" |
| --output | 출력 형식 | json, table, text |

**예상 출력:**
```
NAME        | VALUE
-----------|-------
example-domain | example-domain
```

**💡 Tip:** `--query`를 사용해 특정 필드만 조회할 수 있습니다. 예: `"[].DomainStatus"`

---

### 예제 2: OpenSearch 리소스 생성
```bash
# OpenSearch 도메인 생성
aws opensearch create-domain \
    --domain-name "example-domain" \
    --cluster-config '{"InstanceType":"t3.medium","InstanceCount":1}' \
    --access-policies '{"AWSIdentityBased":true}'
```

**필수 옵션:**
- `--domain-name`: 도메인 이름
- `--cluster-config`: 클러스터 구성 (InstanceType, InstanceCount)
- `--access-policies`: 보안 정책

**예상 출력:**
```json
{
    "DomainStatus": {
        "DomainName": "example-domain",
        "DomainId": "example-domain-id",
        "ClusterConfig": {
            "InstanceType": "t3.medium",
            "InstanceCount": 1
        },
        "Status": "creating"
    }
}
```

**⚠️ 주의:** 클러스터 구성은 `JSON` 형식으로 전달해야 합니다.

---

### 예제 3: OpenSearch 리소스 수정
```bash
# OpenSearch 도메인 업데이트
aws opensearch update-domain \
    --domain-name "example-domain" \
    --cluster-config '{"InstanceType":"t3.large","InstanceCount":2}'
```

**필수 옵션:**
- `--domain-name`: 수정할 도메인 이름
- `--cluster-config`: 변경할 클러스터 구성

---

### 예제 4: OpenSearch 리소스 삭제
```bash
# OpenSearch 도메인 삭제
aws opensearch delete-domain --domain-name "example-domain"

# 삭제 확인
aws opensearch describe-domain --domain-name "example-domain"
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. `--region`을 명시해 리전을 확인하세요.

---

### 자주 사용하는 명령어 정리
```bash
# OpenSearch 도메인 관리
aws opensearch list-domains
aws opensearch describe-domain --domain-name "example-domain"
aws opensearch create-domain --domain-name "example-domain"
aws opensearch update-domain --domain-name "example-domain"
aws opensearch delete-domain --domain-name "example-domain"

# CloudWatch 대시보드 및 경보 설정
aws cloudwatch put-dashboard --dashboard-name "OpenSearch-Monitoring" --dashboard-body '{"widgets":[{"type":"metric","properties":{"metrics":[{"namespace":"AWS/CloudWatch","metricName":"CPUUtilization","dimensions":[{"name":"InstanceId","value":"i-1234567890"}]}]}}]}'
aws cloudwatch put-metric-alarm --alarm-name "OpenSearch-CPU-Usage" --metric-name "CPUUtilization" --namespace "AWS/CloudWatch" --dimensions "InstanceId=i-1234567890" --threshold 80 --comparison-operator "GreaterThanThreshold" --evaluation-periods 1 --alarm-actions "arn:aws:sns:region:account-id:example-topic"
```

**💡 Tip:** CloudWatch 대시보드는 JSON 형식으로 정의해야 합니다.

---

### 실무 예제: CloudWatch 대시보드 및 경보 설정
1. **CloudWatch 대시보드 생성**
   ```bash
   aws cloudwatch put-dashboard --dashboard-name "OpenSearch-Cluster-Monitoring" \
       --dashboard-body '{"widgets":[{"type":"metric","properties":{"metrics":[{"namespace":"OpenSearch","metricName":"SearchRequestRate","dimensions":[{"name":"DomainName","value":"example-domain"}]}]}}]}'
   ```

2. **CPU 사용률 경보 설정**
   ```bash
   aws cloudwatch put-metric-alarm --alarm-name "OpenSearch-CPU-Usage" \
       --metric-name "CPUUtilization" --namespace "AWS/CloudWatch" \
       --dimensions "InstanceId=i-1234567890" --threshold 80 \
       --comparison-operator "GreaterThanThreshold" --evaluation-periods 1 \
       --alarm-actions "arn:aws:sns:region:account-id:example-topic"
   ```

**💡 Tip:** `--region`을 생략하면 기본 리전에서 실행됩니다. 리전을 명시해 오류를 방지하세요.

---

### 비용 최적화 팁
- **프리티어 활용:** OpenSearch 도메인 생성 시 `--cluster-config`에서 `t3.micro` 사용
- **CloudWatch 비용:** 대시보드 및 경보 설정 시 `--region`을 `us-east-1`로 설정해 비용 절감
- **X-Ray 연동:** `--xray-enabled` 옵션으로 추적 기능 활성화

> **⚠️ 주의:** OpenSearch 도메인 생성 시 `--region`을 명시해 리전을 설정해야 합니다. 예: `--region us-east-1`

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 OpenSearch 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **CloudWatch Metrics and Alarms (클라우드워치 지표 및 경보)**  
   - 설명: OpenSearch 클러스터의 성능, 가용성, 리소스 사용량을 실시간으로 모니터링하기 위한 핵심 기능입니다. CPU 사용률, 네트워크 지연, 요청 성공률 등 메트릭을 기반으로 경보를 설정해 이상 징후를 사전에 탐지해야 합니다.  
   - 키워드: `CloudWatch Metrics`, `Alarms`, `Performance Monitoring`

2. **CloudWatch Logs Insights (클라우드워치 로그 인사이트)**  
   - 설명: OpenSearch 로그를 필터링하고 분석해 디버깅 및 트리깅을 효율화하는 기능입니다. 로그에 대한 복잡한 쿼리(예: `filter` 또는 `stats` 명령어)를 사용해 특정 패턴을 추출해야 하며, 이는 시험에서 자주 로그 분석 사례로 출제됩니다.  
   - 키워드: `Logs Insights`, `Query Syntax`, `Log Analysis`

3. **X-Ray Tracing and Sampling (X-Ray 추적 및 샘플링)**  
   - 설명: OpenSearch 요청의 실행 경로를 추적해 서비스 간 연동 문제를 진단하는 기능입니다. 샘플링 비율 조정과 추적 ID를 통한 로그 상관관계 분석은 시험에서 자주 다루는 주제입니다.  
   - 키워드: `X-Ray`, `Tracing`, `Sampling Ratio`

4. **Athena로 로그 분석 (Athena 기반 로그 분석)**  
   - 설명: S3에 저장된 OpenSearch 로그를 Athena로 분석해 비용 효율적인 데이터 처리 방식을 평가하는 문제입니다. 로그 형식 파싱, 쿼리 성능 최적화, 비용 계산 방식을 이해해야 합니다.  
   - 키워드: `Athena`, `S3 Log Analysis`, `Cost Optimization`

5. **OpenSearch Log Search and Analytics (OpenSearch 로그 검색 및 분석)**  
   - 설명: OpenSearch 자체의 Kibana 기능을 통해 로그를 시각화하고, 분석을 수행하는 방법입니다. 실시간 로그 검색, 메트릭 시각화, 경보 설정 등이 시험에서 직접적인 사례로 출제됩니다.  
   - 키워드: `Kibana`, `Log Search`, `Analytics Dashboard`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "CloudWatch Metrics"와 "CloudWatch Logs"를 혼동해 메트릭과 로그의 차이를 모르는 경우 | 메트릭은 실시간 성능 지표, 로그는 세부 이벤트 기록 |
| 함정 2 | "X-Ray Tracing" 대신 "CloudTrail"을 사용해 추적 기능을 혼동하는 경우 | X-Ray는 요청 실행 경로 추적, CloudTrail은 API 액션 로그 |
| 함정 3 | "Athena" 대신 "Redshift"를 로그 분석 도구로 선택해 비용 구조를 혼동하는 경우 | Athena는 Serverless 방식, Redshift는 프로비저닝 방식 |

#### 🔄 OpenSearch vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | OpenSearch | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 클라우드 기반 검색 및 분석 엔진 | Elasticsearch, AWS CloudSearch | 데이터 분석, 로그 관리, 실시간 검색 |
| 확장성 | 자동 확장 및 클러스터 관리 | Elasticsearch (수동 확장) | 고가용성 및 대규모 데이터 처리 |
| 비용 | Serverless 방식, 요청 기반 요금제 | Elasticsearch (EC2 + EBS) | 저비용, 간단한 설정 |
| 지연시간 | 실시간 처리 가능 | Elasticsearch (지연 최소화) | 실시간 분석 요구 시 |

#### 📝 시험 대비 체크리스트
- [ ] OpenSearch의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  *예: "OpenSearch는 클라우드 기반 검색 및 분석을 위한 서버리스 엔진입니다."*
- [ ] OpenSearch를 선택해야 하는 시나리오를 알고 있는가?  
  *예: 로그 분석, 실시간 검색, 대규모 데이터 처리*
- [ ] OpenSearch의 제한사항/한계를 알고 있는가?  
  *예: 서버리스 방식의 복잡한 설정, 특정 작업의 성능 한계*
- [ ] OpenSearch와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  *예: Elasticsearch vs OpenSearch의 확장성, 비용 구조 비교*
- [ ] OpenSearch의 비용 구조를 이해하고 있는가?  
  *예: 요청 기반 요금제, 프리티어 사용법, S3 로그 분석 비용*

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 OpenSearch를 떠올리세요:  
> - **Log Analysis**  
> - **Real-time Search**  
> - **Cloud Monitoring**  
> - **Serverless Architecture**  
> - **Kibana Dashboard**  

> **💡 Tip:** CloudWatch 대시보드 생성 시, `Custom Metrics`와 `Log Groups`를 연결해 실시간 모니터링을 구현하세요.  
> **⚠️ 주의:** X-Ray 추적을 활성화할 때 샘플링 비율을 `100%`로 설정하면 성능 저하가 발생할 수 있으니, `1%`에서 시작해 조정하세요.

---

| [⬅️ Athena](./Athena.md) | [📑 Day 4 목차](./README.md) | [🏠 Week 4](../README.md) | [다음 Day ➡️](../day5/README.md) |

---
