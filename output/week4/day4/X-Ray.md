---

| [⬅️ CloudWatch](./CloudWatch.md) | [📑 Day 4 목차](./README.md) | [🏠 Week 4](../README.md) | [CloudTrail ➡️](./CloudTrail.md) |

---

# X-Ray 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** X-Ray는 분산형 애플리케이션의 **성능 모니터링 및 디버깅**을 위한 AWS의 **트레이싱, 메트릭, 로그 분석 통합 서비스**입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 마이크로서비스 아키텍처에서 요청이 여러 서비스를 거치면서 어떤 서비스가 지연되고 있는지 파악하기 어려움. 예: 사용자가 결제를 시도할 때, 결제 서비스가 느려도 전체 시스템에서 어디서 지연되는지 파악하기 어려웠습니다.  
- **문제 2:** 로그 파일을 수동으로 분석해야 하며, 특정 요청에 대한 상세한 트레이스 정보를 추적하는 데 시간이 많이 소요됩니다. 예: 개발자가 로그를 스 canned 후, 특정 오류 발생 시 해당 요청의 전체 흐름을 추적하는 데 수시간이 걸렸습니다.  
- **문제 3:** 시스템의 전체적인 성능 지표를 통합적으로 분석하기 어려워, CPU, 메모리, 네트워크 등의 요소를 분리해 볼 수 없습니다. 예: 높은 지연이 발생했지만, 어떤 서비스에서 발생했는지 파악하지 못해 원인을 찾기 어려웠습니다.  

**X-Ray로 해결:**
- **해결 1:** 트레이싱 기능을 통해 요청이 거치는 모든 서비스를 시각화하고, 각 서비스의 지연 시간, 응답 시간을 확인할 수 있습니다. 예: 결제 요청이 결제 서비스에서 지연되고 있음을 한눈에 파악할 수 있습니다.  
- **해결 2:** 로그와 트레이스 데이터를 통합해 특정 요청의 전체 흐름을 분석할 수 있습니다. 예: 로그에서 특정 오류가 발생했을 때, 해당 요청의 트레이스를 클릭해 서비스 간 전달 경로를 확인할 수 있습니다.  
- **해결 3:** 메트릭, 로그, 트레이스를 통합 분석해 시스템의 전체적인 성능을 파악할 수 있습니다. 예: CPU 사용량과 트레이스의 지연 시간을 함께 보고, 특정 서비스의 부하가 전체 시스템에 영향을 주는지 분석할 수 있습니다.  

### 비유로 이해하기
X-Ray는 배달 서비스에서 각 단계(예: 주문, 포장, 운송, 배달)를 추적하는 시스템입니다. 주문이 처리되는 동안, 각 서비스(예: 주문 시스템, 결제 시스템, 배송 시스템)에서 어떤 일이 발생했는지, 얼마나 걸렸는지를 실시간으로 확인할 수 있습니다. 이렇게 하면 어떤 단계에서 지연이 발생했는지, 문제 원인을 빠르게 파악할 수 있습니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 마이크로서비스 아키텍처에서 요청의 흐름을 모니터링 | 기업이 100개 이상의 마이크로서비스를 사용해 결제 시스템을 구축한 경우 |
| 시나리오 2 | 특정 요청의 성능 지연 원인 분석 | 금융 애플리케이션에서 결제 요청이 5초 이상 지연되는 경우 |
| 시나리오 3 | 서비스 간 통신의 보안 및 성능 분석 | 게임 서비스에서 클라이언트와 서버 간의 통신 지연이 발생하는 경우 |

**이럴 때 X-Ray를 선택하세요:**
- ✅ **분산형 시스템의 요청 흐름 모니터링**이 필요할 때  
- ✅ **특정 요청의 성능 지연 원인을 빠르게 파악**해야 할 때  
- ✅ **서비스 간 통신의 보안 및 성능 분석**이 필요할 때  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **단순한 메트릭 모니터링**이 필요할 때 → **CloudWatch Metrics**  
- ❌ **특정 로그 파일의 특정 패턴 분석**이 필요할 때 → **CloudWatch Logs Insights**  
- ❌ **로그 데이터를 SQL로 질의**해야 할 때 → **Athena**  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| CloudWatch | 메트릭 수집 및 경보 설정 | X-Ray → CloudWatch Metrics → 경보 생성 |
| CloudWatch Logs Insights | 로그 데이터의 실시간 분석 | X-Ray → CloudWatch Logs → Insights Query |
| Athena | 로그 데이터의 SQL 기반 분석 | S3 → Athena → X-Ray 로그 분석 |
| OpenSearch | 로그 검색 및 분석 | X-Ray → OpenSearch → 실시간 검색 |

**자주 사용되는 아키텍처 패턴:**
```
User → API Gateway → Lambda → X-Ray Tracing → S3 (로그 저장) → Athena/CloudWatch Logs Insights → OpenSearch
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| 트레이스 데이터 저장 | $0.05/GB | 월 1GB 무료 |
| 트레이스 요청 처리 | $0.0001/요청 | 월 100만 건 무료 |
| 메트릭 수집 | $0.000001/요청 | 12개월 무료 |

**비용 최적화 팁:**
1. 💡 **프리티어를 꽉 채우기:** 1GB의 트레이스 데이터와 100만 건의 요청을 무료로 사용할 수 있으므로, 초기 단계에서는 테스트를 위해 최대한 활용하세요.  
2. 💡 **샘플링 비율 조정:** 트레이스 데이터를 줄이기 위해 샘플링 비율을 10%로 설정하면, 데이터 저장 비용을 10% 절감할 수 있습니다.  
3. 💡 **Athena로 로그 분석:** S3에 저장된 로그를 Athena로 분석하면, X-Ray의 데이터 저장 비용을 줄일 수 있습니다.  

> **⚠️ 비용 주의:** 샘플링 비율을 낮추면 트레이스 데이터가 부족해 원인 분석이 어려울 수 있으므로, 프로덕션 환경에서는 샘플링 비율을 10~20% 범위로 설정하는 것이 안전합니다.

## 📚 핵심 개념

### 개념 1: **CloudWatch 모니터링과 알림**
CloudWatch는 AWS 서비스의 성능, 사용량, 오류 등을 실시간으로 모니터링하는 기능입니다. **Metrics**는 CPU 사용률, 네트워크 트래픽, 응답 시간 등 수치형 데이터를 제공하며, **Logs**는 디버깅에 유용한 상세 로그를 수집합니다. **Alarms**은 특정 임계값을 초과하면 이메일 또는 SNS를 통해 알림을 보내고, **Dashboards**는 다양한 메트릭과 로그를 시각화해 운영자에게 통찰을 제공합니다.

#### 왜 중요한가?
- **실시간 모니터링**: 시스템 상태를 실시간으로 파악해 문제를 빠르게 대응할 수 있습니다.  
- **비용 최적화**: 과도한 리소스 사용을 감지해 비용 절감을 도와줍니다.  

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **Metrics** | CPU, 메모리, 네트워크 등 수치형 데이터를 실시간으로 추적 | EC2 인스턴스의 CPU 사용률 80% 이상 감지 |
| **Logs** | 요청 로그, 오류 메시지, 디버깅 정보를 저장 | Lambda 함수의 오류 로그 분석 |
| **Alarms** | 특정 메트릭 임계값을 초과하면 알림을 보내는 기능 | RDS DB 연결 수 1000 초과 시 알림 |
| **Dashboards** | 여러 메트릭과 로그를 시각화해 한눈에 확인 | EC2, RDS, Lambda의 통합 대시보드 |

> **💡 Tip:** CloudWatch는 AWS 서비스 간 연동이 용이하며, 프리티어는 100만 개의 로그 항목까지 무료로 제공됩니다.  

---

### 개념 2: **X-Ray 추적과 샘플링**
X-Ray는 애플리케이션의 요청 흐름을 추적해 성능 분석과 디버깅에 도움을 줍니다. **Tracing**은 요청이 서비스 간 이동하는 과정을 추적하고, **Sampling**은 모든 요청을 기록하지 않고 일부만 샘플링해 데이터 부담을 줄입니다. 샘플링 비율은 100%에서 0.1%까지 조절 가능하며, 중요한 요청은 고정밀 추적을 위해 우선 처리됩니다.

#### 작동 원리
1. **요청 추적**: 클라이언트 요청이 서비스 간 전달될 때, X-Ray는 **segment**라는 단위로 요청 흐름을 기록합니다.  
2. **샘플링 결정**: 요청이 들어올 때, X-Ray는 설정된 샘플링 비율에 따라 기록 여부를 결정합니다.  
3. **데이터 분석**: 추적 데이터는 X-Ray UI에서 시각화되어, 지연 시간, 오류 발생 지점 등을 분석할 수 있습니다.  

> **💡 Tip:** 샘플링 비율을 조정해 데이터 부담을 줄이되, 중요한 요청은 100% 추적하도록 설정해야 합니다.  

---

### 개념 3: **Athena와 OpenSearch로 로그 분석**
Athena는 S3에 저장된 로그 파일을 SQL 쿼리로 분석하는 서버리스 서비스로, OpenSearch는 실시간 로그 검색 및 분석을 제공합니다. **Athena**는 로그를 저장한 S3 버킷을 연결해 쿼리 수행 가능하며, **OpenSearch**는 Kibana와 같은 대시보드를 통해 로그를 시각화하고, 분석 결과를 실시간으로 제공합니다. 두 도구는 서로 보완적인 역할을 하며, 로그 분석의 유연성과 성능을 극대화합니다.

#### 주요 특징
1. **Athena의 S3 통합**: S3에 저장된 로그 파일을 SQL로 직접 분석 가능하며, 쿼리 결과는 콘솔에서 즉시 확인 가능합니다.  
2. **OpenSearch의 실시간 검색**: 로그를 실시간으로 검색해 이상 징후를 빠르게 감지할 수 있습니다.  
3. **비용 효율성**: Athena는 데이터 처리 비용이 낮고, OpenSearch는 확장성과 검색 속도를 동시에 제공합니다.  

> **💡 Tip:** 로그 분석 시 Athena는 단순한 쿼리 분석에 적합하고, OpenSearch는 실시간 대시보드와 복잡한 분석이 필요할 때 사용하는 것이 좋습니다.

## 🖥️ AWS 콘솔에서 X-Ray 사용하기

### Step 1: X-Ray 서비스 접속
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
2. 상단 검색창에서 **"X-Ray"**를 입력하고, 검색 결과에서 **"X-Ray"**를 클릭합니다.  
   - 클릭 시 **X-Ray Dashboard**가 표시됩니다.  
3. **"X-Ray"** 메뉴에서 **"Service graph"** 또는 **"Segments"** 탭을 선택하여 데이터를 확인합니다.  

> **📸 화면 확인:** X-Ray 대시보드가 표시되면 정상입니다.  
> **💡 Tip:** 서비스 그래프는 애플리케이션의 호출 흐름을 시각화하고, 세그먼트는 트레이스 데이터를 보여줍니다.

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **서비스 그룹 생성**  
   - **"Service graph"** 탭에서 **"Create service"** 버튼을 클릭합니다.  
   - **"Service name"**에 애플리케이션 이름을 입력합니다 (예: `MyApp-Service`).  
   - **"Resource"** 필드에서 AWS 자원 이름을 입력합니다 (예: `MyApp-Backend`).  
   - **"Tags"**에 태그를 추가하여 리소스를 구분합니다.  
2. **트레이스 샘플링 규칙 설정**  
   - **"Sampling rules"** 탭에서 **"Create sampling rule"**을 클릭합니다.  
   - **"Rule name"**에 규칙 이름을 입력하고, **"Rate"**에 샘플링 비율을 설정합니다 (예: `0.1`).  
   - **"Host"** 필드에 트레이스를 수집할 호스트 이름을 입력합니다.  
3. **X-Ray 통합 활성화**  
   - **"AWS services"** 탭에서 **"Amazon CloudWatch"**를 선택합니다.  
   - **"Enable X-Ray"** 옵션을 활성화하여 X-Ray 데이터를 CloudWatch에 전송합니다.  

> **📸 화면 확인:** 리소스 생성 후 **"Service graph"**에서 새 서비스가 표시되고, 샘플링 규칙이 정상적으로 설정되었는지 확인합니다.  
> **⚠️ 주의:** 서비스 이름은 기존 리소스와 중복되지 않도록 설정해야 합니다.

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **트레이스 필터링 설정**  
   - **"Insights"** 탭에서 **"Create filter"**를 클릭합니다.  
   - **"Name"**에 필터 이름을 입력하고, **"Filter"**에 조건을 설정합니다 (예: `error: true`).  
   - **"Actions"**에서 알림을 설정하거나, 로그로 전송할 수 있습니다.  
2. **AWS Lambda 통합 설정**  
   - **"AWS services"** 탭에서 **"AWS Lambda"**를 선택합니다.  
   - **"Enable X-Ray tracing"** 옵션을 활성화하고, **"Mode"**를 `Active` 또는 `Passive`로 설정합니다.  
3. **OpenSearch 연동 설정**  
   - **"AWS services"** 탭에서 **"Amazon OpenSearch"**를 선택합니다.  
   - **"X-Ray data"**를 OpenSearch에 전송할 수 있도록 설정하고, 인덱스 이름을 지정합니다.  

> **⚠️ 주의:** OpenSearch 연동 시 데이터 전송 비용이 발생할 수 있으므로, 사용량을 모니터링해야 합니다.

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인 방법**  
   - **"Service graph"**에서 생성한 서비스 이름을 검색하고, **"Segments"** 탭에서 트레이스 데이터를 확인합니다.  
   - **"Sampling rules"** 탭에서 샘플링 비율이 적용되었는지 확인합니다.  
2. **상태 확인 방법**  
   - **"X-Ray health"** 탭에서 서비스 상태를 확인합니다.  
   - **"CloudWatch Metrics"**에서 X-Ray 관련 지표 (예: `XRaySegmentCount`)를 확인합니다.  
3. **정상 동작 테스트 방법**  
   - 애플리케이션에 요청을 보내고, **"Service graph"**에서 트레이스가 생성되었는지 확인합니다.  
   - **"Insights"** 탭에서 필터링된 오류 정보를 확인하고, 알림이 전송되었는지 확인합니다.  

> **💡 Tip:** 테스트 후에는 **"X-Ray"**의 **"Cost Explorer"**에서 사용량을 모니터링하여 비용을 최적화하세요.  
> **⚠️ 주의:** X-Ray는 데이터 전송 시 네트워크 비용이 발생할 수 있으므로, 프리티어 사용량을 초과하지 않도록 주의해야 합니다.

## ⌨️ AWS CLI로 X-Ray 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

> **💡 Tip:** AWS CLI를 사용하기 전에 [AWS CLI 설치 가이드](https://docs.aws.amazon.com/cli/latest/userguide/install-cliv2.html)를 참고해 설치 및 구성하세요.  
> **⚠️ 주의:** 리전 설정은 X-Ray 리소스 조회 시 결과에 영향을 미칩니다. `us-east-1`이 기본 리전입니다.

---

### 예제 1: X-Ray 리소스 조회
```bash
# X-Ray 트레이스 목록 조회
aws xray list-traces \
    --start-time 1620000000000 \
    --end-time 1620000000000 \
    --query 'Traces[].[TraceId]' \
    --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--start-time` | 트레이스 시작 시간 (UNIX 타임스탬프) | `1620000000000` |
| `--end-time` | 트레이스 종료 시간 (UNIX 타임스탬프) | `1620000000000` |
| `--query` | JSON 결과 필터링 | `'Traces[].[TraceId]'` |
| `--output` | 출력 형식 | `table` (테이블 형식) |

**예상 출력:**
```
| TraceId                |
|------------------------|
| 1234567890abcdef123456 |
| 0987654321fedcba098765 |
```

---

### 예제 2: X-Ray 리소스 생성
```bash
# X-Ray 그룹 생성
aws xray create-group \
    --group-name "example-group" \
    --insights-configuration "InsightsEnabled":true
```

**필수 옵션:**
- `--group-name`: 그룹 이름 (예: `example-group`)
- `--insights-configuration`: 트레이스 분석 기능 활성화 여부 (`InsightsEnabled`:true)

**예상 출력:**
```json
{
    "GroupArn": "arn:aws:xray::123456789012:group/example-group",
    "GroupReferences": [],
    "Status": "UP_TO_DATE"
}
```

---

### 예제 3: X-Ray 리소스 수정
```bash
# X-Ray 그룹 수정 (트레이스 분석 기능 비활성화)
aws xray update-group \
    --group-name "example-group" \
    --insights-configuration "InsightsEnabled":false
```

**옵션 설명:**
- `--insights-configuration`: 트레이스 분석 기능 설정 (`InsightsEnabled`:true/false)

---

### 예제 4: X-Ray 리소스 삭제
```bash
# X-Ray 그룹 삭제
aws xray delete-group --group-name "example-group"

# 삭제 확인 (에러 발생 시 삭제 완료)
aws xray describe-groups --group-name "example-group"
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. `describe-groups` 명령어로 삭제 전 상태를 확인하세요.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws xray list-traces
aws xray describe-traces --trace-id "trace-id"
aws xray list-groups

# 생성/수정/삭제
aws xray create-group --group-name "group-name"
aws xray update-group --group-name "group-name" --insights-configuration "InsightsEnabled":false
aws xray delete-group --group-name "group-name"
```

**⚠️ 주의:** X-Ray CLI 명령어는 `aws xray` 서비스 명령어로 시작하며, 리소스별 명령어(`list-traces`, `create-group`)를 정확히 사용해야 합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 X-Ray 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **X-Ray의 핵심 목적: 분산 트레이싱 및 성능 분석**  
   - 설명: 애플리케이션의 요청 흐름을 추적하고, 서비스 간 지연 시간과 결함을 식별하는 데 사용됩니다. AWS 서비스 간 통신을 모니터링하고, 복잡한 아키텍처에서 성능 최적화를 위해 필수적인 기능입니다.  
   - 키워드: `분산 트레이싱`, `성능 분석`, `요청 흐름 추적`

2. **X-Ray 샘플링 전략 이해**  
   - 설명: 트레이스 데이터를 저장할 수 있는 제한을 해결하기 위해 샘플링 비율을 조절하는 것이 시험에서 빈출입니다. 예: 100% 샘플링은 모든 트레이스를 기록하지만, 비용이 높고 1%는 대규모 시스템에서만 사용됩니다.  
   - 키워드: `샘플링 비율`, `트레이스 기록`, `비용 최적화`

3. **X-Ray와 CloudWatch의 연동**  
   - 설명: X-Ray의 트레이스 데이터를 CloudWatch Metrics로 전송해 지표를 생성하는 방식이 시험에 자주 등장합니다. 예: `AWSXRayRecorder` 라이브러리를 사용해 로그를 CloudWatch로 전송하는 구현이 중요합니다.  
   - 키워드: `CloudWatch 연동`, `메트릭스 전송`, `리코더 라이브러리`

4. **X-Ray의 비용 구조 이해**  
   - 설명: 트레이스 데이터 저장량, 샘플링 비율, 사용량에 따라 비용이 변동하며, 프리티어의 제한(1000개 트레이스/월)이 시험에서 자주 출제됩니다.  
   - 키워드: `비용 모델`, `프리티어 제한`, `트레이스 저장량`

5. **X-Ray의 보안 및 IAM 설정**  
   - 설명: X-Ray는 IAM 정책을 통해 트레이스 데이터의 접근 권한을 제어하고, AWS KMS를 사용해 데이터 암호화가 시험에서 중요한 포인트로 출제됩니다.  
   - 키워드: `IAM 정책`, `KMS 암호화`, `데이터 보안`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | X-Ray는 로그 분석 서비스인가? | X-Ray는 분산 트레이싱 서비스로, CloudWatch Logs Insights나 Athena가 로그 분석에 사용됩니다. |
| 함정 2 | X-Ray 샘플링 비율은 100%일수록 더 정확한가? | 100% 샘플링은 모든 트레이스를 기록하지만, 비용이 매우 높아 대규모 시스템에서만 사용됩니다. |
| 함정 3 | X-Ray는 모든 AWS 서비스에 자동으로 통합되는가? | X-Ray는 AWS SDK를 사용하는 서비스에만 통합되며, 직접 라이브러리를 추가해야 합니다. |

#### 🔄 X-Ray vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | X-Ray | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 분산 트레이싱 및 성능 분석 | CloudWatch(메트릭스), CloudTrail(로그), OpenSearch(검색) | 서비스 간 통신 모니터링 시 X-Ray, 로그 저장 시 CloudWatch |
| 확장성 | 트레이스 데이터 크기와 샘플링 비율로 조절 | CloudWatch Logs Insights는 로그 처리량에 따라 확장 | 실시간 트레이싱 시 X-Ray, 대량 로그 분석 시 CloudWatch |
| 비용 | 트레이스 저장량과 샘플링 비율에 따라 변동 | CloudWatch Logs는 저장량 기반, Athena는 쿼리량 기반 | 비용 효율성 우선 시 Athena, 실시간 분석 시 X-Ray |
| 지연시간 | 트레이스 수집과 분석에 추가 지연 | CloudWatch Logs Insights는 로그 처리 시간 추가 | 실시간 성능 분석 시 X-Ray, 이력 로그 분석 시 CloudWatch |

#### 📝 시험 대비 체크리스트
- [ ] X-Ray의 핵심 목적을 한 문장으로 설명할 수 있는가?  
- [ ] X-Ray를 선택해야 하는 시나리오를 알고 있는가? (예: 분산 시스템 모니터링)  
- [ ] X-Ray의 제한사항/한계를 알고 있는가? (예: 프리티어 제한, 샘플링 비율 조절)  
- [ ] X-Ray와 비슷한 서비스의 차이점을 설명할 수 있는가? (예: CloudWatch vs. X-Ray)  
- [ ] X-Ray의 비용 구조를 이해하고 있는가? (트레이스 저장량, 샘플링 비율에 따른 비용)

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 X-Ray를 떠올리세요:  
> - **분산 트레이싱**  
> - **샘플링 비율**  
> - **트레이스 기록**  
> - **AWS SDK 통합**  
> - **성능 분석**

---

| [⬅️ CloudWatch](./CloudWatch.md) | [📑 Day 4 목차](./README.md) | [🏠 Week 4](../README.md) | [CloudTrail ➡️](./CloudTrail.md) |

---
