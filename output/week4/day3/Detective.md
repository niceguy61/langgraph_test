---

| [⬅️ Security Hub](./Security-Hub.md) | [📑 Day 3 목차](./README.md) | [🏠 Week 4](../README.md) | [Config ➡️](./Config.md) |

---

# Detective 완전 정복

> **한 줄 정의:** Detective는 AWS에서 보안 모니터링 및 위협 탐지를 위한 **실시간 위협 분석 및 보안 데이터 통합** 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 수동적인 보안 모니터링으로 인해 위협 탐지 지연 및 인력 부담 증가  
  기존에는 로그 분석 및 감시 도구를 별도로 운영해야 했으며, 실시간 위협 탐지가 어려웠습니다.  
- **문제 2:** 다양한 AWS 서비스 간 보안 데이터 분산으로 인한 분석 복잡성 증가  
  GuardDuty, CloudTrail, Security Hub 등 각 서비스에서 수집된 데이터를 통합 분석하는 것이 어렵습니다.  
- **문제 3:** 자동화된 보안 준수 검증 및 정책 적용이 부족  
  수동적인 정책 적용으로 인해 비용 절감 및 오류 발생 위험이 커졌습니다.  

**Detective로 해결:**
- **해결 1:** 머신러닝 기반 실시간 위협 분석으로 탐지 속도 향상  
  Detective는 AWS 내 모든 리소스의 이벤트를 실시간으로 분석하여 위협을 즉시 식별합니다.  
- **해결 2:** 통합 보안 데이터 플랫폼 제공으로 분석 효율성 증대  
  GuardDuty, Security Hub, CloudTrail 등의 데이터를 하나의 대시보드에서 통합 관리합니다.  
- **해결 3:** 자동화된 정책 적용으로 보안 준수 강화  
  Config Rules와 연동하여 정책 위반을 자동으로 감지 및 수정합니다.  

### 비유로 이해하기
Detective는 기업의 보안 수색대를 대표하는 AI 분석가입니다. 기존에는 수백 명의 인력이 수동으로 로그를 검토해야 했지만, Detective는 머신러닝을 활용해 실시간으로 이상 트래픽을 감지하고, 다양한 보안 데이터를 통합해 위협을 분석합니다. 예를 들어, 네트워크 침투를 감지했을 때, 자동으로 관련 리소스를 차단하고, 감사 로그에 기록해 보안 팀이 신속히 대응할 수 있도록 합니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | **실시간 위협 탐지**가 필요한 환경 | 금융 기관이 해킹 시도를 즉시 탐지하여 자산 보호 |
| 시나리오 2 | **보안 준수 검증**이 필수적인 규제 산업 | 의료 기업이 HIPAA 규정을 자동으로 검토 |
| 시나리오 3 | **다중 서비스 통합 분석**이 필요한 복잡한 아키텍처 | 글로벌 E-Commerce 플랫폼의 클라우드 보안 통합 감시 |

**이럴 때 Detective를 선택하세요:**
- ✅ **실시간 위협 분석**이 필수적인 시스템  
- ✅ **다양한 AWS 서비스 통합**이 필요한 환경  
- ✅ **자동화된 보안 정책 적용**이 필요한 조직  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **단일 리소스 보안 모니터링** → **CloudWatch** (리소스별 메트릭 추적)  
- ❌ **단기적인 보안 위험 분석** → **GuardDuty** (실시간 위협 탐지)  
- ❌ **수동 정책 적용** → **Config Rules** (자동 정책 적용)  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **GuardDuty** | **위협 데이터 공유** | Detective → GuardDuty → Security Hub |
| **Security Hub** | **통합 보안 데이터 관리** | Detective → Security Hub → CloudTrail |
| **CloudTrail** | **API 로그 수집 및 분석** | Detective → CloudTrail → Trusted Advisor |

**자주 사용되는 아키텍처 패턴:**
```
User → API Gateway → Lambda → Detective (위협 분석) ← CloudTrail (로그 수집) ← GuardDuty (위협 탐지) ← Security Hub (통합 보안 데이터)
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **기본 사용량** | $0.01~$0.50/일 (리소스 수에 따라 달라짐) | **월 12개월 무료** |
| **추가 분석 기능** | $0.10~$0.30/일 (고급 분석 기능) | 12개월 무료 |
| **데이터 저장** | $0.02/GB/일 | **무료 (10GB까지)** |

**비용 최적화 팁:**
1. 💡 **프리티어 활용**: 12개월 동안 무료로 사용할 수 있으므로 초기 설정 시 활용  
2. 💡 **필요한 리소스만 선택**: 불필요한 리소스는 제거해 과금을 줄이세요  
3. 💡 **CloudTrail 최소화**: 로그 수집량을 줄이면 분석 비용도 감소합니다  

> **⚠️ 비용 주의:** **데이터 저장량**이 10GB를 초과하면 추가 비용이 발생하므로, 저장 공간을 정기적으로 관리해야 합니다.

## 📚 핵심 개념

### 개념 1: **GuardDuty 통합을 통한 위협 탐지**
GuardDuty는 AWS에서 제공하는 실시간 위협 탐지 서비스로, 네트워크, 호스트, 컨테이너의 위협을 머신러닝 기반으로 분석합니다. Detective는 GuardDuty의 위협 데이터를 활용해 보안 이벤트를 시각화하고, 지속적인 위협 분석을 지원합니다. 이는 기존 보안 도구에서 수집한 데이터를 단일 플랫폼에서 통합해 보안 전략 수립을 용이하게 합니다.

#### 왜 중요한가?
- **위협 식별의 자동화**: GuardDuty의 머신러닝 모델은 수동 분석을 줄이고, 위협을 빠르게 식별합니다.  
- **실시간 분석 가능성**: 네트워크 트래픽, 악성 활동, 보안 이벤트를 실시간으로 모니터링하여 사전 대응이 가능합니다.  
- **지속적 보안 모니터링**: 지속적인 데이터 분석을 통해 보안 취약점과 위협 패턴을 식별하고, 보안 정책을 최적화합니다.

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **위협 탐지 기능** | 네트워크, 호스트, 컨테이너에서 발생한 이상 행동을 감지합니다. | 불법적인 SSH 접근 시도, 악성 IP 주소의 네트워크 활동 감지 |
| **머신러닝 기반 분석** | 대규모 데이터를 기반으로 위협 패턴을 학습하고, 새로운 위협을 예측합니다. | 이전에 없는 악성 패턴의 공격을 감지 |
| **AWS 자동화 통합** | AWS 서비스 간 데이터를 실시간으로 연동해 보안 분석을 강화합니다. | CloudTrail 로그와 GuardDuty 데이터를 Detective에 통합 |

> **💡 Tip:** GuardDuty와 Detective의 통합은 AWS 보안 아키텍처에서 필수적인 구성 요소로, 위협 탐지의 정확도와 신속성을 높입니다. 예를 들어, GuardDuty에서 감지된 IP 주소는 Detective에서 네트워크 흐름 분석을 통해 위협 패턴을 식별할 수 있습니다.

---

### 개념 2: **Security Hub를 통한 통합 보안 데이터 관리**
Security Hub는 AWS 보안 상태를 통합적으로 모니터링하고, 위험을 분류해 관리하는 서비스입니다. Detective는 Security Hub의 위험 분류 기능을 활용해 보안 이벤트를 시각화하고, 분석을 효율화합니다. 이는 다양한 보안 도구에서 수집한 데이터를 중앙집중식으로 관리해 보안 팀의 운영 효율성을 높입니다.

#### 작동 원리
1. **데이터 수집**: Security Hub는 CloudTrail, GuardDuty, Inspector 등에서 수집된 보안 데이터를 모아 분석합니다.  
2. **위험 분류**: 수집된 데이터를 기반으로 위험 수준(예: 중간, 높음, 매우 높음)을 자동으로 분류합니다.  
3. **Detective 연동**: 분류된 위험 데이터를 Detective에 전달해, 보안 이벤트를 시각화하고 분석이 가능하게 합니다.

> **💡 Tip:** Security Hub와 Detective의 통합은 보안 이벤트를 빠르게 파악하고, 분석을 간소화하는 데 유용합니다. 예를 들어, Security Hub에서 감지된 보안 위험은 Detective에서 자동으로 분석되어, 보안 팀이 대응 전략을 수립할 수 있습니다.

---

### 개념 3: **Config Rules를 통한 자동화된 보안 준수 검증**
Config Rules는 AWS 리소스의 구성이 보안 정책에 부합하는지 자동으로 검증하는 서비스입니다. Detective는 Config Rules의 검증 결과를 활용해 보안 준수 상태를 모니터링하고, 위험한 구성 변경을 감지합니다. 이는 보안 정책 위반을 사전에 방지하고, 리소스의 일관된 보안 상태를 유지합니다.

#### 주요 특징
1. **자동화된 준수 검증**: 리소스 구성이 보안 정책에 부합하는지 실시간으로 확인합니다.  
   - 예: S3 버킷에 암호화 설정이 누락된 경우 경고를 발생시킵니다.  
2. **위험한 구성 변경 감지**: 리소스 변경이 보안 정책을 위반할 경우, 실시간으로 알림을 제공합니다.  
   - 예: EC2 인스턴스에 SSH 허용 규칙이 변경된 경우 경고를 발생시킵니다.  
3. **보안 정책 최적화**: 검증 결과를 기반으로 보안 정책을 지속적으로 개선할 수 있습니다.  
   - 예: 정책에 없는 보안 설정을 추가하거나, 불필요한 규칙을 제거합니다.

> **💡 Tip:** Config Rules와 Detective의 통합은 리소스 구성에 대한 보안 관리를 자동화해, 보안 팀의 부담을 줄이고, 리소스의 일관된 보안 상태를 유지합니다. 예를 들어, Config Rules에서 감지된 보안 위험은 Detective에서 자동으로 분석되어, 보안 팀이 대응 전략을 수립할 수 있습니다.

## 🖥️ AWS 콘솔에서 Detective 사용하기

### Step 1: Detective 서비스 접속
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - AWS 계정과 지역을 선택해 로그인합니다.  
2. 상단 검색창에서 **"Detective"**를 입력하고, 검색 결과에서 **"Detective"** 서비스를 클릭합니다.  
   - **"Detective"**는 보안 분석을 위한 통합 분석 플랫폼으로, 클라우드 리소스의 위협을 탐지하고 분석합니다.  
3. **"Detective"** 대시보드가 표시되면 정상적으로 서비스에 접근했습니다.  

> **📸 화면 확인:** Detective 대시보드는 "Dashboard" 탭에서 확인할 수 있으며, "Graph" 탭에서는 네트워크 및 보안 이벤트를 시각화한 차트를 볼 수 있습니다.

---

### Step 2: 리소스 생성 - Detective 인스턴스 생성
1. **"Create Detective"** 버튼을 클릭합니다.  
   - **"Create Detective"**는 기본적으로 1개의 Detective 인스턴스를 생성합니다.  
   - **"Region"** 필드에서 지역을 선택합니다 (예: us-east-1).  
2. **"Enable"** 버튼을 클릭하여 서비스를 활성화합니다.  
   - 활성화 시, Detective는 AWS CloudTrail, VPC Flow Logs, AWS Config 등의 데이터를 수집하여 분석합니다.  
   - **"Auto-Enable"** 옵션을 선택하면 자동으로 연동된 서비스의 데이터를 수집합니다.  
3. **"Create"** 버튼을 클릭하여 인스턴스를 생성합니다.  
   - 생성 후, **"Detective"** 인스턴스의 상태를 확인합니다 (예: "Active" 또는 "Pending").  

> **📸 화면 확인:** "Detective" 인스턴스 생성 후, "Overview" 탭에서 "Status"가 "Active"로 표시되어야 합니다.  

> **💡 Tip:** Detective는 AWS Free Tier의 100GB 데이터 수집 기능을 제공합니다. 사용량이 초과되면 추가 비용이 발생하므로 주의하세요.

---

### Step 3: 설정/구성 - 데이터 소스 연동 및 알림 설정
1. **"Data Sources"** 탭을 클릭하여 데이터 수집 설정을 확인합니다.  
   - **"CloudTrail"**, **"VPC Flow Logs"**, **"AWS Config"** 등의 데이터 소스가 자동으로 연동됩니다.  
   - **"Enable"** 또는 **"Disable"** 버튼으로 각 서비스의 데이터 수집을 제어할 수 있습니다.  
2. **"Alerts"** 탭에서 알림 규칙을 설정합니다.  
   - **"Create Alert"** 버튼을 클릭해 특정 이벤트(예: 이상 트래픽 감지)에 대한 알림을 생성합니다.  
   - **"Severity"** 필드에서 경고 수준을 설정하고, **"SNS Topic"**을 연결해 알림을 수신할 수 있습니다.  
3. **"Graph"** 탭에서 네트워크 및 보안 이벤트를 시각화한 차트를 확인합니다.  
   - **"Nodes"**와 **"Edges"**를 통해 리소스 간의 연결 관계를 분석할 수 있습니다.  

> **⚠️ 주의:** Detective는 데이터 수집을 위해 **AWS Config**와 **CloudTrail**이 필요합니다. 이 서비스들이 활성화되어 있지 않으면 데이터 수집이 실패할 수 있습니다.

---

### Step 4: 설정 확인 및 테스트
1. **"Detective"** 인스턴스의 상태를 확인하는 방법  
   - **"Overview"** 탭에서 **"Status"** 필드가 **"Active"**로 표시되는지 확인합니다.  
   - **"Data Sources"** 탭에서 수집된 데이터의 양이 0이 아닌지 확인합니다.  
2. **"Graph"** 탭에서 최근 이벤트를 검색하여 분석 결과를 확인합니다.  
   - **"Search"** 필드에 **"Suspicious"** 또는 **"High Risk"**와 같은 키워드를 입력해 이상 이벤트를 필터링합니다.  
3. **"Alerts"** 탭에서 생성한 알림이 정상적으로 작동하는지 테스트합니다.  
   - **"Test Alert"** 버튼을 클릭해 가상의 이벤트를 유발하고, 알림이 수신되는지 확인합니다.  

> **📸 화면 확인:** "Graph" 탭에서 "Nodes"와 "Edges"가 표시되고, "Alerts" 탭에서 알림이 생성된 후 SNS Topic에 메시지가 전송되는지 확인하세요.  

> **💡 Tip:** Detective는 실시간 분석을 위해 AWS Lambda와 같은 서버리스 기능과도 연동 가능합니다. 추가 기능을 활용해 보안 대응을 강화하세요.

## ⌨️ AWS CLI로 Detective 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**💡 Tip:**  
AWS CLI를 사용하려면 먼저 [AWS CLI 설치 가이드](https://docs.aws.amazon.com/cli/latest/userguide/cli-install.html)를 참고해 설치하세요.  
AWS 자격 증명은 [IAM 사용자 생성](https://console.aws.amazon.com/iam/home#/users) 후 `aws configure` 명령어로 설정해야 합니다.

---

### 예제 1: Detective 리소스 조회
```bash
# Detective 멤버 목록 조회
aws detective list-members --query '[].Name' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 결과 필터링 | `'[].Name'` (멤버 이름만 표시) |
| `--output` | 출력 형식 | `json`, `table`, `text` |

**예상 출력:**
```
| Name         |
|--------------|
| example-member |
```

**💡 Tip:**  
`list-members` 명령어는 Detective 멤버 목록을 반환합니다. 멤버는 AWS 계정 간의 보안 분석 협업을 위한 단위입니다.

---

### 예제 2: Detective 리소스 생성
```bash
# Detective 멤버 생성
aws detective create-member \
    --name "example-member" \
    --email "user@example.com" \
    --tags "Key=Environment,Value=Production"
```

**필수 옵션:**
- `--name`: 멤버 이름 (예: `example-member`)
- `--email`: 멤버 이메일 주소 (AWS SSO 연동 시 필요)

**예상 출력:**
```json
{
    "Member": {
        "AccountId": "123456789012",
        "Arn": "arn:aws:detective:us-east-1:123456789012:member/m-1234567890abcdef1234567890abcdef",
        "Email": "user@example.com",
        "Name": "example-member",
        "Status": "ENABLED"
    }
}
```

**💡 Tip:**  
`--tags` 옵션은 자원 태깅을 통해 리소스 관리를 용이하게 합니다. 태그는 AWS Cost Explorer 및 CloudTrail에서 필터링에 활용됩니다.

---

### 예제 3: Detective 리소스 수정
```bash
# Detective 멤버 상태 업데이트
aws detective update-member \
    --member-id "m-1234567890abcdef1234567890abcdef" \
    --status "DISABLED"
```

**필수 옵션:**
- `--member-id`: 수정할 멤버의 고유 ID (예: `m-1234567890abcdef1234567890abcdef`)
- `--status`: `ENABLED` 또는 `DISABLED` (멤버 활성화/비활성화)

**💡 Tip:**  
멤버 상태 변경 시, 해당 멤버의 보안 분석 데이터 수집이 중단됩니다. 재활성화 시 다시 데이터 수집이 시작됩니다.

---

### 예제 4: Detective 리소스 삭제
```bash
# Detective 멤버 삭제
aws detective delete-member --member-id "m-1234567890abcdef1234567890abcdef"

# 삭제 확인
aws detective describe-member --member-id "m-1234567890abcdef1234567890abcdef"
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. 멤버가 포함하는 모든 보안 분석 데이터도 함께 삭제됩니다.  
> **💡 Tip:** 삭제 전에 `describe-member` 명령어로 상태를 확인하세요.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws detective list-members
aws detective describe-member --member-id "id"

# 생성
aws detective create-member --name "name" --email "email"

# 수정
aws detective update-member --member-id "id" --status "ENABLED"

# 삭제
aws detective delete-member --member-id "id"
```

---

### 📌 Detective CLI 사용 시 주의사항
| 주의사항 | 설명 |
|---------|------|
| **리전 제한** | Detective는 AWS 리전에 따라 사용 가능 여부가 다릅니다. `us-east-1` 리전에서만 사용 가능합니다. |
| **비용 관리** | Detective는 `AWS Free Tier`로 1개 멤버, 1개 데이터 소스(예: CloudTrail)를 1년간 무료로 제공합니다. |
| **보안 정책** | 멤버 생성 시 IAM 정책을 통해 접근 권한을 제어해야 합니다. `detective:CreateMember` 권한이 필요합니다. |

---

### 📋 CLI 사용 체크리스트
- [ ] AWS CLI 버전 확인 및 설치 완료
- [ ] AWS 자격 증명 설정 완료 (`aws configure`)
- [ ] Detective 멤버 생성 시 이메일 주소 정확히 입력
- [ ] 멤버 상태 변경 전 `describe-member`로 확인
- [ ] 삭제 시 멤버 ID 정확히 입력
- [ ] **삭제 전** 데이터 백업 또는 복구 계획 준비

---

### 🧠 연관 서비스 및 통합
| 서비스 | Detective 통합 방법 |
|--------|----------------------|
| **GuardDuty** | Detective와 연동해 보안 위협 분석 자동화 |
| **CloudTrail** | Detective의 분석 데이터 수집에 사용되는 로그 저장소 |
| **Security Hub** | Detective의 분석 결과를 Security Hub에 시각화 |
| **Trusted Advisor** | 보안 위험 점검 및 최적화 권장 사항 제공 |

**💡 Tip:** Detective는 **GuardDuty**, **CloudTrail**, **Inspector** 등과 연동해 통합 보안 분석을 가능하게 합니다. CLI로 자동화된 분석 시스템을 구축하세요.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Detective 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **포인트 1**: **Detective의 자동화된 로그 수집 및 분석 기능**
   - 설명: Detective는 AWS 서비스 로그를 자동으로 수집하고, 보안 위협을 탐지하는 데 필요한 분석을 제공합니다. 이는 보안 사고 조사와 사고 대응을 효율화하는 핵심 기능으로, 시험에서 자주 출제되는 주제입니다.
   - 키워드: `자동화된 로그 수집`, `보안 사고 분석`

2. **포인트 2**: **Detective의 타임라인 기반 분석 및 시각화**
   - 설명: Detective는 보안 이벤트를 시간 순으로 정리해 타임라인을 제공하며, 이는 사고 원인 추적과 대응 전략 수립에 필수적입니다. 시험에서는 타임라인 기능의 활용 방법과 제한 사항이 주요 포인트로 출제됩니다.
   - 키워드: `타임라인 분석`, `시각화 기능`

3. **포인트 3**: **Detective와 Security Hub의 통합 활용**
   - 설명: Security Hub는 여러 보안 서비스의 결과를 통합하고, Detective는 그 데이터를 분석하는 역할을 합니다. 두 서비스의 협업 방식은 시험에서 자주 다루는 주제로, 혼동을 유발할 수 있는 개념입니다.
   - 키워드: `통합 분석`, `보안 서비스 연동`

4. **포인트 4**: **Detective의 비용 구조 및 프리티어 활용**
   - 설명: Detective는 로그 데이터 크기 기반으로 요금이 청구되며, 프리티어는 1년간 2GB까지 무료로 제공됩니다. 비용 계산 방식과 프리티어 한계는 시험에서 반드시 이해해야 하는 주요 개념입니다.
   - 키워드: `요금 청구 방식`, `프리티어 한계`

5. **포인트 5**: **Detective의 제한 사항 및 적합한 시나리오**
   - 설명: Detective는 네트워크 트래픽 로그를 수집하지 않으며, 실시간 분석이 불가능합니다. 이는 서비스의 한계를 파악하는 데 중요한 포인트로, 시험에서 혼동을 유발할 수 있는 개념입니다.
   - 키워드: `데이터 수집 범위`, `실시간 분석 불가`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "Detective는 네트워크 트래픽 로그를 실시간 분석한다"는 문장이 출제될 수 있음 | **False** (Detective는 로그 수집 후 분석에 시간이 소요됨) |
| 함정 2 | "Security Hub와 Detective는 동일한 데이터 소스를 사용한다"는 문장이 출제될 수 있음 | **False** (Security Hub는 다양한 서비스의 결과를 통합하고, Detective는 로그 분석에 집중) |
| 함정 3 | "Detective는 프리티어로 10GB까지 무료로 제공된다"는 문장이 출제될 수 있음 | **False** (프리티어는 1년간 2GB까지 제공) |

#### 🔄 Detective vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | Detective | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| **용도** | 보안 이벤트 분석 및 타임라인 제공 | GuardDuty(위협 탐지), Inspector(보안 검사) | 로그 분석 중심의 보안 대응이 필요한 경우 |
| **확장성** | 로그 데이터 크기 기반 스케일링 | CloudWatch(모니터링), CloudTrail(요청 로그) | 대규모 로그 분석이 필요한 경우 |
| **비용** | 로그 크기 기반 요금 청구 | CloudTrail(요청 로그 저장), Kinesis(데이터 전송) | 데이터 크기에 따라 비용이 달라질 수 있음 |
| **지연시간** | 로그 수집 후 분석 수행 | CloudWatch(실시간 모니터링), CloudTrail(이력 보관) | 실시간 분석이 불가능한 점 주의 |

#### 📝 시험 대비 체크리스트
- [ ] Detective의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  → "보안 이벤트를 타임라인 기반으로 분석하여 사고 대응을 지원하는 서비스입니다."
- [ ] Detective를 선택해야 하는 시나리오를 알고 있는가?  
  → "로그 분석을 통한 보안 사고 조사 및 대응이 필요한 경우"
- [ ] Detective의 제한사항/한계를 알고 있는가?  
  → "네트워크 트래픽 로그 수집 불가, 실시간 분석 불가, 프리티어 한계(2GB)"
- [ ] Detective와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  → "GuardDuty는 위협 탐지, Inspector는 보안 검사, Security Hub는 통합 분석에 집중"
- [ ] Detective의 비용 구조를 이해하고 있는가?  
  → "로그 크기 기반 요금 청구, 프리티어는 1년간 2GB까지 무료"

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Detective를 떠올리세요:  
> - **타임라인 분석**  
> - **로그 수집 및 분석**  
> - **보안 사고 조사**  
> - **비용 효율성**  
> - **Security Hub 통합**

---

| [⬅️ Security Hub](./Security-Hub.md) | [📑 Day 3 목차](./README.md) | [🏠 Week 4](../README.md) | [Config ➡️](./Config.md) |

---
