---

| [⬅️ Parameter Store](./Parameter-Store.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 4](../README.md) | [Macie ➡️](./Macie.md) |

---

# ACM 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** ACM는 SSL/TLS 인증서를 자동화하여 애플리케이션 보안을 강화하는 AWS의 인증서 관리 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 수동으로 인증서를 생성, 배포, 갱신해야 하며, 유효기간 경과 시 보안 위험이 발생합니다. 예를 들어, 웹사이트 인증서가 만료되면 사용자에게 경고 메시지가 표시되고, 서버 연결이 중단될 수 있습니다.
- **문제 2:** 민감한 데이터를 암호화할 때 키 관리가 복잡하며, 키 유출 시 전체 시스템이 위험에 처할 수 있습니다. 예를 들어, S3 버킷의 데이터 암호화 키가 노출되면 데이터 해독이 가능해집니다.
- **문제 3:** 인증서 및 비밀번호 같은 민감 정보를 관리할 때, 수동으로 파일에 저장하거나 복잡한 암호를 사용해야 하며, 이는 실수로 정보가 유출될 위험이 있습니다.

**ACM으로 해결:**
- **해결 1:** AWS Certificate Manager는 AWS 서비스(예: CloudFront, API Gateway)와 연동해 인증서를 자동으로 생성, 갱신, 배포합니다. 예를 들어, CloudFront에 ACM 인증서를 연결하면 사용자 요청 시 자동으로 SSL/TLS 암호화가 적용됩니다.
- **해결 2:** KMS CMK와 연동해 암호화 키를 안전하게 관리하며, 키 회전을 자동화해 보안 위험을 최소화합니다. 예를 들어, RDS 인스턴스의 데이터 암호화 키를 KMS로 관리하면 키 유출 시 데이터가 해독되지 않습니다.
- **해결 3:** Secrets Manager와 통합해 비밀번호 등을 자동으로 교체하고, Parameter Store와 연동해 환경 변수를 안전하게 저장합니다. 예를 들어, 애플리케이션의 데이터베이스 비밀번호를 Secrets Manager에서 자동으로 가져오면, 파일에 직접 저장하는 실수를 방지할 수 있습니다.

### 비유로 이해하기
ACM은 애플리케이션의 보안을 책임지는 **디지털 안전고**입니다. 기존에는 인증서를 수동으로 관리하는 것처럼, 매일 키를 교체하고 파일에 비밀번호를 저장하는 번거로움이 있었지만, ACM은 이를 자동화해 **안전한 키 관리와 인증서 갱신을 끝장내는 전문가**로, 애플리케이션의 보안을 24시간 감시하고 있습니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 웹사이트나 API의 SSL/TLS 인증서 자동 관리 | AWS CloudFront에 ACM 인증서를 연결해 HTTPS 암호화 적용 |
| 시나리오 2 | S3 버킷 데이터 암호화 및 키 관리 | KMS CMK로 암호화 키 생성 후, S3 버킷에 암호화된 데이터 저장 |
| 시나리오 3 | 민감 정보 자동 교체 및 환경 변수 관리 | Secrets Manager로 데이터베이스 비밀번호 자동 교체 및 Parameter Store에서 환경 변수 가져오기 |

**이럴 때 ACM를 선택하세요:**
- ✅ **AWS 서비스와 통합된 인증서 관리가 필요할 때** (예: CloudFront, API Gateway)
- ✅ **민감한 데이터 암호화 키를 안전하게 관리해야 할 때**
- ✅ **비밀번호 등 민감 정보를 자동으로 교체해야 할 때**

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **수동으로 인증서 관리가 가능한 간단한 환경** → **AWS Certificate Manager 대신 자체 인증서 관리**
- ❌ **데이터베이스 암호화 키가 필요하지 않거나, 외부 키 관리 시스템이 이미 존재할 때** → **KMS 대신 외부 키 관리 솔루션 사용**
- ❌ **단순한 환경 변수 관리만 필요할 때** → **Parameter Store 대신 AWS Systems Manager Parameter Store 사용**

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **KMS** | 암호화 키 생성 및 관리 | ACM → KMS → RDS |
| **Secrets Manager** | 민감 정보 자동 교체 | ACM → Secrets Manager → Application |
| **CloudHSM** | 하드웨어 보안 모듈 활용 | ACM → CloudHSM → S3 |

**자주 사용되는 아키텍처 패턴:**
```
User → CloudFront → ACM 인증서 → S3 버킷 (KMS 암호화)
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **ACM 인증서 발급** | $0.20/월 (1년 기준 $2.40) | 12개월 무료 |
| **KMS CMK 사용** | $0.03/100만 회 | 항상 무료 |
| **Secrets Manager 사용** | $0.02/100만 회 | 12개월 무료 |

**비용 최적화 팁:**
1. 💡 **ACM 무료 인증서 사용:** AWS의 무료 인증서를 우선으로 사용해 비용을 절감합니다. 예: *.example.com 도메인에 ACM 무료 인증서 배포.
2. 💡 **KMS CMK 재사용:** 동일한 CMK를 여러 서비스에 재사용해 비용을 절감합니다. 예: RDS와 S3 모두 동일한 KMS CMK로 암호화.
3. 💡 **Secrets Manager 자동 교체 정책 설정:** 비밀번호 등 민감 정보의 자동 갱신으로 실수로 노출되는 비용을 줄입니다.

> **⚠️ 비용 주의:** ACM은 자체적으로 요금이 발생하지 않지만, CloudFront, S3, RDS 등과 연동 시 해당 서비스의 요금이 추가될 수 있습니다. 예: CloudFront에 ACM 인증서를 연결하면 CloudFront 요금이 발생할 수 있습니다.

## 📚 핵심 개념

### 개념 1: 암호화 및 키 관리 (KMS CMK, 봉투 암호화, CloudHSM)
AWS Key Management Service (KMS)는 암호화 키를 생성, 관리, 회전하는 핵심 서비스입니다. KMS CMK는 클라우드 환경에서 데이터 암호화에 사용되는 보안 키로, 암호화 및 복호화 프로세스를 효율적으로 관리합니다. 봉투 암호화(Envelope Encryption)는 데이터를 암호화할 때 대량의 데이터에 적합한 대칭 키를 사용하고, 이 키를 비대칭 키로 암호화하여 보안성을 높이는 메커니즘입니다. CloudHSM은 하드웨어 보안 모듈(HSM)을 제공하여 고도로 보안이 필요한 시나리오에서 키를 보호하고 관리하는 역할을 합니다.

#### 왜 중요한가?
- **보안 강화**: KMS CMK와 CloudHSM을 통해 키의 보호 수준을 높여 해킹 시 데이터 유출을 방지합니다.
- **효율적인 키 관리**: 봉투 암호화는 대량 데이터 처리 시 성능 최적화를 가능하게 하며, 키 회전 시 복잡도를 줄입니다.
- **규정 준수**: GDPR, HIPAA 등 데이터 보호 규정 준수를 위한 키 관리 체계를 구축할 수 있습니다.

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **KMS CMK 관리** | AWS KMS를 통해 CMK를 생성, 사용 권한 설정, 회전 등을 관리합니다. | `aws kms create-key` 명령어로 CMK 생성 |
| **봉투 암호화** | 데이터 암호화 시 대칭 키를 사용하고, 이 키를 비대칭 키로 암호화하여 저장합니다. | S3에 데이터 저장 시 데이터 암호화와 키 암호화 동시에 수행 |
| **CloudHSM 활용** | 하드웨어 기반 HSM을 통해 키를 물리적으로 보호하고, 암호화/복호화 작업을 수행합니다. | CloudHSM 클러스터 생성 후 키 생성 및 암호화 작업 |

> **💡 Tip:** 키 관리는 클라우드 보안의 핵심으로, KMS와 CloudHSM을 조합하여 고도 보안을 달성할 수 있습니다. 특히, 봉투 암호화는 대량 데이터 처리 시 성능 최적화에 유리합니다.

---

### 개념 2: 비밀 정보 관리 및 자동화 (Secrets Manager 자동 교체, Parameter Store)
Secrets Manager는 애플리케이션의 비밀 정보(예: 데이터베이스 비밀번호, API 키)를 안전하게 저장하고, 자동으로 회전하는 서비스입니다. Parameter Store는 AWS Systems Manager를 통해 키-값 쌍 형태로 환경 설정 및 비밀 정보를 저장하는 기능으로, Parameter Store는 별도의 보안 메커니즘 없이도 사용할 수 있지만, Secrets Manager는 보안성이 더 높습니다. 두 서비스는 비밀 정보의 관리와 보안을 달리하는 방식으로, 애플리케이션의 요구에 따라 선택적으로 활용됩니다.

#### 작동 원리
1. **Secrets Manager 자동 교체**: 암호화된 비밀 정보를 저장하고, 특정 시간 간격 또는 이벤트 발생 시 자동으로 새로운 비밀 정보로 교체합니다. 예를 들어, RDS 인스턴스의 비밀번호를 매일 자동으로 변경합니다.
2. **Parameter Store 사용**: 환경 변수나 설정 값을 키-값 쌍으로 저장하여 애플리케이션에서 직접 접근 가능합니다. 단, Parameter Store는 별도의 암호화 없이 저장하므로, 보안 요구가 높은 경우 Secrets Manager를 권장합니다.
3. **권한 설정**: IAM 정책을 통해 Secrets Manager와 Parameter Store의 접근 권한을 세부적으로 설정하여 보안을 강화합니다.

> **💡 Tip:** Secrets Manager는 보안이 높은 비밀 정보 관리를 위해 사용하고, Parameter Store는 간단한 설정 관리에 적합합니다. 자동화된 비밀 교체는 보안 사고 시 대응 시간을 줄입니다.

---

### 개념 3: 인증서 자동화 및 보안 모니터링 (ACM, Macie)
AWS Certificate Manager (ACM)는 SSL/TLS 인증서를 자동으로 관리하고, 애플리케이션의 인증서를 AWS Certificate Manager에서 생성 및 배포합니다. 이는 인증서 유효 기간 만료 시 자동으로 새 인증서를 발급하고, 인증서를 EC2, ALB, CloudFront 등에 배포합니다. Macie는 데이터 유출 방지 및 보안 위험을 감지하는 서비스로, S3 버킷, RDS 데이터베이스 등에서 감시하며, 감지된 위험을 알림 및 보고서로 제공합니다.

#### 주요 특징
1. ****ACM 인증서 자동화**: 인증서 생성, 배포, 만료 알림 등 전 과정을 자동화하여 관리 부담을 줄입니다. 예를 들어, 웹 서버 인증서를 CloudFront에 자동으로 배포합니다.
2. ****Macie의 데이터 감시**: S3 버킷의 파일 접근 로그를 분석하여 불법 접근이나 이상 행동을 감지합니다. 예를 들어, 특정 IP에서 빈번한 파일 다운로드를 감지하고 알림을 발송합니다.
3. ****보안 통합**: ACM과 Macie는 AWS 보안 아키텍처에서 상호 보완적인 역할을 하며, 인증서 관리와 데이터 보호를 동시에 수행합니다. 예를 들어, 인증서가 만료되면 Macie가 데이터 접근 권한을 검토합니다.

> **💡 Tip:** ACM은 인증서 관리의 효율성을 높이고, Macie는 데이터 보안을 실시간으로 감시합니다. 두 서비스를 결합하면 클라우드 환경의 보안을 종합적으로 강화할 수 있습니다.

## 🖥️ AWS 콘솔에서 ACM 사용하기

### Step 1: ACM 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: [https://console.aws.amazon.com](https://console.aws.amazon.com)  
2. 상단 검색창에 **"ACM"**을 입력하고, **AWS Certificate Manager** 서비스를 선택합니다.  
3. **"ACM"**을 클릭하여 대시보드로 이동합니다.  

> **📸 화면 확인:** ACM 대시보드에 들어가면, 생성된 인증서 목록과 상태가 표시됩니다.  

---

### Step 2: 인증서 생성 및 KMS CMK 설정  
1. **"Request a certificate"** 버튼을 클릭합니다.  
   - 이 버튼은 인증서 요청 창으로 이동합니다.  
2. **"Domain name"** 필드에 도메인명을 입력합니다 (예: `example.com`).  
   - **"Validation method"**에서 **"DNS validation"**을 선택합니다.  
3. **"Options"** 섹션에서 **"Key store"** 설정을 확인합니다.  
   - **"Use AWS KMS"**를 선택하고, **KMS CMK**를 지정합니다.  
   - **KMS CMK**는 AWS Key Management Service에서 생성한 고객 관리 키입니다.  

> **📸 화면 확인:** **"Options"** 섹션에서 **KMS CMK**를 선택한 후, **"Validate"** 버튼을 클릭합니다.  

4. **DNS validation**을 완료한 후, **"Confirm"** 버튼을 클릭하여 인증서를 생성합니다.  
   - 생성된 인증서는 **"Pending validation"** 상태로 표시됩니다.  

> **⚠️ 주의:** KMS CMK는 인증서 암호화에 사용됩니다. **KMS CMK**가 미리 생성되어 있어야 합니다.  

---

### Step 3: KMS CMK 설정 및 권한 확인  
1. **"AWS KMS"** 서비스로 이동하여 **CMK 생성**을 확인합니다.  
   - URL: [https://console.aws.amazon.com/kms/home](https://console.aws.amazon.com/kms/home)  
2. **"Create key"** 버튼을 클릭하고, **CMK**를 생성합니다.  
   - **Key usage**에서 **"Encrypt/Decrypt"**를 선택합니다.  
3. **ACM**에서 생성한 인증서가 KMS CMK를 사용하도록 설정되었는지 확인합니다.  
   - ACM 인증서 세부 정보에서 **"Key store"** 섹션을 확인합니다.  

> **📸 화면 확인:** ACM 인증서 세부 정보에서 **"Key store"**가 **KMS CMK**로 설정된 것을 확인합니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **ACM 대시보드**에서 생성된 인증서의 **"Status"**를 확인합니다.  
   - **"Valid"** 상태가 되면 정상적으로 작동합니다.  
2. **"Download"** 버튼을 클릭하여 인증서를 다운로드하고, **KMS CMK**를 사용하는지 확인합니다.  
3. **CloudFront** 또는 **API Gateway**에서 인증서를 적용하여 정상 작동을 테스트합니다.  
   - 예: **CloudFront**에서 인증서를 배포하고, HTTPS 연결을 확인합니다.  

> **💡 Tip:** ACM 인증서는 **AWS WAF** 또는 **CloudFront**와 연동하여 보안을 강화할 수 있습니다.  

---

### ✅ 체크리스트: ACM 설정 완료 여부  
- [ ] ACM 서비스에 접근할 수 있음  
- [ ] KMS CMK 생성 및 설정 완료  
- [ ] ACM 인증서 생성 및 DNS 검증 완료  
- [ ] 인증서 상태가 **Valid** 상태로 변경됨  
- [ ] 인증서를 **CloudFront** 또는 **API Gateway**에 적용함  

> **💰 비용 주의:** ACM 인증서는 **무료**이지만, KMS CMK는 **사용량에 따라 요금이 부과**됩니다. 프리티어를 활용해 비용을 점검하세요.

## ⌨️ AWS CLI로 ACM 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**⚠️ 주의:** AWS CLI를 사용하려면 먼저 [AWS CLI 설치 가이드](https://docs.aws.amazon.com/cli/latest/userguide/cli-install.html)를 참고하여 설정해야 합니다.  
**💡 Tip:** AWS CLI를 사용하기 전에 `aws configure` 명령어로 AWS 액세스 키, 비밀 키, 리전, 출력 형식을 설정하세요.

---

### 예제 1: ACM 인증서 목록 조회
```bash
# [ACM 인증서 목록 조회]
aws acm list-certificates --query 'Certificates[*].Arn' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query 'Certificates[*].Arn'` | 인증서 ARN 필터링 | `arn:aws:acm:us-east-1:123456789012:certificate/12345678-1234-1234-1234-123456789012` |
| `--output table` | 표 형식으로 출력 | - |

**예상 출력:**
```
| Arn                                                                 |
|------------------|
| arn:aws:acm:us-east-1:123456789012:certificate/12345678-1234-1234-1234-123456789012 |
```

---

### 예제 2: ACM 인증서 생성
```bash
# [ACM 인증서 요청]
aws acm request-certificate \
    --domain-name "example.com" \
    --validation-method "DNS" \
    --subject-name "CN=example.com" \
    --tags "Key=Environment,Value=Production"
```

**필수 옵션:**
- `--domain-name`: 인증서 발급 대상 도메인
- `--validation-method`: DNS 또는 EMAIL (기본값: DNS)
- `--subject-name`: 인증서 주제 (예: CN=example.com)

**예상 출력:**
```json
{
    "CertificateArn": "arn:aws:acm:us-east-1:123456789012:certificate/12345678-1234-1234-1234-123456789012"
}
```

---

### 예제 3: ACM 인증서 수정
```bash
# [ACM 인증서 상태 업데이트]
aws acm update-certificate --certificate-arn "arn:aws:acm:us-east-1:123456789012:certificate/12345678-1234-1234-1234-123456789012" --status "PENDING_DELETION"
```

**옵션 설명:**
- `--certificate-arn`: 수정할 인증서 ARN
- `--status`: 상태 변경 (예: `PENDING_DELETION`, `ISSUED`, `EXPIRED`)

---

### 예제 4: ACM 인증서 삭제
```bash
# [ACM 인증서 삭제]
aws acm delete-certificate --certificate-arn "arn:aws:acm:us-east-1:123456789012:certificate/12345678-1234-1234-1234-123456789012"

# 삭제 확인
aws acm describe-certificate --certificate-arn "arn:aws:acm:us-east-1:123456789012:certificate/12345678-1234-1234-1234-123456789012"
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. `describe-certificate` 명령어로 삭제 전 상태를 확인하세요.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws acm list-certificates
aws acm describe-certificate --certificate-arn "arn:aws:acm:us-east-1:123456789012:certificate/12345678-1234-1234-1234-123456789012"

# 생성
aws acm request-certificate --domain-name "example.com" --validation-method "DNS"

# 삭제
aws acm delete-certificate --certificate-arn "arn:aws:acm:us-east-1:123456789012:certificate/12345678-1234-1234-1234-123456789012"
```

---

### 📌 AWS CLI 사용 시 주의사항
1. **비용 관리:** 인증서 요청 시 `--validation-method`를 `DNS`로 설정하면 자동화된 도메인 검증이 가능합니다.  
2. **프리티어 사용:** ACM은 프리티어가 제공되나, 인증서 생성 시 도메인 검증을 위한 자동화된 도메인 이름을 사용해야 합니다.  
3. **권한 설정:** `acm:RequestCertificate`, `acm:DescribeCertificate` 권한이 필요합니다.  
4. **삭제 안전성:** `delete-certificate`는 즉시 실행되므로, `describe-certificate`로 상태를 확인하세요.  

---

### 🧾 CLI 명령어 테이블
| 명령어 | 기능 | 예시 |
|-------|-|-|
| `list-certificates` | 인증서 목록 조회 | `aws acm list-certificates` |
| `request-certificate` | 인증서 요청 | `aws acm request-certificate --domain-name "example.com"` |
| `describe-certificate` | 인증서 상세 정보 조회 | `aws acm describe-certificate --certificate-arn "arn:aws:acm:..."` |
| `delete-certificate` | 인증서 삭제 | `aws acm delete-certificate --certificate-arn "arn:aws:acm:..."` |
| `update-certificate` | 인증서 상태 업데이트 | `aws acm update-certificate --certificate-arn "arn:aws:acm:..." --status "PENDING_DELETION"` |

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 ACM 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **ACM 인증서 자동화 및 통합**  
   - 설명: AWS Certificate Manager은 SSL/TLS 인증서의 자동 생성, 재발급, 만료 알림을 제공하여 보안 관리 효율성을 높입니다. 시험에서는 인증서 자동화 정책과 AWS 서비스 통합 사례(예: ALB, CloudFront)를 묻는 문제가 자주 출제됩니다.  
   - 키워드: `인증서 자동화`, `AWS Certificate Manager`

2. **ACM과 Secrets Manager의 차이점**  
   - 설명: ACM은 인증서 관리에 특화되어 있으며, Secrets Manager는 비밀 정보(예: API 키, 비밀번호) 저장에 사용됩니다. 시험에서는 "인증서 저장"과 "비밀 정보 저장"을 구분하는 문제로 출제됩니다.  
   - 키워드: `인증서 관리`, `비밀 정보 저장`

3. **ACM의 제한사항과 한계**  
   - 설명: ACM은 AWS 서비스(예: ALB, CloudFront)에만 직접 통합 가능하며, 외부 서버에 직접 배포할 수 없습니다. 시험에서는 ACM의 사용 범위와 제약 조건을 묻는 문제가 자주 등장합니다.  
   - 키워드: `제한사항`, `외부 서버 통합`

4. **ACM과 Parameter Store의 비교**  
   - 설명: Parameter Store는 환경 변수나 애플리케이션 설정을 저장하는 데 사용되며, ACM은 인증서 관리에 특화되어 있습니다. 시험에서는 "인증서 저장" vs "환경 변수 저장"을 구분하는 문제가 자주 출제됩니다.  
   - 키워드: `환경 변수`, `인증서 저장`

5. **ACM의 비용 구조 이해**  
   - 설명: ACM은 기본적으로 무료이며, 인증서 생성/재발급 시 AWS 서비스(예: Route 53, ALB) 사용량에 따라 비용이 발생합니다. 시험에서는 비용 최적화 전략과 사용량 기반 요금제를 묻는 문제가 있습니다.  
   - 키워드: `비용 구조`, `사용량 요금제`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "Secrets Manager로 인증서 저장"이라는 문장이 등장할 때 ACM 대신 Secrets Manager를 선택할 수 있습니다. | **ACM**은 인증서 관리에 특화되어 있으므로 이 경우 ACM을 선택해야 합니다. |
| 함정 2 | "인증서 저장"이라는 키워드가 등장할 때 Parameter Store를 선택할 수 있습니다. | **ACM**은 인증서 저장에 전용 서비스이므로 이 경우 ACM을 선택해야 합니다. |
| 함정 3 | "SSL/TLS 인증서 생성" 문제에서 KMS 대신 ACM을 선택할 수 있습니다. | **ACM**은 인증서 생성에 최적화되어 있으므로 이 경우 ACM을 선택해야 합니다. |

#### 🔄 ACM vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | ACM | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | SSL/TLS 인증서 자동화 및 관리 | Secrets Manager (비밀 정보 저장), Parameter Store (환경 변수 저장) | 인증서 발급/재발급이 필요한 경우 |
| 확장성 | AWS 서비스와 원활한 통합 가능 | 외부 시스템과의 통합이 필요할 때 | 내부 AWS 인프라에 집중할 때 |
| 비용 | 기본적으로 무료, 인증서 발급 시 AWS 서비스 사용량 요금 적용 | Secrets Manager는 요청별 요금, Parameter Store는 요청 수 요금 | 사용량 기반 비용 최적화가 필요한 경우 |
| 지연시간 | 자동화로 인해 수동 관리 시간 감소 | 수동 관리 시 지연 발생 | 자동화된 인증서 관리가 필요한 경우 |

#### 📝 시험 대비 체크리스트
- [ ] ACM의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  **예:** "AWS Certificate Manager은 SSL/TLS 인증서를 자동으로 생성, 재발급, 관리하는 서비스입니다."  
- [ ] ACM를 선택해야 하는 시나리오를 알고 있는가?  
  **예:** "ALB의 HTTPS 설정에 SSL 인증서를 자동으로 적용해야 할 때"  
- [ ] ACM의 제한사항/한계를 알고 있는가?  
  **예:** "외부 서버에 직접 배포할 수 없고, AWS 서비스에만 통합 가능합니다."  
- [ ] ACM와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  **예:** "Secrets Manager는 비밀 정보 저장에 사용되며, ACM은 인증서 관리에 특화되어 있습니다."  
- [ ] ACM의 비용 구조를 이해하고 있는가?  
  **예:** "기본적으로 무료이며, 인증서 발급 시 AWS 서비스 사용량에 따라 비용이 발생합니다."  

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 ACM를 떠올리세요:  
> - **인증서 자동화**  
> - **SSL/TLS 인증서 관리**  
> - **AWS Certificate Manager**  
> - **ALB/CloudFront 통합**  
> - **SSL 인증서 생성/재발급**

---

| [⬅️ Parameter Store](./Parameter-Store.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 4](../README.md) | [Macie ➡️](./Macie.md) |

---
