---

| [⬅️ 이전 Day](../day1/README.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 4](../README.md) | [CloudHSM ➡️](./CloudHSM.md) |

---

# KMS 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** KMS는 데이터 보안을 위한 AWS의 **암호화 키 관리** 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 기업이 직접 암호화 키를 생성, 저장, 관리해야 했으며, 키를 안전하게 보호하는 방법이 부족했습니다. 예를 들어, 키를 서버에 저장하면 해킹 위험이 있으며, 키를 분산 저장하면 관리 복잡도가 증가합니다.
- **문제 2:** 키 회전(ROTATION)이 수동으로 이루어져, 오래된 키로 인한 보안 취약점이 발생할 수 있었습니다. 키를 주기적으로 교체하지 않으면 해킹 시 이전 키를 통해 데이터가 유출될 수 있습니다.
- **문제 3:** 다양한 AWS 서비스 간 키 관리가 분산되어 있어, 일관된 보안 정책을 적용하는 것이 어렵습니다. 예를 들어, S3, RDS, DynamoDB 등 각 서비스별로 다른 키를 사용하는 경우, 키 관리가 복잡해집니다.

**KMS로 해결:**
- **해결 1:** AWS가 제공하는 중앙 집중식 키 관리 시스템을 통해 키 생성, 저장, 암호화/복호화를 안전하게 처리합니다. KMS는 FIPS 140-2 인증된 하드웨어 보안 모듈(HSM)을 기반으로 하여 키를 보호합니다.
- **해결 2:** 자동 키 회전 기능을 통해 키의 수명을 설정하고, 지정된 주기마다 키를 자동으로 교체합니다. 이는 보안 취약점을 줄이고, 관리자 부담을 줄입니다.
- **해결 3:** AWS 서비스 간 키를 공유하고 일관된 보안 정책을 적용할 수 있습니다. 예를 들어, S3에 저장된 데이터는 KMS 키로 암호화되며, RDS 데이터베이스도 동일한 키를 사용해 통합된 보안 체계를 구축할 수 있습니다.

### 비유로 이해하기
KMS는 기업의 금고를 대신하는 시스템입니다. 기존에는 사장이 직접 금고에 키를 두고, 매일 키를 바꾸어야 했습니다. KMS는 이 금고를 클라우드에 두고, 키를 안전하게 보관하며, 필요 시 자동으로 키를 제공해 줍니다. 또한, 키를 바꾸는 일도 자동으로 처리해 주며, 모든 직원이 동일한 키를 사용해 금고를 열 수 있도록 합니다. 이처럼 KMS는 기업이 보안을 유지하면서도 키 관리를 효율적으로 도와줍니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 데이터 암호화를 위한 키 관리 | AWS S3에 저장된 고객 데이터를 KMS 키로 암호화해 저장 |
| 시나리오 2 | 서비스 간 일관된 보안 정책 적용 | RDS 데이터베이스와 DynamoDB 모두 KMS 키를 사용해 데이터를 암호화 |
| 시나리오 3 | 자동 키 회전이 필요한 시스템 | 매일 자동으로 키를 교체해 보안 위험을 최소화하는 애플리케이션 |

**이럴 때 KMS를 선택하세요:**
- ✅ **데이터 암호화**가 필요한 시스템 (S3, RDS, DynamoDB 등)
- ✅ **중앙 집중식 키 관리**가 필요한 환경
- ✅ **자동 키 회전**을 통해 보안을 강화해야 하는 경우

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **단일 서버 기반 암호화 키 관리** → **Secrets Manager** (단일 서버에서 키 관리 시)
- ❌ **인증서 관리** → **ACM** (SSL/TLS 인증서 자동화 시)
- ❌ **데이터 키 생성 및 복호화** → **Envelope Encryption** (데이터 키를 별도로 관리해야 할 때)

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **S3** | 데이터 암호화 및 키 관리 | KMS 키를 사용해 S3 오브젝트 암호화 |
| **RDS** | 데이터베이스 암호화 | RDS 인스턴스에 KMS 키를 적용해 데이터 암호화 |
| **CloudFront** | 콘텐츠 전송 시 암호화 | CloudFront 배달 콘텐츠에 KMS 키로 암호화된 데이터 사용 |
| **Secrets Manager** | 자동 키 회전 및 보안 정책 통합 | KMS 키를 Secrets Manager에서 자동으로 회전 |
| **ACM** | 인증서 자동화 및 키 관리 | ACM 인증서를 KMS 키로 보호해 SSL/TLS 통신 보안 강화 |

**자주 사용되는 아키텍처 패턴:**
```
User → CloudFront → S3 (KMS 암호화) → RDS (KMS 암호화)
           ↓
       Secrets Manager (KMS 키 회전)
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **CMK 생성 및 관리** | $0.00025/요청 | 월 25,000 요청 무료 |
| **데이터 암호화/복호화** | $0.000005/요청 | 12개월 무료 |
| **CloudHSM 사용** | $0.25/시간 | 항상 무료 (단, 사용량 제한) |

**비용 최적화 팁:**
1. 💡 **프리티어 활용:** 초기 사용 시 요청 수를 제한하여 비용 절감 (월 25,000 요청 무료)
2. 💡 **자동 키 회전:** 수동 키 관리로 인한 오류를 방지해 비용 절감
3. 💡 **CloudHSM 사용:** 필요 시 하드웨어 보안 모듈을 활용해 복잡한 키 관리를 최소화

> **⚠️ 비용 주의:** KMS는 요청 수에 따라 비용이 발생하므로, 과도한 키 생성/암호화 작업이 없는지 주기적으로 모니터링해야 합니다. 또한, CloudHSM은 사용량 제한이 있으므로, 대규모 키 관리 시 비용이 급증할 수 있습니다.

## 📚 핵심 개념

### 개념 1: KMS CMK 관리 및 회전  
KMS CMK는 AWS Key Management Service에서 생성하고 관리하는 **암호화 키**로, 데이터를 암호화하고 복호화하는 데 사용됩니다. CMK는 **대칭 키**(Data Key)와 **비대칭 키**(Public/Private Key)로 나뉘며, AWS는 자동으로 키를 생성, 저장, 회전하는 기능을 제공합니다. CMK 관리는 데이터 보안을 유지하고, 키의 노출 위험을 줄이는 데 필수적입니다. 특히, **데이터 키 회전**(Key Rotation)은 정기적으로 키를 변경해 해킹 시 피해를 최소화하는 전략입니다.  

#### 왜 중요한가?  
- **보안 강화**: 키의 노출 위험을 줄이고, 데이터 유출 시 피해를 제한합니다.  
- **규정 준수**: GDPR, HIPAA 등 규제 요구사항에 따라 키 관리가 필수적입니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |  
|-----|-----|-----|  
| CMK 유형 | **AWS Managed CMK** (AWS가 관리), **Customer Managed CMK** (사용자가 직접 관리) | S3 버킷 암호화에 사용되는 CMK는 AWS Managed입니다. |  
| 키 회전 정책 | 키를 자동으로 변경하는 주기 설정 (예: 30일) | IAM 정책으로 회전 주기를 정의합니다. |  
| 키 사용 범위 | 특정 리소스(예: S3, DynamoDB)에 암호화 키를 적용 | S3 버킷의 데이터 암호화에 CMK를 할당합니다. |  

> **💡 Tip:** AWS KMS 콘솔에서 CMK를 생성하고, CLI 명령어 `aws kms rotate-key`로 회전을 자동화할 수 있습니다. 회전 주기를 정의할 때는 **IAM 정책**을 통해 접근 권한을 제어하세요.  

---

### 개념 2: 봉투 암호화(Envelope Encryption)  
봉투 암호화는 **데이터 키(Data Key)**를 사용해 데이터를 암호화하고, 해당 키를 **CMK**로 다시 암호화하는 방식입니다. 이 방식은 데이터를 보호하면서도 키 관리의 복잡도를 줄입니다. 데이터 키는 메모리에 잠시 보관되고, CMK는 AWS KMS에 저장되어 보안이 강화됩니다.  

#### 작동 원리  
1. **데이터 키 생성**: AWS KMS가 임시 데이터 키를 생성합니다.  
2. **데이터 암호화**: 데이터 키로 데이터를 암호화합니다.  
3. **데이터 키 암호화**: 생성된 데이터 키를 CMK로 암호화하고, 데이터에 함께 저장합니다.  

> **💡 Tip:** 봉투 암호화는 S3, DynamoDB 등에서 데이터 암호화에 자주 사용됩니다. 데이터 키는 메모리에서만 존재하므로, **레코드 암호화**에 적합합니다.  

---

### 개념 3: ACM 인증서 자동화 및 통합  
ACM(AWS Certificate Manager)은 **SSL/TLS 인증서**를 자동으로 생성, 관리, 갱신하는 서비스입니다. 인증서는 애플리케이션의 보안을 강화하고, 인증서의 만료 시 자동으로 갱신해 인터넷 서비스 중단을 방지합니다. ACM은 CloudFront, Elastic Load Balancing, RDS 등과 원활하게 통합됩니다.  

#### 주요 특징  
1. **자동 갱신**: 인증서가 만료되기 30일 전에 자동으로 갱신됩니다.  
2. **무료 인증서 제공**: Let's Encrypt를 기반으로 무료 인증서를 발급합니다.  
3. **AWS 서비스 통합**: CloudFront, ALB, RDS 등에 바로 연결해 인증서를 적용할 수 있습니다.  

> **💡 Tip:** ACM으로 발급한 인증서는 AWS 서비스에만 사용할 수 있으므로, 외부 웹 서버에 적용하려면 **인증서 다운로드** 후 수동으로 설치해야 합니다.  

---  

### 개념 4: CloudHSM 하드웨어 보안 모듈 활용  
CloudHSM은 **하드웨어 보안 모듈(HSM)**을 클라우드에 제공해, 민감한 키를 보호하고 암호화 작업을 수행합니다. HSM은 FIPS 140-2 인증을 받은 하드웨어로, 키 저장소가 물리적으로 분리되어 있어 보안성이 높습니다. CloudHSM은 AWS KMS와 연동해 키 생성 및 암호화를 처리합니다.  

#### 주요 특징  
1. **FIPS 140-2 인증**: 정부 기관 및 금융 서비스에 적합한 보안 표준입니다.  
2. **키 저장소 분리**: 키는 하드웨어 내부에 저장되어 외부 접근이 불가능합니다.  
3. **AWS 서비스 통합**: KMS, RDS, Redshift 등과 연동해 암호화 작업을 자동화합니다.  

> **💡 Tip:** CloudHSM은 하드웨어 기반으로 보안성이 높기 때문에, **민감한 데이터 암호화**나 **정부 규정 준수** 시점에 활용하세요.  

---

### 개념 5: Secrets Manager 자동 교체 정책  
Secrets Manager는 **데이터베이스 비밀번호**, **API 키** 등 민감한 정보를 안전하게 저장하고, 자동으로 교체하는 서비스입니다. 교체 정책은 특정 주기(예: 90일)에 비밀번호를 자동으로 갱신해, 외부 공격 시 피해를 줄입니다.  

#### 주요 특징  
1. **자동 교체**: 정책에 따라 비밀번호를 주기적으로 변경합니다.  
2. **데이터베이스 연동**: RDS, MongoDB 등에 직접 연결해 비밀번호를 자동으로 갱신합니다.  
3. **IAM 정책**: 접근 권한을 제어해 보안을 강화합니다.  

> **💡 Tip:** Secrets Manager는 **AWS CLI**나 **Terraform**으로 자동화된 교체 정책을 설정할 수 있으며, **IAM 역할**을 통해 서비스 간 통합을 쉽게 구현할 수 있습니다.  

---  

### 개념 6: Parameter Store  
Parameter Store는 **환경 설정**, **비밀 정보**, **템플릿 파라미터** 등을 저장하고 관리하는 서비스입니다. SecureString 형식으로 암호화된 데이터를 저장할 수 있어, 민감한 정보도 안전하게 보관할 수 있습니다. Parameter Store는 AWS Systems Manager에 포함되어 있으며, CLI나 API를 통해 접근 가능합니다.  

#### 주요 특징  
1. **SecureString 형식**: 암호화된 데이터 저장 가능 (예: 비밀번호).  
2. **명확한 파라미터 관리**: 템플릿에 파라미터를 삽입해 배포 자동화 가능.  
3. **IAM 정책**: 접근 권한을 제어해 보안을 강화.  

> **💡 Tip:** Parameter Store는 **S3 버킷 설정**, **EC2 인스턴스 환경 변수** 등에 활용되며, **AWS CLI**로 `aws ssm put-parameter` 명령어로 데이터를 저장할 수 있습니다.  

---  

### 개념 7: Macie 데이터 보호 및 분류  
Macie는 **데이터 유출 방지**를 위한 서비스로, S3, DynamoDB 등에서 Sensitive Data를 자동으로 식별하고 분류합니다. AWS WAF와 연동해 보안 위협을 감지하며, 데이터의 **감염 위험**을 줄이는 데 기여합니다.  

#### 주요 특징  
1. **데이터 분류**: S3 버킷, DynamoDB 테이블에서 Sensitive Data(예: 사회보장번호)를 식별.  
2. **위협 감지**: AWS WAF와 연동해 이상 트래픽을 감지.  
3. **자동 보고**: 보안 이벤트를 AWS Security Hub에 통합 보고.  

> **💡 Tip:** Macie는 **데이터 유출 예방**에 필수적이며, **AWS WAF**와 연동해 실시간으로 보안 위협을 대응할 수 있습니다.

## 🖥️ AWS 콘솔에서 KMS 사용하기

### Step 1: KMS 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: [https://console.aws.amazon.com](https://console.aws.amazon.com)  
   - AWS 계정이 활성화되어 있고, IAM 권한이 부여된 사용자로 로그인해야 합니다.  
2. 상단 검색창에 **"KMS"**를 입력하고, 검색 결과에서 **"Key Management Service (KMS)"**를 클릭합니다.  
   - 검색 결과에서 **"KMS"** 항목이 나타나면 클릭하여 대시보드로 이동합니다.  

> **📸 화면 확인:** KMS 대시보드가 표시되면 정상입니다. **"Key Management"** 탭이 활성화되고, **"Customer Master Keys (CMKs)"** 목록이 표시됩니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **CMK 생성**  
   - 왼쪽 메뉴에서 **"Customer Master Keys"** → **"Create key"** 버튼을 클릭합니다.  
   - **"Create a customer managed key"** 탭에서 **"Create"**를 선택합니다.  
   - **"Key usage"**에서 **"Encipher/decipher"** 및 **"Generate data key"** 권한을 선택합니다.  
   - **"Origin"** 필드에서 **"AWS KMS"**를 선택하여 AWS 관리 키를 생성합니다.  

2. **암호화 키 정책 설정**  
   - 생성한 CMK를 선택한 후, **"Key policy"** 탭에서 **"Edit policy"**를 클릭합니다.  
   - **"Principal"** 필드에 **"root"** 또는 특정 IAM 역할을 추가하여 암호화 키의 접근 권한을 설정합니다.  
   - **"Resource-based policy"**에서 **"Allow"** 권한을 부여합니다.  

3. **암호화 키 상태 확인**  
   - **"Key state"**가 **"Enabled"**로 표시되어야 합니다.  
   - **"Key alias"**를 생성하여 키를 쉽게 참조할 수 있도록 합니다.  

> **📸 화면 확인:** **"Create key"** 화면에서 **"Key name"** 및 **"Description"** 필드가 제대로 입력되었는지 확인합니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **Envelope Encryption 구성**  
   - **"Customer Master Keys"** 목록에서 생성한 CMK를 선택합니다.  
   - **"Actions"** → **"Generate data key"**를 클릭하여 데이터 키를 생성합니다.  
   - 생성된 데이터 키의 **"Plaintext"**와 **"Ciphertext"** 값을 확인합니다.  
   - **"Plaintext"** 값을 암호화할 데이터로, **"Ciphertext"** 값을 저장하여 암호화된 데이터를 보호합니다.  

2. **Secrets Manager 자동 교체 설정**  
   - AWS Secrets Manager로 이동하여 **"Secrets"** → **"Create secret"**을 클릭합니다.  
   - **"Secret name"**을 입력하고, **"Secret string"**에 암호화된 데이터를 입력합니다.  
   - **"Secrets Manager"**에서 **"Rotation"** 설정을 활성화하여 KMS CMK를 사용해 자동으로 암호화 키를 교체합니다.  

3. **ACM 및 Macie 연동**  
   - **"AWS Certificate Manager (ACM)"**에서 **"Import certificate"**를 선택하고, KMS CMK로 인증서를 암호화합니다.  
   - **"Amazon Macie"**에서 **"Settings"** → **"Data identifiers"**에서 KMS CMK로 암호화된 데이터를 감지하도록 설정합니다.  

> **⚠️ 주의:** KMS CMK를 사용할 때는 **"Key policy"**와 **"Resource-based policy"**를 정확히 설정해야 합니다. 권한 부여 오류는 암호화/복호화 실패로 이어질 수 있습니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인 방법**  
   - **"Customer Master Keys"** 목록에서 생성한 CMK를 선택하여 **"Key details"**를 확인합니다.  
   - **"Key ARN"**과 **"Key state"**가 **"Enabled"**인지 확인합니다.  

2. **상태 확인 방법**  
   - **"Key state"**가 **"Enabled"**로 표시되고, **"Key alias"**가 생성되었는지 확인합니다.  
   - **"Usage"** 탭에서 암호화/복호화 요청이 성공적으로 처리되는지 확인합니다.  

3. **정상 동작 테스트**  
   - **"Generate data key"**를 사용해 데이터 키를 생성한 후, **"Encrypt"** 및 **"Decrypt"** 기능을 테스트합니다.  
   - 암호화된 데이터를 복호화할 때는 생성된 데이터 키의 **"Ciphertext"** 값을 사용해야 합니다.  

> **💡 Tip:** AWS Free Tier은 KMS 사용 시 **1년간 25개의 CMK 생성**과 **100,000개의 암호화 요청**을 제공합니다. 무료로 테스트하기 위해 **AWS Free Tier**를 활용하세요.  

---

### ✅ 체크리스트: KMS 설정 완료 여부  
- [ ] KMS CMK가 성공적으로 생성되었는지 확인  
- [ ] Key Policy 및 Resource-based Policy가 올바르게 설정되었는지 확인  
- [ ] Secrets Manager 자동 교체 및 ACM 연동이 완료되었는지 확인  
- [ ] 데이터 암호화/복호화 기능이 정상 작동하는지 테스트  
- [ ] KMS 리소스 상태가 **"Enabled"**로 표시되는지 확인  

> **📝 참고:** KMS 사용 시 비용이 발생할 수 있으므로, **AWS Cost Explorer**에서 사용량을 주기적으로 모니터링하세요.

## ⌨️ AWS CLI로 KMS 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

> **💡 Tip:** AWS CLI를 사용하기 전에 `aws configure`로 액세스 키, 리전, 리전, 콜백 URL을 설정해야 합니다.  
> **⚠️ 주의:** CLI 명령어는 서비스별로 차이가 있으므로, KMS 관련 명령어는 `aws kms`로 시작합니다.

---

### 예제 1: KMS CMK 목록 조회
```bash
# CMK 목록 조회
aws kms list-keys --query '[].KeyId' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| --query | 결과 필터링 | '[].KeyId' |
| --output | 출력 형식 | table, json, text |

**예상 출력:**
```
KeyId
--------------------------------------------------
arn:aws:kms:us-east-1:123456789012:key/abcd1234-a123-4567-8abc-def123456789
```

> **💡 Tip:** `--query`를 사용해 특정 필드만 출력할 수 있으며, `--output table`은 가독성을 높입니다.

---

### 예제 2: KMS CMK 생성
```bash
# CMK 생성
aws kms create-key \
    --description "My Encryption Key" \
    --policy file://key-policy.json
```

**필수 옵션:**
- `--description`: CMK 설명 (필수)
- `--policy`: 키 정책 파일 경로 (선택)

**예상 출력:**
```json
{
    "KeyMetadata": {
        "KeyId": "arn:aws:kms:us-east-1:123456789012:key/abcd1234-a123-4567-8abc-def123456789",
        "Description": "My Encryption Key",
        "Enabled": true
    }
}
```

> **💡 Tip:** `--policy` 옵션을 사용해 JSON 파일로 키 정책을 정의할 수 있습니다. 예시 파일은 아래와 같습니다:  
```json
{
  "Version": "1",
  "Statement": [
    {
      "Effect": "Allow",
      "Principal": { "AWS": "arn:aws:iam::123456789012:root" },
      "Action": "kms:*"
    }
  ]
}
```

---

### 예제 3: KMS CMK 정책 수정
```bash
# CMK 정책 업데이트
aws kms put-key-policy \
    --key-id "arn:aws:kms:us-east-1:123456789012:key/abcd1234-a123-4567-8abc-def123456789" \
    --policy-name "default" \
    --policy file://updated-policy.json
```

> **⚠️ 주의:** KMS는 CMK 자체를 수정할 수 없으므로, `put-key-policy`를 사용해 정책을 갱신해야 합니다.

---

### 예제 4: KMS CMK 삭제
```bash
# CMK 삭제
aws kms delete-key \
    --key-id "arn:aws:kms:us-east-1:123456789012:key/abcd1234-a123-4567-8abc-def123456789"

# 삭제 확인
aws kms describe-key \
    --key-id "arn:aws:kms:us-east-1:123456789012:key/abcd1234-a123-4567-8abc-def123456789"
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없으므로, `--force-delete`를 사용해 즉시 삭제할 수 있습니다.  
> **💡 Tip:** 삭제 후 `describe-key` 명령어로 상태를 확인할 수 있습니다.

---

### 자주 사용하는 명령어 정리
```bash
# CMK 조회
aws kms list-keys
aws kms describe-key --key-id "arn:aws:kms:..."

# CMK 생성/수정/삭제
aws kms create-key --description "..."
aws kms put-key-policy --key-id "arn:..." --policy-file "..."
aws kms delete-key --key-id "arn:..."
```

> **💡 Tip:** `aws kms get-aliases` 명령어로 앨리어스를 확인하고, `aws kms create-alias`로 앨리어스를 생성할 수 있습니다.

---

### 📌 AWS CLI 사용 시 주의사항
- **비용:** KMS는 1,000건 이상의 요청 시 0.00025 USD/요청 요금이 적용됩니다. 프리티어는 25,000건까지 무료입니다.
- **리전:** KMS는 AWS 리전별로 제공되며, `aws configure set region`으로 설정해야 합니다.
- **정책:** CMK의 정책은 IAM 역할과 연동되어야 하므로, 정책 권한을 철저히 검토해야 합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 KMS 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **포인트 1: CMK 관리 및 회전 정책**
   - 설명: KMS의 핵심은 **Customer Master Key(CMK)** 관리입니다. 시험에서 CMK 생성, 키 회전 정책 설정, 키의 암호화/복호화 역할을 묻는 문제가 빈번합니다. 특히 **키 회전 정책**은 보안 강화를 위한 필수 사항으로, 자동화된 정책 구성을 이해해야 합니다.
   - 키워드: `CMK`, `키 회전`, `자동화 정책`

2. **포인트 2: 봉투 암호화(Envelope Encryption) 메커니즘**
   - 설명: 데이터 암호화 시 **데이터 키(Data Key)**와 **암호화 키(Encrypted Data Key)**의 구분이 시험 핵심입니다. 봉투 암호화는 데이터 키를 KMS로 암호화하여 저장하는 방식으로, 이 메커니즘을 이해하지 않으면 시험에서 실수할 수 있습니다.
   - 키워드: `Envelope Encryption`, `데이터 키`, `암호화 키`

3. **포인트 3: Secrets Manager 자동 교체 정책**
   - 설명: KMS와 Secrets Manager의 연동은 **Secrets 자동 교체** 시나리오에서 중요합니다. 시험에서는 비밀번호, API 키 등 민감 정보를 자동으로 암호화하고 교체하는 방식을 묻는 문제가 자주 출제됩니다.
   - 키워드: `Secrets Manager`, `자동 교체`, `암호화 정책`

4. **포인트 4: CloudHSM 하드웨어 보안 모듈 활용**
   - 설명: KMS는 소프트웨어 기반 키 관리지만, **CloudHSM**은 하드웨어 기반 보안 모듈로 특화되어 있습니다. 시험에서는 두 서비스의 차이점과 사용 사례를 구분하는 문제가 자주 등장합니다.
   - 키워드: `CloudHSM`, `하드웨어 모듈`, `보안 요구사항`

5. **포인트 5: ACM 인증서 자동화 및 통합**
   - 설명: KMS와 ACM(ACM)의 연동은 **인증서 관리** 시나리오에서 핵심입니다. 시험에서는 인증서 자동 생성, 암호화, 키 관리의 통합 방식을 이해해야 하며, 이는 네트워크 보안 구현과 직접 관련됩니다.
   - 키워드: `ACM`, `인증서 자동화`, `키 관리 통합`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | KMS와 Secrets Manager의 차이를 혼동할 수 있습니다. 예: "Secrets Manager는 CMK를 사용하지 않음"이라는 오류 | KMS는 CMK 관리, Secrets Manager는 Secret 암호화에 CMK를 사용 |
| 함정 2 | 봉투 암호화에서 데이터 키와 암호화 키의 역할을 혼동할 수 있습니다. 예: "데이터 키는 암호화 키로 암호화됨"이라는 오류 | 데이터 키는 KMS로 암호화되어 저장되고, 암호화 키는 데이터 키로 암호화 |
| 함정 3 | KMS의 비용 구조에서 키 사용량과 비용의 관계를 오해할 수 있습니다. 예: "KMS는 키 사용량에 따라 비용이 증가함"이라는 오류 | KMS는 키 생성/삭제에 비용이 발생하며, 암호화/복호화는 무료 |

#### 🔄 KMS vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | KMS | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | CMK 관리, 암호화 키 보호 | CloudHSM(하드웨어 모듈), Secrets Manager(비밀 정보 관리) | 키 관리 vs 하드웨어 보안 모듈 vs 비밀 정보 암호화 |
| 확장성 | 클라우드 기반으로 확장성 높음 | CloudHSM은 하드웨어 리소스 제한 | 소프트웨어 기반 vs 하드웨어 리소스 의존 |
| 비용 | 키 생성/삭제에 비용 발생, 암호화/복호화 무료 | CloudHSM은 하드웨어 사용료 발생 | 키 관리 비용 vs 하드웨어 리소스 비용 |
| 지연시간 | 실시간 암호화/복호화 가능 | CloudHSM은 네트워크 지연 발생 | 빠른 처리 필요 vs 네트워크 지연 허용 |

#### 📝 시험 대비 체크리스트
- [ ] KMS의 핵심 목적을 한 문장으로 설명할 수 있는가? (예: "클라우드 기반의 키 관리 및 암호화 서비스")
- [ ] KMS를 선택해야 하는 시나리오를 알고 있는가? (예: "민감 정보 암호화, 키 회전 정책 필요 시")
- [ ] KMS의 제한사항/한계를 알고 있는가? (예: "하드웨어 보안 모듈과의 연동 제한")
- [ ] KMS와 비슷한 서비스의 차이점을 설명할 수 있는가? (예: "KMS vs CloudHSM: 소프트웨어 vs 하드웨어")
- [ ] KMS의 비용 구조를 이해하고 있는가? (예: "키 생성/삭제 비용, 암호화/복호화 무료")

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 KMS를 떠올리세요:  
> - `CMK` (Customer Master Key)  
> - `Envelope Encryption` (봉투 암호화)  
> - `Key Rotation` (키 회전)  
> - `Secrets Manager` (비밀 정보 관리)  
> - `ACM` (인증서 자동화)

---

| [⬅️ 이전 Day](../day1/README.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 4](../README.md) | [CloudHSM ➡️](./CloudHSM.md) |

---
