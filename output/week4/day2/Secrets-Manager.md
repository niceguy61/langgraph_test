---

| [⬅️ CloudHSM](./CloudHSM.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 4](../README.md) | [Parameter Store ➡️](./Parameter-Store.md) |

---

# Secrets Manager 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** Secrets Manager는 기밀 정보를 안전하게 저장하고 관리하기 위한 AWS의 보안 관리 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 애플리케이션 코드나 구성 파일에 비밀번호, API 키 등 민감 정보를 plain text로 저장해 보안 취약점이 발생합니다. 예: `db_password = "mySecret123!"`로 직접 입력한 경우.
- **문제 2:** 비밀 정보의 회전(ROTATION)을 수동으로 관리해야 하며, 실수로 이전 비밀번호를 잊거나 보안 규정을 위반할 위험이 있습니다.
- **문제 3:** 다중 AWS 리소스(예: RDS, Lambda, EC2)에 걸쳐 분산된 비밀 정보를 통합 관리하기 어려워 운영 효율성이 떨어집니다.

**Secrets Manager로 해결:**
- **해결 1:** 암호화된 형태로 AWS 클라우드에 안전하게 저장하고, 필요 시 암호화 키를 통해 접근합니다. 이는 KMS CMK와 봉투 암호화(Envelope Encryption) 기술을 기반으로 합니다.
- **해결 2:** 자동화된 회전 정책을 설정해 정해진 주기(예: 매일, 매주)에 비밀 정보를 자동으로 교체해 보안 위험을 최소화합니다.
- **해결 3:** 단일 포탈에서 모든 리소스에 걸쳐 비밀 정보를 중앙 집중식으로 관리해 운영 편의성과 보안 수준을 동시에 확보합니다.

### 비유로 이해하기
Secrets Manager는 집안의 보물 상자와 같은 역할을 합니다. 중요한 문서나 금전은 상자에 잠금 해제 시켜 보관하고, 필요할 때만 열어 사용합니다. 이처럼 민감 정보는 AWS 내부의 보안 시스템에 의해 암호화되어 저장되며, 접근 시에만 복호화되어 사용됩니다. 이는 보안 위협을 최소화하고, 팀원 간 정보 누출을 방지합니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 데이터베이스 연결 정보(예: RDS)를 안전하게 저장하고, 애플리케이션에서 자동으로 접근 | 웹 애플리케이션에서 MySQL DB의 비밀번호를 Secrets Manager에서 추출해 연결 |
| 시나리오 2 | 자동화된 비밀 정보 회전 정책을 통해 보안 규정 준수(예: GDPR, SOC2)를 달성 | 매일 밤 12시에 API 키를 자동으로 교체해 보안 리스크 감소 |
| 시나리오 3 | AWS 서비스 간 통합 시 암호화된 비밀 정보를 공유 | Lambda 함수에서 S3 버킷의 암호화 키를 Secrets Manager에서 불러와 암호화된 파일 접근 |

**이럴 때 Secrets Manager를 선택하세요:**
- ✅ **민감 정보를 클라우드에 안전하게 저장해야 할 때**
- ✅ **비밀 정보 회전 정책을 자동화해야 할 때**
- ✅ **다중 AWS 리소스에 걸친 비밀 정보 통합 관리가 필요할 때**

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **단순한 암호화 필요 시 → AWS KMS** (Secrets Manager와 KMS는 함께 사용되지만, 단독 암호화는 KMS에 위임)
- ❌ **서버리스 환경에서 키 관리 필요 시 → AWS CloudHSM** (하드웨어 기반 보안 모듈로 더 높은 보안 수준 제공)
- ❌ **인증서 관리 필요 시 → AWS ACM** (SSL/TLS 인증서 자동화 및 관리)

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **AWS KMS** | 비밀 정보를 암호화하는 데 사용되는 키 관리 서비스 | Secrets Manager → KMS CMK → 암호화된 Secret 저장 |
| **AWS ACM** | 인증서 관리 및 SSL/TLS 인증서 자동화 | Secrets Manager → ACM → 암호화된 인증서 자동 배포 |
| **AWS Macie** | 데이터 유출 방지 및 비밀 정보 감지 | Macie → Secrets Manager 연동 → 비밀 정보 누출 감지 및 경고 |

**자주 사용되는 아키텍처 패턴:**
```
User → API Gateway → Lambda (Secrets Manager에서 비밀 정보 가져오기) → RDS
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **Secrets Manager 저장 및 액세스** | $0.45/GB/월 (기본 저장) | 12개월 무료 (월 10GB 이하) |
| **Secrets Manager 암호화 키 사용** | $0.20/1,000,000 개체 | 12개월 무료 (10,000,000 개체) |
| **Secrets Manager 회전 정책 실행** | $0.00001/회전 | 항상 무료 |

**비용 최적화 팁:**
1. 💡 **프리티어 활용:** 초기 12개월 동안 무료 할당량(10GB 저장, 10M 개체)을 활용해 비용 절감.
2. 💡 **비밀 정보 최소화:** 불필요한 정보를 저장하지 않도록 정기적으로 체크리스트 작성.
3. 💡 **암호화 키 관리:** KMS CMK를 사용해 암호화 키를 별도 관리해 비용 절감.

> **⚠️ 비용 주의:** 비밀 정보의 저장량이 10GB를 초과하거나, 회전 정책이 자주 실행될 경우 비용이 급증할 수 있습니다. AWS Cost Explorer를 사용해 정기적으로 사용량 모니터링하세요.

## 📚 핵심 개념

### 개념 1: KMS CMK 관리 및 회전  
AWS Key Management Service (KMS)는 AWS Secrets Manager에서 데이터를 암호화하기 위한 **암호화 키 관리 서비스**입니다. CMK( Customer Master Key)는 암호화 키의 루트 키로, Secret 데이터를 암호화하고 복호화하는 데 사용됩니다. Secrets Manager는 KMS를 통해 Secret 데이터를 암호화하여 저장하고, 암호화된 데이터는 AWS RDS, ElastiCache 등과 같은 서비스에서 안전하게 사용할 수 있도록 합니다.  

#### 왜 중요한가?  
- **보안 강화**: CMK를 통해 Secret 데이터를 암호화하여 해킹 시 데이터 노출을 방지합니다.  
- **관리 효율성**: KMS는 키 생성, 회전, 삭제, 접근 제어 등을 자동화하여 운영 부담을 줄입니다.  
- **규제 준수**: GDPR, HIPAA 등 규제 요구사항을 충족하기 위한 키 관리 체계를 제공합니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| CMK 유형 | AWS KMS에서 생성된 키 또는 사용자 관리 키 | AWS KMS CMK 생성 후 Secrets Manager에 연결 |
| 키 회전 | 키를 주기적으로 변경하여 보안 강화 | 매일 자동 키 회전 정책 설정 |
| 사용 시나리오 | RDS 인증 정보, API 키 등 민감 데이터 암호화 | RDS 인스턴스에 Secrets Manager Secret 연결 |

> **💡 Tip:** CMK를 직접 관리하는 경우, AWS KMS에 키를 등록하고 Secrets Manager에서 해당 CMK를 선택해 암호화 키로 사용합니다.  

---

### 개념 2: 봉투 암호화(Envelope Encryption) 메커니즘  
Secrets Manager는 **봉투 암호화(Envelope Encryption)** 기법을 사용해 데이터를 안전하게 저장합니다. 이 메커니즘은 데이터를 암호화하기 위해 **데이터 키(Data Key)**를 생성하고, 이 데이터 키를 **CMK로 암호화**한 후 Secret 데이터를 암호화하는 방식입니다.  

#### 작동 원리  
1. **데이터 키 생성**: Secret 데이터를 암호화하기 위해 임시 데이터 키가 생성됩니다.  
2. **데이터 키 암호화**: 생성된 데이터 키는 CMK를 사용해 암호화됩니다.  
3. **Secret 저장**: 암호화된 Secret 데이터와 암호화된 데이터 키를 함께 저장합니다.  

> **💡 Tip:** 봉투 암호화는 Secret 데이터를 직접 CMK로 암호화하지 않고, 데이터 키를 중간에 암호화해 성능과 보안을 균형 있게 제공합니다.  

---

### 개념 3: CloudHSM 하드웨어 보안 모듈 활용  
CloudHSM은 **하드웨어 기반 암호화 모듈**로, Secrets Manager와 연동해 고도로 보안이 필요한 환경에서 **암호화 키를 안전하게 관리**합니다. CloudHSM은 물리적으로 보호된 하드웨어로, 해킹 시 키 노출 위험이 낮아 금융, 의료 등 민감한 데이터 저장에 적합합니다.  

#### 주요 특징  
1. **하드웨어 보안**: 물리적으로 보호된 하드웨어로, 키는 메모리에 저장되어 해킹 시 노출 위험이 없습니다.  
2. **KMS 통합**: AWS KMS와 연동해 키 생성, 회전, 접근 제어를 자동화합니다.  
3. **규제 준수**: FIPS 140-2 인증을 받은 하드웨어로, 엄격한 보안 요구사항을 충족합니다.  

> **💡 Tip:** CloudHSM을 사용하면 CMK를 하드웨어 기반으로 관리해, 더 높은 보안 수준을 요구하는 시스템에서 Secrets Manager를 활용할 수 있습니다.

## 🖥️ AWS 콘솔에서 Secrets Manager 사용하기

### Step 1: Secrets Manager 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - 로그인 후, 상단 메뉴에서 **Services**를 클릭하고 "Secrets Manager"를 검색합니다.  
2. 검색 결과에서 **Secrets Manager**를 선택합니다.  
   - 이 서비스는 암호화된 비밀 정보를 안전하게 저장하고 관리하는 도구입니다.  

> **📸 화면 확인:** Secrets Manager 대시보드가 표시되면 정상입니다. **Secrets Manager** 탭이 활성화된 상태에서 작업을 시작합니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **Secret 생성**  
   - 상단 메뉴에서 **Create secret**을 클릭합니다.  
   - **Secret type**에서 **Other type (custom JSON)**를 선택합니다.  
   - **Secret name**에 `my-kms-secret`과 같은 이름을 입력합니다.  
   - **Secret value**에 `{"username": "admin", "password": "SecurePass123!"}`와 같은 JSON 형식의 데이터를 입력합니다.  

2. **암호화 설정**  
   - **Encryption key** 섹션에서 **AWS KMS**를 선택합니다.  
   - **Customer Master Key (CMK)**에서 **AWS managed CMK**를 선택하거나, 직접 생성한 CMK를 선택할 수 있습니다.  
   - **Key alias**에 `alias/my-kms-key`와 같은 이름을 입력합니다.  

3. **추가 설정**  
   - **Description**에 "KMS-encrypted secret for database"와 같은 설명을 입력합니다.  
   - **Tags**에 **Environment: Production**과 같은 태그를 추가합니다.  

> **📸 화면 확인:** **Create secret** 페이지에서 KMS CMK 선택 및 암호화 설정이 완료된 상태입니다.  

> **⚠️ 주의:** KMS CMK는 AWS가 관리하는 기본 키를 사용할 경우 무료입니다. 사용자 정의 키는 추가 비용이 발생할 수 있습니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **Secret 키 관리**  
   - 대시보드에서 생성한 Secret(`my-kms-secret`)을 클릭합니다.  
   - **Version** 탭에서 **Encryption key**를 확인합니다.  
   - KMS CMK가 올바르게 연결되었는지, 암호화된 데이터가 저장되었는지 확인합니다.  

2. **자동 교체 설정**  
   - **Rotation** 탭에서 **Rotate secret**을 클릭합니다.  
   - **Rotation schedule**에서 **Create a new rotation schedule**을 선택합니다.  
   - **Rotation interval**을 30일로 설정하고, **Lambda function**을 선택합니다.  
   - **Rotation rules**에서 **Rotate automatically**를 활성화합니다.  

3. **Parameter Store 통합**  
   - **Integration** 탭에서 **Parameter Store**를 선택합니다.  
   - **Parameter name**에 `db/password`와 같은 이름을 입력하고, **Value**에 Secret 값을 연결합니다.  
   - **Description**에 "Database password from Secrets Manager"를 추가합니다.  

> **⚠️ 주의:** Parameter Store는 AWS Systems Manager의 기능으로, Secret과 별도로 관리됩니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인**  
   - **Secrets** 탭에서 `my-kms-secret`을 선택합니다.  
   - **Version** 탭에서 암호화된 Secret 값을 확인합니다.  
   - **Tags**에서 추가한 태그가 정상적으로 표시되는지 확인합니다.  

2. **상태 확인**  
   - **Status** 탭에서 Secret이 **Enabled** 상태인지 확인합니다.  
   - **Rotation status**에서 자동 교체가 활성화되었는지, Lambda 함수가 정상적으로 연결되었는지 확인합니다.  

3. **정상 동작 테스트**  
   - **Test** 탭에서 Secret 값을 복호화하여 사용하는 코드 예제를 실행합니다.  
   - 예:  
     ```python
     import boto3
     client = boto3.client('secretsmanager', region_name='us-east-1')
     response = client.get_secret_value(SecretId='my-kms-secret')
     print(response['SecretString'])
     ```
   - 복호화된 값이 정상적으로 출력되면 설정이 성공적으로 완료되었습니다.  

> **📸 화면 확인:** Secret 값을 복호화한 후, 사용 가능한 데이터가 출력되는지 확인합니다.  

---

### 📌 요약 및 체크리스트  
**체크리스트:**  
- [ ] AWS Management Console에 로그인하여 Secrets Manager에 접근  
- [ ] KMS CMK를 선택하여 Secret 생성  
- [ ] Secret 값을 암호화하고 Parameter Store에 통합  
- [ ] 자동 교체 및 Lambda 통합 설정  
- [ ] Secret 상태 및 복호화 테스트 완료  

**비용 주의사항:**  
- KMS CMK는 12개월간 무료입니다.  
- 사용자 정의 CMK는 추가 비용이 발생할 수 있습니다.  
- 프리티어는 12개월간 1000시간의 KMS 사용량이 제공됩니다.

## ⌨️ AWS CLI로 Secrets Manager 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

> **💡 Tip:** AWS CLI를 사용하기 전에 `aws configure` 명령어로 액세스 키, 비밀 키, 리전을 설정해야 합니다.  
> **⚠️ 주의:** AWS CLI는 AWS Identity and Access Management(IAM) 정책에 따라 권한이 제한될 수 있습니다.

---

### 예제 1: Secrets Manager 리소스 조회
```bash
# Secrets Manager Secret 목록 조회
aws secretsmanager list-secrets --query '[].Name' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 결과 필터링 (예: `'[].Name'`로 Secret 이름만 추출) | `'[].Name'` |
| `--output` | 출력 형식 (json, table, text) | `table` |

**예상 출력:**
```
|       Name        |
+-------------------+
| example-secret-1 |
| example-secret-2 |
```

---

### 예제 2: Secrets Manager Secret 생성 (KMS CMK 사용)
```bash
# KMS CMK를 사용해 Secret 생성
aws secretsmanager create-secret \
    --name "example-secret" \
    --description "Test secret with KMS encryption" \
    --secret-string "my-secret-value" \
    --kms-key-id "arn:aws:kms:region:account-id:key/key-id"
```

**필수 옵션:**
- `--name`: Secret 이름 (필수)
- `--secret-string`: Secret 값 (필수)
- `--kms-key-id`: KMS CMK ARN (암호화용)

**예상 출력:**
```json
{
    "ARN": "arn:aws:secretsmanager:region:account-id:secret:example-secret-123456",
    "Name": "example-secret",
    "VersionId": "12345678-1234-1234-1234-123456789012"
}
```

> **💡 Tip:** KMS CMK를 사용하지 않으면 기본 AWS KMS 키가 자동으로 생성됩니다.

---

### 예제 3: Secrets Manager Secret 수정
```bash
# Secret 값 수정
aws secretsmanager update-secret \
    --secret-id "example-secret" \
    --description "Updated secret description" \
    --secret-string "new-secret-value"
```

**옵션 설명:**
- `--secret-id`: 수정할 Secret 이름 또는 ARN
- `--secret-string`: 새로운 Secret 값

---

### 예제 4: Secrets Manager Secret 삭제
```bash
# Secret 삭제
aws secretsmanager delete-secret --secret-id "example-secret"

# 삭제 확인 (Secret이 삭제된 경우 오류 발생)
aws secretsmanager describe-secret --secret-id "example-secret"
```

> **⚠️ 주의:** 삭제된 Secret은 복원 불가능하며, 삭제 후 `describe-secret` 명령어는 오류를 반환합니다.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws secretsmanager list-secrets
aws secretsmanager describe-secret --secret-id "id"

# 생성
aws secretsmanager create-secret --name "name" --secret-string "value" --kms-key-id "arn"

# 수정
aws secretsmanager update-secret --secret-id "id" --secret-string "new-value"

# 삭제
aws secretsmanager delete-secret --secret-id "id"
```

---

### ⚠️ 비용 및 주의사항
- **프리티어:** 1년간 25개 Secret 생성 및 100MB 데이터 저장 가능  
- **KMS CMK:** 별도의 KMS 비용 발생 (기본 키는 무료)  
- **CloudHSM:** 추가 비용 발생 (HSM 장비 및 관리 비용)  
- **자동 교체:** `rotate-secret` 명령어로 설정 가능 (예: `aws secretsmanager rotate-secret --secret-id "id" --rotation-rules "schedule: cron(0 12 * * ? *)"`)  

---

### ✅ 체크리스트: KMS 기본 구성 및 설정
- [ ] AWS CLI 버전 확인 및 설정  
- [ ] KMS CMK ARN 확인 (IAM 정책 권한 부여)  
- [ ] Secret 생성 시 `--kms-key-id` 옵션 사용  
- [ ] Secret 삭제 전 `describe-secret`로 상태 확인  
- [ ] 프리티어 사용 여부 확인 (비용 절감)

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Secrets Manager 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **Secrets Manager의 자동 교체 정책**  
   - 설명: 시험에서 자동 교체 정책의 구성 방법과 사용 시나리오가 주요 포인트입니다. AWS에서 비밀 정보의 암호화 키를 주기적으로 교체하여 보안을 강화하는 기능을 이해해야 합니다.  
   - 키워드: `자동 교체`, `Secrets Manager 정책`, `암호화 키 회전`

2. **봉투 암호화(Envelope Encryption) 메커니즘**  
   - 설명: Secrets Manager는 데이터를 암호화할 때 봉투 암호화 방식을 사용하며, 이 메커니즘을 이해하는 것이 필수적입니다. 이는 데이터 보호와 키 관리의 기초 지식입니다.  
   - 키워드: `봉투 암호화`, `Envelope Encryption`, `암호화 키 관리`

3. **KMS CMK 관리 및 회전**  
   - 설명: Secrets Manager는 AWS KMS를 통해 CMK(관리형 암호화 키)를 사용하여 비밀 정보를 보호합니다. CMK의 생성, 키 회전, 접근 제어 등은 시험에서 빈번히 출제됩니다.  
   - 키워드: `KMS CMK`, `암호화 키 회전`, `암호화 키 관리`

4. **Secrets Manager와 Parameter Store의 차이**  
   - 설명: 비밀 정보와 구성 정보를 관리하는 두 서비스의 주요 차이점(예: 비밀 정보의 암호화, 정책 기반 접근 제어 등)을 구분하는 문제가 시험에서 자주 출제됩니다.  
   - 키워드: `Secrets Manager`, `Parameter Store`, `암호화`, `정책 기반 접근`

5. **비밀 정보의 암호화 및 암호화 키 관리**  
   - 설명: Secrets Manager는 데이터를 암호화하고, 키를 KMS에 저장하는 방식으로 보안을 강화합니다. 이 메커니즘을 이해하지 못하면 시험에서 실수할 수 있습니다.  
   - 키워드: `암호화`, `암호화 키`, `KMS`, `Secrets Manager`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "Secrets Manager와 Parameter Store의 차이" 문제에서 "Secrets Manager는 암호화가 필요 없다"고 오해할 수 있음 | Secrets Manager는 암호화 키를 KMS로 관리하며, Parameter Store는 암호화 없이 저장됨 |
| 함정 2 | "암호화 키 회전" 문제에서 CMK 회전과 Secret 값 회전을 혼동할 수 있음 | CMK 회전은 KMS에서 이루어지며, Secret 값 회전은 Secrets Manager에서 설정 |
| 함정 3 | "Secrets Manager의 비용" 문제에서 KMS 사용 여부를 고려하지 않을 수 있음 | KMS 사용 시 암호화 키 관리 비용이 추가되며, Secret 저장량에 따라 요금이 변동 |

#### 🔄 Secrets Manager vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | Secrets Manager | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| **용도** | 비밀 정보(예: DB 비밀번호, API 키) 암호화 및 관리 | Parameter Store: 구성 정보 관리, ACM: 인증서 관리 | 민감 데이터 보호 시 Secrets Manager, 구성 정보 관리 시 Parameter Store |
| **확장성** | 대규모 암호화 키 관리 및 정책 기반 접근 제어 | Parameter Store: 간단한 구성 정보 저장 | 고보안 환경에서 대규모 비밀 관리 시 Secrets Manager, 작은 규모에서 Parameter Store |
| **비용** | KMS 사용 시 암호화 키 관리 비용 발생 | Parameter Store: 기본 비용 낮음, ACM: 인증서 관리 비용 | 민감 데이터 보호 시 비용 상관관계 고려, 단순 구성 관리 시 Parameter Store |
| **지연시간** | 암호화/복호화 처리로 인한 지연 발생 | Parameter Store: 실시간 접근 가능 | 실시간 데이터 접근이 필요할 경우 Parameter Store, 보안 우선시 Secrets Manager |

#### 📝 시험 대비 체크리스트
- [ ] Secrets Manager의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  (예: "비밀 정보를 암호화하여 안전하게 저장하고 관리하는 서비스")
- [ ] Secrets Manager를 선택해야 하는 시나리오를 알고 있는가?  
  (예: DB 비밀번호, API 키 등 민감 데이터 보호)
- [ ] Secrets Manager의 제한사항/한계를 알고 있는가?  
  (예: 암호화 키 회전은 KMS에 의존, 대규모 데이터 저장 시 비용 증가)
- [ ] Secrets Manager와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  (예: Parameter Store는 암호화 없이 저장, ACM은 인증서 관리에 특화)
- [ ] Secrets Manager의 비용 구조를 이해하고 있는가?  
  (예: KMS 사용 시 암호화 키 관리 비용, Secret 저장량에 따른 요금)

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Secrets Manager를 떠올리세요:  
> - **암호화 키 회전**  
> - **Secrets Manager 정책**  
> - **봉투 암호화(Envelope Encryption)**  
> - **KMS CMK 관리**  
> - **민감 데이터 보호**

---

| [⬅️ CloudHSM](./CloudHSM.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 4](../README.md) | [Parameter Store ➡️](./Parameter-Store.md) |

---
