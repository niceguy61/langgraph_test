---

| [⬅️ ACM](./ACM.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 4](../README.md) | [다음 Day ➡️](../day3/README.md) |

---

# Macie 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** Macie는 데이터 유출 및 보안 위협을 방지하기 위한 AWS의 **자동화된 데이터 보호 및 감사 서비스**입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 데이터 위치를 수동으로 파악해야 하며, S3 버킷, RDS, DynamoDB 등 다양한 저장소에서 민감 정보를 분석하는 데 시간이 많이 소요되었습니다. 예를 들어, 고객 정보가 포함된 CSV 파일이 S3에 저장되었는지 확인하려면 수동 검색이 필요했습니다.
- **문제 2:** 암호화 키 관리가 복잡했으며, KMS CMK를 사용해도 키 회전이나 접근 제어 설정이 자동화되지 않아 보안 위험이 존재했습니다. 특히 봉투 암호화(Envelope Encryption)의 경우, 키 관리가 실수로 소홀해질 수 있었습니다.
- **문제 3:** 인증서 자동 갱신이 없어 ACM에서 발급한 인증서가 만기되면 서비스 중단이 발생할 수 있었고, 매뉴얼로 검토하고 갱신해야 하는 번거로움이 있었습니다.

**Macie로 해결:**
- **해결 1:** Macie는 S3, RDS, DynamoDB 등 다양한 저장소에서 데이터를 자동으로 스캔하여 민감 정보(예: SSN, 카드 번호)를 식별하고, 위험 수준을 평가하여 보고서를 제공합니다. 이를 통해 수동 검색 대신 자동화된 감사가 가능해집니다.
- **해결 2:** KMS CMK와 봉투 암호화를 통합해 데이터를 암호화하고, 키 회전 및 접근 제어를 자동화합니다. 예를 들어, S3 객체를 암호화할 때 봉투 암호화로 키를 관리하면, 키를 안전하게 보호하면서도 데이터 접근이 가능합니다.
- **해결 3:** ACM 인증서 자동화를 통해 Macie가 인증서 만기 알림을 제공하고, AWS Certificate Manager와 연동해 자동 갱신을 수행합니다. 이로 인해 인증서 만료로 인한 서비스 중단을 방지할 수 있습니다.

### 비유로 이해하기
Macie는 건물의 보안 감시원을 상상해보세요. 건물 내부의 모든 문과 창문을 감시하며, 위험한 물건(민감 데이터)이 들어오는 것을 감지하고 경보를 울립니다. 또한, 문을 잠그는 데 사용하는 열쇠(KMS CMK)를 안전하게 관리하고, 열쇠를 주기적으로 교체하는 루틴을 자동화합니다. 인증서는 건물의 출입증으로, 만료 시 자동으로 갱신해 주는 시스템입니다. 이렇게 Macie는 데이터 보호의 모든 단계를 자동화해줍니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 민감 데이터를 자동으로 식별하고 보호하려는 경우 | 한 보험 회사가 S3에 저장된 고객 데이터를 분석해 SSN, 보험 번호 등 민감 정보를 식별하고, 암호화 및 접근 제어를 설정 |
| 시나리오 2 | GDPR, CCPA 등 규제 준수를 위한 데이터 감사 수행 | 금융 기관이 Macie를 통해 고객 정보의 저장 위치와 암호화 상태를 정기적으로 점검해 법적 위험을 줄임 |
| 시나리오 3 | 클라우드 환경에서의 보안 위험 감지 및 대응 | 클라우드 서버에 저장된 데이터가 탈취 시도를 감지하고, 자동으로 보호 조치를 시행 |

**이럴 때 Macie를 선택하세요:**
- ✅ **데이터 유출 위험을 예방해야 할 때** (예: 고객 정보 저장소 감시)
- ✅ **규제 준수를 위한 정기 감사를 수행해야 할 때** (예: GDPR 준수)
- ✅ **다양한 저장소에서 데이터를 통합적으로 보호해야 할 때** (예: S3, RDS, DynamoDB)

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **단일 저장소에서의 데이터 감시** → **Inspector** (특정 인프라에 대한 보안 검사)
- ❌ **암호화 키 관리** → **KMS** (암호화 키 생성 및 관리)
- ❌ **인증서 자동 갱신** → **ACM** (인증서 생성 및 관리)

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **KMS** | 암호화 키를 안전하게 관리하고, 봉투 암호화를 지원 | Macie → S3 (암호화) → KMS (키 관리) |
| **Secrets Manager** | 민감 정보(예: 데이터베이스 비밀번호)를 자동으로 교체 및 관리 | Macie → Secrets Manager (비밀번호 자동 갱신) |
| **ACM** | 인증서 자동 갱신 및 관리를 통해 HTTPS 통신 보장 | Macie → ACM (인증서 만료 알림) |

**자주 사용되는 아키텍처 패턴:**
```
User → CloudFront → S3 (Macie 감시) → RDS (Secrets Manager 통합)
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **데이터 스캔 및 보고서 생성** | $0.0015/GB (S3, RDS, DynamoDB) | 월 10GB 무료 |
| **암호화 키 관리 및 회전** | $0.0015/1,000개 키 | 12개월 무료 |
| **인증서 자동 갱신 및 관리** | $0.0025/인증서 | 항상 무료 |

**비용 최적화 팁:**
1. 💡 **프리티어를 활용해 초기 사용량을 감당** (예: S3 데이터 스캔은 월 10GB 무료 제공)
2. 💡 **암호화 키 회전 주기를 최적화** (불필요한 키 생성을 방지해 비용 절감)
3. 💡 **ACM 인증서 자동 갱신 설정** (수동 관리 비용을 줄이고 오류를 방지)

> **⚠️ 비용 주의:** 데이터 스캔량이 예상보다 많아질 경우, S3 또는 RDS 사용량에 따라 비용이 급증할 수 있습니다. 또한, 봉투 암호화를 사용하면 KMS 키 관리 비용이 추가됩니다. 정기적으로 사용량을 모니터링하고, 프리티어 한도를 초과하지 않도록 주의해야 합니다.

## 📚 핵심 개념

### 개념 1: KMS CMK 관리 및 회전  
AWS Key Management Service (KMS)는 암호화 키(CKM)를 생성, 관리, 회전하는 핵심 서비스입니다. CMK는 데이터 암호화에 사용되는 고유한 키로, 암호화된 데이터를 복호화하려면 이 키가 필요합니다. KMS는 키의 생명주기(생성, 회전, 삭제)를 자동화하여 보안을 강화하고, 사용자의 키를 직접 저장하지 않아 블랙박스 위험을 줄입니다.  

#### 왜 중요한가?  
- **보안 강화**: 키를 직접 저장하지 않아 해킹 시 키가 노출되는 위험을 줄입니다.  
- **자동 회전**: 정기적으로 키를 변경해 데이터 유출 위험을 최소화합니다.  
- **정책 통합**: IAM 정책을 통해 키 사용 권한을 세분화해 보안 정책을 구현할 수 있습니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| 키 생명주기 | 키 생성, 회전, 삭제 등 키의 전체 수명 관리 | `aws kms rotate-key --key-id alias/my-key` |
| 회전 정책 | 정해진 주기로 자동 키 회전 | 매일 자동 회전 설정 |
| IAM 정책 | 키 사용 권한을 사용자/그룹별로 설정 | `kms:Encrypt`, `kms:Decrypt` 권한 부여 |

> **💡 Tip:** AWS KMS 콘솔에서 키 생성 후 `Rotate Key` 버튼을 클릭해 간단히 회전할 수 있습니다. CLI 명령어로도 자동화 가능합니다.  

---

### 개념 2: 봉투 암호화(Envelope Encryption) 메커니즘  
봉투 암호화는 데이터를 암호화할 때 두 단계의 키를 사용하는 방식입니다. 먼저 데이터 암호화 키(DEK)로 데이터를 암호화하고, 이 DEK를 암호화 키 키(KEK)로 암호화합니다. 이 과정에서 DEK는 데이터에 직접 적용되고, KEK는 KMS로 관리됩니다. 이 방식은 데이터를 보호하면서도 키 관리를 효율적으로 합니다.  

#### 작동 원리  
1. **DEK 생성**: 데이터 암호화 키(예: AES-256)를 생성합니다.  
2. **KEK 암호화**: DEK을 KMS의 KEK로 암호화합니다.  
3. **데이터 저장**: 암호화된 데이터와 암호화된 DEK를 함께 저장합니다.  

> **💡 Tip:** 봉투 암호화는 S3, RDS 등 AWS 서비스에서 자동으로 지원되며, KMS를 통해 KEK를 관리해 보안을 강화할 수 있습니다.  

---

### 개념 3: Secrets Manager 자동 교체 및 ACM 통합  
AWS Secrets Manager는 비밀 정보(예: 데이터베이스 패스워드, API 키)를 안전하게 저장하고, 자동으로 교체하는 기능을 제공합니다. 이는 정기적으로 비밀 정보를 갱신해 보안 위험을 줄입니다. 또한, AWS Certificate Manager (ACM)는 SSL/TLS 인증서를 자동으로 관리해 서비스 통신 보안을 강화합니다. 두 서비스는 보안 인프라를 통합해 운영 효율성을 높입니다.  

#### 주요 특징  
1. **자동 교체**: 설정된 주기(예: 30일)마다 비밀 정보를 자동으로 교체합니다.  
2. **서비스 통합**: RDS, Elasticsearch 등 AWS 서비스와 원활히 연동해 비밀 정보를 자동으로 제공합니다.  
3. **ACM 자동화**: 인증서의 생성, 재발급, 만료 알림 등을 자동으로 관리해 인증서 관리 부담을 줄입니다.  

> **💡 Tip:** Secrets Manager는 AWS CLI로 `rotate-secret` 명령어를 사용해 자동 교체를 설정할 수 있으며, ACM은 콘솔에서 인증서를 직접 관리하거나 자동화된 CI/CD 파이프라인과 연동할 수 있습니다.

## 🖥️ AWS 콘솔에서 Macie 사용하기

### Step 1: Macie 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - **Tip:** AWS 계정이 없으신가요? [AWS 계정 생성](https://console.aws.amazon.com/console/home?#/)으로 이동해 계정을 생성하세요.  

2. 상단 검색창에서 **"Macie"**를 입력하고 검색합니다.  
   - **Tip:** "Macie"를 입력하면 자동 완성으로 서비스가 표시됩니다.  

3. 검색 결과에서 **"Macie"**를 클릭하여 대시보드에 접근합니다.  
   - **⚠️ 주의:** Macie는 AWS Organizations에서 사용할 수 있는 서비스입니다. 조직이 없으신 경우, [AWS Organizations 생성](https://console.aws.amazon.com/organizations/home#/create-organization)을 먼저 수행해야 합니다.  

> **📸 화면 확인:** Macie 대시보드가 표시되면 정상입니다.  
> - 대시보드에는 **"Find sensitive data"**, **"Data exposure"**, **"Compliance"** 등 주요 메트릭이 표시됩니다.  

---

### Step 2: [주요 작업 1 - KMS CMK 생성 및 Macie 연동]  
1. **KMS CMK 생성**  
   - **AWS KMS 서비스 접근:**  
     1. AWS Console 메인 메뉴에서 **"Services"**를 클릭합니다.  
     2. **"Key Management Service (KMS)"**를 선택합니다.  
     3. **"Create key"** 버튼을 클릭하여 CMK를 생성합니다.  
     4. **Key type**에서 **"Customer managed key"**를 선택하고, **"Create"** 버튼을 클릭합니다.  
     5. 생성된 CMK의 **ARN**을 저장해두세요 (예: `arn:aws:kms:region:account-id:key/key-id`).  

   - **Macie에 CMK 연동:**  
     1. Macie 대시보드에서 **"Settings"** 메뉴를 클릭합니다.  
     2. **"Data encryption"** 섹션에서 **"Encryption key"**를 선택합니다.  
     3. **"Create new key"**를 클릭하고, 위에서 생성한 CMK의 ARN을 입력합니다.  
     4. **"Save changes"**를 클릭하여 설정을 저장합니다.  

2. **Secrets Manager 자동 교체 설정**  
   - **AWS Secrets Manager 접근:**  
     1. AWS Console에서 **"Secrets Manager"** 서비스를 열고, **"Store a new secret"**을 클릭합니다.  
     2. **Secret name**을 입력하고, **"Password"** 형식으로 비밀번호를 생성합니다.  
     3. 생성된 Secret의 **ARN**을 저장해두세요.  

   - **Macie에 Secret 연동:**  
     1. Macie 대시보드에서 **"Settings"** → **"Data sources"** 메뉴로 이동합니다.  
     2. **"Add data source"**를 클릭하고, **"Secrets Manager"**를 선택합니다.  
     3. Secret ARN을 입력하고, **"Save"**를 클릭합니다.  

3. **CloudHSM 및 ACM 연동 확인**  
   - **CloudHSM 설정:**  
     1. AWS Console에서 **"CloudHSM"** 서비스를 열고, **"Create HSM"**을 클릭합니다.  
     2. **HSM 템플릿**을 선택하고, **"Create"** 버튼을 클릭하여 HSM을 생성합니다.  
     3. 생성된 HSM의 **ARN**을 저장해두세요.  

   - **ACM 인증서 관리:**  
     1. AWS Console에서 **"ACM"** 서비스를 열고, **"Create certificate"**를 클릭합니다.  
     2. **"Request a certificate"** 옵션을 선택하고, **"Save"**를 클릭합니다.  
     3. 생성된 인증서의 **ARN**을 저장해두세요.  

> **📸 화면 확인:**  
> - KMS CMK 생성 후 **"Key details"** 페이지에서 CMK ARN이 표시됩니다.  
> - Secrets Manager Secret 생성 후 **"Secret details"** 페이지에서 ARN이 표시됩니다.  
> - CloudHSM 및 ACM 설정 후 **"Dashboard"**에서 생성된 리소스가 표시됩니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **데이터 소스 설정**  
   - Macie 대시보드에서 **"Settings"** → **"Data sources"** 메뉴로 이동합니다.  
   - **"Add data source"**를 클릭하고, **"S3"**, **"DynamoDB"**, **"Redshift"** 등의 옵션을 선택합니다.  
   - 선택한 데이터 소스에 대한 **IAM 역할**을 설정하고, **"Save"**를 클릭합니다.  

2. **암호화 설정**  
   - **"Data encryption"** 섹션에서 **"Encryption key"**를 선택합니다.  
   - KMS CMK 또는 AWS Managed Key를 선택하고, **"Save changes"**를 클릭합니다.  

3. **자동 교체 정책 설정**  
   - **"Secrets Manager"**에서 **"Rotation policies"** 메뉴로 이동합니다.  
   - **"Create rotation policy"**를 클릭하고, **Secret ARN**을 입력합니다.  
   - **Rotation schedule**을 설정하고, **"Save"**를 클릭합니다.  

> **⚠️ 주의:**  
> - KMS CMK는 **AWS Free Tier**에서 25개까지 무료로 생성할 수 있습니다.  
> - Secrets Manager는 **Free Tier**에서 1개의 Secret을 무료로 생성할 수 있습니다.  
> - CloudHSM 및 ACM은 **Free Tier** 제한이 없으나, 실제 사용 시 비용이 발생할 수 있습니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인 방법**  
   - **KMS CMK:** AWS KMS 대시보드에서 **"Keys"** 섹션에서 생성된 CMK가 표시됩니다.  
   - **Secrets Manager Secret:** Secrets Manager 대시보드에서 생성된 Secret이 표시됩니다.  
   - **CloudHSM:** CloudHSM 대시보드에서 생성된 HSM이 표시됩니다.  
   - **ACM 인증서:** ACM 대시보드에서 생성된 인증서가 표시됩니다.  

2. **상태 확인 방법**  
   - Macie 대시보드에서 **"Settings"** → **"Data encryption"** 섹션에서 KMS CMK가 정상적으로 연결되었는지 확인합니다.  
   - **"Data sources"** 섹션에서 S3, DynamoDB 등 데이터 소스가 정상적으로 연결되었는지 확인합니다.  

3. **정상 동작 테스트**  
   - **Macie 스캔 실행:**  
     1. Macie 대시보드에서 **"Find sensitive data"**를 클릭합니다.  
     2. **"Start scan"** 버튼을 클릭하여 특정 데이터 소스를 스캔합니다.  
     3. 스캔 결과를 확인하고, **"Compliance"** 섹션에서 정책 위반 여부를 확인합니다.  

   - **Secrets Manager 자동 교체 테스트:**  
     1. AWS Secrets Manager에서 **"Rotation"** 메뉴로 이동합니다.  
     2. 설정된 Rotation 정책이 자동으로 실행되었는지 확인합니다.  
     3. Secret의 최신 값을 확인하고, **"Get secret value"**를 클릭합니다.  

> **📸 화면 확인:**  
> - Macie 스캔 결과는 **"Find sensitive data"** 섹션에서 확인됩니다.  
> - Secrets Manager의 Rotation 상태는 **"Secrets"** 섹션에서 확인됩니다.  
> - KMS CMK의 사용 현황은 **"Key usage"** 섹션에서 확인됩니다.  

---

### ✅ 학습 요약 체크리스트  
- [ ] AWS Management Console에 로그인 및 Macie 서비스 접근  
- [ ] KMS CMK 생성 및 Macie에 연동  
- [ ] Secrets Manager Secret 생성 및 자동 교체 설정  
- [ ] CloudHSM 및 ACM 리소스 생성  
- [ ] Macie 데이터 소스 및 암호화 설정  
- [ ] 생성된 리소스 상태 확인  
- [ ] Macie 스캔 및 Secret Rotation 테스트  

> **💡 Tip:** AWS Free Tier 사용 시, KMS CMK 25개, Secrets Manager Secret 1개, CloudHSM 1개까지 무료로 사용할 수 있습니다.  
> **⚠️ 주의:** 실제 환경에서는 비용이 발생할 수 있으므로, **AWS Cost Explorer**를 사용해 비용을 모니터링하세요.

## ⌨️ AWS CLI로 Macie 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

> **💡 Tip:** CLI 명령어를 실행하기 전에 AWS 계정과 리전 설정이 올바르게 되어 있는지 확인하세요.  
> **⚠️ 주의:** CLI 명령어는 특정 리전에만 적용되므로, 작업 리전을 명시적으로 설정해야 합니다.

---

### 예제 1: Macie 리소스 조회
```bash
# Macie 멤버 목록 조회
aws macie list-members --query 'Members[*].MemberId' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | JSON 결과 필터링 | `'Members[*].MemberId'` (멤버 ID 추출) |
| `--output` | 출력 형식 | `table` (테이블 형식으로 출력) |

**예상 출력:**
```
MemberId
----------------
EXAMPLE123456789
```

> **💡 Tip:** `list-members` 명령어는 Macie에서 관리하는 멤버(계정) 목록을 조회합니다.  
> **⚠️ 주의:** `--query` 옵션은 JSON 형식의 결과를 필터링해 편리하게 출력할 수 있습니다.

---

### 예제 2: Macie 리소스 생성
```bash
# Macie 멤버 생성
aws macie create-member --name "example-member"
```

**필수 옵션:**
- `--name`: 생성할 멤버 이름 (예: `example-member`)

**예상 출력:**
```json
{
    "memberId": "EXAMPLE123456789",
    "status": "ACTIVE"
}
```

> **💡 Tip:** `create-member`는 Macie에서 보호할 멤버(계정)를 생성합니다.  
> **⚠️ 주의:** 멤버 생성 시 AWS Organizations 구성이 필요할 수 있습니다.

---

### 예제 3: Macie 리소스 수정
```bash
# Macie 멤버 상태 수정
aws macie update-member --member-id "EXAMPLE123456789" --status "INACTIVE"
```

**필수 옵션:**
- `--member-id`: 수정할 멤버 ID
- `--status`: `ACTIVE`/`INACTIVE` (상태 변경)

> **💡 Tip:** 멤버 상태를 변경하면 Macie의 보호 범위가 달라질 수 있습니다.  
> **⚠️ 주의:** 상태 변경 시 데이터 보호 정책이 영향을 받을 수 있으므로 주의해야 합니다.

---

### 예제 4: Macie 리소스 삭제
```bash
# Macie 멤버 삭제
aws macie delete-member --member-id "EXAMPLE123456789"

# 삭제 후 상태 확인
aws macie describe-member --member-id "EXAMPLE123456789"
```

> **⚠️ 주의:** 삭제된 리소스는 복구할 수 없으므로, 삭제 전에 반드시 확인하세요.  
> **💡 Tip:** `describe-member` 명령어는 삭제 후 상태를 확인할 수 있습니다.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws macie list-members
aws macie describe-member --member-id "id"

# 생성/수정/삭제
aws macie create-member --name "name"
aws macie update-member --member-id "id" --status "value"
aws macie delete-member --member-id "id"
```

> **💡 Tip:** Macie CLI 명령어는 AWS Organizations 구성과 연동되어 사용됩니다.  
> **⚠️ 주의:** CLI 명령어는 리전별로 다른 결과를 반환할 수 있으므로, 리전 설정을 반드시 확인하세요.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Macie 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **포인트 1: Macie의 데이터 분류 및 보안 위협 탐지 기능**  
   - 설명: Macie는 S3, Glacier, DynamoDB 등에 저장된 데이터를 자동으로 분류하고, 개인 정보, 보안 위협 등을 감지하는 기능이 시험에서 주요 출제 포인트입니다. 이는 데이터 보호 규정 준수(예: GDPR, CCPA)를 위한 핵심 요소로, 데이터 유형별 보안 정책 설정에 직접 연결됩니다.  
   - 키워드: `데이터 분류`, `보안 위협 탐지`, `규정 준수`

2. **포인트 2: Macie의 자동화된 보안 정책 생성 및 적용**  
   - 설명: Macie는 데이터 분류 결과를 바탕으로 자동으로 보안 정책을 생성하고, 이를 AWS 서비스에 통합하여 보호 수준을 강화합니다. 이 기능은 시험에서 자동화된 보안 관리 체계 설계 문제에 자주 등장합니다.  
   - 키워드: `자동화 정책`, `보안 통합`, `정책 생성`

3. **포인트 3: Macie의 머신러닝 기반 감지 메커니즘**  
   - 설명: Macie는 머신러닝을 활용해 비정상적인 데이터 패턴(예: 비정상적인 접근, 데이터 유출 시도)을 감지하는 기능이 핵심입니다. 이는 보안 사고 예방과 사고 대응 전략 설계에 직접적인 영향을 미칩니다.  
   - 키워드: `머신러닝`, `패턴 감지`, `사고 예방`

4. **포인트 4: Macie의 비용 모델과 프리티어 활용**  
   - 설명: Macie는 데이터 처리량에 따라 요금이 적용되므로, 시험에서는 비용 최적화 전략(예: 프리티어 사용, 데이터 분류 기준 조정)을 묻는 문제가 자주 출제됩니다.  
   - 키워드: `비용 모델`, `프리티어`, `최적화 전략`

5. **포인트 5: Macie와 KMS, Secrets Manager의 연동 구조**  
   - 설명: Macie는 KMS를 통해 암호화된 데이터를 보호하고, Secrets Manager와 연동해 민감 정보를 관리하는 방식이 시험에서 자주 묻습니다. 이는 보안 인프라 설계의 핵심 요소입니다.  
   - 키워드: `KMS 연동`, `Secrets Manager`, `암호화 관리`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "Macie는 데이터 암호화를 직접 수행하는가?" → **아니요**, 데이터 암호화는 KMS, CloudHSM 등이 담당합니다. | **KMS/CloudHSM 사용** |
| 함정 2 | "Macie의 감지 기능은 실시간으로 작동하는가?" → **아니요**, 분석은 일정 시간이 소요됩니다. | **분석 시간 소요** |
| 함정 3 | "Macie는 AWS 서비스 간 통합이 불가능한가?" → **아니요**, AWS 서비스와 연동해 보안 정책을 자동 적용합니다. | **자동 통합 가능** |

#### 🔄 Macie vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | Macie | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 데이터 분류, 보안 위협 탐지 | KMS(암호화), Secrets Manager(비밀 관리) | 데이터 보호 규정 준수 시 |
| 확장성 | 대규모 데이터 분석 가능 | KMS(정적 관리), CloudHSM(하드웨어 제한) | 대규모 데이터 처리 시 |
| 비용 | 데이터 처리량 기반 요금제 | KMS(정액제), Secrets Manager(요금제 차이) | 비용 최적화가 필요할 때 |
| 지연시간 | 분석 시간 소요 | KMS(실시간), CloudHSM(실시간) | 실시간 보안 검증이 필요할 때 |

#### 📝 시험 대비 체크리스트
- [ ] Macie의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  *예: "Macie는 AWS 내 데이터를 자동 분류하고 보안 위협을 탐지하여 보호 수준을 강화하는 서비스입니다."*
- [ ] Macie를 선택해야 하는 시나리오를 알고 있는가?  
  *예: GDPR 준수, 데이터 유출 위험 대응, 대규모 데이터 보호*
- [ ] Macie의 제한사항/한계를 알고 있는가?  
  *예: 실시간 분석 불가, 암호화 서비스와 별도로 운영*
- [ ] Macie와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  *예: KMS는 암호화 관리, Macie는 데이터 분석 및 위협 탐지*
- [ ] Macie의 비용 구조를 이해하고 있는가?  
  *예: 데이터 처리량 기반 요금제, 프리티어 사용 권장*

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Macie를 떠올리세요:  
> - **데이터 분류**  
> - **보안 위협 탐지**  
> - **규정 준수**  
> - **머신러닝 기반 감지**  
> - **자동화된 보안 정책**

---

| [⬅️ ACM](./ACM.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 4](../README.md) | [다음 Day ➡️](../day3/README.md) |

---
