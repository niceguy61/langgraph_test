---

| [⬅️ Secrets Manager](./Secrets-Manager.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 4](../README.md) | [ACM ➡️](./ACM.md) |

---

# Parameter Store 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** Parameter Store는 애플리케이션의 설정값과 민감 정보를 안전하게 저장하고 관리하기 위한 AWS의 **설정 저장소 및 보안 관리** 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 애플리케이션 코드에 민감 정보(예: 데이터베이스 패스워드)를 직접 저장해 보안 위험이 존재. 개발자들이 코드를 수정하며 정보를 실수로 노출할 수 있음.  
- **문제 2:** 다중 환경(예: 개발/생산)에서 설정값을 관리하는 데 복잡한 프로세스로 인해 오류 발생 가능성 높음.  
- **문제 3:** 데이터암호화 없이 저장된 정보가 데이터 유출 시 해킹 위험에 노출됨.  

**Parameter Store로 해결:**
- **해결 1:** KMS 암호화를 통해 데이터를 암호화하여 저장, 해킹 시도 시 데이터가 노출되지 않음.  
- **해결 2:** 파라미터의 버전 관리 및 환경별 설정 분리 기능으로 설정값을 안정적으로 관리할 수 있음.  
- **해결 3:** AWS Key Management Service(KMS)와 연동해 암호화 키를 안전하게 관리하고, 자동 키 회전을 지원하여 보안 위험을 최소화함.  

### 비유로 이해하기
Parameter Store는 집안의 **보안 금고**를 생각해보세요. 중요한 문서나 비밀번호는 금고에 넣고, 필요할 때만 열어 사용합니다. 애플리케이션도 마찬가지로 민감 정보를 Parameter Store에 저장하고, 필요할 때만 암호화된 데이터를 호출해 사용합니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 애플리케이션의 민감 정보(예: 데이터베이스 패스워드)를 안전하게 저장하고 관리 | 클라우드 마이크로서비스 아키텍처에서 DB 연결 정보를 Parameter Store에 암호화 저장 |
| 시나리오 2 | 환경별 설정값(예: 개발/생산)을 분리하여 관리 | 웹 애플리케이션의 로그레벨을 개발 환경에서는 디버깅 모드로, 생산 환경에서는 정보 모드로 설정 |
| 시나리오 3 | 애플리케이션 실행 시 동적으로 파라미터 값을 불러와 구성 | 컨테이너화된 서비스에서 환경 변수를 Parameter Store에서 동적 로딩 |

**이럴 때 Parameter Store를 선택하세요:**
- ✅ **민감 정보를 암호화하여 저장해야 할 때**  
- ✅ **환경별 설정값을 관리해야 할 때**  
- ✅ **애플리케이션 실행 시 동적 설정값이 필요한 경우**  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **단순한 환경 변수 관리** → **AWS Systems Manager Parameter Store**  
- ❌ **고도로 보안이 필요한 비밀 정보** → **AWS Secrets Manager**  
- ❌ **인증서 자동화 관리** → **AWS Certificate Manager (ACM)**  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **AWS KMS** | 암호화 키를 관리하고 파라미터 암호화에 사용 | Parameter Store → KMS → 암호화 데이터 저장 |
| **Secrets Manager** | 민감 정보 자동 교체 및 보안 관리 | Parameter Store → Secrets Manager → 자동 키 회전 |
| **CloudHSM** | 하드웨어 기반 암호화 모듈로 보안 강화 | Parameter Store → CloudHSM → 고도 보안 암호화 |

**자주 사용되는 아키텍처 패턴:**
```
User → Application → [Parameter Store (암호화 저장)] → [KMS (암호화 키 관리)] → [CloudHSM (하드웨어 암호화)]
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **Parameter Store 저장 공간** | $0.00015/GB/월 | 12개월 무료 (최대 10GB) |
| **Parameter Store 요청** | $0.0002/요청 | 월 100,000 요청까지 무료 |
| **KMS 암호화 키 사용** | $0.20/월 (단일 키) | 12개월 무료 (최대 25개 키) |

**비용 최적화 팁:**
1. 💡 **프리티어 활용:** 12개월 동안 무료로 사용 가능한 저장 공간과 요청 수량을 초과하지 않도록 관리하세요.  
2. 💡 **암호화 키 관리:** 단일 키로 여러 파라미터를 암호화하여 KMS 요금 절감.  
3. 💡 **CloudHSM 대체:** 하드웨어 기반 암호화가 필요하지 않은 경우, 소프트웨어 기반 KMS 사용으로 비용 절감.  

> **⚠️ 비용 주의:** 파라미터를 수동으로 삭제하지 않으면 저장 공간이 무한 증가해 비용이 누적될 수 있음. 정기적으로 사용하지 않는 파라미터를 삭제하세요.

## 📚 핵심 개념

### 개념 1: Parameter Store의 암호화 및 보안 관리
Parameter Store는 AWS Key Management Service(KMS)를 기반으로 데이터를 암호화하여 보안을 강화합니다. KMS CMK( Customer Master Key)는 Parameter Store가 민감한 데이터(예: 비밀번호, API 키)를 암호화하고 복호화하는 데 사용됩니다. 이는 애플리케이션에서 직접 키를 관리할 필요 없이 AWS의 보안 인프라를 활용할 수 있도록 합니다. 또한, Parameter Store는 봉투 암호화(Envelope Encryption) 방식을 통해 데이터를 보호하며, 이는 데이터를 암호화된 키로 암호화하고, 이 키를 KMS CMK로 보호하는 방식입니다. 이는 데이터 유출 시 해독이 어려워지는 보안 강화를 제공합니다.

#### 왜 중요한가?
- **보안 강화**: 민감한 데이터를 KMS CMK로 암호화하여 외부 공격에 대비합니다.  
- **일관된 키 관리**: Parameter Store는 KMS CMK를 자동으로 관리하고, 키 회전을 자동화하여 보안을 유지합니다.  
- **복잡한 인프라 단순화**: 애플리케이션 개발자는 키 관리 로직을 작성할 필요가 없어 개발 생산성 향상.

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-|-|
| **KMS CMK 관리** | Parameter Store는 KMS CMK를 생성하고, 암호화 키를 보관합니다. | `aws ssm get-parameter --name "SecretKey" --with-decryption` |
| **봉투 암호화(Envelope Encryption)** | 데이터를 암호화 키로 암호화하고, 이 키를 KMS CMK로 보호합니다. | 데이터: `data`, 암호화 키: `key`, KMS CMK: `cmk-1234` |
| **암호화 키 회전** | KMS CMK가 변경될 때 Parameter Store는 자동으로 키를 업데이트하고 데이터를 재암호화합니다. | `aws ssm put-parameter --name "SecretKey" --value "new_value" --type "SecureString"` |

> **💡 Tip:** Parameter Store는 KMS CMK를 사용해 데이터를 암호화하므로, 키 관리 정책을 설정해 보안을 강화해야 합니다. 예를 들어, 키 회전 주기를 설정하거나, 암호화된 데이터를 복호화할 때 KMS를 통한 접근 권한을 설정해야 합니다.

---

### 개념 2: Secrets Manager와의 자동 교체 정책
Secrets Manager는 Parameter Store와 협력하여 민감한 정보를 관리하며, 자동 교체 정책을 통해 보안을 유지합니다. 예를 들어, 애플리케이션의 데이터베이스 암호가 유출 위험성이 있을 경우, Secrets Manager는 Parameter Store에 저장된 정보를 자동으로 교체하고, 기존 암호를 삭제합니다. 이는 보안 사고 시 빠르게 대응할 수 있도록 설계되었습니다. 또한, Parameter Store는 Secrets Manager와 연동하여 암호화된 데이터를 저장하고, 암호화 키를 KMS CMK로 보호하는 방식을 사용합니다.

#### 작동 원리
1. **정책 설정**: Secrets Manager에 자동 교체 정책을 등록합니다. 이 정책은 특정 시간 간격이나 이벤트(예: 데이터베이스 연결 실패)에 따라 암호를 교체하도록 설정합니다.  
2. **암호화 및 저장**: Parameter Store는 KMS CMK를 사용해 암호를 암호화하고, 암호화된 데이터를 저장합니다.  
3. **자동 교체**: 정책에 따라 Parameter Store는 기존 암호를 삭제하고, 새로운 암호를 생성해 저장합니다. 이 과정에서 KMS CMK가 변경될 경우, 자동으로 키를 회전합니다.

> **💡 Tip:** Secrets Manager와 Parameter Store의 연동은 보안 및 운영 효율성을 동시에 높입니다. 예를 들어, 애플리케이션은 Parameter Store에서 암호를 읽고, Secrets Manager는 주기적으로 암호를 업데이트하여 빈번한 키 관리 부담을 줄입니다.

---

### 개념 3: CloudHSM 및 ACM 통합
CloudHSM은 하드웨어 보안 모듈(HSM)을 제공하여 높은 보안 수준의 키 관리를 가능하게 합니다. Parameter Store는 CloudHSM을 통해 KMS CMK를 생성하고, 이를 사용해 데이터를 암호화합니다. 이는 고도로 보안이 필요한 환경(예: 금융, 의료)에서 데이터 보호에 유용합니다. 또한, AWS Certificate Manager(ACM)는 인증서를 자동으로 관리하고, Parameter Store에 저장된 인증서 정보를 암호화하여 보호합니다. 이는 인증서의 만료나 복구 시 빠른 대응이 가능합니다.

#### 주요 특징
1. **하드웨어 보안 모듈 활용**: CloudHSM은 물리적으로 보호된 하드웨어로, 키를 저장하고 암호화/복호화 작업을 수행합니다. 이는 소프트웨어 기반 키 관리보다 보안성이 높습니다.  
2. **자동 인증서 관리**: ACM은 인증서의 생성, 만료 알림, 자동 갱신을 자동화하여 Parameter Store에 저장된 인증서 정보를 보호합니다.  
3. **암호화된 인증서 저장**: Parameter Store는 ACM으로 생성된 인증서를 암호화하여 저장하고, 암호화 키를 KMS CMK로 보호합니다. 이는 인증서 유출 시 해독이 어려워집니다.

> **💡 Tip:** CloudHSM과 ACM을 Parameter Store에 통합하면, 민감한 데이터와 인증서의 보안을 동시에 강화할 수 있습니다. 예를 들어, 금융 애플리케이션은 CloudHSM을 통해 데이터 키를 관리하고, ACM을 통해 SSL 인증서를 자동 갱신해 보안을 유지할 수 있습니다.

## 🖥️ AWS 콘솔에서 Parameter Store 사용하기

### Step 1: Parameter Store 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: [https://console.aws.amazon.com](https://console.aws.amazon.com)  
   - **⚠️ 주의:** AWS 계정이 없는 경우 [AWS Management Console](https://console.aws.amazon.com)에서 신규 계정 생성 후 로그인합니다.  

2. 상단 검색창에서 **"Parameter Store"**를 입력하고 검색합니다  
   - **💡 Tip:** 검색 결과에서 **"Parameter Store"**를 클릭해 대시보드로 이동합니다.  

3. **Parameter Store 대시보드**가 표시되면 정상입니다  
   - 화면 상단에 **"Parameters"** 탭이 표시되고, **"Parameter Store"** 서비스의 기본 화면이 뜨면 됩니다.  
   - **📸 화면 확인:** Parameter Store 대시보드가 표시되면 정상입니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **"Parameters"** 탭에서 **"Create parameter"** 버튼을 클릭합니다  
   - **💡 Tip:** 이 버튼은 Parameter Store의 핵심 기능인 파라미터 생성을 시작합니다.  

2. **파라미터 생성 설정**  
   - **Name:** 저장할 파라미터의 이름을 입력합니다 (예: `/myapp/db/password`).  
   - **Value:** 저장할 값 (예: `mysecretpassword`).  
   - **Type:** `String` 또는 `SecureString`을 선택합니다.  
     - `SecureString`: 암호화된 값 저장 (KMS CMK 사용 시).  
     - `String`: 일반 텍스트 저장.  
   - **KMS CMK:** 암호화 옵션을 활성화하고, 사용할 KMS 키를 선택합니다.  
     - **⚠️ 주의:** KMS CMK를 사용할 경우, **AWS KMS 서비스**에서 키를 미리 생성해야 합니다.  

3. **생성 및 확인**  
   - **"Create"** 버튼을 클릭해 파라미터를 저장합니다.  
   - **📸 화면 확인:** 생성된 파라미터가 **"Parameters"** 탭의 목록에 표시되면 정상입니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **암호화 설정 수정**  
   - 기존 파라미터를 선택한 후, **"Edit"** 버튼을 클릭해 암호화 설정을 변경합니다.  
   - **KMS CMK**를 활성화하고, 사용할 KMS 키를 선택합니다.  

2. **CloudHSM 또는 Bag Encryption 선택**  
   - **Advanced Encryption Options** 섹션에서 **CloudHSM** 또는 **Bag Encryption**을 선택할 수 있습니다.  
   - **💡 Tip:** Bag Encryption은 KMS CMK 대신 암호화된 키를 저장하는 방식으로, 데이터 복호화 시 KMS 키가 필요합니다.  

3. **Secrets Manager 자동 교체 설정**  
   - **"Secrets Manager"**와 연동해 자동으로 파라미터를 갱신할 수 있습니다.  
   - **"Auto-Replace"** 옵션을 활성화하고, 연동할 Secrets Manager Secret을 선택합니다.  

> **⚠️ 주의:** KMS CMK 사용 시, **AWS Free Tier**의 KMS 기능은 25개의 키 생성이 가능합니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인**  
   - **"Parameters"** 탭에서 생성한 파라미터의 **Name**과 **Value**를 확인합니다.  
   - **📸 화면 확인:** 파라미터 목록에서 **Name** 필드가 `/myapp/db/password`로 표시되면 정상입니다.  

2. **암호화 상태 확인**  
   - 파라미터를 선택한 후, **"Details"** 탭에서 **"Encrypted"** 여부를 확인합니다.  
   - **💡 Tip:** 암호화된 값은 **"Value"** 필드에서 직접 확인할 수 없습니다.  

3. **테스트: 파라미터 값 조회**  
   - **AWS CLI**를 사용해 파라미터 값을 조회합니다:  
     ```bash
     aws ssm get-parameter --name "/myapp/db/password" --with-decryption
     ```  
   - **📸 화면 확인:** CLI에서 암호화된 값이 해독되어 출력되면 정상입니다.  

---

### 📋 체크리스트  
- [ ] AWS Management Console에 로그인했는지 확인  
- [ ] Parameter Store 대시보드로 이동했는지 확인  
- [ ] 파라미터 이름, 값, 타입을 올바르게 입력했는지 확인  
- [ ] KMS CMK 또는 Bag Encryption을 선택했는지 확인  
- [ ] 생성된 파라미터가 목록에 표시되는지 확인  
- [ ] CLI로 파라미터 값을 조회해 암호화/해독 기능을 테스트했는지 확인  

> **💡 Tip:** AWS Free Tier를 사용할 경우, KMS CMK는 25개까지 무료로 생성할 수 있습니다.  
> **⚠️ 주의:** KMS CMK를 사용할 때는 IAM 권한을 반드시 설정해야 합니다.

## ⌨️ AWS CLI로 Parameter Store 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

> **💡 Tip:** Parameter Store는 AWS Systems Manager(SSM) 서비스에 포함되어 있습니다. CLI 명령어는 `aws ssm`로 시작합니다. 사전에 `aws configure`로 AWS 자격 증명을 설정해 주세요.

---

### 예제 1: Parameter Store 리소스 조회
```bash
# [Parameter Store 파라미터 목록 조회]
aws ssm get-parameters-by-path --path "/example-path" --query "Parameters[*].Name" --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--path` | 조회할 경로 | `/example-path` |
| `--query` | 결과 필터링 | `"Parameters[*].Name"` |
| `--output` | 출력 형식 | `table` (테이블 형식) |

**예상 출력:**
```
NAME
-----
example-param1
example-param2
```

> **💡 Tip:** 경로를 지정하지 않으면 `/` 경로의 모든 파라미터를 조회합니다.

---

### 예제 2: Parameter Store 리소스 생성
```bash
# [Parameter Store 파라미터 생성]
aws ssm put-parameter \
    --name "example-param" \
    --value "secure-value" \
    --type "SecureString" \
    --tier "MultiRegion"
```

**필수 옵션:**
- `--name`: 파라미터 이름 (예: `example-param`)
- `--value`: 값 (암호화된 값은 `SecureString` 타입으로 전달)
- `--type`: `String` (평문) 또는 `SecureString` (암호화)
- `--tier`: `Standard` (단일 리전) 또는 `MultiRegion` (다중 리전)

**예상 출력:**
```json
{
    "Parameter": {
        "Name": "example-param",
        "Value": "secure-value",
        "Type": "SecureString",
        "Version": 1,
        "ARN": "arn:aws:ssm:region:account-id:parameter/example-param"
    }
}
```

> **⚠️ 주의:** `SecureString` 타입의 파라미터는 암호화되어 저장되며, 복호화를 위해서는 KMS 키가 필요합니다.

---

### 예제 3: Parameter Store 리소스 수정
```bash
# [Parameter Store 파라미터 수정]
aws ssm put-parameter \
    --name "example-param" \
    --value "updated-value" \
    --type "SecureString" \
    --overwrite
```

**옵션 설명:**
- `--overwrite`: 기존 파라미터를 덮어쓰기 (기본값은 `false`)

**예상 출력:**
```json
{
    "Parameter": {
        "Name": "example-param",
        "Value": "updated-value",
        "Type": "SecureString",
        "Version": 2,
        "ARN": "arn:aws:ssm:region:account-id:parameter/example-param"
    }
}
```

> **💡 Tip:** `--overwrite` 없이 동일한 파라미터 이름을 사용하면 새로운 버전으로 생성됩니다.

---

### 예제 4: Parameter Store 리소스 삭제
```bash
# [Parameter Store 파라미터 삭제]
aws ssm delete-parameter --name "example-param"

# 삭제 확인
aws ssm get-parameters --names "example-param"
```

**예상 출력:**
```
{
    "Parameters": []
}
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. `--force` 옵션 없이 실행 시 확정적입니다.

---

### 자주 사용하는 명령어 정리
```bash
# 파라미터 조회
aws ssm get-parameters --names "example-param"
aws ssm get-parameters-by-path --path "/example-path"

# 파라미터 생성/수정
aws ssm put-parameter --name "example-param" --value "value" --type "SecureString"

# 파라미터 삭제
aws ssm delete-parameter --name "example-param"
```

> **💡 Tip:** `SecureString` 파라미터는 KMS CMK를 사용해 암호화되며, `aws ssm get-parameters`로 복호화된 값을 조회할 수 있습니다.

---

### 보안 및 비용 고려 사항
| 항목 | 설명 |
|-----|-|
| **KMS CMK** | `SecureString` 타입 파라미터는 KMS 키로 암호화됩니다. `aws kms encrypt`로 수동 암호화도 가능합니다. |
| **Secrets Manager 자동 교체** | `SecureString` 파라미터는 AWS Secrets Manager와 연동해 자동으로 교체될 수 있습니다. |
| **비용** | `SecureString` 파라미터는 보안 수준이 높아 비용이 약 1.5배 증가할 수 있습니다. 프리티어는 1년간 100만 건까지 제공됩니다. |
| **CloudHSM** | 고보안 환경에서 KMS 키를 직접 관리하려면 CloudHSM을 사용할 수 있습니다. |
| **ACM** | 파라미터 암호화 시 AWS ACM으로 SSL/TLS 인증서를 사용할 수 있습니다. |

> **⚠️ 주의:** `SecureString` 파라미터는 `aws ssm get-parameters`로 복호화된 값을 확인할 수 있지만, 원본 값을 보관해야 할 경우 `aws ssm put-parameter`로 다시 저장해야 합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Parameter Store 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **Parameter Store의 핵심 목적: 구성 정보 보안 저장**  
   - 설명: 애플리케이션의 민감하지 않은 구성 데이터(예: 데이터베이스 호스트명, 포트 번호)를 안전하게 저장하고 관리하는 기능. 시험에서는 Parameter Store의 주요 용도와 보안 메커니즘에 대한 이해를 평가합니다.  
   - 키워드: `Parameter Store`, `Configuration Management`, `Secure Storage`

2. **KMS와의 연동 및 암호화 방식**  
   - 설명: Parameter Store는 AWS KMS를 통해 데이터를 암호화하고, 암호화 키를 관리하는 기능이 시험에서 자주 출제됩니다. 암호화 키의 회전, 암호화 방식(Envelope Encryption) 등에 대한 이해가 필수적입니다.  
   - 키워드: `KMS`, `Envelope Encryption`, `CMK 관리`

3. **Parameter Store vs Secrets Manager 비교**  
   - 설명: 시험에서는 Parameter Store와 Secrets Manager의 차이점(예: 민감 데이터 저장 여부, 암호화 메커니즘, 사용 시나리오)을 구분하는 문제가 자주 출제됩니다. 각 서비스의 적절한 사용 시나리오를 파악해야 합니다.  
   - 키워드: `Secrets Manager`, `Data Sensitivity`, `Use Case`

4. **Parameter Store의 제한사항과 성능 특성**  
   - 설명: Parameter Store는 대규모 데이터 저장에 적합하지 않으며, 지연 시간이 짧고 고가용성 유지가 중요한 시나리오에서 사용됩니다. 시험에서는 Parameter Store의 한계와 적절한 사용 조건을 평가합니다.  
   - 키워드: `Data Size Limit`, `Latency`, `High Availability`

5. **Parameter Store의 비용 구조 이해**  
   - 설명: Parameter Store는 기본적으로 무료이지만, KMS 사용 시 비용이 발생합니다. 시험에서는 Parameter Store와 관련된 비용 구조(예: 프리티어, 요금제)를 이해하는 것이 중요합니다.  
   - 키워드: `Cost Structure`, `Free Tier`, `KMS Pricing`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "Parameter Store는 민감한 데이터를 저장할 수 있는가?" → **No** (Secrets Manager 사용) | Parameter Store는 비민감 데이터 저장에 적합하며, 민감 데이터는 Secrets Manager를 사용해야 합니다. |
| 함정 2 | "Parameter Store의 데이터는 언제 암호화되나요?" → **Always** (KMS 암호화) | Parameter Store는 KMS를 사용해 모든 데이터를 암호화합니다. |
| 함정 3 | "Parameter Store는 대규모 데이터 저장에 적합한가?" → **No** (Systems Manager Parameter Store 사용) | Parameter Store는 소규모 데이터 저장에 적합하며, 대규모 데이터는 Systems Manager Parameter Store를 사용해야 합니다. |

#### 🔄 Parameter Store vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | Parameter Store | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 비민감 구성 데이터 저장 | Secrets Manager (민감 데이터 저장) | 민감/비민감 데이터 구분 시 |
| 확장성 | 소규모 데이터 저장에 적합 | Systems Manager Parameter Store (대규모 데이터) | 데이터 크기 요구사항에 따라 |
| 비용 | 기본 무료, KMS 사용 시 비용 발생 | Secrets Manager (암호화 및 관리 비용) | 보안 수준과 비용 균형 고려 |
| 지연시간 | 매우 낮음 (고가용성) | Systems Manager Parameter Store (지연 시간 유사) | 실시간 접근 요구 시 |

#### 📝 시험 대비 체크리스트
- [ ] Parameter Store의 핵심 목적을 한 문장으로 설명할 수 있는가?  
- [ ] Parameter Store를 선택해야 하는 시나리오를 알고 있는가?  
- [ ] Parameter Store의 제한사항/한계를 알고 있는가?  
- [ ] Parameter Store와 비슷한 서비스의 차이점을 설명할 수 있는가?  
- [ ] Parameter Store의 비용 구조를 이해하고 있는가?

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Parameter Store를 떠올리세요:  
> - `Configuration Management`  
> - `Secure Storage`  
> - `KMS Integration`  
> - `Non-Sensitive Data`  
> - `Low Latency`

---

| [⬅️ Secrets Manager](./Secrets-Manager.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 4](../README.md) | [ACM ➡️](./ACM.md) |

---
