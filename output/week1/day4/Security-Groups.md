---

| [⬅️ Instance Store](./Instance-Store.md) | [📑 Day 4 목차](./README.md) | [🏠 Week 1](../README.md) | [다음 Day ➡️](../day5/README.md) |

---

> **한 줄 정의:** Security Groups는 EC2 인스턴스의 네트워크 트래픽을 제어하기 위한 AWS의 가상 방화벽 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 인스턴스 IP 주소를 수동으로 관리해야 하며, IP 변경 시 보안 정책을 다시 설정해야 하는 번거로움  
  (예: 로컬 서버에서 IP 변경 후 방화벽 규칙을 매번 수정해야 함)  
- **문제 2:** 특정 포트(예: SSH 22, HTTP 80)에 대한 접근 제어가 불충분해 보안 위험이 존재  
  (예: 외부 공격자가 임의로 포트를 스캔해 취약점 탐색 가능)  
- **문제 3:** 여러 인스턴스 간의 네트워크 통신 규칙을 중앙에서 관리하지 못해 일관성 부족  
  (예: DB 서버와 웹 서버 간 통신 규칙이 서로 다른 보안 그룹에 분산되어 있음)  

**Security Groups로 해결:**
- **해결 1:** 가상 방화벽을 통해 IP 주소 관리 대신 포트 및 프로토콜 기반 규칙을 설정해 보안 정책 자동화  
  (예: SSH 포트만 허용해 외부 공격 차단)  
- **해결 2:** 규칙 기반 접근 제어로 특정 포트에 대한 트래픽을 정밀하게 제어  
  (예: HTTP 80 포트만 허용해 보안 위험 최소화)  
- **해결 3:** 중앙에서 통합된 보안 규칙 관리로 인스턴스 간 네트워크 통신을 효율적으로 제어  
  (예: DB 서버와 웹 서버 간 통신 규칙을 하나의 보안 그룹에 집중 관리)  

### 비유로 이해하기
**집의 문을 닫는 방화벽**을 생각해보세요. Security Groups는 집의 문에 설치된 방화벽과 같아요. 외부에서 들어오는 사람(트래픽)이 집(인스턴스)에 들어가려면, 먼저 문(보안 그룹)의 규칙을 통과해야 합니다. 예를 들어, "22번 포트(SSH)는 허용"이라는 규칙이 설정되면, 외부에서 SSH 접속 시 허용되지만, 다른 포트는 차단됩니다. 이처럼 보안 그룹은 네트워크 트래픽을 제어하는 가상의 문 역할을 합니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 웹 서버의 SSH 접근 제어 | AWS EC2에서 웹 서버를 운영할 때, 관리자만 SSH로 접속하도록 설정 |
| 시나리오 2 | 데이터베이스 보안 강화 | RDS 인스턴스에만 특정 IP 범위에서의 MySQL 3306 포트 접근 허용 |
| 시나리오 3 | VPC 내 인스턴스 간 통신 | VPC 내에서 EC2 인스턴스 간의 HTTP 통신을 허용하고 외부 접근 차단 |

**이럴 때 Security Groups를 선택하세요:**
- ✅ **상황 1:** 특정 포트만 허용해야 하는 네트워크 보안이 필요한 경우  
- ✅ **상황 2:** 여러 인스턴스 간 통신 규칙을 중앙에서 관리해야 할 때  
- ✅ **상황 3:** IP 주소가 자주 변경되는 환경에서 보안 정책을 자동화해야 할 때  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **상황 → 대안 서비스:** IP 기반 방화벽이 필요한 경우 → **Network ACLs** (VPC 레벨의 보안 정책)  
- ❌ **상황 → 대안 서비스:** 고급 보안 정책이 필요한 경우 → **AWS WAF** (웹 애플리케이션 방화벽)  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **VPC (Virtual Private Cloud)** | 보안 그룹이 VPC 내에서 작동하므로, VPC 설정이 필수적 | 예: Security Groups → VPC → EC2 인스턴스 |
| **EC2 인스턴스** | 보안 그룹은 EC2 인스턴스의 네트워크 트래픽을 제어 | 예: EC2 → Security Groups → 외부 네트워크 |
| **Elastic Network Interface (ENI)** | ENI에 보안 그룹을 할당해 네트워크 트래픽을 제어 | 예: EC2 → ENI → Security Groups |

**자주 사용되는 아키텍처 패턴:**
```
[사용자] → [클라우드 프론트] → [S3]  
[사용자] → [SSH] → [EC2 인스턴스] (Security Groups 적용)  
[EC2 인스턴스] → [RDS] (Security Groups로 통신 허용)  
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **Security Groups 자체 비용** | **무료** | **항상 무료** |
| **VPC 사용 비용** | $0.01/시간 (기본 VPC) | 12개월 무료 |
| **EC2 인스턴스 사용 비용** | $0.01/시간 (t2.micro) | 월 750시간 무료 |

**비용 최적화 팁:**
1. 💡 **팁 1:** 프리티어를 활용해 최대한 많은 리소스를 테스트 (예: t2.micro 인스턴스 750시간 무료)  
2. 💡 **팁 2:** 보안 그룹 규칙을 정확히 설정해 불필요한 트래픽 차단 (예: SSH 포트만 허용)  
3. 💡 **팁 3:** VPC 설정 시 기본 VPC를 사용해 비용 절감 (기본 VPC는 무료)  

> **⚠️ 비용 주의:** Security Groups 자체는 무료이지만, VPC나 EC2 인스턴스 등 연동 서비스 사용 시 비용이 발생할 수 있습니다. AWS Cost Explorer를 활용해 사용량을 모니터링하세요.

## 📚 핵심 개념

### 개념 1: 보안 그룹의 역할과 중요성  
보안 그룹은 AWS EC2 인스턴스에 적용되는 가상 방화벽으로, **인바운드 및 아웃바운드 트래픽을 제어하는 핵심 요소**입니다. 이는 네트워크 보안을 강화하고, 악성 트래픽을 차단하여 인스턴스의 안전성을 보장합니다. 보안 그룹은 **IP 주소 기반의 규칙**으로 구성되며, 특정 포트나 프로토콜을 허용하거나 거부할 수 있습니다. 예를 들어, SSH(22번 포트) 접근을 허용하거나 HTTP(80번 포트) 트래픽을 차단하는 설정이 가능합니다.  

#### 왜 중요한가?  
- **트래픽 제어의 기본 수단**: 인스턴스에 접근하는 모든 트래픽을 필터링하여 보안 위험을 최소화합니다.  
- **다중 인스턴스 관리**: 동일한 보안 그룹을 여러 인스턴스에 적용하여 일관된 보안 정책을 유지할 수 있습니다.  
- **사전 설정된 기본 규칙**: 기본적으로 모든 트래픽을 거부하고, 명시적으로 허용된 트래픽만 통과시키는 **모든 거부** 원칙을 따릅니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **인바운드 규칙** | 외부에서 인스턴스로 들어오는 트래픽을 제어 | SSH 접근 허용 (0.0.0.0/0, TCP, 22) |
| **아웃바운드 규칙** | 인스턴스에서 외부로 나가는 트래픽을 제어 | 모든 트래픽 허용 (0.0.0.0/0, 모든 프로토콜) |
| **기본 설정** | 모든 트래픽을 거부하고, 명시적으로 허용된 트래픽만 통과 | 기본적으로 모든 포트 차단, 예외 설정 필요 |

> **💡 Tip:** 실무에서는 **SSH 접근만 허용**하고, 다른 포트는 최소한의 범위로 제한하는 것이 보안 강화의 핵심입니다. 또한, **IP 범위 설정** 시 `0.0.0.0/0`은 전체 인터넷을 허용하므로 주의해야 합니다.

---

### 개념 2: 보안 그룹 규칙의 작동 원리  
보안 그룹 규칙은 **위쪽에서 아래쪽으로 순차적으로 검사**되며, 첫 번째로 일치하는 규칙에 따라 트래픽이 허용되거나 거부됩니다. 이는 **allow 규칙이 우선**되며, **deny 규칙은 이후에 적용**됩니다. 예를 들어, 22번 포트를 허용하는 규칙이 있고, 80번 포트를 거부하는 규칙이 있다면, 22번 포트는 허용되고 80번 포트는 거부됩니다.  

#### 작동 원리  
1. **규칙 순서 검사**: 보안 그룹 내의 규칙은 입력 순서대로 검사됩니다.  
2. **일치 여부 확인**: 트래픽이 규칙의 IP 범위, 프로토콜, 포트에 일치하면 해당 규칙을 적용합니다.  
3. **결과 결정**: 규칙이 허용이면 트래픽을 통과시킵니다. 거부이면 차단합니다.  

> **💡 Tip:** 규칙을 추가할 때는 **위쪽에 우선순위가 높은 규칙을 배치**해야 하며, **모든 트래픽을 거부하는 기본 상태**를 유지하는 것이 중요합니다.

---

### 개념 3: 보안 그룹과 네트워크 ACL의 차이  
보안 그룹은 **인스턴스 수준**에서 작동하는 반면, 네트워크 ACL(NACL)은 **서브넷 수준**에서 트래픽을 제어합니다. 이 두 서비스는 서로 보완적인 역할을 하며, **중복 적용**을 통해 보다 강력한 보안을 제공할 수 있습니다. 보안 그룹은 **동적**으로 설정되며, NACL은 **정적**으로 설정됩니다.  

#### 주요 특징  
1. **범위 차이**:  
   - 보안 그룹: 인스턴스별로 적용됨 (예: 특정 인스턴스에만 허용)  
   - NACL: 서브넷 전체에 적용됨 (예: 특정 서브넷의 모든 인스턴스에 적용)  
2. **규칙 순서**:  
   - 보안 그룹: 규칙 순서가 중요 (위에서 아래로 검사)  
   - NACL: 규칙 순서가 중요 (위에서 아래로 검사)  
3. **기본 설정**:  
   - 보안 그룹: 기본적으로 모든 트래픽 거부  
   - NACL: 기본적으로 모든 트래픽 허용 (명시적으로 거부해야 함)  

> **💡 Tip:** 보안 그룹과 NACL을 함께 사용할 때는 **중복된 규칙을 피하고**, **보안 그룹을 우선으로 설정**하는 것이 좋습니다. 예를 들어, 보안 그룹에서 SSH 접근을 허용하고, NACL에서 추가적인 제한을 걸 수 있습니다.

## 🖥️ AWS 콘솔에서 Security Groups 사용하기

### Step 1: Security Groups 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - AWS 계정 ID와 비밀번호를 입력해 로그인합니다.  
2. 상단 검색창에서 "Security Groups"를 입력하고, 검색 결과에서 **"Security Groups"**를 클릭합니다.  
   - **EC2 서비스 > Security Groups** 메뉴로 이동합니다.  

> **📸 화면 확인:** "Security Groups" 대시보드가 표시되면 정상입니다.  
> **💡 Tip:** 기본적으로 "default" 보안 그룹이 생성되어 있습니다. 이는 EC2 인스턴스의 기본 네트워크 보호 기능을 제공합니다.

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **보안 그룹 생성**  
   - **"Create security group"** 버튼을 클릭합니다.  
   - **Name** 필드에 `MyWebServerSG`를 입력하고, **Description**에 "Web server access"를 추가합니다.  
   - **VPC**는 기본 VPC를 선택합니다.  
   - **Tags**에 필요 시 키-값을 입력합니다.  
2. **규칙 설정**  
   - **Inbound rules**에서 **"HTTP"** 규칙을 추가합니다.  
     - **Type**: All traffic (HTTP), **Port Range**: 80, **Source**: 0.0.0.0/0  
   - **Outbound rules**에서 **"All traffic"**를 허용합니다.  
3. **확인 및 생성**  
   - **"Create"** 버튼을 클릭해 보안 그룹을 생성합니다.  

> **📸 화면 확인:** "MyWebServerSG"가 생성된 목록에 표시되고, 규칙이 정상적으로 입력되었는지 확인합니다.

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **규칙 수정**  
   - 보안 그룹을 클릭해 **"Inbound rules"** 탭으로 이동합니다.  
   - **SSH 접근 허용**을 추가합니다.  
     - **Type**: SSH, **Port Range**: 22, **Source**: 192.168.1.0/24  
2. **IP 범위 제한**  
   - **Inbound rules**에서 **"Source"**를 **"Custom IP ranges"**로 변경해 특정 IP 주소만 허용합니다.  
3. **Outbound 규칙 조정**  
   - **Outbound rules**에서 **"All traffic"**를 **"Deny all"**로 변경해 외부 트래픽을 차단합니다.  

> **⚠️ 주의:** 보안 그룹은 **"Allow all"** 규칙이 기본값이므로, **필요한 트래픽만 명시적으로 허용**해야 합니다. 모든 트래픽을 허용하면 보안 위험이 있습니다.

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인**  
   - **"Security Groups"** 목록에서 `MyWebServerSG`를 클릭해 규칙을 확인합니다.  
   - **Inbound/Outbound rules**가 올바르게 설정되었는지 점검합니다.  
2. **인스턴스 연결 테스트**  
   - **EC2 인스턴스**를 생성하고, 생성한 보안 그룹을 할당합니다.  
   - **SSH**로 인스턴스에 접속해 네트워크 설정이 정상인지 확인합니다.  
3. **외부 접근 테스트**  
   - 웹 브라우저로 인스턴스의 IP 주소를 입력해 HTTP 서비스가 정상 작동하는지 테스트합니다.  

> **📸 화면 확인:** 인스턴스 생성 후, **EC2 대시보드**에서 보안 그룹이 할당되었는지, 외부에서 HTTP 요청이 처리되는지 확인합니다.  

---

### 📌 실무 예제: 보안 그룹 규칙 설정  
```bash
# CLI로 보안 그룹 생성 (참고)
aws ec2 create-security-group --group-name MyWebServerSG --description "Web server access" --vpc-id vpc-12345678
```

| 규칙 유형 | 포트 | 프로토콜 | 설명 |
|----------|------|----------|------|
| Inbound  | 80   | TCP      | HTTP 웹 트래픽 허용 |
| Inbound  | 22   | TCP      | SSH 접근 허용 |
| Outbound | -    | -        | 모든 트래픽 차단 |

> **💡 Tip:** 보안 그룹은 **리소스 생성 시 자동 적용**되므로, 인스턴스 생성 후 보안 그룹을 수정해도 **이미 생성된 인스턴스는 변경 사항을 반영하지 않습니다**.

## ⌨️ AWS CLI로 Security Groups 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**💡 Tip:** AWS CLI를 사용하기 전에 `aws configure`로 액세스 키, 시크릿 키, 리전을 설정해야 합니다.  
**⚠️ 주의:** CLI 명령어 실행 전에 `--region` 옵션으로 리전을 명시적으로 지정하는 것이 권장됩니다.

---

### 예제 1: Security Groups 리소스 조회
```bash
# Security Groups 목록 조회
aws ec2 describe-security-groups --query 'SecurityGroups[*].GroupName' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 결과 필터링 | `'SecurityGroups[*].GroupName'` |
| `--output` | 출력 형식 | `json`, `table`, `text` |

**예상 출력:**
```
TableName: SecurityGroups
+------------------+
| GroupName        |
+------------------+
| default          |
| my-secure-group  |
+------------------+
```

**🔍 설명:** `describe-security-groups` 명령어는 Security Groups의 이름, ID, 규칙 등을 조회합니다. `--query`를 통해 특정 필드만 출력할 수 있습니다.

---

### 예제 2: Security Groups 리소스 생성
```bash
# Security Group 생성
aws ec2 create-security-group --group-name "my-secure-group" --description "My secure group for web server"
```

**필수 옵션:**
- `--group-name`: Security Group 이름 (문자열)
- `--description`: 설명 (선택적)

**예상 출력:**
```json
{
    "GroupId": "sg-0abcdef12345678901"
}
```

**🔍 설명:** 생성된 Security Group의 ID(`GroupId`)는 이후 규칙 추가 시 필수적으로 사용됩니다.  
**💡 Tip:** 생성된 Security Group은 EC2 인스턴스와 VPC에 연결되어야 합니다.

---

### 예제 3: Security Groups 규칙 수정
```bash
# Inbound 규칙 추가 (HTTP)
aws ec2 authorize-security-group-ingress \
    --group-id sg-0abcdef12345678901 \
    --protocol tcp \
    --port-range 80:80 \
    --cidr 0.0.0.0/0
```

**필수 옵션:**
- `--group-id`: 수정할 Security Group ID
- `--protocol`: `tcp`, `udp`, `icmp` 등
- `--port-range`: 포트 범위 (예: `80:80` 또는 `22:443`)
- `--cidr`: 허용할 IP 범위

**🔍 설명:** `authorize-security-group-ingress`는 inbound 트래픽을 허용하고, `revoke-security-group-ingress`는 규칙을 제거합니다.

---

### 예제 4: Security Groups 리소스 삭제
```bash
# Security Group 삭제
aws ec2 delete-security-group --group-id sg-0abcdef12345678901

# 삭제 확인 (실패 시 오류 발생)
aws ec2 describe-security-groups --group-id sg-0abcdef12345678901
```

**⚠️ 주의:**  
1. Security Group은 EC2 인스턴스나 ENI에 연결되어 있으면 삭제할 수 없습니다.  
2. 삭제 후에는 **복구 불가**합니다.  
3. AWS Free Tier에 포함되므로 비용 걱정 없이 테스트 가능합니다.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws ec2 describe-security-groups --group-id "sg-0123456789abcdef0"
aws ec2 describe-security-groups --query "SecurityGroups[*].GroupName" --output table

# 생성
aws ec2 create-security-group --group-name "my-group" --description "My group"

# 규칙 추가/제거
aws ec2 authorize-security-group-ingress --group-id "sg-0123456789abcdef0" --protocol tcp --port-range 80:80 --cidr 0.0.0.0/0
aws ec2 revoke-security-group-ingress --group-id "sg-0123456789abcdef0" --protocol tcp --port-range 80:80 --cidr 0.0.0.0/0

# 삭제
aws ec2 delete-security-group --group-id "sg-0123456789abcdef0"
```

**📌 주의사항:**  
- Security Group은 VPC에 속한 자원만 관리 가능합니다.  
- `--region` 옵션을 생략하면 `aws configure`에서 설정한 리전이 자동으로 적용됩니다.  
- CLI 사용 시 `jq` 도구로 JSON 결과를 편리하게 필터링할 수 있습니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Security Groups 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **포인트 1: 규칙 순서의 중요성**  
   - 설명: Security Groups의 규칙은 위에서 아래로 순차적으로 적용되며, 첫 번째로 일치하는 규칙이 실행됩니다. 이는 트래픽 제어 시 우선순위를 결정하는 핵심 개념으로, 시험에서 규칙 오버라이드나 중복 규칙 문제는 주요 시험 항목입니다.  
   - 키워드: `규칙 순서`, `트래픽 우선순위`

2. **포인트 2: 인바운드/아웃바운드 규칙의 차이**  
   - 설명: 인바운드 규칙은 외부에서 인스턴스로의 트래픽을 제어하고, 아웃바운드 규칙은 인스턴스에서 외부로의 트래픽을 제어합니다. 시험에서는 이 두 규칙의 구분과 기본 정책(인바운드는 `deny by default`, 아웃바운드는 `allow by default`)을 묻는 문제가 자주 출제됩니다.  
   - 키워드: `인바운드`, `아웃바운드`, `기본 정책`

3. **포인트 3: 상태 기반 규칙(stateful)**  
   - 설명: Security Groups는 상태 기반 규칙을 지원하여, 예를 들어 SSH 인증 후 생성된 트래픽도 자동으로 허용합니다. 이는 네트워크 보안 설계에서 중요한 개념으로, 시험에서는 상태 기반 규칙의 작동 방식과 제한 사항이 주요 테스트 항목입니다.  
   - 키워드: `상태 기반`, `stateful`, `트래픽 허용`

4. **포인트 4: 인스턴스 연결 및 규칙 적용 범위**  
   - 설명: Security Groups는 인스턴스에 연결되어야 하며, 규칙은 해당 인스턴스의 IP 주소와 포트를 기반으로 적용됩니다. 시험에서는 다중 인스턴스 연결 시 규칙 적용 범위, VPC 내외의 트래픽 제어 방식이 주요 시험 항목입니다.  
   - 키워드: `인스턴스 연결`, `VPC 범위`, `IP 주소`

5. **포인트 5: 기본 규칙의 제한성**  
   - 설명: Security Groups는 기본적으로 모든 트래픽을 거부합니다. 시험에서는 이 기본 정책을 위반하는 경우(예: 인바운드 규칙을 누락) 발생할 수 있는 보안 취약점과 해결 방법이 주요 시험 포인트입니다.  
   - 키워드: `기본 거부`, `보안 취약점`, `규칙 누락`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "인바운드 규칙은 항상 모든 트래픽을 허용한다"는 오해 | **인바운드 규칙은 기본적으로 거부**하며, 명시적으로 허용해야 함 |
| 함정 2 | "아웃바운드 규칙은 인스턴스 내부 트래픽만 제어한다"는 오해 | **아웃바운드 규칙은 인스턴스에서 외부로의 트래픽을 제어** |
| 함정 3 | "Security Groups는 상태 비기반 규칙만 지원한다"는 오해 | **상태 기반 규칙이 지원되며, 이는 보안 설계의 핵심** |

#### 🔄 Security Groups vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | Security Groups | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 인스턴스에 연결된 네트워크 트래픽 제어 | NACLs (Network Access Control Lists) | VPC 내부 트래픽 제어 시 선택 |
| 확장성 | 규칙 수 제한(50개) | NACLs는 100개 규칙 지원 | 규칙 수 제한이 있을 경우 선택 |
| 비용 | 무료 | NACLs도 무료 | 비용 관계 없이 선택 |
| 지연시간 | 저지연(인스턴스와 직접 연결) | NACLs는 추가 레이어로 지연 증가 | 실시간 트래픽 제어 시 선택 |

#### 📝 시험 대비 체크리스트
- [ ] Security Groups의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  **→ 인스턴스에 연결된 네트워크 트래픽을 제어하는 방화벽 역할**
- [ ] Security Groups를 선택해야 하는 시나리오를 알고 있는가?  
  **→ VPC 내 인스턴스 간 트래픽 제어, 인바운드/아웃바운드 트래픽 관리**
- [ ] Security Groups의 제한사항/한계를 알고 있는가?  
  **→ 규칙 수 제한(50개), 상태 기반 규칙의 한계, VPC 외부 트래픽 제어 불가**
- [ ] Security Groups와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  **→ NACLs는 상태 비기반, VPC Flow Logs는 트래픽 로깅 기능**
- [ ] Security Groups의 비용 구조를 이해하고 있는가?  
  **→ 무료, NACLs도 무료, 추가 비용 없음**

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Security Groups를 떠올리세요:  
> - **트래픽 제어**  
> - **인바운드/아웃바운드**  
> - **상태 기반 규칙**  
> - **VPC 내 트래픽**  
> - **규칙 순서**

---

| [⬅️ Instance Store](./Instance-Store.md) | [📑 Day 4 목차](./README.md) | [🏠 Week 1](../README.md) | [다음 Day ➡️](../day5/README.md) |

---
