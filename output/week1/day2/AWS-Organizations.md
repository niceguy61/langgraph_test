---

| [⬅️ IAM](./IAM.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 1](../README.md) | [AWS Control Tower ➡️](./AWS-Control-Tower.md) |

---

# AWS Organizations 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** AWS Organizations는 AWS 계정을 통합하여 관리하고 보안을 강화하는 데 사용되는 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 여러 AWS 계정을 분리 관리할 때 정책 일관성 유지가 어려웠습니다. 각 계정별로 IAM 정책을 수동으로 적용해야 하며, 실수로 권한을 과도하게 부여할 수 있었습니다.  
- **문제 2:** 계정 간 보안 리스크가 증가했습니다. 예를 들어, 개발용 계정에서 생산 환경에 접근할 수 있는 권한이 부여된 경우, 데이터 유출 위험이 있었습니다.  
- **문제 3:** 계정 생성/관리에 따른 운영 부담이 커졌습니다. 수십 개의 계정을 개별적으로 관리하면 리소스 할당, 모니터링, 정책 적용이 복잡해졌습니다.

**AWS Organizations로 해결:**
- **해결 1:** 단일 관리 포털에서 모든 계정을 통합 관리할 수 있습니다. 계정 생성, 삭제, 정책 적용 등이 자동화되어 운영 효율성이 높아집니다.  
- **해결 2:** **SCP(Service Control Policy)**를 통해 계정별 권한을 제한할 수 있습니다. 예를 들어, 개발 계정에서는 S3 버킷 생성이 허용되지만, 생산 계정에서는 접근이 거부됩니다.  
- **해결 3:** **IAM 역할 위임**과 **서비스 연결 역할**을 통해 계정 간 자동화된 자격 증명 연결이 가능합니다. 예를 들어, 로컬 개발 환경에서 AWS Lambda를 실행할 때 자동으로 임시 자격 증명을 생성해 보안을 강화할 수 있습니다.

### 비유로 이해하기
AWS Organizations는 기업의 조직 구조를 관리하는 역할을 합니다. 예를 들어, 회사가 5개 부서를 운영할 때, 각 부서가 독자적인 AWS 계정을 가지고 있다면, 정책 일관성과 보안 관리가 어려웠습니다. AWS Organizations는 이 계정들을 하나의 "회사"로 통합해, 모든 부서가 공통된 정책을 따르고, 필요 시 자동으로 자격 증명을 연결하는 체계를 만듭니다. 이렇게 하면 관리자는 한 번에 모든 계정을 모니터링하고, 보안 위험을 최소화할 수 있습니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 기업이 여러 부서/팀이 독자적인 AWS 계정을 사용하는 경우 | 한 회사의 개발팀, 운영팀, 마케팅팀이 각각 별도의 AWS 계정을 운영 |
| 시나리오 2 | 법적/규제 요구사항을 준수해야 하는 경우 | GDPR, HIPAA 등 특정 규제를 충족해야 하는 조직 |
| 시나리오 3 | DevOps 팀이 CI/CD 파이프라인을 구축하는 경우 | 코드 변경 시 자동으로 AWS 자원을 생성/삭제하는 프로세스 |

**이럴 때 AWS Organizations를 선택하세요:**
- ✅ **다양한 계정을 통합 관리해야 할 때**  
- ✅ **보안 정책을 일관되게 적용해야 할 때**  
- ✅ **자원 공유 및 자동화된 자격 증명 연결이 필요할 때**

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **단일 계정만 사용하는 경우** → **AWS Identity and Access Management (IAM)**  
- ❌ **계정 간 자원 공유가 필요한 경우** → **AWS Resource Access Manager**  
- ❌ **개별 계정에서 정책 관리가 충분한 경우** → **AWS IAM 정책**

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **AWS IAM** | 자격 증명 관리 및 권한 제어 | Organizations → IAM 역할 생성 → S3 버킷 접근 |
| **AWS STS** | 임시 자격 증명 생성 및 서비스 간 연결 | Lambda → STS → Organizations 계정 자격 증명 획득 |
| **AWS SCP** | 계정별 정책 제어 | Organizations → SCP 정책 적용 → S3 버킷 제한 |

**자주 사용되는 아키텍처 패턴:**
```
[사용자] → [IAM 역할] → [AWS Organizations] → [S3 버킷]  
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **계정 생성/관리** | $0.25/월 (최대 25개 계정) | 12개월 무료 |
| **SCP 정책 적용** | $0.01/정책/월 | 항상 무료 |
| **서비스 연결 역할 생성** | $0.01/역할/월 | 항상 무료 |

**비용 최적화 팁:**
1. 💡 **SCP 정책을 통해 권한을 최소화하세요**. 예를 들어, S3 버킷 생성 권한만 허용하고, 삭제 권한은 거부하는 정책을 적용하면 불필요한 리소스 사용을 줄일 수 있습니다.  
2. 💡 **프리티어 기간 동안 계정 수를 최대한 활용하세요**. AWS Organizations는 최대 25개 계정까지 무료로 제공되므로, 필요한 모든 계정을 포함하세요.  
3. 💡 **AWS Cost Explorer를 사용해 비용을 모니터링하세요**. 비용이 예상보다 높아지는 경우, SCP 정책을 재검토하거나 계정 수를 줄이는 방법을 고려하세요.

> **⚠️ 비용 주의:** **계정 수를 초과하면 추가 비용이 발생합니다**. AWS Organizations는 최대 25개 계정까지 무료로 제공되며, 26개 이상의 계정을 생성할 경우 월 $0.25당 비용이 발생합니다. 또한, **SCP 정책이 없을 경우** 계정 관리 비용이 증가할 수 있으므로, 반드시 최소 권한 원칙을 적용해야 합니다.

## 📚 핵심 개념

### 개념 1: IAM 역할 위임 및 Trust 정책  
AWS Organizations에서 IAM 역할은 리소스에 대한 액세스 권한을 부여하는 기능입니다. IAM 역할은 **Trust 정책**을 통해 특정 엔티티(예: EC2 인스턴스, Lambda 함수, 다른 AWS 서비스)가 해당 역할을 가정할 수 있도록 허용합니다. 이는 리소스에 대한 액세스를 제한하면서도 보안을 유지하는 데 핵심적인 역할을 합니다. 예를 들어, EC2 인스턴스가 S3 버킷에 데이터를 읽을 수 있도록 하려면 EC2 역할을 생성하고, Trust 정책을 통해 EC2 서비스가 해당 역할을 가정할 수 있도록 설정해야 합니다.  

#### 왜 중요한가?  
- **보안 강화**: IAM 역할은 장기적인 액세스 키 대신 임시 자격 증명을 제공하여 데이터 유출 위험을 줄입니다.  
- **리소스 관리**: 역할을 통해 특정 서비스나 사용자만 특정 리소스에 접근할 수 있도록 제한하여 팀 내 협업 효율성을 높입니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **Trust 정책** | 역할을 가정할 수 있는 엔티티를 정의하는 정책 | `sts:AssumeRole` 권한을 EC2 서비스에 부여 |
| **Role ARN** | 역할의 고유 식별자로, 리소스에 대한 액세스 권한을 지정 | `arn:aws:iam::123456789012:role/MyRole` |
| **Principal** | 역할을 가정할 수 있는 엔티티(예: 사용자, 서비스) | `Service: ec2.amazonaws.com` |

> **💡 Tip:** IAM 역할을 생성할 때는 `Principal` 필드에 서비스 이름을 명시해 서비스 간 통신을 안전하게 설정하세요.  

---

### 개념 2: STS(Security Token Service)를 통한 임시 자격 증명 생성  
STS는 AWS에서 임시 AWS 자격 증명(예: Access Key ID, Secret Access Key, Session Token)을 생성하는 서비스입니다. 이 임시 자격 증명은 특정 시간 동안만 유효하며, 보안을 강화하는 데 사용됩니다. 예를 들어, 웹 애플리케이션에서 사용자 인증 후 임시 자격 증명을 생성해 S3 버킷에 접근할 수 있도록 할 수 있습니다.  

#### 작동 원리  
1. **요청 생성**: 애플리케이션은 `GetFederationToken` 또는 `GetSessionToken` API를 호출해 임시 자격 증명을 요청합니다.  
2. **STS 처리**: STS는 요청을 검증한 후 임시 자격 증명을 생성하고, 이를 사용자에게 반환합니다.  
3. **리소스 액세스**: 애플리케이션은 생성된 임시 자격 증명을 사용해 AWS 리소스에 액세스합니다.  

> **💡 Tip:** STS는 AWS CLI 명령어 `aws sts get-session-token`을 통해 간단하게 테스트할 수 있습니다.  

---

### 개념 3: 서비스 연결 역할(Service Linked Role)  
서비스 연결 역할은 AWS 서비스가 다른 서비스와 상호작용할 때 자동으로 생성되는 역할입니다. 예를 들어, S3 버킷을 생성하는 서비스는 `s3.amazonaws.com`과 연결된 역할을 생성해 해당 버킷에 접근할 수 있도록 합니다. 이 역할은 서비스가 정확한 권한을 가진 상태에서 자동으로 리소스에 액세스할 수 있도록 보장합니다.  

#### 주요 특징  
1. **자동 생성**: 서비스가 필요한 권한을 자동으로 역할에 부여해 사용자 관리 부담을 줄입니다.  
2. **보안 제어**: 서비스 연결 역할은 특정 서비스만 접근할 수 있도록 제한되어, 권한 오버라이드를 방지합니다.  
3. **관리 가능성**: AWS 콘솔에서 서비스 연결 역할을 확인하고 필요 시 수정할 수 있습니다.  

> **⚠️ 주의:** 서비스 연결 역할은 수동으로 삭제할 수 없으며, 연결된 서비스를 제거해야만 삭제가 가능합니다.  

---

### 개념 4: 권한 경계(Permission Boundary)  
권한 경계는 IAM 사용자 또는 역할이 가질 수 있는 최대 권한을 제한하는 정책입니다. 이는 사용자가 지정된 정책을 초과하여 리소스에 접근하지 못하도록 방지합니다. 예를 들어, 특정 사용자가 EC2 인스턴스 생성 권한만 가질 수 있도록 설정하면, 다른 리소스(예: S3 버킷)에 대한 접근이 차단됩니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **Boundary Policy** | 권한 경계를 정의하는 정책 | `arn:aws:iam::123456789012:policy/MyBoundaryPolicy` |
| **Principal** | 권한 경계를 적용할 사용자 또는 역할 | `AWSPrincipal: arn:aws:iam::123456789012:user/MyUser` |
| **Scope** | 권한 경계가 적용되는 범위(예: 조직 전체, 특정 루트) | `Root: root-123456789012` |

> **💡 Tip:** 권한 경계는 정책을 통해 설정할 수 있으며, AWS Organizations의 SCP와 함께 사용해 보다 정교한 보안을 구현할 수 있습니다.  

---

### 개념 5: SCP(Service Control Policy) 정책 관리  
SCP는 AWS Organizations에서 조직 내 모든 AWS 서비스에 대한 액세스 권한을 제어하는 정책입니다. 이는 조직 내 모든 서비스가 특정 정책을 준수하도록 강제하며, 보안 및 규정 준수를 위해 필수적입니다. 예를 들어, S3 버킷 생성을 금지하는 SCP를 설정하면, 조직 내 모든 AWS 서비스에서 S3 버킷 생성이 차단됩니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **Policy Type** | SCP의 유형(예: 서비스 수준, 리소스 수준) | `ServiceControlPolicy` |
| **Scope** | SCP 적용 범위(예: 조직 전체, 특정 루트) | `Root: root-123456789012` |
| **Policy Document** | SCP의 구체적인 권한 제어 내용 | `{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Deny",
      "Action": "s3:CreateBucket",
      "Resource": "*"
    }
  ]
}` |

> **💡 Tip:** SCP는 조직 루트 또는 루트 하위에 적용되며, 하위 조직의 정책을 상속받는 방식으로 관리됩니다.

## 🖥️ AWS 콘솔에서 AWS Organizations 사용하기

### Step 1: AWS Organizations 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - IAM 사용자 또는 AWS 계정 ID로 로그인합니다.  
2. 상단 검색창에서 **"AWS Organizations"**를 입력하고, 검색 결과에서 서비스를 클릭합니다.  
3. **"AWS Organizations" 대시보드**가 열리면, 조직의 구조 및 구성 요소를 확인할 수 있습니다.  

> **📸 화면 확인:**  
> - 왼쪽 메뉴에서 **"Organizations"** 탭이 활성화된 상태인지 확인합니다.  
> - 조직 이름과 상태가 표시되는 대시보드가 뜨는지 확인합니다.  

---

### Step 2: [주요 작업 1 - IAM 사용자 및 역할 생성]  
1. **"Organizations" 대시보드**에서 **"Create organization"** 또는 **"Create account"** 버튼을 클릭합니다.  
   - **"Create organization"**: 기존 조직에 계정 추가.  
   - **"Create account"**: 새로운 조직 생성.  
2. **계정 생성 설정**  
   - **Account name**: 생성할 계정 이름 입력 (예: `Dev-Team`).  
   - **Email address**: 관리자 이메일 입력 (예: `admin@example.com`).  
   - **Password**: 계정 비밀번호 설정.  
3. **IAM 사용자 생성**  
   - 조직 내 계정 생성 후, **"IAM" 서비스**로 이동하여 **"Users"**를 클릭합니다.  
   - **"Add user"** 버튼을 클릭하고, 사용자 이름 입력 후 **"Programmatic access"** 권한 부여.  
   - 생성된 사용자에게 **"AWS Organizations"** 역할을 연결합니다.  

> **📸 화면 확인:**  
> - **"Create account"** 화면에서 이메일과 비밀번호 입력 후 생성 완료.  
> - **"IAM"** 서비스에서 사용자 생성 후, **"Permissions"** 탭에서 **"AWS Organizations"** 역할 연결 확인.  

---

### Step 3: [주요 작업 2 - SCP 및 권한 경계 설정]  
1. **"AWS Organizations" 대시보드**에서 **"Organization units"** 또는 **"Root"** 클릭.  
2. **"Create policy"** 버튼을 클릭하고, **"Service Control Policy (SCP)"**를 선택.  
   - **"Policy name"**: `Dev-Team-SCP` 입력.  
   - **"Policy document"**:  
     ```json
     {
       "Version": "2012-10-17",
       "Statement": [
         {
           "Effect": "Deny",
           "Action": "iam:*",
           "Resource": "*"
         }
       ]
     }
     ```  
   - **"Attach policy"** 버튼으로 조직 단위에 정책 연결.  
3. **권한 경계 설정**  
   - **"Create permission boundary"** 버튼 클릭.  
   - **"Boundary name"**: `Dev-Team-Boundary` 입력.  
   - **"Boundary policy"**:  
     ```json
     {
       "Version": "2012-10-17",
       "Statement": [
         {
           "Effect": "Allow",
           "Action": "iam:ListUsers",
           "Resource": "*"
         }
       ]
     }
     ```  
   - **"Attach boundary"** 버튼으로 사용자에게 권한 경계 연결.  

> **⚠️ 주의:**  
> - SCP는 **"Deny"** 효과만 지원하며, **"Allow"** 효과는 제외됩니다.  
> - 권한 경계는 사용자에게 **"iam:ListUsers"**와 같은 특정 권한만 허용합니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인 방법**  
   - **"AWS Organizations" 대시보드**에서 **"Accounts"** 탭으로 이동하여 생성한 계정 확인.  
   - **"IAM" 서비스**에서 사용자 및 역할을 확인하고, **"Permissions"** 탭에서 SCP 및 권한 경계 적용 여부 확인.  
2. **상태 확인 방법**  
   - **"Account status"** 탭에서 계정이 **"Enabled"** 상태인지 확인.  
   - **"Policy status"** 탭에서 SCP와 권한 경계가 정상적으로 연결되었는지 확인.  
3. **정상 동작 테스트 방법**  
   - 생성한 IAM 사용자로 **AWS CLI** 또는 **Console** 접속 시, **"iam:ListUsers"** 권한만 허용되는지 테스트.  
   - **"iam:CreateUser"** 권한이 거부되는지 확인하여 SCP 및 권한 경계 적용 여부 검증.  

> **📸 화면 확인:**  
> - **"IAM"** 서비스에서 사용자 권한 확인.  
> - **"AWS Organizations"** 대시보드에서 SCP 및 권한 경계 적용 여부 확인.  

--- 

### 📌 실습 요약 체크리스트  
- [ ] AWS Organizations 서비스 접속  
- [ ] IAM 사용자 및 역할 생성  
- [ ] SCP 정책 생성 및 연결  
- [ ] 권한 경계 설정 및 연결  
- [ ] 생성된 리소스 확인 및 테스트  
- [ ] 정상 동작 여부 검증

## ⌨️ AWS CLI로 AWS Organizations 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**필수 조건:**  
- `iam:CreateUser`, `iam:AttachRolePolicy`, `organizations:CreatePolicy` 권한이 포함된 IAM 역할이 필요합니다.  
- AWS Organizations 서비스의 `OrganizationsAdministratorAccess` 정책을 부여받은 사용자 권한이 필요합니다.  
- CLI 설정 시 `region`이 `us-east-1` 또는 `eu-west-1`과 같은 기본 리전으로 설정되어 있어야 합니다.

---

### 예제 1: AWS Organizations 리소스 조회
```bash
# 조직 목록 조회
aws organizations list-organizations --query 'Organizations[0].Id' --output text
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 필터링할 JSON 경로 | `'Organizations[0].Id'` |
| `--output` | 출력 형식 | `json`, `table`, `text` |

**예상 출력:**
```
o-1234567890abcdef
```

> **💡 Tip:** `list-organizations`는 조직의 ID와 이름을 반환합니다. `list-accounts`는 구성원 계정 목록을 확인할 수 있습니다.

---

### 예제 2: AWS Organizations 리소스 생성
```bash
# 서비스 제어 정책 (SCP) 생성
aws organizations create-policy \
    --name "ExampleSCP" \
    --policy-content file://scp.json
```

**필수 옵션:**
- `--name`: 정책 이름 (예: `ExampleSCP`)
- `--policy-content`: JSON 형식의 정책 내용 파일 경로 (예: `scp.json`)

**예상 출력:**
```json
{
    "Policy": {
        "PolicyId": "p-1234567890abcdef",
        "Arn": "arn:aws:organizations::123456789012:policy/o-1234567890abcdef/p-1234567890abcdef",
        "Name": "ExampleSCP",
        "Description": "",
        "Tags": [],
        "CreateDate": "2023-10-01T00:00:00Z"
    }
}
```

> **💡 Tip:** `scp.json` 파일에는 정책 내용을 JSON 형식으로 작성해야 합니다. 예:  
```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Deny",
      "Action": "s3:*",
      "Resource": "*"
    }
  ]
}
```

---

### 예제 3: AWS Organizations 리소스 수정
```bash
# 서비스 제어 정책 수정
aws organizations update-policy \
    --policy-id "p-1234567890abcdef" \
    --policy-content file://scp.json
```

**필수 옵션:**
- `--policy-id`: 수정할 정책 ID (예: `p-1234567890abcdef`)
- `--policy-content`: 수정할 JSON 파일 경로 (예: `scp.json`)

**예상 출력:**
```json
{
    "Policy": {
        "PolicyId": "p-1234567890abcdef",
        "Arn": "arn:aws:organizations::123456789012:policy/o-1234567890abcdef/p-1234567890abcdef",
        "Name": "ExampleSCP",
        "Description": "",
        "Tags": [],
        "CreateDate": "2023-10-01T00:00:00Z",
        "UpdateDate": "2023-10-02T00:00:00Z"
    }
}
```

---

### 예제 4: AWS Organizations 리소스 삭제
```bash
# 서비스 제어 정책 삭제
aws organizations delete-policy --policy-id "p-1234567890abcdef"

# 삭제 확인
aws organizations describe-policy --policy-id "p-1234567890abcdef"
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. `describe-policy`를 사용해 삭제 전 상태를 확인하세요.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws organizations list-organizations
aws organizations describe-organization --organizational-unit-id "ou-1234567890abcdef"
aws organizations list-policies

# 생성
aws organizations create-policy --name "ExampleSCP" --policy-content file://scp.json

# 수정
aws organizations update-policy --policy-id "p-1234567890abcdef" --policy-content file://scp.json

# 삭제
aws organizations delete-policy --policy-id "p-1234567890abcdef"
```

---

### 🔒 권한 및 비용 주의사항
- **IAM 권한:** `organizations:CreatePolicy`, `organizations:AttachPolicy` 권한이 필요합니다.  
- **프리티어:** 조직 생성 및 SCP 생성은 무료로 가능합니다.  
- **비용:** 구성원 계정의 리소스 사용량에 따라 요금이 부과됩니다.  
- **모니터링:** `aws organizations get-organization`로 조직 상태를 확인하세요.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 AWS Organizations 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **IAM 역할 위임 및 trust 정책 구성**  
   - 설명: AWS Organizations에서 계정 간 리소스 접근을 제어하기 위해 **trust 정책**이 필수적입니다. 이는 IAM 역할을 다른 계정에서 사용할 수 있도록 허용하는 **trust 관계**를 설정하는 과정으로, **cross-account access** 시나리오에서 반드시 다루는 개념입니다.  
   - 키워드: `trust 정책`, `cross-account access`

2. **STS 서비스로 임시 자격 증명 생성**  
   - 설명: **STS(Security Token Service)**는 임시 AWS 자격 증명을 생성하여 보안을 강화하는 핵심 기술입니다. 이는 IAM 사용자와 서비스 간의 **임시 권한 부여**를 위한 표준 방식으로, 시험에서 **STS 정책 구성**과 **자격 증명 연동** 관련 문제를 자주 다룹니다.  
   - 키워드: `STS`, `임시 자격 증명`

3. **서비스 연결 역할(Service Linked Role) 구성**  
   - 설명: AWS 서비스(예: S3, RDS)가 자동으로 생성하는 **Service Linked Role**은 해당 서비스의 정상 작동을 보장합니다. 이 역할은 **AWS Organizations** 내에서 **서비스 연동** 시 자동 생성되며, 이 설정을 잘못하면 서비스가 작동하지 않을 수 있습니다.  
   - 키워드: `Service Linked Role`, `서비스 연동`

4. **권한 경계(Permission Boundary) 적용**  
   - 설명: **Permission Boundary**는 IAM 사용자의 권한을 제한하는 추가적인 정책으로, AWS Organizations에서 **최소 권한 원칙**을 구현하는 핵심 개념입니다. 이는 보안 위험을 줄이고, **권한 격리**를 위한 필수 사항입니다.  
   - 키워드: `Permission Boundary`, `최소 권한`

5. **SCP(Service Control Policy) 정책 관리**  
   - 설명: **SCP**는 AWS Organizations에서 **모든 계정**에 적용되는 정책으로, 리소스 생성/삭제 권한을 제어합니다. 이는 조직 내 **일관된 보안 정책**을 시행하는 데 필수적이며, 시험에서 **SCP 정책 우선순위**와 **제외 예외** 관련 문제가 자주 출제됩니다.  
   - 키워드: `SCP`, `정책 우선순위`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | SCP는 특정 계정에만 적용되며, 전체 조직에 적용되지 않음 | ❌ 오류. SCP는 **모든 하위 계정**에 자동 적용됨 |
| 함정 2 | Service Linked Role은 사용자가 직접 생성해야 함 | ❌ 오류. 서비스가 자동 생성함 |
| 함정 3 | Trust 정책은 IAM 역할 간만 적용 가능 | ❌ 오류. AWS Organizations 계정 간도 적용 가능 |

#### 🔄 AWS Organizations vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | AWS Organizations | AWS Control Tower | IAM | 선택 기준 |
|----------|----------|-----------|------|----------|
| 용도 | 여러 AWS 계정 통합 관리 | 보안 및 규정 준수 기준 설정 | 개별 사용자/서비스 권한 관리 | **다중 계정 관리 시** |
| 확장성 | 수만 개 계정 관리 가능 | 조직 내 보안 정책 통일 | 개별 사용자 관리 | **규모 확장 시** |
| 비용 | 프리티어 제공, 계정 수에 따라 비용 증가 | 보안 관련 서비스 추가 비용 | 사용자 수에 따라 비용 증가 | **비용 효율성** |
| 지연시간 | 계정 간 통신 최적화 | 정책 적용 지연 시간 있음 | 실시간 권한 처리 | **실시간 처리 필요 시** |

#### 📝 시험 대비 체크리스트
- [ ] AWS Organizations의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  → "다수의 AWS 계정을 통합 관리하고 일관된 보안 정책을 적용하는 서비스"
- [ ] AWS Organizations를 선택해야 하는 시나리오를 알고 있는가?  
  → "다중 계정 환경에서 보안 통일, 리소스 관리, 비용 최적화가 필요한 경우"
- [ ] AWS Organizations의 제한사항/한계를 알고 있는가?  
  → "SCP는 계정 생성 시 자동 적용, Service Linked Role은 서비스에 의존적"
- [ ] AWS Organizations와 AWS Control Tower의 차이점을 설명할 수 있는가?  
  → "Organizations는 계정 관리, Control Tower는 보안 정책 통합"
- [ ] AWS Organizations의 비용 구조를 이해하고 있는가?  
  → "프리티어 제공, 계정 수에 따라 비용 증가, SCP/Trust 정책 추가 비용 없음"

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 AWS Organizations를 떠올리세요:  
> - **trust 정책**  
> - **STS**  
> - **Service Linked Role**  
> - **Permission Boundary**  
> - **SCP**

---

| [⬅️ IAM](./IAM.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 1](../README.md) | [AWS Control Tower ➡️](./AWS-Control-Tower.md) |

---
