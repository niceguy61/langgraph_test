---

| [⬅️ 이전 Day](../day1/README.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 1](../README.md) | [AWS Organizations ➡️](./AWS-Organizations.md) |

---

# IAM 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** IAM은 AWS 리소스에 대한 접근을 관리하기 위한 AWS의 보안 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- 문제 1: 사용자가 모든 AWS 리소스에 접근할 수 있어 보안 위험 증가 (예: 개발자가 프로덕션 DB에 직접 접근)
- 문제 2: 권한 부여 시 정책이 복잡해져 오류 가능성 증가 (예: "모든 리소스 접근" 권한으로 인한 악의적 사용)
- 문제 3: 공유 자격 증명으로 인한 보안 취약점 (예: S3 버킷 공유 시 비밀번호 유출)

**IAM로 해결:**
- 해결 1: 사용자별, 역할별 세분화된 권한 부여로 접근 제어 (예: 개발자에게만 코드 저장소 접근 권한 부여)
- 해결 2: 정책 문서화 및 정책 조합으로 오류 방지 (예: `AmazonS3ReadOnlyAccess` 정책으로 특정 리소스만 접근)
- 해결 3: 임시 자격 증명 생성으로 장기 자격 증명 유출 방지 (예: STS 서비스로 EC2 인스턴스에 임시 자격 증명 전달)

### 비유로 이해하기
IAM은 회사의 사무실 문을 관리하는 보안 요원입니다. 외부 인력은 사무실에 들어가기 위해 사전에 허가를 받고, 특정 부서만 해당 구역 접근이 가능합니다. 개발자는 개발실만, 관리자는 서버실만 접근할 수 있으며, 모든 접근은 로그로 추적됩니다. 이처럼 IAM은 AWS 환경의 접근 제어를 체계적으로 관리합니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 다중 팀이 동일한 AWS 환경을 사용하는 경우 | 기업 내 개발/운영/보안 팀이 공유 리소스를 관리 |
| 시나리오 2 | 서드파티 서비스와 연동할 때 | AWS Lambda에서 S3 버킷에 접근하는 외부 API |
| 시나리오 3 | 규정 준수 및 보안 감사 필요 시 | GDPR, SOC2 등 규제 준수를 위한 권한 분리 |

**이럴 때 IAM을 선택하세요:**
- ✅ 상황 1: 사용자 권한을 세분화해야 할 때
- ✅ 상황 2: 외부 시스템과 AWS 리소스를 안전하게 연결해야 할 때
- ✅ 상황 3: 정책 기반 접근 제어가 필요한 규제 환경에서

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ 상황: 단순한 사용자 생성이 필요할 때 → **AWS Organizations** (유료 서비스, 조직 단위 관리)
- ❌ 상황: 리소스 자체 보안이 필요할 때 → **AWS KMS** (암호화 키 관리)
- ❌ 상황: 동적 자격 증명 생성이 필요할 때 → **AWS STS** (STS는 IAM의 일부 기능)

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **S3** | 버킷 접근 제어 | `IAM User → S3 Bucket` |
| **EC2** | 인스턴스에 임시 자격 증명 부여 | `STS → EC2 Instance` |
| **Lambda** | 함수 실행 시 리소스 접근 제한 | `Lambda Function → IAM Role` |

**자주 사용되는 아키텍처 패턴:**
```
User → CloudFront → S3 (IAM 정책으로 S3 접근 제어)
User → EC2 Instance (STS로 임시 자격 증명 전달)
Lambda Function → DynamoDB (IAM Role로 데이터베이스 접근)
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| 사용자 생성 | $0.00 /월 | 월 100명 무료 |
| 역할 생성 | $0.00 /월 | 무제한 무료 |
| 정책 관리 | $0.00 /월 | 무제한 무료 |
| STS 요청 | $0.00 /만 건 | 12개월 무료 |

**비용 최적화 팁:**
1. 💡 팁 1: **최소 권한 원칙** 적용 (예: S3 읽기 권한 대신 쓰기 권한만 부여)
2. 💡 팁 2: **IAM 정책 조합**으로 중복 정책 방지 (예: `AmazonS3ReadOnlyAccess` 재사용)
3. 💡 팁 3: **AWS Organizations**로 조직 단위 관리로 비용 통합 관리

> **⚠️ 비용 주의:** **STS 요청**은 10만 건/월까지 무료이지만, 대규모 사용 시 추가 비용 발생 가능 (예: 100만 건/월 → $100)

## 📚 핵심 개념

### 개념 1: IAM 역할 위임 및 Trust 정책
IAM 역할 위임은 AWS 서비스 간 권한을 위임하여 안전하게 리소스에 접근할 수 있도록 하는 기능입니다. 이는 특정 서비스(예: EC2, Lambda)가 특정 IAM 역할을 가정하여 작업을 수행하도록 허용하는 것입니다. Trust 정책은 역할이 어떤 서비스에서 호출될 수 있는지 정의하며, 이는 역할을 생성할 때 필수적인 정책입니다. 예를 들어, EC2 인스턴스는 S3 버킷에 접근하려면 EC2에서 생성한 IAM 역할을 가정해야 하며, 이 역할의 Trust 정책은 EC2 서비스를 허용해야 합니다.

#### 왜 중요한가?
- **보안 강화**: 역할을 사용하면 장기 자격 증명을 직접 노출하지 않아 보안 위험을 줄입니다.  
- **자원 제한**: 특정 서비스만 권한을 부여해 비용과 리소스 사용을 최적화할 수 있습니다.  
- **관리 효율성**: 역할을 통해 서비스 간 권한을 중앙 집중식으로 관리해 운영 효율성을 높입니다.

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|------|---|
| **Trust 정책** | 역할을 가정할 수 있는 서비스를 정의합니다. | `AWS service: ec2.amazonaws.com` |
| **Role ARN** | 역할의 고유 식별자로, 서비스가 역할을 참조할 때 사용합니다. | `arn:aws:iam::123456789012:role/MyEC2Role` |
| **External ID** | 역할을 가정할 때 추가 인증을 요구하는 경우 사용합니다. | `externalId: "SecureAccess123"` |

> **💡 Tip:** EC2 인스턴스에서 S3 접근 권한을 부여할 때, EC2 서비스를 Trust 정책에 추가해야 합니다. CLI 명령어는 `aws iam create-role --role-name MyRole --assume-role-policy-document file://trust-policy.json`로 생성할 수 있습니다.

### 개념 2: STS 서비스로 임시 자격 증명 생성
STS(Security Token Service)는 임시 자격 증명(ARN, SecretAccessKey, SessionToken)을 생성하여 AWS 서비스에 제한된 권한으로 접근할 수 있도록 지원합니다. 이는 장기 자격 증명을 노출하지 않고도 외부 시스템(예: 애플리케이션, 클라이언트)이 AWS 리소스에 접근할 수 있도록 합니다. 임시 자격 증명은 시간 제한이 있어 보안 위험을 최소화합니다.

#### 작동 원리
1. **요청**: 외부 시스템이 STS에 임시 자격 증명 생성을 요청합니다.  
2. **인증**: 요청에 포함된 장기 자격 증명(예: IAM 사용자)을 검증합니다.  
3. **발급**: 검증이 성공하면 임시 자격 증명을 생성하고 반환합니다.  

> **💡 Tip:** Lambda 함수에서 S3에 접근할 때 STS를 사용해 임시 자격 증명을 발급하면, Lambda의 IAM 역할을 변경할 필요 없이 접근이 가능합니다. CLI 명령어는 `aws sts get-federationToken --name MyUser --policy file://policy.json`로 사용할 수 있습니다.

### 개념 3: 서비스 연결 역할(Service Linked Role)
서비스 연결 역할은 AWS 서비스가 특정 리소스에 접근할 때 자동으로 생성되는 IAM 역할입니다. 이 역할은 서비스가 리소스를 제어할 수 있도록 권한을 부여합니다. 예를 들어, S3 버킷을 생성할 때 AWS S3 서비스는 자동으로 `AWSServiceRoleForS3` 역할을 생성하여 버킷 접근 권한을 제공합니다. 이 역할은 서비스에만 제한적으로 사용되며, 직접 수정하거나 삭제할 수 없습니다.

#### 주요 특징
1. **자동 생성**: 서비스가 리소스를 사용할 때 자동으로 생성됩니다.  
2. **제한된 범위**: 역할은 특정 서비스만 사용할 수 있도록 권한이 제한됩니다.  
3. **관리 보호**: 사용자가 직접 수정하거나 삭제할 수 없어 보안성이 높습니다.  

> **⚠️ 주의:** 서비스 연결 역할은 AWS 서비스의 내부 운영에 필수적이므로, 삭제하면 해당 서비스가 정상적으로 작동하지 않을 수 있습니다.  

### 개념 4: 권한 경계(Permission Boundary)
권한 경계는 IAM 사용자 또는 역할이 사용할 수 있는 최대 권한을 제한하는 정책입니다. 이는 사용자가 특정 정책을 가진 역할을 가정하더라도, 더 높은 권한을 사용할 수 없도록 설정합니다. 예를 들어, `AdministratorAccess` 정책을 가진 역할을 사용하더라도, 권한 경계를 통해 `AmazonS3FullAccess` 권한만 허용할 수 있습니다.

#### 왜 중요한가?
- **보안 강화**: 사용자가 의도치 않게 고위험 권한을 사용할 수 없도록 제한합니다.  
- **사용자 오류 방지**: 권한을 과도하게 부여해 발생할 수 있는 실수를 방지합니다.  
- **규정 준수**: GDPR, HIPAA 등 규제 기준을 준수하는 데 도움을 줍니다.  

> **💡 Tip:** 권한 경계는 정책 파일로 생성해 IAM 역할에 연결해야 합니다. CLI 명령어는 `aws iam create-role --role-name MyRole --assume-role-policy-document file://trust-policy.json --permission-boundary arn:aws:iam::123456789012:policy/MyBoundaryPolicy`로 설정할 수 있습니다.

### 개념 5: SCP(Service Control Policy)
SCP는 AWS Organizations에서 조직 내 모든 AWS 계정에 적용되는 권한 제한 정책입니다. 이 정책은 특정 서비스, 리소스, 또는 작업을 제한해 조직의 보안 및 운영 정책을 강제합니다. 예를 들어, 모든 계정에서 EC2 인스턴스 생성을 제한하거나, 특정 리전에서만 리소스를 사용하도록 설정할 수 있습니다.

#### 주요 특징
1. **조직 전체 적용**: 모든 계정에 동일한 정책을 적용할 수 있습니다.  
2. **정책 상속**: 하위 조직이나 계정에 정책이 상속되어 적용됩니다.  
3. **사용자 정의**: 특정 서비스, 리소스, 작업을 제한하거나 허용할 수 있습니다.  

> **💡 Tip:** SCP는 정책 파일로 생성해 Organizations 콘솔에서 적용해야 합니다. 예시 정책은 `aws iam create-policy --policy-name MySCP --policy-document file://scp-policy.json`로 생성할 수 있습니다.

## 🖥️ AWS 콘솔에서 IAM 사용하기

### Step 1: IAM 서비스 접속  
1. AWS Management Console에 로그인합니다.  
   - URL: https://console.aws.amazon.com  
2. 상단 검색창에서 **"IAM"**을 입력하고 검색합니다.  
3. 검색 결과에서 **"IAM"**을 클릭하여 서비스에 접근합니다.  

> **📸 화면 확인:** IAM 대시보드가 표시되면 정상입니다. 대시보드에는 사용자, 역할, 정책 등이 요약되어 보여집니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **사용자 생성**  
   - 좌측 메뉴에서 **"Users"** → **"Add user"** 클릭  
   - 사용자 이름: `example-user` 입력  
   - 권한: **"No permissions"** 선택 (초보자 권장)  
   - **"Create user"** 버튼 클릭  
   - 생성 후 **"Security credentials"** 탭에서 액세스 키 생성 (필요 시)  

2. **역할 생성**  
   - 좌측 메뉴에서 **"Roles"** → **"Create role"** 클릭  
   - 서비스 타입: **"AWS service"** → **"EC2"** 선택  
   - 역할 이름: `example-role` 입력  
   - **"Create role"** 클릭  

3. **정책 연결**  
   - 생성된 역할을 클릭 후 **"Permissions"** 탭에서 **"Attach policies"** 클릭  
   - **"AmazonEC2FullAccess"** 정책 선택 후 **"Attach"**  

> **📸 화면 확인:** 사용자 및 역할이 목록에 추가되고, 정책이 연결되었는지 확인합니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **정책 제한 (SCP)**  
   - 좌측 메뉴에서 **"Policies"** → **"Create policy"** 클릭  
   - 정책 이름: `example-scp`  
   - JSON 편집기에서 다음 정책 추가:  
     ```json
     {
       "Version": "2012-10-17",
       "Statement": [
         {
           "Effect": "Deny",
           "Action": "s3:*",
           "Resource": "*"
         }
       ]
     }
     ```  
   - **"Create policy"** 클릭  

2. **권한 경계 설정**  
   - 좌측 메뉴에서 **"Permissions boundaries"** → **"Create permission boundary"** 클릭  
   - 정책: `example-scp` 선택  
   - **"Create"** 클릭  

3. **STS 연동 설정**  
   - 좌측 메뉴에서 **"Roles"** → `example-role` 클릭  
   - **"Trust policy"**에서 **"sts.amazonaws.com"** 추가  
   - **"Permissions"** 탭에서 **"Attach policies"**로 **"sts:GetFederationToken"** 추가  

> **⚠️ 주의:** 정책을 연결할 때는 **최소 권한 원칙**을 지켜야 합니다. 불필요한 권한은 추가하지 마세요.  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인**  
   - **"Users"** 탭에서 `example-user`가 존재하는지 확인  
   - **"Roles"** 탭에서 `example-role`이 존재하고, 정책이 연결되었는지 확인  

2. **STS 테스트**  
   - CLI로 테스트:  
     ```bash
     aws sts get-federationToken \
       --name example-user \
       --policy "{'Version': '2012-10-17', 'Statement': [{'Effect': 'Allow', 'Action': 'sts:GetFederationToken', 'Resource': '*'}]}"
     ```  
   - 성공 시 `Credentials` 필드가 반환됩니다.  

3. **권한 경계 테스트**  
   - `example-role`로 EC2 인스턴스 생성 시 S3 접근 권한이 거부되는지 확인  
   - **AWS CLI**로 S3 버킷 접근 시 오류 발생 여부 확인  

> **💡 Tip:** AWS 프리티어는 1년간 $125까지 사용 가능합니다. 사용량을 체크하며 비용을 절약하세요.  

---

### 📌 학습 체크리스트  
- [ ] IAM 대시보드에 접근  
- [ ] 사용자 및 역할 생성 완료  
- [ ] 정책 연결 및 권한 경계 설정  
- [ ] STS 연동 테스트  
- [ ] 모든 리소스 정상 작동 확인  

> **📸 화면 확인:** 모든 단계에서 생성된 리소스가 정상적으로 작동하는지 확인하세요.

## ⌨️ AWS CLI로 IAM 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**설명:**  
AWS CLI를 사용하기 전에 버전을 확인해 최신 버전인지 확인하세요. `sts get-caller-identity` 명령어는 현재 사용자의 AWS 계정 ID, 유저 이름, 리전을 출력합니다. 리전 설정은 `aws configure get region`으로 확인할 수 있습니다.  
> **💡 Tip:** CLI를 처음 사용하는 경우 `aws configure` 명령어로 액세스 키, 비밀번호, 리전을 설정해야 합니다.

---

### 예제 1: IAM 사용자 조회
```bash
# IAM 사용자 목록 조회
aws iam list-users --query 'Users[*].UserName' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| --query | 결과 필터링 | 'Users[*].UserName' |
| --output | 출력 형식 | table, json, text |

**예상 출력:**
```
| UserName   |
|------------|
| example-user |
```

**설명:**  
`list-users` 명령어는 IAM 사용자 목록을 조회합니다. `--query` 옵션으로 특정 필드만 출력하고, `--output table`으로 테이블 형식으로 결과를 보여줍니다.  
> **⚠️ 주의:** `--query`는 JSON 데이터를 필터링하는 기능으로, 복잡한 조건을 사용할 수 있습니다.

---

### 예제 2: IAM 사용자 생성
```bash
# IAM 사용자 생성
aws iam create-user --user-name "example-user"
```

**필수 옵션:**
- `--user-name`: 사용자 이름 (예: "example-user")

**예상 출력:**
```json
{
    "User": {
        "UserName": "example-user",
        "UserId": "AIDAIEXAMPLE1234567890",
        "Arn": "arn:aws:iam::123456789012:user/example-user"
    }
}
```

**설명:**  
`create-user` 명령어는 IAM 사용자를 생성합니다. 생성된 사용자의 ARN(亚马존 리소스 이름)은 권한 부여 시 필수입니다.  
> **💡 Tip:** 사용자 생성 후 `get-user` 명령어로 생성된 사용자 정보를 확인할 수 있습니다.

---

### 예제 3: IAM 사용자 권한 설정
```bash
# IAM 사용자 정책 부여
aws iam attach-user-policy \
    --user-name "example-user" \
    --policy-arn "arn:aws:iam::123456789012:policy/LeastPrivilegePolicy"
```

**설명:**  
`attach-user-policy` 명령어는 사용자에게 정책을 부여합니다. `--policy-arn`은 최소 권한 정책(Least Privilege Policy)의 ARN을 입력합니다.  
> **⚠️ 주의:** 정책을 부여할 때는 `iam:AttachUserPolicy` 권한이 필요합니다.

---

### 예제 4: IAM 사용자 삭제
```bash
# IAM 사용자 삭제
aws iam delete-user --user-name "example-user"

# 삭제 확인
aws iam get-user --user-name "example-user"
```

**예상 출력:**
```
An error occurred (NoSuchEntity) when calling the GetUser operation: The user 'example-user' does not exist.
```

**설명:**  
`delete-user` 명령어는 사용자를 영구적으로 삭제합니다. 삭제 후 `get-user` 명령어로 확인하면 오류가 발생합니다.  
> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. 사전에 `--force` 옵션 없이 실행할 수 있습니다.

---

### 자주 사용하는 명령어 정리
```bash
# 사용자 관리
aws iam list-users
aws iam create-user --user-name "name"
aws iam delete-user --user-name "name"

# 정책 관리
aws iam list-policies
aws iam attach-user-policy --user-name "name" --policy-arn "arn"
aws iam detach-user-policy --user-name "name" --policy-arn "arn"
```

**설명:**  
위 명령어는 사용자 생성, 정책 부여/해제, 사용자 삭제 등 기본적인 IAM 관리 작업을 수행합니다.  
> **💡 Tip:** 정책을 적용할 때는 `aws iam get-policy` 명령어로 정책 내용을 확인할 수 있습니다.

---

### CLI 사용 팁
- **비용 최적화:** `aws iam list-users`로 사용자 목록을 확인해 불필요한 사용자를 제거하세요.  
- **보안 강화:** `Least Privilege` 원칙을 적용해 사용자에게 필요한 권한만 부여합니다.  
- **자동화:** `aws iam` 명령어를 스크립트에 포함해 리소스 관리를 자동화할 수 있습니다.  

> **⚠️ 주의:** CLI 명령어는 권한이 필요한 작업을 수행하므로, `aws iam get-user`로 권한 확인 후 실행하세요.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 IAM 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **IAM 역할 위임 및 Trust 정책 구성**  
   - 설명: AWS 서비스 간 통신 시 IAM 역할을 사용하여 자격 증명을 관리해야 하며, Trust 정책은 역할을 사용할 수 있는 서비스를 명시합니다. 이는 보안 및 권한 분할을 위한 핵심 개념으로, 시험에서 자주 다중 서비스 통신 시 권한 부여 방식을 묻는 문제에 등장합니다.  
   - 키워드: `Trust 정책`, `역할 위임`, `서비스 간 통신`

2. **STS 서비스로 임시 자격 증명 생성**  
   - 설명: AWS STS(Security Token Service)를 사용해 임시 자격 증명을 생성하는 것은 임시 권한 부여를 위한 표준 방식입니다. 이는 단기 사용자 또는 애플리케이션에 최소 권한을 부여하는 데 필수적입니다. 시험에서는 IAM 역할을 STS로 전달하는 과정을 묻는 경우가 많습니다.  
   - 키워드: `STS`, `임시 자격 증명`, `AWS federation`

3. **서비스 연결 역할(Service Linked Role) 구성**  
   - 설명: 특정 AWS 서비스(예: S3, DynamoDB)와 연동할 때 자동으로 생성되는 역할로, 서비스 간 통신을 안전하게 관리합니다. 이 역할은 AWS 관리 정책으로 생성되며, 사용자가 직접 수정할 수 없어 시험에서 주의해야 하는 함정이 됩니다.  
   - 키워드: `Service Linked Role`, `자동 생성`, `서비스 연동`

4. **권한 경계(Permission Boundary) 적용**  
   - 설명: IAM 정책의 권한 범위를 제한하는 추가 레이어로, 사용자가 특정 권한을 수행할 수 있는 범위를 설정합니다. 이는 최소 권한 원칙을 강화하는 핵심 개념으로, 시험에서는 정책과 경계의 차이를 묻는 경우가 많습니다.  
   - 키워드: `Permission Boundary`, `최소 권한`, `정책 제한`

5. **SCP(Service Control Policy) 정책 관리**  
   - 설명: AWS Organizations 내에서 AWS 계정의 권한을 제어하는 정책으로, 단일 계정에서의 IAM 정책보다 더 강력한 제어를 제공합니다. 이는 조직 내 보안 정책을 통합적으로 관리하는 시나리오에 필수적입니다.  
   - 키워드: `SCP`, `AWS Organizations`, `조직 내 보안`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | Trust 정책에서 `Principal` 필드를 `arn:aws:iam::123456789012:root`로 설정하면 자동으로 모든 서비스에 접근 가능하다고 오인 | `Principal` 필드는 특정 서비스(예: `ec2.amazonaws.com`)를 명시해야 하며, `root`는 권한 부여 대상이 아닙니다 |
| 함정 2 | 서비스 연결 역할이 사용자 직접 생성 가능하다고 오인 | 서비스 연결 역할은 AWS 서비스가 자동으로 생성하며, 사용자가 직접 수정하거나 삭제할 수 없습니다 |
| 함정 3 | 권한 경계는 정책보다 우선순위가 낮다고 오인 | 권한 경계는 정책보다 우선순위가 높아, 사용자의 권한 범위를 제한합니다 |

#### 🔄 IAM vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | IAM | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | IAM 사용자/역할로 리소스 접근 권한 관리 | AWS Organizations: 조직 내 계정 관리, SCP 적용 | 리소스 접근 제어 시 IAM, 조직 구조 관리 시 Organizations |
| 확장성 | 단일 AWS 계정에서 사용 가능 | AWS Organizations: 여러 AWS 계정을 관리 가능 | 조직 내 통합 보안 정책이 필요한 경우 Organizations |
| 비용 | 사용자/역할 생성 시 기본 비용 발생 | SCP 적용 시 추가 비용 없음 | 단일 계정 관리 시 IAM, 조직 통합 관리 시 Organizations |
| 지연시간 | 실시간 권한 검증 | SCP 적용 시 통합 검증 필요 | 실시간 권한이 필요한 경우 IAM, 조직 전체 검증이 필요한 경우 Organizations |

#### 📝 시험 대비 체크리스트
- [ ] IAM의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  → "AWS 리소스에 대한 접근 권한을 관리하고 보안을 강화합니다."
- [ ] IAM를 선택해야 하는 시나리오를 알고 있는가?  
  → "단일 AWS 계정에서 특정 사용자/역할이 리소스에 접근할 수 있도록 제어할 때"
- [ ] IAM의 제한사항/한계를 알고 있는가?  
  → "단일 AWS 계정만 관리 가능, 서비스 연결 역할은 사용자 직접 수정 불가"
- [ ] IAM과 AWS Organizations의 차이점을 설명할 수 있는가?  
  → "IAM은 리소스 접근 제어, Organizations는 조직 내 계정 관리"
- [ ] IAM의 비용 구조를 이해하고 있는가?  
  → "사용자/역할 생성 시 기본 비용, SCP는 추가 비용 없음"

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 IAM을 떠올리세요:  
> - **Trust 정책**  
> - **임시 자격 증명**  
> - **서비스 연결 역할**  
> - **권한 경계**  
> - **SCP**  

> **CLI 예제:**  
> ```bash
> # IAM 역할 생성 (CLI)
> aws iam create-role --role-name MyRole --assume-role-policy-document file://trust-policy.json
> 
> # STS 임시 자격 증명 생성
> aws sts assume-role --role-arn arn:aws:iam::123456789012:role/MyRole --role-session-name MySession
> ```

---

| [⬅️ 이전 Day](../day1/README.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 1](../README.md) | [AWS Organizations ➡️](./AWS-Organizations.md) |

---
