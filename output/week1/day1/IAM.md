---

| [⬅️ AWS Global Infrastructure](./AWS-Global-Infrastructure.md) | [📑 Day 1 목차](./README.md) | [🏠 Week 1](../README.md) | [다음 Day ➡️](../day2/README.md) |

---

# IAM 완전 정복

> **한 줄 정의:** IAM은 **AWS 리소스에 대한 접근 권한을 관리**하고 **보안을 강화**하기 위한 AWS의 **핵심 인증 및 권한 관리 서비스**입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**  
- **문제 1:** 다수의 사용자가 동일한 AWS 계정을 공유할 때 비밀번호를 공유해 안전성이 저하됩니다. 예를 들어, 팀원 간 AWS CLI 명령어를 실행할 때 계정 비밀번호를 직접 입력해야 하며, 이로 인해 해킹 위험이 증가합니다.  
- **문제 2:** 특정 리소스에 대한 접근 권한을 세밀하게 제어하지 못해, 예를 들어 개발자가 프로덕션 환경의 S3 버킷에 접근할 수 없도록 설정하지 못해 보안 취약점이 발생합니다.  
- **문제 3:** 접근 활동을 추적하고 감사할 수 없어, 불법적인 접근이나 오류를 조기에 탐지하기 어렵습니다.  

**IAM으로 해결:**  
- **해결 1:** 사용자, 역할, 정책을 통해 역할 기반 접근 제어(RBAC)를 구현해, 개발자가 특정 리소스에 접근할 수 있는 권한을 명확히 설정합니다.  
- **해결 2:** MFA(Multi-Factor Authentication)를 통해 이중 인증을 적용해, 비밀번호 유출 시에도 보안을 유지합니다.  
- **해결 3:** AWS CloudTrail과 통합해 모든 접근 활동을 로그로 기록해, 이상 행동을 실시간으로 감지하고 분석할 수 있습니다.  

### 비유로 이해하기  
IAM은 집의 문을 관리하는 **키 관리 시스템**을 생각해보세요. 각 가족 구성원에게 고유한 키를 발급하고, 각 문에 접근 권한을 설정합니다. 예를 들어, 어린이용 문은 부모만 열 수 있도록 설정해, 안전성을 확보합니다. 이처럼 IAM은 AWS 리소스에 대한 접근 권한을 세밀하게 제어해, 보안을 강화합니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 개발자 팀이 AWS 리소스에 접근할 수 있도록 역할을 생성하고 정책을 설정 | 기업 A는 DevOps 팀에 EC2 인스턴스에 접근할 수 있는 역할을 부여해 CI/CD 파이프라인을 구축 |
| 시나리오 2 | 특정 부서만 특정 S3 버킷에 접근할 수 있도록 제한 | 기업 B는 마케팅 팀만 고객 데이터를 저장한 S3 버킷에 접근할 수 있도록 정책을 설정 |
| 시나리오 3 | 외부 파트너에게 AWS 서비스 접근 권한을 부여 | 기업 C는 외부 개발자에게 Lambda 함수에 접근할 수 있는 임시 역할을 제공해 서비스를 운영 |

**이럴 때 IAM을 선택하세요:**  
- ✅ 상황 1: 여러 사용자가 AWS 리소스에 접근해야 할 때  
- ✅ 상황 2: 리소스에 대한 접근 권한을 세밀하게 제어해야 할 때  
- ✅ 상황 3: 외부 파트너나 서비스 간 인증/권한을 관리해야 할 때  

**이럴 때는 다른 서비스를 고려하세요:**  
- ❌ 상황: 단일 사용자에 대한 접근 권한만 필요하면 → **AWS IAM 사용자 생성**  
- ❌ 상황: 자동화된 서비스 간 인증이 필요하면 → **AWS STS(Security Token Service)**  
- ❌ 상황: 리소스에 대한 접근 활동을 모니터링해야 할 때 → **AWS CloudTrail + AWS CloudWatch**  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **AWS S3** | S3 버킷에 접근하는 사용자의 권한을 정책으로 제어 | IAM 정책 → S3 버킷 정책 → 데이터 액세스 |
| **AWS EC2** | EC2 인스턴스가 다른 AWS 서비스에 접근할 수 있도록 역할을 부여 | EC2 역할 → IAM 정책 → 서비스 간 통신 |
| **AWS Lambda** | Lambda 함수가 S3 또는 DynamoDB에 접근할 수 있도록 역할을 설정 | Lambda 역할 → IAM 정책 → 외부 리소스 접근 |

**자주 사용되는 아키텍처 패턴:**  
```
[사용자] → [IAM 역할] → [EC2 인스턴스] → [S3 버킷]
```
이 패턴은 EC2 인스턴스가 S3에 접근할 수 있도록 IAM 역할을 통해 권한을 부여합니다.  

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **IAM 사용자 생성 및 관리** | $0.00 (AWS 무료 테스트) | 12개월 무료 |
| **IAM 정책 생성 및 관리** | $0.00 (AWS 무료 테스트) | 12개월 무료 |
| **MFA 장치 사용** | $0.00 (AWS 무료 테스트) | 12개월 무료 |

**비용 최적화 팁:**  
1. 💡 팁 1: **관리 정책(Managed Policies)**을 사용해 반복적인 정책 작성을 방지하고, AWS가 업데이트된 보안 정책을 자동으로 적용합니다.  
2. 💡 팁 2: **IAM 역할** 대신 사용자를 생성해, EC2 인스턴스나 Lambda 함수 등 자동화된 서비스에 접근 권한을 부여합니다.  
3. 💡 팁 3: **AWS Organizations**를 사용해 기업 내 AWS 계정을 관리해, IAM 정책을 중앙 집중식으로 설정하고 비용을 통제합니다.  

> **⚠️ 비용 주의:** IAM 자체는 무료이지만, IAM을 사용하는 리소스(예: S3, EC2)에 대한 요금이 발생할 수 있습니다. 예를 들어, IAM 역할을 사용해 EC2 인스턴스가 S3에 접근할 때, S3의 데이터 전송 요금이 부과될 수 있습니다.

## 📚 핵심 개념

### 개념 1: AWS 리전, 가용 영역, 엣지 로케이션  
AWS 리전은 전 세계에 위치한 대규모 데이터 센터 그룹으로, 각 리전 내에는 가용 영역(Availability Zone)이 포함됩니다. 리전은 데이터 저장, 서비스 제공, 네트워크 지연 최소화 등을 위해 선택되며, 엣지 로케이션은 CDN을 통해 글로벌 사용자에게 근접한 콘텐츠를 제공합니다. 이 구조는 장애 발생 시 복구 속도를 높이고, 데이터 전송 비용을 줄이는 데 중요합니다.  

#### 왜 중요한가?  
- **장애 회복력**: 가용 영역 간 복제를 통해 단일 지점 실패를 방지합니다.  
- **성능 최적화**: 사용자 지역에 가까운 리전 선택으로 네트워크 지연을 줄입니다.  
- **데이터 전송 비용**: 엣지 로케이션을 활용해 데이터 전송 비용을 절감할 수 있습니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| 리전 | 전 세계에 분산된 데이터 센터 그룹 | us-east-1 (N. Virginia), ap-northeast-1 (Tokyo) |
| 가용 영역 | 리전 내 단일 데이터 센터 | us-east-1a, us-east-1b |
| 엣지 로케이션 | 글로벌 사용자에게 근접한 CDN 노드 | AWS Global Accelerator, CloudFront |

> **💡 Tip:** 리전 선택 시 데이터 법규(예: GDPR)와 지연 시간을 고려하세요.  
> **⚠️ 주의:** 리전 간 데이터 이동은 추가 비용이 발생할 수 있습니다.  

---

### 개념 2: IAM 사용자 vs. 역할  
IAM 사용자는 AWS 계정 내 특정 자원에 접근할 수 있는 인증 정보를 가진 개체입니다. 역할은 EC2 인스턴스나 다른 AWS 서비스에 권한을 부여하는 것입니다. 사용자와 역할의 차이는 **인증 주체**에 있습니다. 사용자는 AWS CLI나 웹 콘솔에서 직접 인증을 수행하지만, 역할은 자동 인증을 통해 서비스 간 권한을 전달합니다.  

#### 작동 원리  
1. **사용자 생성**: `aws iam create-user` 명령어로 사용자를 만듭니다.  
2. **역할 생성**: `aws iam create-role` 명령어로 역할을 생성하고, `sts assume-role`을 통해 자동 인증을 수행합니다.  
3. **정책 부여**: 사용자 또는 역할에 `iam:AttachPolicy`를 통해 권한을 할당합니다.  

> **💡 Tip:** EC2 인스턴스는 역할을 통해 S3에 접근할 수 있도록 설정하세요.  

---

### 개념 3: IAM 정책  
IAM 정책은 리소스에 대한 권한을 정의하는 JSON 문서입니다. **관리 정책**은 AWS가 제공하는 공통 정책(예: `AmazonS3ReadOnlyAccess`)이고, **인라인 정책**은 사용자가 직접 작성한 정책입니다. 정책은 **주체**(사용자, 역할, 그룹)와 **권한**을 연결해 보안을 강화합니다.  

#### 주요 특징  
1. **관리 정책**: 공통된 권한을 빠르게 적용할 수 있습니다.  
2. **인라인 정책**: 특정 주체에만 제한적으로 적용 가능하며, 정책 수정 시 주체를 다시 설정해야 합니다.  
3. **정책 조합**: 여러 정책을 결합해 세밀한 권한을 설정할 수 있습니다.  

> **💡 Tip:** 정책을 수정할 때는 `iam:PutPolicy`를 사용해 업데이트하세요.  

---

### 개념 4: Multi-Factor Authentication (MFA)  
MFA는 두 가지 인증 요소(예: 비밀번호 + 휴대폰 앱 인증 코드)를 요구해 계정 보안을 강화합니다. AWS에서는 **Hardware MFA** 또는 **Virtual MFA**를 지원하며, MFA 활성화는 AWS Organizations에서 관리할 수 있습니다.  

#### 왜 중요한가?  
- **보안 강화**: 해킹 시 계정 접근이 어려워집니다.  
- **규정 준수**: GDPR, HIPAA 등 규정에 따라 MFA 사용이 필수적일 수 있습니다.  

> **💡 Tip:** CLI로 MFA 활성화는 `aws iam enable-mfa-user` 명령어로 수행할 수 있습니다.  

---

### 개념 5: AWS Organizations  
AWS Organizations는 여러 AWS 계정을 관리하는 서비스로, **센터드 컨트롤**과 **비용 관리**를 가능하게 합니다. IAM 정책을 통해 조직 내 계정에 권한을 일관되게 적용할 수 있으며, **Service Control Policies**로 서비스 접근을 제한할 수 있습니다.  

#### 주요 특징  
1. **계정 관리**: 조직 내 계정 생성, 삭제, 권한 부여를 통합 관리합니다.  
2. **비용 통합**: 모든 계정의 사용량을 한눈에 확인할 수 있습니다.  
3. **보안 통일**: IAM 정책을 통해 조직 내 모든 계정에 일관된 보안 정책을 적용합니다.  

> **💡 Tip:** AWS Organizations는 프리티어로 10개 계정까지 무료로 사용 가능합니다.  

--- 

### ✅ 실습 예제 (CLI)  
```bash
# IAM 사용자 생성
aws iam create-user --user-name MyUser

# IAM 역할 생성 및 정책 부여
aws iam create-role --role-name MyRole
aws iam attach-role-policy --role-name MyRole --policy-arn arn:aws:iam::123456789012:policy/MyPolicy

# MFA 활성화
aws iam enable-mfa-user --user-name MyUser --serial-number arn:aws:iam::123456789012:mfa/MyMFA
``` 

> **⚠️ 주의:** 정책을 수정 시 권한이 제거된 사용자는 작업을 수행할 수 없습니다.

## 🖥️ AWS 콘솔에서 IAM 사용하기

### Step 1: IAM 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: [https://console.aws.amazon.com](https://console.aws.amazon.com)  
   - **AWS 계정 ID**와 **IAM 사용자 권한**이 있는 계정으로 로그인해야 합니다.  
2. 상단 검색창에서 `"IAM"`을 입력 후 검색 결과에서 **IAM 서비스**를 클릭합니다.  
   - **IAM 대시보드**는 AWS 리소스 관리의 핵심 중앙 랩입니다.  

> **📸 화면 확인:** IAM 대시보드가 표시되면 정상입니다. 왼쪽 메뉴에서 **Users**, **Groups**, **Roles**, **Policies** 등의 항목이 보여야 합니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **IAM 사용자 생성**  
   - 왼쪽 메뉴에서 **Users** → **Add user** 클릭  
   - **User name** 입력 (예: `dev-iam-user`)  
   - **Access type**에서 **Programmatic access** 선택  
   - **Next: Permissions** → **Attach existing policies directly** 선택  
   - `AmazonS3FullAccess` 및 `AmazonEC2FullAccess` 정책 체크  
   - **Create user** 클릭  

2. **IAM 그룹 생성 및 사용자 추가**  
   - **Groups** → **Create group** 클릭  
   - **Group name** 입력 (예: `DevGroup`)  
   - **Attach existing policies directly** → `AmazonEC2ReadOnlyAccess` 정책 체크  
   - **Create group** 클릭  
   - **Users** → **Add user to group** → `DevGroup` 선택  

3. **IAM 역할 생성**  
   - **Roles** → **Create role** 클릭  
   - **Trusted entity**에서 **AWS service** → **EC2** 선택  
   - **Permissions** → `AmazonEC2FullAccess` 체크  
   - **Create role** 클릭  
   - 생성된 역할 이름 확인 (예: `DevRole`)  

> **📸 화면 확인:**  
   - **Users** 목록에 `dev-iam-user`가 생성된 상태  
   - **Groups** 목록에 `DevGroup`이 생성된 상태  
   - **Roles** 목록에 `DevRole`이 생성된 상태  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **IAM 정책 정의 및 할당**  
   - **Policies** → **Create policy** 클릭  
   - **JSON** 탭에서 다음 정책 입력 (S3 리소스 접근 제한):  
     ```json
     {
       "Version": "2012-10-17",
       "Statement": [
         {
           "Effect": "Allow",
           "Action": "s3:GetObject",
           "Resource": "arn:aws:s3:::example-bucket/*"
         }
       ]
     }
     ```  
   - **Policy name** 입력 (예: `S3GetObjectPolicy`) → **Create policy** 클릭  
   - **Users** → `dev-iam-user` 선택 → **Permissions** → **Add permissions** → 정책 할당  

2. **MFA 활성화**  
   - **Users** → `dev-iam-user` 선택 → **Security credentials** 탭  
   - **Enable MFA** → **Verify your MFA device**  
   - **Authentication code** 입력 → **Set MFA** 클릭  
   - **MFA type**에서 **Virtual MFA device** 선택 → **Next** → **Activate**  

3. **AWS Organizations 설정**  
   - **Organizations** → **Create organization** 클릭  
   - **Organization name** 입력 → **Create**  
   - **Create account** → **Create account** 클릭 → **Account name** 입력  
   - 생성된 계정 확인 → **Attach policies**로 IAM 정책 연결  

> **⚠️ 주의:** MFA를 활성화할 때는 항상 **AWS CLI 또는 SDK**를 사용하는 것이 안전합니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인 방법**  
   - **Users** → `dev-iam-user` 클릭 → **Security credentials** 탭 → MFA 상태 및 액세스 키 확인  
   - **Groups** → `DevGroup` 클릭 → **Permissions** 탭 → 할당된 정책 확인  
   - **Roles** → `DevRole` 클릭 → **Trust relationship** 및 권한 검토  

2. **상태 확인 방법**  
   - **Policies** → `S3GetObjectPolicy` 클릭 → **Policy document** 검토  
   - **Organizations** → 생성된 계정 목록 확인 → **Account ID** 및 상태 확인  

3. **정상 동작 테스트 방법**  
   - **AWS CLI**로 S3 버킷 접근 테스트:  
     ```bash
     aws s3 ls example-bucket --region us-east-1
     ```  
   - MFA 인증 테스트:  
     ```bash
     aws sts get-caller-identity --region us-east-1
     ```  
     - MFA 인증이 필요한 경우, `--serial-number` 및 `--authentication-code` 옵션 추가  

---

### 📌 핵심 팁  
- **AWS CLI 사용 권장:**  
  ```bash
  aws iam get-user
  aws iam list-groups
  aws iam list-roles
  ```  
- **비용 절약:**  
  - **Free Tier** 활용: IAM 서비스는 기본적으로 무료  
  - 정책 권한을 최소 권한 원칙에 따라 제한  
- **보안 강화:**  
  - 모든 사용자에 MFA 활성화  
  - 정책 권한을 정기적으로 검토  

### ✅ 단계별 확인 체크리스트  
1. IAM 대시보드에 접근 가능한지 확인  
2. 사용자, 그룹, 역할이 생성되었는지 확인  
3. 정책이 할당되었는지 확인  
4. MFA가 활성화되었는지 확인  
5. Organizations 계정이 생성되었는지 확인  
6. CLI로 테스트 실행 및 결과 확인  

> **💡 Tip:** IAM 설정은 AWS 리소스 접근 권한을 제어하는 핵심입니다. 실무에서는 **Least Privilege Principle**을 항상 지켜야 합니다.

## ⌨️ AWS CLI로 IAM 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**설명:**  
AWS CLI를 사용하기 전에는 CLI 버전을 확인하고, 현재 사용 중인 AWS 자격 증명(예: IAM 사용자)을 확인해야 합니다. `sts get-caller-identity` 명령어는 현재 로그인한 사용자의 ARN, 계정 ID, 역할 정보를 반환합니다. 리전은 `configure get region`으로 확인할 수 있으며, 이는 AWS 서비스의 위치를 결정합니다.

---

### 예제 1: IAM 사용자 목록 조회
```bash
# IAM 사용자 목록 조회
aws iam list-users --query 'Users[*].UserName' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 결과 필터링 | `'Users[*].UserName'` |
| `--output` | 출력 형식 | `json`, `table`, `text` |

**예상 출력:**
```
| UserName |
|----------|
| example-user |
| admin    |
```

**설명:**  
`list-users` 명령어는 현재 AWS 계정에 등록된 IAM 사용자를 목록화합니다. `--query` 옵션을 통해 특정 필드(예: 사용자 이름)만 추출하고, `--output table`은 테이블 형식으로 결과를 보여줍니다.

---

### 예제 2: IAM 사용자 생성
```bash
# IAM 사용자 생성
aws iam create-user --user-name "example-user"
```

**필수 옵션:**
- `--user-name`: 생성할 사용자 이름 (예: "example-user")

**예상 출력:**
```json
{
    "User": {
        "UserName": "example-user",
        "CreateDate": "2023-10-05T00:00:00Z",
        "UserId": "AIDAJDQAAAAAAAAAAA"
    }
}
```

**설명:**  
`create-user` 명령어는 새로운 IAM 사용자를 생성합니다. 생성된 사용자의 `UserId`와 `CreateDate`를 확인할 수 있으며, 이 사용자는 이후 정책과 역할을 할당할 수 있습니다.

---

### 예제 3: IAM 사용자 수정
```bash
# IAM 사용자 비밀번호 설정 (Password Policy 적용)
aws iam update-login-profile --user-name "example-user" --password "SecureP@ss123"
```

**필수 옵션:**
- `--user-name`: 수정할 사용자 이름
- `--password`: 사용자 비밀번호 (AWS Password Policy에 따라 설정)

**예상 출력:**
```json
{
    "LoginProfile": {
        "UserName": "example-user",
        "PasswordLastUsed": "2023-10-05T00:00:00Z"
    }
}
```

**설명:**  
`update-login-profile` 명령어는 사용자 비밀번호를 설정하거나 업데이트합니다. 이 명령어는 MFA 활성화가 필수인 경우 `--password` 옵션을 사용할 수 없습니다. 비밀번호 정책을 준수해야 하며, 이는 AWS 관리 정책(AWS Managed Policy)에서 설정됩니다.

---

### 예제 4: IAM 사용자 삭제
```bash
# IAM 사용자 삭제
aws iam delete-user --user-name "example-user"

# 삭제 확인
aws iam get-user --user-name "example-user"
```

> **⚠️ 주의:** 삭제된 사용자는 복구할 수 없습니다. 삭제 후 `get-user` 명령어는 오류를 반환합니다.

**설명:**  
`delete-user` 명령어는 사용자를 영구적으로 삭제합니다. 삭제 후에는 해당 사용자의 권한이 즉시 제거되며, 복구가 불가능합니다. 삭제 전에 관련된 정책이나 역할을 확인해야 합니다.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws iam list-users
aws iam get-user --user-name "example-user"
aws iam list-groups --query 'Groups[*].GroupName' --output table

# 생성
aws iam create-user --user-name "new-user"
aws iam create-group --group-name "developers"

# 수정
aws iam attach-group-policy --group-name "developers" --policy-arn "arn:aws:iam::123456789012:policy/ExamplePolicy"
aws iam update-login-profile --user-name "admin" --password "SecureP@ss123"

# 삭제
aws iam delete-user --user-name "new-user"
aws iam delete-group --group-name "developers"
```

**설명:**  
IAM CLI 명령어는 사용자, 그룹, 정책 등을 관리하는 데 필수적입니다. `list-*`, `get-*`, `create-*`, `update-*`, `delete-*` 형식의 명령어를 통해 자동화된 관리를 수행할 수 있습니다. 특히, 정책 부착(`attach-group-policy`)은 권한 할당에 중요합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 IAM 포인트

#### 📌 핵심 출제 포인트 TOP 5  
1. **IAM 사용자 vs. 역할 (Users vs. Roles)**  
   - 설명: AWS에서 리소스에 접근하는 주체를 구분하는 핵심 개념. 사용자는 직접 리소스에 접근하지만, 역할은 EC2 인스턴스나 서비스 간 권한 위임에 사용됨. AWS 서비스 간 통신 시 역할이 필수적임.  
   - 키워드: `역할`, `사용자`, `서비스 통신`, `리소스 접근`

2. **정책 우선순위 (Inline vs. Managed Policies)**  
   - 설명: 동일한 리소스에 대해 Inline Policy와 Managed Policy가 충돌할 경우, Inline Policy가 우선 적용됨. 이는 보안 정책의 유연성과 위험 관리에 직접적인 영향을 미침.  
   - 키워드: `정책 우선순위`, `Inline Policy`, `Managed Policy`, `보안 정책`

3. **AWS Organizations과 IAM 통합**  
   - 설명: AWS Organizations를 통해 조직 단위별로 IAM 정책을 관리할 수 있음. 이는 중앙 집중식 권한 관리 및 비용 통제에 필수적.  
   - 키워드: `AWS Organizations`, `중앙 집중식 관리`, `비용 통제`, `조직 단위`

4. **MFA (Multi-Factor Authentication)**  
   - 설명: 보안 강화를 위해 MFA를 필수적으로 적용해야 하는 시나리오. 특히 관리자 계정이나 중요 리소스 접근 시 MFA가 요구됨.  
   - 키워드: `MFA`, `보안 강화`, `관리자 계정`, `리소스 접근`

5. **정책 형식 및 조건 (Policy Document)**  
   - 설명: `Effect`, `Action`, `Resource`, `Condition` 필드의 정확한 구성이 필수적. 조건식은 리소스에 대한 세부적인 접근 제어를 가능하게 함.  
   - 키워드: `Policy Document`, `Effect`, `Condition`, `세부 접근 제어`

#### ⚠️ 시험에서 자주 나오는 함정  
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "IAM 사용자와 역할의 차이" 문제에서, 사용자가 리소스에 직접 접근하고 역할이 리소스에 접근한다고 오인 | 사용자는 직접 리소스에 접근, 역할은 서비스 간 통신 시 사용 |
| 함정 2 | "Inline Policy와 Managed Policy의 우선순위" 문제에서, Managed Policy가 우선이라고 오인 | Inline Policy가 항상 우선 적용됨 |
| 함정 3 | "MFA 요구사항" 문제에서, 모든 사용자에 MFA를 강제한다고 오인 | 관리자 계정이나 중요 리소스 접근 시 MFA 필수 |

#### 🔄 IAM vs 비슷한 서비스 비교 (시험 단골!)  
| 비교 항목 | IAM | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 리소스 접근 제어, 사용자 관리 | AWS Organizations, CloudTrail | 리소스 권한 관리 시 IAM 선택 |
| 확장성 | 유연한 정책 관리 가능 | AWS Organizations | 조직 단위 관리 시 Organizations 선택 |
| 비용 | 관리 비용이 낮음 | CloudTrail | 로깅 및 감사 시 CloudTrail 선택 |
| 지연시간 | 실시간 권한 검증 | - | - |

#### 📝 시험 대비 체크리스트  
- [ ] IAM의 핵심 목적을 한 문장으로 설명할 수 있는가?  
- [ ] IAM를 선택해야 하는 시나리오를 알고 있는가?  
- [ ] IAM의 제한사항/한계를 알고 있는가?  
- [ ] IAM와 비슷한 서비스의 차이점을 설명할 수 있는가?  
- [ ] IAM의 비용 구조를 이해하고 있는가?

#### 💡 시험 팁  
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 IAM를 떠올리세요:  
> - **정책**  
> - **역할**  
> - **MFA**  
> - **사용자**  
> - **조직 단위**

---

| [⬅️ AWS Global Infrastructure](./AWS-Global-Infrastructure.md) | [📑 Day 1 목차](./README.md) | [🏠 Week 1](../README.md) | [다음 Day ➡️](../day2/README.md) |

---
