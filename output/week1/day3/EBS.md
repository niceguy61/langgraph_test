---

| [⬅️ AMI](./AMI.md) | [📑 Day 3 목차](./README.md) | [🏠 Week 1](../README.md) | [다음 Day ➡️](../day4/README.md) |

---

> **한 줄 정의:** EBS는 EC2 인스턴스에 데이터를 저장하고 관리하기 위한 AWS의 블록 스토리지 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**  
- **문제 1:** EC2 인스턴스가 중단되면 임시 저장소(예: /tmp)에 저장된 데이터가 모두 삭제되는 문제  
  - 기존 방식: 서버 종료 시 모든 데이터가 손실되며, 복구가 어려움  
- **문제 2:** 여러 인스턴스 간 데이터 공유 및 일관성 유지가 어려운 문제  
  - 기존 방식: 파일 공유나 DB 서버를 별도로 운영해야 함  
- **문제 3:** 대규모 데이터 처리 시 성능 한계가 발생하는 문제  
  - 기존 방식: 로컬 디스크의 IOPS 제약으로 응답 지연  

**EBS로 해결:**  
- **해결 1:** EBS 볼륨을 인스턴스에 첨부하여 데이터를 지속적으로 저장하고, 인스턴스 종료 시 데이터 보존  
- **해결 2:** 여러 인스턴스가 동일 EBS 볼륨을 공유하거나, 복제 기능으로 데이터 일관성 유지  
- **해결 3:** SSD 기반 EBS 유형(예: gp2)으로 높은 IOPS와 읽기/쓰기 성능 제공  

### 비유로 이해하기  
EBS는 컴퓨터에 USB 드라이브를 연결한 것처럼, EC2 인스턴스에 데이터를 영구 저장하는 장치입니다. 컴퓨터를 껐다 켜도 데이터가 사라지지 않고, 여러 기기(인스턴스)가 공유할 수 있는 저장소 역할을 합니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 데이터베이스 서버 운영 | 금융 기관이 EBS를 사용해 고객 데이터를 안전하게 저장 |
| 시나리오 2 | 분산 애플리케이션 데이터 저장 | 쇼핑몰이 여러 서버에서 EBS를 통해 실시간 주문 데이터 공유 |
| 시나리오 3 | 고성능 데이터 처리 | 실시간 분석 시스템이 EBS SSD 볼륨을 통해 초고속 처리 |

**이럴 때 EBS를 선택하세요:**  
- ✅ 데이터를 영구 저장해야 할 경우  
- ✅ 여러 인스턴스 간 데이터 공유가 필요할 때  
- ✅ 대규모 IOPS 성능이 요구되는 시나리오  

**이럴 때는 다른 서비스를 고려하세요:**  
- ❌ 단기 사용 데이터 → **S3** (비용 효율성)  
- ❌ 대용량 백업 → **S3 Glacier** (저비용 보관)  
- ❌ 비동기 데이터 저장 → **DynamoDB** (NoSQL 기반)  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **EC2** | EBS 볼륨이 EC2 인스턴스에 첨부되어 데이터 저장 | EC2 → EBS 볼륨 |
| **S3** | EBS 백업 및 장기 보관 용도 | EBS → S3 백업 |
| **RDS** | 데이터베이스 서버와 EBS 볼륨의 별도 관리 | RDS → EBS 볼륨 |

**자주 사용되는 아키텍처 패턴:**  
```
User → Load Balancer → EC2 (EBS 볼륨 첨부) → S3 (백업)
```  

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **EBS 볼륨 저장 공간** | $0.10/GB-월 | 10GB 무료 (1개월) |
| **데이터 전송** | $0.15/GB | 12개월 무료 (10TB) |
| **스냅샷 저장** | $0.01/GB-월 | 항상 무료 |

**비용 최적화 팁:**  
1. 💡 **SSD 유형 선택:** gp2(3 IOPS/GB) 대비 io1(1000 IOPS/GB)로 성능/비용 균형 맞춤  
2. 💡 **스냅샷 자동 백업:** 정기적인 백업으로 데이터 손실 방지 및 비용 절감  
3. 💡 **프리티어 활용:** 초기 테스트 시 10GB 무료로 인스턴스 구성  

> **⚠️ 비용 주의:** 스냅샷 볼륨이 과도하게 증가하면 저장 공간 비용이 급격히 상승할 수 있음. 정기적인 청소 및 백업 정책 관리 필수.

## 📚 핵심 개념

### 개념 1: **EBS 볼륨(Elastic Block Store Volume)**
EBS 볼륨은 AWS에서 제공하는 블록 스토리지 서비스로, EC2 인스턴스에 부착되어 데이터를 저장하고 관리하는 기본 단위입니다. 이 볼륨은 데이터의 지속성, 확장성, 고성능을 제공하며, 인스턴스의 종료나 재부팅 시에도 데이터가 사라지지 않습니다. EBS는 가상 머신에 직접 연결되어 파일 시스템을 통해 접근할 수 있으며, 다양한 스토리지 유형(예: SSD, HDD)과 성능 옵션(예: IOPS)을 제공합니다. 특히, 백업 및 복구 시 유연한 스냅샷 기능과 결합해 데이터 보호를 강화합니다.

#### 왜 중요한가?
- **데이터 지속성 보장**: 인스턴스 종료 시 데이터가 손실되지 않도록 설계되어 장애 대응에 필수적입니다.  
- **스케일러블성**: 필요에 따라 용량을 늘리거나 줄일 수 있어 다양한 워크로드에 적합합니다.  
- **성능 최적화**: IOPS(입/출력 수) 조절이 가능해 높은 처리량이 필요한 애플리케이션(예: 데이터베이스)에 적합합니다.  

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **볼륨 유형** | SSD(OPS), HDD(스토리지), GP2(일반용) 등 다양한 성능/비용 조합 | SSD 볼륨을 사용해 고성능 DB 서버 구성 |
| **용량 및 IOPS** | 최대 16TB까지 확장 가능, IOPS는 100~64,000까지 조절 | 1000 IOPS로 중규모 웹 서버 운영 |
| **데이터 복제** | 복제 볼륨으로 동일 지역 내 복제 지원 | 가용성 존 내 복제로 장애 복구 시간 단축 |

> **💡 Tip:** 고성능이 필요한 애플리케이션(예: RDS, NoSQL)은 **SSD 볼륨**을, 저장용은 **HDD 볼륨**을 선택해 비용과 성능을 균형 있게 조절하세요.

---

### 개념 2: **EBS 스냅샷(Snapshot)**
EBS 스냅샷은 볼륨의 현재 상태를 포인트 인 타임(Point-in-Time)으로 저장하는 기능입니다. 이는 데이터 백업, 복구, 볼륨 복제 등에 활용되며, AWS S3에 저장되어 볼륨의 크기와 관계없이 저장 공간을 절약합니다. 스냅샷은 볼륨이 생성될 때 자동으로 생성되거나, 수동으로 생성할 수 있으며, 복원 시 볼륨을 생성해 원본 데이터를 복구합니다. 또한, 스냅샷은 백업을 자동화하고, 볼륨 복제를 통해 클러스터 구성에 유용합니다.

#### 작동 원리
1. **스냅샷 생성**: 볼륨의 데이터가 S3에 저장되며, 이 과정은 백그라운드에서 비동기적으로 진행됩니다.  
2. **데이터 복제**: 처음에는 전체 데이터를 복사하지만, 이후 변경된 데이터만 차분식으로 저장해 성능 향상.  
3. **복원**: 볼륨 생성 시 스냅샷을 기반으로 데이터를 복원해 원본 볼륨과 동일한 상태로 만듭니다.  

> **💡 Tip:** **CloudWatch 알림**과 **Lambda**를 결합해 정기적인 스냅샷 생성을 자동화해 데이터 손실 위험을 최소화하세요.

---

### 개념 3: **볼륨 부착 및 분리**
EBS 볼륨은 EC2 인스턴스에 직접 부착되어 데이터 접근이 가능합니다. 이 과정은 CLI, 콘솔, 또는 Terraform 등으로 관리할 수 있으며, 인스턴스 종료 시 볼륨은 자동으로 분리됩니다. 볼륨은 여러 인스턴스에 부착될 수 없고, 단일 인스턴스에만 연결됩니다. 이 기능은 데이터 접근 권한을 제어하고, 인스턴스 간 데이터 공유나 백업 시 유용합니다. 또한, 볼륨의 상태(예: 부착, 분리, 가용)를 모니터링해 시스템 안정성을 유지할 수 있습니다.

#### 주요 특징
1. ****Persistent Storage**: 인스턴스 종료 시 볼륨은 여전히 존재해 데이터를 안전하게 유지합니다.  
2. **Performance Tuning**: 볼륨의 IOPS와 용량을 조절해 특정 워크로드(예: DB, 파일 서버)에 최적화된 성능 제공.  
3. **Security & Access Control**: IAM 정책으로 볼륨 접근 권한을 제어해 보안 위험을 방지합니다.  

> **💡 Tip:** 볼륨을 부착할 때 **/dev/xvd* 형식**을 사용하고, **fstab** 파일에 자동 마운트 설정해 장애 시 데이터 접근성을 높이세요.

## 🖥️ AWS 콘솔에서 EBS 사용하기

### Step 1: EBS 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - AWS 계정이 활성화되어 있고 IAM 권한이 부여된 경우만 접근 가능합니다.  
2. 상단 검색창에서 "EBS"를 입력하고 검색합니다.  
   - "Elastic Block Store (EBS)" 서비스가 검색 결과에 표시됩니다.  
3. 검색 결과에서 "EBS"를 클릭합니다.  
   - **AWS 콘솔 > EBS 서비스**로 이동하여 EBS 대시보드를 확인합니다.  

> **📸 화면 확인:** EBS 대시보드가 표시되면 정상입니다.  
> 대시보드에는 생성된 볼륨 목록, 사용량 통계, 볼륨 타입별 분포가 표시됩니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **볼륨 생성**  
   - 좌측 메뉴에서 **"Volumes"** > **"Create volume"** 클릭  
   - **Volume Type**: `gp2` (General Purpose SSD, 기본값) 선택  
   - **Size**: 10GB 입력 (프리티어 기준 10GB까지 무료)  
   - **Availability Zone**: 현재 인스턴스가 속한 존 선택 (예: `us-east-1a`)  
   - **Tags**: 필요 시 `Name` 태그 입력 (예: `WebServer-Storage`)  
   - **Create** 버튼 클릭  
2. **볼륨 확인**  
   - 생성 후 **Volumes** 탭에서 생성된 볼륨이 `available` 상태로 표시됩니다.  
   - **Volume ID**와 **Size**가 일치하는지 확인합니다.  
3. **인스턴스에 볼륨 부착**  
   - 볼륨 선택 후 **"Attach"** 클릭  
   - **Instance ID**: 이미 생성된 EC2 인스턴스 선택  
   - **Device Name**: `/dev/xvd` (예: `/dev/xvdg`) 입력  
   - **Attach** 버튼 클릭  

> **📸 화면 확인:** 볼륨 생성 및 부착 완료 시 `attached` 상태로 표시됩니다.  
> **⚠️ 주의:** 볼륨이 `available` 상태일 때만 부착 가능합니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **볼륨 상태 확인**  
   - **Volumes** 탭에서 볼륨 상태를 `in-use`로 변경했는지 확인합니다.  
   - **Volume Actions** > **Detach** (필요 시)  
2. **볼륨 설정 수정**  
   - 볼륨 선택 후 **"Actions"** > **"Modify"** 클릭  
   - **Size**: 20GB로 변경 (프리티어 기준 10GB까지만 무료)  
   - **Apply** 버튼 클릭  
   - **Wait** 탭에서 확장이 완료될 때까지 대기 (최대 15분)  
3. **볼륨 백업 및 복구**  
   - **Actions** > **"Create snapshot"** 클릭  
   - **Snapshot Name** 입력 후 **Create**  
   - 복구 시 **"Restore snapshot"**을 통해 기존 볼륨을 복원할 수 있습니다.  

> **⚠️ 주의:** 볼륨을 `detach`할 때 **"Force detach"** 옵션은 데이터 손실 위험 있으므로 주의해야 합니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인 방법**  
   - **Volumes** 탭에서 볼륨 이름, 크기, 상태를 확인합니다.  
   - **Snapshots** 탭에서 백업된 스냅샷이 존재하는지 확인합니다.  
2. **인스턴스에서 볼륨 접근 확인**  
   - SSH로 EC2 인스턴스에 접속 후 `/dev/xvdg` 경로가 마운트되었는지 확인합니다.  
   - 명령어: `lsblk` 또는 `df -h`  
   - 예상 출력: `/dev/xvdg`가 `10GB` 크기로 표시됨  
3. **정상 동작 테스트**  
   - 웹서버를 구성한 경우 `curl http://localhost` 명령어로 HTTP 응답을 확인합니다.  
   - 볼륨에 데이터를 저장 후 `ls` 명령어로 파일이 생성되었는지 확인합니다.  

> **💡 Tip:** EBS 볼륨은 데이터 일관성 보장, 백업, 복구가 가능하며, EC2 인스턴스와 별도로 관리됩니다.  
> **💰 비용 주의:** EBS 볼륨은 10GB 이상 사용 시 비용이 발생하므로 프리티어 범위를 확인하세요.

## ⌨️ AWS CLI로 EBS 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**설명:**  
AWS CLI를 사용하기 전에 기본 설정이 완료되었는지 확인합니다. `aws --version`은 CLI 버전을 확인하고, `sts get-caller-identity`는 현재 사용자의 AWS 계정 정보를 출력합니다. 리전은 EBS 리소스 생성 시 자동으로 적용되지만, 명시적으로 설정하는 것이 좋습니다.

---

### 예제 1: EBS 리소스 조회
```bash
# EBS 볼륨 목록 조회
aws ec2 describe-volumes --query 'Volumes[*].{VolumeId:VolumeId, Size:Size, State:State}' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 결과 필터링 (VolumeId, Size, State 등 필드 선택) | `'Volumes[*].{VolumeId:VolumeId, Size:Size, State:State}'` |
| `--output` | 출력 형식 | `table` (테이블 형식), `json` (JSON 형식), `text` (텍스트 형식) |

**예상 출력:**
```
| VolumeId | Size | State  |
|----------|------|--------|
| vol-1234 | 10   | in-use |
| vol-5678 | 20   | available |
```

**팁:**  
`describe-volumes` 명령어는 EBS 볼륨의 상태, 크기, 할당 여부 등을 확인하는 데 사용됩니다. `--query`를 활용해 필요한 정보만 추출할 수 있습니다.

---

### 예제 2: EBS 리소스 생성
```bash
# EBS 볼륨 생성
aws ec2 create-volume \
    --availability-zone us-west-2 \
    --size 10 \
    --volume-type gp2 \
    --tag-specifications 'ResourceType=volumes,Tags=[{Key=Environment,Value=Production}]'
```

**필수 옵션:**
- `--availability-zone`: 볼륨이 할당될 리전 (예: `us-west-2`)
- `--size`: 볼륨 크기 (GB 단위)
- `--volume-type`: 볼륨 타입 (gp2: General Purpose SSD, io1: Provisioned IOPS SSD 등)
- `--tag-specifications`: 태그 추가 (관리 정책에 따라 필수)

**예상 출력:**
```json
{
    "VolumeId": "vol-1234567890abcdef0"
}
```

**주의사항:**  
- 볼륨 생성 시 `--size`는 최소 1GB 이상이어야 합니다.
- `--volume-type`은 성능 요구사항에 따라 선택해야 합니다 (예: 고성능 DB 서버에는 `io1` 사용).

---

### 예제 3: EBS 리소스 수정
```bash
# EBS 볼륨 크기 변경
aws ec2 modify-volume --volume-id vol-1234567890abcdef0 --size 20
```

**설명:**  
`modify-volume` 명령어는 볼륨 크기 변경, IOPS 조정, 볼륨 유형 변경 등을 수행합니다.  
**참고:**  
- 볼륨 크기 증가는 가능하지만, 감소는 불가능합니다.  
- 크기 변경 시 `--size`는 기존 크기보다 커야 합니다.

**예상 출력:**
```json
{
    "Return": true
}
```

---

### 예제 4: EBS 리소스 삭제
```bash
# EBS 볼륨 삭제
aws ec2 delete-volume --volume-id vol-1234567890abcdef0

# 삭제 확인
aws ec2 describe-volumes --volume-id vol-1234567890abcdef0
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. 볼륨이 인스턴스에 연결되어 있으면 먼저 분리해야 합니다.  
> **확인 절차:** 삭제 후 `describe-volumes` 명령어로 상태를 확인하세요. 삭제 완료 시 `VolumeId`가 출력되지 않습니다.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws ec2 describe-volumes --query 'Volumes[*].{VolumeId:VolumeId, Size:Size, State:State}' --output table
aws ec2 describe-volumes --volume-id vol-1234567890abcdef0

# 생성
aws ec2 create-volume --availability-zone us-west-2 --size 10 --volume-type gp2

# 수정
aws ec2 modify-volume --volume-id vol-1234567890abcdef0 --size 20

# 삭제
aws ec2 delete-volume --volume-id vol-1234567890abcdef0
```

**추가 명령어:**  
- 볼륨 연결: `aws ec2 attach-volume --volume-id vol-1234567890abcdef0 --instance-id i-1234567890abcdef0 --device /dev/sdh`
- 볼륨 분리: `aws ec2 detach-volume --volume-id vol-1234567890abcdef0`

---

### 📌 비용 및 프리티어 활용 팁
| 항목 | 설명 |
|------|------|
| **비용 절감:** | 볼륨 크기 증가 시 비용이 상승하므로, 필요 사양만 선택하세요. |
| **프리티어:** | 12개월 동안 100만 원 상당의 AWS 서비스 사용 가능 (EBS 포함). [AWS 프리티어 신청](https://aws.amazon.com/free/) |
| **리전 선택:** | 리전에 따라 가격 차이가 있으므로, 예산에 맞는 리전 선택이 중요합니다. |

**경고:**  
- EBS 볼륨은 인스턴스와 분리되어도 데이터가 보존됩니다.  
- 장기 보관 시 `snapshots`를 활용해 백업을 권장합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 EBS 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **EBS 볼륨 유형 및 성능 차이**:
   - 설명: EBS는 SSD, HDD, NVMe 등 다양한 볼륨 유형을 제공하며, 각 유형의 IOPS, 통화량, 용량 제한 등이 시험에서 주요 포인트로 출제됩니다. 예: `gp2`는 일반적인 IOPS 성능을 제공하지만, `io1`은 고성능 DB 서버에 적합합니다.
   - 키워드: `볼륨 유형`, `IOPS`, `통화량`, `성능 튜닝`

2. **스냅샷 및 백업 전략**:
   - 설명: EBS 스냅샷은 데이터 복구 및 백업에 필수적이며, 시험에서 스냅샷의 작동 방식, 제한 사항(예: 스토리지 클래스에 따른 복구 시간), 및 백업 전략(예: 정기 스냅샷 정책)을 묻는 문제가 자주 출제됩니다.
   - 키워드: `스냅샷`, `백업`, `복구 시간`, `정기 백업`

3. **비용 구조 및 최적화 방법**:
   - 설명: EBS는 볼륨 생성, 데이터 전송, 스냅샷 저장 등에 따라 비용이 발생하며, 시험에서 스팟 인스턴스와 결합된 비용 최적화 전략(예: `Storage Class-optimized` 인스턴스)을 묻는 문제가 자주 등장합니다.
   - 키워드: `비용 모델`, `스팟 인스턴스`, `Storage Class`, `비용 최적화`

4. **EBS의 제한 사항 및 지연 시간**:
   - 설명: EBS는 볼륨 크기 제한(예: `gp2`는 16TB까지), 볼륨 전송 지연 시간, 및 데이터 일관성 보장에 대한 제한 사항이 시험에서 주요 포인트로 출제됩니다. 예: `io1`은 64TB까지 확장 가능하지만, `st1`은 128TB까지 확장 가능합니다.
   - 키워드: `볼륨 크기`, `지연 시간`, `데이터 일관성`, `확장 한계`

5. **EC2와의 연동 및 데이터 지속성**:
   - 설명: EBS 볼륨은 EC2 인스턴스와 연결되어 데이터 지속성을 제공하지만, 인스턴스 종료 시 데이터 보존 여부(예: `EBS volume retention`)와 볼륨 전송 방식(예: `snapshot` vs `volume copy`)은 시험에서 자주 묻습니다.
   - 키워드: `데이터 지속성`, `인스턴스 종료`, `볼륨 전송`, `EBS 볼륨 보존`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | EBS 스냅샷이 실시간 백업을 제공한다고 오인할 수 있음 | 스냅샷은 **이미지 기반**이며, 실시간 복제가 아님 |
| 함정 2 | `io1` 볼륨이 `gp2`보다 항상 더 높은 IOPS를 제공한다고 오인할 수 있음 | `io1`은 IOPS를 설정할 수 있지만, `gp2`는 자동 IOPS 계산 |
| 함정 3 | EBS 볼륨의 데이터가 인스턴스 종료 시 자동 삭제된다고 오인할 수 있음 | 볼륨은 **인스턴스 종료 시도 삭제되지 않음** (단, 삭제 명령이 필요) |

#### 🔄 EBS vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | EBS | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | EC2 인스턴스에 부착된 **블록 스토리지** | S3 (객체 저장소), EFS (파일 저장소), Glacier (아카이브 저장소) | **데이터 지속성** 및 **성능 요구사항**에 따라 선택 |
| 확장성 | 볼륨 크기 제한 (예: `io1` 64TB, `st1` 128TB) | S3: 무한 용량, EFS: 클라우드 파일 시스템 | **데이터 접근 패턴** (랜덤 vs 시퀀셜 I/O)에 따라 선택 |
| 비용 | 볼륨 생성, 데이터 전송, 스냅샷 저장에 따라 비용 발생 | S3: 스토리지 + 데이터 전송 비용, Glacier: 저비용 아카이브 | **데이터 접근 빈도** 및 **비용 최적화**에 따라 선택 |
| 지연 시간 | 블록 단위 접근으로 **저지연** (예: `ssd` 볼륨) | S3: 오브젝트 단위 접근 (일정 지연), EFS: 네트워크 지연 | **실시간 데이터 처리**가 필요한 경우 EBS 선택 |

#### 📝 시험 대비 체크리스트
- [ ] EBS의 핵심 목적을 한 문장으로 설명할 수 있는가? (예: "EC2 인스턴스에 부착되어 데이터 지속성을 제공하는 블록 스토리지")
- [ ] EBS를 선택해야 하는 시나리오를 알고 있는가? (예: 고성능 DB 서버, 실시간 데이터 처리)
- [ ] EBS의 제한사항/한계를 알고 있는가? (예: 볼륨 크기 제한, 스냅샷 복구 시간)
- [ ] EBS와 비슷한 서비스의 차이점을 설명할 수 있는가? (예: EBS vs S3: 블록 vs 오브젝트 스토리지)
- [ ] EBS의 비용 구조를 이해하고 있는가? (예: 볼륨 생성, 스냅샷, 데이터 전송 비용)

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 EBS를 떠올리세요:  
> - **Persistent Storage** (데이터 지속성)  
> - **IOPS** (입/출력 수)  
> - **Snapshot** (스냅샷)  
> - **Volume Type** (볼륨 유형)  
> - **Data Retention** (데이터 보존)

---

| [⬅️ AMI](./AMI.md) | [📑 Day 3 목차](./README.md) | [🏠 Week 1](../README.md) | [다음 Day ➡️](../day4/README.md) |

---
