---

| [⬅️ Auto Scaling](./Auto-Scaling.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 1](../README.md) | [다음 Day ➡️](../../week2/day1/README.md) |

---

# Launch Templates 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** Launch Templates는 Auto Scaling 그룹 및 ELB를 통해 고가용성과 확장성을 달성하기 위한 AWS의 인스턴스 구성 템플릿 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 수동으로 인스턴스 구성 시 동일한 스펙을 유지하기 어려웠습니다. 예를 들어, ELB에 연결된 EC2 인스턴스가 서로 다른 CPU/메모리 스펙을 가질 경우, 요청 분산 불균형과 성능 저하가 발생했습니다.
- **문제 2:** Auto Scaling 그룹을 운영할 때, 인스턴스 생성 시마다 매뉴얼로 파라미터를 입력해야 해서 시간이 많이 소요되었습니다. 예를 들어, 템포러리 디스크 크기나 보안 그룹 설정이 누락될 수 있었습니다.
- **문제 3:** 확장성 요구사항이 변동할 경우, 인스턴스 템플릿을 매번 수정해야 해서 운영 효율성이 떨어졌습니다. 예를 들어, 트래픽 증가 시 CPU가 80%에 도달하면 자동으로 인스턴스를 추가해야 하는데, 이 과정에서 매뉴얼 설정 실수가 발생할 수 있었습니다.

**Launch Templates로 해결:**
- **해결 1:** 템플릿을 통해 일관된 인스턴스 스펙을 정의해 ELB 연결 시 불균형을 방지합니다. 예를 들어, 모든 인스턴스가 4GB RAM, 2vCPU로 동일하게 구성됩니다.
- **해결 2:** Auto Scaling 그룹과 연동해 인스턴스 생성 시 자동으로 템플릿의 파라미터를 적용해 시간과 오류를 줄입니다. 예를 들어, CLI 명령어 `aws ec2 describe-launch-templates`로 템플릿을 한 번에 배포할 수 있습니다.
- **해결 3:** 조정 정책(Scaling Policy)과 결합해 트래픽 변화에 따라 자동으로 인스턴스 수를 조절할 수 있습니다. 예를 들어, CPU 사용률이 80%에 도달하면 `aws autoscaling put-scaling-policy` 명령어로 인스턴스를 자동으로 추가합니다.

### 비유로 이해하기
**Launch Templates는 레시피를 작성하는 주방장입니다.**  
요리할 때마다 재료를 수동으로 측정하는 대신, 레시피에 모든 재료의 양과 조리 방법을 정의해 놓으면, 언제든 요리할 때 정확한 양으로 만들 수 있습니다.  
Launch Templates도 마찬가지로, 인스턴스 생성 시 필요한 모든 설정을 템플릿에 정의해 놓고, ELB와 Auto Scaling 그룹이 필요할 때마다 템플릿을 기반으로 인스턴스를 자동으로 생성합니다. 이로 인해 운영 효율성과 일관성을 확보할 수 있습니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 트래픽 폭주 시 자동 확장이 필요한 서비스 | 온라인 쇼핑몰이 할인 이벤트 기간 동안 트래픽이 급증할 때 Auto Scaling 그룹을 통해 인스턴스를 자동으로 확장 |
| 시나리오 2 | ELB에 연결된 인스턴스의 일관된 구성이 필요한 경우 | 클라우드 게임 플랫폼에서 모든 서버 인스턴스가 동일한 CPU/메모리 스펙으로 구성되어 서버 간 성능 차이를 방지 |
| 시나리오 3 | 인스턴스 생성 시 보안 그룹, 태그, 스토리지 정의가 필요한 경우 | SaaS 플랫폼에서 모든 인스턴스가 동일한 보안 정책과 태그로 생성되어 관리 효율성 향상 |

**이럴 때 Launch Templates를 선택하세요:**
- ✅ **트래픽 변동에 따라 자동 확장이 필요한 서비스**
- ✅ **ELB와 Auto Scaling 그룹을 사용하는 클라우드 아키텍처**
- ✅ **인스턴스 생성 시 일관된 설정이 필요한 환경**

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **동적인 인스턴스 구성이 필요하지 않은 경우 → EC2 Classic**  
  이유: Launch Templates는 Auto Scaling과 ELB 연동 시 효과적이며, 단순한 인스턴스 생성 시에는 EC2 Classic이 더 적합합니다.
- ❌ **인스턴스 템플릿을 실시간으로 변경해야 하는 경우 → CloudFormation**  
  이유: CloudFormation은 인프라 코드화를 통해 템플릿을 동적으로 관리하지만, Launch Templates는 Auto Scaling과 ELB 연동에 최적화되어 있습니다.
- ❌ **리소스 비용 최적화가 주 목표인 경우 → EC2 Spot Fleet**  
  이유: Spot Fleet는 저비용으로 인스턴스를 배치하지만, Launch Templates는 일관된 구성과 확장성 보장에 중점이 있습니다.

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **Auto Scaling** | 인스턴스 수를 자동으로 조절해 확장성 확보 | `User → Load Balancer → Auto Scaling Group → EC2 Instances` |
| **ELB (Elastic Load Balancer)** | 트래픽을 인스턴스에 분산해 고가용성 제공 | `User → ALB/NLB/GLB → Auto Scaling Group → EC2 Instances` |
| **EC2** | Launch Templates로 정의된 인스턴스 스펙을 실제 인스턴스로 배포 | `Launch Template → EC2 Instances` |

**자주 사용되는 아키텍처 패턴:**
```
User → CloudFront → ELB (ALB) → Auto Scaling Group → EC2 Instances (Launch Template 기반)
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **Launch Template 생성 및 관리** | $0.01/템플릿/월 | 12개월 무료 |
| **Auto Scaling 그룹 운영** | $0.01/그룹/월 | 12개월 무료 |
| **ELB 사용 비용** | $0.05/월 (기본 요금) | 12개월 무료 |

**비용 최적화 팁:**
1. 💡 **프리티어 기간 동안 서비스를 활성화해 무료 사용**  
   AWS Free Tier에서 ELB, Auto Scaling, Launch Templates를 12개월 동안 무료로 사용할 수 있으므로, 초기 테스트 시에 활용하세요.
2. 💡 **인스턴스 스펙을 최적화해 비용 절감**  
   예를 들어, CPU 사용률이 낮은 서비스에는 `t2.micro` 대신 `t2.nano` 인스턴스를 선택해 비용을 절감할 수 있습니다.
3. 💡 **Auto Scaling 정책을 정밀하게 설정해 과도한 인스턴스 생성 방지**  
   예를 들어, CPU 사용률이 70%에 도달할 때만 인스턴스를 추가하도록 설정해 과소비를 방지해야 합니다.

> **⚠️ 비용 주의:** ELB의 '데이터 전송 비용'이나 Auto Scaling의 '스케일링 정책 오버헤드'로 인해 예상치 못한 비용이 발생할 수 있습니다. AWS Cost Explorer를 통해 비용 트래킹을 꼭 수행해야 합니다.

## 📚 핵심 개념

### 개념 1: **Launch Templates의 핵심 개념**
Launch Templates은 AWS EC2 인스턴스를 생성할 때 사용되는 일관된 구성 정의를 제공하는 기능입니다. 이 템플릿은 인스턴스 유형, AMI, 보안 그룹, 네트워크 설정, 스토리지 옵션 등과 같은 다양한 파라미터를 미리 정의하여, 동일한 환경에서 일관된 인스턴스 배포를 가능하게 합니다. 특히 Auto Scaling 그룹과 연동되어 자동으로 인스턴스를 생성/삭제할 때도 템플릿을 기반으로 동작하므로, 시스템의 일관성과 관리성을 높입니다. Launch Templates는 기존 Launch Configurations에 비해 동적 구성 변경이 가능하고, 버전 관리 기능을 지원하여 다양한 환경에 유연하게 적용할 수 있습니다.

#### 왜 중요한가?
- **일관성 확보**: 모든 인스턴스가 동일한 설정을 기반으로 생성되어 오류를 줄입니다.  
- **유연성**: 템플릿을 수정하면 기존 인스턴스는 영향을 받지 않으며, 새로운 인스턴스는 즉시 변경 사항을 반영합니다.  
- **비용 최적화**: 자동화된 인스턴스 생성을 통해 수동 오류를 방지하고, 리소스 사용을 효율적으로 관리할 수 있습니다.  

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| 인스턴스 유형 | EC2 인스턴스의 사양을 정의합니다. | `t2.micro` |
| AMI | 사용할 Amazon Machine Image를 지정합니다. | `ami-0c55b159cbfafe1f0` |
| 보안 그룹 | 네트워크 보안 설정을 정의합니다. | `sg-0a1b2c3d4e5f67890` |
| 네트워크 설정 | VPC, 서브넷, 공용 IP 등을 설정합니다. | `subnet-0a1b2c3d` |
| 스토리지 | 부트 볼륨 크기 및 암호화 옵션을 정의합니다. | `VolumeSize=20, Encrypted=True` |

> **💡 Tip:** 템플릿을 버전화하여 변경 사항을 추적하고, 테스트 환경에서 변경 사항을 검증한 후 본선에 배포하는 것이 안전합니다.

---

### 개념 2: **Launch Templates와 Auto Scaling의 연동 원리**
Launch Templates는 Auto Scaling 그룹(Auto Scaling Group, ASG)과 깊게 연동되어, 자동화된 인스턴스 확장/축소 시 일관된 구성이 유지됩니다. ASG는 요청량 증가 시 Launch Template에 정의된 설정을 기반으로 인스턴스를 자동으로 생성하고, 요청량 감소 시에는 인스턴스를 제거합니다. 이 과정에서 Launch Template는 자동화된 인스턴스 생성에 필요한 모든 매개변수를 제공하며, 조정 정책(Scaling Policy)에 따라 트리거됩니다. 예를 들어, CPU 사용률이 임계치를 넘으면 ASG는 Launch Template를 기반으로 새로운 인스턴스를 생성하여 처리 능력을 확장합니다.

#### 작동 원리
1. **템플릿 생성**: Launch Template를 생성하여 인스턴스의 기본 설정을 정의합니다.  
2. **ASG 연동**: 생성한 템플릿을 ASG에 연결하여 자동화된 인스턴스 관리를 설정합니다.  
3. **조정 정책 트리거**: CPU 사용률, 네트워크 트래픽 등이 임계치를 넘으면 ASG가 템플릿을 기반으로 인스턴스를 자동으로 확장/축소합니다.  

> **💡 Tip:** ASG와 Launch Templates를 함께 사용할 때는 조정 정책의 트리거 조건을 정확히 설정해, 리소스 과다 사용을 방지해야 합니다.

---

### 개념 3: **Launch Templates vs. Launch Configurations 비교**
Launch Templates는 Launch Configurations에 비해 더 유연하고 강력한 기능을 제공합니다. Launch Configurations는 정적 설정을 사용하는 반면, Launch Templates는 동적 설정을 지원하며 버전 관리 기능을 포함합니다. 예를 들어, Launch Templates는 Spot Instance를 지원하여 비용을 절감할 수 있으며, 템플릿의 특정 버전을 ASG에 연결해 변경 사항을 안정적으로 적용할 수 있습니다. 이는 테스트 환경에서의 변경 사항을 검증한 후 본선에 배포하는 데 유리합니다. 반면, Launch Configurations는 단일 버전만 지원하며, 변경 시 기존 인스턴스에 영향을 줄 수 있습니다.

#### 주요 특징
1. **동적 구성 관리**: 템플릿의 버전을 관리해 변경 사항을 추적하고, 특정 버전을 ASG에 연결해 안정적으로 적용할 수 있습니다.  
2. **Spot Instance 지원**: 비용 절감을 위해 Spot Instance를 포함한 템플릿 생성이 가능합니다.  
3. **버전화 기능**: 테스트 환경에서 변경 사항을 검증한 후, 본선에 배포할 템플릿 버전을 선택할 수 있습니다.  

> **💡 Tip:** 생산 환경에서 Launch Templates를 사용할 때는 버전 관리와 테스트 환경 검증을 필수적으로 수행해야 합니다.

## 🖥️ AWS 콘솔에서 Launch Templates 사용하기

### Step 1: Launch Templates 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - IAM 계정으로 로그인 후, **Services** 메뉴에서 **EC2**를 선택합니다.  
2. 상단 검색창에서 "Launch Templates"을 입력하고, **Launch Templates**를 클릭합니다.  
   - 이 서비스는 EC2에서 인스턴스를 빠르게 배포하고 관리하기 위한 템플릿을 생성/관리합니다.  
3. 생성된 **Launch Templates** 대시보드가 표시되면 정상입니다.  
   - 이 대시보드에서는 템플릿 목록, 템플릿 편집, Auto Scaling 그룹과의 연동 등을 확인할 수 있습니다.  

> **📸 화면 확인:**  
> ![Launch Templates 대시보드](https://example.com/launch-templates-dashboard.png)  
> - **Launch Templates** 메뉴가 정상적으로 표시되고, 템플릿 목록이 보이는지 확인하세요.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **Launch Template 생성**  
   - **Actions > Create launch template** 버튼을 클릭합니다.  
   - **Launch template name**을 입력하고, **Version description**을 작성합니다.  
     - 예: `Web-Server-Template-v1`  
   - **Launch template version**을 1로 설정하고, **Next**를 클릭합니다.  
2. **인스턴스 구성 설정**  
   - **AMI**를 선택합니다 (예: Ubuntu Server 22.04 LTS).  
   - **Instance type**을 선택합니다 (예: `t2.micro`).  
   - **Network** 설정에서 **VPC**와 **Subnet**을 선택합니다.  
   - **Security groups**에 접근할 수 있는 보안 그룹을 추가합니다.  
   - **Key pair**를 선택하거나 생성합니다 (SSH 접근을 위해 필요).  
   - **Tags**에 필요한 메타데이터를 입력합니다 (예: `Environment=Production`).  
3. **Launch permissions 설정**  
   - **Launch permissions**에서 **EC2 instances**를 선택하고, **Add**를 클릭합니다.  
   - **Auto Scaling groups**에 연결할 그룹을 선택합니다.  
   - **Next**를 클릭하여 생성을 완료합니다.  

> **📸 화면 확인:**  
> ![Launch Template 생성 화면](https://example.com/launch-template-create.png)  
> - **AMI**, **Instance type**, **Security groups** 등이 정확히 입력되었는지 확인하세요.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **템플릿 편집**  
   - **Actions > Edit**를 클릭하여 템플릿을 수정합니다.  
   - **Instance details**, **Network**, **Storage**, **Tags** 등 각 항목을 수정할 수 있습니다.  
2. **파라미터 설정**  
   - **Parameterized launch templates**를 사용하여 동적 파라미터를 설정합니다.  
     - 예: `InstanceType`을 `t2.micro`에서 `t2.medium`로 변경할 수 있도록 파라미터화합니다.  
3. **Launch permissions 조정**  
   - **Launch permissions**에서 **Specific accounts**를 선택하고, 허용할 AWS 계정을 추가합니다.  
   - **No restrictions**를 선택하면 모든 계정이 템플릿을 사용할 수 있습니다.  

> **⚠️ 주의:**  
> - **Launch permissions**을 제한하지 않으면 보안 위험이 있으므로, 실제 환경에서는 접근 제한을 설정하세요.  
> - 파라미터화된 템플릿은 Auto Scaling Group과 연동할 때 유연하게 사용할 수 있습니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인**  
   - **Launch Templates** 메뉴에서 생성한 템플릿을 선택하고, **Description** 탭에서 설정 내용을 확인합니다.  
2. **인스턴스 생성 상태 확인**  
   - **Auto Scaling Groups** 메뉴에서 연결된 그룹을 선택하고, **Instances** 탭에서 생성된 인스턴스 상태를 확인합니다.  
   - **Status**가 `InService`로 표시되면 정상적으로 배포되었습니다.  
3. **테스트 실행**  
   - **Actions > Create Auto Scaling Group**을 클릭하고, 생성한 템플릿을 선택합니다.  
   - **Minimum**, **Maximum**, **Desired capacity** 값을 설정하고, **Create**를 클릭하여 테스트를 수행합니다.  

> **💡 Tip:**  
> - 템플릿 테스트 후에는 **CloudWatch Metrics**에서 CPU 사용량, 네트워크 트래픽 등을 모니터링하여 정상 작동 여부를 확인하세요.  
> - **Cost Explorer**를 사용해 템플릿으로 생성된 인스턴스의 비용을 분석해 예산을 관리하세요.  

---

### ✅ 체크리스트: Launch Templates 설정 완료 여부  
- [ ] Launch Templates 생성 및 편집 완료  
- [ ] Auto Scaling Group과의 연동 설정 완료  
- [ ] Launch permissions 제한 설정 완료  
- [ ] 인스턴스 생성 및 상태 확인 완료  
- [ ] 템플릿 테스트 및 모니터링 완료  

> **💡 실무 팁:**  
> - Launch Templates는 Auto Scaling Group과 함께 사용하여 고가용성 환경을 구축하는 데 필수적입니다.  
> - 템플릿을 변경할 경우, **Version**을 업데이트하고 기존 그룹과의 호환성을 확인하세요.

## ⌨️ AWS CLI로 Launch Templates 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

> **💡 Tip:** AWS CLI를 사용하기 전에 `aws configure`로 액세스 키, 시크릿 액세스 키, 리전, 스토리지 클래스를 설정해야 합니다.  
> **⚠️ 주의:** CLI 명령어 실행 전에 `aws ec2 describe-launch-templates`로 기존 템플릿이 있는지 확인하세요.

### 예제 1: Launch Templates 리소스 조회
```bash
# [Launch Templates 리소스 목록 조회]
aws ec2 describe-launch-templates \
    --query 'LaunchTemplates[].LaunchTemplateName' \
    --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 결과 필터링 | `'LaunchTemplates[].LaunchTemplateName'` |
| `--output` | 출력 형식 | `json`, `table`, `text` |

**예상 출력:**
```
| LaunchTemplateName |
+---------------------+
| example-template   |
| default-template   |
```

> **💡 Tip:** `--query`를 사용해 특정 필드만 조회할 수 있습니다. 예를 들어 `LaunchTemplates[].LaunchTemplateId`로 ID만 확인할 수 있습니다.

### 예제 2: Launch Templates 리소스 생성
```bash
# [Launch Templates 리소스 생성]
aws ec2 create-launch-template \
    --launch-template-name "example-template" \
    --launch-template-data '{
        "InstanceType": "t2.micro",
        "ImageId": "ami-0c55b159cbfafe1f0",
        "KeyName": "example-key",
        "SecurityGroupIds": ["sg-0123456789abcdef0"]
    }'
```

**필수 옵션:**
- `--launch-template-name`: 템플릿 이름 (중복 불가)
- `--launch-template-data`: 인스턴스 구성 정보 (JSON 형식)

**예상 출력:**
```json
{
    "LaunchTemplate": {
        "LaunchTemplateId": "lt-0123456789abcdef0",
        "LaunchTemplateName": "example-template",
        "Version": "1"
    }
}
```

> **💡 Tip:** `--launch-template-data`는 EC2 인스턴스의 사양(InstanceType, ImageId 등)을 정의합니다.  
> **⚠️ 주의:** JSON 형식을 엄격히 지키지 않으면 오류 발생합니다.

### 예제 3: Launch Templates 리소스 수정
```bash
# [Launch Templates 리소스 수정]
aws ec2 update-launch-template \
    --launch-template-id "lt-0123456789abcdef0" \
    --launch-template-data '{
        "InstanceType": "t2.medium",
        "SecurityGroupIds": ["sg-0a1b2c3d4e5f67890"]
    }'
```

**필수 옵션:**
- `--launch-template-id`: 수정할 템플릿 ID
- `--launch-template-data`: 업데이트할 구성 정보

> **💡 Tip:** 수정 시 이전 버전은 자동으로 "2"로 변경됩니다. 버전 관리를 위해 `--version` 파라미터를 사용할 수 있습니다.

### 예제 4: Launch Templates 리소스 삭제
```bash
# [Launch Templates 리소스 삭제]
aws ec2 delete-launch-template --launch-template-id "lt-0123456789abcdef0"

# 삭제 확인
aws ec2 describe-launch-templates --launch-template-id "lt-0123456789abcdef0"
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. 삭제 후에는 `describe-launch-templates`로 상태를 확인하세요.

### 자주 사용하는 명령어 정리
```bash
# 조회
aws ec2 describe-launch-templates --launch-template-ids "lt-0123456789abcdef0"
aws ec2 describe-launch-templates --launch-template-names "example-template"

# 생성
aws ec2 create-launch-template --launch-template-name "new-template" --launch-template-data '...'

# 수정
aws ec2 update-launch-template --launch-template-id "lt-0123456789abcdef0" --launch-template-data '...'

# 삭제
aws ec2 delete-launch-template --launch-template-id "lt-0123456789abcdef0"
```

> **💡 Tip:** Auto Scaling Group과 연동 시 `--launch-template-id`를 사용해 템플릿을 참조합니다.  
> **⚠️ 주의:** 프리티어는 750시간까지 사용 가능하며, Launch Templates는 EC2 인스턴스 생성 시 비용이 발생합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Launch Templates 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **포인트 1: ALB/NLB/GLB/CLB 차이점**  
   - 설명: ELB(Apache Load Balancer)와 NLB(Network Load Balancer), GLB(Global Load Balancer), CLB(Classic Load Balancer)의 차이점은 AWS의 확장성과 가용성 설계 시 필수적인 지식입니다. 시험에서 키워드로는 "Application Load Balancer", "Network Load Balancer", "Global Load Balancer", "Classic Load Balancer"가 자주 등장합니다.  
   - 키워드: `Application Load Balancer`, `Network Load Balancer`, `Global Load Balancer`, `Classic Load Balancer`

2. **포인트 2: 대상 그룹(Target Group) 구성 및 상태 검사**  
   - 설명: 대상 그룹은 ALB/NLB에서 트래픽을 분배할 대상 인스턴스를 관리하는 핵심 구성 요소입니다. 상태 검사 설정(HTTP/HTTPS, TCP, Ping)은 서비스 가용성 판단에 직접 영향을 미칩니다. 시험에서는 상태 검사 실패 시 자동 복구 로직과 관련된 문제를 출제합니다.  
   - 키워드: `Target Group`, `Health Check`, `HTTP/HTTPS`, `TCP`

3. **포인트 3: Auto Scaling 그룹(Auto Scaling Group) 운영 원리**  
   - 설명: Auto Scaling 그룹은 요청량에 따라 인스턴스 수를 자동 조절하는 기능을 제공합니다. 시험에서는 "Minimum Capacity", "Maximum Capacity", "Desired Capacity" 설정과, Auto Scaling 정책(예: CPU 사용률 기준)에 따른 인스턴스 확장/축소 로직을 출제합니다.  
   - 키워드: `Auto Scaling Group`, `Scaling Policy`, `Capacity`, `Desired Capacity`

4. **포인트 4: 조정 정책(Scaling Policy) 타입 및 트리거 조건**  
   - 설명: 조정 정책은 CPU, 메모리, 네트워크 트래픽 등 특정 메트릭에 따라 인스턴스를 확장/축소하는 로직을 정의합니다. 시험에서는 "Target Tracking Scaling Policy"와 "Scheduled Scaling"의 차이, 그리고 "CloudWatch Alarm"과의 연동 방식을 다룹니다.  
   - 키워드: `Scaling Policy`, `Target Tracking`, `CloudWatch Alarm`, `Scheduled Scaling`

5. **포인트 5: Launch Templates의 인스턴스 구성 정의 방법**  
   - 설명: Launch Templates는 EC2 인스턴스의 사양(예: AMI, 인스턴스 유형, 보안 그룹, 키 페어)을 정의하는 템플릿입니다. 시험에서는 "Launch Template" vs "Launch Configuration"의 차이, 그리고 "Version Control"을 통한 템플릿 관리 방식을 출제합니다.  
   - 키워드: `Launch Template`, `Launch Configuration`, `Version Control`, `Instance Specification`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | ALB vs NLB의 차이를 혼동할 수 있음 | ALB는 HTTP/HTTPS 기반의 7층 로드 밸런서, NLB는 TCP/UDP 기반의 4층 로드 밸런서 |
| 함정 2 | 상태 검사 시 HTTP vs TCP의 의미를 헷갈릴 수 있음 | HTTP는 상태 코드(200 OK)를 기준으로 검사, TCP는 연결 성공 여부만 확인 |
| 함정 3 | Auto Scaling 정책의 "Target Tracking" vs "Scheduled Scaling"을 혼동할 수 있음 | Target Tracking은 실시간 메트릭 기반, Scheduled Scaling은 일정 시간에 자동 조정 |

#### 🔄 Launch Templates vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | Launch Templates | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| **용도** | EC2 인스턴스 생성 시 사양 정의 | Launch Configuration | 동적 템플릿 관리 시 Launch Templates 사용 |
| **확장성** | 자동 최신화(Version Control) 지원 | Launch Configuration | 고정된 템플릿만 사용 가능 |
| **비용** | 템플릿 버전 관리로 비용 최적화 가능 | Launch Configuration | 단일 버전만 관리 |
| **지연시간** | 최신 템플릿이 우선 적용 | Launch Configuration | 모든 인스턴스에 동일한 템플릿 적용 |

#### 📝 시험 대비 체크리스트
- [ ] Launch Templates의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  (예: "인스턴스 생성 시 동적 템플릿을 통해 자동 확장 및 관리")  
- [ ] Launch Templates를 선택해야 하는 시나리오를 알고 있는가?  
  (예: "동적 템플릿 관리, 버전 제어가 필요한 경우")  
- [ ] Launch Templates의 제한사항/한계를 알고 있는가?  
  (예: "버전 관리 기능 제한, 특정 템플릿만 지원")  
- [ ] Launch Templates와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  (예: "Launch Configuration vs Launch Templates의 버전 관리 차이")  
- [ ] Launch Templates의 비용 구조를 이해하고 있는가?  
  (예: "템플릿 버전 관리로 인한 리소스 최적화 비용 절감")

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Launch Templates를 떠올리세요:  
> - `Scaling Policy` (조정 정책)  
> - `Target Group` (대상 그룹)  
> - `Launch Template` (시작 템플릿)  
> - `Health Check` (상태 검사)  
> - `Auto Scaling Group` (자동 확장 그룹)

---

| [⬅️ Auto Scaling](./Auto-Scaling.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 1](../README.md) | [다음 Day ➡️](../../week2/day1/README.md) |

---
