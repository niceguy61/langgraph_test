---

| [⬅️ Lambda](./Lambda.md) | [📑 Day 4 목차](./README.md) | [🏠 Week 3](../README.md) | [SAM ➡️](./SAM.md) |

---

# API Gateway 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** API Gateway는 API 개발, 배포, 관리에 대한 AWS의 통합형 API 관리 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- 문제 1: 기존에는 직접 서버를 운영하며 API 요청을 처리해야 하며, 보안 인증, 로깅, 요청 제한 등의 기능을 개발자 스스로 구현해야 했습니다.  
- 문제 2: 클라이언트가 직접 백엔드 서비스에 접근해 보안 취약점이 발생할 수 있었고, 요청 트래픽 관리가 어려웠습니다.  
- 문제 3: 다양한 클라이언트(웹, 모바일, 기타)가 동일한 백엔드 서비스에 접근할 때 요청 처리 로직이 중복되며, 유지보수 비용이 증가했습니다.

**API Gateway로 해결:**
- 해결 1: API Gateway는 요청을 정적/동적 라우팅, 인증, 로깅, 요청 제한, 보안 등 모든 기능을 제공해 개발자 부담을 줄입니다.  
- 해결 2: API Gateway의 인증/허가 기능을 통해 클라이언트 접근을 제어하고, 요청 트래픽을 모니터링 및 제한해 보안을 강화합니다.  
- 해결 3: API Gateway는 단일 포인트에서 모든 클라이언트 요청을 수신하고, 백엔드 서비스에 요청을 전달해 요청 처리 로직을 중복하지 않게 구조화합니다.

### 비유로 이해하기
API Gateway는 호텔의 프론트 데스크 역할을 합니다. 고객이 요청할 때, 프론트 데스크가 요청을 분류하고 필요한 서비스에 전달하며, 보안 검사를 수행하고 고객의 요청을 효율적으로 처리합니다. 예를 들어, 고객이 객실 예약을 요청하면 프론트 데스크가 해당 요청을 예약 부서로 전달하고, 다른 요청은 다른 부서로 분배합니다. 이처럼 API Gateway는 클라이언트와 백엔드 서비스 사이의 중간 매개체로, 요청 처리를 단순화하고 보안을 강화합니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 외부 클라이언트가 내부 서비스에 접근할 때 | 기업 내부 API를 외부 파트너에게 노출하는 경우 |
| 시나리오 2 | 클라우드 기반 API 서비스를 제공할 때 | 웹/모바일 앱에서 사용하는 기업용 API 서비스 |
| 시나리오 3 | 다중 백엔드 서비스를 통합할 때 | SaaS 플랫폼에서 다양한 백엔드 서비스를 통합 관리하는 경우 |

**이럴 때 API Gateway를 선택하세요:**
- ✅ 상황 1: 클라이언트가 직접 백엔드 서비스에 접근하는 보안 문제를 해결해야 할 때  
- ✅ 상황 2: 다양한 클라이언트가 동일한 백엔드 서비스에 접근해야 할 때  
- ✅ 상황 3: API 요청 트래픽을 모니터링하고 제한해야 할 때  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ 상황: 백엔드 서비스가 특정 네트워크 환경에 고정되어 있을 때 → **VPC Endpoint**  
- ❌ 상황: API 요청 처리 로직이 복잡하고 람다 함수에 의존할 때 → **AWS Lambda**  
- ❌ 상황: API 요청이 전 세계 사용자에게 제공되어 지연 최소화가 필요할 때 → **CloudFront + API Gateway**

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **AWS Lambda** | 백엔드 로직 처리 및 서버리스 아키텍처 구현 | API Gateway → Lambda → DB/Storage |
| **Amazon S3** | API 요청에 필요한 정적 자원(이미지, 문서 등) 제공 | API Gateway → S3 (Static Files) |
| **Amazon DynamoDB** | API 요청에 필요한 데이터 저장 및 검색 | API Gateway → Lambda → DynamoDB |

**자주 사용되는 아키텍처 패턴:**
```
[사용자] → [CloudFront] → [API Gateway] → [Lambda] → [DynamoDB]
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| API 요청 수 | $0.0045/요청 | 월 100,000 요청 무료 |
| API 테스트 요청 수 | $0.0015/요청 | 월 100,000 요청 무료 |
| API 게이트웨이 관리 | $0.0002/월 | 항상 무료 |

**비용 최적화 팁:**
1. 💡 팁 1: 프리티어 한도 내에서 테스트 및 개발을 완료해 과금을 최소화하세요.  
2. 💡 팁 2: 요청 수를 모니터링하고, 과도한 트래픽은 CloudFront + API Gateway로 분산 처리하세요.  
3. 💡 팁 3: Lambda 함수를 통해 API 로직을 처리해 서버리스 아키텍처로 비용을 절감하세요.

> **⚠️ 비용 주의:** API 요청 수가 급증할 경우 예상치 못한 비용이 발생할 수 있으므로, 요청 수를 모니터링하고, 필요 시 API 요청 수 제한을 설정하세요.

## 📚 핵심 개념

### 개념 1: Lambda 실행 역할 및 VPC 내 Lambda 배포
Lambda 실행 역할은 AWS Lambda 함수가 실행 시 사용하는 IAM 역할로, 함수가 AWS 서비스나 외부 리소스에 접근할 수 있는 권한을 제공합니다. 이 역할은 Lambda가 S3, DynamoDB, 다른 AWS 서비스와 상호작용할 때 필수적입니다. VPC 내 Lambda 배포는 Lambda 함수가 AWS Virtual Private Cloud(VPC) 내부에 실행되도록 설정하는 기능으로, 네트워크 보안 정책을 강화하고 민감한 데이터를 외부에서 보호하는 데 사용됩니다.  
Lambda 실행 역할은 권한 관리의 최소 권한 원칙을 따르고, VPC 내 Lambda는 내부 네트워크를 통해 외부 리소스에 접근하는 기능을 제공합니다. 이 두 기능은 보안과 유연성을 동시에 확보하는 데 중요한 역할을 합니다.  
#### 왜 중요한가?
- **보안 강화**: 실행 역할은 함수가 필요한 권한만 부여해 보안 위험을 줄입니다.  
- **네트워크 격리**: VPC 내 Lambda는 내부 네트워크를 통해 외부 리소스에 접근해 데이터 유출을 방지합니다.  

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-|-|
| **Lambda 실행 역할** | Lambda 함수가 AWS 서비스나 외부 리소스에 접근할 수 있는 IAM 역할 | S3 버킷에서 데이터를 읽기 위해 `s3:GetObject` 권한 부여 |
| **VPC 내 Lambda** | Lambda 함수가 AWS VPC 내부에서 실행되도록 설정 | 내부 네트워크를 통해 DynamoDB에 접근 |
| **VPC 설정 옵션** | Lambda가 VPC에 연결할 때 고정 IP, 보안 그룹, 서브넷 선택 | 고정 IP로 연결해 IP 블랙리스트 적용 |

> **💡 Tip:** 실행 역할은 IAM 정책으로 세부 권한을 설정하고, VPC 내 Lambda는 네트워크 보안 정책에 따라 보안 그룹과 서브넷을 정확히 구성해야 합니다.

---

### 개념 2: Lambda@Edge 아키텍처 및 API Gateway 차이
Lambda@Edge는 AWS CloudFront를 통해 글로벌 네트워크에서 Lambda 함수를 실행하는 기능으로, 콘텐츠를 분산 처리해 지연 시간을 줄입니다. API Gateway의 REST/HTTP/WebSocket는 각각 다른 사용 케이스에 최적화된 API 관리 서비스입니다. REST API는 JSON 기반의 HTTP 요청/응답을 처리하고, HTTP API는 간소화된 설계로 비용 효율성을 제공하며, WebSocket은 실시간 양방향 통신을 지원합니다.  
Lambda@Edge는 클라우드 프론트의 요청/응답 단계에서 Lambda 함수를 실행해 콘텐츠를 가공하거나 필터링할 수 있습니다. 이는 글로벌 사용자에게 빠른 응답을 제공하는 데 유리합니다.  
#### 작동 원리
1. **CloudFront 요청 수신**: 사용자의 요청이 CloudFront에 도달합니다.  
2. **Lambda@Edge 함수 실행**: 요청 또는 응답 단계에서 Lambda 함수가 실행됩니다.  
3. **응답 전달**: 처리된 응답이 사용자에게 전달됩니다.  

> **💡 Tip:** Lambda@Edge는 CloudFront와 함께 사용해야 하며, HTTP API는 비용 효율적이고 간단한 API 설계에 적합합니다.

---

### 개념 3: Lambda 계층 구조
Lambda 계층은 공유 코드, 라이브러리, 설정을 Lambda 함수에 배포하는 방법으로, 여러 함수 간 코드 중복을 줄이고 관리 효율성을 높입니다. 계층은 AWS Lambda에 등록되어 함수 실행 시 자동으로 로드되며, 버전 관리 및 의존성 관리를 통해 안정성을 확보합니다.  
Lambda 계층은 함수의 크기를 줄이고, 개발 생산성을 높이며, 팀 내 공유 자산으로 활용할 수 있습니다. 특히, 대규모 애플리케이션에서 동일한 라이브러리를 여러 함수에 재사용할 때 유용합니다.  
#### 주요 특징
1. **공유 코드 재사용**: 여러 Lambda 함수가 동일한 코드를 공유해 유지보수를 간소화합니다.  
2. **버전 관리**: 계층의 버전을 관리해 의존성 충돌을 방지합니다.  
3. **확장성**: 새로운 라이브러리나 기능이 추가될 때 계층을 갱신해 함수에 즉시 반영됩니다.  

> **💡 Tip:** 계층은 AWS Lambda 콘솔에서 생성하고, 함수에 연결해 사용할 수 있으며, 의존성 충돌을 피하기 위해 버전 관리가 필수적입니다.

## 🖥️ AWS 콘솔에서 API Gateway 사용하기

### Step 1: API Gateway 서비스 접속
1. AWS Management Console에 로그인합니다  
   - URL: [https://console.aws.amazon.com](https://console.aws.amazon.com)  
2. 상단 검색창에서 **"API Gateway"**를 입력 후 검색합니다  
3. 검색 결과에서 **"API Gateway"**를 클릭하여 서비스로 이동합니다  

> **📸 화면 확인:** API Gateway 대시보드가 표시되면 정상입니다. 대시보드에는 생성된 API 목록, 통계, 최근 활동이 표시됩니다.

---

### Step 2: [주요 작업 1 - 리소스 생성]
1. **새로운 API 생성**  
   - 메뉴에서 **"Create API"** 버튼을 클릭합니다  
   - **REST API** 또는 **HTTP API**를 선택합니다 (WebSocket은 별도의 설정 필요)  
   - API 이름을 입력하고, **Resource** 및 **Method**를 구성합니다 (예: `GET /hello`)  
   - **Integration Type**에서 **Lambda Function**을 선택합니다  

2. **Lambda 함수 연결 설정**  
   - **Lambda Function** 섹션에서 **"Create a new Lambda function"**을 클릭합니다  
   - **Execution Role**을 선택하거나 새로운 역할을 생성합니다 (IAM 권한 부여 필요)  
   - Lambda 함수의 **ARN**을 입력하거나, **Lambda Console**에서 ARN을 복사해 붙입니다  

3. **API Gateway 통합 확인**  
   - **Method Response** 및 **Integration Response**를 설정합니다 (HTTP 상태 코드, 헤더, 본문 정의)  
   - **Deploy API** 버튼을 클릭하여 배포합니다 (Stage 이름 입력)  

> **📸 화면 확인:** **"Resources"** 탭에서 생성된 리소스와 메서드가 표시되고, **"Actions"** 메뉴에서 **"Deploy API"**를 클릭할 수 있어야 합니다.

---

### Step 3: [주요 작업 2 - 설정/구성]
1. **실행 역할 (Execution Role) 설정**  
   - Lambda 함수가 AWS 서비스(예: S3, DynamoDB)와 상호작용할 경우, **Execution Role**에 해당 권한을 추가해야 합니다  
   - IAM 콘솔에서 역할을 확인하거나, API Gateway의 **"Integration Response"**에서 권한을 수정할 수 있습니다  

2. **VPC 내 Lambda 함수 연결**  
   - Lambda 함수가 VPC 내부에 배포된 경우, **"VPC"** 설정을 활성화합니다  
   - API Gateway의 **"Integration Response"**에서 **"Private Subnets"**과 **"Security Groups"**를 선택합니다  
   - **Lambda Function**의 VPC 설정과 동일한 네트워크 구성이 필요합니다  

3. **Lambda@Edge 활성화**  
   - **HTTP API**에서 **"Lambda@Edge"**를 사용하려면, **"Edge Optimization"**을 활성화해야 합니다  
   - **"Actions"** > **"Enable Edge Optimization"**을 클릭하고, Lambda 함수를 연결합니다  

> **⚠️ 주의:** Lambda@Edge는 HTTP API에서만 지원되며, REST API에서는 사용할 수 없습니다. 또한, VPC 내 Lambda 함수는 API Gateway의 **Private Subnets**와 연결되어야 합니다.

---

### Step 4: 설정 확인 및 테스트
1. **생성된 리소스 확인 방법**  
   - **"Resources"** 탭에서 생성된 API, 메서드, Lambda 연결을 확인합니다  
   - **"Stages"** 탭에서 배포한 Stage의 상태를 확인합니다 (예: `prod`)  

2. **상태 확인 방법**  
   - **"Actions"** > **"View Usage"**를 클릭해 API 사용량을 확인합니다  
   - **"Monitoring"** 탭에서 요청 수, 오류율, 지연 시간 등을 분석합니다  

3. **정상 동작 테스트 방법**  
   - **"Test"** 버튼을 클릭해 POST/GET 요청을 시뮬레이션합니다  
   - **"Invoke URL"**을 복사해 브라우저나 Postman에서 요청을 보내고 응답을 확인합니다  
   - CLI 명령어 예시:  
     ```bash
     curl -X GET https://<api-id>.execute-api.<region>.amazonaws.com/<stage>/hello
     ```

> **💡 Tip:** 프리티어는 100,000개의 요청/월까지 무료로 사용 가능합니다. 실제 환경에서는 **Usage Plans** 및 **API Key**를 사용해 요금을 관리해야 합니다.

## ⌨️ AWS CLI로 API Gateway 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**스크린샷 설명:**  
1. AWS CLI 버전 확인 시 `aws --version` 명령어 실행 결과는 `aws-cli/2.x.x Python/3.x.x` 형식으로 표시됩니다.  
2. `aws sts get-caller-identity`는 IAM 사용자 ID, 계정 번호, 지역 정보를 반환합니다.  
3. `aws configure get region`은 현재 설정된 리전(예: `us-east-1`)을 확인합니다.

---

### 예제 1: API Gateway 리소스 조회
```bash
# [API Gateway REST API 목록 조회]
aws apigateway get-resources --rest-api-id <rest-api-id> --query '[].id' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--rest-api-id` | 조회할 API Gateway ID | `1234567890abcdef` |
| `--query` | 결과 필터링 | `'$.items[*].id'` |
| `--output` | 출력 형식 | `json`, `table`, `text` |

**예상 출력:**
```
|      id       |
+---------------+
| 1234567890abc |
```

> **💡 Tip:** `--rest-api-id`를 생략하면 모든 REST API 목록을 조회합니다.

---

### 예제 2: API Gateway REST API 생성
```bash
# [REST API 생성]
aws apigateway create-rest-api --name "MyAPI" --description "Sample API"
```

**필수 옵션:**
- `--name`: API 이름 (예: `MyAPI`)
- `--description`: API 설명 (선택 사항)

**예상 출력:**
```json
{
    "id": "1234567890abcdef",
    "name": "MyAPI",
    "description": "Sample API"
}
```

> **⚠️ 주의:** 생성된 API ID는 이후 배포 및 인터그레이션 시 필수적으로 사용됩니다.

---

### 예제 3: API Gateway REST API 수정
```bash
# [REST API 이름 수정]
aws apigateway update-rest-api --rest-api-id <rest-api-id> --patch-operations "[{\"op\":\"replace\",\"path\":\"/name\",\"value\":\"UpdatedAPI\"}]"
```

**옵션 설명:**
- `--rest-api-id`: 수정할 API ID
- `--patch-operations`: JSON 형식의 수정 사항 (예: 이름 변경)

**예상 출력:**
```json
{
    "id": "1234567890abcdef",
    "name": "UpdatedAPI"
}
```

---

### 예제 4: API Gateway REST API 삭제
```bash
# [REST API 삭제]
aws apigateway delete-rest-api --rest-api-id <rest-api-id>

# 삭제 확인
aws apigateway get-resources --rest-api-id <rest-api-id>
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. `--force` 옵션 없이 실행 시 완전히 제거됩니다.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws apigateway get-resources --rest-api-id <rest-api-id>
aws apigateway get-api --rest-api-id <rest-api-id>

# 생성
aws apigateway create-rest-api --name "MyAPI"

# 수정
aws apigateway update-rest-api --rest-api-id <rest-api-id> --patch-operations "[{\"op\":\"replace\",\"path\":\"/name\",\"value\":\"NewName\"}]"

# 삭제
aws apigateway delete-rest-api --rest-api-id <rest-api-id>
```

---

### 💡 비용 및 프리티어 활용 팁
- **API Gateway는 요청 수에 따라 요금이 발생**합니다.  
- **프리티어**: 100,000 건의 요청/월 제공 (1년간)  
- **비용 절감 방법**:  
  1. `usagePlan`과 `usagePlanKey`로 요청 제한 설정  
  2. `cache`와 `requestValidation` 옵션 활성화  
  3. `Metrics`를 통해 요청 패턴 분석 후 최적화  

> **💡 Tip:** CLI로 배포한 API는 [AWS Console](https://console.aws.amazon.com/apigateway/)에서 테스트 및 모니터링이 가능합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 API Gateway 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **API Gateway REST/HTTP/WebSocket API의 차이점**  
   - 설명: 시험에서 API Gateway의 3가지 API 유형(REST, HTTP, WebSocket)의 기능적 차이와 적절한 사용 시나리오를 묻는 문제가 빈번합니다. REST API는 HTTP 메서드(POST/GET 등)를 기반으로 하며, HTTP API는 더 간단한 구조로 비용 효율적입니다. WebSocket은 양방향 통신을 지원합니다.  
   - 키워드: `HTTP API`, `REST API`, `WebSocket`

2. **Lambda와의 연동 및 실행 역할 설정**  
   - 설명: Lambda 함수와 API Gateway의 연동 방식, Lambda 실행 역할(IAM Role)의 중요성, 그리고 권한 부여 방식(예: IAM 정책, Resource-Based Policy)은 필수적으로 이해해야 합니다.  
   - 키워드: `Lambda 실행 역할`, `IAM 정책`, `API Gateway 연동`

3. **Lambda@Edge 아키텍처 및 활용 사례**  
   - 설명: Lambda@Edge를 사용해 CDN(예: CloudFront)에서 전역 로직을 실행하는 방식은 고성능 및 저비용의 서버리스 아키텍처를 시험에서 자주 다룹니다.  
   - 키워드: `Lambda@Edge`, `CloudFront`, `전역 로직`

4. **API Gateway의 비용 구조 및 프리티어 제한**  
   - 설명: API Gateway의 요금 구조(요청 수, 데이터 전송량, 프리티어 한도)는 비용 최적화 문제에서 자주 출제됩니다. 예: 10만 요청/월이 프리티어의 한계입니다.  
   - 키워드: `요청 수`, `프리티어`, `요금 구조`

5. **API Gateway의 제한사항 및 보안 설정**  
   - 설명: API Gateway의 제한사항(예: 500개의 API 리소스, 100개의 스테이지)과 보안 설정(예: 인증, 암호화, VPC 통신)은 시험에서 자주 묻는 주제입니다.  
   - 키워드: `VPC 통신`, `인증`, `제한사항`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | REST API와 HTTP API의 차이를 혼동할 수 있음 | HTTP API는 더 간단한 구조로 비용 효율적, REST API는 HTTP 메서드 기반 |
| 함정 2 | Lambda 실행 역할과 IAM 정책의 역할을 혼동할 수 있음 | Lambda 실행 역할은 함수 실행 권한, IAM 정책은 리소스 접근 권한 |
| 함정 3 | API Gateway의 VPC 통신 설정과 Lambda의 VPC 배포를 혼동할 수 있음 | API Gateway VPC 통신은 외부 요청을 VPC 내로 전달, Lambda VPC 배포는 함수 자체가 VPC에 배포 |

#### 🔄 API Gateway vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | API Gateway | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | HTTP API, REST API, WebSocket을 통한 API 관리 | ALB(로드 밸런서), CloudFront(캐시 서비스) | API 관리 및 통합이 필요한 경우 |
| 확장성 | 요청 수 및 스테이지 수 제한 | ALB는 확장성에 유연 | 고성능 및 대규모 트래픽 처리 시 |
| 비용 | 요청 수 기반 요금 | ALB는 트래픽 기반 요금 | 저비용 및 단순한 API 관리 시 |
| 지연시간 | API Gateway 자체가 CDN 기능 제공 | CloudFront와 결합 시 저지연 | 저지연 및 글로벌 접근 시 |

#### 📝 시험 대비 체크리스트
- [ ] API Gateway의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  (예: "HTTP API를 관리하고 Lambda와 연동하여 서버리스 애플리케이션을 구축하는 서비스")
- [ ] API Gateway를 선택해야 하는 시나리오를 알고 있는가?  
  (예: "HTTP 통신을 기반으로 하는 API 관리, Lambda와의 연동")
- [ ] API Gateway의 제한사항/한계를 알고 있는가?  
  (예: "최대 500개 API 리소스, 100개 스테이지, 10만 요청/월 프리티어")
- [ ] API Gateway와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  (예: "ALB는 로드 밸런서로 복잡한 트래픽 처리, CloudFront는 캐싱 기능 제공")
- [ ] API Gateway의 비용 구조를 이해하고 있는가?  
  (예: "요청 수 기반 요금, 프리티어 한도 10만 요청/월")

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 API Gateway를 떠올리세요:  
> - `HTTP API`  
> - `Lambda 연동`  
> - `요청 수`  
> - `WebSocket`  
> - `VPC 통신`

---

| [⬅️ Lambda](./Lambda.md) | [📑 Day 4 목차](./README.md) | [🏠 Week 3](../README.md) | [SAM ➡️](./SAM.md) |

---
