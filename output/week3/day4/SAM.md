---

| [⬅️ API Gateway](./API-Gateway.md) | [📑 Day 4 목차](./README.md) | [🏠 Week 3](../README.md) | [다음 Day ➡️](../day5/README.md) |

---

# SAM 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** SAM는 AWS의 **Serverless Application Model**을 기반으로 **Lambda 함수 및 API Gateway**를 통합하여 **서버리스 애플리케이션을 효율적으로 개발하고 배포**할 수 있는 도구입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** Lambda 함수를 배포할 때 API Gateway, DynamoDB, S3 등 여러 AWS 서비스를 별도로 설정해야 하며, 의존성 관리가 복잡했습니다.  
- **문제 2:** 배포 과정에서 환경 변수, 실행 역할, 네트워크 설정 등 다양한 구성 옵션을 수동으로 조정해야 하며, 실수로 인한 배포 실패가 자주 발생했습니다.  
- **문제 3:** Lambda@Edge나 VPC 내 Lambda와 같은 고급 기능을 사용할 때 복잡한 아키텍처 설계가 필요해, 개발 생산성이 저하되었습니다.

**SAM으로 해결:**
- **해결 1:** SAM 템플릿을 통해 Lambda, API Gateway, DynamoDB 등 다양한 서비스를 단일 파일로 정의해, 복잡한 설정을 자동화합니다.  
- **해결 2:** CLI나 콘솔을 통해 단계별 배포를 수행하며, 환경 변수나 실행 역할을 쉽게 관리할 수 있습니다.  
- **해결 3:** Lambda@Edge나 VPC 내 Lambda와 같은 고급 기능을 템플릿으로 정의해, 네트워크 구성이나 보안 설정을 간단하게 적용할 수 있습니다.

### 비유로 이해하기
**SAM은 건축가의 설계 도구입니다.**  
이전에는 각 벽돌, 도장, 전기 설비를 별도로 구매해 직접 조립해야 했지만, SAM은 미리 준비된 건축 키트를 제공해, 모든 구성 요소를 한 번에 조립할 수 있습니다.  
예를 들어, API Gateway는 벽돌, DynamoDB는 전기 설비, Lambda는 도장으로 비유할 수 있습니다. SAM은 이 모든 것을 하나의 설계도로 묶어, 효율적으로 집을 짓는 데 도움을 줍니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | **서버리스 API 개발** | SaaS 플랫폼에서 사용자 데이터를 실시간으로 처리하는 REST API 구축 |
| 시나리오 2 | **이벤트 기반 데이터 처리** | 클라우드 이벤트(예: S3 파일 업로드)를 Lambda로 처리해 데이터 분석 및 저장 |
| 시나리오 3 | **CDN 통한 글로벌 가속화** | CloudFront와 Lambda@Edge를 연동해 지역별 콘텐츠 최적화 |

**이럴 때 SAM을 선택하세요:**
- ✅ **서버리스 아키텍처 설계 시**  
- ✅ **다양한 AWS 서비스 통합이 필요한 경우**  
- ✅ **빠른 프로토타입 개발 및 배포가 필요할 때**

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **장기적인 마스터 서버 관리가 필요할 때** → **EC2/eks**  
- ❌ **고성능 데이터베이스가 필수적일 때** → **RDS/Aurora**  
- ❌ **고정된 IP 주소가 필요할 때** → **Elastic IP**  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **API Gateway** | HTTP/REST 요청을 Lambda로 전달 | `User → API Gateway → Lambda` |
| **DynamoDB** | 데이터 저장 및 조회 기능 제공 | `Lambda → DynamoDB` |
| **CloudFront** | Lambda@Edge로 글로벌 콘텐츠 최적화 | `User → CloudFront → Lambda@Edge → Origin` |

**자주 사용되는 아키텍처 패턴:**
```
User → CloudFront → Lambda@Edge → S3
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **Lambda 실행 시간** | $0.20/GB-sec | 12개월 무료 (100만 초) |
| **API Gateway 요청** | $0.00000035/요청 | 12개월 무료 (100만 요청) |
| **DynamoDB 읽기/쓰기** | $0.25/1,000 요청 | 7일 무료 (100만 요청) |

**비용 최적화 팁:**
1. 💡 **프리티어를 꽉 채우기:** 100만 요청/초의 한계를 넘기기 전에 서비스를 출시하세요.  
2. 💡 **Lambda 메모리 최적화:** 필요하지 않은 메모리 크기로 설정해 비용 절감.  
3. 💡 **API Gateway 무료 트래픽 활용:** 100만 요청/월을 초과하지 않도록 모니터링.

> **⚠️ 비용 주의:** **Lambda@Edge**는 CloudFront와 연동 시 **API Gateway 요금이 적용되지 않음**에 주의하세요.  
> **⚠️ 비용 주의:** **DynamoDB**는 읽기/쓰기 요청 기준으로 요금이 적용되므로, 트래픽 예측이 필수적입니다.  

--- 

## 📌 핵심 개념 (Key Concepts)
- **Lambda 실행 역할:** Lambda 함수가 AWS 서비스에 접근할 수 있는 권한을 정의하는 IAM 역할.  
- **VPC 내 Lambda:** VPC 내부에서 Lambda를 실행해 네트워크 보안을 강화하고, 내부 서비스에 접근 가능.  
- **Lambda@Edge:** CloudFront를 통해 글로벌 지역에서 Lambda 함수를 실행해 콘텐츠 최적화.  
- **API Gateway REST/HTTP/WebSocket 차이:**  
  - **REST API:** HTTP 메서드(POST/GET)를 사용해 JSON 형식으로 데이터 전송.  
  - **HTTP API:** REST보다 더 간단한 설정으로 저비용, 저지연 API 제공.  
  - **WebSocket:** 실시간 양방향 통신을 위한 WebSocket 프로토콜 지원.  
- **Lambda 계층:** 공통 라이브러리나 의존성을 여러 함수에서 공유해, 배포 크기 최소화.  

---

## 🧪 실습 예제 (CLI & 콘솔)
**1. Lambda 함수 생성 (CLI)**
```bash
sam build
sam deploy --guided
```

**2. API Gateway 연동 (콘솔)**
- AWS 콘솔 → **Services → Lambda** → 함수 선택 → **Actions → Configure triggers**  
- **API Gateway**를 선택하고, HTTP 메서드(예: POST) 및 경로(`/hello`) 설정.

**3. VPC 내 Lambda 배포 (CLI)**
```bash
sam build --template-file template.yaml
sam deploy --template-file template.yaml --stack-name vpc-lambda-stack
```
- `template.yaml`에 VPC 설정을 추가해 네트워크 구성.

**4. Lambda@Edge 활성화 (콘솔)**
- AWS 콘솔 → **CloudFront** → **Distribution** → **Edit** → **Lambda@Edge** 설정.  
- **Origin Request**에서 Lambda 함수 연결.  

**5. Lambda 계층 관리 (CLI)**
```bash
sam package --template-file template.yaml --output-template-file packaged.yaml
sam deploy --template-file packaged.yaml
```
- `template.yaml`에 `AWS::Serverless::LayerVersion` 정의.

## 📚 핵심 개념

### 개념 1: Lambda 실행 역할  
Lambda 함수가 AWS 서비스와 상호작용할 수 있도록 권한을 부여하는 **IAM 역할**입니다. Lambda는 자체 관리되는 실행 역할을 사용하여 S3, DynamoDB, EC2 등과 같은 서비스에 접근할 수 있습니다. 이 역할은 함수가 실행 시 필요한 권한을 정의하며, 보안과 리소스 관리를 효율적으로 수행합니다.  

#### 왜 중요한가?  
- **보안 강화**: 실행 역할을 통해 함수가 필요한 권한만 부여해, 불필요한 리소스 접근을 방지합니다.  
- **관리 효율성**: 역할을 공유하거나 재사용해, 여러 함수 간 권한 관리를 단순화합니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |  
|-----|-----|-----|  
| IAM 역할 생성 | Lambda 실행 시 사용하는 AWS IAM 역할을 생성합니다. | `aws iam create-role --role-name LambdaExecutionRole` |  
| 권한 정의 | `AWSLambdaBasicExecutionRole`과 같은 기본 정책 또는 커스텀 정책을 추가합니다. | `aws iam attach-role-policy --policy-arn arn:aws:iam::123456789012:policy/CustomPolicy` |  
| CLI 설정 | `sam build` 및 `sam deploy` 시 실행 역할을 자동으로 설정합니다. | `sam deploy --guided` |  

> **💡 Tip:** 프로덕션 환경에서는 실행 역할에 필요한 권한을 최소화하고, 로그를 통해 권한 부여 여부를 모니터링하는 것이 좋습니다.  

---

### 개념 2: VPC 내 Lambda 배포  
Lambda 함수를 **AWS VPC 내부에 배포**하여, 내부 네트워크에만 접근할 수 있도록 하는 기능입니다. 이는 고민밀도 데이터베이스나 내부 시스템과 통신해야 하는 경우에 유용합니다.  

#### 작동 원리  
1. **VPC 구성**: Lambda 함수를 배포할 VPC의 서브넷과 보안 그룹을 정의합니다.  
2. **NAT 게이트웨이 연결**: 공개 인터넷에 접근해야 하는 경우, NAT 게이트웨이를 통해 외부 리소스에 연결합니다.  
3. **내부 네트워크 통신**: VPC 내부의 EC2, RDS 등과 같은 리소스와의 통신이 가능해집니다.  

> **💡 Tip:** VPC 내 Lambda는 외부 인터넷 접근을 제한할 수 있으므로, 필요한 경우 NAT 게이트웨이를 사용해 연결성을 확보하세요.  

---

### 개념 3: Lambda@Edge 아키텍처  
Lambda@Edge는 **CloudFront 캐시에 직접 실행되는 Lambda 함수**로, 글로벌 네트워크에서 실시간으로 요청을 처리합니다. 이는 사용자 요청을 처리할 때 높은 성능과 저지연을 제공합니다.  

#### 주요 특징  
1. **Edge 레이어 실행**: Lambda 함수가 CloudFront의 글로벌 레이어에서 실행되어, 사용자 지역에 가까운 서버에서 처리됩니다.  
2. **실시간 처리**: 사용자 요청에 대해 즉시 처리할 수 있어, 개인화된 콘텐츠 제공이 가능합니다.  
3. **비용 효율성**: 요청별로 실행되며, 사용량에 따라 비용이 발생하므로 대규모 트래픽을 고려해야 합니다.  

> **💡 Tip:** 고성능 저지연이 필요한 콘텐츠 필터링, 동적 캐싱 등에 Lambda@Edge를 활용하세요.  

---

### 개념 4: API Gateway REST/HTTP/WebSocket 차이  
API Gateway는 REST API, HTTP API, WebSocket API를 제공하며, 각각의 사용 케이스와 기능이 다릅니다.  

#### 세부 요소  
| 유형 | 설명 | 예시 |  
|------|------|------|  
| REST API | HTTP 메서드(POST, GET 등)를 기반으로 요청을 처리합니다. | 사용자 인증, 데이터 저장 등 복잡한 로직이 필요한 경우 |  
| HTTP API | REST API보다 간소화된 API로, 저비용 저지연이 특징입니다. | 빠른 응답이 필요한 단순 API 호출 |  
| WebSocket API | WebSocket 프로토콜을 기반으로 실시간 양방향 통신을 지원합니다. | 채팅, 게임, 실시간 알림 등 |  

> **💡 Tip:** 실시간 통신이 필요한 경우 WebSocket API를, 단순한 API 호출은 HTTP API로 선택하세요.  

---

### 개념 5: Lambda 계층 구조  
Lambda 계층은 **공통 라이브러리나 의존성을 공유하는 구조**로, 여러 Lambda 함수에서 동일한 코드를 재사용할 수 있습니다. 이는 코드 중복을 줄이고 관리 효율성을 높입니다.  

#### 주요 특징  
1. **공유 라이브러리**: Python, Node.js 등에서 사용 가능한 라이브러리를 중앙에 관리합니다.  
2. **버전 관리**: 계층의 버전을 관리해, 여러 함수에서 특정 버전의 라이브러리를 사용할 수 있습니다.  
3. **비용 절감**: 라이브러리 관리를 통해 배포 및 유지보수 비용을 절감할 수 있습니다.  

> **💡 Tip:** 공통 코드를 분리해 Lambda 계층으로 관리하면, 함수 간 코드 중복을 줄이고 배포 속도를 높일 수 있습니다.

## 🖥️ AWS 콘솔에서 SAM 사용하기

### Step 1: SAM 서비스 접속
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - 로그인 후 `Services` 메뉴에서 `Compute` → `Serverless Application Model (SAM)`로 이동합니다.  
2. 상단 검색창에서 "Serverless Application Model (SAM)"을 검색합니다  
   - 검색 결과에서 `Serverless Application Model`을 클릭합니다.  

> **📸 화면 확인:** SAM 대시보드가 표시되면 정상입니다.  
> **💡 Tip:** SAM 대시보드는 애플리케이션 목록, 실행 상태, 로그 등을 확인할 수 있는 중심 화면입니다.

---

### Step 2: [주요 작업 1 - 리소스 생성]
1. **리소스 생성**  
   - `Create application` 버튼을 클릭합니다.  
   - 애플리케이션 이름을 입력합니다 (예: `MyFirstSAMApp`).  
   - `Runtime`을 선택합니다 (예: `Python 3.10`).  
   - `Add a Lambda function`을 선택하고, 함수 이름과 역할을 설정합니다.  
   - `Deploy` 버튼을 클릭하여 애플리케이션을 배포합니다.  

2. **API Gateway 설정**  
   - `Add an API Gateway REST API`를 선택합니다.  
   - `HTTP API` 또는 `REST API`를 선택하고, `Method` (GET/POST 등)를 설정합니다.  
   - `Integration` 탭에서 Lambda 함수를 연결합니다.  

3. **VPC 및 Lambda@Edge 설정 (선택 사항)**  
   - VPC를 사용하는 Lambda 함수라면, `VPC` 설정 탭에서 `Subnets`와 `Security Groups`를 선택합니다.  
   - Lambda@Edge를 사용하는 경우, `Edge` 옵션을 활성화하고, `Edge Lambda`를 선택합니다.  

> **📸 화면 확인:** `CloudFormation` 스택이 생성되고, Lambda 함수와 API Gateway가 연결된 상태를 확인합니다.  
> **⚠️ 주의:** Lambda 함수 실행 역할을 생성하지 않으면 권한 오류가 발생할 수 있습니다.

---

### Step 3: [주요 작업 2 - 설정/구성]
1. **실행 역할 설정**  
   - `IAM` 서비스에서 `Roles` → `Create role` → `Lambda`를 선택합니다.  
   - `Trust policy`를 설정하고, `Attach` 버튼으로 `AWSLambdaBasicExecutionRole`을 연결합니다.  

2. **Lambda 계층 추가**  
   - `Add a Lambda layer`를 선택하고, ZIP 파일을 업로드하거나 AWS 저장소에서 선택합니다.  
   - `Layer version`을 선택하고, Lambda 함수에 연결합니다.  

3. **API Gateway 정책 설정**  
   - `Permissions` 탭에서 `Resource-based policy`를 생성하고, Lambda 함수를 호출할 수 있도록 권한을 부여합니다.  
   - `Method response`와 `Integration response`를 설정하여 요청/응답 형식을 정의합니다.  

> **⚠️ 주의:** API Gateway의 `Resource`와 `Method`를 잘못 설정하면 요청이 처리되지 않을 수 있습니다.  
> **💡 Tip:** IAM 역할과 API Gateway 정책은 항상 엄격하게 설정해야 합니다.

---

### Step 4: 설정 확인 및 테스트
1. **리소스 확인 방법**  
   - `CloudFormation` 서비스에서 `Stacks`를 클릭하고, 생성된 스택을 선택합니다.  
   - `Resources` 탭에서 Lambda 함수, API Gateway, VPC 등이 정상적으로 생성되었는지 확인합니다.  

2. **상태 확인 방법**  
   - `CloudWatch` 서비스에서 `Logs` → `AWS Lambda` → 함수 이름을 선택하여 로그를 확인합니다.  
   - API Gateway의 `Monitor` 탭에서 요청 수와 상태 코드를 확인합니다.  

3. **정상 동작 테스트 방법**  
   - API Gateway의 `Test` 버튼을 클릭하고, GET/POST 요청을 보내어 응답을 확인합니다.  
   - Lambda 함수를 CLI로 호출:  
     ```bash
     aws lambda invoke --function-name MyLambdaFunction --payload '{"key":"value"}' response.json
     ```  
   - `CloudWatch`에서 `Logs Insights`로 함수 실행 로그를 분석합니다.  

> **💡 Tip:** SAM은 CLI와 콘솔 모두에서 사용 가능합니다. CLI로 배포할 경우 `sam build` 및 `sam deploy` 명령어를 사용하세요.  
> **⚠️ 주의:** SAM은 자동화된 배포를 지원하지만, 수동 설정이 필요한 경우 `CloudFormation`으로 대체해야 합니다.

## ⌨️ AWS CLI로 SAM 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**설명:**  
AWS CLI를 사용하기 전에 버전을 확인하고, AWS 계정의 자격 증명이 올바르게 설정되어 있는지 확인하세요. `aws configure get region`으로 현재 리전을 확인하여 리소스 생성 시 지역 설정을 확인합니다.

---

### 예제 1: Lambda 함수 목록 조회
```bash
# Lambda 함수 목록 조회
aws lambda list-functions --query '[].FunctionName' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 결과 필터링 | `'[].FunctionName'` (함수 이름 추출) |
| `--output` | 출력 형식 | `json`, `table`, `text` (테이블 형식으로 출력) |

**예상 출력:**
```
FunctionName
------------
my-sam-function
another-lambda
```

**설명:**  
AWS SAM으로 배포된 Lambda 함수 목록을 AWS CLI로 조회합니다. `--query` 옵션을 사용하여 명확한 결과를 얻을 수 있습니다.

---

### 예제 2: Lambda 함수 생성
```bash
# Lambda 함수 생성
aws lambda create-function \
    --function-name "MySAMFunction" \
    --runtime "python3.9" \
    --role "arn:aws:iam::123456789012:role/lambda-role" \
    --handler "lambda.handler" \
    --zip-file "fileb://lambda_function.zip"
```

**필수 옵션:**
- `--function-name`: Lambda 함수 이름
- `--runtime`: 실행 환경 (예: `python3.9`)
- `--role`: IAM 역할 ARN
- `--handler`: 처리 함수 경로
- `--zip-file`: ZIP 파일 경로

**예상 출력:**
```json
{
    "FunctionName": "MySAMFunction",
    "FunctionArn": "arn:aws:lambda:region:account-id:function:MySAMFunction",
    "Runtime": "python3.9",
    "Role": "arn:aws:iam::123456789012:role/lambda-role",
    "Handler": "lambda.handler",
    "State": "Active"
}
```

**설명:**  
SAM으로 생성된 Lambda 함수를 AWS CLI로 직접 생성할 수 있습니다. `--zip-file`은 Lambda 함수 코드를 포함한 ZIP 파일을 지정합니다.

---

### 예제 3: API Gateway REST API 생성
```bash
# REST API 생성
aws apigateway create-rest-api \
    --name "MySAMAPI" \
    --description "API for SAM application"
```

**필수 옵션:**
- `--name`: API 이름
- `--description`: API 설명

**예상 출력:**
```json
{
    "id": "1234567890",
    "name": "MySAMAPI",
    "description": "API for SAM application",
    "createdDate": "2023-10-01T12:34:56Z"
}
```

**설명:**  
SAM 애플리케이션과 연동된 REST API를 AWS CLI로 직접 생성할 수 있습니다. 이후 API Gateway를 Lambda와 연결해야 합니다.

---

### 예제 4: Lambda 함수 삭제
```bash
# Lambda 함수 삭제
aws lambda delete-function --function-name "MySAMFunction"

# 삭제 확인
aws lambda get-function --function-name "MySAMFunction"
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. `get-function` 명령어로 삭제 전 상태를 확인하세요.

**설명:**  
Lambda 함수를 삭제하기 위해 `delete-function` 명령어를 사용합니다. 삭제 후 `get-function`으로 상태를 확인할 수 있지만, 삭제된 함수는 더 이상 조회할 수 없습니다.

---

### 자주 사용하는 명령어 정리
```bash
# Lambda 함수 관리
aws lambda list-functions
aws lambda get-function --function-name "MySAMFunction"

# API Gateway 관리
aws apigateway get-rest-apis
aws apigateway create-resource --rest-api-id "1234567890" --path "/hello"
```

**설명:**  
Lambda 함수와 API Gateway를 AWS CLI로 관리할 수 있습니다. `list-functions`는 모든 Lambda 함수 목록을, `get-rest-apis`는 REST API 목록을 조회합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 SAM 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **Lambda 실행 역할 (Execution Role)**
   - 설명: Lambda 함수가 AWS 서비스에 접근하기 위해 필요한 권한을 관리하는 IAM 역할. 시험에서 실행 역할의 정의, 구성 방법, 및 권한 범위 설정이 주요 포인트로 출제됨.  
   - 키워드: `IAM 역할`, `AWS 서비스 접근`, `권한 범위`

2. **VPC 내 Lambda 배포**
   - 설명: Lambda 함수가 VPC 내에서 실행될 때 네트워크 설정, 사전 구성 필요성, 및 사설 IP 사용 여부에 대한 이해가 필수적. VPC 내 Lambda는 내부 네트워크 보안 및 특정 리소스 접근 시 필수적.  
   - 키워드: `VPC 통신`, `사설 IP`, `네트워크 보안`

3. **Lambda@Edge 아키텍처**
   - 설명: CloudFront와 연동하여 글로벌 지연 최소화하는 기능. Edge 레이어에서 Lambda 함수 실행 시, 요청 처리 로직의 위치 및 성능 영향에 대한 이해가 중요.  
   - 키워드: `CloudFront`, `Edge 레이어`, `글로벌 지연`

4. **API Gateway REST/HTTP/WebSocket 차이**
   - 설명: REST API는 HTTP 메서드 기반, HTTP API는 간소화된 설계, WebSocket은 양방향 통신을 지원. 시험에서 각 API 유형의 특징, 사용 사례, 및 성능 차이를 비교 분석해야 함.  
   - 키워드: `HTTP 메서드`, `양방향 통신`, `성능 비교`

5. **Lambda 계층 (Lambda Layer)**
   - 설명: 공통 라이브러리나 의존성을 중앙 집중화하여 관리하는 기능. 시험에서 계층의 구조, 배포 방법, 및 최적화 전략이 주요 테스트 항목.  
   - 키워드: `라이브러리 공유`, `의존성 관리`, `최적화 전략`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | API Gateway REST vs HTTP의 차이를 혼동해 질문에 답할 수 없음 | REST는 HTTP 메서드 기반, HTTP API는 간소화된 설계로, WebSocket은 양방향 통신 지원 |
| 함정 2 | VPC 내 Lambda 배포 시 사설 IP 사용 여부를 혼동 | VPC 내 Lambda는 사설 IP 사용 불가, NAT 게이트웨이 또는 VPN을 통해 외부 네트워크 접근 |
| 함정 3 | Lambda@Edge의 실행 위치를 잘못 파악 | Lambda@Edge는 CloudFront Edge 레이어에서 실행, 글로벌 지연 최소화에 최적화됨 |
| 함정 4 | Lambda 실행 역할과 IAM 정책의 차이를 혼동 | 실행 역할은 Lambda 자체의 권한, IAM 정책은 리소스에 대한 접근 권한을 정의 |
| 함정 5 | Lambda 계층의 배포 방식을 잘못 이해 | 계층은 ZIP 파일로 배포, Lambda 함수에서 `layers` 속성으로 참조 |

#### 🔄 SAM vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | SAM | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 서버리스 애플리케이션 개발 및 배포 도구 | AWS Lambda (계산 서비스) | 서버리스 아키텍처 구축 시 |
| 확장성 | 자동 확장 및 관리 기능 제공 | Serverless Framework (도구) | 커스터마이징이 필요한 경우 |
| 비용 | Lambda 실행 시간에 따라 요금 부과 | AWS Lambda (계산 서비스) | 실행 시간이 긴 작업 시 |
| 지연시간 | Lambda@Edge로 글로벌 지연 최소화 | API Gateway (HTTP/REST) | 글로벌 사용자 대상 서비스 시 |

#### 📝 시험 대비 체크리스트
- [ ] SAM의 핵심 목적을 한 문장으로 설명할 수 있는가? (서버리스 애플리케이션 개발 및 배포 도구)
- [ ] SAM를 선택해야 하는 시나리오를 알고 있는가? (VPC 내 Lambda, Lambda@Edge, API Gateway 연동)
- [ ] SAM의 제한사항/한계를 알고 있는가? (사설 IP 사용 불가, 네트워크 복잡성 증가)
- [ ] SAM과 비슷한 서비스의 차이점을 설명할 수 있는가? (AWS Lambda vs Serverless Framework)
- [ ] SAM의 비용 구조를 이해하고 있는가? (Lambda 실행 시간 기반, API Gateway 요청 수 기반)

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 SAM을 떠올리세요:  
> - `서버리스 아키텍처`  
> - `Lambda@Edge`  
> - `VPC 내 Lambda`  
> - `API Gateway REST/HTTP/WebSocket`  
> - `Lambda 계층`

---

| [⬅️ API Gateway](./API-Gateway.md) | [📑 Day 4 목차](./README.md) | [🏠 Week 3](../README.md) | [다음 Day ➡️](../day5/README.md) |

---
