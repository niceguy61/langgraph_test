---

| [⬅️ 이전 Day](../day3/README.md) | [📑 Day 4 목차](./README.md) | [🏠 Week 3](../README.md) | [API Gateway ➡️](./API-Gateway.md) |

---

# Lambda 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** Lambda는 서버리스 컴퓨팅을 위한 AWS의 '함수로서의 컴퓨팅' 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 서버 인프라 구축 및 관리에 고비용과 복잡도가 동반됨 (예: EC2 인스턴스 운영 시 서버 가동 시간, 확장성 관리 필요)  
- **문제 2:** 트래픽 피크 시 서버 자동 확장이 어렵고, 리소스 낭비가 발생함 (예: 하이브리드 클라우드 환경에서의 스케일링 관리 어려움)  
- **문제 3:** 코드 실행 시 서버 가동 비용이 발생해 비용 최적화가 어려움 (예: 24/7 서버 가동 시 고정 비용 발생)

**Lambda로 해결:**
- **해결 1:** 코드만 배포해 실행 시 자동으로 가상 머신 생성 및 종료 (서버 관리 부담 해소)  
- **해결 2:** 요청 수에 따라 자동 확장 (예: 1000개 요청 시 자동으로 10개 인스턴스 생성)  
- **해결 3:** 실행 시간에 따라 요금 결제 (예: 1초 실행 시 $0.0015 결제, 0초 실행 시 요금 없음)

### 비유로 이해하기
Lambda는 프리랜서 개발자를 비유할 수 있어요. 고객이 요청이 있을 때만 일하고, 요청이 없으면 아무것도 하지 않아요. 예를 들어, 사용자가 웹사이트에 요청을 보내면 Lambda가 즉시 실행되어 데이터를 처리하고, 요청이 끝나면 자동으로 종료됩니다. 이처럼 Lambda는 '필요할 때만 실행'하는 방식으로 비용을 최대한 절약합니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 실시간 데이터 처리나 분석이 필요한 경우 | AWS S3에서 파일 업로드 시 Lambda가 파일을 분석하여 데이터베이스에 저장 |
| 시나리오 2 | HTTP 요청 처리 및 API 엔드포인트 제공 | API Gateway와 연동해 사용자 요청을 Lambda 함수로 전달 |
| 시나리오 3 | 배경 작업 처리 및 비동기 처리 | 사용자 입력 후 시간이 오래 걸리는 작업(예: 이미지 리사이징)을 Lambda로 배경 처리 |

**이럴 때 Lambda를 선택하세요:**
- ✅ **요청량이 불규칙한 경우** (예: 사용자 피크 시 자동 확장)  
- ✅ **비용 최적화가 필요한 경우** (예: 24/7 운영이 아닌 단일 요청 처리 시)  
- ✅ **빠른 프로토타입 개발이 필요한 경우** (코드 배포만으로 서비스 시작 가능)

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **고정된 서버 자원이 필요한 경우** → **EC2** (예: 24/7 서버 운영)  
- ❌ **고성능 웹 애플리케이션이 필요한 경우** → **ECS** (예: 복잡한 백엔드 로직 처리)  
- ❌ **장기적인 데이터 저장이 필요한 경우** → **DynamoDB** (예: 실시간 데이터 저장 및 검색)  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **API Gateway** | HTTP 요청 전달 및 보안 관리 | User → API Gateway → Lambda → Database |
| **S3** | 파일 업로드 및 처리 | User → S3 → Lambda (파일 분석) → DynamoDB |
| **DynamoDB** | 실시간 데이터 저장 및 검색 | Lambda → DynamoDB (데이터 저장) → API Gateway (응답 전달) |

**자주 사용되는 아키텍처 패턴:**
```
User → CloudFront → Lambda (요청 처리) → S3 (데이터 저장)
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **요청 수 (1 million)** | $0.0015 / 요청 | 월 1,000,000 요청 무료 |
| **요청 시간 (1,000,000 ns)** | $0.00001667 / ns | 12개월 1,000,000 ns 무료 |
| **데이터 전송 (GB-seconds)** | $0.00001667 / GB-sec | 400,000 GB-sec 무료 |

**비용 최적화 팁:**
1. 💡 **요청 시간 최소화:** 코드 최적화 및 비동기 처리로 실행 시간 단축 (예: `async/await` 사용)  
2. 💡 **요청 수 감소:** 비필수 요청 필터링 (예: 로그 수집 시 필터링 로직 추가)  
3. 💡 **예약 용량 구독:** 고정된 요청량이 있을 경우 예약 용량 구독으로 비용 절감 (예: 100,000 요청/월 구독 시 60% 할인)  

> **⚠️ 비용 주의:** 예상치 못한 비용이 발생할 수 있는 상황 설명  
- **트래픽 피크:** 갑작스러운 사용자 증가로 인한 비용 증가 (예: 프로모션 시 10배 트래픽 증가)  
- **장기 실행:** Lambda 함수가 무한히 실행될 경우 과금 (예: `sleep(3600)`로 1시간 실행 시 $3.60 결제)  
- **데이터 전송:** 데이터 전송량이 클 경우 비용 증가 (예: 1GB 전송 시 $0.01667 결제)

## 📚 핵심 개념

### 개념 1: Lambda 실행 역할  
AWS Lambda 함수는 실행 역할(Execution Role)을 통해 AWS 서비스와 상호작용할 수 있는 권한을 얻습니다. 이 역할은 Lambda가 S3 버킷에 파일을 읽고 쓰거나 DynamoDB에서 데이터를 조회할 수 있도록 IAM 정책을 포함합니다. 실행 역할은 함수가 외부 자원에 접근할 때 필요한 권한을 관리하며, 보안을 위해 역할에 필요한 최소한의 권한만 부여하는 원칙을 따릅니다.  
Lambda는 실행 역할을 통해 자동으로 AWS 서비스에 접근할 수 있도록 설정되며, 이는 함수가 외부 서비스와 통신할 수 있는 기반이 됩니다.  
#### 왜 중요한가?  
- **보안성 강화**: 역할에 필요한 권한만 부여해 데이터 유출을 방지합니다.  
- **관리 효율성**: 단일 역할로 여러 함수를 관리할 수 있어 리소스를 절약합니다.  
#### 세부 요소  
| 요소 | 설명 | 예시 |  
|-----|-----|-----|  
| **IAM 역할** | Lambda가 AWS 서비스에 접근할 수 있는 권한을 정의합니다. | S3 버킷에 파일을 읽고 쓰는 권한을 부여합니다. |  
| **트러블슈팅** | 실행 역할이 없으면 "Access Denied" 오류가 발생합니다. | `aws lambda get-function --function-name my-function` 명령어로 역할 확인 가능. |  
| **사용 권장 사항** | AWS IAM 정책을 직접 작성해 역할을 세분화하세요. | `iam:PassRole` 권한을 포함한 역할을 생성합니다. |  
> **💡 Tip:** 실행 역할을 생성할 때는 `iam:PassRole` 권한을 포함해 Lambda가 역할을 사용할 수 있도록 설정해야 합니다.  

---

### 개념 2: VPC 내 Lambda 배포  
Lambda 함수는 AWS VPC(가상 개인 클라우드) 내에서 실행할 수 있습니다. 이는 특정 네트워크 환경에서의 보안성이나 프라이빗 자원 접근이 필요한 경우에 유용합니다. VPC 내 Lambda는 외부 인터넷에 접근할 수 없으며, 내부 자원(예: RDS, DynamoDB)에만 연결할 수 있습니다.  
Lambda가 VPC 내에서 실행되면, 네트워크 보안 정책에 따라 외부 서비스에 대한 접근이 제한됩니다. 이는 데이터 유출을 방지하고, 내부 네트워크에 대한 제어력을 높입니다.  
#### 작동 원리  
1. **VPC 연결 설정**: Lambda 함수 생성 시 VPC 및 서브넷을 선택해 네트워크 연결을 구성합니다.  
2. **보안 그룹 정의**: Lambda가 VPC 내에서 다른 서버와 통신할 수 있도록 보안 그룹을 설정합니다.  
3. **내부 자원 접근**: 설정된 네트워크 정책에 따라 RDS, EC2 인스턴스 등 내부 자원에 접근합니다.  
> **💡 Tip:** VPC 내 Lambda를 사용할 때는 네트워크 지연이 발생할 수 있으므로, 성능에 민감한 작업은 주의해야 합니다.  

---

### 개념 3: Lambda@Edge 아키텍처  
Lambda@Edge는 CloudFront의 글로벌 라우터에서 Lambda 함수를 실행해 콘텐츠를 실시간으로 처리할 수 있는 기능입니다. 이는 CDN(콘텐츠 전달 네트워크)에 배치된 Lambda가 사용자 요청을 처리해 응답 속도를 높입니다.  
Lambda@Edge는 사용자 요청이 CloudFront에 도달할 때, Edge 위치에서 Lambda 함수를 실행해 콘텐츠를 필터링하거나 변환할 수 있습니다. 이는 로컬에서 처리해 전송 대역폭을 줄이고, 사용자 경험을 개선합니다.  
#### 주요 특징  
1. **실시간 처리**: 사용자 요청이 도달하는 즉시 Edge 위치에서 처리해 응답 속도를 높입니다.  
2. **글로벌 확장**: AWS의 글로벌 데이터 센터에서 Lambda 함수를 실행해 전 세계 사용자에게 빠른 응답을 제공합니다.  
3. **비용 효율성**: 데이터 전송 비용을 줄이고, 네트워크 부하를 분산해 운영 비용을 절감합니다.  
> **💡 Tip:** 콘텐츠 필터링, 동적 캐싱, 요청 변환 등에 Lambda@Edge를 활용해 성능을 최적화하세요.

## 🖥️ AWS 콘솔에서 Lambda 사용하기

### Step 1: Lambda 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - 로그인 후, 상단 메뉴에서 **Services**를 클릭하고 "Lambda"를 검색합니다  
2. 검색 결과에서 **Lambda**를 클릭해 대시보드로 이동합니다  
   - **Lambda 대시보드**에 도착하면 "Functions" 탭이 표시됩니다  
   - 이 화면은 생성된 함수 목록, 실행 역할, 정책 등을 확인할 수 있습니다  

> **📸 화면 확인:** Lambda 대시보드가 표시되고 "Functions" 탭이 활성화된 상태인지 확인합니다  

---

### Step 2: [주요 작업 1 - 리소스 생성] Lambda 함수 생성  
1. **"Create function"** 버튼을 클릭합니다  
   - 이 버튼은 대시보드 상단에 위치합니다  
2. **Runtime** 선택  
   - 예: `Python 3.12` 또는 `Node.js 18`  
   - 선택한 런타임은 함수가 실행될 환경을 정의합니다  
3. **Function name** 입력  
   - 예: `MyFirstLambdaFunction`  
   - 이름은 알파벳, 숫자, 하이픈, 언더바만 사용 가능합니다  
4. **Execution role** 생성  
   - **"Create a new role"** 선택  
   - Lambda가 S3, DynamoDB 등 외부 서비스에 접근할 수 있도록 권한을 부여합니다  
   - 생성된 역할은 **IAM** 서비스에서 확인할 수 있습니다  

> **📸 화면 확인:** "Create function" 화면에서 런타임, 이름, 실행 역할 선택이 완료된 상태  

---

### Step 3: [주요 작업 2 - 설정/구성] 함수 설정 및 VPC 설정  
1. **Handler** 설정  
   - 예: `lambda_handler`  
   - Lambda가 실행할 메인 함수의 경로를 정의합니다  
2. **VPC 설정 (선택 사항)**  
   - **"VPC"** 탭에서 **"Specify VPC"**를 선택  
   - Lambda가 AWS VPC 내에서 실행되도록 설정합니다  
   - VPC 설정은 네트워크 보안, 데이터 전송 경로 등을 조절할 수 있습니다  
3. **Environment variables** 추가  
   - **"Environment variables"** 탭에서 키-값 쌍을 입력합니다  
   - 예: `API_ENDPOINT=https://api.example.com`  
   - 환경 변수는 함수 실행 시 동적으로 사용됩니다  

> **⚠️ 주의:** VPC 설정 시 **Lambda가 외부 인터넷에 접근할 수 있는지 확인**하세요. VPC 내 Lambda는 NAT 게이트웨이 또는 VPN을 통해 외부 서비스에 연결해야 합니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인**  
   - Lambda 대시보드에서 함수 이름을 클릭해 상세 정보 확인  
   - **"Configuration"** 탭에서 실행 역할, 환경 변수, VPC 설정 확인  
2. **상태 확인**  
   - **"Monitoring"** 탭에서 함수 실행 상태, 오류 로그 확인  
   - **"Executions"** 탭에서 지난 1일 내 실행 내역 확인  
3. **테스트 실행**  
   - **"Test"** 버튼 클릭 후, **"Test event"**에서 JSON 입력  
   - 예:  
     ```json
     {
       "key": "value"
     }
     ```
   - 테스트 결과는 **"Execution result"** 섹션에서 확인 가능  

> **💡 Tip:** 테스트 후 **"Publish version"** 버튼을 눌러 함수 버전을 고정해 안정성 확보하세요.  

---

### 실습 예제: API Gateway 연동  
1. **API Gateway 생성**  
   - Lambda 대시보드에서 함수를 클릭 → **"Actions"** → **"Create API Gateway"**  
   - REST API 또는 HTTP API 선택  
2. **Lambda 연동 설정**  
   - API Gateway에서 **"Integration"** 탭 → **"Lambda"** 선택  
   - 생성한 Lambda 함수를 선택하고 **"Save"**  
3. **테스트**  
   - API Gateway에서 생성된 URL을 통해 HTTP 요청을 보내고 응답 확인  

> **📸 화면 확인:** API Gateway의 "Test" 섹션에서 요청을 보내고 응답 코드(200 OK) 확인  

---

### 비용 및 프리티어 활용  
- **Lambda 프리티어**: 1,000,000 건의 요청/월, 1,000,000 초 실행 시간 제공  
- **비용 절약 팁**:  
  - **"Concurrent executions"**을 제한해 과다 사용 방지  
  - **"Reserved Concurrency"** 설정으로 비용 최적화  
  - **"Logging"**을 비활성화해 로그 저장 비용 절감  

> **⚠️ 주의:** VPC 내 Lambda는 외부 서비스 접근 시 **NAT 게이트웨이** 비용이 추가될 수 있습니다.  

---

### 체크리스트: Lambda 함수 생성 및 테스트  
- [ ] Lambda 서비스 접속 완료  
- [ ] Lambda 함수 생성 및 실행 역할 설정 완료  
- [ ] VPC 설정 및 환경 변수 추가 완료  
- [ ] API Gateway 연동 설정 완료  
- [ ] 함수 테스트 및 응답 확인 완료  
- [ ] 비용 절약 설정(예: 로깅 비활성화) 완료

## ⌨️ AWS CLI로 Lambda 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**설명:**  
- `aws --version`은 CLI 버전을 확인합니다.  
- `aws sts get-caller-identity`는 AWS 계정 ID, 유저명, 리전을 확인합니다.  
- `aws configure get region`은 현재 설정된 리전을 확인합니다.  
**⚠️ 주의:** AWS CLI를 사용하기 전에 `aws configure`로 액세스 키, 비밀 키, 리전, 프로필을 설정해야 합니다.

---

### 예제 1: Lambda 리소스 조회
```bash
# Lambda 함수 목록 조회
aws lambda list-functions --query 'Functions[*].FunctionName' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | JSON 결과 필터링 (예: `Functions[*].FunctionName`) | `'Functions[*].FunctionName'` |
| `--output` | 출력 형식 (json, table, text) | `table` |

**예상 출력:**
```
FUNCTION_NAME
-------------------
example-function
another-function
```

**💡 Tip:** `--query`를 사용해 특정 필드만 출력할 수 있습니다. 예: `Functions[*].FunctionArn`으로 ARN을 조회할 수 있습니다.

---

### 예제 2: Lambda 함수 생성
```bash
# Lambda 함수 생성
aws lambda create-function \
    --function-name "MyLambdaFunction" \
    --runtime "python3.9" \
    --role "arn:aws:iam::123456789012:role/lambda-role" \
    --handler "lambda_function.lambda_handler" \
    --zip-file "fileb://lambda_function.zip"
```

**필수 옵션:**
- `--function-name`: 함수 이름 (예: `MyLambdaFunction`)
- `--runtime`: 실행 환경 (예: `python3.9`)
- `--role`: 실행 역할 ARN (IAM 역할 필요)
- `--handler`: 코드 진입점 (예: `lambda_function.lambda_handler`)
- `--zip-file`: 로컬 ZIP 파일 경로

**예상 출력:**
```json
{
    "FunctionArn": "arn:aws:lambda:us-east-1:123456789012:function:MyLambdaFunction",
    "FunctionName": "MyLambdaFunction",
    "Runtime": "python3.9",
    "Role": "arn:aws:iam::123456789012:role/lambda-role",
    "Handler": "lambda_function.lambda_handler",
    "LastModified": "2023-10-05T00:00:00.000+00:00",
    "CodeSize": 1234,
    "Description": "",
    "Timeout": 3,
    "MemorySize": 128,
    "Version": "$LATEST"
}
```

**⚠️ 주의:** `--role`에 설정된 IAM 역할은 `lambda:InvokeFunction` 권한이 필요합니다.

---

### 예제 3: Lambda 함수 수정
```bash
# Lambda 함수 타임아웃 수정
aws lambda update-function-configuration \
    --function-name "MyLambdaFunction" \
    --timeout 5
```

**옵션 설명:**
- `--timeout`: 실행 시간 제한 (초 단위)
- `--memory-size`: 메모리 할당량 (MB 단위)
- `--description`: 함수 설명 추가

**예상 출력:**
```json
{
    "FunctionArn": "arn:aws:lambda:us-east-1:123456789012:function:MyLambdaFunction",
    "FunctionName": "MyLambdaFunction",
    "Runtime": "python3.9",
    "Role": "arn:aws:iam::123456789012:role/lambda-role",
    "Handler": "lambda_function.lambda_handler",
    "LastModified": "2023-10-05T00:00:00.000+00:00",
    "CodeSize": 1234,
    "Description": "",
    "Timeout": 5,
    "MemorySize": 128,
    "Version": "$LATEST"
}
```

---

### 예제 4: Lambda 함수 삭제
```bash
# Lambda 함수 삭제
aws lambda delete-function --function-name "MyLambdaFunction"

# 삭제 확인
aws lambda get-function --function-name "MyLambdaFunction"
```

**⚠️ 주의:** 삭제는 되돌릴 수 없습니다. 삭제 후 `get-function` 명령어는 오류를 반환합니다.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws lambda list-functions
aws lambda get-function --function-name "MyLambdaFunction"

# 생성
aws lambda create-function \
    --function-name "MyLambdaFunction" \
    --runtime "python3.9" \
    --role "arn:aws:iam::123456789012:role/lambda-role" \
    --handler "lambda_function.lambda_handler" \
    --zip-file "fileb://lambda_function.zip"

# 수정
aws lambda update-function-configuration \
    --function-name "MyLambdaFunction" \
    --timeout 5

# 삭제
aws lambda delete-function --function-name "MyLambdaFunction"
```

**💡 Tip:** `--zip-file` 옵션은 로컬 ZIP 파일을 업로드해야 합니다. S3 버킷에서 ZIP 파일을 사용할 경우 `--code` 옵션을 사용해야 합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Lambda 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **Lambda 실행 역할 (Execution Role)**  
   - 설명: Lambda 함수가 AWS 서비스와 상호작용할 때 필요한 권한을 관리하는 IAM 역할. 실행 역할이 없으면 함수가 정상 작동하지 않음. 시험에서 권한 오류 문제는 빈번하게 출제됨.  
   - 키워드: `IAM 역할`, `Execution Role`

2. **VPC 내 Lambda 배포**  
   - 설명: Lambda 함수가 VPC 내에서 실행될 때 네트워크 설정, NAT 게이트웨이, 보안 그룹 구성이 필수적. VPC 내 Lambda는 외부 인터넷 접근이 제한되므로 사전에 트래픽 전달 방식을 설정해야 함.  
   - 키워드: `VPC`, `NAT 게이트웨이`

3. **Lambda@Edge 아키텍처**  
   - 설명: CloudFront와 연동해 글로벌 CDN에서 Lambda 함수를 실행하는 기능. 지역별 지연 최소화와 실시간 콘텐츠 변환에 사용. 시험에서 CDN과 Lambda@Edge의 조합 문제는 자주 등장.  
   - 키워드: `CloudFront`, `글로벌 배포`

4. **API Gateway REST/HTTP/WebSocket 차이**  
   - 설명: REST API는 HTTP 메서드 지원, HTTP API는 간소화된 설계, WebSocket은 실시간 양방향 통신. 시험에서 API 유형에 따른 적절한 서비스 선택 문제는 필수.  
   - 키워드: `HTTP 메서드`, `양방향 통신`

5. **Lambda 계층 (Layer) 구조**  
   - 설명: 공통 라이브러리나 의존성을 중앙집중식으로 관리해 함수 간 재사용성 향상. 시험에서 계층의 구성 방식, 최대 크기 제한(50MB)은 핵심 개념.  
   - 키워드: `라이브러리 재사용`, `50MB 제한`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "Lambda 실행 역할은 항상 생성해야 한다"는 문장이 주어졌을 때, 역할이 없어도 작동 가능하다고 오인 | Lambda는 실행 역할이 필수적. 역할 없이는 함수 실행 불가 |
| 함정 2 | VPC 내 Lambda는 외부 인터넷에 접근할 수 없다고 생각해 NAT 게이트웨이를 무조건 사용해야 한다고 오인 | NAT 게이트웨이 대신 VPC 피어링 또는 IP 테이블 설정도 가능 |
| 함정 3 | WebSocket API는 HTTP API보다 느리다고 오인 | WebSocket은 실시간 통신에 최적화된 반면, HTTP API는 단순한 REST 요청에 더 적합 |

#### 🔄 Lambda vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | Lambda | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 이벤트 기반, 서버리스 함수 실행 | EC2 (서버 기반) | 서버 관리가 필요 없는 경우 |
| 확장성 | 자동 확장, 수천 개의 함수 동시 실행 | EC2 (수동 확장) | 트래픽 급증 시 유연성 필요 |
| 비용 | 실행 시간 기반 요금제 (0.000001667 USD/100ms) | EC2 (예약/사용 시간 기반) | 단일 요청 처리 시 비용 효율적 |
| 지연시간 | 100ms 이내 초고속 실행 | EC2 (OS 부팅 시간 포함) | 실시간 처리 시 필수 |

#### 📝 시험 대비 체크리스트
- [ ] Lambda의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  → "이벤트 기반으로 서버리스 함수를 실행하여 서버 관리 부담을 줄인 서비스"
- [ ] Lambda를 선택해야 하는 시나리오를 알고 있는가?  
  → "단일 요청 처리, 빈도가 낮은 작업, 자동 확장 필요 시"
- [ ] Lambda의 제한사항/한계를 알고 있는가?  
  → "VPC 내 실행 시 외부 인터넷 접근 제한, 15분 실행 시간 제한, 50MB 계층 크기 제한"
- [ ] Lambda와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  → "Lambda는 서버리스, EC2는 서버 기반, Step Functions는 작업 흐름 관리"
- [ ] Lambda의 비용 구조를 이해하고 있는가?  
  → "실행 시간 기반 요금, 100ms 단위 요금, 100만 건 이상 요청 시 분할 요금 적용"

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Lambda를 떠올리세요:  
> - `서버리스`  
> - `이벤트 기반`  
> - `비용 효율적`  
> - `실행 역할`  
> - `VPC 내 배포`  
> - `계층 관리`  
> - `CloudFront 연동`

---

| [⬅️ 이전 Day](../day3/README.md) | [📑 Day 4 목차](./README.md) | [🏠 Week 3](../README.md) | [API Gateway ➡️](./API-Gateway.md) |

---
