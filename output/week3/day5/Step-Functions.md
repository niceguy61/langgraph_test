---

| [⬅️ 이전 Day](../day4/README.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 3](../README.md) | [EventBridge ➡️](./EventBridge.md) |

---

# Step Functions 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** Step Functions는 AWS의 **워크플로우 자동화 및 상태 관리**를 위한 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 복잡한 작업 흐름을 수동으로 관리해야 하며, 중간에 실패 시 전체 프로세스를 재시작해야 합니다. 예: S3에서 데이터를 처리하고 RDS에 저장하는 작업 중, RDS 저장 실패 시 데이터를 다시 로드해야 합니다.  
- **문제 2:** 작업 상태를 실시간으로 모니터링할 수 없어 디버깅이 어려웠습니다. 예: Lambda 함수 실행 중 오류가 발생했지만, 어떤 단계에서 문제가 생겼는지 파악하기 어려웠습니다.  
- **문제 3:** 작업 중간에 중단된 상태를 저장하지 못해, 재시작 시 데이터 손실 또는 중복 처리 문제가 발생했습니다. 예: SQS 큐에서 메시지를 처리 중 시스템 다운으로 인해 메시지가 소실되었습니다.

**Step Functions로 해결:**
- **해결 1:** 시각화된 상태 머신을 통해 작업 흐름을 정의하고, 실패 시 자동으로 이전 상태로 되돌아가며 재시도합니다.  
- **해결 2:** 실행 중인 작업의 상태를 실시간으로 추적하고, 로그 및 알림을 통해 문제를 신속히 진단합니다.  
- **해결 3:** 작업 중단 시 상태를 저장하고, 재시작 시 중단된 상태에서부터 계속 처리합니다. 예: SQS FIFO 큐와 연동해 메시지 순서를 보장하며, 중단 시 메시지를 복원합니다.

### 비유로 이해하기
Step Functions는 요리 레시피를 기록하는 것처럼, 복잡한 작업 단계를 순서대로 정의합니다. 예를 들어, "재료 준비 → 조리 → 조리 완료 알림"과 같은 단계를 하나씩 실행하고, 중간에 실패해도 이전 단계로 돌아가 재시도합니다. 이는 워크플로우를 명확하게 관리하고, 오류를 최소화하는 데 유용합니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|-----|------------|
| 시나리오 1 | 복잡한 작업 흐름을 자동화해야 할 때 | 고객 등록 시, 데이터 검증 → DB 저장 → 이메일 발송을 순차적으로 처리 |
| 시나리오 2 | 병렬 처리가 필요한 작업을 관리할 때 | S3에서 데이터를 다운로드 후, Lambda로 분석하고 SNS로 알림을 동시에 전송 |
| 시나리오 3 | 상태 관리가 필요한 작업을 처리할 때 | Kinesis 데이터 스트리밍 시, 데이터를 여러 Lambda 함수로 분산 처리하고 상태를 기록 |

**이럴 때 Step Functions를 선택하세요:**
- ✅ **작업 흐름이 복잡하거나 중단 시 복구가 필요**  
- ✅ **실시간 모니터링 및 로그 기록이 필수적**  
- ✅ **다양한 서비스(예: Lambda, SNS, SQS)와 연동이 필요**  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **단순한 메시지 전송만 필요** → **SNS/SQS**  
- ❌ **단일 함수 실행만 필요** → **AWS Lambda**  
- ❌ **작업 상태 관리가 필요 없고, 빠른 처리가 우선** → **Kinesis Data Streams**  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|----------|------------------|
| **EventBridge** | 이벤트 기반 워크플로우 트리거 | User → EventBridge → Step Functions → Lambda |
| **SQS 표준/FIFO** | 메시지 큐를 통한 작업 대기열 관리 | Step Functions → SQS FIFO → Lambda |
| **Kinesis Data Streams** | 실시간 데이터 스트리밍 처리 | Kinesis Data Streams → Step Functions → Lambda |

**자주 사용되는 아키텍처 패턴:**
```
User → API Gateway → Step Functions → Lambda (데이터 처리) → SNS (알림)
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **상태 전환** | $0.00015 / 상태 전환 | 월 1,000,000 상태 전환 무료 |
| **데이터 전송** | $0.10 / GB (AWS 내) | 12개월 10GB 무료 |
| **API 요청** | $0.000001 / 요청 | 100,000 요청 무료 |

**비용 최적화 팁:**
1. 💡 **병렬 처리로 실행 시간 단축**: 병렬 상태를 사용해 작업을 동시에 실행해 비용 절감  
2. 💡 **적절한 상태 전환 수 제한**: 불필요한 상태 전환을 줄여 비용을 최소화  
3. 💡 **프리티어 활용**: 초기 테스트 시 프리티어를 사용해 비용을 절감  

> **⚠️ 비용 주의:** 작업이 오래 실행되거나 데이터 전송량이 급증할 경우, 예상치 못한 비용이 발생할 수 있습니다. AWS Cost Explorer를 활용해 비용을 모니터링하세요.

## 📚 핵심 개념

### 개념 1: Step Functions 상태 머신과 워크플로우 오케스트레이션
Step Functions는 AWS에서 제공하는 **서버리스 워크플로우 오케스트레이션 서비스**로, 복잡한 작업 흐름을 **상태 머신**으로 정의하여 실행합니다. 상태 머신은 단계별 작업을 정의하고, 각 단계의 성공/실패 조건을 설정해 작업을 자동으로 조율합니다. 이는 데이터베이스 트랜잭션, 배치 처리, API 호출 등 다양한 작업을 효율적으로 관리할 수 있습니다.  

#### 왜 중요한가?
- **복잡한 작업 흐름 단순화**: 여러 서비스와 인터페이스를 연결하는 복잡한 로직을 상태 머신으로 정리해 가독성과 유지보수성을 높입니다.  
- **자동 재시도 및 실패 처리**: 실패 시 자동으로 재시도하거나, 복구 로직을 포함해 시스템의 신뢰성을 강화합니다.  
- **비용 효율성**: 서버리스 기반으로 실행되므로, 사용량에 따라만 요금이 청구되어 작은 규모의 작업에도 적합합니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| 상태 머신 | 작업 단계와 전환 조건을 정의한 기계적 모델 | `StartState`, `TaskState`, `EndState`로 구성된 JSON 파일 |
| 작업 유형 | Lambda, DynamoDB, S3 등 다양한 서비스 연동 | `InvokeLambda`, `PutItem`, `GetObject` 등 |
| 이벤트 트리거 | 외부 이벤트(예: S3 파일 업로드)로 작업 시작 | EventBridge 또는 S3 버킷 알림으로 트리거 |

> **💡 Tip:** 상태 머신은 JSON 형식으로 정의되며, AWS 콘솔에서 시각화하여 흐름을 확인할 수 있습니다. 예를 들어, `StartAt`을 시작 상태로 지정하고, 각 단계를 `Next`로 연결해 작업 흐름을 구성할 수 있습니다.  

---

### 개념 2: EventBridge 이벤트 패턴과 룰 생성  
EventBridge는 **클라우드 내/외부 이벤트를 실시간으로 수집**하고, Step Functions에 전달하는 서비스입니다. 이벤트 패턴을 정의해 특정 조건을 만족하는 이벤트만 Step Functions로 전달함으로써, 불필요한 처리를 줄이고 시스템 효율성을 높입니다.  

#### 작동 원리  
1. **이벤트 생성**: S3 버킷에서 파일이 업로드될 때, S3 이벤트가 생성됩니다.  
2. **이벤트 패턴 정의**: EventBridge에서 `source`가 `aws.s3`이고 `detail-type`이 `s3:AllObjectCreate`인 이벤트를 필터링합니다.  
3. **Step Functions 트리거**: 필터링된 이벤트가 Step Functions에 전달되어, 예를 들어 데이터 처리 작업이 자동으로 시작됩니다.  

> **💡 Tip:** EventBridge 룰을 생성할 때, `Event Pattern` 필드에서 `source`, `detail-type`, `resources` 등을 정의해 이벤트를 정밀하게 필터링할 수 있습니다.  

---

### 개념 3: SQS 표준/FIFO 큐의 메시지 순서 보장  
SQS는 **메시지 큐 서비스**로, Step Functions와 연동하여 작업을 분산 처리합니다. **FIFO 큐**는 메시지 순서를 보장해, 작업의 일관성을 유지하는 데 필수적입니다. 반면, 표준 큐는 메시지 순서를 보장하지 않지만, 높은吞吐량을 지원합니다.  

#### 주요 특징  
1. **메시지 순서 보장**: FIFO 큐는 동일한 큐 내 메시지가 순서대로 처리되며, 이는 데이터 일관성에 중요한 역할을 합니다.  
2. **중복 메시지 방지**: FIFO 큐는 동일한 메시지가 중복되어 처리되지 않도록 설계되어, 데이터 오염을 방지합니다.  
3. **성능 최적화**: 표준 큐는 고성능을 위해 병렬 처리를 지원하며, 대규모 작업 처리에 적합합니다.  

> **💡 Tip:** FIFO 큐는 `FIFO`를 포함한 큐 이름으로 생성해야 하며, 메시지 전송 시 `MessageGroupId`를 지정해 순서를 보장할 수 있습니다.  

---

### 개념 4: SNS 토픽 구독과 메시지 전달  
SNS는 **메시지 퍼블리싱 및 구독 서비스**로, Step Functions에서 작업 완료 후 알림을 전달하는 데 사용됩니다. SNS 토픽을 구독해 특정 이벤트가 발생할 때 알림을 받도록 설정하면, 작업 결과를 실시간으로 다른 시스템에 전달할 수 있습니다.  

#### 주요 특징  
1. **대규모 메시지 배포**: SNS는 단일 메시지를 수백만 개의 구독자에게 동시에 전달할 수 있어, 알림 시스템에 적합합니다.  
2. **구독 유형 다양성**: 토픽을 구독하는 방식으로는 `Email`, `SMS`, `SQS` 등 다양한 방식을 지원해, 다중 플랫폼 알림을 가능하게 합니다.  
3. **메시지 필터링**: 구독 시 `FilterPolicy`를 설정해, 특정 조건에 맞는 메시지만 전달할 수 있습니다.  

> **💡 Tip:** SNS 토픽을 생성하고, Step Functions 작업 완료 후 `PublishMessage` API를 사용해 메시지를 전송하면, 실시간 알림을 구현할 수 있습니다.  

---

### 개념 5: Kinesis Data Streams 샤드와 데이터 인그레션  
Kinesis Data Streams는 **실시간 데이터 흐름을 처리하는 서비스**로, Step Functions와 연동해 대량의 데이터를 처리하고 분석합니다. **샤드(Shard)**는 데이터 흐름을 파티셔닝해, 병렬 처리를 가능하게 합니다.  

#### 주요 특징  
1. **실시간 데이터 처리**: Kinesis는 초단위로 데이터를 전송해, 실시간 분석에 적합합니다.  
2. **샤드 확장성**: 필요에 따라 샤드 수를 증가시켜 처리량을 확장할 수 있어, 대규모 데이터 처리에 유리합니다.  
3. **데이터 인그레션**: Step Functions는 Kinesis의 데이터를 받아 작업을 수행하고, 결과를 다른 시스템으로 전달할 수 있습니다.  

> **💡 Tip:** Kinesis 데이터 스트림을 생성하고, Step Functions에서 `GetRecords` API를 사용해 데이터를 읽어 처리할 수 있습니다. 이는 로그 분석, 실시간 통계 등에 활용됩니다.

## 🖥️ AWS 콘솔에서 Step Functions 사용하기

### Step 1: Step Functions 서비스 접속  
1. **AWS Management Console 로그인**  
   - [https://console.aws.amazon.com](https://console.aws.amazon.com)에 접속하여 AWS 계정으로 로그인합니다.  
   - **IAM 권한**: Step Functions 사용 권한이 부여된 IAM 사용자로 로그인해야 합니다.  

2. **서비스 검색**  
   - 상단 메뉴의 검색창에 **"Step Functions"**를 입력하고, 검색 결과에서 **Step Functions**를 클릭합니다.  
   - **검색 결과**: "Step Functions" 서비스가 목록에 표시되면 서비스로 이동합니다.  

> **📸 화면 확인:** Step Functions 대시보드가 열리고, "State Machines" 목록이 표시되면 정상입니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **State Machine 생성**  
   - 왼쪽 메뉴에서 **"Create state machine"** 버튼을 클릭합니다.  
   - **템플릿 선택**: "Hello World" 예제 템플릿을 선택하여 기본 작업 흐름을 생성합니다.  

2. **리소스 설정**  
   - **State Machine 이름**: `HelloWorldStateMachine`로 입력합니다.  
   - **Execution Role**: 기본 역할을 선택하거나, **IAM 역할 생성**을 클릭해 새 역할을 생성합니다.  
     - 역할은 Lambda 함수 실행 권한이 포함되어야 합니다.  
   - **Tags**: 필요 시 태그를 추가하여 리소스를 관리합니다.  

3. **확인 및 생성**  
   - **Next** 버튼을 클릭해 설정을 저장하고, **Create**를 선택합니다.  
   - 생성 후, "State Machines" 목록에서 새 리소스가 표시되면 성공입니다.  

> **📸 화면 확인:** "Create state machine" 폼에서 이름, 역할, 태그 입력 후 생성 완료 화면.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **Execution Role 설정**  
   - 생성된 State Machine을 클릭해 **"Execution role"** 설정을 확인합니다.  
   - 역할이 **AWSLambdaBasicExecutionRole**을 포함하고 있는지 확인합니다.  
   - 권한 부족 시, IAM 콘솔에서 역할에 `AWSLambdaBasicExecutionRole`을 추가해야 합니다.  

2. **State Machine 정의 수정**  
   - **"Edit"** 버튼을 클릭해 JSON 형식의 작업 흐름을 직접 수정할 수 있습니다.  
   - 예: `StartAt: HelloWorld, EndAt: HelloWorld`와 같은 구조를 사용합니다.  

3. **EventBridge 연동 설정 (선택적)**  
   - **"Events"** 탭에서 EventBridge를 연동해 State Machine을 자동으로 실행할 수 있도록 설정합니다.  
   - 이벤트 유형 및 규칙을 정의해 외부 시스템과의 통합을 완성합니다.  

> **⚠️ 주의:** IAM 역할에 필요한 권한을 명확히 설정하지 않으면 State Machine 실행 시 오류가 발생할 수 있습니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **리소스 확인**  
   - **"State Machines"** 목록에서 생성된 State Machine을 클릭해 기본 정보를 확인합니다.  
   - **ARN** 및 **Status** 필드가 정상적으로 표시되면 리소스가 생성되었습니다.  

2. **실행 상태 확인**  
   - **"Executions"** 탭에서 실행 내역을 확인합니다.  
   - **"Start execution"** 버튼을 클릭해 State Machine을 수동으로 실행하고, 상태를 실시간으로 모니터링합니다.  

3. **테스트 및 로그 확인**  
   - 실행 후, **"Execution details"**에서 **"Logs"** 탭을 통해 Lambda 함수 로그를 확인합니다.  
   - **CloudWatch Logs**에서 실행 결과를 디버깅할 수 있습니다.  

> **📸 화면 확인:** Execution 시작 후, "Execution details"에서 로그 및 상태 변화를 확인하는 화면.  

---  
**✅ 학습 완료 체크리스트**  
- [ ] Step Functions 콘솔 접속  
- [ ] State Machine 생성 및 설정  
- [ ] Execution Role 및 정의 확인  
- [ ] 실행 테스트 및 로그 분석  
- [ ] EventBridge 연동 (선택적)

## ⌨️ AWS CLI로 Step Functions 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**💡 Tip:**  
AWS CLI를 사용하기 전에 `aws configure`로 액세스 키, 비밀번호, 리전, 프로파일을 설정해야 합니다.  
`aws configure set region us-east-1` 명령어로 리전을 설정할 수 있습니다.

---

### 예제 1: Step Functions 리소스 조회
```bash
# [Step Functions 상태 머신 목록 조회]
aws stepfunctions list-state-machines --query '[].name' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| --query | 결과 필터링 | '[].name' (상태 머신 이름 추출) |
| --output | 출력 형식 | `json`, `table`, `text` (테이블 형식으로 보기) |

**예상 출력:**
```
NAME
example-state-machine
another-state-machine
```

**💡 Tip:**  
`--query`는 JSON 결과를 필터링하고, `--output table`은 테이블 형식으로 보기 쉽게 합니다.  
`list-state-machines` 명령어는 현재 리전에 있는 모든 상태 머신을 조회합니다.

---

### 예제 2: Step Functions 리소스 생성
```bash
# [Step Functions 상태 머신 생성]
aws stepfunctions create-state-machine \
    --name "example-state-machine" \
    --definition file://state-machine.json \
    --role-arn "arn:aws:iam::123456789012:role/lambda-role"
```

**필수 옵션:**
- `--name`: 상태 머신 이름 (예: `example-state-machine`)
- `--definition`: 상태 머신 정의 파일 경로 (JSON 형식)  
  (예: `file://state-machine.json` 또는 JSON 스트링)
- `--role-arn`: Lambda 함수 실행에 필요한 IAM 역할 ARN

**예상 출력:**
```json
{
    "stateMachineArn": "arn:aws:states:us-east-1:123456789012:stateMachine:example-state-machine",
    "name": "example-state-machine"
}
```

**⚠️ 주의:**  
`--definition`는 JSON 형식으로 작성해야 하며, `file://` 경로를 사용하거나 JSON 스트링을 전달할 수 있습니다.  
예시 JSON 정의:
```json
{
  "Comment": "A simple example of a state machine",
  "StartAt": "Hello",
  "States": {
    "Hello": {
      "Type": "Succeed",
      "End": true
    }
  }
}
```

---

### 예제 3: Step Functions 리소스 수정
```bash
# [Step Functions 상태 머신 정의 수정]
aws stepfunctions update-state-machine \
    --state-machine-arn "arn:aws:states:us-east-1:123456789012:stateMachine:example-state-machine" \
    --definition file://updated-state-machine.json
```

**필수 옵션:**
- `--state-machine-arn`: 수정할 상태 머신 ARN
- `--definition`: 수정된 JSON 정의 파일 경로

**💡 Tip:**  
`update-state-machine`는 기존 상태 머신을 덮어쓰므로, 변경 사항을 확인한 후 실행해야 합니다.

---

### 예제 4: Step Functions 리소스 삭제
```bash
# [Step Functions 상태 머신 삭제]
aws stepfunctions delete-state-machine \
    --state-machine-arn "arn:aws:states:us-east-1:123456789012:stateMachine:example-state-machine"

# 삭제 확인
aws stepfunctions describe-state-machine \
    --state-machine-arn "arn:aws:states:us-east-1:123456789012:stateMachine:example-state-machine"
```

**⚠️ 주의:**  
`delete-state-machine`는 되돌릴 수 없는 작업입니다.  
`describe-state-machine` 명령어는 삭제 후 에러를 반환합니다.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws stepfunctions list-state-machines
aws stepfunctions describe-state-machine --state-machine-arn "arn"

# 생성
aws stepfunctions create-state-machine --name "name" --definition "definition" --role-arn "arn"

# 수정
aws stepfunctions update-state-machine --state-machine-arn "arn" --definition "definition"

# 삭제
aws stepfunctions delete-state-machine --state-machine-arn "arn"
```

**💡 Tip:**  
`--definition`는 JSON 스트링 또는 파일 경로를 지정할 수 있으며, `--role-arn`은 Lambda 함수 실행 권한을 부여해야 합니다.  
AWS CLI는 AWS Step Functions 서비스의 `list-state-machines`, `create-state-machine`, `update-state-machine`, `delete-state-machine` 명령어를 지원합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Step Functions 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **포인트 1: 상태 머신과 워크플로우 오케스트레이션**  
   - 설명: Step Functions는 상태 머신을 통해 복잡한 작업 흐름을 제어하고, Lambda, DynamoDB, S3 등 다양한 서비스를 조율하는 핵심 기능입니다. 시험에서는 상태 머신의 정의, 작업 전환(logic)의 구성, 오류 처리 메커니즘 등이 주요 포인트로 출제됩니다.  
   - 키워드: `상태 머신`, `워크플로우 오케스트레이션`, `작업 전환`

2. **포인트 2: 비동기 처리와 유연한 작업 흐름**  
   - 설명: Step Functions는 비동기 처리를 지원하며, 작업 중단/재시작, 병렬 처리, 조건별 분기 등 유연한 흐름을 제공합니다. 시험에서는 `Wait` 상태, `Choice` 상태, `Parallel` 상태 등이 자주 등장합니다.  
   - 키워드: `비동기 처리`, `조건별 분기`, `병렬 처리`

3. **포인트 3: 이벤트 기반 통합과 서비스 연동**  
   - 설명: EventBridge, SNS, SQS 등과 연동해 이벤트 기반 아키텍처를 구축할 수 있습니다. 시험에서는 `Input/Output` 정의, `Event Pattern` 설정, `Service Integration` 구조 등이 중요합니다.  
   - 키워드: `이벤트 기반 아키텍처`, `Service Integration`, `Input/Output`

4. **포인트 4: 비용 구조와 리소스 관리**  
   - 설명: Step Functions는 작업 실행 시간에 따라 요금이 청구되며, 최대 실행 시간 제한(15분)과 리소스 제한(예: 상태 머신 크기)이 중요합니다. 시험에서는 `Execution Time`, `Parallel State`의 비용 차이, `Cost Optimization` 전략이 출제됩니다.  
   - 키워드: `비용 구조`, `최대 실행 시간`, `리소스 제한`

5. **포인트 5: 오류 처리와 재시도 전략**  
   - 설명: `Retry`, `Catch`, `Fail` 상태를 통해 오류를 관리해야 합니다. 시험에서는 `Error Handling` 메커니즘, `Retry Policy` 설정, `Dead Letter Queue` 연동이 주요 포인트입니다.  
   - 키워드: `Error Handling`, `Retry Policy`, `Dead Letter Queue`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | Step Functions와 Lambda의 역할을 혼동해 문제를 푸는 경우 | Step Functions는 작업 흐름을 조율하는 **오케스트레이션 도구**로, Lambda는 **compute** 역할을 합니다. |
| 함정 2 | 상태 머신에서 `Wait` 상태 사용 시 최대 시간 제한을 간과하는 경우 | `Wait` 상태는 최대 15분만 실행 가능하며, 초과 시 `Execution Time Exceeded` 오류 발생 |
| 함정 3 | `Choice` 상태에서 조건을 잘못 작성해 로직이 예상과 달라지는 경우 | 조건은 `$.status`와 같은 동적 변수를 사용해야 하며, `AND/OR` 조합을 정확히 설정해야 합니다. |

#### 🔄 Step Functions vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | Step Functions | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 복잡한 작업 흐름을 조율하는 **오케스트레이션 도구** | AWS Lambda (Compute) / AWS Batch (Batch Processing) | 작업 흐름 조율이 필요한 경우 |
| 확장성 | 상태 머신 구조로 **동적 확장** 가능 | Lambda는 **동적 확장** 가능, Batch는 **고정된 작업 단위** | 작업 유형에 따라 선택 (예: 병렬 처리 시 Step Functions) |
| 비용 | 실행 시간 기반 요금 | Lambda는 요청 기반, Batch는 작업 단위 기반 | 비용 모델에 따라 선택 (예: 비동기 처리 시 Step Functions) |
| 지연시간 | 상태 전환 시 **지연 발생** | Lambda는 실시간 처리, Batch는 작업 큐 기반 | 실시간 처리가 필요한 경우 Lambda 선택 |

#### 📝 시험 대비 체크리스트
- [ ] Step Functions의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  ✅ "작업 흐름을 상태 머신으로 조율하여 복잡한 작업을 관리하는 도구"
- [ ] Step Functions를 선택해야 하는 시나리오를 알고 있는가?  
  ✅ "다양한 서비스 간 조율, 병렬 처리, 조건별 분기, 오류 처리가 필요한 경우"
- [ ] Step Functions의 제한사항/한계를 알고 있는가?  
  ✅ "최대 실행 시간 15분, 상태 머신 크기 제한, 비동기 처리 시 지연 발생"
- [ ] Step Functions와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  ✅ "Lambda는 Compute, Batch는 Batch Processing, EventBridge는 이벤트 기반 통신"
- [ ] Step Functions의 비용 구조를 이해하고 있는가?  
  ✅ "작업 실행 시간에 따라 요금, 최대 실행 시간 제한, 리소스 제한"

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Step Functions를 떠올리세요:  
> - `상태 머신`, `워크플로우 오케스트레이션`, `작업 전환`, `비동기 처리`, `조건별 분기`, `병렬 처리`, `Error Handling`, `Retry Policy`, `Dead Letter Queue`, `Input/Output`

---

| [⬅️ 이전 Day](../day4/README.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 3](../README.md) | [EventBridge ➡️](./EventBridge.md) |

---
