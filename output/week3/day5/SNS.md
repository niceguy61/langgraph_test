---

| [⬅️ SQS](./SQS.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 3](../README.md) | [Kinesis ➡️](./Kinesis.md) |

---

# SNS 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** SNS는 메시지 전송을 위한 AWS의 확장 가능한 통신 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 전통적인 메시지 처리 방식은 중앙 집중식 구조로 인해 확장성 제한이 있었고, 복잡한 네트워크 구성이 필요했습니다. 예를 들어, 애플리케이션 간 실시간 통신을 위해 직접 서버를 운영해야 했습니다.
- **문제 2:** 다중 시스템 간 동기화 시 메시지 손실 또는 지연이 발생하며, 이에 대한 추적 및 모니터링이 어려웠습니다. 특히 높은 트래픽 시스템에서는 메시지 누락이 자주 발생했습니다.
- **문제 3:** 실시간 알림이 필요한 시나리오에서 메시지 전달 지연이 발생했으며, 이로 인해 사용자 경험 저하가 발생했습니다. 예를 들어, 결제 성공 알림이 늦게 도착하는 문제가 있었습니다.

**SNS로 해결:**
- **해결 1:** SNS는 메시지 전달을 중앙 집중식 토픽으로 관리하여, 다양한 시스템 간 간접 통신을 가능하게 합니다. 이는 확장성과 유연성을 제공합니다.
- **해결 2:** SNS는 메시지 전달 과정에서 손실 방지 및 지연 최소화를 위해 고가용성 아키텍처를 제공합니다. 또한, 메시지 추적 및 모니터링 기능으로 트래픽 상태를 실시간으로 확인할 수 있습니다.
- **해결 3:** SNS는 실시간 알림을 위해 메시지 전달 지연을 최소화하고, 다양한 구독 방식(이메일, SQS, Lambda 등)을 지원해 사용자 경험을 개선합니다.

### 비유로 이해하기
**SNS는 뉴스 사이트의 뉴스 편집자 역할을 합니다.** 사용자는 뉴스를 구독하고, 편집자는 새로운 뉴스를 생성하면 모든 구독자에게 실시간으로 알림을 전달합니다. 이와 같이 SNS는 메시지 발행자(편집자)와 구독자(사용자) 간의 실시간 통신을 가능하게 합니다. 이 비유를 통해 SNS가 메시지 전달을 중앙 집중식으로 관리하는 방식을 쉽게 이해할 수 있습니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 실시간 알림 시스템 구축 | 모바일 앱의 푸시 알림, 결제 성공 알림 등 |
| 시나리오 2 | 데이터 파이프라인 통합 | 로그 수집 후 분석 시스템으로 전달, 데이터 처리 흐름 구성 |
| 시나리오 3 | 이벤트 기반 아키텍처 설계 | 사용자 행동 이벤트를 기반으로 다른 시스템에 알림 전달 |

**이럴 때 SNS를 선택하세요:**
- ✅ **실시간 알림이 필요한 시나리오** (예: 결제 성공 알림)
- ✅ **다중 시스템 간 메시지 전달이 필요한 경우** (예: 로그 시스템과 분석 시스템 연결)
- ✅ **확장성과 유연성 요구가 높은 시스템** (예: 클라우드 기반 서비스)

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **메시지 순서 보장이 필수적인 경우** → **SQS FIFO 큐** (SNS는 메시지 순서 보장 기능이 없음)
- ❌ **메시지 처리 후 결과를 다시 전달해야 하는 경우** → **Step Functions** (SNS는 단방향 전달만 가능)
- ❌ **고성능 데이터 스트리밍이 필요한 경우** → **Kinesis Data Streams** (SNS는 실시간 전달에 적합하지 않음)

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **Lambda** | SNS 토픽에 메시지가 도착하면 Lambda 함수를 자동 실행 | User → API Gateway → SNS → Lambda |
| **SQS** | SNS에서 메시지를 SQS 큐로 전달하여 순차 처리 | SNS → SQS → Lambda |
| **EventBridge** | SNS 이벤트를 EventBridge로 전달하여 다른 시스템에 전달 | SNS → EventBridge → CloudWatch |
| **Kinesis Data Streams** | 실시간 데이터 스트림을 SNS로 전달하여 분석 시스템에 전달 | Kinesis → SNS → Analytics |
| **Step Functions** | SNS 메시지를 기반으로 상태 머신을 실행하여 복잡한 워크플로우 관리 | SNS → Step Functions → Lambda |

**자주 사용되는 아키텍처 패턴:**
```
User → API Gateway → SNS → Lambda
```
이 아키텍처는 사용자의 요청을 API Gateway에서 받고, SNS를 통해 메시지를 전달하여 Lambda 함수를 실행하는 방식입니다. 이 구조는 실시간 알림 시스템과 같은 시나리오에서 널리 사용됩니다.

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **Standard Topic Publish** | $0.0001 / 1000 messages | 월 1,000,000 messages 무료 |
| **FIFO Topic Publish** | $0.0002 / 1000 messages | 월 1,000,000 messages 무료 |
| **SMS Subscription** | $0.0075 / 1000 messages | 12개월 무료 (1,000,000 messages) |

**비용 최적화 팁:**
1. 💡 **FIFO 토픽 사용**: 메시지 순서 보장이 필요할 때 FIFO를 선택하면, 메시지 처리 효율성이 높아집니다.
2. 💡 **프리티어 활용**: 초기 사용 시 1,000,000개 메시지 무료로 시작해, 비용을 절감할 수 있습니다.
3. 💡 **메시지 수량 모니터링**: CloudWatch를 통해 메시지 전송량을 실시간으로 확인해, 과도한 사용을 방지합니다.

> **⚠️ 비용 주의:** 높은 트래픽 시스템에서는 메시지 전송량이 급증할 수 있으므로, 정기적으로 사용량을 점검하고, 필요 시 메시지 전송량 제한을 설정해야 합니다. 또한, SMS 구독 시 SMS 요금이 별도로 발생할 수 있으므로 주의해야 합니다.

## 📚 핵심 개념

### 개념 1: SNS 토픽 구독과 메시지 전달  
SNS( Simple Notification Service )는 AWS에서 제공하는 **메시지 전달 서비스**로, 다양한 리소스(예: Lambda, SQS, API Gateway)에 메시지를 비동기적으로 전달합니다. 이 서비스는 **이벤트 기반 아키텍처**에서 핵심 역할을 하며, 실시간 알림, 상태 변경 통보, 시스템 간 통신 등에 활용됩니다. SNS는 **토픽(topic)**을 중심으로 운영되며, 특정 토픽에 구독(subscription)된 리소스에 메시지가 전달됩니다.  

#### 왜 중요한가?  
- **확장성과 유연성**: 메시지를 여러 리소스로 전달할 수 있어 시스템 간 통신을 간소화합니다.  
- **비동기 처리**: 메시지 전달이 비동기적이므로 시스템 부하를 줄이고, 고가용성을 제공합니다.  
- **실시간 알림**: 상태 변경이나 이벤트 발생 시 즉시 알림을 제공하여 반응 시간을 단축합니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **토픽(topic)** | 메시지 전달을 담당하는 주제로, 여러 구독자에게 메시지가 전달됩니다. | "order-created" 토픽을 생성하여 주문 생성 시 알림을 전달합니다. |
| **구독(subscription)** | 특정 리소스가 SNS 토픽에 가입한 상태로, 메시지를 수신합니다. | Lambda 함수를 SNS 토픽에 구독하여 주문 처리 로직을 실행합니다. |
| **메시지 전달 방식** | 포인트-to-포인트(P2P) 또는 퍼블리시-서브스크립트(Pub/Sub) 방식으로 메시지를 전달합니다. | SQS 큐를 통해 메시지가 전달되거나, Lambda 함수가 메시지를 처리합니다. |

> **💡 Tip:** SNS를 사용할 때는 메시지 전달 대상 리소스에 따라 적절한 구독 방식을 선택하세요. 예를 들어, Lambda 함수와 SQS 큐를 동시에 구독할 수 있습니다.  

---

### 개념 2: SNS와 EventBridge의 연동  
SNS는 EventBridge와 함께 사용될 수 있으며, 이는 **이벤트 기반 아키텍처**에서 중요한 역할을 합니다. EventBridge는 클라우드 내외부의 이벤트를 수집하고, SNS 토픽에 전달하여 다른 서비스로 전달할 수 있습니다. 이는 시스템 간 데이터 흐름을 효율적으로 관리하는 데 유용합니다.  

#### 작동 원리  
1. **이벤트 생성**: 예를 들어, DynamoDB에서 데이터가 변경되면 이벤트가 생성됩니다.  
2. **EventBridge 통과**: EventBridge가 이벤트를 수집하고, SNS 토픽으로 전달합니다.  
3. **메시지 전달**: SNS 토픽에 구독된 Lambda 함수, SQS 큐 등에 메시지가 전달되어 처리됩니다.  

> **💡 Tip:** EventBridge를 사용하여 SNS 토픽에 이벤트를 전달할 때는 **Rule**을 정의해 이벤트 필터링을 수행하세요. 예를 들어, 특정 조건의 변경 사항만 전달할 수 있습니다.  

---

### 개념 3: SNS와 SQS의 통합  
SNS는 SQS와 연동되어 **메시지 중개자** 역할을 수행합니다. SNS는 메시지를 여러 리소스로 전달하지만, SQS는 메시지를 큐에 저장하여 처리할 수 있도록 합니다. 이는 메시지 전달의 신뢰성과 처리 능력을 높이는 데 기여합니다.  

#### 주요 특징  
1. **메시지 중개**: SNS는 메시지를 여러 리소스로 전달하고, SQS는 메시지를 큐에 저장하여 처리합니다.  
2. **메시지 순서 보장**: SQS FIFO 큐를 사용하면 메시지의 처리 순서를 보장할 수 있습니다.  
3. **확장성**: SNS와 SQS를 함께 사용하면 고로드 시나리오에서도 메시지 전달을 안정적으로 수행할 수 있습니다.  

> **💡 Tip:** SNS 토픽을 SQS FIFO 큐로 구독할 때는 메시지의 **중복 방지**와 **처리 순서 보장**이 가능해지므로, 중요한 데이터 처리 시 고려해야 합니다.

## 🖥️ AWS 콘솔에서 SNS 사용하기

### Step 1: SNS 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: [https://console.aws.amazon.com](https://console.aws.amazon.com)  
   - IAM 계정으로 로그인한 후, `Services` 메뉴에서 `SNS`를 검색합니다.  
2. 검색 결과에서 "SNS"를 클릭합니다  
   - 메뉴에서 `SNS`를 선택하면, `SNS Console`이 열리며 `Topics` 탭이 기본적으로 표시됩니다.  

> **📸 화면 확인:** SNS 대시보드가 표시되면 정상입니다. `Topics` 탭에서 생성할 토픽을 확인할 수 있습니다.  

---

### Step 2: [주요 작업 1 - 토픽 생성]  
1. 상세 단계 1  
   - `Create topic` 버튼을 클릭합니다.  
   - `Standard` 또는 `FIFO` 유형을 선택합니다.  
     - **Standard**: 일반적인 메시지 전송 (중복 허용).  
     - **FIFO**: 메시지 순서 보장, 중복 제거 (FIFO 큐와 연동 시 사용).  
2. 상세 단계 2  
   - `Topic name`을 입력합니다.  
     - 예: `MySnsTopic`  
   - `Tags`를 추가할 수 있습니다 (선택 사항).  
   - `Create topic` 버튼을 클릭하여 생성합니다.  
3. 상세 단계 3  
   - 생성된 토픽이 `Topics` 목록에 표시되는지 확인합니다.  
   - 토픽 이름을 클릭하면 상세 설정 및 구독자 관리를 할 수 있습니다.  

> **📸 화면 확인:** `Topics` 목록에 생성된 토픽이 표시되고, 클릭 시 상세 정보가 나타납니다.  

---

### Step 3: [주요 작업 2 - 구독자 설정]  
1. 상세 단계 1  
   - 토픽을 클릭한 후, `Subscriptions` 탭을 선택합니다.  
   - `Create subscription` 버튼을 클릭하여 구독자를 추가합니다.  
2. 상세 단계 2  
   - **Protocol**을 선택합니다.  
     - 예: `email`, `HTTPS`, `SQS` (SQS를 연동할 경우).  
   - **Endpoint**를 입력합니다.  
     - 이메일 주소 또는 SQS 큐 ARN을 입력합니다.  
   - `Subscription ARN`이 생성되며, `Confirm subscription`을 클릭합니다.  
3. 상세 단계 3  
   - 구독이 활성화된 후, `Status`가 `Confirmed`로 변경되는지 확인합니다.  
   - 토픽이 정상적으로 작동하는지 테스트하기 위해 `Publish message`를 클릭합니다.  

> **⚠️ 주의:** 구독자 설정 시, **AWS IoT Core** 또는 **SQS**와 연동할 경우, 정확한 ARN을 입력해야 합니다.  

---

### Step 4: 설정 확인 및 테스트  
1. 생성된 리소스 확인 방법  
   - `Topics` 목록에서 토픽을 선택해 `Overview` 탭에서 ARN, 상태, 구독자 수를 확인합니다.  
   - `Subscriptions` 탭에서 활성화된 구독자 목록을 확인합니다.  
2. 상태 확인 방법  
   - `Metrics` 탭에서 토픽의 메시지 수, 실패 수 등 실시간 메트릭을 확인합니다.  
   - `Activity` 탭에서 메시지 전송 기록을 확인할 수 있습니다.  
3. 정상 동작 테스트 방법  
   - `Publish message`를 클릭 후, 메시지 내용을 입력하고 `Publish` 버튼을 클릭합니다.  
   - 구독자(이메일/SQS)에 메시지가 전송되었는지 확인합니다.  

> **💡 Tip:** 테스트 시 `Sample message`를 사용하면 편리합니다. 예: `"Test message from SNS"`.  

---

### 📊 SNS vs 기타 서비스 비교  
| 서비스         | 특징                                      | SNS 적합한 시나리오                     |  
|----------------|-------------------------------------------|----------------------------------------|  
| **SNS**        | 메시지 전송, 토픽 기반, 확장성 높         | 이벤트 알림, 로그 수집, 서버리스 통신   |  
| **SQS**        | 메시지 큐, FIFO/Standard 지원            | 동기화 필요, 작업 큐 관리               |  
| **EventBridge**| 이벤트 기반 아키텍처, 로그/메트릭 연동    | 클라우드 리소스 이벤트 처리, 분석        |  
| **Kinesis**    | 실시간 데이터 스트리밍, 분석/저장 기능    | 실시간 데이터 처리, 대규모 데이터 수집   |  

---

### 💡 비용 및 프리티어 활용  
- **프리티어**: 1년간 100만 건까지 무료 (Standard 토픽, 100개 토픽, 10개 구독자).  
- **비용 요소**:  
  - 메시지 전송 수  
  - 토픽 수  
  - 구독자 수  
- **비용 최소화 팁**:  
  - FIFO 토픽 사용 시 메시지 중복 제거로 비용 절감.  
  - SQS와 연동 시 메시지 처리 효율성 향상.  

> **⚠️ 주의:** 프리티어 한도 초과 시, 요금이 부과됩니다. AWS Cost Explorer를 활용해 비용 모니터링을 권장합니다.

## ⌨️ AWS CLI로 SNS 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

> **💡 Tip:** CLI를 사용하기 전에 AWS 계정이 생성되어야 하며, `aws configure` 명령어로 기본 설정을 완료해야 합니다. 설정 파일은 `~/.aws/config`에 저장됩니다.

---

### 예제 1: SNS 리소스 조회
```bash
# [SNS 토픽 목록 조회]
aws sns list-topics --query 'Topics[*].TopicArn' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 결과 필터링 | `'Topics[*].TopicArn'` |
| `--output` | 출력 형식 | `json`, `table`, `text` |

**예상 출력:**
```
Table Output
--------------------
|       TopicArn                        |
+---------------------------------------+
| arn:aws:sns:region:account:example-topic |
```

> **⚠️ 주의:** `list-topics`는 토픽 목록을 반환하지만, `describe-topic` 명령어로 특정 토픽의 세부 정보를 확인할 수 있습니다.

---

### 예제 2: SNS 리소스 생성
```bash
# [SNS 토픽 생성]
aws sns create-topic --name "example-topic" --attributes '{"DeliveryPolicy": "{\"http\": {\"deliveryMode\": \"1\"}}", "StoragePolicy": \"Queue"}'
```

**필수 옵션:**
- `--name`: 토픽 이름 (예: `example-topic`)
- `--attributes`: 토픽 속성 (JSON 형식)

**예상 출력:**
```json
{
    "TopicArn": "arn:aws:sns:region:account:example-topic"
}
```

> **💡 Tip:** `DeliveryPolicy`는 메시지 전달 방식을 설정하고, `StoragePolicy`는 메시지 저장 옵션을 결정합니다. `Queue`는 SQS 큐에 메시지를 저장합니다.

---

### 예제 3: SNS 리소스 수정
```bash
# [SNS 토픽 속성 수정]
aws sns set-topic-attributes --topic-arn "arn:aws:sns:region:account:example-topic" --attributes '{"DeliveryPolicy": "{\"http\": {\"deliveryMode\": \"2\"}}"}'
```

> **⚠️ 주의:** `set-topic-attributes`는 토픽의 속성을 변경할 수 있는 유일한 명령어입니다. 삭제는 `delete-topic` 명령어로만 가능합니다.

---

### 예제 4: SNS 리소스 삭제
```bash
# [SNS 토픽 삭제]
aws sns delete-topic --topic-arn "arn:aws:sns:region:account:example-topic"

# 삭제 확인 (실패 시 오류 반환)
aws sns describe-topic --topic-arn "arn:aws:sns:region:account:example-topic"
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. 삭제 후 `describe-topic` 명령어는 오류를 반환합니다.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws sns list-topics
aws sns describe-topic --topic-arn "arn:aws:sns:region:account:topic-name"

# 생성
aws sns create-topic --name "example-topic" --attributes '{"StoragePolicy": "Queue"}'

# 수정
aws sns set-topic-attributes --topic-arn "arn:aws:sns:region:account:topic-name" --attributes '{"DeliveryPolicy": "{\"http\": {\"deliveryMode\": \"1\"}}"}'

# 삭제
aws sns delete-topic --topic-arn "arn:aws:sns:region:account:topic-name"
```

---

### ⚠️ 주의사항
1. **비용 관리:** SNS는 메시지 전송 시 요금이 발생하므로, 프리티어 한도를 초과하지 않도록 주의하세요.
2. **ARN 필수:** `delete-topic` 및 `set-topic-attributes`는 토픽 ARN을 필수로 요구합니다.
3. **구독 관리:** SNS 토픽에 구독을 추가하려면 `subscribe` 명령어를 사용해야 합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 SNS 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **포인트 1: 토픽 생성 및 구독 관리**  
   - 설명: SNS의 핵심 기능은 메시지 토픽을 생성하고, 다양한 구독자(메시지 수신자)를 등록하는 것입니다. 시험에서는 토픽의 생성 방식, 구독 유형(이메일, SQS, Lambda 등), 메시지 전달 방식을 묻는 문제가 자주 출제됩니다.  
   - 키워드: `토픽`, `구독`, `메시지 전달`

2. **포인트 2: 메시지 전달 방식 (Standard vs FIFO)**  
   - 설명: SNS는 Standard 및 FIFO 두 가지 메시지 전달 모드를 지원합니다. Standard는 높은 처리량을 요구하지만 순서 보장이 없고, FIFO는 메시지 순서를 보장하지만 처리량이 제한됩니다. 시험에서는 이 차이점과 적절한 사용 사례를 묻는 문제가 많습니다.  
   - 키워드: `Standard`, `FIFO`, `메시지 순서`

3. **포인트 3: SNS와 SQS의 차이 이해**  
   - 설명: SNS는 퍼ubscription 기반의 퍼브리시-서브스크립션 모델을 사용하고, SQS는 메시지 큐를 통해 처리합니다. 시험에서는 두 서비스의 목적, 메시지 전달 방식, 사용 사례 등을 비교하는 문제가 자주 출제됩니다.  
   - 키워드: `퍼브리시-서브스크립션`, `메시지 큐`, `단방향/양방향 전송`

4. **포인트 4: 비용 구조와 프리티어 제한**  
   - 설명: SNS는 메시지 전송 수량에 따라 요금이 결정되며, 프리티어는 1년간 100만 건까지 무료로 제공됩니다. 시험에서는 비용 계산 방식, 프리티어 활용 방법, 사용량 초과 시 비용 등을 묻는 문제가 있습니다.  
   - 키워드: `비용 계산`, `프리티어`, `사용량 제한`

5. **포인트 5: SNS와 Step Functions/EventBridge 통합**  
   - 설명: SNS는 이벤트 기반 아키텍처에서 핵심 역할을 하며, Step Functions나 EventBridge와 연동하여 복잡한 워크플로우를 구현합니다. 시험에서는 이 통합 사례와 사용 시나리오를 묻는 문제가 있습니다.  
   - 키워드: `이벤트 기반`, `워크플로우`, `연동`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | SNS와 SQS의 메시지 전달 방식을 혼동해 메시지 순서를 잘못 판단 | SNS의 FIFO 모드는 메시지 순서 보장을 제공하지만, Standard 모드는 보장하지 않음 |
| 함정 2 | SNS의 비용이 메시지 수량에만 의존한다고 오해 | SNS는 메시지 전송 수량과 구독자 수, 지역별 비용 구조에 따라 달라짐 |
| 함정 3 | SNS 토픽을 생성할 때 FIFO 모드를 사용해야 하는 상황을 잘못 판단 | FIFO 모드는 메시지 순서 보장이 필요한 경우에만 사용해야 함 |

#### 🔄 SNS vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | SNS | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 메시지 퍼브리시-서브스크립션, 이벤트 알림 | SQS (메시지 큐), EventBridge (이벤트 라우팅) | 실시간 알림이 필요할 때 |
| 확장성 | 고유한 토픽 기반 확장 | SQS의 FIFO/Standard, EventBridge의 규칙 기반 확장 | 대규모 이벤트 처리가 필요할 때 |
| 비용 | 메시지 수량 기반 | SQS의 메시지 수량, EventBridge의 이벤트 수량 | 비용 최적화가 필요한 경우 |
| 지연시간 | 실시간 전송 | SQS의 지연 시간, EventBridge의 지연 시간 | 실시간 반응이 필수적일 때 |

#### 📝 시험 대비 체크리스트
- [ ] SNS의 핵심 목적을 한 문장으로 설명할 수 있는가?  
- [ ] SNS를 선택해야 하는 시나리오를 알고 있는가?  
- [ ] SNS의 제한사항(예: FIFO 모드의 처리량 제한)을 알고 있는가?  
- [ ] SNS와 SQS/EventBridge의 차이점을 설명할 수 있는가?  
- [ ] SNS의 비용 구조(메시지 수량, 지역, 프리티어)를 이해하고 있는가?

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 SNS를 떠올리세요:  
> - **토픽** (Topic)  
> - **구독** (Subscription)  
> - **메시지 전달 방식** (Standard/FIFO)  
> - **이벤트 기반 아키텍처**  
> - **비용 최적화**

---

| [⬅️ SQS](./SQS.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 3](../README.md) | [Kinesis ➡️](./Kinesis.md) |

---
