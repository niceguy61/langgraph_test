---

| [⬅️ EventBridge](./EventBridge.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 3](../README.md) | [SNS ➡️](./SNS.md) |

---

# SQS 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** SQS는 **메시지 큐**를 위한 AWS의 **확장성 및 신뢰성**을 제공하는 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 분산 시스템에서 메시지 손실 또는 중복 처리로 인한 데이터 일관성 문제  
  *구체적 설명:* 동기화된 통신이 아닌 비동기 처리 시, 요청이 중간에서 끊기거나 메시지가 중복 전송될 수 있음  
- **문제 2:** 고성능 시스템에서 메시지 처리 지연으로 인한 서비스 가용성 저하  
  *구체적 설명:* 요청 처리가 지연되면 사용자 경험 저하 및 시스템 오버로드 발생  
- **문제 3:** 메시지 처리 순서 보장이 필요한 작업에서의 일관성 문제  
  *구체적 설명:* 결제 시스템 같은 고도로 순차적인 처리가 필요한 시나리오에서 메시지 순서가 섞일 수 있음  

**SQS로 해결:**
- **해결 1:** 메시지 버퍼링 기능으로 요청 손실 방지 및 중복 처리 방지  
  *작동 원리:* SQS는 메시지를 임시 저장해 요청이 중단되더라도 처리가 가능  
- **해결 2:** 메시지 대기열 기능으로 처리 지연 최소화  
  *작동 원리:* 작업이 완료될 때까지 메시지가 대기열에 머무르며 시스템 부하 분산  
- **해결 3:** FIFO 큐를 통해 메시지 순서 보장  
  *작동 원리:* 동일한 토픽 메시지가 순차적으로 처리되도록 설계  

### 비유로 이해하기
SQS는 우체국의 편지 배달 시스템을 비유할 수 있어요. 고객이 편지를 우체국에 맡기면, 우체국은 편지를 정확한 주소에 순차적으로 배달해 줍니다. 만약 배달 중 문제가 발생해도, 편지가 손실되거나 중복 배달되지 않도록 보장합니다. 이처럼 SQS는 시스템 간 메시지를 안전하게 전달하고 처리 순서를 관리해 줍니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 마이크로서비스 간 통신 시 메시지 중개 | 결제 시스템에서 주문 처리 후 결제 확인 메시지 전송 |
| 시나리오 2 | 고성능 시스템에서 요청 처리 지연 최소화 | 쇼핑몰 이벤트 시 유저 요청을 대기열로 분산 처리 |
| 시나리오 3 | 메시지 순서 보장이 필요한 처리 | 금융 거래 시스템에서 거래 메시지의 순차 처리 필요 |

**이럴 때 SQS를 선택하세요:**
- ✅ **상황 1:** 시스템 간 비동기 통신이 필요한 경우  
- ✅ **상황 2:** 고성능 시스템에서 요청 처리 지연을 줄이고자 할 때  
- ✅ **상황 3:** 메시지 처리 순서를 보장해야 하는 시나리오  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **상황 → 대안 서비스 추천:** 메시지 전송이 필요하지만 순서 보장이 필요 없는 경우 → **SNS**  
  *이유:* SNS는 메시지를 1대다 방식으로 전송하지만 순서 보장이 불가능  
- ❌ **상황 → 대안 서비스 추천:** 실시간 데이터 스트리밍이 필요한 경우 → **Kinesis Data Streams**  
  *이유:* Kinesis는 데이터 스트리밍에 특화되어 있으며, 메시지 큐 기능은 제공하지 않음  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **Step Functions** | 상태 머신을 통해 SQS 메시지 처리 작업을 시퀀스화 | SQS → Step Functions → DB 처리 |
| **EventBridge** | 이벤트 기반으로 SQS 메시지 전송 트리거 | Lambda → EventBridge → SQS |
| **SNS** | SQS 메시지 전송을 위한 알림 및 메시지 전달 | SNS → SQS → 처리 서비스 |
| **Kinesis Data Streams** | 대량 데이터 스트리밍 시 SQS 대신 사용 | Producer → Kinesis → Consumer |

**자주 사용되는 아키텍처 패턴:**
```
[사용자] → [Frontend] → [API Gateway] → [Lambda] → [SQS] → [Step Functions] → [Database]
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **메시지 전송** | $0.00015/1000 건 | 월 100,000 건 무료 |
| **메시지 저장** | $0.00015/GB/월 | 12개월 100GB 무료 |
| **FIFO 큐** | $0.0002/1000 건 | 항상 무료 |

**비용 최적화 팁:**
1. 💡 **팁 1:** FIFO 큐 사용 시 메시지 순서 보장과 비용 절감을 동시에 달성  
2. 💡 **팁 2:** 메시지 저장 기간을 최소한으로 설정해 저장 비용 절감  
3. 💡 **팁 3:** 메시지 처리 실패 시 재처리를 최소화해 처리 비용 감소  

> **⚠️ 비용 주의:** 메시지 저장 기간을 14일 이상으로 설정하면 저장 비용이 급격히 증가할 수 있으며, 고성능 시스템에서 메시지 전송량이 예상보다 많을 경우 과금이 급증할 수 있습니다. AWS Free Tier를 활용해 초기 테스트 시 비용을 제어하세요.

## 📚 핵심 개념

### 개념 1: SQS 표준/FIFO 큐의 메시지 순서 보장  
SQS는 **Standard Queue**와 **FIFO Queue** 두 가지 유형을 제공하며, 이는 메시지의 처리 순서와 일관성을 보장하는 핵심 기능입니다. Standard Queue는 메시지의 처리 순서를 보장하지 않지만, 높은吞吐량과 유연성을 제공합니다. 반면 FIFO Queue는 메시지의 **순서를 보장**하고 **중복 메시지 방지** 기능을 지원하여, 금융 거래나 주문 처리 같은 고신뢰성 시스템에서 필수적입니다. 이 기능은 시스템의 **데이터 일관성**을 유지하고, **비동기 처리** 시 발생할 수 있는 문제를 해결합니다.

#### 왜 중요한가?
- **시스템 신뢰성 강화**: FIFO는 메시지의 처리 순서를 보장해, 중요한 데이터 처리 시 오류를 방지합니다.  
- **성능 최적화**: Standard Queue는 높은吞吐량을 지원해 대규모 데이터 처리에 적합합니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **Standard Queue** | 메시지 처리 순서 보장 **안함** | 주문 처리 시, 메시지의 순서가 중요하지 않은 경우 사용 |
| **FIFO Queue** | 메시지 처리 순서 **보장** + 중복 메시지 **방지** | 금융 거래 시, 메시지가 정확히 한 번만 처리되어야 하는 경우 사용 |
| **메시지 순서 보장** | FIFO만 적용 가능 | 예: 고객 주문이 1번 고객 → 2번 고객 순서로 처리됨 |

> **💡 Tip:** FIFO는 메시지 처리 순서를 보장하지만, **성능이 낮아** 대규모 데이터 처리 시 Standard Queue를 우선적으로 사용하는 것이 좋습니다.  

---

### 개념 2: Step Functions 상태 머신과 워크플로우 오케스트레이션  
AWS Step Functions은 **상태 머신**을 기반으로 한 워크플로우 오케스트레이션 서비스입니다. 복잡한 작업 흐름을 **그래프 형태**로 정의하여, 여러 AWS 서비스(예: Lambda, DynamoDB)를 연결해 **자동화된 처리**를 수행합니다. 이는 단일 서비스에서 처리할 수 없는 복잡한 작업을 **분산 처리**하고, **실패 시 재시도**나 **리소스 관리**를 자동화합니다.

#### 작동 원리  
1. **워크플로우 정의**: JSON 형식의 상태 머신 정의 파일로 작업 단계를 설계합니다.  
2. **실행**: Step Functions가 정의된 작업을 순차적으로 실행하며, 각 단계의 상태를 추적합니다.  
3. **실패 처리**: 특정 단계에서 오류가 발생하면, 재시도, 알림, 또는 복구 로직을 자동으로 실행합니다.  

> **💡 Tip:** Step Functions는 병렬 처리와 조건 분기(예: if-else)를 지원해, 복잡한 비즈니스 로직을 효율적으로 관리할 수 있습니다.  

---

### 개념 3: SNS 토픽 구독과 메시지 전달  
SNS(Simple Notification Service)는 **이벤트 기반 메시지 전달**을 위한 서비스로, 메시지를 여러 구독자(예: Lambda, SQS, HTTP)에게 **동시에 전달**합니다. 이는 시스템 간 **실시간 통신**이나 **이벤트 알림**에 유용하며, **확장성**과 **저지연 처리**를 지원합니다. SNS는 메시지 전달의 **비동기성**을 강조하며, 메시지 전달 실패 시 **재전송** 기능도 제공합니다.

#### 주요 특징  
1. **다중 구독자 지원**: 하나의 메시지를 여러 서비스에 동시에 전달 가능 (예: Lambda + SQS)  
2. **확장성**: 수천 개의 구독자에 메시지를 빠르게 전달 (예: 실시간 알림 시스템)  
3. **메시지 재전송**: 전달 실패 시 메시지를 재전송해 데이터 손실 방지  

> **💡 Tip:** SNS는 메시지 전달을 중심으로 설계되어, 데이터 처리보다는 **알림 및 이벤트 전파**에 적합합니다. 메시지 처리가 필요한 경우 SQS와 결합해 사용하는 것이 좋습니다.

## 🖥️ AWS 콘솔에서 SQS 사용하기

### Step 1: SQS 서비스 접속  
1. AWS Management Console에 로그인합니다.  
   - URL: [https://console.aws.amazon.com](https://console.aws.amazon.com)  
2. 상단 검색창에서 **"SQS"**를 입력하고, 검색 결과에서 **"SQS"**를 클릭합니다.  
3. **SQS 대시보드**가 표시되면, **"Queues"** 탭에서 SQS 리소스를 관리할 수 있습니다.  

> **📸 화면 확인:** SQS 대시보드에 **"Queues"** 탭이 표시되고, 생성된 큐 목록이 보이면 정상입니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **큐 생성**  
   - **"Create new queue"** 버튼을 클릭합니다.  
   - **Queue name**을 입력합니다 (예: `MyStandardQueue`).  
   - **Visibility timeout**, **Maximum message size**, **Message retention period** 등의 설정을 조정합니다.  
   - **"Create queue"** 버튼을 클릭하여 큐를 생성합니다.  

2. **FIFO 큐 생성**  
   - **"Create new queue"** 버튼을 클릭합니다.  
   - **Queue name**에 `.fifo` 확장자를 추가합니다 (예: `MyFIFOQueue.fifo`).  
   - **FIFO queue** 옵션을 활성화하고, **Content-based deduplication**을 설정합니다.  
   - **"Create queue"** 버튼을 클릭합니다.  

3. **확인 사항**  
   - 생성된 큐가 **"Queues"** 탭에서 표시되는지 확인합니다.  
   - **FIFO 큐**는 메시지 중복 제거 기능이 활성화되었는지 확인합니다.  

> **📸 화면 확인:** 생성된 큐 목록에 **"MyStandardQueue"**와 **"MyFIFOQueue.fifo"**가 추가되어 있는지 확인합니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **SQS 큐 설정**  
   - 선택한 큐를 클릭하여 **"Queue attributes"** 탭에서 설정을 확인합니다.  
   - **"Visibility timeout"**은 메시지가 다른 작업에서 처리되지 않도록 기다리는 시간을 조절합니다.  
   - **"Message retention period"**는 메시지가 저장되는 최대 시간을 설정합니다.  

2. **ACL 설정**  
   - **"Permissions"** 탭에서 **"Access Control List (ACL)"**을 설정합니다.  
   - **"Add permission"**을 클릭하여 특정 IAM 역할이나 사용자를 허용합니다.  

3. **메시지 전송 테스트**  
   - **"Send message"** 탭에서 메시지를 수동으로 전송합니다.  
   - **"Message body"**를 입력하고, **"Send"** 버튼을 클릭합니다.  

> **⚠️ 주의:** 메시지를 전송하기 전에 **"Receive message"** 탭에서 수신 설정을 확인하세요.  

---

### Step 4: 설정 확인 및 테스트  
1. **리소스 확인 방법**  
   - **"Queues"** 탭에서 생성된 큐 목록을 확인합니다.  
   - **"Queue URL"**을 복사하여 다른 서비스(예: Step Functions, EventBridge)와 연동할 수 있습니다.  

2. **상태 확인 방법**  
   - **"Metrics"** 탭에서 **"ApproximateNumberOfMessages"** 또는 **"ApproximateNumberOfMessagesNotVisible"** 메트릭을 확인합니다.  
   - **"Activity"** 탭에서 메시지 전송/수신 이력 확인 가능합니다.  

3. **정상 동작 테스트**  
   - **"Send message"**에서 메시지를 전송하고, **"Receive message"**에서 수신 여부를 확인합니다.  
   - **"Delete message"** 버튼을 클릭하여 처리 완료된 메시지를 삭제합니다.  

> **💡 Tip:** SQS는 비동기 처리에 적합하지만, 메시지 전송 후 수신이 지연될 수 있으므로 **"Visibility timeout"** 설정을 주의 깊게 조절하세요.  

---

### 📊 서비스 비교 및 가격 요약  
| 서비스 | 기능 | 주요 사용 사례 |  
|--------|------|------------------|  
| **SQS 표준** | 메시지 큐, 고가용성 | 배달 시스템, 작업 큐 |  
| **SQS FIFO** | 메시지 중복 제거, 순서 보장 | 결제 시스템, 주문 처리 |  
| **SNS** | 푸시 알림, 이메일 알림 | 이벤트 알림, 사용자 통지 |  
| **Kinesis** | 실시간 데이터 스트림 | 로그 분석, 실시간 분석 |  

> **💰 비용 주의사항:**  
- SQS는 **프리티어**가 제공되지 않으므로, **"Request a Free Tier"**에서 무료 사용량을 확인하세요.  
- FIFO 큐는 표준 큐보다 **10배 비용**이 더 들 수 있으므로, 필요에 따라 선택하세요.

## ⌨️ AWS CLI로 SQS 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**설명:**  
SQS CLI 명령어를 실행하기 전에 AWS CLI 버전이 1.18 이상인지 확인하고, IAM 자격 증명이 정상적으로 설정되어 있는지 확인하세요. 리전 설정은 리소스 생성 시 지역별 호환성을 위해 중요합니다.  
> **💡 Tip:** AWS CLI 설정은 `~/.aws/config` 파일에 저장됩니다. 필요 시 `aws configure set region us-east-1` 명령어로 리전을 변경할 수 있습니다.

---

### 예제 1: SQS 리소스 조회
```bash
# [SQS 큐 목록 조회]
aws sqs list-queues --query 'Queues[*].QueueUrl' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query 'Queues[*].QueueUrl'` | 큐 URL 필터링 | `Queues[*].QueueUrl` |
| `--output table` | 테이블 형식으로 출력 | `table`, `json`, `text` |

**예상 출력:**
```
QUEUE URL
https://sqs.ap-northeast-2.amazonaws.com/123456789012/my-queue
```

**설명:**  
`list-queues` 명령어는 AWS 계정에 생성된 모든 SQS 큐를 목록화합니다. `--query` 옵션으로 특정 필드(예: `QueueUrl`)만 추출할 수 있으며, `--output table`은 명확한 시각화를 제공합니다.

---

### 예제 2: SQS 리소스 생성
```bash
# [SQS 큐 생성]
aws sqs create-queue \
    --queue-name "example-queue" \
    --attributes "VisibilityTimeout=30,MaximumMessageSize=256"
```

**필수 옵션:**
- `--queue-name`: 큐 이름 (필수)
- `--attributes`: 큐 속성 설정 (VisibilityTimeout, MaximumMessageSize 등)

**예상 출력:**
```json
{
    "QueueUrl": "https://sqs.ap-northeast-2.amazonaws.com/123456789012/example-queue",
    "ResponseMetadata": {
        "RequestId": "12345678-90ab-cdef-ghij-klmnopqrstuv",
        "HTTPStatusCode": 200
    }
}
```

**설명:**  
`create-queue` 명령어는 큐를 생성하고, `--attributes` 옵션으로 VisibilityTimeout(메시지 가시성 시간)과 MaximumMessageSize(메시지 최대 크기)를 설정할 수 있습니다. 생성된 큐 URL은 이후 메시지 전송/수신 시 사용됩니다.

---

### 예제 3: SQS 리소스 수정
```bash
# [SQS 큐 속성 수정]
aws sqs set-queue-attributes \
    --queue-url "https://sqs.ap-northeast-2.amazonaws.com/123456789012/example-queue" \
    --attributes "VisibilityTimeout=60"
```

**옵션 설명:**
- `--queue-url`: 수정할 큐 URL (필수)
- `--attributes`: 수정할 속성 (VisibilityTimeout, Policy 등)

**예상 출력:**
```json
{
    "ResponseMetadata": {
        "RequestId": "12345678-90ab-cdef-ghij-klmnopqrstuv",
        "HTTPStatusCode": 200
    }
}
```

**설명:**  
`set-queue-attributes` 명령어는 기존 큐의 속성을 수정합니다. 예를 들어, VisibilityTimeout을 30초에서 60초로 변경할 수 있습니다. 이 명령어는 실시간으로 설정을 반영하므로, 작업 중단 없이 유연한 관리를 가능하게 합니다.

---

### 예제 4: SQS 리소스 삭제
```bash
# [SQS 큐 삭제]
aws sqs delete-queue --queue-url "https://sqs.ap-northeast-2.amazonaws.com/123456789012/example-queue"

# 삭제 확인
aws sqs get-queue-url --queue-name "example-queue"
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. `delete-queue` 명령어 실행 전에 `get-queue-url` 명령어로 삭제 대상이 올바른지 확인하세요.

**예상 출력:**
```json
{
    "QueueUrl": "https://sqs.ap-northeast-2.amazonaws.com/123456789012/example-queue"
}
```

**설명:**  
`delete-queue` 명령어는 큐를 완전히 삭제합니다. 삭제 후 `get-queue-url` 명령어로 해당 큐가 존재하는지 확인할 수 있지만, 삭제된 큐는 복구가 불가능합니다. 실무에서는 삭제 전에 `describe-queue`로 상태를 점검하는 것이 좋습니다.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws sqs list-queues --query 'Queues[*].QueueUrl' --output table
aws sqs get-queue-url --queue-name "example-queue"

# 생성
aws sqs create-queue --queue-name "example-queue" --attributes "VisibilityTimeout=30"

# 수정
aws sqs set-queue-attributes --queue-url "https://sqs.ap-northeast-2.amazonaws.com/123456789012/example-queue" --attributes "VisibilityTimeout=60"

# 삭제
aws sqs delete-queue --queue-url "https://sqs.ap-northeast-2.amazonaws.com/123456789012/example-queue"
```

**팁:**  
SQS CLI 명령어는 작업 중인 큐를 수정/삭제할 수 있으므로, 작업 중단 없이 유연한 관리를 위해 `set-queue-attributes`를 활용하세요.  
> **💡 Tip:** `describe-queue` 명령어는 큐의 상세 정보(예: 메시지 수, 대기 시간)를 확인할 수 있습니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 SQS 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **포인트 1**: **SQS FIFO 큐의 메시지 순서 보장**  
   - 설명: SQS FIFO 큐는 동일한 토픽에 대한 메시지가 순서대로 처리되도록 보장하며, 이는 **메시지 순서 보장**과 **FIFO**라는 핵심 개념으로 시험에서 빈번히 출제됩니다.  
   - 키워드: `메시지 순서 보장`, `FIFO`

2. **포인트 2**: **SQS 표준 큐 vs FIFO 큐의 차이점**  
   - 설명: 표준 큐는 메시지 순서 보장이 없고, FIFO 큐는 메시지 중복 방지 및 순서 보장이 가능합니다. 이 차이점은 **메시지 처리 방식**과 **비용** 측면에서 시험에 자주 등장합니다.  
   - 키워드: `표준 큐`, `FIFO 큐`

3. **포인트 3**: **SQS의 확장성과 스크알링**  
   - 설명: SQS는 수평 확장이 가능하며, 메시지 처리량을 유연하게 조절할 수 있습니다. 이는 **스케일링**과 **고가용성** 개념과 연결되어 시험에서 중요합니다.  
   - 키워드: `스케일링`, `고가용성`

4. **포인트 4**: **SQS의 비용 구조 이해**  
   - 설명: SQS는 수신/전송별 요금이 적용되며, **프리티어**의 한도 내에서 사용할 수 있습니다. 비용 최적화 전략은 시험에서 자주 출제됩니다.  
   - 키워드: `비용 구조`, `프리티어`

5. **포인트 5**: **SQS의 메시지 지연 시간과 가시성 타임아웃**  
   - 설명: 메시지의 처리 지연 시간과 가시성 타임아웃 설정은 **시스템 성능 최적화**에 직결되며, 이는 시험에서 실무 사례로 자주 등장합니다.  
   - 키워드: `지연 시간`, `가시성 타임아웃`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | FIFO 큐와 표준 큐의 차이를 혼동해 메시지 중복 여부를 오인 | FIFO 큐는 메시지 중복 방지, 표준 큐는 중복 허용 |
| 함정 2 | 메시지 지연 시간이 작으면 처리 속도가 빠르다고 오인 | 지연 시간은 메시지 처리 지연, 가시성 타임아웃은 메시지 처리 시간 |
| 함정 3 | SQS의 비용이 항상 낮다고 생각 | 수신/전송량에 따라 비용이 변동, 프리티어 한도 초과 시 비용 증가 |

#### 🔄 SQS vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | SQS | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 메시지 큐로 비동기 처리, 작업 분산 | SNS(공지), Kinesis(스트리밍) | 메시지 순서 보장 필요 시 SQS |
| 확장성 | 수평 확장 가능, 메시지 처리량 유연 | SNS: 메시지 전달, Kinesis: 실시간 데이터 처리 | 고가용성 및 스케일링 요구 시 SQS |
| 비용 | 수신/전송별 요금, 프리티어 한도 | SNS: 메시지 전송 요금, Kinesis: 데이터 저장/전송 요금 | 메시지 처리량이 예측 가능한 경우 SQS |
| 지연시간 | 메시지 처리 지연 시간 설정 | SNS: 실시간 전송, Kinesis: 데이터 지연 최소화 | 즉시 처리가 필요한 경우 SNS/Kinesis |

#### 📝 시험 대비 체크리스트
- [ ] SQS의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  *(메시지 큐로 비동기 처리 및 시스템 분리)*  
- [ ] SQS를 선택해야 하는 시나리오를 알고 있는가?  
  *(메시지 순서 보장, 작업 분산, 시스템 분리 필요 시)*  
- [ ] SQS의 제한사항/한계를 알고 있는가?  
  *(메시지 중복 방지 불가(표준 큐), FIFO 큐는 비용 증가)*  
- [ ] SQS와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  *(SNS: 1대다, Kinesis: 실시간 스트리밍, SQS: 메시지 큐)*  
- [ ] SQS의 비용 구조를 이해하고 있는가?  
  *(수신/전송량 기반, 프리티어 한도, 메시지 처리량에 따른 비용 변동)*

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 SQS를 떠올리세요:  
> - **메시지 순서 보장**  
> - **FIFO**  
> - **스케일링**  
> - **비동기 처리**  
> - **시스템 분리**

---

| [⬅️ EventBridge](./EventBridge.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 3](../README.md) | [SNS ➡️](./SNS.md) |

---
