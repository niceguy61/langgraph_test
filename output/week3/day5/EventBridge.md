---

| [⬅️ Step Functions](./Step-Functions.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 3](../README.md) | [SQS ➡️](./SQS.md) |

---

# EventBridge 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** EventBridge는 애플리케이션 간 통신을 단순화하고 이벤트 기반 아키텍처를 구현하기 위한 AWS의 **이벤트 중개 서비스**입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 애플리케이션 간 직접적인 의존 관계로 인한 유연성이 떨어짐. 예: 주문 시스템이 결제 시스템에 직접 의존하면 결제 변경 시 리팩토링 필요  
- **문제 2:** 이벤트 전송 과정에서 메시지 손실 또는 중복 발생 가능성. 예: 로그 수집 시 메시지가 중간 서버에서 제거될 수 있음  
- **문제 3:** 대규모 이벤트 처리 시 확장성 부족. 예: 실시간 데이터 분석 시 서버 리소스 한계로 지연 발생  

**EventBridge로 해결:**
- **해결 1:** 이벤트 중개자 역할로 애플리케이션 간 간접 통신 가능. 구독자와 발행자 분리해 유연성 확보  
- **해결 2:** 메시지 전송 보장 및 중복 제어 기능 제공. 예: `Duplicates` 설정을 통해 메시지 중복 방지  
- **해결 3:** 자동 스케일링과 라우팅 규칙으로 대규모 처리 성공. 예: `Rule Pattern`으로 이벤트를 분기 처리  

### 비유로 이해하기
EventBridge는 우체국처럼 다양한 이벤트(우편물)를 수신해 목적지(구독자)에 정확히 전달하는 시스템입니다. 고객이 주문을 넣으면 이벤트가 EventBridge에 전달되고, 결제, 재고, 배송 시스템 등 각 서비스가 해당 이벤트를 수신하여 처리합니다. 이 과정에서 중간 서버가 필요 없고, 메시지 손실도 방지됩니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 실시간 데이터 처리 시 이벤트를 효율적으로 전달 | IoT 센서 데이터를 Kinesis Firehose로 전송 후 분석 |
| 시나리오 2 | 마이크로서비스 간 이벤트 기반 통신 | 결제 시스템이 주문 시스템에 주문 완료 이벤트 전달 |
| 시나리오 3 | 로그 및 메트릭 집중 관리 | AWS CloudWatch 로그를 EventBridge로 수집 후 분석 도구로 전달 |

**이럴 때 EventBridge를 선택하세요:**
- ✅ **실시간 이벤트 처리가 필요한 경우** (예: 실시간 분석, 알림 시스템)  
- ✅ **이벤트 기반 아키텍처 설계 시** (예: 마이크로서비스 통신, 비즈니스 로직 분리)  
- ✅ **대규모 이벤트 라우팅이 필요한 경우** (예: 다중 토픽/구독자 분기 처리)  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **간단한 알림 전달 시** → **SNS** (이벤트 전달 대신 토픽 구독자에게 직접 메시지 전송)  
- ❌ **메시지 순서 보장이 필요한 경우** → **SQS FIFO 큐** (EventBridge는 메시지 순서 보장 기능이 없음)  
- ❌ **데이터 인그레션 시** → **Kinesis Data Streams** (이벤트를 실시간으로 처리하는 데 최적화됨)  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **Step Functions** | 이벤트를 기반으로 상태 머신 실행 | EventBridge → Step Functions → DB/External API |
| **SNS** | 이벤트를 토픽으로 전달해 다양한 구독자에게 알림 | EventBridge → SNS → Lambda/Email/HTTPS |
| **Kinesis Data Streams** | 실시간 데이터 스트림 처리 | EventBridge → Kinesis → Analytics/Processing Service |

**자주 사용되는 아키텍처 패턴:**
```
User → API Gateway → Lambda (이벤트 생성) → EventBridge → [Step Functions/SNS/Kinesis] → Backend Services
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **이벤트 전송** | $0.0005/이벤트 | 월 1,000,000 이벤트 무료 |
| **Rule 실행** | $0.0001/Rule 실행 | 12개월 무료 |
| **Event Bus 관리** | $0.10/Event Bus/월 | 항상 무료 |

**비용 최적화 팁:**
1. 💡 **프리티어 활용**: 1,000,000 이벤트 이상 사용 시 비용 절감 (예: 로그 수집 시 이벤트 수 제한)  
2. 💡 **이벤트 패턴 최적화**: 불필요한 이벤트 필터링으로 전송량 감소 (예: `MatchPattern`으로 특정 이벤트만 전송)  
3. 💡 **이벤트 버스 관리**: 별도의 Event Bus를 생성하지 않고 기본 버스 사용 (예: 단일 애플리케이션에 한 개의 Event Bus 사용)  

> **⚠️ 비용 주의:** 실시간 처리 시 이벤트 수가 급증할 수 있으므로, **CloudWatch 알림**과 **Auto Scaling**을 통해 비용 통제를 강화해야 합니다.

## 📚 핵심 개념

### 개념 1: EventBridge의 이벤트 패턴과 룰 생성  
EventBridge는 클라우드 환경에서 이벤트 기반 아키텍처를 구축하는 핵심 서비스로, 이벤트 생성, 전달, 처리를 관리합니다. 이벤트 패턴은 특정 조건을 충족하는 이벤트를 필터링하고, 룰은 이벤트를 특정 서비스(예: Lambda, SNS)로 전달하는 로직을 정의합니다. 이는 실시간 데이터 처리, 서비스 간 동기화, 시스템 모니터링 등 다양한 시나리오에 활용됩니다. EventBridge는 이벤트 소스(예: EC2, DynamoDB)와 타겟(예: Lambda, SNS)을 연결하여 무중단으로 데이터 흐름을 처리합니다.  

#### 왜 중요한가?  
- **실시간 반응**: 이벤트가 발생하면 즉시 처리되므로, 실시간 분석이나 자동화 작업이 가능합니다.  
- **복잡한 흐름 단순화**: 다양한 서비스 간 통합을 단일 플랫폼에서 관리해, 시스템 복잡도를 낮춥니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| 이벤트 소스 | 이벤트를 생성하는 시스템(예: EC2, DynamoDB) | EC2 인스턴스가 생성될 때 이벤트 발생 |
| 이벤트 패턴 | 이벤트 필터링 조건(예: 타입, 파라미터) | `source = "aws.ec2" AND detail-type = "EC2 Instance State-change Notification"` |
| 룰 | 이벤트를 타겟으로 전달하는 로직 | EC2 상태 변경 이벤트를 Lambda 함수로 전달 |
| 타겟 | 이벤트를 처리하는 서비스(예: Lambda, SNS) | Lambda 함수가 이벤트를 처리해 로그 기록 |

> **💡 Tip:** EventBridge는 이벤트 흐름을 시각화해 실시간 모니터링이 가능하며, 이벤트 소스와 타겟을 연결해 복잡한 워크플로우를 간단하게 구현할 수 있습니다.

---

### 개념 2: Step Functions 상태 머신과 워크플로우 오케스트레이션  
Step Functions는 상태 머신을 기반으로 복잡한 워크플로우를 관리하는 서비스입니다. 이는 여러 AWS 서비스(예: Lambda, DynamoDB)를 연동해 단일 흐름으로 처리할 수 있도록 합니다. 상태 머신은 "Start", "Task", "Wait", "Choice" 등 다양한 상태를 정의해, 조건에 따라 작업을 분기하거나 반복할 수 있습니다. 이는 데이터 처리, 배치 작업, 시스템 자동화 등에 적합합니다.  

#### 작동 원리  
1. **상태 머신 정의**: JSON 형식으로 작업 순서와 조건을 정의합니다.  
2. **이벤트 트리거**: EventBridge 또는 API Gateway 등의 이벤트로 상태 머신 실행을 트리거합니다.  
3. **작업 처리**: 각 상태에서 Lambda 함수나 다른 서비스를 호출해 작업을 수행하고, 결과를 기록합니다.  

> **💡 Tip:** Step Functions는 실패 시 재시도, 실패 로그 수집, 작업 상태 추적 기능을 제공해 신뢰성 있는 워크플로우 구현이 가능합니다.

---

### 개념 3: SQS 표준/FIFO 큐의 메시지 순서 보장  
SQS는 메시지 큐 서비스로, 표준 큐와 FIFO 큐 두 가지 유형이 있습니다. 표준 큐는 메시지의 순서를 보장하지 않지만, 높은 처리량을 지원합니다. 반면 FIFO 큐는 메시지의 순서를 보장하고, 중복 메시지 방지 기능을 제공합니다. 이는 데이터 일관성과 정확한 처리가 필요한 시나리오(예: 결제 처리, 주문 처리)에 적합합니다.  

#### 주요 특징  
1. **메시지 순서 보장**: FIFO 큐는 메시지가 전달 순서대로 처리되며, 이는 데이터 일관성을 유지합니다.  
2. **중복 메시지 방지**: 메시지가 5분 이내에 다시 전송될 경우, 중복으로 처리되지 않도록 합니다.  
3. **스케일링 유연성**: 표준 큐는 수천 개의 메시지 동시 처리가 가능하며, FIFO는 1000개의 메시지 처리량을 지원합니다.  

> **💡 Tip:** FIFO 큐는 메시지 순서가 필수적인 시나리오에서 사용해야 하지만, 처리량 제한이 있어 대규모 시스템에서는 표준 큐와 함께 사용하는 것이 효율적입니다.

## 🖥️ AWS 콘솔에서 EventBridge 사용하기

### Step 1: EventBridge 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - **AWS 계정**과 **IAM 권한**이 올바르게 설정되어 있어야 합니다.  
2. 상단 검색창에서 **"EventBridge"**를 입력 후 검색합니다  
   - **EventBridge** 서비스는 AWS의 통합 이벤트 관리 플랫폼으로, 다양한 서비스 간 이벤트를 연결하는 역할을 합니다.  
3. 검색 결과에서 **"EventBridge"**를 클릭합니다  
   - 이 화면은 **EventBridge 컨소尔**의 메인 대시보드로, 이벤트 규칙, 토픽, 통합 서비스 등을 관리할 수 있습니다.  

> **📸 화면 확인:** EventBridge 대시보드가 표시되면 정상입니다. **"EventBus"** 또는 **"Rules"** 탭을 통해 다양한 리소스를 확인할 수 있습니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **Step Functions 상태 기계 생성**  
   - **EventBridge**와 **Step Functions**를 연동하려면 먼저 상태 기계를 생성해야 합니다.  
   - **Step Functions**는 AWS 콘솔에서 별도로 생성해야 합니다.  
     - 메뉴: **Services > Step Functions**  
     - **Create state machine** 버튼 클릭 후, **"Choose template"**에서 **"Hello World"** 템플릿 선택  
     - **Name**을 `StepFunctionTest`로 입력하고, **Role**을 IAM 역할로 설정  
     - **Start state machine** 버튼 클릭  
2. **EventBridge 규칙 생성**  
   - EventBridge 메인 화면에서 **"Create rule"** 버튼 클릭  
   - **Rule name**을 `StepFunctionTriggerRule`로 입력  
   - **Event source**를 **"CloudWatch Events"**로 선택  
   - **Target**에서 **"Step Functions"**를 선택하고, 생성한 상태 기계의 ARN 입력  
   - **Schedule expression**으로 `rate(1 minute)` 설정  
   - **Create rule** 버튼 클릭  
3. **리소스 확인**  
   - **EventBridge > Rules** 탭에서 생성한 규칙이 표시되는지 확인  
   - **Step Functions > State machines** 탭에서 상태 기계가 활성화되었는지 확인  

> **📸 화면 확인:** EventBridge 규칙 목록에서 `StepFunctionTriggerRule`가 존재하고, Step Functions 상태 기계가 실행 가능한 상태인지 확인하세요.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **EventBridge 규칙 편집**  
   - **Rule details**에서 **"Edit"** 클릭  
   - **Targets** 섹션에서 **Step Functions** 타겟의 **Input path**를 `$.detail`로 설정  
   - **Input transformation**에서 **JSON Path**를 사용해 입력 데이터를 변환  
2. **Step Functions 상태 기계 수정**  
   - **Step Functions > State machines > StepFunctionTest** 클릭  
   - **Edit** 버튼 클릭 후, **"Start at"**을 `StartState`로 설정  
   - **Output**을 `$.result`로 지정  
3. **IAM 권한 검토**  
   - **EventBridge**와 **Step Functions** 간 통신을 위해 IAM 역할이 올바르게 설정되었는지 확인  
   - **Policy**에서 `AWSStepFunctionsFullAccess`를 포함해야 합니다  

> **⚠️ 주의:** EventBridge 규칙의 **Input transformation**은 JSON 형식을 지켜야 하며, 잘못된 형식은 상태 기계 실행을 실패시킵니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **리소스 상태 확인**  
   - **EventBridge > Rules**에서 `StepFunctionTriggerRule`의 **State**가 `ENABLED`인지 확인  
   - **Step Functions > State machines**에서 `StepFunctionTest`의 **Status**가 `RUNNING`인지 확인  
2. **테스트 실행**  
   - **EventBridge > Rules**에서 `StepFunctionTriggerRule` 클릭 후 **"Test"** 버튼 클릭  
   - **Input**에 `{"detail": {"message": "Test event"}}` 입력 후 **"Test"** 클릭  
   - 상태 기계가 실행되고, 로그에서 `message: Test event`가 출력되는지 확인  
3. **로그 확인**  
   - **CloudWatch Logs**에서 `StepFunctionTest`의 실행 로그를 확인  
   - **EventBridge**의 **Rule execution** 로그에서 이벤트 전달 여부를 확인  

> **💡 Tip:** 테스트 시 EventBridge 규칙이 활성화되어 있어야 하며, IAM 역할이 제한되지 않도록 설정해야 합니다.  
> **⚙️ 비용 주의:** EventBridge는 **프리티어**로 100만 건까지 무료이며, Step Functions는 1000번까지 무료입니다.  

---

### 📊 서비스 비교 및 가격 정보  
| 서비스             | 주요 기능                          | 프리티어 제한         | 비용 요소                   |  
|-------------------|-----------------------------------|------------------------|----------------------------|  
| **EventBridge**   | 이벤트 리디렉션, 규칙 관리         | 100만 건/월           | 이벤트 수, 데이터 전송량   |  
| **Step Functions**| 작업 흐름 자동화                  | 1000번/월             | 상태 기계 실행 횟수        |  
| **SQS**           | 메시지 큐, FIFO/STANDARD          | 100만 메시지/월       | 메시지 수, 데이터 전송량   |  
| **SNS**           | 이메일/HTTP 알림 전송             | 100만 알림/월         | 알림 수, 전송 대상 수      |  

> **⚠️ 주의:** EventBridge와 Step Functions를 함께 사용할 경우, **Cost Explorer**에서 비용을 분석해야 합니다.  

---

### ✅ 체크리스트: 단계별 확인 사항  
- [ ] AWS 콘솔에 로그인하고 EventBridge로 이동  
- [ ] Step Functions 상태 기계가 생성되었는지 확인  
- [ ] EventBridge 규칙이 생성되고 활성화되었는지 확인  
- [ ] 이벤트 전달 및 상태 기계 실행이 성공적으로 이루어졌는지 확인  
- [ ] 로그 및 비용 요약을 확인하여 문제가 없는지 검토

## ⌨️ AWS CLI로 EventBridge 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**설명:**  
AWS CLI를 사용하기 전에 버전 확인, IAM 자격 증명 검증, 리전 설정이 필수적입니다.  
`aws sts get-caller-identity`는 현재 사용자의 IAM ID, ARN, 리전을 확인합니다.  
리전은 `us-east-1` 또는 `ap-northeast-1` 등으로 설정할 수 있습니다.

---

### 예제 1: EventBridge 리소스 조회
```bash
# EventBridge 이벤트 버스 목록 조회
aws events list-event-buses --query 'EventBuses[*].Name' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query 'EventBuses[*].Name'` | 이벤트 버스 이름 필터링 | `EventBuses[*].Name` |
| `--output table` | 테이블 형식으로 출력 | `table`, `json`, `text` |

**예상 출력:**
```
| Name         |
|--------------|
| default      |
| my-event-bus |
```

**설명:**  
`list-event-buses` 명령어는 AWS 계정에 등록된 모든 이벤트 버스 목록을 반환합니다.  
`--query`를 사용해 특정 필드만 출력하고, `--output`으로 형식을 조정할 수 있습니다.

---

### 예제 2: EventBridge 리소스 생성
```bash
# 이벤트 버스 생성
aws events create-event-bus --name "my-event-bus"
```

**필수 옵션:**
- `--name`: 이벤트 버스 이름 (예: `my-event-bus`)  
- `--tags`: 태그 추가 (선택적, `Key=Value` 형식)

**예상 출력:**
```json
{
    "EventBusArn": "arn:aws:events:us-east-1:123456789012:event-bus/my-event-bus"
}
```

**설명:**  
`create-event-bus` 명령어는 새로운 이벤트 버스를 생성합니다.  
이벤트 버스는 이벤트를 수신하고, 다른 서비스(예: Lambda, SNS)로 전달하는 중개자 역할을 합니다.  
생성된 버스의 ARN은 이후 규칙(Rule) 생성 시 필수적으로 사용됩니다.

---

### 예제 3: EventBridge 리소스 수정
```bash
# 이벤트 버스에 태그 추가
aws events tag-resource --resource-arn "arn:aws:events:us-east-1:123456789012:event-bus/my-event-bus" --tags Key=Project,Value=MyProject
```

**필수 옵션:**
- `--resource-arn`: 수정할 리소스 ARN  
- `--tags`: 태그 키-값 쌍 (예: `Key=Project,Value=MyProject`)

**예상 출력:**
```json
{
    "Tags": {
        "Project": "MyProject"
    }
}
```

**설명:**  
이벤트 버스에 태그를 추가해 리소스를 관리하는 데 활용할 수 있습니다.  
태그는 리소스의 메타데이터로, 비용 관리, 정책 적용에 유용합니다.

---

### 예제 4: EventBridge 리소스 삭제
```bash
# 이벤트 버스 삭제
aws events delete-event-bus --name "my-event-bus"

# 삭제 확인
aws events describe-event-bus --name "my-event-bus"
```

> **⚠️ 주의:** 이벤트 버스는 삭제 후 복구할 수 없습니다.  
> 삭제 전에 모든 관련 규칙(Rule)과 태그를 확인하고, 필요 시 백업을 수행하세요.

**예상 출력:**
```json
{
    "EventBusArn": "arn:aws:events:us-east-1:123456789012:event-bus/my-event-bus"
}
```

**설명:**  
`delete-event-bus` 명령어는 이벤트 버스를 영구적으로 삭제합니다.  
삭제 후 `describe-event-bus`는 오류를 반환하며, 리소스가 존재하지 않는다고 표시합니다.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws events list-event-buses
aws events describe-event-bus --name "my-event-bus"

# 생성
aws events create-event-bus --name "my-event-bus"
aws events tag-resource --resource-arn "arn:aws:events:us-east-1:123456789012:event-bus/my-event-bus" --tags Key=Project,Value=MyProject

# 삭제
aws events delete-event-bus --name "my-event-bus"
```

**팁:**  
- 이벤트 버스 생성 시 `--tags` 옵션을 사용해 리소스를 분류하세요.  
- 비용 관리를 위해 태그 기반의 AWS Cost Explorer를 활용할 수 있습니다.  
- 프리티어 사용 시, `us-east-1` 리전에서 100만 건의 이벤트 처리가 가능합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 EventBridge 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **이벤트 패턴과 룰 생성**  
   - 설명: EventBridge는 이벤트를 필터링하고 특정 작업을 트리거하는 룰을 정의하는 기능이 시험에서 필수적으로 출제됩니다. 이벤트 패턴은 JSON 형식으로 정의되며, "source", "detail-type", "resources" 등의 키워드를 통해 이벤트를 필터링합니다.  
   - 키워드: `이벤트 패턴`, `루ル 생성`

2. **Lambda 및 Step Functions 통합**  
   - 설명: EventBridge는 Lambda 함수나 Step Functions 작업 플로우를 트리거하는 기능이 핵심입니다. 시험에서는 이벤트를 처리하는 서비스 연결 방식, 예를 들어 `PutEvent` 또는 `PutRule` 명령어의 사용법이 직접적으로 묻는 경우가 많습니다.  
   - 키워드: `Lambda 통합`, `Step Functions`

3. **비용 관리 및 프리티어 한계**  
   - 설명: EventBridge의 비용 구조는 이벤트 수, 리소스 사용량에 따라 달라지며, 프리티어는 100만 건 이벤트 처리까지 제공합니다. 시험에서는 "이벤트 수 제한", "비용 최적화 전략" 등에 대한 이해가 필수적입니다.  
   - 키워드: `비용 관리`, `프리티어`

4. **보안 및 IAM 정책**  
   - 설명: EventBridge는 IAM 정책을 통해 접근 제어를 설정해야 하며, 암호화 설정(예: KMS 키)도 시험에서 자주 다룹니다. 특히 "EventBus 권한", "CloudTrail 로그 수집"과 관련된 사례가 흔합니다.  
   - 키워드: `IAM 정책`, `암호화`

5. **에러 처리 및 모니터링**  
   - 설명: EventBridge의 실패한 이벤트를 처리하는 방법(예: Dead Letter Queue)과 CloudWatch를 통한 모니터링은 시험에서 자주 출제됩니다. 특히 "이벤트 실패 원인 분석"에 대한 사례가 중요합니다.  
   - 키워드: `Dead Letter Queue`, `CloudWatch`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "EventBridge는 SNS와 같은 메시지 전달 서비스인가?" → 혼동 유발 | EventBridge는 이벤트 라우팅 서비스로, SNS는 퍼블리시-구독 모델의 메시지 전달 서비스입니다. |
| 함정 2 | "이벤트 패턴에서 'source' 필드는 필수적인가?" → 정확한 조건 설정 유도 | 'source'는 필수적이지 않지만, 특정 리소스만 필터링할 때 사용됩니다. |
| 함정 3 | "EventBridge의 비용은 이벤트 수에 따라만 결정되는가?" → 비용 구조 혼동 | 이벤트 수, 리소스 사용량, 보안 설정 등 여러 요소가 비용에 영향을 줍니다. |

#### 🔄 EventBridge vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | EventBridge | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 이벤트 라우팅, 서비스 간 통신 | SNS(메시지 전달), Kinesis(데이터 스트림) | 이벤트를 특정 서비스로 전달해야 할 때 |
| 확장성 | 수평 확장 가능, 무제한 리소스 사용 | SNS는 구독자 수 제한, Kinesis는 샤드 수 제한 | 고성능 처리가 필요한 시나리오 |
| 비용 | 이벤트 수, 리소스 사용량 기반 | SNS는 요청 수 기반, Kinesis는 데이터량 기반 | 실시간 데이터 전송 vs. 이벤트 라우팅 |
| 지연시간 | 실시간 처리 가능 | Kinesis는 데이터 스트림으로 지연 가능성 있음 | 즉시 처리가 필요한 경우 |

#### 📝 시험 대비 체크리스트
- [ ] EventBridge의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  **예:** "이벤트를 특정 서비스로 라우팅하고, 이벤트 처리를 자동화하는 서비스입니다."
- [ ] EventBridge를 선택해야 하는 시나리오를 알고 있는가?  
  **예:** "Lambda 함수를 이벤트 기반으로 트리거하거나, 여러 서비스 간 이벤트 전달이 필요한 경우."
- [ ] EventBridge의 제한사항/한계를 알고 있는가?  
  **예:** "프리티어는 100만 이벤트 처리만 가능하며, 고성능 처리에는 추가 비용 발생."
- [ ] EventBridge와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  **예:** "SNS는 메시지 전달, Kinesis는 데이터 스트림 처리, EventBridge는 이벤트 라우팅에 특화됨."
- [ ] EventBridge의 비용 구조를 이해하고 있는가?  
  **예:** "이벤트 수, 리소스 사용량, 보안 설정 등이 비용에 영향을 줍니다."

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 EventBridge를 떠올리세요:  
> - **이벤트 라우팅**  
> - **루ル 생성**  
> - **Lambda 통합**  
> - **비용 관리**  
> - **보안 설정**

---

| [⬅️ Step Functions](./Step-Functions.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 3](../README.md) | [SQS ➡️](./SQS.md) |

---
