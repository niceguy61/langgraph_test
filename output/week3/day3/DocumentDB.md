---

| [⬅️ Neptune](./Neptune.md) | [📑 Day 3 목차](./README.md) | [🏠 Week 3](../README.md) | [Keyspaces ➡️](./Keyspaces.md) |

---

# DocumentDB 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** DocumentDB는 MongoDB 호환 문서 데이터베이스를 위한 AWS의 fully managed 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 관리형 NoSQL 데이터베이스를 운영할 때 서버 인프라 설정, 백업, 유지보수 등 복잡한 관리 작업이 필요했습니다.  
  *기존에는:* 개발자가 클러스터 구성, 스토리지 확장, 실패 시 재해복구를 직접 처리해야 했습니다.  

- **문제 2:** 유연한 스키마를 요구하는 데이터(예: JSON 형식)를 관리하는 데 관계형 데이터베이스의 고정 스키마 제약이 걸렸습니다.  
  *기존에는:* 데이터 모델을 수동으로 조정해야 하며, 성능 최적화가 어려웠습니다.  

- **문제 3:** 실시간 데이터 처리와 확장성 요구가 높은 시나리오에서 기존 데이터베이스의 스태틱 아키텍처가 제약이었습니다.  
  *기존에는:* 수동 스케일링으로 인해 지연이 발생하거나, 성능 저하가 발생했습니다.  

**DocumentDB로 해결:**
- **해결 1:** AWS가 클러스터 구성, 백업, 자동 업데이트 등을 자동 관리해 개발자 부담을 줄였습니다.  
  *예시:* `aws docdb describe-db-clusters` CLI 명령어로 상태 확인이 가능합니다.  

- **해결 2:** JSON 형식의 유연한 스키마를 지원해 데이터 모델 변경 없이도 새로운 필드 추가가 가능합니다.  
  *예시:* `INSERT INTO users (name, age) VALUES ("Alice", 30)`처럼 스키마 없는 쿼리 실행.  

- **해결 3:** 자동 스케일링과 복제 기능으로 트래픽 증가 시에도 성능 저하 없이 처리할 수 있습니다.  
  *예시:* `aws docdb modify-db-cluster-parameter-group`로 파라미터 조정 후 자동 확장.  

### 비유로 이해하기
DocumentDB는 도서관의 디지털 버전을 상상해보세요. 각 책은 문서(데이터)로, 도서관은 자동으로 책을 추가하고, 검색 시 빠르게 제공합니다. 사용자가 책을 추가하면 자동으로 책장이 확장되고, 데이터가 많아질수록 성능이 저하되지 않습니다. 이처럼 DocumentDB는 유연한 스키마와 자동 확장을 통해 데이터 관리의 복잡성을 줄입니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 실시간 데이터 처리가 필요한 시스템 | 고객 리뷰 시스템, 실시간 채팅 플랫폼 |
| 시나리오 2 | 유연한 스키마를 요구하는 애플리케이션 | SaaS 플랫폼, IoT 데이터 수집 |
| 시나리오 3 | 고가용성과 확장성이 필수적인 서비스 | 재테크 플랫폼, 글로벌 콘텐츠 서비스 |

**이럴 때 DocumentDB를 선택하세요:**
- ✅ **데이터 모델이 유동적일 때** (JSON, BSON 형식 데이터 처리)  
- ✅ **고가용성과 자동 복제가 필요할 때** (24/7 운영 시스템)  
- ✅ **트래픽 증가 시 성능 저하 없이 확장해야 할 때** (성장 중인 SaaS 서비스)  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **분석용 데이터 저장이 필요할 때** → **Redshift** (실시간 분석 기능 제공)  
- ❌ **그래프 데이터베이스가 필요할 때** → **Neptune** (SPARQL 쿼리 지원)  
- ❌ **시간 시리즈 데이터 처리가 필요할 때** → **Timestream** (자동 정리 및 쿼리 최적화)  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **S3** | 백업 및 지속적 저장 | DocumentDB → S3 (백업) → Lambda (지속적 처리) |
| **Lambda** | 실시간 데이터 처리 | S3 → Lambda → DocumentDB (데이터 전송) |
| **CloudFront** | 콘텐츠 분배 및 가속 | User → CloudFront → S3 → DocumentDB |

**자주 사용되는 아키텍처 패턴:**
```
User → CloudFront → S3 → DocumentDB → Lambda → S3
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **DB 클러스터** | $0.30 ~ $1.50/시간 (기본 인스턴스) | 7일 무료 (1 DB 클러스터) |
| **백업 저장소** | $0.025/GB/월 | 14일 무료 (1GB) |
| **API 요청** | $0.00015/요청 | 10,000 요청 무료 (14일) |

**비용 최적화 팁:**
1. 💡 **프리티어 활용:** 7일간의 무료 사용 기간 동안 테스트 데이터를 처리해 비용을 절감하세요.  
2. 💡 **사용량 모니터링:** CloudWatch를 통해 과도한 사용을 감지하고, 필요 시 인스턴스 크기 조정.  
3. 💡 **백업 최적화:** 14일간의 무료 백업 기간 동안 중요한 데이터만 백업하고, 지속적 저장은 S3 Glacier로 전환.  

> **⚠️ 비용 주의:** 백업 데이터 저장소 비용이 예상보다 높아질 수 있으므로, S3 버전 관리 및 데이터 정리 전략을 항상 고려해야 합니다.

## 📚 핵심 개념

### 개념 1: DocumentDB의 문서 모델과 스케일링  
Amazon DocumentDB는 MongoDB 호환의 NoSQL 문서 데이터베이스로, JSON 형식의 문서를 기반으로 데이터를 저장합니다. 이 문서 모델은 동적 스키마를 지원해 데이터 유연성을 높이고, 복잡한 데이터 구조(예: 중첩된 필드, 배열)를 쉽게 관리할 수 있습니다. 스케일링은 자동화된 클러스터 확장 기능을 통해 처리되며, 쓰기 작업은 분산된 노드 간에 분산되어 처리되어 높은 처리량을 달성합니다.  

#### 왜 중요한가?  
- **유연한 스키마**: 데이터 구조 변경 없이 새로운 필드 추가 가능  
- **고가용성**: 자동 복제와 장애 터미널 장비로 서비스 중단 방지  

#### 세부 요소  
| 요소 | 설명 | 예시 |  
|-----|-----|-----|  
| 문서 모델 | JSON 형식의 데이터 저장 방식 | `{name: "Alice", age: 30, hobbies: ["reading", "hiking"]}` |  
| 자동 스케일링 | CPU/메모리 사용량에 따라 자동 확장 | 10개 노드에서 100개 노드로 자동 확장 |  
| 복제 및 장애 터미널 | 노드 실패 시 자동 복제 노드로 전환 | 장애 발생 시 10초 이내 복구 |  

> **💡 Tip:** 문서 모델은 관계형 데이터베이스보다 데이터 구조 변경이 간편하며, 스케일링은 클러스터 크기 조절을 통해 실시간으로 조정 가능합니다.  

---

### 개념 2: DocumentDB의 클러스터 구성 및 고가용성  
DocumentDB는 클러스터 기반으로 운영되며, 각 클러스터는 3개 이상의 노드로 구성됩니다. 이 클러스터는 AWS Region 내에서 고가용성 환경을 제공하며, 쓰기 작업은 주 노드에 집중되고 읽기 작업은 부 노드에서 분산 처리됩니다. 이 구조는 데이터 일관성과 동시성 보장을 위해 MongoDB의 프로토콜을 기반으로 설계되었습니다.  

#### 작동 원리  
1. **클러스터 생성**: AWS 콘솔에서 Region 선택 후 클러스터 생성  
2. **노드 분산**: 쓰기 작업은 주 노드에, 읽기 작업은 부 노드에서 처리  
3. **장애 터미널**: 주 노드 실패 시 자동으로 부 노드로 전환 및 복제  

> **💡 Tip:** 클러스터의 노드 수를 조절해 처리량에 따라 스케일링하고, 콘솔에서 "Read Replicas"를 통해 읽기 작업을 분산할 수 있습니다.  

---

### 개념 3: DocumentDB의 데이터베이스 작업 및 백업  
DocumentDB는 MongoDB의 명령어를 호환해 쿼리 실행이 가능하며, 데이터베이스 작업은 실시간으로 수행됩니다. 또한, AWS의 자동 백업 및 복원 기능을 통해 데이터 손실 방지가 가능하며, 백업은 7일간 보관되며 필요 시 복원이 가능합니다. 이 기능은 데이터 무결성과 시스템 안정성을 보장합니다.  

#### 주요 특징  
1. **MongoDB 호환 쿼리**: `find()`, `update()`, `delete()` 등의 MongoDB 명령어 사용 가능  
2. **자동 백업**: 매일 밤 1시에 백업 생성 및 7일간 보관  
3. **데이터 복원**: 백업 파일을 기반으로 특정 시간점으로 데이터 복원 가능  

> **💡 Tip:** 백업을 위해 "Point-in-Time Recovery" 기능을 사용해, 데이터 손실 시 15분 간격으로 복원이 가능합니다.

## 🖥️ AWS 콘솔에서 DocumentDB 사용하기

### Step 1: DocumentDB 서비스 접속  
1. **AWS Management Console 로그인**  
   - [https://console.aws.amazon.com](https://console.aws.amazon.com)에 접속 후 IAM 계정으로 로그인합니다.  
   - AWS 콘솔 메인 화면에서 **"Services"** 메뉴를 클릭하고, **"Database"** 섹션을 확장합니다.  
   - **"DocumentDB"**를 검색하여 해당 서비스를 클릭합니다.  

2. **대시보드 확인**  
   - DocumentDB 대시보드가 표시되면 **"Clusters"** 탭을 선택하여 클러스터 목록을 확인합니다.  
   - 이 화면은 생성된 클러스터, 상태, 연결 정보 등을 확인할 수 있습니다.  

> **📸 화면 확인:** DocumentDB 대시보드에서 **"Clusters"** 탭이 표시되고, **"Create cluster"** 버튼이 보이면 정상입니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **클러스터 생성**  
   - **"Create cluster"** 버튼을 클릭하여 클러스터 생성 화면으로 이동합니다.  
   - **Cluster name**: 클러스터 이름을 입력합니다 (예: `MyDocDBCluster`).  
   - **Instance type**: `db.t3.micro` 또는 `db.r4.large` 같은 옵션에서 선택합니다 (무료 트라이어는 `db.t3.micro` 권장).  
   - **Storage**: 최소 5GB 이상으로 설정합니다.  

2. **네트워크 및 보안 설정**  
   - **VPC**: 기본 VPC를 선택합니다.  
   - **Subnets**: 공개 서브넷을 선택하여 외부 접근 가능하게 설정합니다.  
   - **Security groups**: `Default` 그룹을 선택하거나, 외부 IP를 허용하는 규칙을 추가합니다.  

3. **수동/자동 백업 설정**  
   - **Backup retention period**: 7일 이상으로 설정합니다.  
   - **Backup window**: 10분 간격으로 설정합니다.  

> **📸 화면 확인:** 클러스터 생성 화면에서 **"Create"** 버튼을 클릭하고, 생성 완료 후 **"Clusters"** 탭에서 새 클러스터가 목록에 표시되는지 확인합니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **사용자 및 권한 관리**  
   - **"Users"** 탭에서 **"Create user"**를 클릭하여 사용자를 생성합니다.  
   - **Username**: `admin` 또는 `user1` 등 입력합니다.  
   - **Password**: 복잡한 비밀번호를 입력하고, **"Password"** 필드에 확인합니다.  
   - **Roles**: `db_owner` 권한을 부여합니다.  

2. **파라미터 그룹 수정**  
   - **"Parameter groups"** 탭에서 **"Create parameter group"**을 클릭합니다.  
   - **Parameter group name**: `my-docdb-params` 입력합니다.  
   - **Parameters**: `maxConnections` 값을 1000으로 수정합니다.  

3. **클러스터 연결 설정**  
   - **"Connect"** 탭에서 **"Connect to cluster"**를 클릭합니다.  
   - **Endpoint**: MongoDB 연결 주소를 복사합니다 (예: `my-docdb-cluster.cluster-xxxxx.docdb.eu-west-1.amazonaws.com`).  
   - **Port**: 기본 포트는 `27017`입니다.  

> **⚠️ 주의:** 클러스터 생성 후 **Security groups**를 확인하여 외부 IP가 허용되었는지 반드시 확인해야 합니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **리소스 상태 확인**  
   - **"Clusters"** 탭에서 생성된 클러스터의 **Status**가 `Available`인지 확인합니다.  
   - **Metrics** 탭에서 CPU 사용률, 메모리 사용량 등을 확인합니다.  

2. **테스트 쿼리 실행**  
   - MongoDB Shell을 열고 다음 명령어를 입력하여 데이터베이스를 생성합니다:  
     ```bash
     mongo "mongodb://admin:password@my-docdb-cluster.cluster-xxxxx.docdb.eu-west-1.amazonaws.com:27017/admin?authSource=admin"
     ```  
   - 테스트 데이터베이스 생성:  
     ```javascript
     use testDB
     db.createCollection("users")
     ```  

3. **연결 테스트**  
   - AWS CLI로 클러스터 상태를 확인합니다:  
     ```bash
     aws docdb describe-db-clusters --cluster-identifier my-docdb-cluster
     ```  
   - **Output**에서 `Status`가 `ACTIVE`인지 확인합니다.  

> **💡 Tip:** DocumentDB는 MongoDB 호환 포맷이므로, MongoDB CLI나 드라이버를 사용해도 문제 없습니다.  

---

### ✅ 체크리스트 (Step 1~4)  
- [ ] AWS 콘솔 로그인 및 DocumentDB 서비스 접속  
- [ ] 클러스터 생성 시 네트워크 및 보안 설정 완료  
- [ ] 사용자 및 파라미터 그룹 생성  
- [ ] 클러스터 연결 주소 및 포트 확인  
- [ ] MongoDB Shell로 데이터베이스 생성 테스트  
- [ ] AWS CLI로 클러스터 상태 확인  

> **📊 비용 주의사항:** DocumentDB는 **무료 트라이어**가 제공되며, `db.t3.micro` 인스턴스로 7일간 사용 가능합니다. 장기 사용 시 **On-Demand** 또는 **Reserved Instances**를 고려해야 합니다.

## ⌨️ AWS CLI로 DocumentDB 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**필수 사전 조건:**  
- AWS CLI가 최신 버전으로 설치되어 있어야 합니다.  
- `docdb` 서비스에 대한 접근 권한이 있는 IAM 역할이 부여되어 있어야 합니다.  
- CLI 환경에서 `aws configure`로 AWS 자격 증명(ACCESS_KEY, SECRET_KEY, REGION)이 설정되어 있어야 합니다.  

> **💡 Tip:** 프리티어로 750시간의 DocumentDB 클러스터 사용이 가능합니다. `aws docdb describe-clusters`로 리소스 사용량을 확인하세요.

---

### 예제 1: DocumentDB 리소스 조회
```bash
# DocumentDB 클러스터 목록 조회
aws docdb list-clusters --query 'Clusters[*].ClusterIdentifier' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | JSON 응답에서 필드 필터링 | `'Clusters[*].ClusterIdentifier'` |
| `--output` | 출력 형식 | `json`, `table`, `text` |

**예상 출력:**
```
ClusterIdentifier
------------------
my-cluster-1
my-cluster-2
```

> **⚠️ 주의:** `list-clusters`는 클러스터 이름만 반환합니다. 상세 정보는 `describe-clusters`를 사용하세요.

---

### 예제 2: DocumentDB 리소스 생성
```bash
# DocumentDB 클러스터 생성
aws docdb create-cluster \
    --cluster-name "my-cluster-1" \
    --engine-version "3.6.5" \
    --master-username "admin" \
    --master-user-password "SecureP@ssw0rd!" \
    --cluster-identifier "my-cluster-1" \
    --engine "docdb" \
    --node-type "db.r4.large" \
    --num-nodes 1
```

**필수 옵션:**
- `--cluster-name`: 클러스터 이름 (필수)
- `--engine-version`: MongoDB 버전 (예: `3.6.5`)
- `--master-username`: 관리자 계정 이름
- `--master-user-password`: 관리자 비밀번호
- `--engine`: `docdb`로 고정
- `--node-type`: 노드 타입 (예: `db.r4.large`)
- `--num-nodes`: 노드 수 (1 이상)

**예상 출력:**
```json
{
    "Cluster": {
        "ClusterIdentifier": "my-cluster-1",
        "ClusterStatus": "active",
        "ClusterArn": "arn:aws:docdb:region:account-id:cluster:my-cluster-1"
    }
}
```

> **💡 Tip:** 클러스터 생성 후 `describe-clusters --cluster-identifier "my-cluster-1"`로 상태 확인 가능합니다.

---

### 예제 3: DocumentDB 리소스 수정
```bash
# 클러스터 관리자 비밀번호 변경
aws docdb update-cluster \
    --cluster-identifier "my-cluster-1" \
    --master-user-password "NewSecureP@ssw0rd!"
```

**필수 옵션:**
- `--cluster-identifier`: 수정할 클러스터 ID
- `--master-user-password`: 새 비밀번호

> **⚠️ 주의:** `update-cluster`는 클러스터 재시작을 유발할 수 있으니, 비상 시점에만 사용하세요.

---

### 예제 4: DocumentDB 리소스 삭제
```bash
# 클러스터 삭제
aws docdb delete-cluster --cluster-identifier "my-cluster-1"

# 삭제 확인 (사용자 삭제 후 5분 이내에만 가능)
aws docdb describe-clusters --cluster-identifier "my-cluster-1"
```

**삭제 후 상태 확인 예시:**
```
{
    "Clusters": [
        {
            "ClusterIdentifier": "my-cluster-1",
            "ClusterStatus": "deleting"
        }
    ]
}
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. `delete-cluster` 실행 전, `describe-clusters`로 상태를 반드시 확인하세요.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws docdb list-clusters
aws docdb describe-clusters --cluster-identifier "my-cluster-1"

# 생성
aws docdb create-cluster \
    --cluster-name "my-cluster-2" \
    --engine-version "3.6.5" \
    --master-user-password "SecureP@ssw0rd!" \
    --engine "docdb" \
    --node-type "db.r4.large" \
    --num-nodes 1

# 수정
aws docdb update-cluster \
    --cluster-identifier "my-cluster-2" \
    --master-user-password "NewSecureP@ssw0rd!"

# 삭제
aws docdb delete-cluster --cluster-identifier "my-cluster-2"
```

> **💡 Tip:** CLI 명령어는 `aws docdb help`로 전체 명령어 목록을 확인할 수 있습니다. `--region` 옵션으로 리전을 명시적으로 설정하세요.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 DocumentDB 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **문서 모델과 JSON 지원**  
   - 설명: DocumentDB는 MongoDB와 호환되는 JSON 기반 문서 모델을 제공해 구조화되지 않은 데이터를 저장/검색 가능. 이는 NoSQL 데이터베이스의 핵심 기능으로, 시험에서 문서 구조, 필드 타이핑, 배열 지원 등에 대한 이해를 묻는 경우가 많음.  
   - 키워드: `JSON`, `문서 모델`, `MongoDB 호환`

2. **수평 확장과 자동 백업**  
   - 설명: DocumentDB는 멀티 AZ 배포 및 자동 백업 기능으로 고가용성과 데이터 안정성을 보장. 시험에서 장애 헤이스트, 데이터 복구, 스토어드 프로시저 지원 등에 대한 지식이 필수.  
   - 키워드: `수평 확장`, `자동 백업`, `멀티 AZ`

3. **비용 효율성과 스토리지 관리**  
   - 설명: DocumentDB는 스토리지 기반 요금제로 사용량에 따라 비용이 변동. 시험에서 스토리지 크기, IOPS, 네트워크 트래픽 등 비용 요소를 분석하는 문제가 자주 출제됨.  
   - 키워드: `요금제`, `스토리지`, `IOPS`

4. **지연 시간 최소화 및 쿼리 성능**  
   - 설명: DocumentDB의 인덱스 최적화, 쿼리 성능 향상, 실시간 데이터 처리 능력은 고성능 애플리케이션에 필수적. 시험에서 지연 시간 최소화 기법, 쿼리 최적화 전략 등이 테스트됨.  
   - 키워드: `지연 시간`, `인덱스`, `성능 최적화`

5. **비슷한 서비스와의 차이점 (DynamoDB, MongoDB)**  
   - 설명: DocumentDB는 MongoDB 호환 DB로, DynamoDB는 NoSQL 기반의 키-값 저장소. 시험에서 두 서비스의 차이점(예: 데이터 모델, 스케일링 방식, 비용 구조)을 구분하는 문제가 자주 등장.  
   - 키워드: `MongoDB 호환`, `DynamoDB`, `데이터 모델`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "DocumentDB는 DynamoDB와 동일한 데이터 모델을 사용한다"는 문장이 주어졌을 때 헷갈릴 수 있음 | DocumentDB는 MongoDB 호환 문서 모델, DynamoDB는 키-값 기반 모델로 구조가 다름 |
| 함정 2 | "DocumentDB는 스토리지 비용 대신 시간 기반 요금제를 사용한다"는 문장이 주어졌을 때 오답 선택 가능성 | DocumentDB는 스토리지 기반 요금제를 사용하며, 시간 기반 요금제는 Timestream 등에 해당 |
| 함정 3 | "DocumentDB는 자동 백업 기능이 없고 수동 백업만 지원한다"는 문장이 주어졌을 때 오답 선택 가능성 | DocumentDB는 멀티 AZ 배포와 자동 백업 기능을 제공해 고가용성 보장 |

#### 🔄 DocumentDB vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | DocumentDB | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | MongoDB 호환 문서 저장소, JSON 데이터 처리 | DynamoDB (키-값 저장소), MongoDB (오픈소스 NoSQL) | JSON 기반 데이터 저장이 필요할 때 |
| 확장성 | 수평 확장, 멀티 AZ 지원 | DynamoDB: 수평 확장, MongoDB: 수평/수직 확장 | 고가용성 및 대규모 데이터 처리 시 |
| 비용 | 스토리지 기반 요금제 | DynamoDB: 요청 기반 요금제, MongoDB: 인스턴스 기반 요금제 | 데이터 저장량에 따라 비용이 변동할 때 |
| 지연 시간 | 인덱스 최적화 및 쿼리 성능 우수 | DynamoDB: 저지연 쿼리, MongoDB: 성능 최적화 필요 | 실시간 데이터 처리 및 고성능 필요 시 |

#### 📝 시험 대비 체크리스트
- [ ] DocumentDB의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  *예: "MongoDB 호환 JSON 문서 데이터베이스로 고가용성과 스케일링을 제공합니다."*
- [ ] DocumentDB를 선택해야 하는 시나리오를 알고 있는가?  
  *예: "JSON 구조 데이터 저장, 실시간 쿼리 처리, MongoDB 호환 필요 시"*
- [ ] DocumentDB의 제한사항/한계를 알고 있는가?  
  *예: "관계형 데이터베이스와의 호환성 부족, 특정 쿼리 성능 제한"*
- [ ] DocumentDB와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  *예: "DynamoDB는 키-값 저장소, MongoDB는 오픈소스 NoSQL, DocumentDB는 MongoDB 호환 DB"*
- [ ] DocumentDB의 비용 구조를 이해하고 있는가?  
  *예: "스토리지 크기 기반 요금제, IOPS 및 네트워크 트래픽 요금제 적용"*

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 DocumentDB를 떠올리세요:  
> - `JSON 문서 저장소`  
> - `MongoDB 호환`  
> - `수평 확장`  
> - `스토리지 기반 요금제`  
> - `자동 백업`

---

| [⬅️ Neptune](./Neptune.md) | [📑 Day 3 목차](./README.md) | [🏠 Week 3](../README.md) | [Keyspaces ➡️](./Keyspaces.md) |

---
