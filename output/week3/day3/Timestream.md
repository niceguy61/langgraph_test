---

| [⬅️ QLDB](./QLDB.md) | [📑 Day 3 목차](./README.md) | [🏠 Week 3](../README.md) | [다음 Day ➡️](../day4/README.md) |

---

# Timestream 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** Timestream은 시간 시리즈 데이터를 효율적으로 저장, 분석 및 시각화하기 위한 AWS의 **시간 시리즈 데이터베이스** 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 전통적인 데이터베이스에서 시간 시리즈 데이터(예: 센서 데이터, 로그, 메트릭)를 저장할 때 스토리지 비용이 높고, 대규모 데이터 처리 시 성능 저하가 발생했습니다.  
  *구체적 설명:* 기존 DB에서 시간 기반 데이터를 저장할 경우, 테이블을 매번 재구성하거나 파티셔닝이 필요해 복잡도가 증가했습니다.  
- **문제 2:** 시간 시리즈 데이터를 실시간으로 분석하고, 이전 데이터와 비교해 트렌드를 파악하는 데 복잡한 ETL 프로세스가 필요했습니다.  
  *구체적 설명:* 실시간 분석을 위해 데이터를 스토리지에 저장 후 분석 시스템으로 전송해야 하며, 처리 지연이 발생했습니다.  
- **문제 3:** 데이터 저장 시 데이터의 품질 보증(예: 누락된 데이터, 중복 데이터)과 데이터 보안이 약했습니다.  
  *구체적 설명:* 데이터 입력 시 누락이나 중복이 발생할 수 있으며, 보안 설정을 별도로 관리해야 했습니다.  

**Timestream로 해결:**
- **해결 1:** 자동 스토리지 관리 및 스케일링 기능으로 저장 비용을 최적화하고, 대규모 데이터 처리 시 성능을 보장합니다.  
  *구체적 설명:* Timestream은 데이터를 시간 기반으로 자동 파티셔닝해 저장하며, 최대 100TB 이상의 데이터를 처리할 수 있습니다.  
- **해결 2:** 실시간 분석을 위한 내장된 쿼리 기능과 기계 학습 기능으로 데이터를 즉시 분석할 수 있습니다.  
  *구체적 설명:* Timestream은 SQL 기반 쿼리와 기계 학습 기능을 결합해 실시간으로 트렌드를 분석할 수 있습니다.  
- **해결 3:** 데이터 입력 시 자동 데이터 유효성 검사와 암호화 기능을 통해 데이터 품질과 보안을 보장합니다.  
  *구체적 설명:* Timestream은 데이터 입력 시 자동으로 누락된 데이터를 식별하고, 암호화를 통해 데이터를 보호합니다.  

### 비유로 이해하기
Timestream은 **스마트 온도 조절기**에 비유할 수 있습니다. 집의 온도를 실시간으로 감지하고, 과거 데이터를 기반으로 자동으로 온도를 조절해 최적의 환경을 만듭니다. 마찬가지로, Timestream은 센서 데이터를 실시간으로 저장하고, 이전 데이터를 분석해 트렌드를 예측해 줍니다. 이처럼 시간에 따른 데이터를 효율적으로 관리하고 분석하는 것이 핵심입니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | IoT 센서 데이터 저장 및 분석 | 제조 시설의 센서 데이터를 실시간으로 저장하고, 이상 징후를 감지 |
| 시나리오 2 | 금융 거래 메트릭 추적 | 주식 시장의 실시간 거래 데이터를 분석해 트렌드를 예측 |
| 시나리오 3 | 애플리케이션 성능 모니터링 | 웹 애플리케이션의 CPU 사용량, 요청 처리 시간 등을 분석해 성능 최적화 |

**이럴 때 Timestream를 선택하세요:**
- ✅ **시간 기반 데이터 저장 및 실시간 분석이 필요**한 경우  
- ✅ **스토리지 비용 최적화와 대규모 데이터 처리**가 필요한 경우  
- ✅ **데이터 품질 보증과 보안**을 고려해야 할 때  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **복잡한 분석**(예: 병합, 조인)이 필요한 경우 → **Redshift** 또는 **BigQuery**  
- ❌ **NoSQL 데이터 저장**이 필요한 경우 → **DynamoDB**  
- ❌ **그래프 데이터베이스**가 필요한 경우 → **Neptune**

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **Kinesis** | 실시간 데이터 스트리밍을 위해 Timestream에 데이터 입력 | User → Kinesis → Timestream → QuickSight |
| **Lambda** | Timestream 데이터를 실시간으로 처리 및 변환 | Timestream → Lambda → S3 |
| **QuickSight** | Timestream 데이터를 시각화하여 보고서 생성 | Timestream → QuickSight |

**자주 사용되는 아키텍처 패턴:**
```
[사용자] → [Kinesis Data Stream] → [Timestream] → [QuickSight]
```  
*예시: 센서 데이터를 Kinesis로 수집 후 Timestream에 저장하고, QuickSight로 시각화해 분석합니다.*

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **데이터 저장** | $0.0065/GB/월 | 월 10GB 무료 |
| **쿼리 처리** | $0.0045/10,000개 쿼리 | 10,000개 쿼리 무료 |
| **데이터 전송** | $0.02/GB/월 | 항상 무료 |

**비용 최적화 팁:**
1. 💡 **프리티어 활용:** 초기 테스트 시 10GB 저장과 10,000개 쿼리 무료 혜택을 사용하세요.  
2. 💡 **쿼리 최적화:** 복잡한 쿼리 대신 파티션 키를 활용해 데이터 분할하여 성능 향상.  
3. 💡 **데이터 압축:** 저장 전 데이터를 압축해 스토리지 비용 절감.  

> **⚠️ 비용 주의:** 실시간 쿼리가 많거나, 대량의 데이터 저장이 필요한 경우 비용이 급격히 증가할 수 있으니, **CloudWatch**를 사용해 비용 모니터링을 꼭 수행하세요.

## 📚 핵심 개념

### 개념 1: **Timestream의 시간 시리즈 데이터 처리**
Timestream은 시간 순서로 저장되는 데이터(시간 시리즈 데이터)를 효율적으로 저장, 분석, 시각화하는 서비스입니다. 기존의 관계형 데이터베이스나 NoSQL 데이터베이스는 시간 기반 데이터를 처리하는 데 한계가 있었지만, Timestream은 실시간으로 수집되는 센서 데이터, 로그, IoT 데이터 등을 빠르게 처리하고, 분석을 위한 최적화된 스토리지 아키텍처를 제공합니다. Timestream은 데이터를 **time-series** 형식으로 저장해, 시간 기반의 패턴 분석, 트렌드 예측, 실시간 대시보드 생성 등이 가능합니다. 또한, AWS Lambda, Amazon QuickSight, AWS IoT Core 등과의 원활한 연동을 통해 데이터 파이프라인을 간소화합니다.

#### 왜 중요한가?
- **실시간 분석의 효율성**: 시간 시리즈 데이터를 빠르게 처리해 대규모 데이터의 실시간 분석이 가능합니다.  
- **데이터 저장 비용 최적화**: 키-값 스토리지 구조를 통해 데이터 저장 비용을 낮추고, 필요에 따라 스케일링이 가능합니다.  
- **분석 기능 통합**: AWS의 분석 서비스와의 연동으로 데이터 처리부터 시각화까지 일관된 플랫폼을 제공합니다.  

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **Time Series 데이터 형식** | 시간(UTC)을 기준으로 데이터를 저장합니다. | 센서 데이터: `2023-10-05T14:30:00Z, 25.3°C` |
| **스토리지 최적화** | 데이터를 빈도(예: 분, 시간, 일)별로 분할해 저장합니다. | 1분 단위로 분할된 데이터는 1시간 단위로 합쳐집니다. |
| **분석 기능 지원** | SQL 기반 쿼리로 시간 기반 데이터를 분석합니다. | `SELECT AVG(temperature) FROM sensor_data WHERE time > '2023-10-05'` |

> **💡 Tip:** 실무에서는 IoT 장치의 실시간 데이터 수집, 로그 분석, 머신러닝 모델의 학습 데이터로 활용할 수 있습니다. 예를 들어, 제조 설비의 센서 데이터를 Timestream에 저장해 이상 징후를 실시간으로 탐지할 수 있습니다.

---

### 개념 2: **Timestream의 자동 스케일링 및 쿼리 최적화**
Timestream은 데이터 저장 공간과 쿼리 처리 능력을 **자동으로 조절**해 사용자에게 최적의 성능과 비용을 제공합니다. 데이터가 많아질수록 자동으로 스토리지 크기를 증가시키고, 쿼리 요청이 늘어날수록 처리 능력을 확장합니다. 또한, Timestream은 쿼리 시 데이터를 **필터링**하고 **집계**하여 불필요한 데이터를 최소화해 처리 속도를 높입니다. 이 기능은 고성능 분석과 저비용 운영을 동시에 가능하게 합니다.

#### 작동 원리
1. **데이터 저장 시 자동 스케일링**: 데이터가 저장될 때, Timestream은 데이터의 양에 따라 스토리지 크기를 자동으로 조절합니다.  
2. **쿼리 시 최적화**: 쿼리 실행 시, Timestream은 데이터를 필터링하고 필요한 데이터만 추출해 처리 시간을 단축합니다.  
3. **비용 최소화**: 사용량에 따라 자동으로 리소스를 조절해 비용을 줄이고, 필요 시 추가 비용을 발생시킵니다.  

> **💡 Tip:** 실무에서는 쿼리 성능을 개선하기 위해 `WHERE` 조건을 명확히 설정해 불필요한 데이터를 제거하는 것이 중요합니다. 예를 들어, 특정 시간대만 분석할 경우, `WHERE time BETWEEN '2023-10-01' AND '2023-10-07'` 조건을 사용하면 처리 시간이 줄어듭니다.

---

### 개념 3: **Timestream의 데이터 저장 및 분석 아키텍처**
Timestream은 데이터를 저장하고 분석하는 데 **세 가지 주요 구성 요소**를 제공합니다.  
1. **Time Series 데이터 저장**: 데이터를 시간 기반으로 저장해 빠른 접근이 가능합니다.  
2. **쿼리 처리**: SQL 기반 쿼리로 데이터를 분석하고, 실시간으로 결과를 반환합니다.  
3. **분석 결과 시각화**: Amazon QuickSight와 연동해 데이터를 시각화해 보고서를 생성할 수 있습니다.  

#### 주요 특징
1. ******시간 기반 저장 구조**: 데이터를 시간 순으로 저장해, 특정 시간대의 데이터를 빠르게 조회할 수 있습니다.  
2. ******SQL 기반 쿼리**: 사용자 친화적인 SQL 문법을 통해 데이터를 필터링, 집계, 그룹화할 수 있습니다.  
3. ******AWS 서비스 통합**: Lambda, QuickSight, IoT Core 등과의 연동을 통해 데이터 파이프라인을 단순화합니다.  

> **💡 Tip:** 분석을 위해 데이터를 저장할 때는 데이터의 **시간 범위**와 **필드 정의**를 명확히 설정해, 쿼리 시 성능 최적화를 도모하세요. 예를 들어, 센서 데이터를 저장할 때 `temperature`, `humidity` 필드를 정의해 분석 시 필터링이 용이합니다.

## 🖥️ AWS 콘솔에서 Timestream 사용하기

### Step 1: Timestream 서비스 접속  
1. AWS Management Console에 로그인합니다.  
   - URL: https://console.aws.amazon.com  
   - **AWS 계정 ID**와 **IAM 권한**이 정상적으로 설정되어 있어야 합니다.  
2. 상단 검색창에서 **"Timestream"**을 입력한 후 검색합니다.  
   - **"Timestream"**을 선택하면 대시보드로 이동합니다.  
3. Timestream 대시보드에서 **"Databases"** 탭을 클릭해 데이터베이스 목록을 확인합니다.  

> **📸 화면 확인:**  
> - 로그인 후 Timestream 대시보드가 표시되면 정상입니다.  
> - **"Databases"** 탭이 활성화된 상태인지 확인하세요.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **데이터베이스 생성**  
   - **"Create database"** 버튼을 클릭합니다.  
   - **Database name**에 `example_db` 입력 후 **"Create"** 버튼을 누릅니다.  
   - **"Data retention"** 설정은 기본값 **1 day**로 자동 적용됩니다.  
2. **테이블 생성**  
   - 생성된 데이터베이스(`example_db`) 클릭 후 **"Create table"** 버튼을 누릅니다.  
   - **Table name**에 `sensor_data` 입력하고, **"Time attribute"**로 `timestamp`를 선택합니다.  
   - **"Data retention"**을 **7 days**로 변경해 장기 저장을 설정합니다.  
3. **확인 사항**  
   - 생성된 데이터베이스와 테이블이 **"Databases"** 탭에 표시되는지 확인합니다.  
   - **"Data retention"** 설정이 변경되었는지 확인하세요.  

> **📸 화면 확인:**  
> - **"Databases"** 탭에서 `example_db`와 `sensor_data`가 표시되는지 확인합니다.  
> - **"Data retention"** 설정이 **7 days**로 변경되었는지 확인합니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **데이터 저장 기간 조정**  
   - 데이터베이스(`example_db`) 클릭 후 **"Edit"** 버튼을 누릅니다.  
   - **"Data retention"** 값을 **30 days**로 변경하고 **"Save"** 버튼을 클릭합니다.  
2. **데이터 저장 용량 설정**  
   - **"Capacity"** 탭에서 **"Auto Scaling"**을 활성화하고, **"Read capacity"**를 `10`으로 설정합니다.  
   - **"Write capacity"**는 기본값 **100**로 자동 적용됩니다.  
3. **로그 기능 활성화**  
   - **"Logs"** 탭에서 **"Enable logging"**을 클릭해 쿼리 로그를 수집합니다.  

> **⚠️ 주의:**  
> - **"Data retention"** 설정이 너무 짧으면 데이터가 삭제될 수 있습니다.  
> - **Auto Scaling**을 사용하면 비용이 증가할 수 있으므로, 실무에서는 리소스 사용량을 모니터링해야 합니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인 방법**  
   - **"Databases"** 탭에서 `example_db` 클릭 후 **"Tables"** 섹션에서 `sensor_data` 테이블을 확인합니다.  
   - **"Capacity"** 탭에서 **"Auto Scaling"**이 활성화되었는지 확인합니다.  
2. ** 상태 확인 방법**  
   - **"Overview"** 탭에서 **"Status"**가 **"ACTIVE"**인지 확인합니다.  
   - **"Data retention"**이 **30 days**로 설정되었는지 확인하세요.  
3. **정상 동작 테스트 방법**  
   - **"Query editor"**에서 다음 쿼리 실행:  
     ```sql
     SELECT * FROM "example_db"."sensor_data" WHERE timestamp > '2023-10-01'
     ```  
   - 결과가 정상적으로 표시되면 설정이 완료되었습니다.  

> **💡 Tip:**  
> - Timestream은 **1GB**까지 무료로 사용할 수 있는 **프리티어**가 제공됩니다.  
> - 쿼리 성능 테스트는 **"Query editor"**에서 직접 확인할 수 있습니다.

## ⌨️ AWS CLI로 Timestream 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

> **💡 Tip:** AWS CLI를 사용하기 전에 `aws configure`로 액세스 키, 비밀번호, 리전을 설정해야 합니다. 설정 방법은 [AWS CLI 설정 가이드](https://docs.aws.amazon.com/cli/latest/userguide/cli-configure-files.html)를 참고하세요.

---

### 예제 1: Timestream 리소스 조회
```bash
# Timestream 데이터베이스 목록 조회
aws timestream write list-databases --query 'Databases[*].DatabaseName' --output table

# Timestream 테이블 목록 조회
aws timestream write list-tables --query 'Tables[*].TableName' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 결과 필터링 (예: `Databases[*].DatabaseName`) | `'Databases[*].DatabaseName'` |
| `--output` | 출력 형식 (json, table, text) | `table` |

**예상 출력:**
```
DatabaseName
------------------
example-database
```

---

### 예제 2: Timestream 리소스 생성
```bash
# Timestream 데이터베이스 생성
aws timestream write create-database \
    --database-name "example-database" \
    --retention-properties "RetentionPeriodInDays=7"
```

```bash
# Timestream 테이블 생성
aws timestream write create-table \
    --database-name "example-database" \
    --table-name "example-table" \
    --retention-properties "RetentionPeriodInDays=7" \
    --magnetic-store-write-properties "EnableMagneticStoreWrite=TRUE"
```

**필수 옵션:**
- `--database-name`: 데이터베이스 이름 (데이터베이스 생성 시 필수)
- `--table-name`: 테이블 이름 (테이블 생성 시 필수)
- `--retention-properties`: 데이터 보존 기간 설정 (예: `RetentionPeriodInDays=7`)

**예상 출력:**
```json
{
    "DatabaseName": "example-database",
    "DatabaseArn": "arn:aws:timestream:region:account-id:database/example-database",
    "Status": "ACTIVE"
}
```

---

### 예제 3: Timestream 리소스 수정
```bash
# Timestream 테이블 보존 기간 수정
aws timestream write update-table \
    --database-name "example-database" \
    --table-name "example-table" \
    --retention-properties "RetentionPeriodInDays=14"
```

> **💡 Tip:** Timestream은 테이블의 보존 기간과 자동 스케일링 설정만 수정 가능합니다. 다른 속성은 변경할 수 없습니다.

---

### 예제 4: Timestream 리소스 삭제
```bash
# Timestream 테이블 삭제
aws timestream write delete-table \
    --database-name "example-database" \
    --table-name "example-table"

# 삭제 확인 (테이블 상태 조회)
aws timestream write describe-table \
    --database-name "example-database" \
    --table-name "example-table"
```

> **⚠️ 주의:** 삭제된 리소스는 복구할 수 없습니다. `Status`가 `DELETING`으로 표시되면 즉시 작업을 중단해야 합니다.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws timestream write list-databases
aws timestream write describe-table --database-name "db-name" --table-name "table-name"

# 생성
aws timestream write create-database --database-name "db-name"
aws timestream write create-table --database-name "db-name" --table-name "table-name"

# 수정
aws timestream write update-table --database-name "db-name" --table-name "table-name" --retention-properties "RetentionPeriodInDays=14"

# 삭제
aws timestream write delete-table --database-name "db-name" --table-name "table-name"
``` 

> **💡 Tip:** Timestream은 데이터베이스 생성 후 테이블을 생성해야 합니다. 데이터베이스가 존재하지 않는 경우 `NotFoundException`이 발생합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Timestream 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **시간 시리즈 데이터 처리 및 사용 사례**  
   - 설명: Timestream은 시간 기반 데이터(예: IoT 센서, 로그, 실시간 분석)를 효율적으로 저장하고 쿼리하는 데 최적화된 서비스입니다. 시험에서는 Timestream이 적합한 시나리오(예: 실시간 메트릭 분석, 장비 상태 모니터링)와 다른 데이터베이스 서비스(예: Redshift, DynamoDB)와의 차이점을 묻는 문제가 자주 출제됩니다.  
   - 키워드: `시간 시리즈`, `실시간 분석`, `IoT 데이터`

2. **아키텍처 및 구성 요소**  
   - 설명: Timestream은 데이터 저장소(Storage)와 메트릭 저장소(Metric Store)로 구성되며, 자동 스케일링과 저지연 쿼리 처리를 지원합니다. 시험에서는 이 구성 요소의 역할과 어떻게 데이터가 처리되는지에 대한 이해가 필수적입니다.  
   - 키워드: `Storage`, `Metric Store`, `자동 스케일링`

3. **비용 구조 및 프리티어 활용**  
   - 설명: Timestream은 쿼리 수에 따라 비용이 발생하며, 스토리지 비용은 무료입니다. 시험에서는 "왜 Timestream이 저비용으로 시간 시리즈 데이터를 처리할 수 있는가?"와 같은 질문이 자주 등장합니다. 프리티어의 한도(예: 100만 쿼리/월)도 중요합니다.  
   - 키워드: `pay-per-query`, `프리티어`, `스토리지 비용`

4. **확장성과 성능 최적화**  
   - 설명: Timestream은 수평 확장과 자동 파티셔닝을 통해 대규모 데이터 처리를 지원합니다. 시험에서는 "Timestream이 다른 데이터베이스에 비해 어떤 장점을 가지는가?"와 같은 질문에 대해 확장성과 저지연 쿼리 처리를 강조해야 합니다.  
   - 키워드: `수평 확장`, `저지연`, `파티셔닝`

5. **비슷한 서비스와의 차이점**  
   - 설명: Timestream은 Redshift(데이터 웨어하우스), DynamoDB(NoSQL), InfluxDB(오픈소스)와 비교됩니다. 시험에서는 "Timestream이 어떤 시나리오에서 적합한가?"와 같은 문제에서 다른 서비스의 한계(예: Redshift의 고비용, DynamoDB의 복잡한 쿼리 처리)를 고려한 비교가 필요합니다.  
   - 키워드: `Redshift`, `DynamoDB`, `InfluxDB`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "Timestream은 스토리지 비용이 발생하므로 비용 효율적이지 않다"는 문장이 주어질 수 있습니다. | Timestream은 스토리지 비용이 무료이며, 쿼리 수에 따라 비용이 발생하므로 **정답은 'False'**입니다. |
| 함정 2 | "Timestream은 실시간 데이터 분석에 적합하지 않다"는 질문이 있을 수 있습니다. | Timestream은 실시간 메트릭 분석과 쿼리 처리에 최적화되어 있으므로 **정답은 'False'**입니다. |
| 함정 3 | "Timestream과 Redshift는 동일한 아키텍처를 가진다"는 문장이 있을 수 있습니다. | Timestream은 분산 저장소 기반, Redshift는 관계형 데이터 웨어하우스 기반으로 **다른 아키텍처**를 가진다. |

#### 🔄 Timestream vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | Timestream | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 시간 시리즈 데이터 저장 및 분석 | Redshift(데이터 웨어하우스), DynamoDB(NoSQL) | 실시간 메트릭, 로그 분석에 적합 |
| 확장성 | 자동 파티셔닝 및 수평 확장 | Redshift(수동 스크일링), DynamoDB(수평 확장) | 대규모 데이터 처리 시 Timestream의 유연성 |
| 비용 | 쿼리 수 기반, 스토리지 비용 무료 | Redshift(스토리지 및 쿼리 비용), DynamoDB(스토리지 및 요청 비용) | 저비용으로 시간 시리즈 데이터 처리 시 |
| 지연시간 | 저지연 쿼리 처리 | Redshift(고지연), DynamoDB(고지연) | 실시간 분석 시 Timestream의 우월성 |

#### 📝 시험 대비 체크리스트
- [ ] Timestream의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  예: "Timestream은 시간 기반 데이터를 효율적으로 저장하고 분석하는 전용 데이터베이스 서비스입니다."
- [ ] Timestream를 선택해야 하는 시나리오를 알고 있는가?  
  예: IoT 장치 데이터 수집, 실시간 로그 분석, 메트릭 모니터링
- [ ] Timestream의 제한사항/한계를 알고 있는가?  
  예: 복잡한 관계형 쿼리 처리에 약점, 실시간 데이터 분석에만 최적화
- [ ] Timestream과 비슷한 서비스의 차이점을 설명할 수 있는가?  
  예: Redshift는 데이터 웨어하우스, DynamoDB는 NoSQL, Timestream은 시간 시리즈에 특화
- [ ] Timestream의 비용 구조를 이해하고 있는가?  
  예: 쿼리 수에 따라 비용 발생, 스토리지 비용 무료

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Timestream를 떠올리세요:  
> - **시간 시리즈**  
> - **실시간 분석**  
> - **IoT 데이터**  
> - **저지연 쿼리**  
> - **pay-per-query**

---

| [⬅️ QLDB](./QLDB.md) | [📑 Day 3 목차](./README.md) | [🏠 Week 3](../README.md) | [다음 Day ➡️](../day4/README.md) |

---
