---

| [⬅️ ElastiCache](./ElastiCache.md) | [📑 Day 3 목차](./README.md) | [🏠 Week 3](../README.md) | [Neptune ➡️](./Neptune.md) |

---

# Redshift 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** Redshift는 대규모 데이터 분석을 위한 AWS의 데이터 웨어하우스 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 전통적인 데이터 웨어하우스는 복잡한 ETL 프로세스와 고비용의 하드웨어 투자로 인해 구축 및 유지가 어렵습니다. 예를 들어, 기업은 물리적인 서버를 구매해 데이터 저장소를 운영해야 했고, 확장성도 제한적이었습니다.
- **문제 2:** 대규모 데이터를 처리할 때 쿼리 성능이 저하되며, 실시간 분석이 불가능했습니다. 예를 들어, 매출 데이터를 실시간으로 분석해 재고 관리에 활용하려면 복잡한 인메모리 처리가 필요했습니다.
- **문제 3:** 데이터 보안 및 접근 제어 설정이 복잡해 데이터 유출 위험이 존재했습니다. 특히, 다양한 사용자 권한 관리가 필요했고, 이에 따른 운영 부담이 컸습니다.

**Redshift로 해결:**
- **해결 1:** 기존의 복잡한 ETL 프로세스를 대체해 클라우드 기반의 관리형 데이터 웨어하우스로 구축 가능합니다. AWS 관리 도구를 통해 자동화된 데이터 처리와 스케일링이 가능해집니다.
- **해결 2:** 컬럼형 스토리지와 MPP 아키텍처를 통해 대규모 데이터 처리 속도를 극대화합니다. 복잡한 쿼리도 빠르게 처리 가능하며, 실시간 분석이 가능해졌습니다.
- **해결 3:** IAM 정책과 VPC 설정을 통해 세밀한 접근 제어를 제공합니다. 데이터 보안을 강화하면서도 사용자 권한 관리가 간소화됩니다.

### 비유로 이해하기
Redshift는 대형 도서관의 책 검색 시스템을 상상해보세요. 책은 효율적으로 분류되어 있으며, 검색 시스템은 책 위치를 빠르게 찾아주고, 필요 시 복사본을 제공합니다. 데이터 분석 시, Redshift는 데이터를 효과적으로 정리해 빠르게 접근할 수 있도록 하며, 필요한 데이터만 빠르게 제공해 분석 효율을 높입니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 대규모 데이터 분석 및 보고서 생성 | 특정 기업이 매출 데이터를 분석해 월간 보고서를 생성하는 경우 |
| 시나리오 2 | 실시간 분석 및 대시보드 제공 | 온라인 쇼핑몰이 실시간으로 고객 행동 데이터를 분석해 맞춤형 추천 제공 |
| 시나리오 3 | 데이터 저장소 및 아카이빙 | 기업이 과거 거래 데이터를 보관해 법적 요구사항 충족 |

**이럴 때 Redshift를 선택하세요:**
- ✅ **대규모 데이터 분석이 필요한 경우** (예: 금융 분석, 마케팅 데이터 처리)
- ✅ **실시간 데이터 처리가 필요한 경우** (예: IoT 데이터 분석)
- ✅ **데이터 보안 및 접근 제어가 중요한 경우** (예: 보험사 고객 데이터 관리)

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **소규모 데이터 저장이 필요한 경우** → **S3** (저비용 저장 솔루션)
- ❌ **그래프 데이터 관리가 필요한 경우** → **Neptune** (그래프 데이터베이스)
- ❌ **문서형 데이터 저장이 필요한 경우** → **DocumentDB** (JSON 기반 데이터 저장)

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **AWS Glue** | ETL 작업 자동화 | 데이터 소스 → Glue → Redshift |
| **Amazon S3** | 대규모 데이터 저장 | S3 → Redshift (데이터 로드) |
| **Amazon QuickSight** | 데이터 시각화 | Redshift → QuickSight (BI 대시보드) |

**자주 사용되는 아키텍처 패턴:**
```
User → CloudFront → S3 → Redshift → QuickSight
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **데이터 스토리지** | $0.25/GB/월 | 월 15GB 무료 |
| **데이터 전송** | $0.01/GB | 12개월 100GB 무료 |
| ** 컴퓨팅 사용량** | $0.20/시간 | 항상 무료 |

**비용 최적화 팁:**
1. 💡 **예약 인스턴스 사용**: 장기 사용 시 예약 인스턴스로 60% 이상 비용 절감 가능
2. 💡 **데이터 압축**: 데이터 저장 공간을 50% 줄여 비용 절감
3. 💡 **쿼리 최적화**: 비효율적인 쿼리 수정으로 컴퓨팅 비용 감소

> **⚠️ 비용 주의:** 실시간 데이터 처리 시 데이터 전송 비용이 급증할 수 있으므로, 네트워크 대역폭 사용량을 모니터링해야 합니다.

## 📚 핵심 개념

### 개념 1: Redshift의 데이터 웨어하우스 아키텍처  
Redshift는 클라우드 기반의 데이터 웨어하우스 솔루션으로, 고성능의 대규모 데이터 분석을 지원합니다. 이는 기존의 온프레미스 데이터 웨어하우스 대비 비용 효율성과 확장성, 실시간 분석 능력을 제공합니다. Redshift는 MPP(메타포스 파라렐 프로세싱) 아키텍처를 기반으로 하여, 여러 노드가 병렬로 작업을 수행해 처리 속도를 극대화합니다. 데이터는 분산되어 저장되며, 각 노드는 고유한 파티션을 관리해 쿼리 성능을 높입니다. 또한, Redshift는 병렬 처리를 통해 대규모 데이터셋을 효율적으로 처리하고, 복잡한 분석 작업에 적합합니다.

#### 왜 중요한가?
- **고성능 분석**: MPP 아키텍처로 대규모 데이터셋을 빠르게 처리해 실시간 분석이 가능합니다.  
- **스케일링 유연성**: 필요에 따라 클러스터 크기를 조정해 자원 사용을 최적화할 수 있습니다.  

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| MPP 아키텍처 | 여러 노드가 병렬로 작업을 수행해 처리 속도를 높입니다 | 클러스터를 확장해 분석 작업의 성능을 개선 |
| 파티셔닝 | 데이터를 여러 노드에 분산해 쿼리 성능을 향상시킵니다 | `DISTKEY`로 데이터를 분산해 효율적인 조회 |
| 병렬 처리 | 여러 노드가 동시에 데이터를 처리해 처리 시간을 단축 | 대규모 데이터 분석 시 병렬 처리로 성능 향상 |

> **💡 Tip:** Redshift는 데이터 웨어하우스로 사용되며, ETL 작업 후 분석에 최적화된 환경을 제공합니다. 데이터 저장소로 활용해 대규모 분석을 수행할 수 있습니다.

---

### 개념 2: Neptune의 그래프 데이터베이스 및 SPARQL 쿼리  
Neptune은 AWS에서 제공하는 그래프 데이터베이스 서비스로, 인과 관계나 연결 관계를 기반으로 데이터를 저장하고 분석합니다. 이는 추천 시스템, 네트워크 분석, 지식 그래프 등에 사용됩니다. Neptune는 SPARQL이라는 질의 언어를 지원해, 그래프 데이터를 효율적으로 검색하고 분석할 수 있습니다. SPARQL은 RDF(Relational Data Format) 기반의 쿼리 언어로, 노드와 엣지의 관계를 기반으로 복잡한 쿼리를 작성할 수 있습니다. Neptune는 또한 실시간 분석과 대규모 데이터 처리를 지원해, 높은 확장성을 제공합니다.

#### 작동 원리
1. **그래프 데이터 저장**: 노드(Node), 엣지(Edge), 속성(Attribute)으로 데이터를 구조화해 저장합니다.  
2. **SPARQL 쿼리 실행**: 사용자가 작성한 SPARQL 쿼리를 기반으로 그래프 데이터를 검색하고 분석합니다.  
3. **실시간 분석 지원**: 병렬 처리 및 고성능 인덱스를 통해 대규모 데이터셋의 빠른 처리가 가능합니다.  

> **💡 Tip:** Neptune은 추천 시스템이나 네트워크 분석에 적합한 서비스로, SPARQL을 통해 복잡한 관계를 분석할 수 있습니다.

---

### 개념 3: QLDB의 불변성 및 트랜잭션 로그  
QLDB(Quantum Ledger Database)는 AWS에서 제공하는 분산 원장 기반 데이터베이스로, 데이터의 불변성과 트랜잭션 로그 기능을 통해 신뢰성 있는 데이터 관리를 지원합니다. QLDB는 모든 트랜잭션을 기록해, 데이터의 변경 내역을 추적할 수 있습니다. 이는 금융, 의료, 공급망 등 데이터 무결성이 중요한 분야에 적합합니다. QLDB는 또한 AWS Lambda와 통합해, 실시간으로 트랜잭션 로그를 처리할 수 있습니다. 트랜잭션 로그는 분석 및 감사 목적에 활용되며, 데이터의 신뢰성을 보장합니다.

#### 주요 특징
1. **불변성**: 모든 데이터 변경이 기록되어, 데이터가 의도치 않게 수정될 수 없습니다.  
2. **트랜잭션 로그**: 모든 변경 사항을 로그로 저장해 분석 및 감사에 활용할 수 있습니다.  
3. **AWS Lambda 통합**: 트랜잭션 로그를 실시간으로 처리해 자동화된 작업이 가능합니다.  

> **💡 Tip:** QLDB는 데이터 무결성과 감사 기능이 필요한 시스템에 적합하며, 트랜잭션 로그를 통해 데이터 변경 내역을 추적할 수 있습니다.

## 🖥️ AWS 콘솔에서 Redshift 사용하기

### Step 1: Redshift 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - 로그인 후, 좌측 메뉴에서 "Services"를 클릭한 후 "Redshift"를 검색합니다.  
2. 검색 결과에서 "Redshift"를 클릭하여 대시보드에 접근합니다.  
   - 이때 **"Clusters"** 탭이 자동으로 활성화되어야 합니다.  

> **📸 화면 확인:** Redshift 대시보드에서 "Clusters" 탭이 표시되고, "Create cluster" 버튼이 보이는지 확인합니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **"Create cluster" 버튼 클릭**  
   - "Create cluster" 버튼을 클릭하면 클러스터 생성 대화상자가 열립니다.  
   - **Cluster Identifier**: 클러스터의 고유한 이름을 입력합니다 (예: `my-redshift-cluster`).  
   - **Node Type**: `ml.m4.large` 또는 `dc2.large` 등의 옵션에서 선택합니다.  
   - **Number of Nodes**: 기본값은 1개 노드입니다.  

2. **Master User 설정**  
   - **Master Username**: `admin` 또는 다른 사용자명 입력.  
   - **Master Password**: 6자 이상의 복잡한 비밀번호 입력 (예: `P@ssw0rd!`).  
   - **Password Validation**: 비밀번호 정책을 확인하고 "Next"를 클릭합니다.  

3. **Security Group 및 Subnet Group 설정**  
   - **Security Group**: 기본적으로 생성된 그룹을 선택하거나, 새로운 그룹을 생성합니다.  
   - **Subnet Group**: VPC의 서브넷을 선택하여 클러스터를 배치합니다.  
   - **Publicly Accessible**: 외부 IP 접근 여부를 설정합니다.  

> **📸 화면 확인:** "Create cluster" 화면에서 모든 설정이 완료되고 "Create" 버튼이 활성화되었는지 확인합니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **클러스터 생성 후 대시보드 확인**  
   - "Clusters" 탭에서 생성된 클러스터를 찾아 클릭합니다.  
   - **Cluster Status**: "Creating" 상태에서 "Available"으로 전환될 때까지 기다립니다.  

2. **IAM 역할 및 보안 설정**  
   - "Security" 탭에서 IAM 역할을 연결해 데이터베이스 접근 권한을 부여합니다.  
   - **VPC 설정**: 클러스터가 배치된 VPC의 네트워크 보안 설정을 확인합니다.  

3. **Endpoint 및 접속 정보 확인**  
   - "Connection details" 섹션에서 **Endpoint URL**을 기록합니다.  
   - 예: `my-redshift-cluster.c7xj34567890.us-east-1.redshift.amazonaws.com`  

> **⚠️ 주의:** 클러스터 생성 후 **30분 이내에 접속하지 않으면 자동 종료될 수 있습니다**.  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인 방법**  
   - "Clusters" 탭에서 클러스터 이름을 클릭해 상세 정보를 확인합니다.  
   - **Cluster Status**: "Available" 상태일 때까지 기다립니다.  

2. ** 상태 확인 방법**  
   - "Cluster Details" 섹션에서 **Cluster Status**, **Cluster Type**, **Node Count**를 확인합니다.  

3. **정상 동작 테스트 방법**  
   - **AWS CLI**를 사용해 간단한 쿼리 실행:  
     ```bash
     psql "host=my-redshift-cluster.c7xj34567890.us-east-1.redshift.amazonaws.com port=5439 dbname=dev user=admin password=P@ssw0rd! sslmode=verify-full"
     ```
   - **pgAdmin** 또는 **DBeaver**로 연결해 테이블 생성 및 쿼리 테스트.  

> **💡 Tip:** Redshift는 데이터 분석에 최적화되어 있으므로, 복잡한 쿼리 성능을 확인하기 위해 `EXPLAIN` 명령어를 활용하세요.  

---

### ✅ 학습 체크리스트  
- [ ] AWS Console에 로그인 및 Redshift 서비스 접근  
- [ ] Redshift 클러스터 생성 완료 (Master User, Node Type 설정)  
- [ ] 보안 그룹 및 VPC 설정 확인  
- [ ] 클러스터 상태 확인 및 Endpoint URL 기록  
- [ ] CLI 또는 도구를 통한 접속 테스트 완료  

### 💰 비용 관리 팁  
- **Free Tier**: 1개 노드 클러스터는 7일간 무료 (월 100달러 이하)  
- **비용 최적화**:  
  - 노드 수를 최소한으로 설정  
  - 클러스터 종료 시 **"Pause"** 옵션 사용  
  - 데이터 저장소를 **Redshift Spectrum**으로 전환  

> **⚠️ 주의:** Redshift는 데이터 저장소가 크면 수요/공급 요금이 발생하므로, **"Resize"** 전 쿼리 성능을 체크하세요.

## ⌨️ AWS CLI로 Redshift 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**설명:**  
AWS CLI를 사용하기 전에 버전을 확인하고, AWS 계정의 자격 증명이 정확히 설정되어 있는지 확인해야 합니다. `aws sts get-caller-identity` 명령어는 현재 로그인한 사용자의 AWS 계정 ID, 유저명, 리전을 표시합니다. 리전이 올바르게 설정되지 않은 경우, Redshift 리소스 조회/생성 시 오류가 발생할 수 있습니다.

---

### 예제 1: Redshift 리소스 조회
```bash
# Redshift 클러스터 목록 조회
aws redshift describe-clusters --query "Clusters[].ClusterIdentifier" --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 결과 필터링 | `"Clusters[].ClusterIdentifier"` |
| `--output` | 출력 형식 | `table` (테이블 형식으로 출력) |

**예상 출력:**
```
ClusterIdentifier
-------------------
my-cluster-1
my-cluster-2
```

**주의사항:**  
`describe-clusters` 명령어는 클러스터의 ID, 상태, 네트워크 설정 등을 확인할 수 있습니다. `--query`를 사용해 특정 필드만 추출할 수 있으며, `--output table`은 테이블 형식으로 결과를 보기 쉽게 만듭니다.

---

### 예제 2: Redshift 리소스 생성
```bash
# Redshift 클러스터 생성
aws redshift create-cluster \
    --cluster-identifier "my-cluster-1" \
    --node-type "dc2.large" \
    --master-username "admin" \
    --master-user-password "SecurePass123!" \
    --cluster-type "single-node" \
    --port 5439
```

**필수 옵션:**
- `--cluster-identifier`: 클러스터 고유 식별자 (예: `my-cluster-1`)
- `--node-type`: 노드 타입 (예: `dc2.large`, `rs3.large`)
- `--master-username`: 데이터베이스 관리자 계정명
- `--master-user-password`: 관리자 계정 비밀번호
- `--cluster-type`: 클러스터 유형 (단일 노드/마스터-슬레이브)

**예상 출력:**
```json
{
    "Cluster": {
        "ClusterIdentifier": "my-cluster-1",
        "ClusterStatus": "creating",
        "NodeType": "dc2.large",
        "MasterUsername": "admin"
    }
}
```

**비용 주의사항:**  
Redshift 클러스터 생성 시 `dc2.large` 이상의 노드 타입을 선택해야 하며, `--port`는 클라이언트 연결 포트를 지정합니다. 생성 후 **AWS 관리 콘솔**에서 클러스터 상태를 확인하세요.  

---

### 예제 3: Redshift 리소스 수정
```bash
# Redshift 클러스터 수정 (노드 타입 변경)
aws redshift modify-cluster \
    --cluster-identifier "my-cluster-1" \
    --node-type "dc2.xlarge"
```

**수정 가능 옵션:**  
- `--node-type`: 노드 타입 변경 (예: `dc2.large` → `dc2.xlarge`)
- `--cluster-type`: 클러스터 유형 변경 (단일 노드 → 마스터-슬레이브)
- `--publicly-accessible`: 외부 접근 여부 변경

**참고:**  
클러스터 수정 시 중단 없이 서비스를 유지하면서도 리소스를 조정할 수 있습니다. 단, `--node-type` 변경 시 일시적으로 연결 차단이 발생할 수 있으니, **수정 후 연결 테스트**를 필수로 수행하세요.

---

### 예제 4: Redshift 리소스 삭제
```bash
# Redshift 클러스터 삭제
aws redshift delete-cluster --cluster-identifier "my-cluster-1"

# 삭제 확인
aws redshift describe-clusters --cluster-identifier "my-cluster-1"
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. 삭제 후에는 `describe-clusters` 명령어로 상태를 확인해야 합니다. 삭제 완료 후에는 리소스를 복구할 수 없으므로, **필요 시 백업**을 반드시 수행하세요.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws redshift describe-clusters --cluster-identifier "my-cluster-1"
aws redshift list-tags-for-resource --resource-arn "arn:aws:redshift:region:account:cluster:cluster-identifier"

# 생성
aws redshift create-cluster --cluster-identifier "my-cluster-2" --node-type "rs3.large"

# 수정
aws redshift modify-cluster --cluster-identifier "my-cluster-1" --node-type "dc2.xlarge"

# 삭제
aws redshift delete-cluster --cluster-identifier "my-cluster-1"
```

**팁:**  
Redshift 클러스터 생성 시 `--cluster-type`을 `multi-node`로 설정하면, **마스터 노드와 슬레이브 노드**가 별도로 구성되어 병렬 처리 성능이 향상됩니다. 또한, **AWS Free Tier**로 생성 시 750시간의 무료 사용 시간이 제공되므로, 실습에 적합합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Redshift 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **Redshift의 데이터 웨어하우스 아키텍처**  
   - 설명: Redshift는 기존 관계형 데이터베이스에서 벗어난 분산형 아키텍처를 사용해 대규모 데이터 처리를 최적화합니다. **Columnar Storage**와 **Massively Parallel Processing (MPP)**은 성능 향상과 스파르타 스키마 설계의 핵심입니다. 시험에서는 아키텍처 구성 요소(예: Cluster, Node, Segment)와 데이터 파티셔닝 방식에 대한 이해가 필수적입니다.  
   - 키워드: `Columnar Storage`, `MPP`, `Segment`

2. **Redshift의 비용 관리 및 프리티어 활용**  
   - 설명: Redshift는 데이터 저장, 컴퓨팅, 네트워크 비용을 별도로 청구합니다. **Provisioned Capacity**와 **Serverless** 모델의 차이, 그리고 **Query Optimization**을 통한 비용 절감 전략이 시험에서 자주 출제됩니다. 특히, 프리티어 사용 시 **14일간의 무료 테스트** 기간을 활용하는 방법이 중요합니다.  
   - 키워드: `Provisioned Capacity`, `Serverless`, `Query Optimization`

3. **Redshift의 보안 및 IAM 통합**  
   - 설명: **Encryption at Rest/In Transit**, **IAM Role**, **VPC 구성**은 Redshift의 보안 강화를 위한 핵심 요소입니다. 시험에서는 데이터 접근 제어, 네트워크 보안 그룹 설정, 그리고 IAM 정책의 적용 범위를 묻는 문제가 자주 등장합니다.  
   - 키워드: `Encryption`, `IAM Role`, `VPC`

4. **Redshift의 데이터 연동 및 ETL 프로세스**  
   - 설명: Redshift는 **AWS Glue**, **EMR**, **Lambda** 등과의 연동을 통해 ETL(Extract, Transform, Load) 작업을 효율화합니다. **S3 버킷 연동** 및 **Data Pipeline** 구성 방법은 시험에서 실무 사례로 자주 등장합니다.  
   - 키워드: `ETL`, `S3 연동`, `Data Pipeline`

5. **Redshift vs Aurora/BigQuery의 차이점**  
   - 설명: Redshift는 **데이터 웨어하우스** 용도에 최적화된 반면, Aurora는 **OLTP**에 적합하며, BigQuery는 **Serverless 기반**의 대규모 분석에 특화됩니다. 시험에서는 **성능, 비용, 확장성**에 따른 선택 기준을 비교하는 문제가 출제됩니다.  
   - 키워드: `OLTP/OLAP`, `Serverless`, `Cost Efficiency`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | Redshift가 OLTP에 적합하다고 오인하는 경우 | **Redshift는 OLAP(데이터 웨어하우스)에 최적화됨** |
| 함정 2 | 비용 구조에서 컴퓨팅 비용과 저장 비용을 혼동하는 경우 | **Compute Node는 수동/자동 확장, Storage는 단일 노드로 관리** |
| 함정 3 | IAM 정책에서 데이터베이스 접근 권한을 정확히 설정하지 못하는 경우 | **IAM Role과 Redshift 클러스터 연결 시 'iam:passRole' 권한 필요** |

#### 🔄 Redshift vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | Redshift | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| **용도** | 데이터 웨어하우스, 분석 기반 대규모 처리 | Aurora(OLTP), BigQuery(Serverless), Snowflake(OLAP) | **실시간 트랜잭션 vs 분석 작업** |
| **확장성** | 수동/자동 컴퓨팅 확장, 스파르타 스키마 | Aurora(스케일 아웃), Snowflake(세분화된 스키마) | **데이터 처리량, 동시성 요구** |
| **비용** | 컴퓨팅/저장 별도 청구, 프리티어 제공 | BigQuery(기본 무료), Snowflake(데이터 전송 비용) | **데이터 양, 분석 빈도** |
| **지연시간** | MPP 아키텍처로 저지연 | BigQuery(분산 처리), Snowflake(컬럼 기반) | **실시간 분석 vs 대규모 집계** |

#### 📝 시험 대비 체크리스트
- [ ] Redshift의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  *(데이터 웨어하우스 및 분석을 위한 고성능 분산 데이터베이스)*  
- [ ] Redshift를 선택해야 하는 시나리오를 알고 있는가?  
  *(대규모 데이터 저장, 복잡한 분석, 실시간 리포트 생성)*  
- [ ] Redshift의 제한사항/한계를 알고 있는가?  
  *(OLTP 처리 불합리, 서버리스 모델의 성능 제한, 복잡한 스키마 설계 필요)*  
- [ ] Redshift와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  *(Aurora: OLTP, BigQuery: Serverless, Snowflake: 분산 처리)*  
- [ ] Redshift의 비용 구조를 이해하고 있는가?  
  *(Compute Node/Storage Node, 프리티어 기간, S3 연동 비용)*

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Redshift를 떠올리세요:  
> - **Data Warehouse** (데이터 웨어하우스 전용)  
> - **Columnar Storage** (컬럼 기반 저장 방식)  
> - **MPP Architecture** (분산 처리)  
> - **Cost Optimization** (비용 절감 전략)  
> - **ETL Integration** (ETL 프로세스 연동)

---

| [⬅️ ElastiCache](./ElastiCache.md) | [📑 Day 3 목차](./README.md) | [🏠 Week 3](../README.md) | [Neptune ➡️](./Neptune.md) |

---
