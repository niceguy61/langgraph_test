---

| [⬅️ DocumentDB](./DocumentDB.md) | [📑 Day 3 목차](./README.md) | [🏠 Week 3](../README.md) | [QLDB ➡️](./QLDB.md) |

---

# Keyspaces 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** Keyspaces는 실시간 데이터 처리와 분석을 위한 AWS의 서버리스, 멀티모델 NoSQL 데이터베이스 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 전통적인 데이터베이스는 스파레드된 데이터 저장소를 관리하는 데 어려움. 예: 사용자 프로필, 채팅 기록, 트랜잭션 로그가 별도 DB에서 관리되며, 데이터 일관성 유지가 복잡합니다.  
- **문제 2:** 스케일링 시 인프라 관리 비용이 급격히 증가. 예: 트래픽 증가 시 DB 서버 확장이 수동적이고, 가용성 확보가 어렵습니다.  
- **문제 3:** 실시간 분석이 필요한 데이터를 처리하는 데 지연이 발생. 예: 재고 관리 시 실시간 판매 데이터 분석이 불가능해 재고 부족이 발생합니다.  

**Keyspaces로 해결:**
- **해결 1:** 서버리스 아키텍처로 인프라 관리 부담을 제거. AWS가 자동으로 자원을 확장/축소하며, 개발자는 단순히 데이터 모델 설계에 집중합니다.  
- **해결 2:** 멀티모델 지원으로 다양한 데이터 유형을 하나의 데이터베이스에서 관리. 예: JSON 문서, 키-값 쌍, 그래프 데이터를 병행하여 저장할 수 있습니다.  
- **해결 3:** 실시간 분석을 위한 고성능 쿼리 처리. Cassandra 기반의 분산 아키텍처로 백만 건 이상의 데이터를 초단위로 처리할 수 있습니다.  

### 비유로 이해하기
**Keyspaces는 도서관의 중앙 서버입니다.**  
도서관에서 책을 찾기 위해 여러 층의 책장과 별도의 도서 카탈로그를 관리하는 대신, Keyspaces처럼 모든 책을 하나의 중앙 서버에서 검색하고, 필요할 때 즉시 데이터를 불러올 수 있습니다. 이는 사용자 프로필, 채팅 기록, 판매 데이터 등 다양한 데이터를 하나의 시스템에서 관리하며, 실시간으로 접근할 수 있게 해줍니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 실시간 데이터 처리 및 분석이 필요한 애플리케이션 | 온라인 쇼핑몰의 재고 관리 시, 실시간 판매 데이터를 분석해 재고 부족을 방지 |
| 시나리오 2 | 대규모 데이터 저장 및 높은 가용성 요구 | 전 세계 사용자에게 제공되는 SaaS 플랫폼의 사용자 활동 로그 저장 |
| 시나리오 3 | 복잡한 그래프 데이터 관리 | 사회관계망 서비스의 친구 추천 알고리즘에 필요한 연결 관계 데이터 저장 |

**이럴 때 Keyspaces를 선택하세요:**
- ✅ **실시간 분석이 필수적인 애플리케이션**  
- ✅ **다양한 데이터 유형을 병행 저장해야 하는 시나리오**  
- ✅ **인프라 관리 부담을 줄이고자 하는 개발자**  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **관계형 데이터베이스가 필요한 경우 → RDS** (트랜잭션 일관성 유지에 적합)  
- ❌ **그래프 분석이 주요 요구사항일 때 → Neptune** (SPARQL 쿼리 및 복잡한 관계 분석)  
- ❌ **시간 시리즈 데이터 저장이 필요 → Timestream** (자동 스케일링 및 분석 기능 제공)

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **AWS Lambda** | 서버리스 애플리케이션과의 통합 | Keyspaces → Lambda (데이터 저장/읽기) |
| **Amazon S3** | 대규모 데이터 백업 및 아카이브 | Keyspaces → S3 (데이터 백업) |
| **Redshift** | 실시간 데이터 분석을 위한 데이터 웨어하우스 연동 | Keyspaces → Redshift (분석용 데이터 전송) |

**자주 사용되는 아키텍처 패턴:**
```
User → API Gateway → Lambda → Keyspaces → Redshift → Dashboard
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **데이터 저장** | $0.025/GB/월 | 12개월 무료 (최대 10GB) |
| **데이터 연산** | $0.013/GB/월 | 12개월 무료 (최대 10GB) |
| **데이터 전송** | $0.02/GB | 12개월 무료 (최대 10GB) |

**비용 최적화 팁:**
1. 💡 **데이터 압축**: JSON 데이터를 이진 형식으로 변환해 저장 공간 절약  
2. 💡 **데이터 타임스탬프**: 오래된 데이터는 아카이브로 이동해 저장 비용 절감  
3. 💡 **캐싱 활용**: ElastiCache와 결합해 빈번한 읽기 요청을 처리  

> **⚠️ 비용 주의:** 데이터 전송 비용이 급격히 증가할 수 있음. 예: EC2에서 Keyspaces로 데이터 전송 시, 데이터 전송 요금이 10배 이상 증가할 수 있으므로, 네트워크 최적화가 필수적입니다.

## 📚 핵심 개념

### 개념 1: Amazon Keyspaces의 서버리스 아키텍처  
Amazon Keyspaces는 서버리스 데이터베이스 서비스로, 클라우드 환경에서 관리형 NoSQL 데이터베이스를 제공합니다. 고객은 인프라 설정, 업데이트, 모니터링 등 복잡한 관리 작업을 수행하지 않아도 됩니다. AWS가 자동으로 데이터 분산, 복제, 확장, 보안 등을 처리하므로, 개발자는 데이터 모델링과 애플리케이션 로직에 집중할 수 있습니다. 이는 리소스 최적화와 운영 효율성을 동시에 달성합니다.  

#### 왜 중요한가?  
- **운영 부담 감소**: 인프라 관리와 업데이트를 AWS가 자동으로 처리합니다.  
- **확장성과 가용성**: 자동 스케일링과 고가용성 아키텍처로 트래픽 증가 시에도 안정적인 성능 제공.  

#### 세부 요소  
| 요소 | 설명 | 예시 |  
|-----|-----|-----|  
| **서버리스 모델** | 고객이 서버를 직접 관리하지 않아도 데이터베이스를 사용할 수 있음 | AWS Lambda와 연동해 실시간 데이터 처리 |  
| **분산 아키텍처** | 데이터를 여러 리전에 복제해 고가용성과 지연 최소화 | 글로벌 사용자에게 균일한 응답 속도 제공 |  
| **자동 업데이트** | 보안 패치, 성능 최적화 등 AWS가 자동으로 관리 | 최신 보안 기능을 즉시 적용 |  

> **💡 Tip:** 서버리스 아키텍처로 인해 개발자는 데이터베이스 관리에 시간을 할애할 필요가 없어, 핵심 비즈니스 로직에 집중할 수 있습니다.  

---

### 개념 2: 데이터 모델링과 쿼리 언어  
Amazon Keyspaces는 Cassandra의 데이터 모델링 방식을 기반으로 하며, 키-값 쌍과 파티션 키를 사용해 데이터를 효율적으로 저장하고 검색합니다. 쿼리 언어는 Cassandra Query Language(CQL)을 기반으로 설계되어, 테이블 생성, 데이터 삽입, 업데이트, 조회 등 다양한 작업을 수행할 수 있습니다.  

#### 작동 원리  
1. **데이터 모델링**: 파티션 키를 정의해 데이터가 분산 저장되도록 설계. 예: `users` 테이블의 `user_id`를 파티션 키로 설정.  
2. **쿼리 처리**: CQL을 사용해 데이터를 검색. 예: `SELECT * FROM users WHERE user_id = '123';`  
3. **자동 복제**: 데이터가 여러 노드에 복제되어 고가용성과 내결함성을 확보.  

> **💡 Tip:** 파티션 키를 적절히 선택해 데이터 분산과 조회 성능을 최적화해야 합니다.  

---

### 개념 3: 서버리스 아키텍처의 주요 특징  
Amazon Keyspaces는 클라우드 기반의 서버리스 데이터베이스로, 여러 기능을 제공해 개발자 경험을 향상시킵니다.  

#### 주요 특징  
1. ****자동 확장**: 트래픽 증가 시 자동으로 리소스를 추가해 성능 유지.  
2. ****지속적인 백업 및 복원**: AWS가 자동 백업을 관리해 데이터 손실 방지.  
3. ****지역별 복제**: 글로벌 사용자에게 균일한 응답 속도 제공.  

> **💡 Tip:** 서버리스 아키텍처로 인해 운영 비용을 최소화하고, 확장성과 가용성을 동시에 확보할 수 있습니다.

## 🖥️ AWS 콘솔에서 Keyspaces 사용하기

### Step 1: Keyspaces 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: [https://console.aws.amazon.com](https://console.aws.amazon.com)  
   - AWS 계정이 없으신가요? [AWS 계정 생성](https://aws.amazon.com/registration/)  
2. 상단 검색창에서 **"Keyspaces"**를 입력하고, 검색 결과에서 **"Keyspaces"**를 클릭합니다  
   - **리소스 탐색기**에서 **"Keyspaces"** 서비스가 표시됩니다  

> **📸 화면 확인:** Keyspaces 대시보드가 표시되면 정상입니다.  
> **💡 Tip:** 처음 방문 시 "Getting Started" 가이드가 제공됩니다.  
> **⚠️ 주의:** AWS 리전 선택이 중요합니다. 지역에 따라 가격 및 성능이 달라집니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **Keyspaces 클러스터 생성**  
   - **"Create cluster"** 버튼을 클릭합니다  
   - **Cluster name**: `MyFirstKeyspacesCluster` 입력  
   - **Keyspace**: `example_keyspace` 입력  
   - **Node type**: `nano` (무료 티어에 적합)  
   - **Regions**: `us-east-1` (미국 동부) 선택  
   - **Encryption**: **Enable** (데이터 보안 강화)  

2. **수동 구성 옵션 설정**  
   - **Data center**: `us-east-1a` 선택  
   - **Read/write capacity**: **Serverless** 모드 선택 (자동 확장)  
   - **Tags**: `Environment=Dev` 입력 (리소스 관리 용도)  

3. **리소스 생성 확인**  
   - **"Create"** 버튼을 클릭하고, 생성 완료 후 **"View cluster"**를 클릭합니다  
   - **"Cluster status"**가 **"ACTIVE"**로 표시되면 정상입니다  

> **📸 화면 확인:** 생성된 클러스터의 **"Overview"** 탭에서 상태를 확인하세요.  
> **💡 Tip:** 무료 티어는 1개의 클러스터만 생성 가능합니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **Keyspace 생성**  
   - **"Create keyspace"** 버튼을 클릭합니다  
   - **Keyspace name**: `example_keyspace` 입력  
   - **Replication strategy**: `NetworkTopologyStrategy` 선택  
   - **Replication factor**: `1` (단일 리전에서 테스트용)  

2. **테이블 생성 및 데이터 삽입**  
   - **"Create table"** 버튼을 클릭합니다  
   - 다음 쿼리 입력:  
     ```sql
     CREATE TABLE example_keyspace.users (
         id UUID PRIMARY KEY,
         name TEXT,
         email TEXT
     );
     ```
   - **"Run query"** 버튼을 클릭하여 쿼리 실행  

3. **데이터 삽입 및 조회**  
   - 다음 쿼리 입력:  
     ```sql
     INSERT INTO example_keyspace.users (id, name, email) 
     VALUES (uuid(), 'Alice', 'alice@example.com');
     ```
   - 조회 쿼리 실행:  
     ```sql
     SELECT * FROM example_keyspace.users;
     ```

> **⚠️ 주의:** 쿼리 실행 시 **"Query"** 탭에서 실행 결과를 확인하세요.  
> **💡 Tip:** 쿼리 로그를 **"Query history"**에서 확인할 수 있습니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **리소스 확인 방법**  
   - **"Clusters"** 탭에서 생성된 클러스터 이름을 클릭합니다  
   - **"Overview"** 탭에서 **"Cluster status"**와 **"Data centers"** 확인  

2. **데이터베이스 상태 확인**  
   - **"Tables"** 탭에서 테이블 이름(`users`)을 클릭합니다  
   - **"Query editor"**에서 쿼리 실행 및 결과 확인  

3. **정상 동작 테스트**  
   - **"Run query"**를 사용해 다음 쿼리 실행:  
     ```sql
     SELECT COUNT(*) FROM example_keyspace.users;
     ```
   - 결과가 `1`이면 정상입니다.  

> **💡 Tip:** AWS Free Tier로 1000개의 쿼리까지 무료로 사용 가능합니다.  
> **⚠️ 주의:** 데이터를 삭제할 경우 **"Delete table"** 또는 **"Delete cluster"**로 완전 삭제해야 합니다.  

---

### 📋 체크리스트: Keyspaces 설정 완료 여부  
- [ ] AWS Management Console에 로그인 완료  
- [ ] Keyspaces 서비스 접속 완료  
- [ ] 클러스터 생성 및 상태 확인 완료  
- [ ] 테이블 생성 및 데이터 삽입/조회 완료  
- [ ] 쿼리 로그 및 상태 확인 완료  
- [ ] 정상 동작 테스트 완료  

> **💰 비용 관리 팁:** AWS Free Tier는 1개 클러스터, 1000개 쿼리, 10GB 데이터 저장 공간까지 제공됩니다.  
> **🔍 추가 자료:** [AWS Keyspaces 공식 문서](https://docs.aws.amazon.com/keyspaces/latest/devguide/what-is.html)

## ⌨️ AWS CLI로 Keyspaces 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**설명:**  
AWS CLI를 사용하기 전에 버전, 자격 증명, 리전 설정을 확인합니다. Keyspaces는 AWS CLI 1.18.188 이상에서 지원됩니다. 자격 증명은 IAM 사용자 키/시크릿 액세스 키를 기반으로 작동합니다. 리전은 Keyspaces 리소스 생성 시 지역을 결정합니다.

---

### 예제 1: Keyspaces 리소스 조회
```bash
# Keyspaces 키스페이스 목록 조회
aws keyspace list-keyspaces --query '[].KeyspaceName' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 결과 필터링 (예: `"[].KeyspaceName"`로 이름만 출력) | `"[].KeyspaceName"` |
| `--output` | 출력 형식 (json, table, text) | `table` |

**예상 출력:**
```
| KeyspaceName |
+--------------+
| example-ks   |
| test-ks      |
```

**팁:**  
`list-keyspaces`는 키스페이스 목록을 반환합니다. `describe-keyspace --keyspace-name "name"`으로 세부 정보를 확인할 수 있습니다.

---

### 예제 2: Keyspaces 리소스 생성
```bash
# 키스페이스 생성
aws keyspace create-keyspace \
    --keyspace-name "example-ks" \
    --replication-factor 1 \
    --strategy-class org.apache.cassandra.locator.SimpleStrategy
```

**필수 옵션:**
- `--keyspace-name`: 생성할 키스페이스 이름 (예: `"example-ks"`)
- `--replication-factor`: 복제 인자 (1~256, 일반적으로 1)
- `--strategy-class`: 복제 전략 (SimpleStrategy 또는 NetworkTopologyStrategy)

**예상 출력:**
```json
{
    "KeyspaceName": "example-ks",
    "Status": "ACTIVE"
}
```

**주의사항:**  
- 키스페이스 생성 시 `--replication-factor`는 1~256 사이여야 합니다.  
- `SimpleStrategy`는 단일 데이터 에 복제됩니다. `NetworkTopologyStrategy`는 다중 데이터 를 지원합니다.

---

### 예제 3: Keyspaces 리소스 수정
```bash
# 키스페이스 수정 (예: 복제 인자 변경)
aws keyspace update-keyspace \
    --keyspace-name "example-ks" \
    --replication-factor 2 \
    --strategy-class org.apache.cassandra.locator.SimpleStrategy
```

**설명:**  
Keyspaces는 키스페이스의 복제 인자나 전략을 수정할 수 있습니다. 하지만 일부 속성은 생성 시만 변경 가능합니다. 수정 시 `--replication-factor`와 `--strategy-class`를 지정해야 합니다.

---

### 예제 4: Keyspaces 리소스 삭제
```bash
# 키스페이스 삭제
aws keyspace delete-keyspace --keyspace-name "example-ks"

# 삭제 확인
aws keyspace describe-keyspace --keyspace-name "example-ks"
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. `describe-keyspace`로 상태를 확인한 후 실행하세요.  
> **💡 Tip:** 삭제 후 1시간 이내에 `describe-keyspace`로 상태를 확인할 수 있습니다.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws keyspace list-keyspaces
aws keyspace describe-keyspace --keyspace-name "name"

# 생성
aws keyspace create-keyspace --keyspace-name "name" --replication-factor 1 --strategy-class SimpleStrategy

# 수정
aws keyspace update-keyspace --keyspace-name "name" --replication-factor 2 --strategy-class SimpleStrategy

# 삭제
aws keyspace delete-keyspace --keyspace-name "name"
```

---

### 💡 비용 및 프리티어 활용
- **프리티어:** 100시간의 사용량 (100GB 데이터 저장) 제공  
- **비용 최적화:**  
  - 복제 인자를 최소한으로 설정 (예: `--replication-factor 1`)  
  - `SimpleStrategy`로 단일 데이터 에 집중  
  - `describe-keyspace`로 실제 사용량 확인 후 스케일링  

**CLI 활용:**  
`aws keyspace describe-keyspace --keyspace-name "name"`로 쿼리 수, 데이터 저장량 등을 확인해 비용을 모니터링할 수 있습니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Keyspaces 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **서버리스 아키텍처와 자동 확장**  
   - 설명: AWS Keyspaces는 서버 관리 없이 데이터베이스를 운영할 수 있는 **서버리스 아키텍처**를 제공하며, **자동 확장**을 통해 트래픽 증가 시 성능을 유지합니다. 이는 클라우드 환경에서의 유연성과 비용 최적화를 시험에서 자주 다룹니다.  
   - 키워드: `서버리스`, `자동 확장`, `AWS Managed`

2. **NoSQL 데이터베이스로서의 유연성**  
   - 설명: Keyspaces는 **JSON 형식의 스키마 없는 데이터 모델**을 지원해, 다양한 데이터 유형을 쉽게 저장 및 관리할 수 있습니다. 시험에서는 **스키마 유연성**과 **스케일 아웃** 기능을 중심으로 출제됩니다.  
   - 키워드: `NoSQL`, `스키마 유연성`, `스케일 아웃`

3. **비용 최적화 및 프리티어 활용**  
   - 설명: Keyspaces는 **예산 관리**와 **프리티어**를 통해 작은 규모 애플리케이션에서의 비용을 줄일 수 있습니다. 시험에서는 **비용 구조**와 **예산 제한**에 대한 이해가 필수적입니다.  
   - 키워드: `비용 최적화`, `프리티어`, `예산 관리`

4. **고가용성 및 지연 시간 최소화**  
   - 설명: Keyspaces는 **다중 리전 배포**와 **리전 간 데이터 복제**를 통해 **고가용성**을 보장하며, **저지연 응답**을 제공합니다. 이는 대규모 트래픽 처리 시 필수적인 개념입니다.  
   - 키워드: `고가용성`, `리전 복제`, `저지연`

5. **GraphQL과의 통합 및 쿼리 최적화**  
   - 설명: Keyspaces는 **GraphQL**을 통한 유연한 데이터 조회를 지원하며, **쿼리 최적화** 기능을 통해 성능을 개선합니다. 시험에서는 **API 통합**과 **성능 튜닝**이 주요 포인트입니다.  
   - 키워드: `GraphQL`, `쿼리 최적화`, `API 통합`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "Keyspaces는 DynamoDB와 동일한 스키마 유연성을 가진다"는 문장에서 혼동 | Keyspaces는 DynamoDB와 달리 **GraphQL 통합**과 **서버리스 아키텍처**를 강조함 |
| 함정 2 | "Keyspaces는 스키마를 정의해야 한다"는 문장에서 혼동 | Keyspaces는 **스키마 없는 데이터 모델**을 지원하며, 데이터 유형은 유연하게 관리됨 |
| 함정 3 | "Keyspaces의 비용은 Always On 방식으로 계산된다"는 문장에서 혼동 | Keyspaces는 **프리티어**와 **예산 제한**을 통해 비용을 최적화 가능함 |

#### 🔄 Keyspaces vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | Keyspaces | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | NoSQL 데이터베이스, GraphQL 통합, 서버리스 아키텍처 | DynamoDB (스키마 유연성), DocumentDB (MongoDB 호환) | **GraphQL 지원**, **서버리스 구조**가 필요한 경우 |
| 확장성 | 자동 확장, 리전 간 복제 | DynamoDB (스케일 아웃), DocumentDB (스케일 아웃) | **트래픽 증가 시 자동 확장**이 필요한 경우 |
| 비용 | 프리티어, 예산 제한 | DynamoDB (요금 정책), DocumentDB (요금 정책) | **예산 제한** 및 **비용 최적화**가 중요한 경우 |
| 지연시간 | 리전 복제로 저지연 | DynamoDB (지연 시간 최적화), DocumentDB (지연 시간 최적화) | **저지연 응답**이 필수적인 시나리오 |

#### 📝 시험 대비 체크리스트
- [ ] Keyspaces의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  (예: "서버리스 NoSQL 데이터베이스로 GraphQL 통합 및 자동 확장 제공")
- [ ] Keyspaces를 선택해야 하는 시나리오를 알고 있는가?  
  (예: "고가용성, 저지연, 스키마 유연성 필요 시")
- [ ] Keyspaces의 제한사항/한계를 알고 있는가?  
  (예: "GraphQL 통합은 일부 기능 제한, 프리티어로만 작동 가능")
- [ ] Keyspaces와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  (예: "DynamoDB는 스키마 유연성, Keyspaces는 GraphQL 통합")
- [ ] Keyspaces의 비용 구조를 이해하고 있는가?  
  (예: "프리티어, 예산 제한, 요금 정책에 따라 차등 적용")

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Keyspaces를 떠올리세요:  
> - **서버리스**  
> - **GraphQL**  
> - **스키마 유연성**  
> - **예산 제한**  
> - **저지연**

---

| [⬅️ DocumentDB](./DocumentDB.md) | [📑 Day 3 목차](./README.md) | [🏠 Week 3](../README.md) | [QLDB ➡️](./QLDB.md) |

---
