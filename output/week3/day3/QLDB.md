---

| [⬅️ Keyspaces](./Keyspaces.md) | [📑 Day 3 목차](./README.md) | [🏠 Week 3](../README.md) | [Timestream ➡️](./Timestream.md) |

---

# QLDB 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** QLDB는 데이터 변경 내역을 불변성으로 보장하는 AWS의 블록체인 기반 데이터베이스 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 데이터가 조작되거나 수정되면서 신뢰성 있는 이력 추적이 어려웠습니다. 예를 들어, 금융 거래 내역이 변경되면 감사 및 분쟁 해결이 불가능했습니다.  
- **문제 2:** 실시간 데이터 변경 이력을 기록하고 검색하는 데 복잡한 시스템 설계가 필요했습니다.  
- **문제 3:** 트랜잭션 간의 일관성과 데이터 무결성을 보장하는 데 높은 개발 및 유지보수 비용이 발생했습니다.  

**QLDB로 해결:**
- **해결 1:** 불변성 보장 기능으로 데이터 조작을 방지하고, 모든 변경 내역을 암호화된 트랜잭션 로그로 기록합니다.  
- **해결 2:** 실시간 데이터 변경 이력을 효율적으로 검색하고 분석할 수 있는 고성능 아키텍처를 제공합니다.  
- **해결 3:** 자동화된 트랜잭션 처리와 일관성 유지 기능으로 개발 및 유지보수 비용을 절감합니다.  

### 비유로 이해하기
QLDB는 블록체인 기술을 기반으로 한 "불변의 일기장"입니다. 예를 들어, 은행 거래 내역을 기록할 때, QLDB는 각 거래를 블록 형태로 저장하고, 이후 변경이 불가능하게 해서 신뢰성을 보장합니다. 이는 블록체인과 유사하지만, 전용 데이터베이스로서 더 높은 성능과 유연성을 제공합니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 법적/규제 요구사항에 부합하는 데이터 이력 관리 | 금융 기관이 보험 계약 변경 내역을 감사 기록으로 보존 |
| 시나리오 2 | 공급망 관리 및 물류 추적 | 제조업자가 부품 이력과 유통 경로를 실시간으로 추적 |
| 시나리오 3 | 분산 시스템의 데이터 일관성 보장 | 다중 클라우드 환경에서 데이터 동기화 및 충돌 방지 |

**이럴 때 QLDB를 선택하세요:**
- ✅ **상황 1:** 데이터 무결성과 감사 기록이 필수적인 규제 준수 요구사항  
- ✅ **상황 2:** 데이터 변경 이력을 실시간으로 추적하고 분석해야 하는 시나리오  
- ✅ **상황 3:** 분산 시스템에서 트랜잭션 일관성을 보장해야 하는 경우  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **상황 → 대안 서비스 추천:** 그래프 데이터 관리 → **Neptune** (SPARQL 쿼리 지원)  
- ❌ **상황 → 대안 서비스 추천:** 대규모 데이터 분석 → **Redshift** (데이터 웨어하우스 아키텍처)  
- ❌ **상황 → 대안 서비스 추천:** 서버리스 NoSQL 데이터베이스 → **Amazon Keyspaces** (자동 스케일링)  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **S3** | 데이터 백업 및 아카이브 | QLDB → S3 (트랜잭션 로그 백업) |
| **Lambda** | 실시간 데이터 처리 | S3 → Lambda → QLDB (이벤트 기반 트랜잭션 처리) |
| **CloudFront** | 데이터 분배 및 보안 | CloudFront → QLDB (API Gateway 통한 보안 통제) |

**자주 사용되는 아키텍처 패턴:**
```
User → API Gateway → Lambda → QLDB → S3 (데이터 백업)
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **트랜잭션 처리** | $0.001~$0.01/단위 | 월 100,000 트랜잭션 무료 |
| **데이터 저장** | $0.01~$0.05/GB/월 | 12개월 무료 (최대 10GB) |
| **API 호출** | $0.0001~$0.005/요청 | 항상 무료 (초기 10,000 요청) |

**비용 최적화 팁:**
1. 💡 **팁 1:** 트랜잭션 수를 최소화하고, 대량 처리 시 배치 작업을 활용하세요.  
2. 💡 **팁 2:** 데이터 저장 비용을 줄이기 위해 적절한 데이터 가공 및 압축을 적용하세요.  
3. 💡 **팁 3:** 프리티어 한도를 초과하지 않도록 실시간 모니터링과 알림 설정을 유지하세요.  

> **⚠️ 비용 주의:** 데이터 저장 기간이 길어질 경우 저장 비용이 누적될 수 있으므로, 정기적으로 데이터 정책을 점검하세요.

## 📚 핵심 개념

### 개념 1: QLDB의 불변성 및 트랜잭션 로그  
QLDB는 **불변성**(Immutability)을 핵심으로 하는 분산 데이터베이스로, 모든 데이터 변경 사항을 기록하고 이력을 완전히 보존합니다. 이는 데이터의 신뢰성과 투명성을 보장하며, 감사 추적, 금융 거래, 공급망 관리 등에 유용합니다. 트랜잭션 로그는 모든 변경 사항을 순차적으로 기록하여, 데이터의 원본을 복원하거나 특정 시점의 데이터를 검색할 수 있도록 합니다. QLDB는 AWS Lambda와 통합해 실시간 처리를 가능하게 하며, AWS KMS를 통해 데이터 암호화를 지원합니다.  

#### 왜 중요한가?  
- **데이터 무결성 보장**: 변경 내역을 기록해 의도치 않은 데이터 손실 방지  
- **감사 및 감시 용이**: 모든 트랜잭션을 로그로 저장해 내부 감사 및 법적 증거로 활용 가능  

#### 세부 요소  
| 요소 | 설명 | 예시 |  
|-----|-----|-----|  
| 불변성 | 데이터 변경 시 이력이 완전히 기록되며, 원본 데이터는 수정 불가능 | 금융 거래 내역은 변경 불가능, 오직 추가 기록만 가능 |  
| 트랜잭션 로그 | 모든 변경 사항을 순차적으로 저장해 원본 데이터를 복원 가능 | 공급망 관리 시, 특정 시점의 데이터를 검색 |  
| 리더서 | 데이터베이스가 분산되어 있어 단일 포인트 실패 방지 | 클라우드 환경에서 고가용성 확보 |  

> **💡 Tip:** QLDB는 AWS Lambda와 연동해 실시간 처리를 가능하게 하며, KMS를 통해 데이터 암호화를 적용해 보안을 강화할 수 있습니다.  

---

### 개념 2: 트랜잭션 로그의 작동 원리  
QLDB는 **트랜잭션 로그**(Transaction Log)를 통해 모든 데이터 변경을 기록합니다. 이 로그는 분산된 노드에 분산 저장되며, 각 트랜잭션은 **일련번호**(Sequence Number)로 식별됩니다. 사용자는 로그를 통해 특정 시점의 데이터를 복원하거나, 변경 내역을 분석할 수 있습니다.  

#### 작동 원리  
1. **트랜잭션 제출**: 사용자는 AWS SDK나 CLI를 통해 트랜잭션을 제출합니다.  
2. **로그 기록**: QLDB는 트랜잭션을 로그에 순차적으로 기록하고, 이력 데이터를 보존합니다.  
3. **데이터 조회**: 사용자는 로그를 통해 특정 시점의 데이터를 복원하거나, 변경 내역을 분석할 수 있습니다.  

> **💡 Tip:** 트랜잭션 로그는 감사 추적 및 데이터 복구에 필수적이므로, AWS KMS를 통해 암호화를 적용해 보안을 강화해야 합니다.  

---

### 개념 3: 리더서(ledger) 구조  
QLDB는 데이터를 **리더서**(Ledger)라는 단위로 관리합니다. 리더서는 분산된 노드에 분산 저장되며, 모든 트랜잭션은 해당 리더서에 기록됩니다. 이 구조는 데이터의 일관성과 확장성을 보장하며, 클라우드 환경에서 고가용성과 스케일링을 지원합니다.  

#### 주요 특징  
1. **분산 리더서**: 데이터가 여러 노드에 분산 저장되어 단일 포인트 실패 방지  
2. **실시간 일관성**: 모든 노드는 실시간으로 데이터 변경을 동기화해 일관성 유지  
3. **감사 가능성**: 모든 트랜잭션은 로그에 기록되어 감사 및 데이터 복구 가능  

> **💡 Tip:** 리더서 구조는 금융, 의료, 공급망 등 데이터 무결성이 중요한 분야에서 필수적입니다. AWS 콘솔에서 리더서를 생성하고, CLI로 트랜잭션을 제출할 수 있습니다.

## 🖥️ AWS 콘솔에서 QLDB 사용하기

### Step 1: QLDB 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - AWS 계정 ID와 암호를 입력해 로그인합니다.  
2. 상단 검색창에서 "QLDB"를 입력하고, 검색 결과에서 **QLDB**를 클릭합니다.  
   - "QLDB" 검색 결과는 "QLDB Console"으로 이동합니다.  
3. AWS 리전 선택: QLDB는 **US East (N. Virginia)**, **EU (Ireland)**, **Asia Pacific (Tokyo)** 등 일부 리전에서 제공됩니다.  

> **📸 화면 확인:** QLDB 대시보드가 표시되면 정상입니다. 대시보드에는 "Ledgers" 탭에서 생성된 레더를 확인할 수 있습니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **레더 생성**  
   - **QLDB Console** 메뉴에서 **"Create ledger"** 버튼을 클릭합니다.  
   - **Ledger name**을 입력합니다 (예: `MyQLDBLedger`).  
     - 이름은 유일해야 하며, 특수문자는 제외합니다.  
   - **Ledger type**을 선택합니다:  
     - **Private**: 내부 사용(예: 내부 시스템 로그)  
     - **Public**: 외부 사용(예: 고객 데이터 공유)  
   - **Capacity configuration**에서 **Provisioned** 또는 **On-demand**를 선택합니다.  
     - **Provisioned**: 고정 용량 단위(WCUs/RCUs) 설정  
     - **On-demand**: 자동 확장 (비용이 더 높음)  

2. **KMS 키 설정**  
   - **Encryption** 섹션에서 AWS KMS 키를 선택합니다.  
     - 암호화된 데이터를 보호하기 위해 필요합니다.  
     - 없으면 기본 KMS 키를 자동 생성합니다.  

3. **Retention period 설정**  
   - **Retention period**를 선택합니다 (예: 7일, 30일, 1년).  
     - 데이터 저장 기간을 제한하고 비용을 절약할 수 있습니다.  

> **📸 화면 확인:** "Create ledger" 폼에서 모든 설정이 완료되면 **"Create"** 버튼을 클릭하고, 생성 완료 메시지가 표시되면 정상입니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **IAM 역할 설정**  
   - QLDB 리소스에 액세스할 IAM 역할을 생성하거나 연결합니다.  
   - 예: `QLDBReadRole` 및 `QLDBWriteRole`을 생성하여 읽기/쓰기 권한을 부여합니다.  
2. **Audit Logging 활성화**  
   - **"Audit"** 탭에서 **"Enable audit logging"**을 선택합니다.  
   - 모든 트랜잭션을 로그로 기록하여 감사 및 트래픽 분석에 활용합니다.  
3. **Read Replica 설정 (선택 사항)**  
   - 주 레더의 복제본을 생성하여 읽기 트래픽을 분산합니다.  
   - **"Read replicas"** 탭에서 **"Create read replica"**를 클릭합니다.  

> **⚠️ 주의:** QLDB는 단일 리전에서만 작동하며, 글로벌 복제 기능은 제공되지 않습니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **리소스 확인**  
   - **QLDB Console > Ledgers**에서 생성된 레더를 확인합니다.  
   - **Status**가 "ACTIVE"인지 확인합니다.  
2. **테스트 쿼리 실행**  
   - **"Query editor"**를 열고 다음 쿼리를 입력합니다:  
     ```sql
     SELECT * FROM ledger MyQLDBLedger;
     ```  
     - 결과가 정상적으로 표시되면 설정이 완료되었습니다.  
3. **AWS CLI 테스트**  
   - CLI를 사용해 레더 상태를 확인합니다:  
     ```bash
     aws qldb get-ledger --ledger-name MyQLDBLedger
     ```  
     - 출력 결과에서 **"Status"**가 `ACTIVE`인지 확인합니다.  

> **💡 Tip:** QLDB는 **프리티어**로 100 MB/일, 10000 트랜잭션/일을 제공합니다. 실무에서는 **Provisioned Capacity**를 사용해 성능을 보장하세요.  
> **💰 비용 주의:** WCUs/RCUs 기반 요금이 적용되므로, 트래픽 증가 시 비용이 급증할 수 있습니다. AWS Cost Explorer를 사용해 예산 관리하세요.

## ⌨️ AWS CLI로 QLDB 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**설명:**  
QLDB CLI 명령어를 실행하기 전에 AWS CLI가 최신 버전인지 확인하고, IAM 자격 증명이 정상적으로 설정되었는지, 그리고 현재 리전이 올바른지 확인해야 합니다.  
`aws configure get region` 명령어로 현재 설정된 리전을 확인하고, 필요 시 `aws configure set region`으로 리전을 변경할 수 있습니다.

---

### 예제 1: QLDB 리소스 조회
```bash
# [QLDB 리소스 목록 조회]
aws qldb list-ledgers --query '[].Name' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 결과 필터링 | `'[].Name'` (리소스 이름만 출력) |
| `--output` | 출력 형식 | `json`, `table`, `text` |

**예상 출력:**
```
NAME
-----
example-ledger
```

**설명:**  
`list-ledgers` 명령어는 QLDB에서 생성된 **ledger(원장)** 목록을 조회합니다. `--query` 옵션을 사용해 특정 필드만 출력하고, `--output table`으로 테이블 형식으로 결과를 보기 쉽게 할 수 있습니다.

---

### 예제 2: QLDB 리소스 생성
```bash
# [QLDB 리소스 생성]
aws qldb create-ledger \
    --name "example-ledger" \
    --permissions "{\"AWSLambdaRole\":\"Allow\"}" \
    --tags "Key1=Value1,Key2=Value2"
```

**필수 옵션:**
- `--name`: 생성할 ledger의 이름 (예: `example-ledger`)
- `--permissions`: 권한 정책 (JSON 형식, AWS Lambda 역할을 허용하는 예시)

**예상 출력:**
```json
{
    "Arn": "arn:aws:qldb:region:account-id:ledger/example-ledger",
    "Name": "example-ledger",
    "Status": "ACTIVE"
}
```

**설명:**  
`create-ledger` 명령어는 새로운 ledger을 생성합니다. `--permissions`은 리소스에 대한 액세스 권한을 정의하며, `--tags`는 리소스에 태그를 추가합니다. 생성 후 `list-ledgers`로 확인할 수 있습니다.

---

### 예제 3: QLDB 리소스 수정
```bash
# [QLDB 리소스 수정]
aws qldb update-ledger \
    --ledger-arn "arn:aws:qldb:region:account-id:ledger/example-ledger" \
    --permissions "{\"AWSLambdaRole\":\"Allow\"}"
```

**설명:**  
`update-ledger` 명령어는 기존 ledger의 권한 정책을 수정합니다. `--ledger-arn`은 수정할 ledger의 ARN을 지정하고, `--permissions`은 새로운 권한 정책을 설정합니다.

---

### 예제 4: QLDB 리소스 삭제
```bash
# [QLDB 리소스 삭제]
aws qldb delete-ledger --ledger-arn "arn:aws:qldb:region:account-id:ledger/example-ledger"

# 삭제 확인
aws qldb describe-ledger --ledger-arn "arn:aws:qldb:region:account-id:ledger/example-ledger"
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. `describe-ledger` 명령어로 삭제 전 상태를 확인하고, `--force` 옵션을 사용해 강제 삭제할 수 있습니다.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws qldb list-ledgers
aws qldb describe-ledger --ledger-arn "arn:aws:qldb:region:account-id:ledger/example-ledger"

# 생성
aws qldb create-ledger --name "example-ledger" --permissions "{\"AWSLambdaRole\":\"Allow\"}"

# 수정
aws qldb update-ledger --ledger-arn "arn:aws:qldb:region:account-id:ledger/example-ledger" --permissions "{\"AWSLambdaRole\":\"Allow\"}"

# 삭제
aws qldb delete-ledger --ledger-arn "arn:aws:qldb:region:account-id:ledger/example-ledger"
```

**팁:**  
- QLDB 리소스는 생성 후 **7일간** 삭제할 수 있는 **폐쇄 기간**이 있습니다.  
- `--tags` 옵션으로 리소스에 태그를 추가해 비용 관리 및 정리에 유용합니다.  
- `aws qldb get-parameters` 명령어로 QLDB의 파라미터를 확인할 수 있습니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 QLDB 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **QLDB의 불변성 (Immutability)**  
   - 설명: QLDB는 데이터 수정이 불가능한 **불변성**을 통해 거래 내역을 감사 및 감사 추적에 적합하게 설계되었습니다. 시험에서 **불변성**, **감사 트래픽**, **불변 레이더** 등의 키워드가 등장하면 QLDB를 고려해야 합니다.  
   - 키워드: `불변성`, `감사 트래픽`, `불변 레이더`

2. **트랜잭션 로그 (Transaction Log)**  
   - 설명: QLDB는 모든 트랜잭션을 **트랜잭션 로그**에 기록하여 데이터의 투명성과 신뢰성을 보장합니다. 이 로그는 블록체인과 유사한 구조를 가지며, 시험에서 **트랜잭션 로그**, **블록체인**, **감사 추적** 키워드가 등장할 때 주목해야 합니다.  
   - 키워드: `트랜잭션 로그`, `블록체인`, `감사 추적`

3. **QLDB의 주요 사용 사례**  
   - 설명: QLDB는 **금융 거래**, **계약 관리**, **법적 감사** 등 **불변성**이 필수적인 시나리오에 적합합니다. 시험에서 "불변성 데이터베이스", "감사 추적"을 요구하는 문제는 QLDB와 관련이 있습니다.  
   - 키워드: `불변성 데이터베이스`, `감사 추적`, `법적 감사`

4. **QLDB와 DynamoDB의 차이점**  
   - 설명: QLDB는 **불변성**을 중심으로 설계된 반면, DynamoDB는 **NoSQL 기반의 실시간 읽기/쓰기**를 지원합니다. 시험에서 "불변성 vs 실시간 처리"와 같은 비교 문제는 QLDB의 특징을 묻는 유형입니다.  
   - 키워드: `불변성`, `NoSQL`, `실시간 처리`

5. **QLDB의 비용 구조**  
   - 설명: QLDB는 **프리티어**가 제공되며, **데이터 저장량**, **트랜잭션 수**에 따라 비용이 책정됩니다. 시험에서 "비용 최적화", "프리티어 활용"을 요구하는 문제는 QLDB의 비용 모델을 파악하는 것이 핵심입니다.  
   - 키워드: `프리티어`, `데이터 저장량`, `트랜잭션 수`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | QLDB가 **NoSQL** 데이터베이스인지 여부를 혼동 | QLDB는 **형식화된 레코드**를 저장하는 **형식화된 데이터베이스**로, NoSQL가 아님 |
| 함정 2 | QLDB의 **트랜잭션 로그**가 데이터베이스 자체에 저장되는지 여부 혼동 | 트랜잭션 로그는 **분리된 저장소**에 저장되며, 데이터베이스 자체가 아님 |
| 함정 3 | QLDB의 **불변성**이 데이터 수정을 완전히 금지하는 것인지 여부 혼동 | 불변성은 **데이터 수정을 기록하지 않음**을 의미하며, 이전 상태는 복원 가능 |

#### 🔄 QLDB vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | QLDB | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 불변성 데이터 관리, 감사 추적 | DynamoDB(NoSQL), PostgreSQL(RDB) | **불변성**이 필요한 시나리오 |
| 확장성 | 수평 확장, 트랜잭션 로그 기반 | DynamoDB(수평 확장), PostgreSQL(수직 확장) | **트랜잭션 로그**와 **감사 요구**가 있는 경우 |
| 비용 | 데이터 저장량 및 트랜잭션 수 기반 | DynamoDB(요금제 복잡), PostgreSQL(인스턴스 비용) | **프리티어 활용** 및 **비용 최적화**가 필요한 경우 |
| 지연시간 | 트랜잭션 로그 처리로 인한 추가 지연 | DynamoDB(저지연), PostgreSQL(고지연) | **실시간 처리**가 필요하지 않은 경우 |

#### 📝 시험 대비 체크리스트
- [ ] QLDB의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  → "QLDB는 불변성 데이터를 관리하고 감사 트래픽을 보장하는 분산 데이터베이스입니다."
- [ ] QLDB를 선택해야 하는 시나리오를 알고 있는가?  
  → "불변성 데이터, 감사 추적, 법적 요구사항이 필요한 시나리오"
- [ ] QLDB의 제한사항/한계를 알고 있는가?  
  → "트랜잭션 로그 처리로 인한 추가 지연, 데이터 수정 불가"
- [ ] QLDB와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  → "QLDB는 불변성 중심, DynamoDB는 실시간 처리 중심"
- [ ] QLDB의 비용 구조를 이해하고 있는가?  
  → "데이터 저장량 및 트랜잭션 수에 따라 비용이 책정되며, 프리티어 제공"

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 QLDB를 떠올리세요:  
> - **불변성**  
> - **트랜잭션 로그**  
> - **감사 추적**  
> - **법적 감사**  
> - **프리티어**  
> - **불변 레이더**

---

| [⬅️ Keyspaces](./Keyspaces.md) | [📑 Day 3 목차](./README.md) | [🏠 Week 3](../README.md) | [Timestream ➡️](./Timestream.md) |

---
