---

| [⬅️ 이전 Day](../day1/README.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 3](../README.md) | [DAX ➡️](./DAX.md) |

---

# DynamoDB 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** DynamoDB는 고가용성, 확장성, 실시간 데이터 처리를 위한 AWS의 NoSQL 데이터베이스 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 관계형 데이터베이스의 복잡한 스키마 설계와 수동적인 스케일링 필요성  
  *구체적 설명:* 전통적인 MySQL/PostgreSQL은 스키마 변경 시 데이터베이스 중단이 발생하며, 트래픽 증가 시 수동 서버 확장이 필요했습니다.  
- **문제 2:** 실시간 데이터 처리 시 높은 지연 시간  
  *구체적 설명:* 기존 데이터베이스는 읽기/쓰기 작업에 동기화 오버헤드로 인해 대규모 트래픽 시 응답 시간이 급격히 증가했습니다.  
- **문제 3:** 데이터 분산 시 복잡한 복제 및 동기화 관리  
  *구체적 설명:* 글로벌 데이터 동기화는 복잡한 네트워크 설정과 수동 모니터링이 필요했으며, 지연 시간이 발생할 수 있었습니다.  

**DynamoDB로 해결:**
- **해결 1:** 자동 스키마 유연성과 무중단 스케일링  
  *어떻게 해결되는지:* NoSQL 구조를 통해 스키마 변경 없이 데이터 유연성 제공, 자동 확장 기능으로 트래픽 증가 시 서버 자동 확장.  
- **해결 2:** 초저지연 실시간 처리  
  *어떻게 해결되는지:* 지역별 리플리케이션과 DAX 캐싱을 통해 읽기/쓰기 지연 최소화, 단일 읽기/쓰기 작업이 10ms 이내 처리.  
- **해결 3:** 글로벌 데이터 동기화 및 지연 최소화  
  *어떻게 해결되는지:* 글로벌 테이블 기능으로 데이터를 여러 리전에 실시간 복제, 지역별 리전에서의 읽기/쓰기 지연 최적화.  

### 비유로 이해하기
DynamoDB는 도서관의 지능형 책장 시스템입니다. 고객이 어떤 책을 찾는지에 따라 책장이 자동으로 분류되고, 책이 빠르게 찾아지도록 최적화됩니다. 예를 들어, "코딩 서적"을 찾는 고객에게는 코딩 관련 책장이 우선 노출되며, 고객이 자주 찾는 책은 특별한 책장에 배치됩니다. 이처럼 DynamoDB는 데이터를 효율적으로 분산하고, 고객의 요청에 따라 자동으로 최적화된 데이터 접근 경로를 제공합니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 고가용성과 확장성 요구 시 | 전자상거래 플랫폼 (예: Amazon, Shopify) |
| 시나리오 2 | 실시간 데이터 처리가 필수적일 때 | IoT 센서 데이터 수집 및 분석 (예: 스마트 시티) |
| 시나리오 3 | 글로벌 사용자 데이터 동기화 시 | 다국적 SaaS 플랫폼 (예: Zoom, Slack) |

**이럴 때 DynamoDB를 선택하세요:**
- ✅ 상황 1: 동시 접속자 수가 급증할 수 있는 웹 애플리케이션  
- ✅ 상황 2: 실시간 데이터 분석이 필요한 스마트 기기 앱  
- ✅ 상황 3: 여러 국가에서 데이터를 실시간으로 동기화해야 하는 서비스  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ 상황 → 대안 서비스 추천 (이유 포함)  
  *예:* 관계형 데이터베이스가 필요할 경우 → **RDS** (트랜잭션 처리에 적합)  
- ❌ 상황 → 대안 서비스 추천 (이유 포함)  
  *예:* 대용량 파일 저장이 필요할 경우 → **S3** (스토리지 용량 확장에 적합)  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **AWS Lambda** | 실시간 데이터 처리 및 백그라운드 작업 | DynamoDB Streams → Lambda 함수 |
| **Amazon S3** | 백업 및 아카이브 저장 | DynamoDB → S3 버킷 |
| **DAX** | 읽기 지연 최소화 | DynamoDB → DAX 캐시 서버 |

**자주 사용되는 아키텍처 패턴:**
```
[사용자] → [CloudFront] → [S3] (정적 자원)  
[사용자] → [API Gateway] → [Lambda] (DynamoDB Streams 처리)  
[Lambda] → [DynamoDB] (데이터 저장 및 조회)
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **Read Capacity Units (RCU)** | $0.00025/단위 | 월 250,000 RCU 무료 |
| **Write Capacity Units (WCU)** | $0.00025/단위 | 월 250,000 WCU 무료 |
| **DAX (기본 인스턴스)** | $0.035/시간 | 7일 무료 (1GB 메모리) |

**비용 최적화 팁:**
1. 💡 팁 1: **On-Demand 모드**로 비용 예측 불가능한 작업 처리 (예: 트래픽 폭주 시)  
2. 💡 팁 2: **TTL (Time to Live)** 설정으로 자동 데이터 삭제로 스토리지 비용 절감  
3. 💡 팁 3: **DAX 캐싱**을 활용해 읽기 요청 수 감소로 비용 최적화  

> **⚠️ 비용 주의:** **Global Tables** 사용 시 리전별 데이터 복제 비용이 발생할 수 있으며, **TTL 미설정** 시 데이터가 무한히 저장되어 비용이 누적될 수 있습니다.  

---

## 📌 학습 목표 (Learning Outcomes)

**실습 예제:**  
```bash
# DynamoDB 테이블 생성 (AWS CLI)
aws dynamodb create-table --table-name UserTable \
  --attribute-definitions AttributeName=UserId,AttributeType=S \
  --key-schema AttributeName=UserId,KeyType=HASH \
  --provisioned-throughput ReadCapacityUnits=5,WriteCapacityUnits=5
```

**AWS 콘솔 사용법:**  
1. [AWS Management Console](https://console.aws.amazon.com/dynamodb/) 접속  
2. "Create table" 버튼 클릭 → 파티션 키 설정 → 용량 모드 선택 (Provisioned/On-Demand)  
3. "Global Secondary Index" 추가 시 GSI 설정 → "Time to Live" 활성화  

**실무 적용 예시:**  
- **e-commerce:** 사용자 장바구니 데이터 저장 (파티션 키: UserId, 정렬 키: ItemId)  
- **IoT:** 센서 데이터 로깅 (TTL 설정으로 30일 후 자동 삭제)  
- **SaaS:** 글로벌 사용자 데이터 동기화 (Global Tables 활용)

## 📚 핵심 개념

### 개념 1: 파티션 키/정렬 키 (Partition Key/Sort Key)
DynamoDB는 데이터를 효율적으로 저장하고 검색하기 위해 파티션 키와 정렬 키를 사용합니다. 파티션 키는 데이터를 분산하여 처리하는 데 중요한 역할을하며, 데이터를 분산할 때 키 값을 해시하여 동일한 파티션에 집중되지 않도록 합니다. 정렬 키는 파티션 키와 함께 사용되어 특정 조건에 맞는 데이터를 정렬하여 검색할 수 있도록 합니다. 예를 들어, 사용자가 고객 정보를 저장할 때 `CustomerId`를 파티션 키로 설정하고 `OrderDate`를 정렬 키로 설정하면, 특정 고객의 주문을 시간 순으로 정렬하여 조회할 수 있습니다.

#### 왜 중요한가?
- **데이터 분산 최적화**: 파티션 키는 데이터를 여러 노드에 분산하여 처리 속도를 높이고, 단일 노드의 부하를 줄입니다.
- **검색 효율성**: 정렬 키는 특정 조건에 맞는 데이터를 빠르게 정렬하여 조회할 수 있도록 하여 쿼리 성능을 향상시킵니다.

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| 파티션 키 | 데이터를 분산하여 처리하는 데 사용되는 필드 | `CustomerId`로 고객 정보를 분산 |
| 정렬 키 | 파티션 키와 함께 데이터를 정렬하여 검색하는 필드 | `OrderDate`로 주문 날짜 순으로 정렬 |
| 해시 분산 | 파티션 키 값을 해시하여 데이터를 균일하게 분산 | `CustomerId` 해시 값으로 데이터를 분산 |

> **💡 Tip:** 파티션 키는 고유성을 가지는 필드를 선택해야 하며, 정렬 키는 조건에 맞는 데이터를 빠르게 검색할 수 있는 필드를 선택해야 합니다.

---

### 개념 2: 글로벌 세컨더리 인덱스 (Global Secondary Index, GSI)
GSI는 DynamoDB에서 데이터를 추가로 인덱싱하여 특정 조건에 맞는 데이터를 빠르게 검색할 수 있도록 합니다. GSI는 주 키(Primary Key)와 별도로 구성되며, 데이터를 추가로 분산하여 여러 조건에 대한 쿼리 성능을 향상시킵니다. 예를 들어, 고객 정보 테이블에서 `CustomerId`를 파티션 키로 설정하고 `Region`을 GSI로 설정하면, 특정 지역의 고객을 빠르게 조회할 수 있습니다.

#### 작동 원리
1. **인덱스 생성**: GSI는 테이블 생성 시 또는 이후에 생성할 수 있으며, 특정 필드를 인덱스로 설정합니다.
2. **데이터 복제**: GSI는 데이터를 별도의 노드에 복제하여 쿼리 처리 속도를 높입니다.
3. **쿼리 처리**: GSI를 사용한 쿼리는 인덱스를 기반으로 데이터를 검색하여 결과를 반환합니다.

> **💡 Tip:** GSI는 주 키와 독립적으로 작동하므로, 데이터를 여러 조건에 따라 검색할 수 있지만, 인덱스 생성 시 추가 비용이 발생합니다.

---

### 개념 3: 용량 모드 (Capacity Modes: Provisioned vs On-Demand)
DynamoDB는 용량 모드를 통해 트래픽 변화에 따라 자동 조정하거나 고정 용량을 설정할 수 있습니다. **프로비저닝 모드**는 사전에 읽기/쓰기 용량을 설정하여 일정한 성능을 보장하고, **온디맨드 모드**는 트래픽 변화에 따라 자동으로 용량을 조정하여 비용을 최소화합니다. 예를 들어, 트래픽이 예측 가능한 서비스는 프로비저닝 모드를 사용하고, 트래픽이 불확실한 서비스는 온디맨드 모드를 사용하는 것이 일반적입니다.

#### 주요 특징
1. **프로비저닝 모드**:  
   - **고정 용량**: 읽기/쓰기 용량을 사전에 설정하여 성능을 보장합니다.  
   - **비용**: 용량을 초과할 경우 비용이 증가할 수 있습니다.  
2. **온디맨드 모드**:  
   - **자동 조정**: 트래픽 증가 시 자동으로 용량을 확장합니다.  
   - **비용**: 실제 트래픽에 따라 비용이 변동하며, 초기 비용이 낮습니다.  
3. **성능**:  
   - 프로비저닝 모드는 일정한 성능을 보장하지만, 온디맨드 모드는 트래픽에 따라 성능이 변동될 수 있습니다.

> **💡 Tip:** 프로비저닝 모드는 예측 가능한 트래픽에 적합하고, 온디맨드 모드는 트래픽이 불확실한 서비스에 적합합니다. 비용 최적화를 위해 온디맨드 모드를 사용하는 것이 일반적입니다.

## 🖥️ AWS 콘솔에서 DynamoDB 사용하기

### Step 1: DynamoDB 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - IAM 사용자 권한이 활성화되어 있어야 합니다.  
2. 상단 검색창에서 "DynamoDB"를 입력하고, 검색 결과에서 **DynamoDB**를 클릭합니다.  
   - **DynamoDB 서비스 대시보드**가 열리면 정상적으로 접속되었습니다.  

> **📸 화면 확인:** DynamoDB 대시보드에서 "Tables" 탭이 표시되고, "Create table" 버튼이 보이는지 확인합니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **테이블 생성**  
   - **"Create table"** 버튼을 클릭합니다.  
   - **Table name**: `Users` (예)  
   - **Partition key**: `userId` (string 타입)  
   - **Sort key**: `timestamp` (number 타입)  
   - **GSI/LSI 추가**:  
     - **GSI**: `emailIndex` (Partition key: `email`, Sort key: `subscriptionDate`)  
     - **LSI**: `ageIndex` (Partition key: `userId`, Sort key: `age`)  

2. **용량 모드 설정**  
   - **Provisioned**: 읽기/쓰기 용량 제한 (예: ReadCapacityUnits: 5, WriteCapacityUnits: 5)  
   - **On-demand**: 자동 스케일링 (비용이 더 높음)  

3. **설정 확인**  
   - **Table status**가 `ACTIVE`로 변경되는지 확인합니다.  
   - **Table details**에서 키 정의, GSI/LSI, 용량 모드가 정확히 설정되었는지 검토합니다.  

> **📸 화면 확인:** "Create table" 페이지에서 Partition key, Sort key, GSI/LSI 설정이 올바르게 입력되었는지 확인합니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **DAX 클러스터 설정**  
   - **"Create DAX cluster"** 버튼을 클릭합니다.  
   - **Cluster name**: `UserDAXCluster`  
   - **Node count**: 3 (기본값)  
   - **Subnet group**: VPC 설정에 따라 선택  

2. **DynamoDB Streams 활성화**  
   - 테이블 설정 후, **"Manage table"** > **"Streams"** 탭에서 **"Enable"**를 선택합니다.  
   - **Stream view type**: `NEW_AND_OLD_IMAGES` (모든 변경 사항 기록)  

3. **글로벌 테이블 설정**  
   - **"Create global table"** 버튼을 클릭합니다.  
   - **Primary region**: `us-west-2`  
   - **Replicated regions**: `eu-west-1`, `ap-northeast-1`  

> **⚠️ 주의:** DAX 및 글로벌 테이블은 추가 비용이 발생하므로, 프리티어 사용 시 **1GB 데이터 저장량** 제한에 주의하세요.  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인 방법**  
   - **"Tables"** 탭에서 `Users`, `UserDAXCluster`, 글로벌 테이블이 표시되는지 확인합니다.  
   - **"Streams"** 탭에서 활성화 상태가 `ENABLED`인지 확인합니다.  

2. **테이블 상태 확인**  
   - **"Overview"** 탭에서 **Table status**가 `ACTIVE`인지, **Capacity units**가 제대로 설정되었는지 확인합니다.  

3. **정상 동작 테스트**  
   - **"Items"** 탭에서 데이터 입력/검색을 시도합니다.  
   - 예:  
     ```bash
     # CLI로 데이터 입력 (실습 시 콘솔에서 직접 입력)
     aws dynamodb put-item --table-name Users --item '{
       "userId": {"S": "123"},
       "timestamp": {"N": "1690000000"},
       "email": {"S": "user@example.com"}
     }'
     ```  
   - **GSI/LSI**를 통해 검색을 시도합니다 (콘솔 검색 필터 활용).  

> **💡 Tip:** 콘솔에서 테이블을 삭제할 경우, **"Delete table"** 버튼을 클릭하고 1분 후 자동으로 삭제됩니다.

## ⌨️ AWS CLI로 DynamoDB 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**설명:**  
AWS CLI를 사용하기 전에 버전을 확인해 최신 버전인지 확인하세요. `sts get-caller-identity` 명령어는 현재 사용자 권한을 확인하고, `configure get region`은 현재 설정된 리전을 확인합니다. 이 설정은 DynamoDB 리소스 위치에 영향을 줄 수 있습니다.

---

### 예제 1: DynamoDB 리소스 조회
```bash
# DynamoDB 테이블 목록 조회
aws dynamodb list-tables --query '[].TableName' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | JSON 결과 필터링 | `'[].TableName'` (모든 테이블 이름 출력) |
| `--output` | 출력 형식 | `json`, `table`, `text` (표 형식으로 출력) |

**예상 출력:**
```
TableName
--------
Users
Orders
```

**참고:**  
`list-tables`는 현재 리전에 있는 모든 DynamoDB 테이블을 반환합니다. `--query`는 결과를 정제해 특정 필드만 표시할 수 있습니다.

---

### 예제 2: DynamoDB 테이블 생성
```bash
# 테이블 생성 (파티션 키/정렬 키 설정)
aws dynamodb create-table \
    --table-name "Users" \
    --attribute-definitions AttributeName=UserId,AttributeType=S \
    --key-schema AttributeName=UserId,KeyType=HASH \
    --provisioned-throughput ReadCapacityUnits=5,WriteCapacityUnits=5
```

**필수 옵션:**
- `--table-name`: 테이블 이름 (예: `Users`)
- `--attribute-definitions`: 파티션 키 정의 (예: `UserId`)
- `--key-schema`: 파티션 키(`HASH`) 및 정렬 키(`RANGE`) 설정

**예상 출력:**
```json
{
    "TableDescription": {
        "TableName": "Users",
        "TableStatus": "ACTIVE",
        "CreationDateTime": 1680000000.0,
        "ProvisionedThroughput": {
            "ReadCapacityUnits": 5,
            "WriteCapacityUnits": 5
        }
    }
}
```

**주의:**  
`provisioned-throughput`는 용량 모드 설정에 직접 연결됩니다. `on-demand` 모드는 이 옵션을 생략해야 합니다.

---

### 예제 3: DynamoDB 테이블 수정
```bash
# 테이블 용량 모드 변경 (온디맨드로 전환)
aws dynamodb update-table \
    --table-name "Users" \
    --provisioned-throughput ReadCapacityUnits=0,WriteCapacityUnits=0
```

**설명:**  
`update-table` 명령어는 용량 모드 변경, GSI 추가, TTL 설정 등 테이블 구조 수정에 사용됩니다. `provisioned-throughput`의 `ReadCapacityUnits`와 `WriteCapacityUnits`를 `0`으로 설정하면 `on-demand` 모드로 전환됩니다.

---

### 예제 4: DynamoDB 테이블 삭제
```bash
# 테이블 삭제
aws dynamodb delete-table --table-name "Users"

# 삭제 확인
aws dynamodb describe-table --table-name "Users"
```

> **⚠️ 주의:**  
> `delete-table`는 영구적으로 삭제되며, 복구가 불가능합니다. 삭제 후 `describe-table` 명령어로 상태를 확인하세요.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws dynamodb list-tables
aws dynamodb describe-table --table-name "Users"

# 생성
aws dynamodb create-table --table-name "Orders" --attribute-definitions ... --key-schema ... --provisioned-throughput ...

# 수정
aws dynamodb update-table --table-name "Orders" --provisioned-throughput ...

# 삭제
aws dynamodb delete-table --table-name "Orders"
```

**추가 팁:**  
- **GSI 생성:** `create-table` 명령어에 `GlobalSecondaryIndexes` 파라미터 추가  
- **DAX 연동:** `update-table` 명령어에 `GlobalSecondaryIndexSettings` 설정  
- **TTL 설정:** `update-table` 명령어에 `TimeToLiveSpecification` 파라미터 추가  

---

### CLI 사용 시 주의사항
| 항목 | 설명 |
|------|------|
| **리전 설정** | `aws configure set region`으로 리전 변경 후 명령어 재실행 필요 |
| **비용 관리** | `on-demand` 모드는 비용이 높으므로 프리티어 사용 시 주의 |
| **리소스 제한** | 1일 최대 100개 테이블 생성 가능 (AWS 제한) |
| **데이터 유실 위험** | `delete-table`는 데이터를 즉시 삭제하므로 백업 필수 |

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 DynamoDB 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **포인트 1: 파티션 키/정렬 키 (Partition Key/Sort Key)**  
   - 설명: DynamoDB의 핵심 데이터 조직 방식으로, 읽기/쓰기 성능과 분산 처리에 직접적인 영향을 미칩니다. 잘못 설계하면 랜덤 읽기/쓰기 성능 저하, 캡슐화 비용 증가 등의 문제가 발생합니다.  
   - 키워드: `Partition Key`, `Sort Key`, `Throughput`

2. **포인트 2: 글로벌 세컨더리 인덱스 (Global Secondary Index, GSI)**  
   - 설명: 로컬 인덱스(LSI)와 달리, GSI는 글로벌 범위의 데이터를 검색할 수 있어 복잡한 쿼리 시스템에서 유용합니다. 하지만 GSI는 별도의 읽기/쓰기 용량을 소비하므로 비용 관리가 필수적입니다.  
   - 키워드: `GSI`, `Query`, `Read Capacity Units`

3. **포인트 3: 용량 모드(프로비저닝 vs 온디맨드)**  
   - 설명: 프로비저닝 모드는 고정된 용량을 설정해 예측 가능한 비용을 제공하지만, 온디맨드 모드는 자동 스케일링을 통해 비용 최적화가 가능합니다. 시험에서 비용 최적화 전략 관련 문제는 대부분 이 두 모드를 중심으로 출제됩니다.  
   - 키워드: `Provisioned`, `On-Demand`, `Cost Optimization`

4. **포인트 4: DAX (DynamoDB Accelerator)**  
   - 설명: 메모리 기반 캐싱 서비스로, DynamoDB의 읽기 지연시간을 최소화합니다. 하지만 DAX는 캐시만 제공하므로 데이터 일관성 유지나 쓰기 처리에는 사용할 수 없습니다. 시험에서 캐싱 관련 문제는 DAX와 DynamoDB Streams를 혼동하는 경우가 많습니다.  
   - 키워드: `DAX`, `Caching`, `Read Latency`

5. **포인트 5: DynamoDB Streams**  
   - 설명: 테이블 변경 사항을 실시간으로 추적해 다른 시스템(예: Kinesis, Lambda)과 연동하는 데 사용됩니다. 시험에서는 Streams의 주요 용도와 데이터 일관성 유지 방식(예: 최종 일관성)을 묻는 문제가 자주 출제됩니다.  
   - 키워드: `Streams`, `Change Data Capture`, `Event Processing`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | GSI와 LSI를 혼동해 GSI가 로컬 인덱스라고 오인 | GSI는 글로벌 범위의 인덱스로, 테이블 전체 데이터를 검색 가능 |
| 함정 2 | 온디맨드 모드가 비용이 항상 낮다고 오인 | 온디맨드는 용량 자동 조정이지만, 트래픽 폭주 시 비용이 급격히 증가할 수 있음 |
| 함정 3 | DAX가 쓰기 처리에 사용 가능하다고 오인 | DAX는 읽기 캐싱만 지원, 쓰기 처리에는 DynamoDB 자체 사용 |
| 함정 4 | DynamoDB Streams가 실시간 데이터 일관성을 보장한다고 오인 | Streams는 최종 일관성 유지, 즉시 반영되지 않음 |
| 함정 5 | TTL이 데이터 삭제 정책을 대체한다고 오인 | TTL은 자동 삭제 기능이지만, 정책 변경 시 수동 삭제 필요 |

#### 🔄 DynamoDB vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | DynamoDB | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| **용도** | NoSQL 데이터베이스, 실시간 읽기/쓰기 처리 | RDS (MySQL/PostgreSQL), MongoDB Atlas | 데이터 모델이 NoSQL인 경우 DynamoDB 선택 |
| **확장성** | 자동 스케일링, 수만 개의 파티션 지원 | RDS: 수동 스케일링, MongoDB: 복제셋 기반 | 높은 확장성과 유연한 스키마 필요 시 DynamoDB |
| **비용** | 프로비저닝/온디맨드 모드로 유연한 비용 관리 | RDS: 고정 비용, MongoDB: 서버 관리 비용 | 비용 최적화 전략이 중요한 시나리오 |
| **지연시간** | DAX 사용 시 1밀리초 이내 | RDS: 캐시 없이 DB 서버 연동 | 저지연 읽기 처리가 필수적인 경우 DynamoDB |

#### 📝 시험 대비 체크리스트
- [ ] DynamoDB의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  *예: "DynamoDB는 확장성과 유연성을 갖춘 NoSQL 데이터베이스로, 실시간 읽기/쓰기를 지원합니다."*
- [ ] DynamoDB를 선택해야 하는 시나리오를 알고 있는가?  
  *예: "서버리스, 유연한 스키마, 고성능 읽기/쓰기, 자동 스케일링이 필요한 시나리오"*
- [ ] DynamoDB의 제한사항/한계를 알고 있는가?  
  *예: "지연 시간 최적화는 DAX를 통해 해결, 복잡한 쿼리 처리는 GSI로 제한"*
- [ ] DynamoDB와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  *예: "RDS는 관계형 데이터베이스로 스키마 정의 필요, DynamoDB는 NoSQL로 스키마 유연성 제공"*
- [ ] DynamoDB의 비용 구조를 이해하고 있는가?  
  *예: "프로비저닝 모드는 고정 비용, 온디맨드 모드는 실제 트래픽 기반 변동 비용"*

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 DynamoDB를 떠올리세요:  
> - **NoSQL**  
> - **Real-time Read/Write**  
> - **Auto Scaling**  
> - **Low Latency**  
> - **GSI**  
> - **DAX**  
> - **Event Stream**  
> - **TTL**

---

| [⬅️ 이전 Day](../day1/README.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 3](../README.md) | [DAX ➡️](./DAX.md) |

---
