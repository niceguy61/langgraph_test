---

| [⬅️ DynamoDB](./DynamoDB.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 3](../README.md) | [DynamoDB Streams ➡️](./DynamoDB-Streams.md) |

---

# DAX 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** DAX는 DynamoDB 쿼리 성능을 향상시키기 위한 AWS의 분산 인메모리 데이터베이스 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** DynamoDB에서 빈도가 높은 읽기 요청이 있을 때, 모든 요청이 원본 데이터베이스에 전달되어 지연 시간이 길어집니다. 예를 들어, e-commerce 애플리케이션에서 상품 목록 조회 시, 수천 번의 읽기 요청이 발생할 수 있습니다.
- **문제 2:** 읽기 요청이 급증할 경우, DynamoDB의 읽기 캍ACITY가 초과되어 요청이 거부되거나, 추가 비용이 발생합니다. 이는 확장성 문제가 됩니다.
- **문제 3:** 글로벌 사용자에게 데이터를 제공할 때, 지연 시간이 급격히 증가하여 사용자 경험에 악영향을 줍니다. 예를 들어, 해외 사용자가 실시간으로 데이터를 조회할 때 지연이 발생합니다.

**DAX로 해결:**
- **해결 1:** DAX는 인메모리 캐시로 빈도가 높은 데이터를 저장하여, 읽기 요청을 원본 데이터베이스로 전달하지 않아 지연 시간을 10~100배 단축합니다. 예: 상품 목록 조회 시 캐시에서 바로 응답합니다.
- **해결 2:** 캐시를 통해 DynamoDB의 읽기 캐파시티를 감소시켜, 비용 절감과 확장성 향상을 도모합니다. 예: 1,000개 요청이 들어오더라도 DAX가 처리해 DynamoDB는 100개 요청만 처리합니다.
- **해결 3:** DAX는 글로벌 리전에서 동기화되어, 사용자 지역에 가까운 노드에서 데이터를 제공해 지연 시간을 최소화합니다. 예: 미국 사용자가 데이터를 조회할 때 미국 리전의 DAX 노드에서 응답합니다.

### 비유로 이해하기
DAX는 고속도로를 이용해 자주 방문하는 식당까지 빠르게 이동하는 비유입니다. 기존에는 자동차로 도로를 따라 가야 했지만, DAX는 자동차 대신 고속도로를 이용해 시간을 절약합니다. 지연 시간(도로 트래픽)을 줄이고, 연료비(비용)를 절약하며, 글로벌 사용자에게도 빠른 접근이 가능해집니다. 

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 빈도가 높은 읽기 요청이 발생하는 애플리케이션 | 온라인 쇼핑몰의 상품 목록 조회, 게임의 실시간 점수 조회 |
| 시나리오 2 | 고성능 및 저지연이 필수적인 시스템 | 랜딩 페이지의 실시간 방문자 수 통계, 스프링 배틀의 실시간 점수 |
| 시나리오 3 | 글로벌 사용자에게 데이터를 제공해야 하는 서비스 | SaaS 플랫폼의 사용자 프로필 조회, 해외 사용자 대상의 실시간 데이터 제공 |

**이럴 때 DAX를 선택하세요:**
- ✅ **상황 1:** 100개 이상의 읽기 요청이 매 초 발생하는 시나리오
- ✅ **상황 2:** 지연 시간이 10ms 미만으로 요구되는 실시간 시스템
- ✅ **상황 3:** 글로벌 사용자에게 데이터를 제공해야 하는 서비스

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **상황 → DynamoDB 글로벌 테이블:** 데이터 복제를 통해 지역별 접근성 향상, 하지만 지연 시간이 DAX보다 높음
- ❌ **상황 → ElastiCache:** DAX와 유사하지만, DynamoDB와 직접 통합되지 않음
- ❌ **상황 → RDS:** 관계형 데이터베이스로, DynamoDB의 NoSQL 특성과 호환되지 않음

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **DynamoDB** | DAX는 DynamoDB의 읽기 요청을 캐시하여 원본 데이터베이스 부담을 줄임 | User → DAX → DynamoDB |
| **AWS Lambda** | Lambda 함수가 DAX 캐시를 통해 데이터를 빠르게 조회함 | Lambda → DAX → DynamoDB |
| **CloudFront** | CloudFront가 DAX 캐시를 통해 전달된 데이터를 사용자에게 제공 | User → CloudFront → DAX → DynamoDB |

**자주 사용되는 아키텍처 패턴:**
```
User → CloudFront → DAX → DynamoDB → Lambda → S3
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **DAX 요청 수 (Per 10,000 requests)** | $0.0045 | 월 100,000 요청 무료 |
| **DAX 저장 용량 (Per GB-month)** | $0.10 | 12개월 무료 |
| **DAX 스토리지 (Per GB)** | $0.10 | 항상 무료 |

**비용 최적화 팁:**
1. 💡 **팁 1:** 프리티어를 활용해 월 100,000 요청까지 무료로 사용하세요. 실무에서 대부분의 캐시 요청이 이 범위 안에 포함됩니다.
2. 💡 **팁 2:** DAX의 저장 용량을 최소화해 스토리지 비용을 절감하세요. 예: 캐시된 데이터를 주기적으로 삭제하거나, TTL 설정을 통해 자동 삭제.
3. 💡 **팁 3:** DAX를 사용하는 애플리케이션에서 읽기 요청을 최대한 줄여, DAX 요청 수를 줄이세요. 예: 캐시 히트율을 높이기 위해 데이터를 적절히 선택적으로 캐시.

> **⚠️ 비용 주의:** DAX 요청 수가 예상치 못하게 증가할 경우, 비용이 급격히 증가할 수 있습니다. 예: 프리티어 한도를 초과하면 $0.0045/요청으로 비용이 늘어납니다. AWS CloudWatch를 통해 요청 수를 모니터링하고, 필요 시 캐시 정책을 조정하세요.

## 📚 핵심 개념

### 개념 1: DAX (DynamoDB Accelerator)
DAX는 DynamoDB에서 데이터 액세스 속도를 높이기 위해 설계된 캐싱 서비스입니다. DynamoDB의 읽기 요청을 캐시하여, 데이터베이스에서 직접 읽는 대신 메모리 기반의 고성능 캐시를 사용해 응답 시간을 단축합니다. DAX는 특히 고부하 읽기 작업이 필요한 애플리케이션에서 유용하며, 실시간 데이터 처리 및 분석 시에도 효과적입니다. DAX는 Redis와 유사한 인메모리 캐시 기술을 기반으로 하며, DynamoDB의 키-값 스토어 구조에 최적화되어 있습니다.

#### 왜 중요한가?
- **응답 시간 단축**: 캐시를 통해 DynamoDB의 읽기 작업이 메모리에서 처리되므로, 일반적인 DynamoDB 읽기 작업 대비 최대 10배 이상의 속도 향상이 가능합니다.  
- **고가용성**: DAX 클러스터는 다중 노드로 구성되어 장애 시 자동 복구를 지원하며, 네트워크 장애나 노드 고장 시에도 데이터 액세스가 지속됩니다.  
- **비용 효율성**: 읽기 작업이 많고 쓰기 작업이 적은 애플리케이션에서는 DAX를 사용해 DynamoDB의 읽기 용량을 줄이고, 비용 절감이 가능합니다.

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-|-|
| **캐시 전략** | 데이터를 메모리에 저장해 읽기 요청을 가속화 | `GET` 요청이 캐시에 존재하면 즉시 응답 |  
| **동기/비동기 업데이트** | 캐시는 DynamoDB의 변경 사항을 비동기적으로 동기화 | `PUT` 요청 시 캐시는 즉시 업데이트되지만, DynamoDB에 반영까지는 시간이 걸림 |  
| **데이터 일관성** | 캐시는 DynamoDB의 최신 상태를 반영하지 않을 수 있음 | 캐시에 저장된 데이터가 DynamoDB와 다를 경우, `ConsistentRead`를 사용해 정확한 데이터를 가져올 수 있음 |  

> **💡 Tip:** DAX는 읽기 작업이 많은 애플리케이션에 최적화되며, 쓰기 작업이 많은 경우 DynamoDB의 기본 기능을 사용하는 것이 더 적절합니다.  

---

### 개념 2: 용량 모드 (Provisioned vs On-Demand)
DynamoDB는 **용량 모드**를 통해 읽기/쓰기 작업의 자원 할당 방식을 결정합니다. 이는 작업량에 따라 성능과 비용을 조절하는 핵심 개념입니다.  
- **Provisioned Capacity**: 예측 가능한 작업량을 가정하고, 읽기/쓰기 용량을 미리 설정합니다.  
- **On-Demand Capacity**: 작업량이 예측 불가능한 경우, AWS가 자동으로 자원을 확장해 처리합니다.  

#### 작동 원리
1. **Provisioned 모드**: 사용자가 읽기/쓰기 용량을 명시적으로 설정합니다. 예를 들어, `ReadCapacityUnits`가 5인 경우, DynamoDB는 5개의 읽기 요청을 동시에 처리할 수 있습니다.  
2. **On-Demand 모드**: AWS가 작업량에 따라 자동으로 용량을 조절합니다. 예를 들어, 100개의 읽기 요청이 발생하면 AWS는 자동으로 추가 자원을 확보해 처리합니다.  
3. **비용 계산**: Provisioned 모드는 고정 비용이지만, Overprovisioning 시 비용이 증가할 수 있습니다. On-Demand 모드는 작업량에 따라 비용이 변동되며, 자원 낭비가 최소화됩니다.  

> **💡 Tip:** 예측 가능한 작업량이 많은 경우 Provisioned 모드를, 불확실한 작업량이 많은 경우 On-Demand 모드를 선택하세요.  

---

### 개념 3: DynamoDB Streams
DynamoDB Streams는 DynamoDB 테이블의 변경 사항을 실시간으로 캡처하여, 외부 시스템에 전달하는 기능입니다. 이 기능은 데이터 변경 사항을 처리하는 트리거, 로깅, 실시간 분석 등에 활용됩니다.  
- **동기/비동기 처리**: DynamoDB Streams는 변경 사항을 즉시 캡처해 AWS Lambda, Kinesis 등으로 전달합니다.  
- **데이터 일관성**: 변경 사항이 DynamoDB에 반영된 후에만 Streams로 전달되므로, 데이터 일관성이 보장됩니다.  

#### 주요 특징
1. ****실시간 데이터 처리**: 읽기/쓰기 작업 시 변경 사항을 즉시 캡처해 분석, 로깅, 외부 시스템 연동에 활용 가능  
2. ****무제한 스트림 유지**: 데이터 변경 사항은 7일간 보관되며, 필요 시 저장소에서 복구 가능  
3. ****스케일러블**: 작업량 증가 시 자동으로 스트림 처리 능력이 확장됨  

> **💡 Tip:** DynamoDB Streams는 실시간 데이터 처리 시스템의 핵심 기능으로, 데이터 분석, 이벤트 추적, 외부 시스템 동기화에 적합합니다.  

--- 

### 📌 요약 및 실무 팁
| 개념 | 주요 사용 사례 | 비용 고려 사항 |
|-----|----------------|----------------|
| **DAX** | 고성능 읽기 작업, 실시간 분석 | 읽기 작업 비중이 높은 경우 비용 절감 |
| **용량 모드** | 예측 가능한 작업량 → Provisioned, 불확실한 작업량 → On-Demand | Overprovisioning 시 비용 증가 주의 |
| **DynamoDB Streams** | 실시간 데이터 처리, 이벤트 추적 | 데이터 저장소 관리 및 비용 모니터링 필수 |  

> **⚠️ 주의:** DAX는 캐시일 수 있으므로, 데이터 일관성은 DynamoDB의 `ConsistentRead`를 통해 보장해야 합니다.

## 🖥️ AWS 콘솔에서 DAX 사용하기

### Step 1: DAX 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: [https://console.aws.amazon.com](https://console.aws.amazon.com)  
2. 상단 검색창에서 **"DAX"**를 입력하고, 검색 결과에서 **"DAX"** 서비스를 클릭합니다  
   - **AWS DAX 대시보드**가 열리면 **DAX 클러스터**를 관리할 수 있는 환경이 준비되었습니다  

> **📸 화면 확인:**  
> - 상단에 **DAX** 타이틀 표시  
> - 좌측 메뉴에서 **Clusters**, **Tags**, **Events** 등의 탭이 보임  
> - **Create cluster** 버튼이 눌러진 상태인지 확인  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **DAX 클러스터 생성**  
   - **"Create cluster"** 버튼을 클릭합니다  
   - **Cluster name**을 입력합니다 (예: `MyDAXCluster`)  
   - **Region**을 선택합니다 (예: `us-east-1`)  
   - **Node type**을 선택합니다 (예: `dax.r4.large`)  
   - **Capacity mode**를 선택합니다 (프로비저닝 또는 온디맨드)  
     - **Provisioned**: 고정된 용량으로 확보 (비용이 더 높음)  
     - **On-demand**: 요청 시 자동 확장 (비용이 유동적)  

2. **VPC 및 서브넷 설정**  
   - **VPC**를 선택합니다 (기본 VPC 또는 사용자 정의 VPC)  
   - **Subnets**을 선택합니다 (일반적으로 3개의 서브넷을 선택)  
   - **Security groups**를 설정하여 DAX 클러스터의 네트워크 보안을 관리합니다  

3. **확정 및 생성**  
   - **"Create"** 버튼을 클릭합니다  
   - 생성 완료 후 **"View cluster"**를 선택하여 상태를 확인합니다  

> **📸 화면 확인:**  
> - **Cluster name**과 **Status**가 `CREATE_COMPLETE`로 표시됨  
> - **Nodes** 섹션에서 노드가 생성되었는지 확인  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **DAX 클러스터 연결 설정**  
   - DynamoDB 테이블과 DAX 클러스터를 연결하기 위해 **AWS CLI** 또는 **SDK**를 사용합니다  
   - 예시:  
     ```bash
     aws dax describe-clusters --cluster-name MyDAXCluster
     ```  
   - **DynamoDB Streams**를 활성화하여 DAX 클러스터에 실시간 데이터 동기화를 설정할 수 있습니다  

2. **TTL 및 GSI/LSI 설정**  
   - DynamoDB 테이블에서 **TTL (Time to Live)**을 설정하여 데이터 유효 기간을 관리합니다  
   - **Global Secondary Index (GSI)** 및 **Local Secondary Index (LSI)**를 생성하여 데이터 검색 속도를 향상시킵니다  
   - DAX는 GSI/LSI의 캐시를 자동으로 관리하므로, DynamoDB 테이블 설정에 따라 DAX가 동작합니다  

3. **글로벌 테이블 설정**  
   - **Global Tables**를 사용하여 데이터를 여러 리전에 복제합니다  
   - DAX 클러스터는 글로벌 테이블의 지역 복제본에 연결되어, 지역별 데이터 캐시를 관리합니다  

> **⚠️ 주의:**  
> - DAX는 **DynamoDB 글로벌 테이블**과 직접 연동되며, 별도의 설정이 필요하지 않습니다  
> - DAX는 **DynamoDB Streams**를 활용해 실시간 데이터 동기화를 지원합니다  

---

### Step 4: 설정 확인 및 테스트  
1. **리소스 상태 확인**  
   - DAX 클러스터 대시보드에서 **Status**를 확인합니다 (예: `ACTIVE`)  
   - **Nodes** 섹션에서 노드 상태가 `ONLINE`인지 확인합니다  

2. **테스트 쿼리 실행**  
   - AWS CLI로 DAX 클러스터에 연결해 테스트 쿼리를 실행합니다:  
     ```bash
     aws dax describe-clusters --cluster-name MyDAXCluster
     ```  
   - DynamoDB 테이블에 데이터를 삽입/검색해 DAX 캐시가 작동하는지 확인합니다  

3. **비용 및 프리티어 확인**  
   - DAX는 **AWS Free Tier**에서 750시간까지 무료로 사용 가능합니다  
   - **Cost Explorer**를 통해 사용량을 모니터링하고, **Auto Scaling**을 설정해 비용을 최적화합니다  

> **📸 화면 확인:**  
> - **DAX 클러스터**의 **Status**가 `ACTIVE`  
> - **DynamoDB 테이블**에서 **TTL** 및 **GSI** 설정이 적용됨  
> - **CLI 명령어**로 DAX 상태를 확인할 수 있는 화면  

---

### 📌 요약 및 팁  
| 항목 | 설명 |  
|------|------|  
| **Capacity Mode** | 프로비저닝(고정 용량) 또는 온디맨드(요청 시 확장) |  
| **TTL** | 데이터 유효 기간을 자동으로 관리 |  
| **GSI/LSI** | 캐시 성능 향상을 위해 인덱스를 활용 |  
| **비용** | 프리티어 750시간 제공, Auto Scaling으로 비용 최적화 |  

> **💡 Tip:**  
> - DAX는 DynamoDB의 읽기 작업에 최적화되어 있으며, 쓰기 작업은 DynamoDB에 직접 전달해야 합니다  
> - **DAX 클러스터**는 DynamoDB 테이블과 병렬로 작동하므로, 데이터 일관성은 DynamoDB에서 관리됩니다

## ⌨️ AWS CLI로 DAX 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

> **💡 Tip:** CLI 명령어 실행 전, AWS CLI가 정상적으로 구성되었는지 확인하세요.  
> **⚠️ 주의:** CLI 명령어는 AWS 서비스에 직접 영향을 줄 수 있으므로, 예제 실행 전 반드시 테스트 환경에서 테스트하세요.

---

### 예제 1: DAX 클러스터 목록 조회
```bash
# DAX 클러스터 목록 조회
aws dax list-clusters --query 'Clusters[].ClusterName' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|-----|--------|
| `--query` | 결과 필터링 (예: 클러스터 이름만 출력) | `'Clusters[].ClusterName'` |
| `--output` | 출력 형식 (JSON, 테이블, 텍스트) | `table` |

**예상 출력:**
```
ClusterName
----------------
example-cluster
```

> **💡 Tip:** `--query`를 사용해 특정 필드만 필터링해 복잡한 결과를 간결하게 관리하세요.

---

### 예제 2: DAX 클러스터 생성
```bash
# DAX 클러스터 생성
aws dax create-cluster \
    --cluster-name "example-cluster" \
    --replication-group "my-replication-group" \
    --availability-zones "us-west-2a,us-west-2b" \
    --node-group-configuration "NodeGroupCount=2,ReplicaCount=1"
```

**필수 옵션:**
- `--cluster-name`: 클러스터 이름 (UNIQUE)
- `--replication-group`: 복제 그룹 이름 (DAX 클러스터에 필수)
- `--availability-zones`: 노드 배치할 존 목록

**예상 출력:**
```json
{
    "Cluster": {
        "ClusterName": "example-cluster",
        "ClusterStatus": "creating",
        "ClusterId": "dax-12345678"
    }
}
```

> **⚠️ 주의:** `--replication-group`는 DAX 클러스터에만 사용 가능하며, DynamoDB와 혼동하지 마세요.

---

### 예제 3: DAX 클러스터 수정
```bash
# DAX 클러스터 노드 수 수정
aws dax update-cluster \
    --cluster-name "example-cluster" \
    --node-group-configuration "NodeGroupCount=3,ReplicaCount=1"
```

**수정 가능한 옵션:**
- `--node-group-configuration`: 노드 그룹 수/복제 수 조정
- `--scaling`: 자동 확장 설정 (DAX 2023년 10월 이후 지원)

> **💡 Tip:** DAX 클러스터는 `--scaling` 파라미터를 사용해 자동 확장을 설정할 수 있습니다.

---

### 예제 4: DAX 클러스터 삭제
```bash
# DAX 클러스터 삭제
aws dax delete-cluster --cluster-name "example-cluster"

# 삭제 확인
aws dax describe-clusters --cluster-name "example-cluster"
```

**삭제 후 출력 예시:**
```
{
    "Clusters": []
}
```

> **⚠️ 주의:** DAX 클러스터 삭제는 즉시 실행되며, 데이터는 복구 불가능합니다. 삭제 전 데이터 백업을 확인하세요.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws dax list-clusters
aws dax describe-clusters --cluster-name "example-cluster"

# 생성
aws dax create-cluster --cluster-name "example-cluster" --replication-group "my-replication-group"

# 수정
aws dax update-cluster --cluster-name "example-cluster" --node-group-configuration "NodeGroupCount=3"

# 삭제
aws dax delete-cluster --cluster-name "example-cluster"
```

> **💡 Tip:** DAX 클러스터는 DynamoDB의 `Global Tables`와 별도로 운영되며, `TTL` 설정은 DynamoDB 테이블에서 관리됩니다. DAX는 캐시 역할만 수행합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 DAX 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **DAX의 핵심 목적과 용도**  
   - 설명: DAX는 DynamoDB의 저지연 읽기 작업을 최적화하는 캐시 서비스로, 대규모 트랜잭션 처리 시 성능 향상이 시험에서 주요 포인트입니다. DAX는 DynamoDB의 읽기 작업을 메모리 기반으로 처리해 지연 시간을 줄이는 데 특화되어 있습니다.  
   - 키워드: `저지연 읽기`, `캐시`, `DynamoDB 통합`

2. **DAX와 DynamoDB Streams의 차이점**  
   - 설명: DAX는 실시간 읽기 성능 개선에 초점이 맞춰진 반면, DynamoDB Streams는 데이터 변경 이력을 추적하는 데 사용됩니다. 시험에서 두 서비스의 구분을 명확히 이해하는 것이 필수적입니다.  
   - 키워드: `데이터 변경 이력`, `실시간 처리`, `이벤트 기반 아키텍처`

3. **DAX의 비용 구조와 프리티어 제한**  
   - 설명: DAX는 프로비저닝 및 온디맨드 모드를 지원하지만, AWS 프리티어는 DAX 서비스에 제한적으로 제공됩니다. 시험에서는 비용 효율성과 리소스 관리 전략을 묻는 문제가 자주 출제됩니다.  
   - 키워드: `프리티어`, `비용 효율`, `리소스 관리`

4. **DAX의 제한사항과 한계**  
   - 설명: DAX는 DynamoDB의 읽기 작업에만 적용 가능하며, 쓰기 작업은 DynamoDB 자체에서 처리해야 합니다. 또한, DAX는 데이터 일관성 보장이 어려워 분산 시스템 설계 시 주의가 필요합니다.  
   - 키워드: `데이터 일관성`, `쓰기 제한`, `분산 시스템`

5. **DAX vs ElastiCache 비교**  
   - 설명: DAX는 DynamoDB와 깊은 통합을 지원해 저지연 읽기를 최적화하지만, ElastiCache는 Redis/Memcached와 호환되는 유연한 캐시 솔루션입니다. 시험에서는 두 서비스의 장단점을 비교하는 문제가 자주 등장합니다.  
   - 키워드: `유연성`, `통합도`, `데이터 일관성`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | DAX가 DynamoDB 대체 서비스라고 오인하는 경우 | DAX는 DynamoDB의 캐시 서비스로, DynamoDB 자체를 대체하지 않습니다. |
| 함정 2 | DAX의 쓰기 작업이 지원된다고 오인하는 경우 | DAX는 읽기 작업에만 최적화되어 있으며, 쓰기 작업은 DynamoDB에서 처리해야 합니다. |
| 함정 3 | DAX의 비용이 ElastiCache보다 항상 낮다고 오인하는 경우 | DAX의 비용은 사용량에 따라 달라지며, 리소스 사용량에 따라 ElastiCache가 더 비용 효율일 수 있습니다. |

#### 🔄 DAX vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | DAX | 대안 서비스 (ElastiCache) | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | DynamoDB 읽기 지연 최소화 | Redis/Memcached 호환 캐시 솔루션 | DynamoDB와 깊은 통합이 필요할 때 |
| 확장성 | DAX 클러스터로 확장 가능 | Redis/Memcached 클러스터로 확장 | 고가용성 및 확장성 요구 시 |
| 비용 | 프로비저닝/온디맨드 모드 지원 | 유연한 인스턴스 크기 선택 | 리소스 사용량에 따라 비용 최적화 |
| 지연시간 | 메모리 기반으로 초단위 지연 | Redis/Memcached의 지연 시간 유동적 | 초단위 저지연 읽기 필수 시 |

#### 📝 시험 대비 체크리스트
- [ ] DAX의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  → "DAX는 DynamoDB의 저지연 읽기 작업을 최적화하는 캐시 서비스입니다."
- [ ] DAX를 선택해야 하는 시나리오를 알고 있는가?  
  → "고성능 읽기 작업이 필요한 DynamoDB 애플리케이션에 적합합니다."
- [ ] DAX의 제한사항/한계를 알고 있는가?  
  → "쓰기 작업은 DynamoDB에서 처리해야 하며, 데이터 일관성 보장이 어렵습니다."
- [ ] DAX와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  → "ElastiCache는 유연한 캐시 솔루션으로, DAX는 DynamoDB와 깊은 통합을 제공합니다."
- [ ] DAX의 비용 구조를 이해하고 있는가?  
  → "프로비저닝 및 온디맨드 모드 지원, 리소스 사용량에 따라 비용이 변동됩니다."

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 DAX를 떠올리세요:  
> - **저지연 읽기**  
> - **DynamoDB 통합**  
> - **캐시 서비스**

---

| [⬅️ DynamoDB](./DynamoDB.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 3](../README.md) | [DynamoDB Streams ➡️](./DynamoDB-Streams.md) |

---
