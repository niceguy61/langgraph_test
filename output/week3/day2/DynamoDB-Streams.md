---

| [⬅️ DAX](./DAX.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 3](../README.md) | [다음 Day ➡️](../day3/README.md) |

---

> **한 줄 정의:** DynamoDB Streams는 데이터베이스 변경 사항을 실시간으로 추적하고 전달하기 위한 AWS의 **실시간 데이터 처리** 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**  
- **문제 1:** 데이터베이스 변경 사항을 다른 시스템에 실시간으로 전달하지 못해 동기화 지연이 발생합니다. 예를 들어, 사용자의 주문 정보가 DynamoDB에 저장되면, 해당 변경 사항을 결제 시스템이나 로그 시스템에 즉시 전달하지 못해 비효율적인 처리가 발생합니다.  
- **문제 2:** 데이터 변경 이력을 추적하기 위해 별도의 로그 시스템을 구축해야 해서 관리 부담이 큽니다. 이 경우, 로그를 분석해 변경 사항을 추적하는 복잡한 프로세스가 필요합니다.  
- **문제 3:** 실시간 데이터 처리 시점에 다른 시스템과의 통합이 어려워 비즈니스 로직의 일관성을 유지하기 어려웠습니다. 예를 들어, 결제 시스템과 재고 시스템 간의 실시간 동기화가 불가능해 재고 부족이 발생할 수 있습니다.  

**DynamoDB Streams로 해결:**  
- **해결 1:** DynamoDB의 변경 사항(INSERT/UPDATE/DELETE)을 실시간으로 캡처해 다른 시스템에 전달합니다. 예를 들어, 주문 정보가 변경되면 결제 시스템에 즉시 알림을 전송해 처리 지연을 방지합니다.  
- **해결 2:** 변경 사항을 로그 형식으로 저장해 분석 및 이력 관리를 쉽게 합니다. 이는 데이터 무결성 검증이나 감사 트레이일 생성에 활용할 수 있습니다.  
- **해결 3:** AWS Lambda와 같은 서버리스 서비스와 연동해 실시간 처리 로직을 간편하게 구현할 수 있습니다. 예를 들어, 사용자 로그를 실시간으로 분석해 이상 행동 탐지 시스템을 구축할 수 있습니다.  

### 비유로 이해하기  
DynamoDB Streams는 **실시간 채팅 앱의 알림 시스템**으로 생각할 수 있습니다. 사용자가 DynamoDB에 데이터를 입력하면, 이 변경 사항은 즉시 다른 시스템(예: 결제 시스템)으로 전달됩니다. 이처럼 데이터베이스의 변화를 실시간으로 감지하고 전달해 다양한 시스템 간의 협업을 원활하게 만듭니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 실시간 데이터 처리가 필요한 시스템 | SNS를 통해 사용자 알림을 실시간으로 전송하는 서비스 |
| 시나리오 2 | 데이터 이력 관리 및 감사 기능 필요 | 금융 거래 내역을 로그로 저장해 감사 트레이일 생성 |
| 시나리오 3 | 다중 시스템 간 실시간 동기화 | 재고 시스템과 주문 시스템 간 실시간 데이터 동기화 |

**이럴 때 DynamoDB Streams를 선택하세요:**  
- ✅ **실시간 처리가 필수적인 시나리오** (예: 실시간 통계, 알림 시스템)  
- ✅ **데이터 변경 이력을 추적해야 하는 경우** (예: 감사, 로그 분석)  
- ✅ **다중 서비스 간 데이터 동기화가 필요한 경우** (예: 마이크로 서비스 아키텍처)  

**이럴 때는 다른 서비스를 고려하세요:**  
- ❌ **데이터 변경이 빈번하지 않거나 이력 관리가 필요하지 않은 경우** → **DynamoDB 기본 기능** 사용  
- ❌ **복잡한 쿼리가 필요한 데이터 분석** → **Amazon Redshift, Athena**  
- ❌ **고성능 로그 저장이 필요한 경우** → **Amazon CloudWatch Logs, S3**  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **AWS Lambda** | DynamoDB Streams의 변경 사항을 실시간으로 처리 | DynamoDB → Streams → Lambda |
| **Kinesis Data Streams** | 대규모 데이터 전송 및 처리 시 확장성 확보 | DynamoDB → Streams → Kinesis → Lambda |
| **Amazon S3** | 변경 사항을 저장하거나 분석 시스템에 전달 | DynamoDB → Streams → Lambda → S3 |

**자주 사용되는 아키텍처 패턴:**  
```
[사용자] → [애플리케이션] → [DynamoDB] → [DynamoDB Streams] → [AWS Lambda] → [Amazon S3/Redshift]
``

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **데이터 추출** | $0.00025/KB (1000개 이상) | 월 1GB 무료 |
| **데이터 전송** | $0.00025/KB (1000개 이상) | 월 1GB 무료 |
| **모니터링 및 분석** | $0.00035/1,000개 | 12개월 무료 |

**비용 최적화 팁:**  
1. 💡 **On-Demand 모드 사용:** 프로비저닝 용량을 설정하지 않고, 실제 트래픽에 따라 자동 조정해 비용 절감  
2. 💡 **데이터 필터링:** Lambda에서 필요 없는 데이터를 필터링해 전송량을 줄임  
3. 💡 **Kinesis Firehose 활용:** 대량의 데이터를 S3/Redshift로 전송 시 비용 효율성 확보  

> **⚠️ 비용 주의:** 실시간 처리 시 대규모 데이터 변경이 발생하면 과금이 급증할 수 있으므로, **CloudWatch 기능으로 사용량 모니터링** 및 **프리티어 한도 확인**이 필수적입니다.

## 📚 핵심 개념

### 개념 1: DynamoDB Streams의 핵심 개념
DynamoDB Streams은 DynamoDB 테이블에 발생하는 모든 변경 사항(INSERT, UPDATE, DELETE)을 실시간으로 기록하는 기능입니다. 이 기능은 실시간 데이터 처리, 이벤트 기반 아키텍처, 데이터 일관성 유지 등에 활용됩니다. Streams는 변경 사항을 이벤트 형식으로 저장하여, AWS Lambda, Kinesis Data Streams 등과 연동하여 실시간 처리가 가능합니다. 특히, 데이터 무결성을 보장하며, 변경 사항을 병렬적으로 처리할 수 있는 기능을 제공합니다.

#### 왜 중요한가?
- **실시간 데이터 처리**: 데이터 변경이 발생할 때마다 즉시 처리할 수 있어, 실시간 분석이나 이벤트 트래킹이 가능합니다.  
- **데이터 일관성 유지**: 변경 사항을 기록하고, 병렬 처리로 인한 데이터 손실을 방지합니다.  

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **변경 사항 기록** | INSERT, UPDATE, DELETE를 모두 기록하여 이벤트 흐름을 추적합니다. | 사용자가 데이터를 수정할 때, 이 변경 사항이 Stream에 저장됩니다. |
| **이벤트 형식** | JSON 형식으로 변경 사항을 저장하여, Lambda와 같은 서비스에서 쉽게 처리할 수 있습니다. | `{"eventID": "1", "eventName": "MODIFY", ...}` |
| **트래픽 제어** | 데이터 처리량에 따라 Stream의 읽기/쓰기 지연 시간을 조절할 수 있습니다. | 트래픽이 많을 때, `enhanced fanout`을 활성화하여 병렬 처리합니다. |

> **💡 Tip:** Streams는 실시간 처리에 적합하지만, 데이터를 저장하는 용도로는 사용하지 마세요. 데이터 저장은 DynamoDB 테이블 자체에 의존해야 합니다.

---

### 개념 2: Partition Key/Sort Key
Partition Key는 DynamoDB 테이블의 데이터를 분산시키는 데 사용되는 고유한 값으로, 데이터의 분산 방식을 결정합니다. Sort Key는 Partition Key에 해당하는 데이터 내에서 추가적으로 정렬하는 데 사용되며, 데이터를 효율적으로 검색하고 분할할 수 있도록 합니다. 이 두 키는 DynamoDB의 성능 최적화와 데이터 모델링에 핵심적인 역할을 합니다.

#### 작동 원리
1. **Partition Key 결정**: 데이터가 입력될 때, DynamoDB는 Partition Key를 기반으로 데이터를 분산시킵니다. 예를 들어, `user_id`가 Partition Key라면, 모든 사용자 데이터는 해당 `user_id`에 따라 분산됩니다.  
2. **Sort Key 사용**: Partition Key에 해당하는 데이터 내에서 Sort Key를 사용하여 특정 조건에 맞는 데이터를 빠르게 검색합니다. 예를 들어, `order_date`를 Sort Key로 설정하면, 특정 날짜 범위의 주문 데이터를 효율적으로 조회할 수 있습니다.  
3. **데이터 분산 최적화**: Partition Key와 Sort Key를 잘 설계하면, 읽기/쓰기 성능을 극대화하고, 데이터 분산을 최적화할 수 있습니다.  

> **💡 Tip:** Partition Key는 고유성과 분산 효율성을 고려해 설계해야 하며, Sort Key는 주요 검색 조건을 반영해야 합니다.

---

### 개념 3: Capacity Modes (Provisioned/On-Demand)
Capacity Modes는 DynamoDB에서 데이터베이스의 읽기/쓰기 처리량을 관리하는 방식입니다. **Provisioned** 모드는 사전에 처리량을 설정하고, **On-Demand** 모드는 자동으로 처리량을 조절해 스케일링합니다. 이 두 모드는 사용 사례와 비용에 따라 선택해야 합니다.

#### 주요 특징
1. **Provisioned 모드**:  
   - **고정 처리량**: 사용자가 사전에 읽기/쓰기 처리량을 설정합니다.  
   - **비용 예측 가능**: 처리량이 고정되어 비용이 예측 가능하지만, 처리량 초과 시 지연이 발생할 수 있습니다.  
2. **On-Demand 모드**:  
   - **자동 스케일링**: 처리량은 요청량에 따라 자동 조절됩니다.  
   - **비용 최소화**: 처리량이 낮은 경우, 비용이 절감됩니다. 하지만 처리량이 높아질수록 비용이 증가합니다.  
3. **가격 차이**:  
   - **Provisioned**: 처리량에 따라 비용이 책정됩니다.  
   - **On-Demand**: 처리량 기준으로 비용이 책정되며, 처리량이 낮은 경우 더 저렴할 수 있습니다.  

> **💡 Tip:** 처리량이 예측 가능하고, 성능을 최우선으로 고려해야 할 경우 Provisioned 모드를 선택하세요. 반대로 처리량이 불확실하거나 유연한 스케일링이 필요할 경우 On-Demand 모드를 사용하세요.

## 🖥️ AWS 콘솔에서 DynamoDB Streams 사용하기

### Step 1: DynamoDB Streams 서비스 접속  
1. AWS Management Console에 로그인합니다.  
   - URL: https://console.aws.amazon.com  
   - **💡 Tip:** AWS 계정이 없으면 [AWS 신규 계정 생성](https://console.aws.amazon.com/console/home?state=redirect%2F%23%2Fconsole%2Fhome%3FredirectedFrom%3D%2F%23%2Fconsole%2Fhome) 링크를 클릭해 계정을 생성하세요.  
2. 상단 검색창에서 **"DynamoDB Streams"**를 입력하고, 검색 결과에서 **"DynamoDB Streams"**를 클릭합니다.  
3. **"DynamoDB Streams"** 대시보드가 표시되면, **"Create table"** 또는 **"Create global table"** 버튼을 클릭해 테이블을 생성합니다.  

> **📸 화면 확인:**  
> - DynamoDB Streams 대시보드에서 **"Tables"** 탭을 선택해 생성된 테이블 목록을 확인하세요.  
> - **"Stream"** 섹션에서 **"Enabled"** 상태가 활성화된지 확인하세요.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **테이블 생성 및 파티션/정렬 키 설정**  
   - **"Create table"** 버튼을 클릭해 테이블 이름을 입력합니다.  
   - **Primary Key** 섹션에서 **Partition Key**를 선택하고, **Sort Key**를 추가합니다.  
     - 예: `PartitionKey: "userId"`, `SortKey: "timestamp"`  
   - **"Create"** 버튼을 클릭해 테이블을 생성합니다.  

2. **GSI/LSI 설정**  
   - 테이블 생성 후, **"Indexes"** 탭을 선택합니다.  
   - **Global Secondary Index (GSI)** 또는 **Local Secondary Index (LSI)**를 추가합니다.  
     - GSI: 다른 파티션/정렬 키를 기반으로 데이터를 인덱싱합니다.  
     - LSI: 동일한 파티션 키를 기반으로 데이터를 인덱싱합니다.  
   - **"Create index"** 버튼을 클릭해 설정을 완료합니다.  

3. **용량 모드 및 TTL 설정**  
   - **"Capacity mode"** 섹션에서 **Provisioned** 또는 **On-demand** 모드를 선택합니다.  
     - **Provisioned**: 고정된 읽기/쓰기 용량을 설정합니다.  
     - **On-demand**: 자동으로 용량을 조정합니다.  
   - **"Time to Live (TTL)"** 섹션에서 TTL을 설정해 데이터 자동 삭제를 활성화합니다.  
     - 예: `TTL: "expirationTimestamp"`  

> **📸 화면 확인:**  
> - **"Indexes"** 탭에서 GSI/LSI 설정이 완료되었는지 확인하세요.  
> - **"Capacity"** 탭에서 용량 모드 설정이 올바르게 적용되었는지 확인하세요.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **DAX 설정**  
   - **"DAX"** 탭을 선택해 DAX 클러스터를 생성합니다.  
   - **Cluster Name**, **Node Count**, **Subnet Group** 등을 입력합니다.  
   - **"Create"** 버튼을 클릭해 DAX를 활성화합니다.  
   - **💡 Tip:** DAX는 캐싱을 통해 읽기 성능을 향상시키지만, **비용이 발생**합니다. 프리티어는 7일간 750시간까지 제공됩니다.  

2. **글로벌 테이블 설정**  
   - **"Create global table"** 버튼을 클릭해 원본 테이블을 선택합니다.  
   - **"Regions"** 섹션에서 글로벌 테이블을 활성화할 리전을 추가합니다.  
   - **"Create"** 버튼을 클릭해 글로벌 테이블을 생성합니다.  
   - **⚠️ 주의:** 글로벌 테이블은 **데이터 복제**를 수행하므로, **비용이 증가**할 수 있습니다.  

3. **Stream 활성화 및 설정**  
   - 테이블 생성 후, **"Streams"** 탭을 선택합니다.  
   - **"Enable stream"** 버튼을 클릭해 Stream을 활성화합니다.  
   - **"Stream view type"**에서 `NEW_AND_OLD_IMAGES` 또는 `NEW_IMAGE`를 선택합니다.  
   - **"Apply"** 버튼을 클릭해 설정을 저장합니다.  

> **⚠️ 주의:**  
> - **Stream은 데이터 변경 시 실시간으로 로그를 생성**하므로, **데이터 무결성**을 유지해야 합니다.  
> - **DAX와 글로벌 테이블은 고급 기능**으로, **비용이 발생**할 수 있으므로 사용 전 비용 계산을 확인하세요.  

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인 방법**  
   - **"Tables"** 탭에서 생성된 테이블과 GSI/LSI를 확인합니다.  
   - **"DAX"** 탭에서 DAX 클러스터 상태를 확인합니다.  
   - **"Global tables"** 탭에서 글로벌 테이블 상태를 확인합니다.  

2. **상태 확인 방법**  
   - **"Overview"** 탭에서 테이블의 **"Status"**가 `ACTIVE`인지 확인합니다.  
   - **"Stream"** 탭에서 **"Stream status"**가 `ENABLED`인지 확인합니다.  
   - **"DAX"** 탭에서 **"Cluster status"**가 `ACTIVE`인지 확인합니다.  

3. **정상 동작 테스트 방법**  
   - **AWS CLI**로 테이블에 데이터를 삽입합니다.  
     ```bash
     aws dynamodb put-item --table-name MyTable --item '{ "userId": {"S": "123"}, "timestamp": {"N": "20231010"}, "expirationTimestamp": {"N": "20231015"} }'
     ```
   - **"Streams"** 탭에서 실시간으로 데이터 변경이 기록되는지 확인합니다.  
   - **DAX**를 사용하는 경우, **"DAX"** 탭에서 캐시된 데이터를 확인합니다.  

> **📸 화면 확인:**  
> - **"Streams"** 탭에서 **"Stream records"** 섹션에서 데이터 변경이 기록되는지 확인하세요.  
> - **"DAX"** 탭에서 **"Cache"** 섹션에서 캐시된 데이터가 존재하는지 확인하세요.  

---

### ✅ Checklist: DynamoDB Streams 설정 완료 확인  
- [ ] DynamoDB 테이블 생성 및 파티션/정렬 키 설정 완료  
- [ ] GSI/LSI 및 용량 모드 설정 완료  
- [ ] TTL 활성화 및 데이터 자동 삭제 설정 완료  
- [ ] DAX 클러스터 생성 및 설정 완료  
- [ ] 글로벌 테이블 활성화 및 설정 완료  
- [ ] Stream 활성화 및 Stream view type 설정 완료  
- [ ] AWS CLI로 데이터 삽입 및 Stream 기록 확인 완료  
- [ ] DAX 및 글로벌 테이블의 상태 및 비용 확인 완료  

> **💡 Tip:** DynamoDB Streams는 실시간 데이터 처리에 유용하지만, **데이터 무결성**과 **비용 관리**에 주의해야 합니다. AWS 프리티어를 활용해 초기 테스트를 진행하세요.

## ⌨️ AWS CLI로 DynamoDB Streams 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**필요한 권한:**  
DynamoDB Streams를 사용하려면 `dynamodb:DescribeStream`, `dynamodb:ListStreams`, `dynamodb:UpdateTable` 권한이 필요합니다.  
IAM 정책 예시:  
```json
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Action": [
                "dynamodb:DescribeStream",
                "dynamodb:ListStreams",
                "dynamodb:UpdateTable"
            ],
            "Resource": "*"
        }
    ]
}
```

---

### 예제 1: DynamoDB Streams 리소스 조회
```bash
# [DynamoDB Streams 목록 조회]
aws dynamodb list-streams --query 'Streams[].StreamArn' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | Stream ARN 필터링 | `'Streams[].StreamArn'` |
| `--output` | 출력 형식 | `json`, `table`, `text` |

**예상 출력:**
```
| StreamArn                                      |
|------------------------------------------------|
| arn:aws:dynamodb:us-west-2:123456789012:table/ExampleTable/stream/2023-10-01/123456789012 |
```

---

### 예제 2: DynamoDB Streams 활성화 및 설정
```bash
# [DynamoDB Streams 활성화 및 구성]
aws dynamodb update-table \
    --table-name ExampleTable \
    --stream-specification '{"Enabled": true, "StreamViewType": "NEW_AND_OLD_IMAGES"}'
```

**필수 옵션:**
- `--table-name`: 테이블 이름
- `--stream-specification`: JSON 형식의 Stream 설정  
  - `Enabled`: true/false (스트림 활성화 여부)  
  - `StreamViewType`: `NEW_IMAGE`, `OLD_IMAGE`, `NEW_AND_OLD_IMAGES` 중 선택

**예상 출력:**
```json
{
    "TableDescription": {
        "StreamSpecification": {
            "StreamViewType": "NEW_AND_OLD_IMAGES",
            "Enabled": true
        }
    }
}
```

---

### 예제 3: DynamoDB Streams 상태 확인
```bash
# [DynamoDB Streams 상세 정보 조회]
aws dynamodb describe-stream \
    --stream-arn arn:aws:dynamodb:us-west-2:123456789012:table/ExampleTable/stream/2023-10-01/123456789012 \
    --query 'StreamDescription.StreamArn' --output text
```

**출력 예시:**
```
arn:aws:dynamodb:us-west-2:123456789012:table/ExampleTable/stream/2023-10-01/123456789012
```

---

### 예제 4: DynamoDB Streams 비활성화
```bash
# [DynamoDB Streams 비활성화]
aws dynamodb update-table \
    --table-name ExampleTable \
    --stream-specification '{"Enabled": false}'
```

**주의사항:**  
- Stream 비활성화 시 실시간 데이터 수집이 중단됩니다.  
- 데이터 손실 위험 없이도, 실시간 처리가 필요한 시스템에 영향을 줄 수 있습니다.

---

### 자주 사용하는 명령어 정리
```bash
# 스트림 목록 조회
aws dynamodb list-streams

# 스트림 상세 정보 조회
aws dynamodb describe-stream --stream-arn <STREAM_ARN>

# 스트림 활성화
aws dynamodb update-table --table-name <TABLE_NAME> --stream-specification '{"Enabled": true}'

# 스트림 비활성화
aws dynamodb update-table --table-name <TABLE_NAME> --stream-specification '{"Enabled": false}'
```

---

### 🔍 추가 실습 예제: Stream 데이터 구독
```bash
# [Kinesis Data Streams로 DynamoDB Stream 데이터 전송]
aws kinesis put-record \
    --stream-name myStream \
    --partition-key 1 \
    --data '{"dynamodb": {"OldImage": {}, "NewImage": {"id": {"S": "123"}, "name": {"S": "Test"}}}}'
```

**설명:**  
- DynamoDB Stream은 Kinesis Data Streams로 데이터를 전송할 수 있습니다.  
- `put-record` 명령어로 실시간 데이터를 전송하고, Kinesis Consumer로 처리할 수 있습니다.

---

> **⚠️ 주의:**  
> - DynamoDB Stream은 테이블 수준에서 활성화되며, 직접 생성/삭제할 수 없습니다.  
> - StreamViewType을 `NEW_AND_OLD_IMAGES`로 설정하면, 모든 변경 사항을 기록합니다.  
> - 프리티어 사용 시, DynamoDB Stream의 데이터 처리량 제한을 초과하지 않도록 주의하세요.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 DynamoDB Streams 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **DynamoDB Streams의 주요 목적**  
   - 설명: DynamoDB Streams는 테이블 변경 사항(INSERT, UPDATE, DELETE)을 실시간으로 기록하여 외부 시스템과 연동하는 기능입니다. 시험에서 데이터 일관성, 실시간 처리, 이벤트 기반 아키텍처 설계와 관련된 문제로 자주 출제됩니다.  
   - 키워드: `real-time processing`, `change data capture`, `event-driven architecture`

2. **Streams의 사용 시나리오**  
   - 설명: 실시간 분석, 데이터 복제, 이벤트 트리거(예: Lambda 함수 연동), 로그 수집 등이 주요 사용 사례입니다. 시험에서는 "왜 이 서비스를 선택해야 하는가"라는 유형의 문제에 자주 등장합니다.  
   - 키워드: `event sourcing`, `data replication`, `Lambda integration`

3. **Streams가 기록하는 데이터 타입**  
   - 설명: 모든 변경 사항(ROW LEVEL)을 세부적으로 기록하며, 이는 데이터 분석 및 복구 시 중요한 정보입니다. 시험에서 Streams의 데이터 세부성과 관련된 문제로 자주 출제됩니다.  
   - 키워드: `ROW LEVEL`, `data granularity`, `change details`

4. **Streams의 제한 사항 및 비용**  
   - 설명: 데이터 보존 기간(7일), 데이터 크기 제한(1MB), 및 데이터 전송 비용이 중요한 시험 포인트입니다. 비용 구조와 제한 사항을 이해해야 시험에서 실수를 줄일 수 있습니다.  
   - 키워드: `retention period`, `cost structure`, `data size limit`

5. **Streams vs Kinesis/Lambda의 차이점**  
   - 설명: DynamoDB Streams는 DynamoDB와 직접 연동하는 반면, Kinesis는 독립적인 데이터 스트리밍 서비스입니다. Lambda는 Streams와 연동하여 실시간 처리를 수행합니다. 시험에서는 서비스의 기능 구분과 적절한 사용 사례를 묻는 문제가 많습니다.  
   - 키워드: `Kinesis integration`, `Lambda event source`, `service coupling`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "DynamoDB Streams는 장기 저장용 데이터 저장소입니다"라고 오해하는 경우 | DynamoDB Streams는 실시간 처리용 이벤트 기록이며, 장기 저장은 S3 또는 Glacier에서 처리합니다 |
| 함정 2 | Streams의 데이터를 실시간으로 복제하는 데 사용되는 서비스를 Kinesis로 오인하는 경우 | Streams는 DynamoDB와 직접 연동하며, Kinesis는 별도의 스트리밍 서비스입니다 |
| 함정 3 | Streams의 데이터 보존 기간을 30일로 오인하는 경우 | Streams의 기본 보존 기간은 7일이며, 최대 35일까지 설정 가능합니다 |

#### 🔄 DynamoDB Streams vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | DynamoDB Streams | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | DynamoDB 변경 사항 실시간 기록 | Kinesis: 데이터 스트리밍, Lambda: 서버리스 함수 처리 | 실시간 데이터 처리가 필요한 경우 |
| 확장성 | DynamoDB 테이블 크기와 동기화됨 | Kinesis: 고성능 스트리밍, Lambda: 서버리스 확장 | 데이터 소스가 DynamoDB일 경우 |
| 비용 | 데이터 전송 및 저장 비용 발생 | Kinesis: 스트리밍 비용, Lambda: 실행 시간에 따라 비용 | 데이터 복잡도와 처리 요구사항 |
| 지연시간 | 실시간 처리 (수초 단위) | Kinesis: 수초~수십초, Lambda: 수초 | 실시간 분석이 필요한 경우 |

#### 📝 시험 대비 체크리스트
- [ ] DynamoDB Streams의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  > "DynamoDB의 변경 사항을 실시간으로 기록하여 외부 시스템과 연동하는 기능"
- [ ] DynamoDB Streams를 선택해야 하는 시나리오를 알고 있는가?  
  > 실시간 분석, 이벤트 트리거, 데이터 복제 등
- [ ] DynamoDB Streams의 제한사항/한계를 알고 있는가?  
  > 7일 보존 기간, 1MB 데이터 크기 제한, 데이터 전송 비용
- [ ] DynamoDB Streams와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  > Kinesis는 독립적 스트리밍, Lambda는 서버리스 함수 처리
- [ ] DynamoDB Streams의 비용 구조를 이해하고 있는가?  
  > 데이터 전송, 저장, 처리에 따른 비용 발생

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 DynamoDB Streams를 떠올리세요:  
> - **real-time processing**  
> - **event sourcing**  
> - **change data capture**  
> - **Lambda integration**  
> - **data replication**

---

| [⬅️ DAX](./DAX.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 3](../README.md) | [다음 Day ➡️](../day3/README.md) |

---
