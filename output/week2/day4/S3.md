---

| [⬅️ 이전 Day](../day3/README.md) | [📑 Day 4 목차](./README.md) | [🏠 Week 2](../README.md) | [S3 Glacier ➡️](./S3-Glacier.md) |

---

# S3 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** S3는 데이터 저장 및 관리를 위한 AWS의 객체 저장소 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- 문제 1: 전통적인 서버 기반 저장소는 확장성 한계로 인해 대규모 데이터 저장이 어려웠습니다. 예를 들어, 서버 공간이 부족해 데이터를 추가로 저장할 수 없거나, 서버 복제를 통해 확장해야 했습니다.
- 문제 2: 데이터 보안 및 접근 제어를 위한 별도의 시스템을 구축해야 했고, 이로 인해 관리 비용이 증가했습니다. 예를 들어, 파일 공유 시 매번 권한을 수동으로 설정해야 했습니다.
- 문제 3: 대량의 데이터 저장 시 비용이 급격히 증가했고, 스토리지 공간의 효율적인 관리가 어려웠습니다. 예를 들어, 삭제된 파일은 공간을 차지하지만 복구할 수 없었습니다.

**S3로 해결:**
- 해결 1: S3는 확장성 있는 클라우드 기반 저장소로, 데이터 저장 용량을 제한 없이 확장할 수 있습니다. 예를 들어, 10PB(10,000,000GB)까지 저장할 수 있습니다.
- 해결 2: S3는 ACL, 버킷 정책, 암호화 기능 등을 통해 데이터 보안 및 접근 제어를 강화합니다. 예를 들어, 특정 사용자만 파일을 읽을 수 있도록 설정할 수 있습니다.
- 해결 3: S3는 Standard, Infrequent Access, Glacier 등 다양한 스토리지 레이어를 제공해 데이터 유형에 따라 최적화된 비용을 적용합니다. 예를 들어, 희소하게 접근하는 데이터는 Glacier로 이동해 비용을 절감할 수 있습니다.

### 비유로 이해하기
S3는 클라우드 기반의 "가상 서랍장"입니다. 집안의 문서를 물리적으로 보관하는 대신, 집 밖에 있는 클라우드 서랍장에 문서를 저장하면 언제든지 접근할 수 있고, 필요할 때만 가져다 쓸 수 있습니다. 또한, 다른 가족원이 문서를 볼 수 있도록 권한을 설정하거나, 중요한 문서는 암호화하여 보관할 수 있습니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 정적 웹사이트 호스팅 | AWS에 정적 HTML, CSS, JS 파일을 저장해 웹사이트 배포 |
| 시나리오 2 | 백업 및 아카이브 | 데이터베이스 백업 또는 장기 보관용 파일 저장 |
| 시나리오 3 | 미디어 스토리지 | 영상, 음성, 이미지 등 대용량 데이터 저장 |

**이럴 때 S3를 선택하세요:**
- ✅ 상황 1: 대량의 정적 파일을 저장하고 빠르게 접근해야 할 때
- ✅ 상황 2: 데이터 복구 및 장기 보관이 필요한 경우
- ✅ 상황 3: 미디어 파일을 저장하고 전송 효율을 높이고자 할 때

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ 상황 → 실시간 데이터 처리 → **Lambda**: 데이터를 처리하기 위해 Lambda와 연동
- ❌ 상황 → 구조화된 데이터 저장 → **RDS**: 관계형 데이터베이스로 저장해야 할 경우
- ❌ 상황 → 저지연 접근 필요 → **EFS**: 고성능 파일 저장소로 요구사항이 맞는 경우

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **CloudFront** | CDN을 통해 S3에 저장된 데이터를 빠르게 전송 | 예: 사용자 → CloudFront → S3 |
| **Lambda** | S3 이벤트 트리거로 파일 처리 | 예: S3 → Lambda → 데이터 가공 |
| **Glacier** | 장기 보관용 데이터를 저비용으로 저장 | 예: S3 → Glacier → 아카이브 |

**자주 사용되는 아키텍처 패턴:**
```
User → CloudFront → S3
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **Standard Storage** | $0.023/GB | 월 5GB 무료 |
| **Requester Charges** | $0.004/GB | 12개월 무료 |
| **Glacier Storage** | $0.004/GB | 항상 무료 |

**비용 최적화 팁:**
1. 💡 팁 1: **S3 Glacier** 또는 **S3 One Zone-Infrequent Access**를 사용해 희소하게 접근하는 데이터의 비용을 절감
2. 💡 팁 2: **버전 관리**를 활성화해 실수로 삭제된 파일을 복구할 수 있도록 설정
3. 💡 팁 3: **리프라이프 정책**을 설정해 데이터를 저비용 스토리지 레이어로 자동 이동

> **⚠️ 비용 주의:** 데이터 전송 비용이 발생할 수 있으며, S3 Transfer Acceleration을 사용할 경우 추가 비용이 발생할 수 있습니다. 실시간 데이터 처리 시 Lambda와 연동해 비용을 절감해야 합니다.

## 📚 핵심 개념

### 개념 1: 버킷 정책 (Bucket Policy)
S3 버킷 정책은 특정 버킷 또는 객체에 대한 접근 권한을 제어하는 JSON 형식의 규칙입니다. 이 정책은 AWS Identity and Access Management (IAM) 역할, 사용자, 또는 외부 서비스(예: Lambda, CloudFront)에 적용되며, 데이터 보안과 접근 제어를 강화합니다. 버킷 정책은 AWS 서비스 간의 통신을 제한하거나 특정 IP 범위에서만 접근을 허용할 수 있습니다.  
#### 왜 중요한가?
- **보안 강화**: 민감한 데이터에 대한 접근 권한을 정밀하게 제어하여 해킹이나 실수로 인한 데이터 유출을 방지합니다.  
- **지정된 사용자만 접근**: 외부 파트너나 특정 애플리케이션에만 특정 객체 접근 권한을 부여해 데이터를 보호합니다.  

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **Principal** | 권한을 부여받는 사용자 또는 역할 (예: `arn:aws:iam::123456789012:user/John`) | `Principal": "arn:aws:iam::123456789012:user/John"` |
| **Effect** | 접근 허용(`Allow`) 또는 거부(`Deny`) | `"Effect": "Allow"` |
| **Resource** | 적용할 버킷 또는 객체의 ARN | `"Resource": "arn:aws:s3:::example-bucket/*"` |

> **💡 Tip:** 버킷 정책은 IAM 정책과 병행해 사용할 경우, 정책 우선순위에 따라 접근 권한이 결정됩니다. 실제 배포 전에는 AWS Policy Simulator를 활용해 권한 검증이 필요합니다.

---

### 개념 2: ACL (Access Control List)
ACL은 S3 버킷 및 객체에 대한 접근 권한을 관리하는 기능으로, IAM 정책과 병행해 사용됩니다. ACL은 `public-read`, `private` 등 기본 권한을 제공하며, 특정 사용자 또는 그룹에게 개별 권한을 부여할 수 있습니다.  
#### 작동 원리
1. **권한 정의**: `public-read`는 모든 사용자가 읽을 수 있지만 쓰기 권한은 없고, `private`은 모든 사용자에게 접근 권한이 없어집니다.  
2. **권한 적용**: 버킷 생성 시 기본 ACL을 설정하거나, 객체 업로드 시 ACL을 명시적으로 지정합니다.  
3. **확인 및 수정**: AWS 콘솔에서 버킷 설정 > **Permissions** 탭에서 ACL을 확인하고 수정할 수 있습니다.  

> **💡 Tip:** ACL은 IAM 정책보다 낮은 우선순위로 작동하므로, 권한 충돌이 발생할 수 있습니다. 민감한 데이터는 IAM 정책으로 관리하는 것이 안전합니다.

---

### 개념 3: SSE-KMS 암호화
SSE-KMS는 AWS Key Management Service(KMS)를 사용해 S3 객체를 암호화하는 기능입니다. 데이터가 저장 시 복호화 키가 KMS에 저장되어 있어, 데이터 유출 시에도 암호화 상태로 남아 안전성을 높입니다. 이 기능은 GDPR, HIPAA 등 규제 준수를 요구하는 업무에서 필수적입니다.  
#### 주요 특징
1. **암호화 키 관리**: KMS에서 생성된 키를 사용해 데이터를 암호화하고, 키는 AWS 관리되며 사용자에게 암호화 키의 복제 권한이 없습니다.  
2. **자동 복호화**: 데이터를 읽을 때 KMS가 키를 자동으로 제공해 복호화를 수행합니다.  
3. **보안성 및 규제 준수**: 데이터가 항상 암호화 상태로 저장되며, 키의 접근 권한을 IAM 정책으로 제어해 보안을 강화합니다.  

> **💡 Tip:** SSE-KMS는 데이터 유출 시에도 암호화 상태로 남아 데이터 보호에 효과적입니다. 그러나 키 관리 비용이 발생하므로, 예산을 고려해 사용해야 합니다.

## 🖥️ AWS 콘솔에서 S3 사용하기

### Step 1: S3 서비스 접속  
1. AWS Management Console에 로그인합니다.  
   - URL: https://console.aws.amazon.com  
   - 로그인 후 `Services` 메뉴에서 `S3`를 검색합니다.  
2. 검색 결과에서 `S3`를 클릭하여 대시보드에 접근합니다.  
   - **버킷 목록**과 **버킷 생성** 버튼이 표시되면 정상입니다.  

> **📸 화면 확인:** S3 대시보드에서 버킷 목록이 표시되고, `Create bucket` 버튼이 보이는지 확인하세요.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
**목표:** S3 버킷 생성 및 기본 설정  
1. **버킷 생성**  
   - `Create bucket` 버튼을 클릭합니다.  
   - **버킷 이름**: `my-unique-bucket-name` (AWS 내 유일해야 함)  
   - **Region**: `us-east-1` (미국 동부) 또는 원하는 지역 선택  
   - **버킷 정책**: `Block public access` 설정 시 자동으로 생성됩니다.  

2. **암호화 설정**  
   - `Create bucket` 화면에서 `Encryption` 섹션에서 **SSE-S3** 또는 **SSE-KMS** 선택.  
     - **SSE-S3**: AWS 관리 암호화 (기본값)  
     - **SSE-KMS**: AWS KMS 키 관리 서비스 사용 (추가 비용 발생)  
   - **SSE-C**: 고객 관리 암호화 (클라이언트가 키를 제공해야 함)  

3. **확인 사항**  
   - 버킷 생성 후 `Bucket settings`에서 `Permissions` 탭을 통해 ACL 및 정책을 확인할 수 있습니다.  

> **📸 화면 확인:** `Create bucket` 화면에서 `Encryption` 섹션에 암호화 유형이 선택되었는지 확인하세요.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
**목표:** 버킷 정책, ACL, 버전 관리, MFA Delete, Object Lock 설정  
1. **버킷 정책 수정**  
   - `Bucket settings` → `Permissions` → `Bucket Policy` 클릭.  
   - JSON 형식으로 정책을 직접 입력하거나, AWS Managed Policies를 선택.  
   - 예시:  
     ```json
     {
       "Version": "2012-10-17",
       "Statement": [
         {
           "Sid": "AllowPublicRead",
           "Effect": "Allow",
           "Principal": "*",
           "Action": "s3:GetObject",
           "Resource": "arn:aws:s3:::my-unique-bucket-name/*"
         }
       ]
     }
     ```  

2. **ACL 설정**  
   - `Permissions` 탭에서 `Access control` 설정.  
   - **Bucket Policy** 또는 **Object ACL**을 통해 권한을 관리할 수 있습니다.  

3. **버전 관리 활성화**  
   - `Bucket settings` → `Management` → `Versioning` → `Enable` 클릭.  
   - 버전 관리로 인해 객체 수정/삭제 시 이력이 저장됩니다.  

4. **MFA Delete 설정**  
   - `Management` 탭에서 `MFA Delete` → `Enable` 클릭.  
   - **주의:** MFA를 사용해야 삭제가 가능합니다.  

5. **Object Lock 설정**  
   - `Management` 탭에서 `Object Lock` → `Enable` 클릭.  
   - **Object Lock mode**: `Governance` 또는 `Compliance` 선택.  
   - Object Lock은 삭제 및 수정을 방지합니다.  

> **⚠️ 주의:** MFA Delete 및 Object Lock은 활성화 후 역설정이 어렵습니다. 사전에 MFA를 설정해 두세요.  

---

### Step 4: 설정 확인 및 테스트  
1. **버킷 생성 확인**  
   - S3 대시보드에서 생성한 버킷 이름을 클릭해 `Properties` 탭에서 설정을 확인합니다.  
   - 암호화 유형, 버전 관리, MFA Delete, Object Lock 상태를 확인하세요.  

2. **상태 확인 방법**  
   - `Permissions` 탭에서 ACL 및 Bucket Policy가 정상적으로 적용되었는지 확인합니다.  
   - `Versioning` 섹션에서 버전 관리 상태를 확인합니다.  

3. **정상 동작 테스트**  
   - 파일 업로드 후 `Properties` → `Metadata`에서 암호화 상태 확인.  
   - 권한이 제한된 경우 `Access denied` 오류가 발생하며, 정책을 수정해야 합니다.  

> **💡 Tip:** AWS Free Tier는 1년간 5GB 저장 공간, 20GB 데이터 전송, 200,000 요청 횟수를 제공합니다. S3 사용 시 비용을 점검하세요.  

---

### ✅ 체크리스트  
- [ ] S3 서비스 접속 확인  
- [ ] 버킷 생성 및 암호화 설정 완료  
- [ ] 버킷 정책 및 ACL 설정 완료  
- [ ] 버전 관리, MFA Delete, Object Lock 활성화  
- [ ] 설정이 정상적으로 적용되었는지 확인  
- [ ] 테스트 파일 업로드 및 암호화 확인

## ⌨️ AWS CLI로 S3 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

> **💡 Tip:** CLI 버전이 1.20 이상이고, IAM 자격 증명이 정상적으로 설정되어야 S3 명령어가 작동합니다. `aws configure`로 기본 리전을 확인하고, 필요 시 `aws configure set region`으로 리전을 변경할 수 있습니다.

---

### 예제 1: S3 리소스 조회
```bash
# S3 버킷 목록 조회
aws s3api list-buckets --query 'Buckets[].Name' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query 'Buckets[].Name'` | 버킷 이름 필터링 | `Buckets[].Name` |
| `--output table` | 테이블 형식으로 출력 | `table` |

**예상 출력:**
```
|       Name        |
+-------------------+
| example-bucket-1 |
| example-bucket-2 |
```

> **⚠️ 주의:** `list-buckets`는 전체 버킷 목록을 반환하지만, `list-objects`는 특정 버킷 내 객체를 조회할 수 있습니다.

---

### 예제 2: S3 리소스 생성
```bash
# S3 버킷 생성 (암호화 및 정책 설정)
aws s3api create-bucket \
    --bucket my-encrypted-bucket \
    --region ap-northeast-2 \
    --acl private \
    --server-side-encryption CustomerManaged \
    --server-side-encryption-KMS-Key-id arn:aws:kms:ap-northeast-2:123456789012:key/abcd1234-a123-4567-8abc-def123456789
```

**필수 옵션:**
- `--bucket`: 생성할 버킷 이름
- `--region`: 리전 지정 (생략 시 기본 리전 사용)
- `--acl`: 액세스 제어 목록 (public/private/default)
- `--server-side-encryption`: SSE-S3, SSE-KMS, SSE-C 설정

**예상 출력:**
```json
{
    "Location": "https://my-encrypted-bucket.s3.ap-northeast-2.amazonaws.com/"
}
```

> **💡 Tip:** `--server-side-encryption`을 사용할 때 `--server-side-encryption-KMS-Key-id`를 반드시 지정해야 합니다. SSE-C는 클라이언트가 암호화 키를 제공해야 합니다.

---

### 예제 3: S3 리소스 수정
```bash
# 버킷 정책 업데이트
aws s3api put-bucket-policy \
    --bucket my-encrypted-bucket \
    --policy file://policy.json
```

**필요 파일 (policy.json):**
```json
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Sid": "AllowPublicRead",
            "Effect": "Allow",
            "Principal": "*",
            "Action": "s3:GetObject",
            "Resource": "arn:aws:s3:::my-encrypted-bucket/*"
        }
    ]
}
```

**출력 예시:**
```
{
    "ResponseMetadata": {
        "RequestId": "ABC123XYZ",
        "HTTPStatusCode": 200
    }
}
```

> **⚠️ 주의:** 정책 수정 시 기존 권한이 중복될 수 있으므로, 변경 전 정책을 `get-bucket-policy`로 확인하세요.

---

### 예제 4: S3 리소스 삭제
```bash
# 버킷 삭제 (사전에 객체 모두 삭제해야 함)
aws s3api delete-bucket --bucket my-encrypted-bucket

# 삭제 확인
aws s3api get-bucket-location --bucket my-encrypted-bucket
```

> **⚠️ 주의:** 버킷 삭제 시 내부 객체가 모두 삭제되어야 합니다. `aws s3 rm s3://my-encrypted-bucket/* --recursive`로 객체를 사전에 삭제하세요.

---

### 자주 사용하는 명령어 정리
```bash
# 버킷 조회
aws s3api list-buckets
aws s3api get-bucket-location --bucket my-bucket

# 버킷 생성
aws s3api create-bucket --bucket my-bucket --region ap-northeast-2

# 버킷 정책 관리
aws s3api get-bucket-policy --bucket my-bucket
aws s3api put-bucket-policy --bucket my-bucket --policy file://policy.json

# 객체 관리
aws s3api put-object --bucket my-bucket --key test.txt --body test.txt
aws s3api delete-object --bucket my-bucket --key test.txt
```

> **💡 Tip:** `--recursive` 옵션으로 디렉토리 전체 삭제 가능 (`aws s3 rm s3://my-bucket/ --recursive`).

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 S3 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **버킷 정책(Bucket Policy)**  
   - 설명: S3 버킷에 대한 접근 제어를 명시적으로 정의하는 정책으로, IAM 정책과 차별화되며 시험에서 자주 비교문제로 출제됩니다. AWS 자동화 도구와 연동 시 반드시 설정해야 하는 요소입니다.  
   - 키워드: `버킷 정책`, `IAM 정책`, `명시적 접근 제어`

2. **ACL(Access Control List)**  
   - 설명: S3 객체 및 버킷에 대한 접근 권한을 설정하는 기능으로, 버킷 정책과 함께 시험에서 자주 출제됩니다. ACL은 "공개" 권한을 설정할 수 있는 유일한 방법으로, 보안 위험을 줄이기 위해 권장되지 않습니다.  
   - 키워드: `ACL`, `공개 권한`, `보안 위험`

3. **SSE-KMS 암호화**  
   - 설명: AWS KMS 키를 사용한 암호화로, S3 데이터 보호의 핵심 기능입니다. 시험에서는 SSE-S3, SSE-KMS, SSE-C의 차이점과 사용 시나리오를 묻는 문제가 자주 출제됩니다.  
   - 키워드: `SSE-KMS`, `KMS 키`, `데이터 보호`

4. **버전 관리(Versioning)**  
   - 설명: 동일한 파일명으로 덮어쓰기 방지를 위한 기능으로, 백업 및 오류 회복 시 필수적입니다. 시험에서는 버전 관리가 활성화된 버킷에서의 삭제 및 복구 과정을 묻는 문제가 자주 등장합니다.  
   - 키워드: `버전 관리`, `덮어쓰기 방지`, `백업`

5. **MFA Delete**  
   - 설명: 삭제 작업 시 MFA 인증을 강제하는 기능으로, 실수로 데이터를 삭제하는 것을 방지합니다. 시험에서는 MFA Delete 활성화 시 필요한 조건과 제외되는 경우를 묻는 문제가 흔합니다.  
   - 키워드: `MFA Delete`, `삭제 보호`, `인증 요구`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | 버킷 정책 vs IAM 정책의 차이를 혼동해 문제 풀이에 오류가 발생 | 버킷 정책은 S3 자원에 직접 적용되며, IAM 정책은 사용자/그룹에 적용됨 |
| 함정 2 | SSE-KMS와 SSE-S3의 구현 방식을 혼동해 보안 수준을 오인 | SSE-KMS는 KMS 키를 사용해 암호화되며, SSE-S3는 AWS 관리 키를 사용 |
| 함정 3 | 버전 관리 활성화 시 삭제 시점의 데이터 보존 기간을 오인 | 버전 관리가 활성화된 버킷에서 삭제 시 해당 버전은 "삭제 버전"으로 남아 있음 |

#### 🔄 S3 vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | S3 | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| **용도** | 대용량 데이터 저장 및 배포 | S3 Glacier(보관), S3 One Zone(단일 존) | 데이터 접근 빈도에 따라 선택 |
| **확장성** | 가상 서버 없이 빠른 확장 가능 | S3 Glacier는 데이터 복구 시간이 길어짐 | 실시간 액세스 필요 시 S3, 비실시간 접근 시 S3 Glacier |
| **비용** | 스토리지 + 데이터 전송 비용 | S3 Glacier는 저장 비용이 저렴하나 복구 비용이 높음 | 데이터 저장 기간과 접근 빈도에 따라 선택 |
| **지연시간** | 실시간 액세스 가능 | S3 Glacier는 복구 시간 1~5시간 필요 | 즉시 접근이 필요한 경우 S3, 장기 보관 시 S3 Glacier |

#### 📝 시험 대비 체크리스트
- [ ] S3의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  → "대용량 데이터 저장 및 분산 배포에 최적화된 스토리지 서비스"  
- [ ] S3를 선택해야 하는 시나리오를 알고 있는가?  
  → "실시간 액세스, 백업, 대용량 데이터 저장"  
- [ ] S3의 제한사항/한계를 알고 있는가?  
  → "최대 5TB 단일 객체, 데이터 복구 시간, 데이터 손실 위험"  
- [ ] S3와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  → "S3 Glacier는 저장 비용이 저렴하지만 복구 시간이 길고, S3 One Zone은 단일 존에 저장하여 보안 위험을 증가시킴"  
- [ ] S3의 비용 구조를 이해하고 있는가?  
  → "스토리지 비용 + 데이터 전송 비용 + 요청 비용 + 암호화 비용"  

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 S3를 떠올리세요:  
> - **버킷 정책**  
> - **ACL**  
> - **SSE-KMS**  
> - **버전 관리**  
> - **MFA Delete**

---

| [⬅️ 이전 Day](../day3/README.md) | [📑 Day 4 목차](./README.md) | [🏠 Week 2](../README.md) | [S3 Glacier ➡️](./S3-Glacier.md) |

---
