---

| [⬅️ Internet Gateway](./Internet-Gateway.md) | [📑 Day 1 목차](./README.md) | [🏠 Week 2](../README.md) | [다음 Day ➡️](../day2/README.md) |

---

> **한 줄 정의:** NAT Gateway는 VPC 내에서 인터넷에 연결된 서버가 외부 리소스에 접근할 수 있도록 하는 AWS의 네트워크 전환 게이트웨이 서비스입니다.

### 이 서비스가 해결하는 문제  
**기존의 문제점:**  
- **문제 1:** 프라이빗 서브넷에 있는 EC2 인스턴스는 외부 인터넷에 접근할 수 없어 데이터베이스 업데이트나 소프트웨어 업데이트가 불가능했습니다. 기존에는 직접 NAT 인스턴스를 생성해 IP 전환을 수행해야 했으며, 관리 부담이 컸습니다.  
- **문제 2:** NAT 인스턴스는 단일 포인트 of failure로, 인스턴스가 다운되면 외부 접근이 중단되어 가용성이 낮았습니다. 또한, 트래픽 증가 시 확장성이 제한적이었습니다.  
- **문제 3:** NAT 인스턴스의 IP 주소가 고정되지 않아, 외부 서버에서 요청을 정확히 처리하기 어려웠습니다. 예를 들어, 외부 서버가 특정 IP를 기대해 요청을 보내면 NAT 인스턴스의 IP 변경으로 인해 연결이 끊겨졌습니다.  

**NAT Gateway로 해결:**  
- **해결 1:** AWS가 관리하는 고가용성 NAT 게이트웨이를 통해 프라이빗 서브넷의 인스턴스가 외부 리소스에 접근할 수 있도록 합니다. NAT 인스턴스 대신 사용해 관리 부담을 줄였습니다.  
- **해결 2:** 자동으로 트래픽을 분산해 확장성과 가용성을 보장합니다. 단일 NAT 게이트웨이가 다운되면 자동으로 다른 인스턴스로 트래픽이 전환되어 서비스 중단이 방지됩니다.  
- **해결 3:** 고정 IP 주소를 제공해 외부 서버와의 정확한 통신이 가능합니다. 예를 들어, 외부 서버가 특정 IP를 기대해 요청을 보내면 NAT 게이트웨이가 그 IP를 통해 요청을 처리할 수 있습니다.  

### 비유로 이해하기  
**집안에서 외부와 통신하는 비유:**  
NAT 게이트웨이는 집안에 있는 사람에게 외부와 통신할 수 있는 '문' 역할을 합니다. 집안에 있는 컴퓨터(프라이빗 서브넷)는 직접 외부로 나가지 못하므로, 이 문을 통해 외부 인터넷에 접근합니다. 이 문은 고정된 주소를 가지고 있어, 외부에서 집안 컴퓨터를 정확히 찾을 수 있습니다. 또한, 이 문은 항상 열려 있어 가용성이 높고, 필요한 만큼 확장할 수 있습니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)  

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 프라이빗 서브넷에 있는 EC2 인스턴스가 외부 인터넷에 접근해야 할 때 | 기업 내부 서버가 외부 소프트웨어 업데이트를 받거나, 외부 API에 요청하는 경우 |
| 시나리오 2 | VPC 내에서 외부 데이터 저장소(예: S3)에 접근해야 할 때 | 데이터베이스 서버가 외부에 저장된 백업 파일을 다운로드해야 하는 경우 |
| 시나리오 3 | 다중 서버가 외부 인터넷에 동시 접근해야 할 때 | 마이크로서비스 아키텍처에서 각 서버가 외부 서비스에 요청을 보내는 경우 |

**이럴 때 NAT Gateway를 선택하세요:**  
- ✅ **프라이빗 서브넷의 인스턴스가 외부 리소스에 접근해야 할 때**  
- ✅ **고가용성과 확장성이 필요한 외부 접근 시나리오**  
- ✅ **고정 IP 주소를 통해 외부 서버와 정확한 통신이 필요한 경우**  

**이럴 때는 다른 서비스를 고려하세요:**  
- ❌ **단일 인스턴스로 충분한 트래픽량이 작은 경우** → [NAT 인스턴스](https://aws.amazon.com/ko/ec2/nat-instance/) 사용 (관리 유연성)  
- ❌ **외부 인터넷 접근이 필요하지 않지만, 내부 서버 간 통신이 필요한 경우** → [VPC 라우팅 테이블](https://docs.aws.amazon.com/ko-kr/eks/latest/userguide/vpc.html) 및 [VPC Endpoints](https://aws.amazon.com/ko/vpc/endpoints/) 고려  

---

## 🔗 연관 서비스 (Used Together With)  

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **VPC** | NAT Gateway는 VPC 내에서 작동하므로 필수적으로 연결됩니다 | 프라이빗 서브넷 → NAT Gateway → 인터넷 |
| **Subnet** | NAT Gateway는 특정 서브넷에 연결되어 외부 트래픽을 처리합니다 | 프라이빗 서브넷 → NAT Gateway → 인터넷 게이트웨이 |
| **Internet Gateway** | NAT Gateway는 인터넷 게이트웨이를 통해 외부 리소스에 접근합니다 | NAT Gateway → 인터넷 게이트웨이 → 외부 리소스 |
| **Route Table** | 라우팅 테이블을 통해 NAT Gateway의 IP 주소를 외부 트래픽에 지정합니다 | 프라이빗 서브넷 라우팅 테이블 → NAT Gateway IP |

**자주 사용되는 아키텍처 패턴:**  
```
[사용자] → [CloudFront] → [S3]  
[사용자] → [EC2 (프라이빗 서브넷)] → [NAT Gateway] → [인터넷 게이트웨이] → [외부 리소스]  
```  

---

## 💰 비용 구조 (Pricing)  

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **NAT Gateway 사용량** | $0.015/GB (데이터 전송량 기준) | 12개월 무료 (최대 10GB/월) |
| **NAT Gateway 생성 및 관리** | $0.01/시간 (NAT Gateway 운영) | 12개월 무료 (1개 NAT Gateway) |
| **NAT Gateway IP 주소** | $0.01/월 (고정 IP 주소) | 12개월 무료 (1개 IP 주소) |

**비용 최적화 팁:**  
1. 💡 **프리티어를 활용해 초기 사용량을 최대한 활용하세요** (월 10GB 이내 데이터 전송 시 무료).  
2. 💡 **트래픽 패턴을 분석해 필요한 시점에 NAT Gateway를 생성/삭제하세요** (예: 작업 완료 후 삭제).  
3. 💡 **데이터 전송량을 줄이는 데 집중하세요** (예: CDN 사용, 압축 데이터 전송).  

> **⚠️ 비용 주의:** 외부 리소스에 접근할 때 데이터 전송량이 급증할 수 있으므로, 데이터 전송 요금이 예상치 못하게 높아질 수 있습니다. [AWS Cost Explorer](https://aws.amazon.com/aws-cost-management/cost-explorer/)를 사용해 비용을 모니터링하세요.

## 📚 핵심 개념

### 개념 1: NAT Gateway의 역할과 목적  
NAT Gateway는 VPC 내에서 공개 서브넷에 연결된 인스턴스가 인터넷에 접근할 수 있도록 하는 **네트워크 번역 장치**입니다. 프라이빗 서브넷에 있는 인스턴스는 직접 인터넷에 노출되지 않으면서도 외부 서비스(예: 업데이트 패키지 다운로드, 클라우드 서비스 인증 등)에 접근할 수 있도록 합니다. 이는 **보안 강화**와 **비용 효율성**을 동시에 달성합니다.  
NAT Gateway는 AWS가 관리하는 고가용성 서비스로, NAT 인스턴스와 달리 수동 관리가 필요 없고, 높은 처리 성능을 제공합니다. 또한, NAT Gateway는 **IPv4 및 IPv6 트래픽을 모두 지원**하며, 단일 인스턴스가 아닌 **하이브리드 아키텍처**를 통해 장애 허용성을 높였습니다.  

#### 왜 중요한가?  
- **보안 강화**: 프라이빗 서브넷 인스턴스가 외부에 노출되지 않아 보안 위험을 줄입니다.  
- **비용 효율성**: NAT 인스턴스처럼 EC2 인스턴스를 관리할 필요가 없어 운영 비용을 절감합니다.  
- **고가용성**: AWS가 관리하는 서비스로 장애 발생 시 자동 복구 기능이 있습니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |  
|-----|-----|-----|  
| **트래픽 번역** | 프라이빗 IP 주소를 공개 IP 주소로 변환하여 인터넷 접근을 허용 | EC2 인스턴스가 AWS S3에 파일을 업로드할 때, NAT Gateway가 IP 주소를 변환 |  
| **고가용성** | 여러 AZ에 분산되어 장애 허용성 제공 | NAT Gateway가 장애 발생 시 자동으로 다른 AZ로 전환 |  
| **IPv6 지원** | IPv4와 IPv6 트래픽을 모두 처리 | IPv6 주소를 사용하는 인스턴스도 인터넷에 접근 가능 |  

> **💡 Tip:** NAT Gateway는 **출발 트래픽**만 지원하며, **입력 트래픽**은 처리할 수 없습니다. 보안 그룹이나 보드가 필요합니다.  

---

### 개념 2: NAT Gateway의 작동 원리  
NAT Gateway는 VPC 내에서 **라우팅 테이블**을 통해 트래픽을 전달하고, 트래픽 번역을 수행합니다. 이는 네트워크 장비가 아니라 AWS가 관리하는 서비스로, EC2 인스턴스처럼 직접 관리할 필요가 없습니다.  

1. **트래픽 전달**: 프라이빗 서브넷에 있는 인스턴스가 인터넷에 접근하려면 라우팅 테이블에 NAT Gateway를 대상으로 설정합니다.  
2. **IP 주소 변환**: NAT Gateway는 인스턴스의 프라이빗 IP 주소를 공개 IP 주소로 변환하고, 응답은 원래의 프라이빗 IP 주소로 전달합니다.  
3. **고가용성 처리**: 트래픽이 증가할 경우, NAT Gateway는 여러 AZ에 분산되어 자동으로 트래픽을 분산하여 처리합니다.  

> **💡 Tip:** 라우팅 테이블을 구성할 때 반드시 **NAT Gateway를 대상으로 설정**해야 합니다. 기본 라우팅 테이블은 인터넷 게이트웨이를 사용하므로, 프라이빗 서브넷의 라우팅 테이블에만 NAT Gateway를 추가해야 합니다.  

---

### 개념 3: NAT Gateway vs NAT 인스턴스  
NAT Gateway와 NAT 인스턴스는 모두 인터넷 접근을 위한 기능을 제공하지만, **관리 방식**, **성능**, **비용** 측면에서 차이가 있습니다.  

#### 주요 특징  
1. ****관리 방식**:  
   - NAT Gateway: AWS가 관리하는 서비스로, 수동 설정이 필요 없습니다.  
   - NAT 인스턴스: EC2 인스턴스를 직접 생성해야 하며, 운영 시 주의가 필요합니다.  

2. ****성능 및 확장성**:  
   - NAT Gateway: 고가용성과 대규모 트래픽 처리가 가능하며, 자동 확장이 지원됩니다.  
   - NAT 인스턴스: 트래픽이 많아지면 성능 저하가 발생할 수 있으며, 수동으로 확장해야 합니다.  

3. ****비용 효율성**:  
   - NAT Gateway: AWS가 관리하므로 EC2 인스턴스 비용을 절감할 수 있습니다.  
   - NAT 인스턴스: EC2 인스턴스 비용이 발생하며, 유지보수 비용이 추가됩니다.  

> **💡 Tip:** 고가용성과 대규모 트래픽 처리가 필요한 경우 **NAT Gateway**를, 단순한 요구사항이라면 **NAT 인스턴스**를 선택하는 것이 좋습니다.

## 🖥️ AWS 콘솔에서 NAT Gateway 사용하기

### Step 1: NAT Gateway 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: [https://console.aws.amazon.com](https://console.aws.amazon.com)  
   - IAM 권한이 부여된 계정으로 로그인해야 합니다 (VPC 관리 권한이 필요)  
2. 상단 검색창에서 **"NAT Gateway"**를 입력하고, 검색 결과에서 **NAT Gateway** 서비스를 클릭합니다  
3. 서비스 대시보드가 열리면 **"Create NAT Gateway"** 버튼을 클릭하여 리소스 생성을 시작합니다  

> **📸 화면 확인:** NAT Gateway 대시보드에서 **"Create NAT Gateway"** 버튼이 보이고, 생성할 리소스의 기본 정보가 표시되면 정상입니다  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **subnet 선택**  
   - **"Subnet"** 드롭다운에서 프라이빗 서브넷을 선택합니다  
   - 선택한 서브넷이 **"Public subnets"**가 아닌지 확인 (NAT Gateway는 프라이빗 서브넷에 생성)  
   - **"Availability Zone"**은 서브넷의 가용 존과 동일하게 설정해야 합니다  
2. **IP 주소 할당**  
   - **"Public IP"** 옵션에서 **"Elastic IP"**를 선택하여 고정 IP 주소를 할당합니다  
   - **"Allocate new IP"**를 클릭하여 새로운 IP를 생성합니다  
3. **설정 확인 및 생성**  
   - **"Confirm"** 버튼을 클릭하여 설정을 저장하고 NAT Gateway 생성을 시작합니다  
   - 생성 완료 후 **"View"** 버튼으로 생성된 리소스를 확인합니다  

> **📸 화면 확인:** 서브넷 선택 화면에서 프라이빗 서브넷이 선택되었고, "Elastic IP"가 할당된 상태를 확인합니다  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **라우팅 테이블 수정**  
   - **"Route Table"**에서 **"Create route"**를 클릭하여 라우팅 규칙을 추가합니다  
   - **"Destination CIDR"**에 `0.0.0.0/0`을 입력하고, **"Target"**에 생성한 NAT Gateway를 선택합니다  
   - **"Route"**를 저장하여 인터넷 접근을 허용합니다  
2. **서브넷 연결 확인**  
   - 생성한 NAT Gateway의 **"Subnet"**이 올바르게 연결되었는지 확인합니다  
   - **"Associated subnets"** 섹션에서 프라이빗 서브넷이 표시되는지 확인합니다  
3. **상태 및 리소스 검사**  
   - **"Status"**가 `Active`로 표시되는지 확인합니다  
   - **"State"**가 `In-service`인지 확인하여 정상 동작 여부를 파악합니다  

> **⚠️ 주의:** NAT Gateway는 프라이빗 서브넷에서 생성되어야 하며, 서브넷의 가용 존과 동일한 존에서 생성해야 합니다. 잘못된 존에서 생성 시 오류가 발생할 수 있습니다  

---

### Step 4: 설정 확인 및 테스트  
1. **리소스 확인 방법**  
   - NAT Gateway 대시보드에서 **"NAT Gateway"** 목록을 확인합니다  
   - **"Name"**, **"Subnet"**, **"Elastic IP"** 정보가 올바르게 표시되는지 확인합니다  
2. **상태 확인 방법**  
   - **"Status"** 필드가 `Active`인지 확인합니다  
   - **"State"**가 `In-service`인지 확인하여 네트워크 연결이 정상인지 파악합니다  
3. **정상 동작 테스트**  
   - **AWS CLI** 명령어로 테스트:  
     ```bash
     aws ec2 describe-nat-gateways --nat-gateway-ids nat-0abcdef1234567890
     ```
   - **SSH 테스트**: 프라이빗 서브넷에 있는 EC2 인스턴스에서 외부 웹사이트에 연결해보기  
     ```bash
     ssh -i key.pem ec2-user@private-instance-ip
     curl https://www.google.com
     ```
   - **VPC Flow Logs**에서 트래픽 흐름을 확인하여 NAT Gateway가 정상적으로 작동하는지 검증합니다  

> **💰 비용 주의:** NAT Gateway는 요청당 0.015달러 이상의 비용이 발생합니다. 프리티어는 14일간 1개의 NAT Gateway 생성이 가능합니다.  
> **✅ 실무 팁:** NAT Gateway는 고가용성 환경에서 사용해야 하며, NAT 인스턴스 대신 사용하는 것이 권장됩니다.

## ⌨️ AWS CLI로 NAT Gateway 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

> **💡 Tip:** CLI 명령어 실행 전 반드시 `aws configure`로 AWS 자격 증명을 설정해야 합니다. `~/.aws/credentials` 파일에 접근 키와 시크릿 액세스 키를 등록해야 합니다.

---

### 예제 1: NAT Gateway 리소스 조회
```bash
# NAT Gateway 목록 조회
aws ec2 describe-nat-gateways --query 'NatGateways[*].NatGatewayId' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 필터링 표현식 (예: `NatGateways[*].NatGatewayId`) | `'NatGateways[*].NatGatewayId'` |
| `--output` | 출력 형식 (json, table, text) | `table` |

**예상 출력:**
```
| NatGatewayId |
+--------------+
| nat-0abcdef1234567890 |
```

> **⚠️ 주의:** `describe-nat-gateways` 명령어는 전체 NAT Gateway 목록을 반환합니다. 특정 리소스를 조회하려면 `--nat-gateway-ids` 파라미터를 사용해야 합니다.

---

### 예제 2: NAT Gateway 리소스 생성
```bash
# NAT Gateway 생성
aws ec2 create-nat-gateway \
    --subnet-id subnet-0123456789abcdef0 \
    --allocation-id eipalloc-0abcdef12345678901
```

**필수 옵션:**
- `--subnet-id`: NAT Gateway를 할당할 프라이빗 서브넷 ID
- `--allocation-id`: EIP(공개 IP) 할당 ID (EIP이 미리 생성되어 있어야 함)

**예상 출력:**
```json
{
    "NatGateway": {
        "NatGatewayId": "nat-0abcdef1234567890",
        "NatGatewayAddress": "52.123.45.67",
        "State": "pending"
    }
}
```

> **💡 Tip:** EIP 생성은 `aws ec2 allocate-address` 명령어로 먼저 수행해야 합니다. NAT Gateway 생성 시 EIP를 할당할 수 없습니다.

---

### 예제 3: NAT Gateway 리소스 수정
```bash
# NAT Gateway 수정 (지원되지 않는 명령어)
aws ec2 update-nat-gateway --nat-gateway-id nat-0abcdef1234567890 --new-value "value"
```

> **⚠️ 주의:** AWS CLI에서 NAT Gateway는 생성 후 **수정이 불가능**합니다. 필요한 변경사항은 새로운 NAT Gateway 생성 후 기존 리소스를 삭제해야 합니다.

---

### 예제 4: NAT Gateway 리소스 삭제
```bash
# NAT Gateway 삭제
aws ec2 delete-nat-gateway --nat-gateway-id nat-0abcdef1234567890

# 삭제 확인
aws ec2 describe-nat-gateways --nat-gateway-ids nat-0abcdef1234567890
```

**예상 출력 (삭제 후):**
```
{
    "NatGateways": []
}
```

> **⚠️ 주의:** NAT Gateway 삭제는 **완전히 되돌릴 수 없습니다**. 삭제 전에 모든 리소스 연결을 해제해야 합니다.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws ec2 describe-nat-gateways --nat-gateway-ids nat-0abcdef1234567890
aws ec2 list-nat-gateways

# 생성
aws ec2 create-nat-gateway --subnet-id subnet-0123456789abcdef0 --allocation-id eipalloc-0abcdef12345678901

# 삭제
aws ec2 delete-nat-gateway --nat-gateway-id nat-0abcdef1234567890
```

> **💡 Tip:** `list-nat-gateways`는 `describe-nat-gateways`보다 간결한 출력을 제공합니다. AWS CLI 버전 1.28.0 이상에서 지원됩니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 NAT Gateway 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **NAT Gateway의 핵심 목적 및 VPC 내 역할**  
   - 설명: NAT Gateway는 프라이빗 서브넷의 인스턴스가 인터넷에 접근할 수 있도록 중간 역할을 수행합니다. 이는 VPC 내에서 외부 리소스에 연결할 수 없는 프라이빗 인스턴스에 필수적인 서비스로, 시험에서 VPC 구조와 연계된 문제에 자주 등장합니다.  
   - 키워드: `VPC`, `프라이빗 서브넷`, `인터넷 접근`

2. **NAT Gateway vs NAT 인스턴스의 차이점**  
   - 설명: NAT Gateway는 확장성과 고가용성을 제공하지만, NAT 인스턴스는 유연한 네트워크 설정이 가능합니다. 시험에서 둘의 사용 시나리오와 성능 차이를 구분할 수 있는지가 평가 포인트입니다.  
   - 키워드: `확장성`, `고가용성`, `네트워크 설정`

3. **NAT Gateway의 비용 구조 및 프리티어 제한**  
   - 설명: NAT Gateway는 데이터 전송량에 따라 비용이 발생하며, 프리티어는 1000시간/월의 제한이 있습니다. 시험에서 비용 최적화 전략과 제한 사항을 파악하는 것이 중요합니다.  
   - 키워드: `데이터 전송량`, `프리티어`, `비용 최적화`

4. **라우팅 테이블 구성 및 NAT Gateway의 영향**  
   - 설명: NAT Gateway는 라우팅 테이블에 `igw-`로 시작하는 게이트웨이를 통해 인터넷 접근을 허용합니다. 라우팅 규칙 오류는 네트워크 연결 실패로 이어질 수 있어 시험에서 주의 깊은 분석이 필요합니다.  
   - 키워드: `라우팅 테이블`, `게이트웨이`, `네트워크 연결`

5. **NAT Gateway의 제한사항 및 사용 시나리오**  
   - 설명: NAT Gateway는 특정 IP 주소를 사용할 수 없고, 다중 인스턴스의 동시 접속에 제한이 있을 수 있습니다. 시험에서 이러한 한계를 고려한 설계 요구사항을 판단하는 문제가 자주 출제됩니다.  
   - 키워드: `IP 주소 제한`, `고가용성`, `스케일링`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | NAT Gateway와 NAT 인스턴스를 혼동해 키워드 판단 오류 발생 | NAT Gateway는 고가용성, 확장성에 적합하고, NAT 인스턴스는 유연한 네트워크 설정에 적합함 |
| 함정 2 | NAT Gateway의 데이터 전송량 비용 구조를 오해 | 데이터 전송량이 클수록 비용 증가, 프리티어는 1000시간/월 제한 |
| 함정 3 | 라우팅 테이블에서 NAT Gateway의 게이트웨이 ID를 오인 | `igw-`로 시작하는 게이트웨이 ID를 반드시 확인해야 함 |

#### 🔄 NAT Gateway vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | NAT Gateway | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 프라이빗 서브넷 인스턴스의 인터넷 접근 | NAT 인스턴스 | 인스턴스 수가 많거나 고가용성 요구 시 |
| 확장성 | 자동 확장 및 고가용성 지원 | 수동 설정 | 스케일링이 필요한 시나리오 |
| 비용 | 데이터 전송량에 따라 비용 발생 | 인스턴스 운영 비용 + 데이터 전송 | 인스턴스 수가 적고 설정 유연성 필요 시 |
| 지연시간 | 고정 지연 시간 | 동적 지연 시간 | 실시간 성능 최적화가 필요한 경우 |

#### 📝 시험 대비 체크리스트
- [ ] NAT Gateway의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  > "프라이빗 서브넷의 인스턴스가 인터넷에 접근할 수 있도록 중간 역할을 수행하는 서비스"  
- [ ] NAT Gateway를 선택해야 하는 시나리오를 알고 있는가?  
  > 다중 인스턴스, 고가용성, 스케일링 요구 시  
- [ ] NAT Gateway의 제한사항/한계를 알고 있는가?  
  > IP 주소 제한, 데이터 전송량 비용, 특정 네트워크 설정 불가  
- [ ] NAT Gateway와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  > NAT 인스턴스는 유연한 설정 가능, NAT Gateway는 고가용성 및 확장성 제공  
- [ ] NAT Gateway의 비용 구조를 이해하고 있는가?  
  > 데이터 전송량 기반 요금제, 프리티어 제한 1000시간/월  

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 NAT Gateway를 떠올리세요:  
> - **고가용성**  
> - **스케일링**  
> - **데이터 전송량**  
> - **프라이빗 서브넷**  
> - **인터넷 접근**

---

| [⬅️ Internet Gateway](./Internet-Gateway.md) | [📑 Day 1 목차](./README.md) | [🏠 Week 2](../README.md) | [다음 Day ➡️](../day2/README.md) |

---
