---

| [⬅️ Subnet](./Subnet.md) | [📑 Day 1 목차](./README.md) | [🏠 Week 2](../README.md) | [NAT Gateway ➡️](./NAT-Gateway.md) |

---

> **한 줄 정의:** Internet Gateway는 가상 개인 클라우드(VPC) 내 인스턴스가 인터넷에 접근할 수 있도록 하는 AWS의 네트워크 인프라 서비스입니다.

### 이 서비스가 해결하는 문제  
**기존의 문제점:**  
- **문제 1:** VPC 내 공개 서브넷에 있는 인스턴스는 직접 인터넷에 접근할 수 없어 별도의 NAT 인스턴스나 게이트웨이가 필요했습니다.  
- **문제 2:** 라우팅 테이블 구성 시 인터넷 연결을 위한 복잡한 설정이 필요해 실수 위험이 있었습니다.  
- **문제 3:** NAT 인스턴스 사용 시 성능 제한, 가용성 저하, 관리 부담이 발생했습니다.  

**Internet Gateway로 해결:**  
- **해결 1:** 공개 서브넷에 직접 연결해 인스턴스가 인터넷에 접근할 수 있도록 단순화했습니다.  
- **해결 2:** 라우팅 테이블에 단일 라우트 규칙만 추가해 구성이 용이해졌습니다.  
- **해결 3:** 고가용성 및 확장성 있는 NAT 게이트웨이를 사용해 성능과 관리 효율성을 개선했습니다.  

### 비유로 이해하기  
인터넷 게이트웨이는 VPC에 연결된 고속도로 입구에 비유할 수 있습니다. 차량이 고속도로로 진입하려면 입구에서의 검사를 거쳐야 하며, 이 과정은 인터넷 트래픽이 VPC로 들어오기 위한 필수 절차입니다. 이처럼 Internet Gateway는 VPC 내 인스턴스가 인터넷에 접근할 수 있도록 트래픽을 안전하게 전달하는 역할을 합니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)  

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 웹 서버나 API 엔드포인트를 인터넷에 노출해야 할 경우 | 기업이 AWS에서 대규모 웹 애플리케이션을 호스팅하고, 사용자가 직접 서버에 접근하는 경우 |
| 시나리오 2 | VPC 내 프라이빗 서브넷 인스턴스가 외부 서비스(예: AWS S3, GitHub)에 접근해야 할 때 | 기업이 내부 애플리케이션에서 외부 라이브러리나 데이터 저장소에 접근하는 경우 |
| 시나리오 3 | 하이브리드 클라우드 아키텍처에서 온프레미스 네트워크와 VPC 통신이 필요한 경우 | 기업이 데이터 센터와 AWS VPC 간의 통신을 설정해야 할 때 |

**이럴 때 Internet Gateway를 선택하세요:**  
- ✅ 상황 1: 인터넷에 노출된 서버가 필요할 때  
- ✅ 상황 2: 프라이빗 서브넷 인스턴스가 외부 리소스에 접근해야 할 때  
- ✅ 상황 3: 하이브리드 클라우드 통신이 필요할 때  

**이럴 때는 다른 서비스를 고려하세요:**  
- ❌ 상황: 프라이빗 서브넷 인스턴스가 인터넷에 접근해야 할 때 → **NAT Gateway** (NAT 인스턴스 대신 고가용성 제공)  
- ❌ 상황: DNS 이름 해석이 필요할 때 → **Route 53** (DNS 관리 및 고가용성)  
- ❌ 상황: VPC 네트워크 정의가 필요할 때 → **VPC 서비스** (서브넷, 라우팅 테이블 관리)  

---

## 🔗 연관 서비스 (Used Together With)  

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **NAT Gateway** | 프라이빗 서브넷 인스턴스가 인터넷에 접근할 수 있도록 보조 | VPC → NAT Gateway → EC2 인스턴스 |
| **Route 53** | DNS 이름 해석을 통해 인터넷 트래픽을 VPC로 전달 | DNS 요청 → Route 53 → Internet Gateway → EC2 인스턴스 |
| **VPC** | 네트워크 환경을 정의하고 라우팅 테이블을 관리 | VPC CIDR 설정 → 라우팅 테이블 생성 → Internet Gateway 연결 |

**자주 사용되는 아키텍처 패턴:**  
```
[사용자] → [Internet Gateway] → [EC2 인스턴스]  
[사용자] → [Route 53] → [Internet Gateway] → [EC2 인스턴스]  
[온프레미스 네트워크] → [Internet Gateway] → [VPC 내 EC2 인스턴스]  
```  

---

## 💰 비용 구조 (Pricing)  

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **Internet Gateway 생성 및 관리** | $0.01/시간 (정확한 요금은 AWS 공식 문서 참조) | 월 1개 무료 (12개월) |
| **NAT Gateway 사용** | $0.02/시간 (정확한 요금은 AWS 공식 문서 참조) | 12개월 무료 |
| **데이터 전송** | $0.09/GB (정확한 요금은 AWS 공식 문서 참조) | 항상 무료 |

**비용 최적화 팁:**  
1. 💡 팁 1: 고가용성 NAT 게이트웨이를 사용해 NAT 인스턴스 대신 비용 절감  
2. 💡 팁 2: 트래픽 모니터링으로 불필요한 데이터 전송을 줄이세요.  
3. 💡 팁 3: 프리티어 기간 동안 서비스를 테스트해 비용을 최소화하세요.  

> **⚠️ 비용 주의:** 고사양 트래픽이나 장기 사용 시 데이터 전송 비용이 급격히 증가할 수 있으므로, **CloudWatch**를 통해 트래픽 패턴을 모니터링하고, 필요 시 **VPC Flow Logs**를 활용해 비용 최적화 전략을 수립하세요.

## 📚 핵심 개념

### 개념 1: 인터넷 게이트웨이의 역할과 중요성  
인터넷 게이트웨이는 AWS Virtual Private Cloud(VPC)가 인터넷과 통신할 수 있도록 하는 핵심 구성 요소입니다. VPC 내에서 인스턴스가 인터넷에 접근하거나 외부 서비스에 연결할 때, 인터넷 게이트웨이는 이 통신을 중계하는 가상 라우터 역할을 수행합니다. 이는 특히 **퍼블릭 서브넷**에 있는 인스턴스가 인터넷에 접속할 수 있도록 필수적인 연결 통로를 제공합니다.  

#### 왜 중요한가?  
- **인스턴스와 인터넷 연결**: VPC 내 인스턴스가 외부 웹사이트, 클라우드 서비스, 또는 다른 네트워크에 접근할 수 있도록 합니다.  
- **보안 및 네트워크 통제**: 트래픽을 특정 경로로 지정해 네트워크 보안 정책을 강화할 수 있습니다.  
- **가상 라우터 기능**: 트래픽을 VPC 내부 네트워크와 외부 인터넷 간에 자동으로 라우팅합니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **목적** | VPC와 인터넷 간 통신 연결 제공 | 웹 서버가 외부 사용자에게 응답하는 데 사용 |
| **트래픽 라우팅** | 트래픽을 VPC 내부 및 외부 네트워크로 자동 지정 | 라우팅 테이블에 IGW를 지정해 트래픽 방향 결정 |
| **사용 가능성** | VPC 생성 시 자동 생성되거나 수동으로 설정 | VPC 생성 시 IGW가 자동으로 생성됨 |

> **💡 Tip:** 인터넷 게이트웨이는 VPC의 **퍼블릭 서브넷**과 직접적으로 연결되며, 프라이빗 서브넷은 NAT 게이트웨이 또는 NAT 인스턴스를 통해 인터넷에 접근해야 합니다.  

---

### 개념 2: 인터넷 게이트웨이의 작동 원리  
인터넷 게이트웨이는 VPC와 인터넷 간의 통신을 중계하는 가상 라우터로, 다음과 같은 단계로 작동합니다.  

1. **게이트웨이 생성 및 VPC 연결**: 먼저 AWS 콘솔 또는 CLI를 통해 인터넷 게이트웨이를 생성하고, 해당 VPC에 연결합니다. 이 과정에서 IGW는 VPC의 공용 IP 주소 범위와 연결됩니다.  
2. **라우팅 테이블 구성**: VPC의 라우팅 테이블에 IGW를 지정해 트래픽 경로를 설정합니다. 예를 들어, 0.0.0.0/0와 같은 대역폭을 사용해 모든 트래픽을 IGW로 라우팅할 수 있습니다.  
3. **인스턴스 통신 중계**: 인스턴스가 인터넷에 접근할 때, IGW가 트래픽을 외부 네트워크로 전송하고, 응답은 IGW를 통해 인스턴스로 전달됩니다.  

> **💡 Tip:** IGW는 **IP 주소를 할당받지 않**으며, VPC의 공용 IP 범위를 사용해 트래픽을 중계합니다. 이는 보안 및 네트워크 관리에 유리합니다.  

---

### 개념 3: 인터넷 게이트웨이의 주요 특징  
인터넷 게이트웨이는 다음과 같은 주요 특징을 가집니다.  

#### 주요 특징  
1. **자동 생성 및 관리**: VPC를 생성할 때 AWS가 자동으로 IGW를 생성해 사용자에게 제공합니다.  
2. **고가용성 및 확장성**: 단일 포인트 실패 없이 고가용성으로 운영되며, 트래픽 증가 시 성능을 확장할 수 있습니다.  
3. **비용 효율성**: 트래픽량에 따라 비용이 부여되며, 프리티어를 활용해 무료로 사용할 수 있습니다.  

> **⚠️ 주의:** IGW는 VPC의 공용 IP 범위에 연결되므로, 네트워크 보안 정책에 따라 IP 주소를 지정하는 것이 중요합니다.  

--- 

**참고**: 인터넷 게이트웨이와 NAT 게이트웨이는 역할이 다릅니다. IGW는 VPC와 인터넷 간의 연결을 제공하는 반면, NAT 게이트웨이는 프라이빗 서브넷의 인스턴스가 인터넷에 접근할 수 있도록 하는 역할을 합니다.

## 🖥️ AWS 콘솔에서 Internet Gateway 사용하기

### Step 1: Internet Gateway 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - AWS 계정이 활성화되어 있고, IAM 권한이 부여된 상태여야 합니다  
2. 상단 검색창에서 **"Internet Gateway"**를 입력하고, 검색 결과에서 해당 서비스를 클릭합니다  
   - 검색창은 "Services" 메뉴 대신 빠르게 서비스를 찾는 데 사용됩니다  

> **📸 화면 확인:** Internet Gateway 대시보드가 표시되면 정상입니다. 대시보드에는 생성된 게이트웨이 목록, 상태, 연결 정보가 표시됩니다  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **Internet Gateway 생성**  
   - "Create Internet Gateway" 버튼을 클릭하여 새 게이트웨이를 생성합니다  
   - 이름을 입력할 수 있으며, "my-igw"와 같은 간단한 이름을 추천합니다  
2. **설정 확인**  
   - 생성된 게이트웨이의 상태가 **"pending"**에서 **"available"**으로 변경될 때까지 기다립니다 (약 1~2분 소요)  
   - 상태가 **"available"**이 되면 게이트웨이가 VPC와 연결되어 통신이 가능합니다  
3. **VPC 연결 확인**  
   - 생성된 게이트웨이를 특정 VPC에 연결해야 합니다. 이는 라우팅 테이블에서 설정합니다  

> **📸 화면 확인:** 생성된 게이트웨이가 "available" 상태로 표시되고, VPC 목록에 포함된 것을 확인하세요  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **VPC에 게이트웨이 연결**  
   - VPC 대시보드에서 목록에서 해당 VPC를 선택한 후, "Actions" > "Attach Internet Gateway"를 클릭합니다  
   - 연결된 게이트웨이가 **"attached"** 상태가 되면 라우팅 테이블에서 해당 게이트웨이를 사용할 수 있습니다  
2. **라우팅 테이블 구성**  
   - 라우팅 테이블에서 **0.0.0.0/0** CIDR을 사용하는 라우트를 추가합니다  
   - **Target** 필드에 생성된 Internet Gateway를 선택하면 외부 인터넷 트래픽이 VPC로 전달됩니다  
3. **NAT 게이트웨이와의 구분**  
   - NAT 게이트웨이는 프라이빗 서브넷에서 외부 인터넷에 접근할 때 사용됩니다.  
   - Internet Gateway는 퍼블릭 서브넷에서 외부 인터넷에 접근할 때 사용됩니다  

> **⚠️ 주의:** VPC에 잘못 연결된 게이트웨이를 사용하면 네트워크 장애가 발생할 수 있습니다. 생성 시 VPC를 정확히 선택하세요  

---

### Step 4: 설정 확인 및 테스트  
1. **리소스 확인 방법**  
   - VPC 대시보드에서 "Internet Gateways" 탭을 클릭하면 연결된 게이트웨이 목록이 표시됩니다  
   - 게이트웨이 상태가 **"attached"**인지 확인합니다  
2. **상태 확인 방법**  
   - AWS 콘솔에서 "Internet Gateway" 대시보드에서 상태를 확인합니다  
   - **"available"** 상태일 때만 외부 인터넷에 접근할 수 있습니다  
3. **정상 동작 테스트**  
   - 퍼블릭 서브넷에 EC2 인스턴스를 생성하고, `ping` 명령어나 웹 브라우저를 통해 외부 호스트에 연결해 보세요  
   - 연결이 성공하면 게이트웨이 설정이 정상입니다  

> **💡 Tip:** 프리티어는 1개의 Internet Gateway 생성 및 1년간의 사용이 가능합니다. 비용을 줄이려면 프리티어를 활용하세요  

---

### 📌 실습 예제: VPC + Internet Gateway 구성  
1. VPC 생성 후, 퍼블릭 서브넷을 추가합니다  
2. 라우팅 테이블에서 `0.0.0.0/0` CIDR에 Internet Gateway를 연결합니다  
3. 퍼블릭 서브넷에 EC2 인스턴스를 생성하고, 외부 인터넷에 연결하는 테스트를 진행합니다  

> **⚠️ 주의:** 프라이빗 서브넷은 Internet Gateway를 사용할 수 없습니다. NAT 게이트웨이 또는 NAT 인스턴스를 사용해야 합니다.

## ⌨️ AWS CLI로 Internet Gateway 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**필수 사전 조건:**  
- AWS CLI가 설치되어 있어야 합니다.  
- AWS 계정이 생성되어 있어야 합니다.  
- `ec2` 서비스에 대한 접근 권한이 있어야 합니다.  
- `us-east-1` 리전이 기본 리전으로 설정되어 있어야 합니다.  

---

### 예제 1: Internet Gateway 리소스 조회
```bash
# [Internet Gateway 리소스 목록 조회]
aws ec2 describe-internet-gateways \
    --query 'InternetGateways[*].{ID:InternetGatewayId, Name:Tags[?Key==`Name`].Value[0], VPC:VpcId}' \
    --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 필터링 및 형식 조정 | `'InternetGateways[*].{ID:InternetGatewayId, Name:Tags[?Key==`Name`].Value[0], VPC:VpcId}'` |
| `--output` | 출력 형식 | `table` (테이블 형식), `json` (JSON 형식), `text` (텍스트 형식) |

**예상 출력:**
```
|     ID        |     Name     |        VPC         |
|---------------|--------------|-------------------|
| igw-0abcdef123| MyVpcIGW     | vpc-0123456789abc|
```

**설명:**  
`Tags[?Key==`Name`]`은 `Name` 태그를 필터링하고, `Value[0]`은 태그 값을 추출합니다. VPC ID는 `VpcId` 필드로 확인할 수 있습니다.

---

### 예제 2: Internet Gateway 리소스 생성
```bash
# [Internet Gateway 생성]
aws ec2 create-internet-gateway \
    --tag-specs '[{"ResourceType":"internet-gateway","Tags":[{"Key":"Name","Value":"MyVpcIGW"}]}]'
```

**필수 옵션:**
- `--tag-specs`: 태그를 추가하는 필수 옵션.  
  - `ResourceType`: `internet-gateway`  
  - `Tags`: 이름(`Name`)과 값(`MyVpcIGW`)을 지정합니다.  

**예상 출력:**
```json
{
    "InternetGateway": {
        "InternetGatewayId": "igw-0abcdef123",
        "Tags": [
            {
                "Key": "Name",
                "Value": "MyVpcIGW"
            }
        ]
    }
}
```

**설명:**  
`create-internet-gateway` 명령어는 태그를 통해 리소스 이름을 지정합니다.  
AWS 콘솔에서 생성한 후 `Tags` 섹션에서 이름을 확인할 수 있습니다.

---

### 예제 3: Internet Gateway 리소스 수정
```bash
# [Internet Gateway 태그 수정]
aws ec2 create-internet-gateway \
    --internet-gateway-id "igw-0abcdef123" \
    --tag-specs '[{"ResourceType":"internet-gateway","Tags":[{"Key":"Environment","Value":"Production"}]}]'
```

**주의 사항:**  
- Internet Gateway는 직접 수정할 수 없습니다.  
- `create-internet-gateway`를 사용해 새로운 리소스를 생성하고, 기존 리소스를 삭제한 후 교체해야 합니다.  
- `--internet-gateway-id`는 기존 리소스 ID를 입력해야 합니다.

**예상 출력:**
```json
{
    "InternetGateway": {
        "InternetGatewayId": "igw-0abcdef123",
        "Tags": [
            {
                "Key": "Name",
                "Value": "MyVpcIGW"
            },
            {
                "Key": "Environment",
                "Value": "Production"
            }
        ]
    }
}
```

---

### 예제 4: Internet Gateway 리소스 삭제
```bash
# [Internet Gateway 삭제]
aws ec2 delete-internet-gateway --internet-gateway-id "igw-0abcdef123"

# 삭제 확인
aws ec2 describe-internet-gateways --internet-gateway-ids "igw-0abcdef123"
```

**삭제 확인 예시:**
```
{
    "InternetGateways": []
}
```

**⚠️ 주의:**  
- 삭제된 Internet Gateway는 복구할 수 없습니다.  
- VPC와 연결된 Internet Gateway는 삭제할 수 없습니다.  
- 삭제 전에 라우팅 테이블을 확인하고, 필요한 경우 NAT Gateway를 사용해야 합니다.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws ec2 describe-internet-gateways --internet-gateway-ids "igw-0abcdef123"
aws ec2 describe-internet-gateways --filters "Name=tag:Name,Values=MyVpcIGW"

# 생성
aws ec2 create-internet-gateway --tag-specs '[{"ResourceType":"internet-gateway","Tags":[{"Key":"Name","Value":"MyVpcIGW"}]}]'

# 삭제
aws ec2 delete-internet-gateway --internet-gateway-id "igw-0abcdef123"
```

**추천 팁:**  
- `--filters`를 사용해 특정 태그로 필터링하는 것이 효율적입니다.  
- `describe-internet-gateways`는 `--internet-gateway-ids`와 `--filters`를 동시에 사용할 수 있습니다.  
- `--query` 옵션으로 필드를 정제해 출력을 간결하게 합니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Internet Gateway 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **포인트 1: Internet Gateway의 핵심 역할**  
   - 설명: VPC 내 인스턴스가 인터넷에 접근할 수 있도록 하기 위한 라우팅 경로를 제공하는 **핵심 컴포넌트**로, **퍼블릭 서브넷**의 인터넷 연결을 담당합니다. 시험에서 **VPC 인터넷 연결 구조**를 묻는 경우 반드시 이 포인트를 포함합니다.  
   - 키워드: `VPC 인터넷 연결`, `퍼블릭 서브넷`, `라우팅 테이블`

2. **포인트 2: 라우팅 테이블 구성**  
   - 설명: **Internet Gateway**를 라우팅 테이블에 추가해야 하며, **0.0.0.0/0** CIDR을 사용해 인터넷 트래픽을 방향지정합니다. 라우팅 테이블 구성 오류는 시험에서 자주 출제되는 **실무 오류 유형**입니다.  
   - 키워드: `라우팅 테이블`, `0.0.0.0/0`, `라우팅 규칙`

3. **포인트 3: NAT Gateway vs NAT 인스턴스 비교**  
   - 설명: **NAT Gateway**는 **확장성**과 **고가용성**을 제공하지만, **NAT 인스턴스**는 유연한 네트워크 설정이 가능합니다. 시험에서 **프라이빗 서브넷의 인터넷 접근 방식**을 묻는 경우 이 두 기술의 차이점을 반드시 구분해야 합니다.  
   - 키워드: `NAT Gateway`, `NAT 인스턴스`, `고가용성`

4. **포인트 4: 비용 구조 및 프리티어 활용**  
   - 설명: **Internet Gateway**는 **프리티어**로 무제한 사용 가능하지만, **NAT Gateway**는 데이터 전송량에 따라 비용이 발생합니다. 시험에서 **비용 최적화 전략**을 묻는 경우 이 차이점을 반드시 고려해야 합니다.  
   - 키워드: `프리티어`, `NAT Gateway 비용`, `데이터 전송량`

5. **포인트 5: 한계 및 제한 사항**  
   - 설명: **Internet Gateway**는 1개 VPC당 1개만 생성 가능하며, **IPv6 지원**이 필요할 경우 별도 설정이 필요합니다. 시험에서 **네트워크 구성 제한 사항**을 묻는 경우 이 점을 반드시 확인해야 합니다.  
   - 키워드: `VPC 제한`, `IPv6 지원`, `스케일링 한계`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "NAT Gateway는 인터넷에 접근할 수 없다"는 오해 | **NAT Gateway는 프라이빗 서브넷에서 인터넷 접근을 제공**하며, Internet Gateway는 퍼블릭 서브넷의 인터넷 연결을 담당 |
| 함정 2 | "Internet Gateway는 VPC 내부 네트워크 연결을 제공한다"는 오해 | **Internet Gateway는 외부 인터넷과 VPC의 연결을 제공**하며, VPC 내부 네트워크 연결은 VPC 자체가 담당 |
| 함정 3 | "Internet Gateway는 라우팅 테이블에 추가하지 않아도 된다"는 오해 | **필수적으로 라우팅 테이블에 추가**해야 하며, 추가하지 않으면 인터넷 접근이 불가능 |

#### 🔄 Internet Gateway vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | Internet Gateway | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| **용도** | 퍼블릭 서브넷의 인터넷 접근 제공 | NAT Gateway (프라이빗 서브넷용), NAT 인스턴스 (고정 IP 필요 시) | **인스턴스가 인터넷에 접근해야 할 때** |
| **확장성** | 고가용성, 자동 확장 | NAT Gateway (고가용성), NAT 인스턴스 (수동 확장) | **대규모 트래픽 처리 시** |
| **비용** | 프리티어로 무제한 사용 | 데이터 전송량에 따라 요금 발생 | **비용 최적화가 중요한 경우** |
| **지연시간** | 고정 지연, 성능 안정 | NAT Gateway (고정 지연), NAT 인스턴스 (변동 지연) | **성능 안정성 요구 시** |

#### 📝 시험 대비 체크리스트
- [ ] Internet Gateway의 핵심 목적을 한 문장으로 설명할 수 있는가?  
- [ ] Internet Gateway를 선택해야 하는 시나리오를 알고 있는가? (예: 퍼블릭 서브넷 인터넷 접근)  
- [ ] Internet Gateway의 제한사항/한계를 알고 있는가? (1개 VPC당 1개, IPv6 지원 필요)  
- [ ] Internet Gateway와 NAT Gateway/NAT 인스턴스의 차이점을 설명할 수 있는가?  
- [ ] Internet Gateway의 비용 구조를 이해하고 있는가? (프리티어, 데이터 전송량에 따른 비용)

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Internet Gateway를 떠올리세요:  
> - **인터넷 접근**  
> - **퍼블릭 서브넷**  
> - **라우팅 테이블 구성**  
> - **고가용성**  
> - **VPC 인터넷 연결**

---

| [⬅️ Subnet](./Subnet.md) | [📑 Day 1 목차](./README.md) | [🏠 Week 2](../README.md) | [NAT Gateway ➡️](./NAT-Gateway.md) |

---
