---

| [⬅️ VPC Flow Logs](./VPC-Flow-Logs.md) | [📑 Day 3 목차](./README.md) | [🏠 Week 2](../README.md) | [다음 Day ➡️](../day4/README.md) |

---

# Network Firewall 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** Network Firewall는 VPC 내외부 네트워크 트래픽을 보호하기 위한 AWS의 관리형 네트워크 보안 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** NACL(네트워크 ACL)과 보안 그룹의 복잡한 중복 설정으로 인한 보안 정책 관리 어려움. 예: 서버 간 통신 차단이 발생할 때 수동으로 규칙을 수정해야 하는 번거로움  
- **문제 2:** VPC 흐름 로그를 통해 트래픽 패턴을 분석하려 할 때, 데이터 분석과 대응이 별도의 도구로 이루어져 시간이 많이 소요됨  
- **문제 3:** DDoS 공격이나 악성 트래픽이 들어오면 수동으로 보안 그룹을 조정해야 하며, 대규모 트래픽 차단 시 리소스 과부하 발생  

**Network Firewall로 해결:**
- **해결 1:** 단일 인터페이스에서 보안 정책을 집중 관리해 규칙 충돌 방지 및 편리한 설정 가능  
- **해결 2:** 실시간 트래픽 모니터링과 정규 표현식 기반의 패킷 필터링으로 분석 속도 향상  
- **해결 3:** 자동화된 트래픽 차단 정책으로 DDoS 대응 속도 개선 및 시스템 부하 감소  

### 비유로 이해하기
**집에 들어오는 사람들의 ID 검사**를 생각해보세요. 기존에는 각 문마다 다른 보안 요원이 ID를 확인하는 것이었지만, Network Firewall은 모든 입구에서 동일한 보안 요원이 ID를 확인하며, 비정상적인 사람(악성 트래픽)은 즉시 차단해줍니다. 이처럼 모든 네트워크 트래픽을 통합적으로 감시하고, 위협을 사전에 차단하는 것이죠.  

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 고가용성 시스템 보호 | 클라우드 기반의 금융 서버에 DDoS 공격 대응 |
| 시나리오 2 | 마이크로서비스 아키텍처 보안 | 여러 서버 간 통신 차단 정책 관리 |
| 시나리오 3 | 로그 분석 및 대응 | VPC 흐름 로그와 연동해 이상 트래픽 탐지 |

**이럴 때 Network Firewall를 선택하세요:**
- ✅ **상황 1:** 네트워크 트래픽을 실시간으로 모니터링하고 대응해야 하는 경우  
- ✅ **상황 2:** 보안 정책을 중앙 집중식으로 관리하고자 할 때  
- ✅ **상황 3:** VPC 내외부 통신을 분석해 보안 위협을 사전에 차단해야 할 때  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **상황 → 대안 서비스 추천:** 인스턴스 레벨 보안 정책 필요 시 → **보안 그룹** 사용 (NACL보다 더 세밀한 통제 가능)  
- ❌ **상황 → 대안 서비스 추천:** HTTP/HTTPS 트래픽 보호 필요 시 → **WAF** 사용 (Application Layer 보안 강화)  
- ❌ **상황 → 대안 서비스 추천:** 멀티-아카운트 보안 정책 관리 필요 시 → **Firewall Manager** 사용 (집중된 정책 관리)  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **WAF** | HTTP/HTTPS 트래픽 보안 강화 | Network Firewall → WAF → EC2 |
| **Shield** | DDoS 공격 대응 | Network Firewall → Shield → S3 |
| **Firewall Manager** | 멀티-아카운트 보안 정책 관리 | Firewall Manager → Network Firewall → VPC |

**자주 사용되는 아키텍처 패턴:**
```
User → CloudFront → Network Firewall → S3
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **Network Firewall 사용량** | $0.0015/GB (데이터 전송량 기준) | 월 10GB 무료 |
| **WAF 요청량** | $0.0025/1000 요청 | 12개월 무료 |
| **Shield 사용량** | $0.005/GB (DDoS 대응량 기준) | 항상 무료 |

**비용 최적화 팁:**
1. 💡 **팁 1:** 트래픽 패턴 분석을 통해 비필수적인 보안 정책 제거해 데이터 전송량 줄이기  
2. 💡 **팁 2:** 프리티어 한도 내에서 테스트 환경 구축 후 실제 환경으로 전환  
3. 💡 **팁 3:** AWS Cost Explorer를 활용해 비용 트렌드 추적 및 최적화 전략 수립  

> **⚠️ 비용 주의:** 프리티어 한도를 초과할 경우, 데이터 전송량에 따라 비용이 급격히 증가할 수 있으므로 실시간 모니터링 필수. 특히 DDoS 대응 시 Shield와 Network Firewall 동시 사용 시 비용이 누적될 수 있음.

## 📚 핵심 개념

### 개념 1: **NACL vs 보안 그룹 비교**
NACL(네트워크 액세스 제어 목록)과 보안 그룹은 VPC 내 트래픽 제어를 위한 두 가지 주요 메커니즘입니다. NACL은 **subnet 단위**로 작동하는 **stateless** 규칙 집합이며, 보안 그룹은 **인스턴스 단위**로 작동하는 **stateful** 규칙 집합입니다. NACL은 모든 트래픽을 필터링하지만, 보안 그룹은 방화벽 규칙을 기반으로 트래픽을 허용/차단합니다. NACL은 기본적으로 모든 트래픽을 거부하고, 보안 그룹은 수동으로 허용 규칙을 설정해야 합니다. 두 서비스는 서로 보완적인 역할을 하며, 보안 그룹을 우선으로 사용하고 NACL을 보조로 활용하는 것이 일반적입니다.

#### 왜 중요한가?
- **트래픽 제어의 유연성**: 보안 그룹은 동적인 인스턴스에 즉시 적용되며, NACL은 정적 네트워크 구조에 적합합니다.  
- **보안 강화**: 보안 그룹의 stateful 특성은 연결 상태를 유지해 보다 정확한 트래픽 제어가 가능합니다.  
- **사전 설정의 필요성**: NACL은 기본적으로 모든 트래픽을 거부하므로, 명시적인 허용 규칙이 필수적입니다.  

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **Stateful vs Stateless** | 보안 그룹은 연결 상태를 유지(예: 응답 허용), NACL은 모든 트래픽을 별도로 검사 | 인스턴스 A에서 B로의 요청이 허용되면, 응답도 자동 허용 |
| **스코프** | 보안 그룹은 인스턴스 단위, NACL은 서브넷 단위 | 서브넷 내 모든 인스턴스에 동일한 NACL 적용 |
| **사용 시나리오** | NACL은 네트워크 레벨 보안, 보안 그룹은 인스턴스 레벨 보안 | 서브넷 전체의 외부 트래픽 차단, 특정 인스턴스의 내부 서비스 접근 제어 |

> **💡 Tip:** 실무에서는 보안 그룹을 우선으로 설정하고, NACL을 보조로 사용해 트래픽을 더 세밀하게 제어하세요. 예를 들어, 보안 그룹으로 특정 포트를 허용하고, NACL로 IP 범위를 추가 필터링하는 방식입니다.

---

### 개념 2: **VPC 흐름 로그**
VPC 흐름 로그는 VPC 내에서 흐르는 모든 IP 트래픽을 기록하는 기능입니다. 이 로그는 VPC 내에서의 트래픽 패턴을 분석하고, 네트워크 문제를 진단하거나 보안 위협을 탐지하는 데 유용합니다. 로그는 AWS CloudWatch 또는 AWS Flow Logs를 통해 분석할 수 있으며, 로그는 **IP 주소**, **포트**, **트래픽 방향**, **타임스탬프** 등의 정보를 포함합니다. 이 기능은 네트워크 보안 및 성능 최적화에 필수적인 도구입니다.

#### 작동 원리
1. **트래픽 모니터링**: VPC 내 모든 IP 트래픽이 네트워크 인터페이스에서 캡처됩니다.  
2. **로그 생성**: 캡처된 트래픽 데이터가 로그 스트림으로 저장됩니다.  
3. **분석 및 대응**: 로그는 CloudWatch에서 시각화되거나, AWS Lambda와 통합해 자동화된 보안 대응을 수행합니다.  

> **💡 Tip:** VPC 흐름 로그는 보안 분석에 필수적이므로, 모든 VPC에 활성화하고, 로그 보존 기간을 최소 1년 이상으로 설정하는 것이 좋습니다.

---

### 개념 3: **Network Firewall**
AWS Network Firewall은 AWS에서 제공하는 **관리형 방화벽 서비스**로, VPC 내외의 네트워크 트래픽을 검사하고 제어합니다. 이 서비스는 **stateful** 및 **stateless** 방식의 규칙을 지원하며, AWS WAF, AWS Shield 등과 연동해 보다 강력한 보안을 제공합니다. Network Firewall은 고가용성, 확장성, 그리고 실시간 트래픽 분석 기능을 갖추고 있어, 클라우드 환경에서 네트워크 보안을 중앙집중식으로 관리하는 데 적합합니다.

#### 주요 특징
1. **Stateful Inspection**: 연결 상태를 유지해 응답 트래픽을 자동으로 허용합니다. 예를 들어, HTTP 요청에 대한 응답은 규칙에 명시된 경우에만 허용됩니다.  
2. **Integration with AWS Services**: WAF로 DDoS 공격을 차단하거나, Shield으로 네트워크 위협을 대응할 수 있습니다.  
3. **Scalability and Performance**: 고성능을 기반으로 대규모 트래픽을 처리하며, 클라우드 환경에서 유연한 확장이 가능합니다.  

> **💡 Tip:** Network Firewall은 기존 방화벽과 비교해 AWS 서비스 간 연동이 용이하며, 특히 다중 VPC 환경에서 중앙집중식 보안 정책을 관리할 때 유용합니다. 예를 들어, 모든 VPC에 동일한 방화벽 규칙을 적용해 보안 정책을 일관되게 유지할 수 있습니다.

## 🖥️ AWS 콘솔에서 Network Firewall 사용하기

### Step 1: Network Firewall 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - 계정 및 지역 선택 후 "VPC" 서비스로 이동합니다.  
2. 상단 검색창에서 **"Network Firewall"**을 입력하고, 결과에서 해당 서비스를 클릭합니다.  
3. **"Network Firewall" 대시보드**가 열리면 정상적으로 서비스에 접속한 것입니다.  

> **📸 화면 확인:**  
> - 왼쪽 메뉴에서 **"Network Firewall"**이 표시되는지 확인합니다.  
> - 대시보드 상단에 **"Firewall"** 또는 **"Rule Group"** 관련 탭이 존재하는지 확인합니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **VPC 네트워크 ACL 생성**  
   - 왼쪽 메뉴에서 **"Network ACLs"**를 클릭합니다.  
   - **"Create network ACL"** 버튼을 클릭하여 새 리소스를 생성합니다.  
   - **Name** 필드에 `My-NACL` 등 유의미한 이름을 입력하고, **VPC**를 선택합니다.  
2. **Ingress/Egress 규칙 설정**  
   - **Ingress** 탭에서 **"Allow"** 방향을 선택하고, **Protocol**(`TCP`), **Port Range**(`22`)를 입력합니다.  
   - **Egress** 탭에서 **"Deny"** 방향을 선택하고, **IP Range**(`0.0.0.0/0`)를 입력하여 모든 트래픽 차단합니다.  
3. **리소스 확인**  
   - **"Create"** 버튼을 클릭한 후, 생성된 NACL이 **"Network ACLs"** 목록에 정상적으로 표시되는지 확인합니다.  

> **📸 화면 확인:**  
> - **"Network ACLs"** 목록에 생성된 NACL이 표시되고, **"Ingress"** 및 **"Egress"** 탭에서 규칙이 구성된 상태인지 확인합니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **규칙 순서 조정**  
   - 규칙은 **상단부터 하단으로 순차적으로 적용**됩니다. 예를 들어, SSH 허용 규칙을 먼저 추가해야 합니다.  
   - **"Edit"** 버튼을 클릭하여 규칙 순서를 조정합니다.  
2. **기본 규칙 추가**  
   - 모든 트래픽을 거부하려면 **"Default"** 탭에서 **"Deny all"** 옵션을 선택합니다.  
3. **정책 적용 확인**  
   - **"Apply"** 버튼을 클릭하여 변경 사항을 저장합니다.  

> **⚠️ 주의:**  
> - 규칙이 **중복되어 적용되면 예상치 못한 허용/차단이 발생할 수 있습니다**.  
> - **"Default"** 규칙은 항상 마지막에 정의해야 하며, **0.0.0.0/0** 대상으로 설정해야 합니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **리소스 상태 확인**  
   - **"Network ACLs"** 목록에서 생성된 NACL의 **"Status"**가 **"active"**인지 확인합니다.  
   - **"Actions"** 메뉴에서 **"View rules"**를 선택해 규칙이 정확히 구성되었는지 확인합니다.  
2. **트래픽 테스트**  
   - **SSH**를 통해 VPC 내 서버에 연결해보세요.  
   - **curl** 또는 **telnet** 명령어로 특정 포트(예: 80)의 접근성을 테스트합니다.  
3. **VPC 흐름 로그 확인**  
   - **"VPC Flow Logs"** 서비스에서 트래픽이 정상적으로 필터링되는지 확인합니다.  
   - 로그에서 **"Accept"** 또는 **"Reject"** 상태가 표시되는지 확인하여 NACL이 작동하는지 검증합니다.  

> **💡 Tip:**  
> - NACL은 **VPC 레벨에서 작동**하므로, 서버와 클라이언트가 동일한 VPC에 있어야 합니다.  
> - **AWS CLI**로 NACL을 관리할 수 있습니다:  
>   ```bash
>   aws ec2 create-network-acl --vpc-id vpc-1234567890abcdef0 --tags Key=Name,Value=My-NACL
>   ```  
> - **CLI로 규칙 추가**:  
>   ```bash
>   aws ec2 create-network-acl-rule --network-acl-id acl-1234567890abcdef0 --rule-number 100 --priority 100 --action allow --protocol tcp --port-range From=22,To=22 --rule-type ingress
>   ```

## ⌨️ AWS CLI로 Network Firewall 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**필수 사전 조건:**
- AWS CLI 버전 1.28.0 이상 (Network Firewall 지원)
- IAM 역할에 `network-firewall:DescribeFirewall`, `network-firewall:CreateFirewall` 권한 부여
- VPC 및 서브넷 리소스 생성 완료

---

### 예제 1: Network Firewall 리소스 조회
```bash
# [Network Firewall 리소스 목록 조회]
aws network-firewall list-firewalls \
    --query 'Firewalls[*].FirewallName' \
    --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| --query | 결과 필터링 | 'Firewalls[*].FirewallName' |
| --output | 출력 형식 | json, table, text |

**예상 출력:**
```
| FirewallName     |
|------------------|
| Example-Firewall |
```

**참고:**  
`list-firewalls`는 생성된 모든 Network Firewall 리소스를 표시합니다.  
`--query`는 특정 필드만 추출할 수 있습니다.

---

### 예제 2: Network Firewall 리소스 생성
```bash
# [Network Firewall 생성]
aws network-firewall create-firewall \
    --firewall-name "Example-Firewall" \
    --firewall-policy "arn:aws:network-firewall:ap-northeast-1:123456789012:firewall-policy/Example-Policy" \
    --subnet-id "subnet-12345678" \
    --vpc-id "vpc-12345678"
```

**필수 옵션:**
- `--firewall-name`: 유일한 리소스 이름
- `--firewall-policy`: 사용할 보안 정책 ARN
- `--subnet-id`: 적용할 서브넷 ID
- `--vpc-id`: 연결할 VPC ID

**예상 출력:**
```json
{
    "Firewall": {
        "FirewallName": "Example-Firewall",
        "FirewallId": "fw-12345678",
        "Status": "ACTIVE"
    }
}
```

**주의사항:**  
- 보안 정책 ARN은 `aws network-firewall list-firewall-policies`로 확인 가능
- 서브넷은 최소 2개 이상 필요 (HA 구성)

---

### 예제 3: Network Firewall 리소스 수정
```bash
# [Network Firewall 정책 변경]
aws network-firewall update-firewall \
    --firewall-id "fw-12345678" \
    --update-firewall "{\"FirewallPolicy\":\"arn:aws:network-firewall:ap-northeast-1:123456789012:firewall-policy/Updated-Policy\"}"
```

**수정 가능한 옵션:**
- `--firewall-policy`: 보안 정책 변경
- `--description`: 설명 수정

**출력 예시:**
```
{
    "Firewall": {
        "FirewallId": "fw-12345678",
        "Status": "UPDATING"
    }
}
```

---

### 예제 4: Network Firewall 리소스 삭제
```bash
# [Network Firewall 삭제]
aws network-firewall delete-firewall --firewall-id "fw-12345678"

# 삭제 확인
aws network-firewall describe-firewall --firewall-id "fw-12345678"
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. 삭제 후 `describe-firewall`는 오류 발생

**삭제 후 상태 확인:**
```
{
    "Error": {
        "Code": "ResourceNotFoundException",
        "Message": "Firewall 'fw-12345678' not found"
    }
}
```

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws network-firewall list-firewalls
aws network-firewall describe-firewall --firewall-id "id"

# 생성
aws network-firewall create-firewall \
    --firewall-name "name" \
    --firewall-policy "arn" \
    --subnet-id "subnet-id" \
    --vpc-id "vpc-id"

# 수정
aws network-firewall update-firewall \
    --firewall-id "id" \
    --update-firewall "{\"FirewallPolicy\":\"arn\"}"

# 삭제
aws network-firewall delete-firewall --firewall-id "id"
```

---

### 💡 비용 및 프리티어 활용 팁
| 항목 | 비용 | 프리티어 |
|-----|-|-|
| Network Firewall | 0.01~0.15 USD/시간 (AWS 요금 페이지 참조) | 1개 리소스 7일 무료 |
| 보안 정책 | 0.01 USD/시간 | 1개 정책 7일 무료 |
| VPC 흐름 로그 | 0.01 USD/GB | 1GB 무료 |

**프리티어 사용 방법:**
1. AWS Console > Network Firewall 생성
2. "Free Tier" 옵션 선택
3. 7일간 사용 가능 (리소스 삭제 시 자동 종료)

---

### 📌 관련 서비스 비교표
| 서비스 | 기능 | Network Firewall vs |
|-------|-----|----------------------|
| NACL | VPC 수준 보안 | 보안 그룹보다 우선 적용 |
| 보안 그룹 | EC2/lambda 보호 | Network Firewall보다 세부 조절 가능 |
| WAF | HTTP 요청 필터링 | Network Firewall 보다 웹 트래픽 대응 |
| Shield | DDoS 대응 | Network Firewall 보다 네트워크 레벨 보호 |

**Network Firewall 강점:**
- VPC 레벨 보안 적용
- 보안 정책 기반 동적 규칙 관리
- 실시간 트래픽 모니터링

**추천 사용 시나리오:**
1. VPC 전체 보안 강화
2. 보안 그룹 보다 더 세밀한 통제 필요
3. 보안 정책 재사용 시 효율적인 관리

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Network Firewall 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **포인트 1**: **Network Firewall의 핵심 목적 및 사용 사례**  
   - 설명: Network Firewall은 VPC 내외부 트래픽을 필터링해 보안 위협을 차단하는 서비스입니다. 시험에서는 **VPC 내부 네트워크 보안**을 구현할 때 Network Firewall을 선택해야 하는 시나리오를 묻는 경우가 많습니다.  
   - 키워드: `VPC 보안`, `트래픽 필터링`, `내부 네트워크 보호`

2. **포인트 2**: **Network Firewall vs WAF/Shield의 차이점**  
   - 설명: Network Firewall은 네트워크 레이어에서 트래픽을 필터링하는 반면, WAF는 애플리케이션 레이어에서 공격을 차단합니다. 시험에서는 **레이어별 보안 서비스 선택**을 묻는 경우가 많습니다.  
   - 키워드: `네트워크 레이어`, `애플리케이션 레이어`, `WAF`, `Shield`

3. **포인트 3**: **Network Firewall의 구성 요소 및 규칙 설정**  
   - 설명: **Rule Group**, **Stateful Inspection**, **Log Insights** 등은 Network Firewall의 핵심 구성 요소입니다. 시험에서는 규칙의 작동 방식이나 로그 분석 방법을 묻는 문제가 자주 등장합니다.  
   - 키워드: `Rule Group`, `Stateful Inspection`, `Log Insights`

4. **포인트 4**: **Network Firewall의 확장성 및 비용 구조**  
   - 설명: Network Firewall은 **AWS Managed Services**로, 확장성과 가격 경계가 명확합니다. 시험에서는 **비용 계산**이나 **확장성 제한**을 묻는 문제가 출제됩니다.  
   - 키워드: `AWS Managed`, `비용 계산`, `확장성 제한`

5. **포인트 5**: **Network Firewall의 제한 사항 및 한계**  
   - 설명: Network Firewall은 **VPC 내부 트래픽만 대상**으로 하며, 외부 공격 대응에는 WAF/Shield가 더 적합합니다. 시험에서는 **사용 범위 제한**을 묻는 문제가 자주 등장합니다.  
   - 키워드: `VPC 내부 트래픽`, `외부 공격 대응`, `사용 범위 제한`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | Network Firewall과 WAF를 혼동해 규칙 적용 범위를 잘못 파악할 수 있음 | **WAF는 애플리케이션 레이어, Network Firewall은 네트워크 레이어**에서 작동 |
| 함정 2 | Network Firewall의 로그 분석 기능을 보안 그룹의 VPC Flow Logs와 혼동할 수 있음 | **Network Firewall의 Log Insights는 실시간 분석, VPC Flow Logs는 저장형 로그** |
| 함정 3 | Network Firewall의 비용 구조를 AWS Lambda 또는 EC2 기반 서비스와 비교해 혼동할 수 있음 | **Network Firewall은 고정 비용, 확장 시 유동 비용** |

#### 🔄 Network Firewall vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | Network Firewall | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| **용도** | VPC 내외부 트래픽 필터링 | WAF (애플리케이션 레이어), Shield (DDoS 방어) | **네트워크 레이어 보안 요구 시** |
| **확장성** | 클라우드 기반, 규모에 따라 자동 확장 | EC2 기반 서비스는 수동 확장 필요 | **고가용성 및 확장성 요구 시** |
| **비용** | 고정 비용 + 유동 비용 | EC2 기반 서비스는 인스턴스 비용 추가 | **비용 예측이 필요한 경우** |
| **지연시간** | AWS Managed로 최소화됨 | EC2 기반 서비스는 네트워크 지연 발생 | **실시간 보안 검사가 필요한 경우** |

#### 📝 시험 대비 체크리스트
- [ ] Network Firewall의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  > "VPC 내외부 트래픽을 필터링해 네트워크 보안을 강화하는 서비스입니다."  
- [ ] Network Firewall를 선택해야 하는 시나리오를 알고 있는가?  
  > "내부 네트워크 보호, 공격 트래픽 차단, 규칙 기반 필터링이 필요한 경우"  
- [ ] Network Firewall의 제한사항/한계를 알고 있는가?  
  > "VPC 내부 트래픽만 대상, 외부 공격 대응에는 WAF/Shield가 더 적합"  
- [ ] Network Firewall와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  > "WAF는 애플리케이션 레이어, Shield는 DDoS 방어, EC2 기반은 수동 확장 필요"  
- [ ] Network Firewall의 비용 구조를 이해하고 있는가?  
  > "고정 비용 + 유동 비용, AWS Managed로 예측 가능"  

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Network Firewall를 떠올리세요:  
> - **VPC 내부 보안**  
> - **트래픽 필터링**  
> - **규칙 기반 방어**  
> - **네트워크 레이어 보안**  
> - **AWS Managed Services**

---

| [⬅️ VPC Flow Logs](./VPC-Flow-Logs.md) | [📑 Day 3 목차](./README.md) | [🏠 Week 2](../README.md) | [다음 Day ➡️](../day4/README.md) |

---
