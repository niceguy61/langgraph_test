---

| [⬅️ VPC Peering](./VPC-Peering.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 2](../README.md) | [VPC Endpoints ➡️](./VPC-Endpoints.md) |

---

# Transit Gateway 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** Transit Gateway는 VPC 및 기타 네트워크를 중앙 집중식으로 연결하기 위한 AWS의 네트워크 연결 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 여러 VPC 간 연결 시 VPC 피어링을 반복적으로 설정해야 하며, 라우팅 테이블 관리가 복잡합니다. 예: 10개 VPC를 연결하려면 45개의 피어링 연결을 구성해야 합니다.  
- **문제 2:** 네트워크 보안이 약합니다. VPC 피어링은 공개 인터넷을 통해 트래픽이 흐를 수 있어, 민감한 데이터 전송 시 위험성이 있습니다.  
- **문제 3:** 하이브리드 클라우드 환경에서 온프레미스 네트워크와 VPC를 연결할 때 복잡한 구성이 필요합니다. 예: Direct Connect 설정과 라우터 구성이 병렬로 진행됩니다.  

**Transit Gateway로 해결:**
- **해결 1:** 중앙 집중형 게이트웨이를 통해 모든 VPC와 네트워크를 단일 포인트에서 연결해, 라우팅 관리가 간소화됩니다.  
- **해결 2:** VPC 게이트웨이 엔드포인트나 PrivateLink를 통해 민감한 데이터 전송 시 네트워크 보안을 강화합니다.  
- **해결 3:** Direct Connect와 연동해 온프레미스 네트워크와 VPC 간 전용 네트워크 인터페이스를 구성해, 하이브리드 클라우드 환경을 안정적으로 운영합니다.  

### 비유로 이해하기
**예:** 도시의 교통망을 생각해보세요. 기존에는 각 지역 간 직접 도로를 연결해야 하지만, Transit Gateway는 모든 지역을 연결하는 중앙 교차로 역할을 합니다. 이 중앙 교차로는 보안 체계를 갖춘 빌딩으로, 민감한 정보는 내부 네트워크만 통과하고, 외부와의 연결은 제한적으로 설정할 수 있습니다. 또한, 이 중앙 교차로는 온프레미스의 물리적 라우터와도 직접 연결되어, 기업의 전체 네트워크를 통합적으로 관리할 수 있습니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 다중 VPC 간 통합 네트워크 연결 | 기업의 개발, 테스트, 운영 환경을 별도 VPC로 구분해도, 모든 VPC가 공유 데이터베이스나 스토리지에 접근해야 할 때 |
| 시나리오 2 | 하이브리드 클라우드 환경 구축 | 온프레미스 데이터 센터와 AWS VPC 간 전용 네트워크 연결이 필요할 때 |
| 시나리오 3 | 민감한 데이터 전송 보안 강화 | 내부 시스템 간 데이터 전송 시 외부 인터넷을 통한 보안 위험을 줄이고자 할 때 |

**이럴 때 Transit Gateway를 선택하세요:**
- ✅ 상황 1: 10개 이상의 VPC 간 연결이 필요한 복잡한 네트워크 아키텍처  
- ✅ 상황 2: 하이브리드 클라우드 환경에서 온프레미스와 클라우드 간 전용 연결이 필요한 경우  
- ✅ 상황 3: 내부 시스템 간 데이터 전송 시 네트워크 보안을 최우선으로 고려해야 할 때  

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ 상황 → VPC 피어링: 단일 VPC 간 연결만 가능하며, 복잡한 네트워크 구성 시 비용이 증가  
- ❌ 상황 → Direct Connect: 하이브리드 클라우드 연결은 가능하지만, 중앙 집중형 연결 기능이 부족  
- ❌ 상황 → Classic VPN: 보안성과 성능이 제한적이며, 라우팅 관리가 복잡  

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| VPC | 중앙 게이트웨이를 통해 VPC 간 네트워크 연결 | VPC1 → Transit Gateway → VPC2 → VPC3 |
| Direct Connect | 온프레미스 네트워크와 VPC 간 전용 연결 | 온프레미스 → Direct Connect → Transit Gateway → VPC |
| PrivateLink | 민감한 데이터 전송 시 프라이빗 연결 | VPC 내 서비스 → PrivateLink → Transit Gateway → 외부 시스템 |

**자주 사용되는 아키텍처 패턴:**
```
User → CloudFront → Transit Gateway → VPC (S3/EC2) → PrivateLink → 내부 시스템
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| 데이터 전송 비용 | $0.10/GB (기본) | 월 10GB 무료 |
| VPC 연결 수 | $0.10/개 (월 기준) | 12개월 동안 50개 무료 |
| Direct Connect 통합 | $0.20/GB | 항상 무료 |

**비용 최적화 팁:**
1. 💡 팁 1: VPC 게이트웨이 엔드포인트를 사용해 데이터 전송 비용을 절감 (인터넷 대신 프라이빗 연결)  
2. 💡 팁 2: 프리티어 기간 동안 최대한 많은 VPC 연결을 구성해 초기 비용을 절감  
3. 💡 팁 3: 하이브리드 클라우드 환경에서 Direct Connect와 연동해 전용 네트워크 인터페이스를 통해 데이터 전송 비용 최소화  

> **⚠️ 비용 주의:** 데이터 전송량이 급증할 경우, 예상치 못한 비용이 발생할 수 있습니다. AWS Cost Explorer를 활용해 사용량을 모니터링하고, 필요 시 VPC 엔드포인트를 추가해 비용을 줄이는 것이 중요합니다.

## 📚 핵심 개념

### 개념 1: **트랜지트 게이트웨이의 중앙 집중형 네트워크 연결**
트랜지트 게이트웨이(TG)는 AWS 환경에서 여러 VPC, 온프레미스 네트워크, Direct Connect 등을 중앙 집중적으로 연결하는 네트워크 인프라입니다. 이는 단일 포인트에서 모든 네트워크 트래픽을 관리함으로써 복잡한 라우팅 테이블 설정과 여러 연결 관리를 단순화합니다. TG는 VPC 피어링, VPN, Direct Connect 등 다양한 연결 방식을 지원하며, 모든 트래픽이 TG를 통해 통과하게 되어 보안 및 관리 효율성을 높입니다. 또한, TG는 AWS Global Accelerator와 같은 서비스와도 통합되어 전 세계 사용자에게 최적화된 네트워크 경로를 제공합니다.

#### 왜 중요한가?
- **확장성**: 수천 개의 VPC와 네트워크를 단일 게이트웨이로 연결해 네트워크 규모를 자유롭게 확장할 수 있습니다.  
- **비용 효율성**: 여러 연결을 별도로 관리하는 대신 TG로 통합하면 라우팅 테이블 관리 및 보안 정책 적용이 용이해져 운영 비용을 절감합니다.  

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **중앙 집중형 라우팅** | 모든 네트워크 트래픽이 TG를 통해 통과하여 라우팅 테이블을 단일 포인트에서 관리 | VPC A → TG → VPC B 통신 시 라우팅 정책을 하나의 장소에서 설정 |
| **다양한 연결 통합** | VPC 피어링, VPN, Direct Connect 등 다양한 연결 방식을 TG에서 통합 관리 | Direct Connect와 VPC 피어링을 동시에 TG에 연결해 단일 네트워크 인터페이스로 관리 |
| **보안 정책 통일** | TG를 통해 모든 트래픽을 필터링 및 보안 정책 적용 가능 | 방화벽 규칙을 TG에서 설정해 모든 연결에 동일한 보안 정책 적용 |

> **💡 Tip:** TG는 복잡한 네트워크 아키텍처를 단순화하고, 라우팅 테이블 관리 및 보안 정책 통일을 통해 운영 효율성을 극대화합니다. 특히, 여러 VPC를 통합 관리해야 할 경우 필수적인 구성 요소입니다.

---

### 개념 2: **VPC 피어링 연결 및 라우팅 테이블 설정**
VPC 피어링은 두 개 이상의 VPC 간 직접적인 네트워크 통신을 가능하게 하는 기능입니다. 이는 라우팅 테이블에 피어링 연결을 설정하고, 각 VPC의 네트워크 주소를 서로 인식하게 함으로써 트래픽을 직접 전달합니다. VPC 피어링은 TG 없이도 가능하지만, TG를 활용하면 여러 VPC를 중앙 집중적으로 관리할 수 있는 추가적인 유연성을 제공합니다. 이 기능은 특정 VPC 간의 고유한 네트워크 요구사항을 충족하는 데 유용합니다.

#### 작동 원리
1. **피어링 연결 생성**: 두 VPC 간에 피어링 연결을 생성하고, 각 VPC의 라우팅 테이블에 상대방의 CIDR을 정적 라우트로 추가합니다.  
2. **트래픽 전달**: 피어링 연결을 통해 설정된 라우팅 경로에 따라 VPC 간 직접 통신이 이루어집니다.  
3. **보안 및 관리**: 각 VPC의 방화벽 규칙과 라우팅 정책을 개별적으로 설정해 네트워크 보안을 강화합니다.

> **💡 Tip:** VPC 피어링은 단순한 네트워크 연결을 위한 기능이지만, TG와 결합하면 복잡한 네트워크 아키텍처에서 유연한 관리를 가능하게 합니다. 예를 들어, 두 VPC 간의 고정 IP 주소 기반의 직접 통신이 필요한 경우에 적합합니다.

---

### 개념 3: **VPC 게이트웨이 엔드포인트 vs 인터페이스 엔드포인트 차이**
VPC 엔드포인트는 AWS 서비스에 대한 프라이빗 연결을 제공하는 기능으로, **게이트웨이 엔드포인트**와 **인터페이스 엔드포인트**로 구분됩니다. 게이트웨이 엔드포인트는 S3, DynamoDB 등 특정 서비스에 대한 트래픽을 중앙 집중적으로 처리하는 반면, 인터페이스 엔드포인트는 VPC 내부의 EC2 인스턴스나 Lambda 함수 등에 직접 연결하여 특정 서비스에 대한 프라이빗 액세스를 제공합니다. 이 두 유형은 네트워크 요구사항에 따라 선택적으로 사용됩니다.

#### 주요 특징
1. **게이트웨이 엔드포인트**:  
   - AWS 서비스(예: S3, DynamoDB)에 대한 프라이빗 액세스를 제공합니다.  
   - 라우팅 테이블에서 단일 라우트 규칙으로 서비스에 대한 접근을 설정할 수 있습니다.  

2. **인터페이스 엔드포인트**:  
   - VPC 내부의 EC2 인스턴스나 Lambda 함수 등에 직접 연결해 특정 서비스에 접근합니다.  
   - 네트워크 성능 최적화를 위해 인스턴스에 직접 연결하여 라우팅 오버헤드를 줄입니다.  

3. **보안 및 관리**:  
   - 게이트웨이 엔드포인트는 서비스별로 별도의 보안 정책을 설정할 수 있습니다.  
   - 인터페이스 엔드포인트는 VPC 내부에서의 네트워크 보안을 강화하며, 외부 공격으로부터 인스턴스를 보호합니다.  

> **💡 Tip:** 게이트웨이 엔드포인트는 특정 서비스에 대한 액세스를 간편하게 제공하지만, 인터페이스 엔드포인트는 VPC 내부에서의 네트워크 최적화 및 보안 강화에 더 적합합니다. 서비스 유형에 따라 적절히 선택해야 합니다.

## 🖥️ AWS 콘솔에서 Transit Gateway 사용하기

### Step 1: Transit Gateway 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - **⚠️ 주의:** AWS 계정이 없으면 [AWS 계정 생성](https://console.aws.amazon.com/console/home?#)으로 이동해 계정을 생성해야 합니다.  
2. 상단 검색창에서 **"Transit Gateway"**를 입력하고, 검색 결과에서 **"Transit Gateway"** 서비스를 클릭합니다.  
   - **💡 Tip:** 검색 시 "Transit Gateway" 서비스가 아닌 "VPC Peering"이나 "VPC" 서비스가 표시될 수 있으니 주의해 주세요.  

> **📸 화면 확인:** Transit Gateway 대시보드가 표시되면 정상입니다. 대시보드에는 생성된 Transit Gateway 목록, 연결 상태, 리소스 정보 등이 표시됩니다.

---

### Step 2: [주요 작업 1 - Transit Gateway 생성]  
1. **Transit Gateway 생성**  
   - **클릭할 버튼:** 대시보드 상단의 **"Create Transit Gateway"** 버튼을 클릭합니다.  
   - **입력해야 할 값:**  
     - **Name tag:** "My-Transit-Gateway" 등 이름을 입력합니다.  
     - **Region:** 사용할 리전을 선택합니다 (예: us-east-1).  
     - **Tags:** 추가 태그를 입력합니다 (예: Project=Network).  
2. **설정 옵션:**  
   - **IPv4/IPv6:** 필요에 따라 IPv4와 IPv6를 활성화합니다.  
   - **Route Tables:** 기본적으로 생성된 Route Table이 자동으로 연결됩니다.  
3. **확인 사항:**  
   - **AWS Free Tier:** Transit Gateway는 AWS Free Tier에 포함되지 않으므로, 비용이 발생할 수 있습니다.  
   - **리전 제한:** Transit Gateway는 생성한 리전에만 생성할 수 있습니다.  

> **📸 화면 확인:** "Create Transit Gateway" 화면에서 이름, 리전, 태그 등을 입력하고, **"Create"** 버튼을 클릭한 후 생성 완료 화면이 표시됩니다.

---

### Step 3: [주요 작업 2 - VPC Peering 설정]  
1. **VPC Peering 연결 생성**  
   - **클릭할 버튼:** 대시보드에서 생성한 Transit Gateway를 선택한 후, **"Peering Connections"** 탭을 클릭합니다.  
   - **설정 옵션:**  
     - **Peering Connection Name:** "My-VPC-Peering" 등 이름을 입력합니다.  
     - **VPC 선택:** 연결할 VPC를 선택합니다 (예: VPC-A와 VPC-B).  
     - **Security Groups:** 보안 그룹을 설정해 트래픽 제어를 합니다.  
2. **Route Table 구성**  
   - **클릭할 버튼:** "Route Tables" 탭에서 Transit Gateway의 라우트 테이블을 선택합니다.  
   - **설정 옵션:**  
     - **Route Propagation:** VPC Peering 연결을 통해 라우트를 자동 전파할 수 있도록 설정합니다.  
     - **Static Routes:** 고정 IP 주소를 통해 트래픽을 라우팅할 수 있습니다.  
3. **확인 사항:**  
   - **VPC 리전:** VPC Peering 연결은 동일한 리전에 있는 VPC만 가능합니다.  
   - **ACL 설정:** VPC Peering 연결 시 ACL(Access Control List)를 설정해 보안을 강화해야 합니다.  

> **⚠️ 주의:** VPC Peering 연결은 생성 후 즉시 작동하지 않을 수 있으므로, 연결 상태와 라우트 테이블을 확인해야 합니다.

---

### Step 4: 설정 확인 및 테스트  
1. **생성된 리소스 확인 방법**  
   - **Transit Gateway 목록:** 대시보드에서 생성한 Transit Gateway의 상태를 확인합니다.  
   - **VPC Peering 연결:** "Peering Connections" 탭에서 연결 상태(예: "Active", "Pending")를 확인합니다.  
2. **상태 확인 방법**  
   - **Route Table 상태:** "Route Tables" 탭에서 라우트 전파 상태를 확인합니다.  
   - **Network ACL:** "Network ACLs" 탭에서 보안 설정을 점검합니다.  
3. **정상 동작 테스트 방법**  
   - **CLI 명령어:**  
     ```bash
     aws ec2 describe-vpc-peering-connections --vpc-peering-connection-id pc-12345678
     ```
     - 이 명령어로 VPC Peering 연결 상태를 확인합니다.  
   - **Ping 테스트:**  
     - VPC 내 EC2 인스턴스에서 다른 VPC의 IP 주소로 `ping` 명령어를 실행해 연결성을 테스트합니다.  

> **💡 Tip:** VPC Peering 연결은 생성 후 최대 15분 이내에 작동할 수 있으므로, 시간이 지나면 자동으로 연결됩니다.  

---

### 📋 요약 체크리스트  
- [ ] Transit Gateway 생성 완료  
- [ ] VPC Peering 연결 설정 완료  
- [ ] 라우트 테이블 상태 확인  
- [ ] 보안 그룹 및 ACL 설정 완료  
- [ ] CLI 및 테스트 명령어로 연결성 확인  
- [ ] 비용 발생 여부 확인 (AWS Free Tier 활용)

## ⌨️ AWS CLI로 Transit Gateway 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**주의 사항:**  
- AWS CLI는 `ec2` 서비스를 통해 Transit Gateway를 관리합니다.  
- 모든 명령어는 `ec2` 서비스 명령어로 실행됩니다.  
- `--region` 파라미터를 명시적으로 추가해야 리전이 제대로 적용됩니다.

---

### 예제 1: Transit Gateway 리소스 조회
```bash
# [Transit Gateway 리소스 목록 조회]
aws ec2 list-transit-gateways --query 'TransitGateways[*].TransitGatewayId' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 필터링 및 특정 필드 선택 | `'TransitGateways[*].TransitGatewayId'` |
| `--output` | 출력 형식 | `json`, `table`, `text` |

**예상 출력:**
```
| TransitGatewayId |
+------------------+
| tgw-1234567890ab |
| tgw-0987654321cd |
```

**팁:**  
- `--query`를 사용해 특정 필드(예: `TransitGateways[*].Tags`)만 조회할 수 있습니다.  
- `--output table`은 테이블 형식으로 결과를 깔끔하게 보여줍니다.

---

### 예제 2: Transit Gateway 리소스 생성
```bash
# [Transit Gateway 생성]
aws ec2 create-transit-gateway \
    --description "My Transit Gateway" \
    --tags Key=Project,Value=Network;Key=Environment,Value=Production
```

**필수 옵션:**
- `--description`: Transit Gateway 설명 (선택 사항)  
- `--tags`: 태그 추가 (필수 아님)  

**예상 출력:**
```json
{
    "TransitGateway": {
        "TransitGatewayId": "tgw-1234567890ab",
        "State": "pending",
        "Tags": [
            {"Key": "Project", "Value": "Network"},
            {"Key": "Environment", "Value": "Production"}
        ]
    }
}
```

**비용 주의사항:**  
- Transit Gateway는 생성 시 기본적으로 1개의 리전에서 실행됩니다.  
- 다른 리전에서 사용하려면 `--region` 파라미터를 추가해야 합니다.

---

### 예제 3: Transit Gateway 리소스 수정
```bash
# [Transit Gateway 태그 수정]
aws ec2 update-transit-gateway \
    --transit-gateway-id tgw-1234567890ab \
    --tags Key=Environment,Value=Production;Key=Team,Value=DevOps
```

**수정 가능한 옵션:**  
- `--tags`: 태그 추가/수정  
- `--description`: 설명 수정 (AWS CLI는 `--description` 파라미터를 직접 지원하지 않음)  

**경고:**  
- `--tags` 파라미터는 **추가**만 가능하며, 기존 태그를 수정하려면 삭제 후 다시 추가해야 합니다.

---

### 예제 4: Transit Gateway 리소스 삭제
```bash
# [Transit Gateway 삭제]
aws ec2 delete-transit-gateway --transit-gateway-id tgw-1234567890ab

# 삭제 확인
aws ec2 describe-transit-gateways --transit-gateway-ids tgw-1234567890ab
```

**삭제 후 상태 확인:**  
- 삭제 완료 시 `State`가 `deleted`로 표시됩니다.  
- 삭제된 리소스는 복구할 수 없으므로, **최종적으로 `delete` 명령어를 실행하기 전에 신중하게 확인**해야 합니다.

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws ec2 list-transit-gateways --query 'TransitGateways[*].TransitGatewayId' --output table
aws ec2 describe-transit-gateways --transit-gateway-ids tgw-1234567890ab

# 생성
aws ec2 create-transit-gateway --description "My TGW" --tags Key=Project,Value=Network

# 수정
aws ec2 update-transit-gateway --transit-gateway-id tgw-1234567890ab --tags Key=Team,Value=DevOps

# 삭제
aws ec2 delete-transit-gateway --transit-gateway-id tgw-1234567890ab
```

**CLI 사용 팁:**  
- `--region` 파라미터를 명시적으로 추가해 리전 오류를 방지하세요.  
- `--query`를 사용해 필요한 데이터만 필터링해 효율적으로 작업하세요.  
- 태그 관리를 통해 리소스를 쉽게 분류하고 관리하세요.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Transit Gateway 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **포인트 1**:  
   - 설명: **트랜지트 게이트웨이의 중앙 집중형 네트워크 연결**은 여러 VPC 간의 통합 연결을 제공하며, 라우팅 테이블을 통한 유연한 네트워크 관리가 시험에서 주요 포인트입니다. 이는 복잡한 네트워크 아키텍처를 단순화하고, 중앙 집중식 관리를 통해 효율적인 트래픽 흐름을 가능하게 합니다.  
   - 키워드: `중앙 집중형 연결`, `VPC 통합`, `라우팅 테이블`

2. **포인트 2**:  
   - 설명: **VPC 게이트웨이 엔드포인트 vs 인터페이스 엔드포인트**의 차이는 시험에서 반드시 구분해야 하는 개념입니다. 게이트웨이 엔드포인트는 외부 서비스에 접근하는 데 사용되며, 인터페이스 엔드포인트는 VPC 내부에서 AWS 서비스에 직접 연결하는 방식으로, 각각의 사용 사례와 구성 방법을 이해해야 합니다.  
   - 키워드: `게이트웨이 엔드포인트`, `인터페이스 엔드포인트`, `AWS 서비스 연결`

3. **포인트 3**:  
   - 설명: **PrivateLink의 프라이빗 연결 구조**는 VPC 내부에서 외부 서비스에 안전하게 접근할 수 있도록 하며, 이는 네트워크 보안과 데이터 유출 방지를 위한 핵심 개념입니다. 시험에서는 PrivateLink의 작동 원리와 사용 사례를 묻는 경우가 많습니다.  
   - 키워드: `PrivateLink`, `프라이빗 연결`, `네트워크 보안`

4. **포인트 4**:  
   - 설명: **트랜지트 게이트웨이의 확장성과 비용 효율성**은 시험에서 자주 출제되는 주제입니다. 여러 VPC를 단일 포인트에서 관리할 수 있어 확장성이 뛰어나며, 이는 Direct Connect나 VPN 대비 비용 절감 효과를 제공합니다.  
   - 키워드: `확장성`, `비용 효율성`, `Direct Connect`

5. **포인트 5**:  
   - 설명: **트랜지트 게이트웨이의 제한사항 및 한계**는 시험에서 주의 깊게 다루는 내용입니다. 예를 들어, 하이브리드 네트워크 연결을 지원하지 않거나, 특정 지역 간 연결 제한 등이 시험에서 자주 다루는 주제입니다.  
   - 키워드: `제한사항`, `지역 제한`, `하이브리드 연결`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | **VPC 피어링 vs 트랜지트 게이트웨이의 혼동** - "VPC 피어링은 두 VPC 간 직접 연결"이라는 설명에, 트랜지트 게이트웨이는 중앙 집중식 연결을 제공하므로 혼동할 수 있습니다. | **트랜지트 게이트웨이는 여러 VPC를 통합 연결** |
| 함정 2 | **엔드포인트 유형 혼동** - "게이트웨이 엔드포인트는 외부 서비스 접근"이라는 설명에, 인터페이스 엔드포인트는 VPC 내부에서 AWS 서비스에 직접 연결하므로 혼동할 수 있습니다. | **인터페이스 엔드포인트는 VPC 내부에서 AWS 서비스 접근** |
| 함정 3 | **비용 구조 혼동** - "트랜지트 게이트웨이는 단일 포인트 관리"라는 설명에, Direct Connect는 고정비용이지만, 트랜지트 게이트웨이는 트래픽 기반 요금이므로 혼동할 수 있습니다. | **트랜지트 게이트웨이는 트래픽 기반 요금, Direct Connect는 고정비용** |

#### 🔄 Transit Gateway vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | Transit Gateway | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 여러 VPC 간 중앙 집중형 연결 | VPC 피어링 (두 VPC 간 직접 연결) | **여러 VPC 통합 연결 필요 시** |
| 확장성 | 수백 개 VPC까지 연결 가능 | VPC 피어링 (1:1 연결 제한) | **확장성 요구 시** |
| 비용 | 트래픽 기반 요금 | Direct Connect (고정비용) | **트래픽량에 따라 비용 변동 시** |
| 지연시간 | 중간 지연 | Direct Connect (저지연) | **저지연 요구 시** |

#### 📝 시험 대비 체크리스트
- [ ] Transit Gateway의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  *예: "여러 VPC 간 중앙 집중형 네트워크 연결을 제공합니다."*
- [ ] Transit Gateway를 선택해야 하는 시나리오를 알고 있는가?  
  *예: "여러 VPC 통합 연결, 확장성 요구 시"*
- [ ] Transit Gateway의 제한사항/한계를 알고 있는가?  
  *예: "하이브리드 연결 지원 없음, 특정 지역 제한"*
- [ ] Transit Gateway와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  *예: "VPC 피어링 vs 트랜지트 게이트웨이: 1:1 연결 vs 중앙 집중형 연결"*
- [ ] Transit Gateway의 비용 구조를 이해하고 있는가?  
  *예: "트래픽 기반 요금, 단일 포인트 관리로 비용 절감"*

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Transit Gateway를 떠올리세요:  
> - **중앙 집중형 연결**  
> - **VPC 통합**  
> - **라우팅 테이블**  
> - **확장성**  
> - **트래픽 기반 요금**

---

| [⬅️ VPC Peering](./VPC-Peering.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 2](../README.md) | [VPC Endpoints ➡️](./VPC-Endpoints.md) |

---
