---

| [⬅️ Transit Gateway](./Transit-Gateway.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 2](../README.md) | [PrivateLink ➡️](./PrivateLink.md) |

---

# VPC Endpoints 완전 정복

> **한 줄 정의:** VPC Endpoints는 AWS 서비스에 대한 내부 네트워크에서의 안전한 접근을 위한 AWS의 네트워크 인터페이스 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**  
- **문제 1:** VPC 내 리소스가 AWS 서비스(예: S3, DynamoDB)에 접근할 때 공개 인터넷을 통해 통신해야 했으며, 이로 인해 데이터 유출 및 보안 위험이 발생했습니다.  
- **문제 2:** 다중 VPC 간 통신 시 라우팅 테이블 설정이 복잡해져 관리 비용이 증가했습니다.  
- **문제 3:** 온프레미스 데이터 센터와 VPC 간 전용 연결을 설정할 때 복잡한 네트워크 구성이 필요했습니다.  

**VPC Endpoints로 해결:**  
- **해결 1:** VPC 내 리소스가 AWS 서비스에 직접 연결되어 공개 인터넷을 통해 통신하지 않도록 해 데이터 보안을 강화합니다.  
- **해결 2:** 게이트웨이 엔드포인트를 통해 VPC 간 라우팅을 간소화하고, 인터페이스 엔드포인트로 특정 서비스에 대한 액세스를 제어할 수 있습니다.  
- **해결 3:** Direct Connect와 결합해 온프레미스 데이터 센터와 VPC 간 전용 네트워크를 구성해 지연 시간을 최소화합니다.  

### 비유로 이해하기  
VPC Endpoints는 고속도로 대신 개인 차량을 통해 목적지에 도달하는 비유입니다. 기존에는 모든 차량이 동일한 고속도로를 사용해 목적지에 도달해야 했지만, VPC Endpoints는 VPC 내 리소스가 AWS 서비스에 직접 연결되어 고속도로를 사용하지 않아도 목적지를 효율적으로 달성할 수 있게 합니다. 이는 데이터 유출 위험을 줄이고, 네트워크 관리의 복잡성을 낮추는 역할을 합니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | VPC 내 리소스가 AWS 서비스(예: S3)에 보안적으로 접근해야 할 때 | 기업이 S3에 저장된 고객 데이터를 VPC 내 애플리케이션에서 안전하게 읽어들이는 경우 |
| 시나리오 2 | 다중 VPC 간 통신을 간소화하고자 할 때 | 분산된 팀이 서로 다른 VPC에서 작업하면서 데이터를 공유해야 하는 경우 |
| 시나리오 3 | 온프레미스 데이터 센터와 VPC 간 전용 네트워크 연결을 원할 때 | 회사가 온프레미스 DB와 VPC 내 애플리케이션을 전용 연결로 통신하는 경우 |

**이럴 때 VPC Endpoints를 선택하세요:**  
- ✅ 상황 1: AWS 서비스에 대한 내부 네트워크 접근이 필요할 때  
- ✅ 상황 2: VPC 간 통신 복잡성을 줄이고자 할 때  
- ✅ 상황 3: 온프레미스 시스템과 VPC 간 안정적인 연결이 필요할 때  

**이럴 때는 다른 서비스를 고려하세요:**  
- ❌ 상황: VPC 간 직접 연결이 필요할 때 → **VPC Peering** (단일 방향 연결 제한)  
- ❌ 상황: 다수의 VPC와 연결해야 할 때 → **Transit Gateway** (중앙 집중형 관리)  
- ❌ 상황: 서브넷 간 통신이 필요할 때 → **VPC Peering** (서브넷 수 제한)

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **VPC Peering** | VPC 간 직접 연결을 보완해 VPC Endpoints와 함께 사용 | 예: VPC A → VPC Peering → VPC B → VPC Endpoints → AWS 서비스 |
| **Transit Gateway** | 중앙 집중형 네트워크 관리를 위해 VPC Endpoints와 결합 | 예: Transit Gateway → VPC Endpoints → AWS 서비스 |
| **PrivateLink** | VPC 내 리소스가 AWS 서비스에 대한 프라이빗 연결을 설정 | 예: VPC Endpoints → PrivateLink → AWS 서비스 |

**자주 사용되는 아키텍처 패턴:**  
```
[사용자] → [VPC Endpoints] → [AWS 서비스]  
[사용자] → [Direct Connect] → [온프레미스] ↔ [VPC Endpoints] → [AWS 서비스]  
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **게이트웨이 엔드포인트** | $0.10/일 (단위: 1일) | 12개월 무료 |
| **인터페이스 엔드포인트** | $0.10/일 (단위: 1일) | 12개월 무료 |
| **Direct Connect** | $0.10/일 (단위: 1일) | 월 100GB 무료 |

**비용 최적화 팁:**  
1. 💡 팁 1: 프리티어를 활용해 초기 사용량을 무료로 사용하고, 필요 시 유연한 확장 전략을 수립하세요.  
2. 💡 팁 2: 인터페이스 엔드포인트를 사용해 특정 서비스에 대한 액세스를 제한해 불필요한 트래픽을 줄이세요.  
3. 💡 팁 3: Direct Connect와 결합해 전용 네트워크를 구성해 대규모 데이터 전송 시 비용을 절감하세요.  

> **⚠️ 비용 주의:** Direct Connect 요금은 데이터 전송량에 따라 달라지므로, 대규모 데이터 전송 시 예상 비용을 미리 계산해 두세요.

## 📚 핵심 개념

### 개념 1: VPC 엔드포인트의 역할과 중요성  
VPC 엔드포인트는 AWS 서비스(예: S3, DynamoDB)나 내부 리소스 간의 **프라이빗 네트워크 통신을 가능하게 하는 기술**입니다. 이는 외부 인터넷을 통한 트래픽을 피하고, 보안성과 비용 효율성을 높이는 데 중점을 둡니다. VPC 엔드포인트는 **게이트웨이 엔드포인트**(S3, DynamoDB 등)와 **인터페이스 엔드포인트**(VPC 내 리소스 간 통신)로 구분됩니다. 게이트웨이 엔드포인트는 AWS 서비스에 직접 연결되며, 인터페이스 엔드포인트는 VPC 내에서의 서비스 통신을 지원합니다. 이는 **인터넷 게이트웨이 없이도 리소스 간 통신**을 가능하게 하여, 네트워크 보안과 데이터 전송 비용을 절감합니다.  

#### 왜 중요한가?  
- **보안 강화**: 외부 인터넷을 통한 공격을 방지하고, 리소스 간 통신을 제어할 수 있습니다.  
- **비용 절감**: 데이터 전송 비용이 발생하지 않으며, 네트워크 트래픽을 최적화할 수 있습니다.  
- **리소스 접근 제어**: VPC 내부 리소스만 접근할 수 있도록 세밀한 정책을 설정할 수 있습니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **게이트웨이 엔드포인트** | AWS 서비스(예: S3, DynamoDB)에 직접 연결하여 프라이빗 통신을 제공 | S3 버킷에 접근할 때 인터넷 게이트웨이를 사용하지 않음 |
| **인터페이스 엔드포인트** | VPC 내 리소스 간 통신을 위한 VPC 내부 인터페이스 | VPC 내 EC2 인스턴스가 RDS 인스턴스에 접근 |
| **PrivateLink** | AWS 서비스(예: RDS, EFS)에 프라이빗 연결을 제공하여 외부 네트워크 접근 차단 | VPC 내 RDS 인스턴스에 애플리케이션 연결 |

> **💡 Tip:** VPC 엔드포인트는 AWS 서비스에 직접 연결하여 **인터넷 게이트웨이 없이도 리소스 간 통신**이 가능하며, 네트워크 보안을 강화하는 데 필수적입니다.  

---

### 개념 2: VPC 피어링 연결 및 라우팅 테이블 설정  
VPC 피어링은 두 개의 VPC 간의 **직접적인 네트워크 연결**을 제공하는 기술입니다. 이는 VPC 간의 리소스 접근을 용이하게 하며, **인터넷 게이트웨이 없이도 통신**이 가능합니다. 라우팅 테이블에 피어링 연결 정보를 추가해야 하며, CIDR 범위가 중복되지 않도록 설정해야 합니다. 이는 **내부 네트워크를 통해 리소스를 안전하게 공유**하는 데 적합합니다.  

#### 작동 원리  
1. **피어링 연결 생성**: 두 VPC 간에 피어링 연결을 설정하고, 각 VPC의 라우팅 테이블에 연결 정보를 추가합니다.  
2. **라우팅 테이블 업데이트**: 각 VPC의 라우팅 테이블에 피어링 연결의 CIDR 범위를 추가하여 트래픽을 전달하도록 설정합니다.  
3. **통신 테스트**: 설정된 라우팅 테이블을 기반으로 VPC 간 리소스 간 통신을 테스트합니다.  

> **💡 Tip:** VPC 피어링은 **리소스 간 직접적인 연결**을 통해 네트워크 성능을 최적화하며, CIDR 범위 중복을 주의해야 합니다.  

---

### 개념 3: 트랜지트 게이트웨이의 중앙 집중형 네트워크 연결  
트랜지트 게이트웨이는 여러 VPC, 온프레미스 네트워크, Direct Connect 간의 **중앙 집중형 연결**을 제공하는 기술입니다. 이는 VPC 피어링 대신 사용되며, **복잡한 네트워크 아키텍처**에서 통합 관리가 가능합니다. 트랜지트 게이트웨이는 라우팅 테이블을 중앙에서 관리하고, 네트워크 트래픽을 효율적으로 전달합니다.  

#### 주요 특징  
1. **중심 집중형 관리**: 여러 VPC와 온프레미스 네트워크를 하나의 게이트웨이로 통합하여 관리 편의성을 높입니다.  
2. **확장성**: 수많은 VPC와 연결을 지원하며, 네트워크 규모가 커질수록 유연하게 대응 가능합니다.  
3. **하이브리드 연결**: Direct Connect 또는 VPN을 통해 온프레미스 네트워크와의 연결을 지원합니다.  

> **💡 Tip:** 트랜지트 게이트웨이는 **복잡한 네트워크 환경**에서 통합 관리를 원하는 경우 필수적인 구성 요소로, 라우팅 정책을 유연하게 설정할 수 있습니다.

## 🖥️ AWS 콘솔에서 VPC Endpoints 사용하기

### Step 1: VPC Endpoints 서비스 접속  
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
   - 로그인 후 **Services** 메뉴에서 **VPC**를 선택합니다.  
2. 상단 검색창에 **"VPC Endpoints"**를 입력하고, 검색 결과에서 **VPC Endpoints**를 클릭합니다.  
   - 이 서비스는 VPC 내부 네트워크를 외부 서비스(예: S3, DynamoDB)에 접근할 수 있도록 하는 기능을 제공합니다.  

> **📸 화면 확인:** VPC Endpoints 대시보드가 표시되면 정상입니다.  
> **💡 Tip:** 대시보드에서 생성된 Endpoints 목록과 상태를 확인할 수 있습니다.  

---

### Step 2: [주요 작업 1 - VPC Peering 구성]  
1. **VPC Peering 연결 생성**  
   - 왼쪽 메뉴에서 **"Peering Connections"** → **"Create Peering Connection"** 클릭  
   - **Peer VPC** 필드에 대상 VPC를 선택합니다.  
   - **Region** 필드는 동일한 리전을 선택해야 합니다.  
   - **Tags**에 필요한 메타데이터를 입력합니다.  
2. **연결 상태 확인**  
   - 생성된 Peering Connection의 **Status**가 **"Pending Acceptance"**로 표시됩니다.  
   - 이 상태는 **Peer VPC**에서 수락해야 연결이 완료됩니다.  
3. **수락 설정**  
   - **Peer VPC**에서 Peering Connection을 수락해야 완전한 연결이 됩니다.  
   - 수락 후 **Status**가 **"Active"**로 변경됩니다.  

> **📸 화면 확인:** Peering Connection 생성 화면에서 VPC 선택 및 태그 입력을 완료한 후 상태를 확인합니다.  

---

### Step 3: [주요 작업 2 - 연결 구성 및 보안 설정]  
1. **Route Table 업데이트**  
   - **Route Tables** → 해당 VPC의 **Route Table**을 선택합니다.  
   - **Routes** 탭에서 **Peering Connection**을 추가합니다.  
   - **Destination CIDR**에 대상 VPC의 CIDR을 입력하고, **Target**으로 Peering Connection을 선택합니다.  
2. **Security Group 및 Network ACL 확인**  
   - Peering 연결이 완료된 후, **Security Group** 및 **Network ACL** 설정이 제대로 되었는지 확인합니다.  
   - 접근 제한이 없는 경우, **Security Group**에서 필요한 포트를 허용해야 합니다.  
3. **DNS 설정 확인**  
   - Peering 연결이 활성화된 후, **DNS Resolver** 설정이 올바르게 구성되었는지 확인합니다.  
   - VPC 내부에서 다른 VPC의 자원을 DNS로 접근할 수 있는지 테스트합니다.  

> **⚠️ 주의:** Peering 연결은 동일한 리전에서만 가능하며, VPC의 CIDR이 중복되지 않아야 합니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **연결 상태 확인**  
   - **VPC Endpoints** 대시보드에서 Peering Connection의 **Status**가 **"Active"**인지 확인합니다.  
   - **Console** → **VPC** → **Peering Connections**에서 상태를 확인할 수 있습니다.  
2. **Route Table 상태 확인**  
   - **Route Table**의 **Routes** 탭에서 Peering Connection이 정상적으로 등록되었는지 확인합니다.  
3. **정상 동작 테스트**  
   - CLI 명령어로 연결 테스트:  
     ```bash
     aws ec2 describe-peering-connections --peering-connection-ids pc-12345678
     ```  
   - EC2 인스턴스에서 다른 VPC의 자원에 대한 접근성을 테스트합니다.  

> **💡 Tip:** Peering 연결은 외부 서비스와의 통신이 아닌 VPC 간 네트워크 연결을 위한 기능입니다. S3 등 외부 서비스 접근은 **VPC Endpoints**를 사용해야 합니다.

## ⌨️ AWS CLI로 VPC Endpoints 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**확인 사항:**  
- AWS CLI가 최신 버전인지 확인하세요 (2.10.0 이상 권장).  
- VPC Endpoints는 EC2 서비스에서 관리되므로, `aws ec2` 명령어를 사용합니다.  
- 리전 설정이 올바르게 되어 있는지 확인하세요.  

---

### 예제 1: VPC Endpoints 리소스 조회
```bash
# VPC Endpoints 목록 조회
aws ec2 describe-vpc-endpoints --query 'VpcEndpoints[].VpcEndpointId' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| --query | JSON 결과 필터링 | `'VpcEndpoints[].VpcEndpointId'` |
| --output | 출력 형식 | `json`, `table`, `text` |

**예상 출력:**
```
VpcEndpointId
-----------------
vpc-endpoint-12345678
vpc-endpoint-09876543
```

**참고:**  
- `describe-vpc-endpoints`는 VPC Endpoint ID 및 상태를 반환합니다.  
- `--query`를 사용해 특정 필드(예: `VpcEndpointId`, `State`)만 필터링할 수 있습니다.  

---

### 예제 2: VPC Endpoints 리소스 생성
```bash
# Gateway VPC Endpoint 생성 (S3 서비스 예시)
aws ec2 create-vpc-endpoint \
    --vpc-id vpc-0abcdef1234567890 \
    --service-id com.amazonaws.us-east-1.s3 \
    --policy '{"Statement": [{"Effect": "Allow", "Principal": "*", "Action": "s3:GetObject", "Resource": "*"}]}'
```

**필수 옵션:**
- `--vpc-id`: 생성할 VPC ID (예: `vpc-0abcdef1234567890`)  
- `--service-id`: 연결할 서비스 ID (예: `com.amazonaws.us-east-1.s3`)  
- `--policy`: 접근 제어 정책 (JSON 형식)  

**예상 출력:**
```json
{
    "VpcEndpoint": {
        "VpcEndpointId": "vpc-endpoint-12345678",
        "VpcId": "vpc-0abcdef1234567890",
        "ServiceId": "com.amazonaws.us-east-1.s3",
        "State": "active"
    }
}
```

**팁:**  
- 서비스 ID는 AWS 서비스별로 고유합니다 (예: `com.amazonaws.us-east-1.s3`는 S3 서비스).  
- 정책은 IAM 정책 형식을 따라야 합니다.  

---

### 예제 3: VPC Endpoints 리소스 수정
```bash
# VPC Endpoint 정책 수정
aws ec2 modify-vpc-endpoint --vpc-endpoint-id vpc-endpoint-12345678 \
    --policy '{"Statement": [{"Effect": "Allow", "Principal": "*", "Action": "s3:GetObject", "Resource": "arn:aws:s3:::example-bucket/*"}]}'
```

**옵션 설명:**  
- `--vpc-endpoint-id`: 수정할 VPC Endpoint ID  
- `--policy`: 새로운 정책 (JSON 형식)  

**참고:**  
- `modify-vpc-endpoint`는 정책만 수정할 수 있습니다. 다른 속성은 변경 불가능합니다.  

---

### 예제 4: VPC Endpoints 리소스 삭제
```bash
# VPC Endpoint 삭제
aws ec2 delete-vpc-endpoint --vpc-endpoint-id vpc-endpoint-12345678

# 삭제 확인
aws ec2 describe-vpc-endpoints --vpc-endpoint-id vpc-endpoint-12345678
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. 삭제 후에는 해당 리소스를 복원할 수 없습니다.  

**삭제 전 확인 사항:**  
1. VPC Endpoint에 연결된 리소스가 없는지 확인하세요.  
2. 다른 VPC 또는 서비스에 영향을 주지 않는지 검토하세요.  

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws ec2 describe-vpc-endpoints --vpc-endpoint-id vpc-endpoint-12345678
aws ec2 describe-vpc-endpoints --filters Name=vpc-endpoint-state,Values=active

# 생성
aws ec2 create-vpc-endpoint --vpc-id vpc-0abcdef1234567890 --service-id com.amazonaws.us-east-1.s3

# 수정
aws ec2 modify-vpc-endpoint --vpc-endpoint-id vpc-endpoint-12345678 --policy '{"Statement": [...]}'

# 삭제
aws ec2 delete-vpc-endpoint --vpc-endpoint-id vpc-endpoint-12345678
```

**비용 주의사항:**  
- VPC Endpoint는 AWS Free Tier에서 1개까지 무료입니다.  
- Gateway Endpoint는 100,000개 요청까지 무료, 이후 요금이 발생합니다.  
- Interface Endpoint는 데이터 전송에 따라 요금이 적용됩니다.  

**추천:**  
- Free Tier 사용 시, `aws ec2 describe-vpc-endpoints`로 생성된 ID를 저장해 두세요.  
- 정책은 IAM 정책 생성기나 JSON 편집기로 작성하세요.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 VPC Endpoints 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **포인트 1: 게이트웨이 엔드포인트 vs 인터페이스 엔드포인트 차이**  
   - 설명: AWS 서비스에 대한 프라이빗 연결을 구현할 때, 게이트웨이 엔드포인트는 AWS 서비스 전체에 접근 가능하지만 인터페이스 엔드포인트는 특정 서비스에만 연결 가능합니다. 시험에서는 이 두 유형의 용도, 구성 방법, 네트워크 흐름 차이를 묻는 문제가 자주 출제됩니다.  
   - 키워드: `게이트웨이 엔드포인트`, `인터페이스 엔드포인트`, `VPC 네트워크 통합`

2. **포인트 2: PrivateLink의 프라이빗 연결 구조**  
   - 설명: PrivateLink는 AWS 서비스와 온프레미스 시스템 간의 프라이빗 연결을 제공하지만, VPC Endpoints는 AWS 서비스 간 연결에 초점이 맞춰 있습니다. 시험에서는 이 두 서비스의 차이와 사용 시나리오를 구분하는 문제가 자주 등장합니다.  
   - 키워드: `PrivateLink`, `온프레미스 연결`, `프라이빗 엔드포인트`

3. **포인트 3: VPC Endpoints의 비용 구조**  
   - 설명: VPC Endpoints는 데이터 전송에 대한 요금이 발생하지 않지만, 리소스 사용량에 따라 비용이 달라집니다. 시험에서는 데이터 전송 비용, 리소스 기반 요금, 프리티어 한도에 대한 이해를 묻는 문제가 많습니다.  
   - 키워드: `데이터 전송 비용`, `리소스 요금`, `프리티어`

4. **포인트 4: VPC Peering과의 통합**  
   - 설명: VPC Peering을 통해 연결된 VPC 간에 VPC Endpoints를 사용할 때, 라우팅 테이블 구성과 보안 그룹 설정이 시험에서 주요 포인트로 다루어집니다.  
   - 키워드: `VPC Peering`, `라우팅 테이블`, `보안 그룹`

5. **포인트 5: VPC Endpoints의 제한사항**  
   - 설명: VPC Endpoints는 AWS 서비스에만 연결 가능하며, 온프레미스 서비스나 외부 인터넷에 직접 연결할 수 없습니다. 이 제한사항과 대안 서비스(예: Direct Connect)의 차이를 이해하는 것이 중요합니다.  
   - 키워드: `AWS 서비스 제한`, `Direct Connect`, `프라이빗 연결`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | "인터페이스 엔드포인트는 AWS 서비스 전체에 접근 가능하다"는 문장이 주어질 때, 혼동할 수 있습니다. | ❌ 틀림. 인터페이스 엔드포인트는 특정 서비스에만 연결 가능합니다. |
| 함정 2 | "VPC Endpoints는 데이터 전송 비용이 발생한다"는 문장이 주어질 때, 비용 구조를 혼동할 수 있습니다. | ❌ 틀림. 데이터 전송 비용은 발생하지 않지만, 리소스 사용량에 따라 요금이 적용됩니다. |
| 함정 3 | "PrivateLink는 VPC Endpoints와 동일한 기능을 제공한다"는 문장이 주어질 때, 두 서비스의 차이를 혼동할 수 있습니다. | ❌ 틀림. PrivateLink는 온프레미스 연결에 특화된 서비스입니다. |

#### 🔄 VPC Endpoints vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | VPC Endpoints | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| **용도** | AWS 서비스(예: S3, DynamoDB)에 대한 프라이빗 연결 | PrivateLink (온프레미스 연결), Direct Connect (외부 네트워크 연결) | AWS 내부 서비스 접근이 필요한 경우 |
| **확장성** | 서비스별로 개별 엔드포인트 생성 가능 | PrivateLink는 연결 대상 서비스에 따라 구성 | 다수의 AWS 서비스에 연결이 필요한 경우 |
| **비용** | 리소스 사용량에 따라 요금, 데이터 전송 비용 없음 | PrivateLink는 연결 대상 서비스에 따라 요금, Direct Connect는 전용 링크 요금 | 데이터 전송 비용을 줄이고자 할 때 |
| **지연시간** | VPC 내부 네트워크로 빠른 연결 | Direct Connect는 물리적 링크로 지연 최소화 | 저지연이 필수적인 시나리오 |

#### 📝 시험 대비 체크리스트
- [ ] VPC Endpoints의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  **예:** "VPC 내부에서 AWS 서비스에 대한 프라이빗 연결을 제공하여 인터넷 게이트웨이 없이도 서비스 접근이 가능하다."
- [ ] VPC Endpoints를 선택해야 하는 시나리오를 알고 있는가?  
  **예:** "S3 버킷에 접근해야 하지만 인터넷 게이트웨이를 사용하지 않으려는 경우."
- [ ] VPC Endpoints의 제한사항/한계를 알고 있는가?  
  **예:** "AWS 서비스에만 연결 가능하며, 온프레미스 서비스나 외부 인터넷에 직접 연결할 수 없다."
- [ ] VPC Endpoints와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  **예:** "PrivateLink는 온프레미스 서비스 연결, Direct Connect는 외부 네트워크 연결에 특화되어 있다."
- [ ] VPC Endpoints의 비용 구조를 이해하고 있는가?  
  **예:** "데이터 전송 비용은 없지만, 사용하는 리소스에 따라 요금이 발생하며, 프리티어 한도는 100,000 USD이다."

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 VPC Endpoints를 떠올리세요:  
> - **프라이빗 연결**  
> - **AWS 서비스 접근**  
> - **인터넷 게이트웨이 없이**

---

| [⬅️ Transit Gateway](./Transit-Gateway.md) | [📑 Day 2 목차](./README.md) | [🏠 Week 2](../README.md) | [PrivateLink ➡️](./PrivateLink.md) |

---
