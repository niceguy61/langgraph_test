---

| [⬅️ 이전 Day](../day4/README.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 2](../README.md) | [CloudFront ➡️](./CloudFront.md) |

---

# S3 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** S3는 객체 스토리지 솔루션을 위한 AWS의 확장성 있는 저장소 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 전통적인 파일 서버는 저장 용량 한계, 장애 시 데이터 손실, 수동적인 관리로 인해 확장성이 부족했습니다. 예를 들어, 회사가 로컬 서버에 데이터를 저장할 경우 서버 공간이 부족해지면 추가 서버 구매가 필요했습니다.
- **문제 2:** 데이터 보안 및 접근 제어를 구현하기 위해 복잡한 네트워크 설정과 수동 보호 정책 관리가 필요했습니다. 예를 들어, 내부 직원이 잘못된 파일을 수정할 수 있는 위험이 있었습니다.
- **문제 3:** 데이터 백업 및 복구 시 시간이 많이 소요되었고, 복구 과정에서 데이터 손실이 발생할 수 있었습니다. 예를 들어, 하드디스크 고장 시 복구가 어려웠습니다.

**S3로 해결:**
- **해결 1:** S3는 확장 가능한 분산 저장소로, 수십 테라바이트 단위의 데이터를 저장할 수 있으며, 자동적인 복제와 복구 기능을 통해 데이터 손실을 방지합니다. 예를 들어, 100TB의 데이터도 쉽게 저장할 수 있습니다.
- **해결 2:** S3는 세밀한 정책 기반 접근 제어, 암호화, 서명된 URL 등을 통해 데이터 보안을 강화합니다. 예를 들어, 특정 IP만 접근할 수 있도록 제어할 수 있습니다.
- **해결 3:** S3의 버전 관리와 수명 주기 정책을 통해 자동 백업 및 데이터 복구가 가능하며, 데이터를 다른 스토리지 클래스로 이동하여 저장 비용을 절감할 수 있습니다. 예를 들어, 3개월 후 데이터를 Glacier로 이동해 저장 비용을 절감할 수 있습니다.

### 비유로 이해하기
S3는 구글 드라이브와 유사한 개념입니다. 집안에서 사진, 문서, 동영상 등을 저장할 때 사용하는 클라우드 저장소로, 언제 어디서나 접근할 수 있으며, 데이터가 손실되지 않고, 저장 공간이 무한합니다. 예를 들어, 가족 사진을 S3에 저장하면 가족이 어디서든 사진을 볼 수 있고, 저장 용량이 부족해질 때도 추가 저장 공간을 쉽게 확장할 수 있습니다.

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 대규모 데이터 저장 및 배포 | AWS에서 콘텐츠 배포 시 S3로 정적 자산(이미지, CSS, JS)을 저장하고, CloudFront로 배포 |
| 시나리오 2 | 백업 및 복구 | 데이터베이스 백업 시 S3에 저장하고, 필요 시 복구 |
| 시나리오 3 | 데이터 레이크 및 분석 | 대규모 데이터 분석을 위해 S3에 데이터를 저장하고, Athena 또는 Redshift로 분석 |

**이럴 때 S3를 선택하세요:**
- ✅ **대규모 데이터 저장이 필요할 때** (스토리지 클래스 선택 가능)
- ✅ **데이터 보안 및 접근 제어가 필수적일 때** (버킷 정책, IAM 정책)
- ✅ **비용 효율적인 백업 및 복구가 필요할 때** (수명 주기 정책, Glacier)

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **NoSQL 데이터 저장 시** → **DynamoDB** (빠른 접근성, 실시간 데이터 처리)
- ❌ **파일 기반 저장이 필요할 때** → **EFS** (공유 파일 시스템, 높은 IOPS)
- ❌ **고성능 데이터 처리가 필요할 때** → **Redshift** (데이터 분석 및 처리)

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **CloudFront** | CDN을 통해 S3에 저장된 콘텐츠를 전 세계 사용자에게 빠르게 제공 | `User → CloudFront → S3` |
| **Route 53** | DNS 관리 및 로드 균형을 통해 S3 엔드포인트를 사용자에게 노출 | `User → Route 53 → S3` |
| **Glacier** | 장기 보관 데이터를 저비용으로 저장하기 위해 S3의 스토리지 클래스 변경 | `S3 → Glacier` |

**자주 사용되는 아키텍처 패턴:**
```
User → CloudFront (CDN) → S3 (스토리지) → DynamoDB (데이터 처리)
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **스토리지 비용** | $0.023/GB (Standard) ~ $0.004/GB (Glacier) | 월 5GB 무료 |
| **데이터 전송 비용** | $0.09/GB (US East) ~ $0.12/GB (EU) | 20GB 무료 |
| **요청 비용** | $0.005/요청 (GET/PUT) | 20,000 요청 무료 |

**비용 최적화 팁:**
1. 💡 **S3 Intelligent-Tiering 사용:** 자동적인 데이터 이동으로 저장 비용 절감
2. 💡 **수명 주기 정책 설정:** 장기 보관 데이터는 Glacier로 이동해 비용 절감
3. 💡 **데이터 전송 최적화:** 데이터 전송 비용이 높은 지역을 피하고, CDN 사용

> **⚠️ 비용 주의:** 데이터 전송 비용이 예상치 못하게 발생할 수 있으므로, CloudFront를 통해 지역에 가까운 엔드포인트를 선택하고, 데이터 전송을 최소화해야 합니다. 버킷 버전 관리 시 저장 공간이 증가할 수 있으므로, 필요 시 버전 관리 기능을 제한해야 합니다.

## 📚 핵심 개념

### 개념 1: S3 스토리지 클래스
S3 스토리지 클래스는 데이터를 저장할 때 비용 효율성과 접근성에 따라 데이터를 분류하는 기능입니다. AWS는 S3 Standard(일반적인 액세스), S3 Intelligent-Tiering(자동 데이터 이동), S3 One Zone-Infrequent Access(단일 지역에 저장), S3 Glacier(장기 보존), S3 Glacier Deep Archive(최저 비용 장기 보존) 등의 클래스를 제공합니다. 이 기능은 데이터의 중요도와 액세스 빈도에 따라 적절한 저장 방식을 선택하여 비용을 최적화하고, 데이터의 가용성을 보장합니다.

#### 왜 중요한가?
- **비용 효율성**: 데이터의 액세스 빈도에 따라 저장 비용을 조절할 수 있어 비용 절감을 도와줍니다.  
- **데이터 보존 및 재사용**: 장기 보존 클래스는 중요한 데이터를 오랜 시간 동안 안전하게 보관할 수 있습니다.  
- **성능 최적화**: 빈도가 높은 데이터는 S3 Standard를 사용해 빠른 액세스를, 빈도가 낮은 데이터는 저비용 클래스로 이동해 성능과 비용을 균형 잡습니다.  

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|------|---|
| S3 Standard | 일반적인 액세스에 최적화된 저장 클래스 | 빈도가 높은 데이터 저장 |
| S3 Intelligent-Tiering | 자동으로 데이터를 이동 및 저장하여 비용 최적화 | 데이터 액세스 패턴에 따라 자동 조정 |
| S3 One Zone-Infrequent Access | 단일 지역에 저장하여 비용 절감 | 월 1회 액세스하는 데이터 저장 |
| S3 Glacier | 장기 보존 및 저비용 저장 | 보존 기간 90일 이상의 데이터 |
| S3 Glacier Deep Archive | 최저 비용 장기 보존 | 180일 이상 보존 및 암호화된 데이터 |

> **💡 Tip:** 데이터의 액세스 빈도를 분석해 적절한 스토리지 클래스를 선택하고, 수명주기 정책과 함께 사용하면 비용 절감 효과가 큽니다.

---

### 개념 2: 수명주기 정책
S3 수명주기 정책은 데이터를 자동으로 이동, 복제, 삭제하는 기능으로, 데이터 관리의 자동화 및 비용 최적화를 도와줍니다. 정책은 특정 시간, 데이터 생성 후 일정 기간, 또는 특정 조건을 충족할 때 실행됩니다. 이 기능은 데이터의 저장 기간을 관리하고, 불필요한 데이터를 제거하거나 저비용 클래스로 이동해 비용을 절감합니다.

#### 작동 원리
1. **정책 생성**: AWS 콘솔 또는 CLI를 통해 정책을 생성하고, 데이터의 저장 클래스 이동, 복제, 삭제 조건을 설정합니다.  
2. **데이터 모니터링**: S3는 데이터를 지속적으로 모니터링하여 정책 조건에 해당하는 데이터를 식별합니다.  
3. **자동 처리**: 정책에 설정된 조건이 충족되면 데이터가 자동으로 이동, 복제, 또는 삭제됩니다.  

> **💡 Tip:** 수명주기 정책은 데이터를 장기 보존하는 동시에 비용을 절감하는 데 필수적이므로, 데이터의 저장 기간과 액세스 빈도를 정확히 분석해야 합니다.

---

### 개념 3: S3 크로스 리전 복제 (CRR)
S3 크로스 리전 복제(Cross-Region Replication, CRR)는 데이터를 여러 지역에 복제하여 장애 발생 시 데이터를 복구하고, 글로벌 사용자에게 빠른 액세스를 제공하는 기능입니다. 이 기능은 데이터의 가용성과 지연 시간을 최소화하며, 재해 복구 및 데이터 보존에 효과적입니다.

#### 주요 특징
1. **자동 복제**: 원본 버킷에서 생성된 데이터를 다른 지역의 대상 버킷으로 자동으로 복제합니다.  
2. **데이터 일관성**: 복제된 데이터는 원본 데이터와 동기화되며, 복제 중인 데이터는 읽기 전용 상태로 유지됩니다.  
3. **비용 최적화**: 복제된 데이터는 S3 Standard 클래스로 저장되며, 사용 시 데이터 전송 비용이 발생합니다.  

> **💡 Tip:** CRR은 재해 복구 및 글로벌 액세스에 적합하지만, 데이터 전송 비용과 복제 지연 시간을 고려해 사용 범위를 설정해야 합니다.

## 🖥️ AWS 콘솔에서 S3 사용하기

### Step 1: S3 서비스 접속  
1. **AWS Management Console 로그인**  
   - AWS 계정으로 로그인 후, 상단 메뉴의 검색창에 **"S3"**를 입력합니다.  
   - 검색 결과에서 **"S3"** 서비스를 선택해 대시보드로 이동합니다.  

2. **버킷 생성 화면 확인**  
   - 왼쪽 메뉴에서 **"버킷 생성"** 버튼을 클릭하면, 새 버킷을 만들 수 있는 화면이 나타납니다.  
   - 버킷 이름을 입력하고, 지역(Region)을 선택합니다.  
   - **버킷 이름은 전역으로 유일해야** 하며, 동일한 이름이 이미 존재하면 생성이 실패합니다.  

> **📸 화면 확인:** S3 대시보드에서 "버킷 생성" 버튼이 보이고, 버킷 이름 입력 필드가 활성화된 상태인지 확인합니다.  

---

### Step 2: [주요 작업 1 - 리소스 생성]  
1. **버킷 생성 및 기본 설정**  
   - **버킷 이름**을 입력하고, **지역**을 선택합니다.  
   - **버킷 정책**을 **"사용자 정의"**로 선택해 후에 수정 가능하게 합니다.  
   - **버킷 ACL**을 **"개인"**으로 설정해 기본 보안 수준을 적용합니다.  

2. **암호화 설정 활성화**  
   - **"암호화"** 섹션에서 **"SSE-S3"** 또는 **"SSE-KMS"**를 선택합니다.  
   - **SSE-S3**는 AWS가 관리하는 암호화 방식으로, **SSE-KMS**는 고객이 관리하는 키를 사용하는 방식입니다.  

3. **버킷 생성 확인**  
   - **"버킷 만들기"** 버튼을 클릭하면, 설정이 완료됩니다.  
   - 생성된 버킷은 S3 대시보드의 **"버킷 목록"**에서 확인할 수 있습니다.  

> **📸 화면 확인:** 버킷 생성 후, "버킷 목록"에서 생성된 버킷이 표시되며, "버킷 설정" 메뉴가 활성화된 상태인지 확인합니다.  

---

### Step 3: [주요 작업 2 - 설정/구성]  
1. **버킷 정책 설정**  
   - 버킷 이름을 클릭해 **"버킷 설정"** 메뉴에서 **"버킷 정책"**을 선택합니다.  
   - JSON 형식의 정책을 직접 입력하거나, **"AWS Managed Policy"**에서 기본 권한을 선택합니다.  
   - 예: `s3:GetObject` 권한을 부여해 특정 사용자가 객체를 읽을 수 있도록 설정합니다.  

2. **수명주기 정책 추가**  
   - **"버킷 설정"** > **"수명주기"** 메뉴에서 **"수명주기 정책 추가"**를 클릭합니다.  
   - 객체가 특정 날짜나 사용량에 도달하면 자동으로 삭제되도록 설정합니다.  
   - 예: "30일 후 삭제" 또는 "10GB 누적 시 삭제" 설정 가능.  

3. **S3 복제 설정 (CRR/SRR)**  
   - **"버킷 설정"** > **"복제"** 메뉴에서 **"복제 활성화"**를 선택합니다.  
   - **CRR (Cross-Region Replication)**: 데이터를 다른 지역으로 복제합니다.  
   - **SRR (Same-Region Replication)**: 동일 지역 내 복제를 설정합니다.  
   - **원본 및 대상 버킷**을 선택해 복제 규칙을 구성합니다.  

> **⚠️ 주의:** 복제 설정 시, **원본 버킷과 대상 버킷의 지역이 동일해야 하거나, CRR의 경우 다른 지역이어야 합니다.**  

---

### Step 4: 설정 확인 및 테스트  
1. **버킷 상태 확인**  
   - **"버킷 목록"**에서 생성된 버킷을 클릭해 **"버킷 설정"** 메뉴에서 **"암호화"**, **"정책"**, **"복제"** 설정이 정상적으로 적용되었는지 확인합니다.  

2. **데이터 업로드 및 접근 테스트**  
   - **"버킷 설정"** > **"객체"** 메뉴에서 파일을 업로드합니다.  
   - **"버킷 정책"**을 통해 특정 사용자가 파일을 읽거나 쓸 수 있는지 테스트합니다.  

3. **복제 확인**  
   - CRR/SRR 설정 후, **"복제"** 메뉴에서 복제된 데이터가 대상 버킷에 생성되었는지 확인합니다.  
   - 복제 시간은 지역 간 네트워크 지연에 따라 달라질 수 있습니다.  

> **💡 Tip:** AWS Free Tier을 활용해 5GB 무료 스토리지 및 20GB 데이터 전송량을 사용할 수 있습니다. 설정 후, **"요금 센터"**에서 사용량을 모니터링하세요.

## ⌨️ AWS CLI로 S3 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

> **💡 Tip:** AWS CLI는 `aws s3api` 명령어를 사용해 S3 버킷을 관리합니다. `aws s3` 명령어는 파일 업로드/다운로드에 사용됩니다.  

---

### 예제 1: S3 리소스 조회
```bash
# S3 버킷 목록 조회
aws s3api list-buckets --query 'Buckets[].Name' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | 결과 필터링 (버킷 이름만 출력) | `'Buckets[].Name'` |
| `--output` | 출력 형식 (table: 테이블 형식) | `table` |

**예상 출력:**
```
|      Name      |
+----------------+
| example-bucket |
| another-bucket |
```

---

### 예제 2: S3 리소스 생성
```bash
# 버킷 생성 (리전 설정 필수)
aws s3api create-bucket \
    --bucket example-bucket \
    --region ap-northeast-2 \
    --acl private
```

**필수 옵션:**
- `--bucket`: 생성할 버킷 이름
- `--region`: 리전 (ex: `ap-northeast-2`)
- `--acl`: 접근 제어 (public/private)

**예상 출력:**
```json
{
    "Location": "https://example-bucket.s3.ap-northeast-2.amazonaws.com/"
}
```

> **⚠️ 주의:** 버킷 이름은 글로벌 유니크해야 하며, 동일한 이름이 이미 존재하면 오류 발생합니다.

---

### 예제 3: S3 리소스 수정
```bash
# 버킷 정책 설정
aws s3api put-bucket-policy \
    --bucket example-bucket \
    --policy file://bucket-policy.json
```

```json
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Sid": "AllowPublicRead",
            "Effect": "Allow",
            "Principal": "*",
            "Action": "s3:GetObject",
            "Resource": "arn:aws:s3:::example-bucket/*"
        }
    ]
}
```

**암호화 설정:**
```bash
# SSE-KMS 암호화 활성화
aws s3api put-bucket-encryption \
    --bucket example-bucket \
    --server-side-encryption-configuration '{
        "Rules": [
            {
                "ApplyServerSideEncryptionByDefault": {
                    "SSEAlgorithm": "aws:kms"
                }
            }
        ]
    }'
```

---

### 예제 4: S3 리소스 삭제
```bash
# 버킷 삭제 (비어 있어야 함)
aws s3api delete-bucket --bucket example-bucket

# 삭제 확인
aws s3api list-buckets --query 'Buckets[].Name' --output table
```

> **⚠️ 주의:** 버킷에 파일이 있으면 삭제 실패. `delete-bucket`은 되돌릴 수 없습니다.

---

### 자주 사용하는 명령어 정리
```bash
# 버킷 관리
aws s3api create-bucket --bucket <name> --region <region>
aws s3api delete-bucket --bucket <name>
aws s3api put-bucket-policy --bucket <name> --policy file://policy.json
aws s3api put-bucket-encryption --bucket <name> --server-side-encryption-configuration <JSON>

# 파일 관리
aws s3 cp local.txt s3://example-bucket/
aws s3 rm s3://example-bucket/remote.txt
```

> **💡 Tip:** `aws s3` 명령어는 파일 업로드/다운로드에, `aws s3api`는 버킷/정책/암호화 관리에 사용됩니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 S3 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **S3 스토리지 클래스 (Storage Class)**  
   - 설명: S3의 스토리지 클래스는 데이터 저장 비용과 데이터 접근 속도를 조절하는 핵심 개념입니다. 시험에서 데이터 보존 기간, 비용 최적화, 데이터 재사용 빈도에 따라 적절한 클래스 선택을 묻는 문제가 자주 출제됩니다.  
   - 키워드: `비용 최적화`, `데이터 재사용 빈도`, `데이터 보존 기간`

2. **수명주기 정책 (Lifecycle Policy)**  
   - 설명: 데이터의 수명주기 관리를 통해 자동으로 데이터를 이동, 삭제, 암호화하는 기능입니다. 시험에서 정책의 동작 시점(예: 객체 생성 후 30일)과 자동화된 관리 방식을 묻는 문제가 자주 등장합니다.  
   - 키워드: `자동 관리`, `이동/삭제`, `시점 조건`

3. **크로스 리전 복제 (Cross-Region Replication, CRR)**  
   - 설명: 데이터를 여러 리전에 실시간으로 복제하여 장애 대응과 데이터 지역화를 지원합니다. 시험에서 복제 설정 방법, 복제 대상 리전, 복제 중단 시 대응 방안 등을 묻는 문제가 주요 출제 항목입니다.  
   - 키워드: `실시간 복제`, `리전 확장`, `장애 대응`

4. **S3 버킷 정책 및 암호화 설정**  
   - 설명: 보안 및 데이터 통제를 위한 필수 설정입니다. 시험에서 버킷 정책의 권한 관리, 서버 사이드 암호화(SSE)와 클라이언트 사이드 암호화의 차이, 암호화 키 관리 방식을 묻는 문제가 자주 출제됩니다.  
   - 키워드: `버킷 정책`, `암호화 방식`, `권한 관리`

5. **S3와 CloudFront, Route 53의 연동**  
   - 설명: S3 버킷을 CloudFront로 배포하고, Route 53으로 도메인 관리하는 구조는 시험에서 자주 출제됩니다. 특히, 캐시 설정, DNS 라우팅 정책(예: Latency, Weighted)의 선택 기준이 중요합니다.  
   - 키워드: `캐시 설정`, `DNS 라우팅`, `성능 최적화`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | S3 Glacier는 실시간 접근이 가능한지 여부를 묻는 문제 | S3 Glacier는 **정기적 접근**을 위한 저장 클래스로, 실시간 접근이 불가능합니다. |
| 함정 2 | 수명주기 정책에서 "삭제" 조건은 언제 적용되는지 | 정책 적용 시점은 **객체 생성 시점**부터 시작되며, 삭제 조건은 설정된 기간 이후에 실행됩니다. |
| 함정 3 | S3 CRR 복제 중단 시 데이터가 자동 삭제되는지 | 복제 중단 시 **기존 복제 데이터는 삭제되지 않으며**, 복제 중단 후 새로운 데이터는 복제되지 않습니다. |

#### 🔄 S3 vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | S3 | 대안 서비스 | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 객체 스토리지 (파일, 이미지, 비디오 등) | EBS (블록 스토리지), EFS (파일 시스템) | 대용량 데이터 저장, 스케일링이 필요한 경우 |
| 확장성 | 수백 PB까지 확장 가능 | EBS: 최대 16TB, EFS: 최대 100PB | 고가용성, 많은 사용자 동시 접근이 필요한 경우 |
| 비용 | 스토리지 클래스별 비용 최적화 가능 | EBS: 고정 비용, EFS: 사용량 기반 비용 | 데이터 액세스 빈도, 보존 기간에 따라 선택 |
| 지연시간 | 데이터 접근 지연 최소화 (S3 Standard) | EBS: 저지연, EFS: 중지연 | 실시간 처리나 고성능 애플리케이션에 적합 |

#### 📝 시험 대비 체크리스트
- [ ] S3의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  (예: "S3는 대규모 객체 스토리지 서비스로, 데이터 저장, 관리, 보안을 제공합니다.")
- [ ] S3를 선택해야 하는 시나리오를 알고 있는가?  
  (예: "대용량 파일 저장, 글로벌 데이터 분산, 빈도가 낮은 데이터 보존")
- [ ] S3의 제한사항/한계를 알고 있는가?  
  (예: "데이터 암호화 키 관리 필요, 복제 설정의 복잡성, 비용 최적화의 기술적 이해 필요")
- [ ] S3와 비슷한 서비스의 차이점을 설명할 수 있는가?  
  (예: "S3는 객체 스토리지, EBS는 블록 스토리지, EFS는 파일 시스템으로 용도가 다릅니다.")
- [ ] S3의 비용 구조를 이해하고 있는가?  
  (예: "스토리지 클래스, 데이터 전송, 요청 수량, 보관 기간에 따라 비용이 달라집니다.")

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 S3를 떠올리세요:  
> - **비용 최적화** (스토리지 클래스, 수명주기 정책)  
> - **데이터 재사용 빈도** (Standard vs. Glacier)  
> - **장애 대응** (크로스 리전 복제, 다중 리전 배포)  
> - **암호화 방식** (SSE, CSE, KMS)  
> - **DNS 라우팅** (Route 53의 Latency, Weighted 정책)

---

| [⬅️ 이전 Day](../day4/README.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 2](../README.md) | [CloudFront ➡️](./CloudFront.md) |

---
