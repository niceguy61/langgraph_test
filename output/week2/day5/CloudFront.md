---

| [⬅️ S3](./S3.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 2](../README.md) | [Route 53 ➡️](./Route-53.md) |

---

# CloudFront 완전 정복

## 📌 핵심 목적 (What & Why)

> **한 줄 정의:** CloudFront는 웹 및 모바일 애플리케이션의 콘텐츠를 전 세계 사용자에게 빠르고 안전하게 제공하기 위한 AWS의 **콘텐츠 전달 네트워크(CDN)** 서비스입니다.

### 이 서비스가 해결하는 문제
**기존의 문제점:**
- **문제 1:** 사용자 요청이 원격 서버에 직접 도달하므로 네트워크 지연으로 인해 페이지 로딩 속도가 느려집니다. 예를 들어, 미국에 거주하는 사용자가 한국에 있는 서버에서 데이터를 요청하면 100ms 이상의 지연이 발생합니다.  
- **문제 2:** 데이터 전송 과정에서 SSL/TLS 암호화를 적용하지 않으면 보안 위험이 있으며, 암호화 처리에 필요한 리소스가 추가로 소비됩니다.  
- **문제 3:** 사용자 요청이 서버에 집중되어 지연이 발생하면 애플리케이션의 가용성이 떨어지고, 트래픽이 급증할 경우 서버 부하가 과도해지게 됩니다.

**CloudFront로 해결:**
- **해결 1:** 전 세계에 위치한 **Edge Location**을 통해 요청을 가까운 위치에 있는 CDN 노드로 전달하여 지연을 최소화합니다. 예를 들어, 한국 사용자의 요청은 한국 Edge Location에서 처리되어 50ms 이내로 응답합니다.  
- **해결 2:** **SSL/TLS 암호화**를 자동으로 적용하여 데이터 전송 과정에서 보안을 강화하고, 암호화 처리가 필요한 경우 **AWS Key Management Service(KMS)**를 연동해 보안을 더욱 강화합니다.  
- **해결 3:** **캐시 기능**을 통해 자주 요청되는 데이터를 Edge Location에 저장해 반복 요청 시 서버 부담을 줄이고, **Global Accelerator**와 연동해 트래픽을 최적화된 경로로 분산합니다.

### 비유로 이해하기
CloudFront는 전 세계에 위치한 도서관 네트워크를 상상해보세요. 사용자가 어떤 도서를 요청하면, 가장 가까운 도서관에서 해당 책을 제공받습니다. 예를 들어, 서울에 거주하는 사용자가 "인공지능 개론"이라는 책을 요청하면, 서울의 도서관에서 즉시 제공받지만, 부산의 사용자는 부산의 도서관에서 책을 받습니다. 이처럼 CloudFront는 사용자의 위치에 따라 가까운 노드에서 콘텐츠를 제공해 속도를 높입니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 고속 콘텐츠 전달이 필요한 웹사이트 운영 | 뉴스 사이트, 온라인 쇼핑몰 등 실시간 정보 제공이 필요한 서비스 |
| 시나리오 2 | 대규모 트래픽을 처리해야 하는 API 백엔드 | SaaS 애플리케이션, 백엔드 서버에 부하를 분산해 가용성을 높이기 위한 시나리오 |
| 시나리오 3 | 보안성과 성능을 동시에 요구하는 서비스 | 금융 앱, 의료 데이터 플랫폼 등 SSL/TLS 암호화와 캐싱이 필수적인 환경 |

**이럴 때 CloudFront를 선택하세요:**
- ✅ **고속 로딩이 필요한 정적 콘텐츠 배포**
- ✅ **대규모 트래픽을 안정적으로 처리해야 하는 API 백엔드**
- ✅ **보안과 성능을 동시에 요구하는 서비스**

**이럴 때는 다른 서비스를 고려하세요:**
- ❌ **데이터베이스 캐싱이 필요한 경우 → ElastiCache** (서버 측 캐싱이 필요할 때)  
- ❌ **사용자 인증이 필수적인 경우 → Cognito** (CloudFront는 인증 기능 자체를 제공하지 않음)  
- ❌ **원격 서버에 직접 접근해야 하는 경우 → EC2/VPC** (CloudFront는 중간 레이어 역할을 함)

---

## 🔗 연관 서비스 (Used Together With)

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **S3** | 콘텐츠 저장소로 사용하여 CloudFront에 배포 | User → CloudFront → S3 |
| **Route 53** | DNS 관리 및 라우팅 정책을 통해 CloudFront와의 연동 | User → Route 53 → CloudFront → S3 |
| **Global Accelerator** | 트래픽 분산 및 라우팅 최적화 | User → Global Accelerator → CloudFront → S3 |

**자주 사용되는 아키텍처 패턴:**
```
User → CloudFront → S3
User → Route 53 → CloudFront → S3
User → Global Accelerator → CloudFront → S3
```

---

## 💰 비용 구조 (Pricing)

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **데이터 전송** | $0.08/GB (미국 서버 기준) | 월 12GB 무료 |
| **요청 수** | $0.005/요청 | 100만 요청/월 무료 |
| **SSL/TLS 암호화** | $0.004/요청 | 100만 요청/월 무료 |

**비용 최적화 팁:**
1. 💡 **S3 Transfer Acceleration**을 사용해 데이터 전송 비용을 절감합니다.  
2. 💡 **캐시 설정 최적화**를 통해 요청 수를 줄이고, **TTL(Timeout to Live)** 값을 적절히 조정합니다.  
3. 💡 **CloudFront WAF**를 활용해 보안 위협을 차단해 서버 부담을 줄이고, **AWS Shield**로 DDoS 공격을 방어합니다.

> **⚠️ 비용 주의:** 트래픽이 급증하거나 **S3 버킷 정책**이 잘못 설정되어 **데이터 전송 비용**이 기대보다 높아질 수 있습니다. **AWS Cost Explorer**를 사용해 비용 분석을 정기적으로 수행해야 합니다.

## 📚 핵심 개념

### 개념 1: S3 스토리지 클래스와 수명주기 정책
S3 스토리지 클래스는 데이터를 저장할 때 비용을 최적화하는 방식으로, 데이터의 액세스 빈도에 따라 적절한 클래스를 선택해야 합니다. 수명주기 정책은 데이터의 저장 기간을 설정하여 자동으로 데이터를 이동하거나 삭제할 수 있는 기능입니다. 이 두 개념은 S3에서 데이터를 효율적으로 관리하고 비용을 절감하는 데 필수적입니다.  

#### 왜 중요한가?
- **비용 최적화**: 데이터의 액세스 빈도에 따라 적절한 스토리지 클래스를 선택하면 저장 비용을 줄일 수 있습니다.  
- **데이터 관리 자동화**: 수명주기 정책을 통해 데이터를 자동으로 이동하거나 삭제해, 수동 관리의 부담을 줄입니다.  

#### 세부 요소
| 요소 | 설명 | 예시 |
|-----|-----|-----|
| **S3 스토리지 클래스** | 데이터 저장 방식에 따라 비용이 달라지는 클래스 (STANDARD, S3 Intelligent-Tiering, S3 Glacier, etc.) | 영상 파일은 S3 Glacier에 저장하여 저장 비용을 절감 |
| **수명주기 정책** | 특정 기간 후 데이터를 이동하거나 삭제하는 정책 | 30일 후 데이터를 S3 Glacier로 이동 |
| **전환 정책** | 데이터를 다른 스토리지 클래스로 자동 전환 | 액세스 빈도가 낮은 데이터는 S3 Glacier로 전환 |

> **💡 Tip:** S3 Intelligent-Tiering은 자동으로 데이터의 액세스 패턴을 분석해 스토리지 클래스를 최적화해주는 기능으로, 비용 절감에 효과적입니다.

---

### 개념 2: S3 크로스 리전 복제 (CRR)
S3 크로스 리전 복제(Cross-Region Replication)는 데이터를 여러 리전에 복제하여 가용성과 장애 회복성을 높이는 기능입니다. 이는 데이터가 하나의 리전에서 손실되더라도 다른 리전에서 접근할 수 있도록 보장합니다.  

#### 작동 원리
1. **데이터 생성**: 원본 리전에서 데이터가 생성되면, CRR 정책이 자동으로 다른 리전으로 복제됩니다.  
2. **복제 확인**: 복제된 데이터는 동기화되어 원본 리전과 같은 상태를 유지합니다.  
3. **장애 회복**: 원본 리전에 문제가 발생하더라도 복제된 데이터를 통해 서비스를 지속할 수 있습니다.  

> **💡 Tip:** CRR은 데이터 보호에 효과적이지만, 복제 비용이 발생하므로 중요한 데이터에만 적용하는 것이 좋습니다.

---

### 개념 3: CloudFront 배포와 Route 53 라우팅 정책
CloudFront는 글로벌 CDN 서비스로, 웹 콘텐츠를 사용자에게 빠르게 전달합니다. Route 53은 DNS 서비스로, 사용자 요청을 CloudFront 도메인에 전달하는 데 사용됩니다. 두 서비스를 결합하면 고가용성 및 저지연 웹 서비스를 구현할 수 있습니다.  

#### 주요 특징
1. ****Global Accelerator**: CloudFront와 함께 사용하여 글로벌 사용자에게 최적의 경로를 제공합니다.  
2. ****Route 53 라우팅 정책**: 사용자 요청을 CloudFront 도메인으로 전달하여 CDN의 성능을 극대화합니다.  
3. ****성능 최적화**: Route 53의 latency-based routing을 사용해 사용자 지역에 따라 최단 경로를 선택합니다.  

> **💡 Tip:** Route 53의 weighted routing 정책을 사용하면, CloudFront 배포를 테스트하면서 트래픽 비율을 조절할 수 있습니다.

## 🖥️ AWS 콘솔에서 CloudFront 사용하기

### Step 1: CloudFront 서비스 접속
1. AWS Management Console에 로그인합니다  
   - URL: https://console.aws.amazon.com  
2. 상단 검색창에서 "CloudFront"을 입력하고, 검색 결과에서 "CloudFront" 서비스를 클릭합니다  
3. "CloudFront" 대시보드가 표시되면, "Distribution" 또는 "Web Distribution" 탭을 선택합니다  

> **📸 화면 확인:** CloudFront 대시보드에서 "Web Distribution" 탭이 활성화된 상태인지 확인하세요.  

---

### Step 2: [주요 작업 1 - S3 버킷 생성 및 설정]  
1. **S3 버킷 생성**  
   - AWS Management Console → "S3" 서비스 → "Create bucket" 클릭  
   - 버킷 이름 입력 (예: `my-cloudfront-bucket`)  
   - 지역 선택 (예: `us-east-1`)  
   - "Block public access" 옵션을 활성화하여 보안 강화  
   - 버킷 생성 후 "Permissions" 탭에서 정책을 설정합니다  

2. **S3 스토리지 클래스 설정**  
   - 버킷 내 "Properties" → "Storage class"에서 "Standard", "Infrequent Access", "Glacier" 중 선택  
   - 예: "Standard"을 선택하여 일반적인 데이터 저장  

3. **암호화 설정**  
   - "Permissions" → "Bucket policy"에서 IAM 정책 추가  
   - 예: `aws:iam:Arns`를 포함한 정책으로 접근 제어  
   - "Server-side encryption"을 활성화하여 `aws:kms` 암호화 사용  

> **📸 화면 확인:** S3 버킷 생성 후 "Permissions" 탭에서 정책과 암호화 설정이 적용된 상태인지 확인하세요.  

---

### Step 3: [주요 작업 2 - CloudFront 배포 및 구성]  
1. **CloudFront 배포 생성**  
   - CloudFront 대시보드 → "Create Distribution" 클릭  
   - "Origin" 탭에서 S3 버킷을 선택 (예: `my-cloudfront-bucket`)  
   - "Default Root Object"를 `index.html`로 설정  

2. **Route 53 라우팅 정책 설정**  
   - Route 53에서 "Hosted Zone" 생성 → CNAME 레코드 추가  
   - 예: `example.com` → CloudFront 배포의 `DNS Name` 입력  
   - 라우팅 정책을 "Weighted" 또는 "Latency"로 설정하여 트래픽 최적화  

3. **Global Accelerator 구성**  
   - Global Accelerator 서비스 → "Create accelerator" 클릭  
   - CloudFront 배포의 `DNS Name`을 "Endpoint"로 등록  
   - 지역별 트래픽 분배를 위해 "Regional endpoint" 추가  

> **⚠️ 주의:** CloudFront 배포 시 "Minimum TTL"을 0으로 설정해 캐시 시간을 최소화하세요.  

---

### Step 4: 설정 확인 및 테스트  
1. **리소스 확인 방법**  
   - S3 버킷 → "Permissions" 탭에서 정책 및 암호화 설정이 적용되었는지 확인  
   - CloudFront 배포 → "Distribution" 탭에서 상태가 "Deployed"인지 확인  

2. **정상 동작 테스트**  
   - 브라우저에서 `https://example.com`에 접근하여 S3 파일이 정상적으로 제공되는지 확인  
   - AWS CLI로 `aws s3 ls` 명령어를 사용해 버킷 내용 확인  

3. **비용 점검**  
   - "Cost Explorer"에서 S3 스토리지 및 CloudFront 데이터 전송 비용 확인  
   - 프리티어 사용 시, 월 12개월간 1200시간의 무료 사용량 제공  

> **📸 화면 확인:** Route 53에서 CNAME 레코드가 CloudFront DNS Name으로 설정된 상태인지 확인하세요.  

---

### 🧾 실습 체크리스트  
- [ ] S3 버킷 생성 및 정책 설정 완료  
- [ ] CloudFront 배포 및 DNS 설정 완료  
- [ ] Route 53 라우팅 정책 및 Global Accelerator 구성  
- [ ] 정상적인 데이터 전송 및 암호화 확인  
- [ ] 비용 점검 및 프리티어 활용 여부 확인

## ⌨️ AWS CLI로 CloudFront 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**필요 사항:**  
- AWS CLI가 설치되어 있어야 합니다.  
- `aws configure`로 AWS 자격 증명이 설정되어 있어야 합니다.  
- CloudFront 리소스 관리 권한이 있어야 합니다.  

---

### 예제 1: CloudFront 리소스 조회
```bash
# [CloudFront 배포 목록 조회]
aws cloudfront list-distributions --query '[].Id' --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | JSON 결과 필터링 (예: `[].Id`로 배포 ID만 조회) | `[].Id` |
| `--output` | 출력 형식 (json, table, text) | `table` |

**예상 출력:**
```
|       Id        |
+-----------------+
| E2QWZ1XQZ7Q8X5 |
| E2QWZ1XQZ7Q8X6 |
```

**팁:**  
`--query`를 사용해 특정 필드만 조회하면 결과가 깔끔하게 정리됩니다.  

---

### 예제 2: CloudFront 배포 생성
```bash
# [CloudFront 배포 생성]
aws cloudfront create-distribution \
    --distribution-config '{
        "Comment": "Example Distribution",
        "Origins": [
            {
                "Id": "S3Origin",
                "DomainName": "example-bucket.s3.amazonaws.com",
                "OriginPath": "/",
                "CustomHeaders": []
            }
        ],
        "DefaultCacheBehavior": {
            "TargetOriginId": "S3Origin",
            "ForwardedValues": {
                "QueryString": false
            },
            "ViewerProtocolPolicy": "allow-all"
        },
        "Enabled": true
    }'
```

**필수 옵션:**  
- `--distribution-config`: 배포 구성 (S3 버킷 연결, 캐싱 정책 등)  
- `Enabled`: 배포 활성화 여부  

**예상 출력:**
```json
{
    "Distribution": {
        "Id": "E2QWZ1XQZ7Q8X7",
        "ARN": "arn:aws:cloudfront::123456789012:distribution/E2QWZ1XQZ7Q8X7",
        "Status": "Deployed"
    }
}
```

**주의:**  
- `--distribution-config`는 JSON 형식으로 전달해야 합니다.  
- S3 버킷 이름은 실제 배포할 S3 버킷 이름으로 변경해야 합니다.  

---

### 예제 3: CloudFront 배포 수정
```bash
# [CloudFront 배포 수정]
aws cloudfront update-distribution \
    --distribution-id "E2QWZ1XQZ7Q8X7" \
    --distribution-config '{
        "Comment": "Updated Distribution",
        "Origins": [
            {
                "Id": "S3Origin",
                "DomainName": "example-bucket.s3.amazonaws.com",
                "OriginPath": "/",
                "CustomHeaders": []
            }
        ],
        "DefaultCacheBehavior": {
            "TargetOriginId": "S3Origin",
            "ForwardedValues": {
                "QueryString": true
            },
            "ViewerProtocolPolicy": "allow-all"
        },
        "Enabled": true
    }'
```

**필수 옵션:**  
- `--distribution-id`: 수정할 배포 ID  
- `--distribution-config`: 수정할 구성 정보  

**팁:**  
- `Enabled`를 `false`로 변경하면 배포를 비활성화할 수 있습니다.  

---

### 예제 4: CloudFront 배포 삭제
```bash
# [CloudFront 배포 삭제]
aws cloudfront delete-distribution --distribution-id "E2QWZ1XQZ7Q8X7"

# 삭제 확인
aws cloudfront get-distribution --id "E2QWZ1XQZ7Q8X7"
```

> **⚠️ 주의:** 삭제는 되돌릴 수 없습니다. `get-distribution`로 삭제 전 상태 확인 후 실행하세요.  

---

### 자주 사용하는 명령어 정리
```bash
# 조회
aws cloudfront list-distributions
aws cloudfront get-distribution --id "id"

# 생성
aws cloudfront create-distribution --distribution-config "..."
aws cloudfront update-distribution --distribution-id "id" --distribution-config "..."
aws cloudfront delete-distribution --distribution-id "id"
```

**추가 팁:**  
- `--query` 옵션으로 특정 필드만 필터링해 결과를 간결하게 관리하세요.  
- `aws cloudfront help`로 전체 명령어 목록을 확인할 수 있습니다.  

---

### 실습: S3 버킷 생성 및 CloudFront 설정
1. **S3 버킷 생성**  
   ```bash
   aws s3api create-bucket --bucket "example-bucket" --region "ap-northeast-1"
   ```

2. **버킷 정책 설정**  
   ```bash
   aws s3api put-bucket-policy --bucket "example-bucket" --policy '{
       "Version": "2012-10-17",
       "Statement": [
           {
               "Sid": "PublicReadGetObject",
               "Effect": "Allow",
               "Principal": "*",
               "Action": "s3:GetObject",
               "Resource": "arn:aws:s3:::example-bucket/*"
           }
       ]
   }'
   ```

3. **버킷 암호화 활성화**  
   ```bash
   aws s3api put-bucket-encryption --bucket "example-bucket" --server-side-encryption-configuration '{
       "Rules": [
           {
               "ApplyServerSideEncryptionByDefault": {
                   "SSEAlgorithm": "AES256"
               }
           }
       ]
   }'
   ```

**비용 주의사항:**  
- CloudFront 배포는 트래픽에 따라 비용이 발생합니다. 프리티어로 12개월간 120,000원까지 무료입니다.  
- S3 버킷 암호화는 데이터 보호에 필수적이지만, 추가 비용이 발생할 수 있습니다.  

---

### ✅ 확인 사항
- AWS CLI 버전이 1.28 이상인지 확인  
- 자격 증명이 정확히 설정되었는지 확인  
- CloudFront 리소스 ID를 정확히 입력  
- S3 버킷 이름과 CloudFront 배포 설정이 일치하는지 확인

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 CloudFront 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **CloudFront의 주요 용도 및 장점**:
   - 설명: CloudFront는 글로벌 CDN 서비스로, 정적 자산 및 동적 콘텐츠의 빠른 전송을 통해 웹사이트 성능 최적화에 필수적입니다. 시험에서는 CDN 기능, 지연 시간 감소, 대역폭 비용 절감 등의 핵심 장점을 묻는 경우가 많습니다.  
   - 키워드: `CDN`, `지연 시간`, `대역폭 비용`, `성능 최적화`

2. **CloudFront의 비용 구조 및 프리티어 제한**:
   - 설명: CloudFront는 데이터 전송량에 따라 요금이 부과되며, 프리티어는 월 10GB 이내 무료입니다. 시험에서는 데이터 전송량 기반 요금 구조와 프리티어 한계를 묻는 문제가 자주 출제됩니다.  
   - 키워드: `데이터 전송량`, `프리티어`, `요금 구조`, `비용 관리`

3. **캐싱 및 TTL 설정**:
   - 설명: CloudFront의 캐싱 전략과 Time to Live(TTL) 설정은 콘텐츠 전송 효율성에 직접적인 영향을 미칩니다. 시험에서는 캐싱 정책, TTL 조절, 캐시된 콘텐츠 삭제 방법 등이 주요 출제 항목입니다.  
   - 키워드: `캐싱`, `TTL`, `캐시 전략`, `콘텐츠 전송`

4. **보안 기능 및 인증 설정**:
   - 설명: CloudFront는 AWS WAF, AWS Shield, SSL/TLS 암호화 등 보안 기능을 지원합니다. 시험에서는 인증 방법, 보안 그룹 설정, DDoS 방어 기술 등이 자주 묻는 내용입니다.  
   - 키워드: `SSL/TLS`, `AWS WAF`, `DDoS 방어`, `인증 방법`

5. **CloudFront와 Route 53의 통합**:
   - 설명: CloudFront와 Route 53의 연동은 DNS 라우팅 및 고가용성 구현에 핵심입니다. 시험에서는 라우팅 정책, DNS 엔드포인트 설정, failover 전략 등이 중요하게 다뤄집니다.  
   - 키워드: `Route 53`, `DNS 라우팅`, `failover`, `고가용성`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | CloudFront는 라우팅 정책을 제공한다고 오해 | CloudFront는 라우팅 정책을 제공하지 않으며, Route 53에서 설정합니다. |
| 함정 2 | CloudFront의 비용이 항상 낮다고 생각 | 데이터 전송량에 따라 비용이 변동되며, 프리티어 한계를 초과하면 요금이 발생합니다. |
| 함정 3 | CloudFront와 Global Accelerator의 차이를 혼동 | CloudFront는 CDN, Global Accelerator는 애플리케이션 가속기로 목적과 기능이 다릅니다. |

#### 🔄 CloudFront vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | CloudFront | Global Accelerator | 선택 기준 |
|----------|----------|-----------|----------|
| 용도 | 정적 자산 및 동적 콘텐츠 전송 (CDN) | 애플리케이션 트래픽 가속 (애플리케이션 가속기) | 콘텐츠 전송 vs. 애플리케이션 레벨 가속 |
| 확장성 | 글로벌 노드 기반 스케일링 | 리전 기반 가속 | 대규모 트래픽 처리 시 CDN vs. 애플리케이션 가속 |
| 비용 | 데이터 전송량 기반 요금 | 리전별 데이터 전송량 기반 요금 | 트래픽 패턴에 따라 적합한 서비스 선택 |
| 지연 시간 | 글로벌 노드로 최소화 | 리전 내에서 최소화 | 지역적 vs. 글로벌 트래픽 최적화 |

#### 📝 시험 대비 체크리스트
- [ ] CloudFront의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  (예: "CloudFront는 글로벌 CDN 서비스로, 웹사이트 성능을 최적화합니다.")  
- [ ] CloudFront를 선택해야 하는 시나리오를 알고 있는가?  
  (예: "정적 자산 배포, 동적 콘텐츠 전송, 지연 시간 최소화")  
- [ ] CloudFront의 제한사항/한계를 알고 있는가?  
  (예: "프리티어 한계, 데이터 전송량 요금, SSL/TLS 설정 필요")  
- [ ] CloudFront와 Global Accelerator의 차이점을 설명할 수 있는가?  
  (예: "CDN vs. 애플리케이션 가속기, 목적과 기능의 차이")  
- [ ] CloudFront의 비용 구조를 이해하고 있는가?  
  (예: "데이터 전송량 기반 요금, 프리티어 한계, 캐싱 전략에 따른 비용 절감")  

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 CloudFront를 떠올리세요:  
> - **CDN** (Content Delivery Network)  
> - **지연 시간** 또는 **지연 감소**  
> - **데이터 전송량** 또는 **비용 관리**  
> - **캐싱** 또는 **TTL**  
> - **SSL/TLS** 또는 **보안 인증**

---

| [⬅️ S3](./S3.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 2](../README.md) | [Route 53 ➡️](./Route-53.md) |

---
