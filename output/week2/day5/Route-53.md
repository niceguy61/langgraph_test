---

| [⬅️ CloudFront](./CloudFront.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 2](../README.md) | [Global Accelerator ➡️](./Global-Accelerator.md) |

---

# Route 53 완전 정복

> **한 줄 정의:** Route 53는 도메인 이름 관리 및 DNS 서비스를 제공하는 AWS의 인프라 서비스입니다.

### 이 서비스가 해결하는 문제  
**기존의 문제점:**  
- **문제 1:** 기존 DNS 서버는 수동으로 설정해야 하며, IP 주소 변경 시 수동 업데이트가 필요해 실시간 반응이 어렵습니다. 예를 들어, 서버 장애 시 IP 주소 변경이 지연되면 사용자 접근이 차단됩니다.  
- **문제 2:** 지역별 네트워크 지연으로 인해 사용자 접근 속도가 저하됩니다. 예를 들어, 미국 사용자가 한국 도메인을 요청하면 데이터가 미국에서 한국으로 전달되며 속도가 느립니다.  
- **문제 3:** DNS 해킹이나 스팸 트래픽으로 인해 서비스가 중단될 수 있습니다. 예를 들어, DNS 캐시 토큰이 조작되면 사용자가 잘못된 웹사이트로 리디렉션될 수 있습니다.  

**Route 53로 해결:**  
- **해결 1:** 자동화된 DNS 관리로 실시간 IP 주소 변경이 가능합니다. 예를 들어, AWS EC2 인스턴스가 장애 발생 시 Route 53가 즉시 대체 IP 주소를 설정해 서비스 중단을 방지합니다.  
- **해결 2:** 글로벌 DNS 네트워크를 통해 사용자 지역에 최적화된 서버로 요청을 전달합니다. 예를 들어, 한국 사용자가 "example.com"을 요청하면 한국 서버로 연결되어 지연을 최소화합니다.  
- **해결 3:** 보안 기능(예: DNSSEC)을 통해 DNS 해킹을 방어하고, 트래픽 필터링으로 스팸 트래픽을 차단합니다.  

### 비유로 이해하기  
Route 53은 도메인 이름을 실제 서버 IP 주소로 변환하는 '전화번호부' 역할을 합니다. 기존 전화번호부는 사람이 직접 업데이트해야 했지만, Route 53은 실시간으로 변경된 번호를 자동으로 업데이트해 사용자가 항상 최신 번호를 얻을 수 있습니다. 예를 들어, 회사 전화번호가 변경되면 전화번호부에 직접 수정해야 했지만, Route 53은 자동으로 업데이트해 사용자가 바로 새 번호를 확인할 수 있습니다.  

---

## 🎯 주요 사용 시나리오 (When to Use)  

| 시나리오 | 설명 | 구체적 예시 |
|---------|------|-----------|
| 시나리오 1 | 기업의 도메인 관리 및 DNS 설정 | 기업이 "example.com"을 사용해 웹사이트를 운영하고, 서버 IP 주소 변경 시 자동 업데이트가 필요함 |
| 시나리오 2 | 글로벌 사용자에게 빠른 접근 제공 | 해외 사용자가 한국 기반 웹사이트를 요청할 때 지역 최적 서버로 연결됨 |
| 시나리오 3 | 다중 서버 환경에서 고가용성 보장 | 서버 장애 시 자동으로 대체 서버로 트래픽 전환 |  

**이럴 때 Route 53를 선택하세요:**  
- ✅ **도메인 이름 관리가 필요할 때**  
- ✅ **사용자 지역에 따라 서버를 자동 전환해야 할 때**  
- ✅ **고가용성 및 보안이 요구되는 시스템 구축 시**  

**이럴 때는 다른 서비스를 고려하세요:**  
- ❌ **단순 DNS 설정이 필요할 때 → Route 53의 기본 기능 사용**  
- ❌ **고성능 CDN이 필요할 때 → CloudFront 사용**  
- ❌ **사용자 트래픽 분산이 필요할 때 → Global Accelerator 사용**  

---

## 🔗 연관 서비스 (Used Together With)  

| 연관 서비스 | 연동 목적 | 일반적인 아키텍처 |
|------------|---------|-----------------|
| **CloudFront** | CDN으로 트래픽 가속화 | User → CloudFront → S3 |
| **S3** | 정적 웹사이트 호스팅 및 파일 저장 | Route 53 → CloudFront → S3 |
| **Global Accelerator** | 글로벌 사용자에게 최적화된 트래픽 전달 | User → Global Accelerator → Route 53 → EC2/ELB |

**자주 사용되는 아키텍처 패턴:**  
```
User → CloudFront → S3 (정적 자산 호스팅)  
User → Global Accelerator → Route 53 → EC2 (서버 부하 분산)  
User → Route 53 → ELB → EC2 (고가용성 웹 서버)  
```  

---

## 💰 비용 구조 (Pricing)  

| 과금 항목 | 요금 | 프리티어 |
|----------|-----|---------|
| **Hosted Zone** | $0.01/월 (최대 100개 무료) | 월 100개까지 무료 |
| **DNS 쿼리** | $0.01/10만 건 | 10만 건까지 무료 |
| **Route 53 Resolver** | $0.01/10만 건 | 10만 건까지 무료 |

**비용 최적화 팁:**  
1. 💡 **프리티어 활용:** 초기 100개 Hosted Zone은 무료로 사용 가능하므로, 기업용 도메인 관리 시 무료로 시작하세요.  
2. 💡 **DNS 쿼리 최소화:** 사용자 요청을 적절히 분산해 쿼리 수를 줄이세요. 예: CloudFront와 함께 사용해 캐싱을 최대화합니다.  
3. 💡 **Resolver 사용 제한:** 필요하지 않은 Resolver 기능은 비활성화해 비용을 절감하세요.  

> **⚠️ 비용 주의:** 프리티어 한도를 초과하면 과금이 시작되므로, AWS CloudWatch를 사용해 사용량을 모니터링하고, 필요 시 리소스를 조정해야 합니다.

## 📚 핵심 개념

### 개념 1: Route 53 라우팅 정책  
Route 53 라우팅 정책은 DNS 요청을 클라우드 리소스에 전달하는 방식을 정의하는 기능입니다. 이 정책은 트래픽을 특정 엔드포인트로 라우팅하거나, 다중 리소스 간에 트래픽을 분산하는 역할을 합니다. 주요 목적은 고가용성, 저지연, 지리적 최적화를 달성하는 것입니다. 예를 들어, 동일한 애플리케이션을 여러 리전에 배포했을 때, Route 53은 사용자의 위치에 따라 가장 가까운 리전의 엔드포인트를 선택할 수 있습니다.  

#### 왜 중요한가?  
- **고가용성 확보**: 단일 포인트 실패를 방지하고, 트래픽을 다중 리소스에 분산합니다.  
- **지연 최소화**: 사용자의 지역에 따라 최적의 엔드포인트를 선택해 응답 속도를 향상시킵니다.  

#### 세부 요소  
| 요소 | 설명 | 예시 |  
|-----|-----|-----|  
| **단일 라우팅** | 트래픽을 하나의 엔드포인트로 전달합니다. | 웹사이트의 주요 엔드포인트로 모든 요청을 라우팅. |  
| **다중 라우팅** | 트래픽을 여러 리소스에 분산합니다. | A형 라우팅을 통해 랜덤하게 서버를 선택. |  
| **지리적 라우팅** | 사용자의 위치에 따라 리소스를 선택합니다. | 유럽 사용자에게 유럽 리전의 서버를 할당. |  

> **💡 Tip:** 라우팅 정책을 선택할 때는 트래픽 분산 방식과 지연 최소화를 고려해 적절한 정책을 적용해야 합니다.  

---

### 개념 2: Global Accelerator  
Global Accelerator는 AWS에서 제공하는 DNS 기반 트래픽 분산 서비스로, 글로벌 사용자에게 저지연을 제공합니다. 이 서비스는 Route 53과 연동되어, 사용자의 지역에 따라 최적의 엔드포인트로 트래픽을 전달합니다. Global Accelerator는 HTTP/HTTPS 트래픽을 처리하고, DNS 캐시를 통해 빠른 응답을 제공합니다.  

#### 작동 원리  
1. **DNS 요청 수신**: 사용자의 브라우저가 도메인을 요청할 때, Global Accelerator는 Route 53을 통해 DNS 캐시를 확인합니다.  
2. **트래픽 분산**: 사용자의 지역에 따라 최적의 엔드포인트(예: EC2 인스턴스, ALB)를 선택해 트래픽을 전달합니다.  
3. **지연 최소화**: 전달된 트래픽이 목적지까지 최단 경로로 전달되어 응답 속도가 향상됩니다.  

> **💡 Tip:** Global Accelerator는 고가용성과 저지연을 동시에 요구하는 서비스에 적합합니다. 예를 들어, 높은 트래픽을 받는 웹사이트나 API 게이트웨이에 적용할 수 있습니다.  

---

### 개념 3: CloudFront 배포  
CloudFront는 AWS의 글로벌 CDN 서비스로, 웹 콘텐츠를 사용자에게 빠르게 전달합니다. CloudFront는 Route 53과 연동되어, DNS 캐시를 통해 사용자에게 최적의 엔드포인트를 제공합니다. 이 서비스는 동영상 스트리밍, 정적 자산 배포, API 요청 처리 등 다양한 시나리오에 활용됩니다.  

#### 주요 특징  
1. **글로벌 라우팅**: 사용자의 지역에 따라 최적의 엔드포인트를 선택해 응답 속도를 향상시킵니다.  
2. **지연 최소화**: CDN을 통해 데이터가 지역적인 라이브러리에서 제공되므로, 네트워크 지연을 줄입니다.  
3. **보안 강화**: WAF, SSL/TLS, DDoS 방어 기능을 통해 보안을 강화할 수 있습니다.  

> **💡 Tip:** CloudFront는 정적 자산 배포나 높은 트래픽을 처리하는 시나리오에서 필수적인 역할을 합니다. 예를 들어, 이미지, CSS, JavaScript 파일의 배포에 활용할 수 있습니다.

## 🖥️ AWS 콘솔에서 Route 53 사용하기

### Step 1: Route 53 서비스 접속  
1. AWS Management Console에 로그인합니다.  
   - URL: [https://console.aws.amazon.com](https://console.aws.amazon.com)  
2. 상단 검색창에 **"Route 53"**을 입력한 후, 검색 결과에서 **"Route 53"** 서비스를 클릭합니다.  
3. **Route 53 대시보드**가 열리면, **Hosted Zone** 목록이 표시됩니다.  

> **📸 화면 확인:** Route 53 대시보드에서 **Hosted Zone** 탭이 활성화된 상태로 표시되면 정상입니다.  

---

### Step 2: [주요 작업 1 - 호스티드 존 생성]  
1. **"Hosted Zone"** 메뉴에서 **"Create Hosted Zone"**을 클릭합니다.  
   - **Domain name**: 예를 들어 `example.com`을 입력합니다.  
   - **Private zone**: 공개 DNS를 사용할 경우 **No**를 선택합니다.  
2. **"Create"** 버튼을 클릭하여 호스티드 존을 생성합니다.  
3. 생성된 호스티드 존의 **ID**와 **DNS Zone** 주소를 확인합니다.  

> **📸 화면 확인:** **"Hosted Zone"** 목록에 생성된 호스티드 존이 표시되고, **DNS Zone** 주소가 표시되는지 확인합니다.  

---

### Step 3: [주요 작업 2 - DNS 레코드 설정]  
1. 호스티드 존을 선택한 후 **"Record sets"** 탭을 클릭합니다.  
2. **"Create record"** 버튼을 클릭하여 레코드를 추가합니다.  
   - **Name**: 예를 들어 `www`를 입력합니다.  
   - **Type**: `A` 또는 `CNAME`을 선택합니다.  
   - **Value**: IP 주소 또는 대상 도메인을 입력합니다.  
   - **TTL**: 300초로 설정합니다.  
3. **"Create"** 버튼을 클릭하여 레코드를 저장합니다.  

> **⚠️ 주의:** 레코드 설정 시 **TTL** 값을 조정하면 DNS 캐시 갱신 주기를 변경할 수 있습니다.  

---

### Step 4: 설정 확인 및 테스트  
1. **"DNS Query"** 탭에서 도메인을 입력해 DNS 조회 결과를 확인합니다.  
   - 예: `dig www.example.com` 명령어로 확인합니다.  
2. **"Health Checks"** 탭에서 호스티드 존의 상태를 확인합니다.  
   - **Status**: `Healthy` 또는 `Unhealthy`로 표시됩니다.  
3. **"Route 53 Resolver"**를 통해 정적/동적 DNS 해결을 테스트합니다.  
   - **Resolver Rules**를 설정하여 VPC 내외부의 DNS 요청을 관리할 수 있습니다.  

> **📸 화면 확인:** **"DNS Query"** 탭에서 **IP 주소**가 정확히 반환되는지, **Health Checks** 탭에서 **Healthy** 상태가 표시되는지 확인합니다.  

---

### CLI 명령어 예시  
```bash
# 호스티드 존 생성
aws route53 create-hosted-zone --name example.com --caller-reference "example-123"

# 레코드 추가
aws route53 change-resource-record-sets --hosted-zone-id Z1234567890ABC --change-batch file://record.json
```

> **💡 Tip:** CLI를 사용할 경우, JSON 파일로 레코드 설정을 정의하여 정확한 구문을 입력하세요.  

---

### 가격 및 비용 관리  
- **Route 53**은 **$0.50/월**부터 시작하며, **100,000개 레코드**까지 무료입니다.  
- **프리티어**는 1년간 **100,000개 레코드** 및 **100GB 트래픽**을 제공합니다.  
- **CloudFront**와 연동 시 **DNS 캐시 최적화**를 통해 비용을 절감할 수 있습니다.  

> **⚠️ 주의:** 레코드 수나 트래픽량이 증가할 경우, **AWS Cost Explorer**를 사용해 비용을 모니터링하세요.  

---

### 실습 예제: S3 버킷 연결  
1. **S3 버킷**을 생성한 후, **Route 53**에서 `CNAME` 레코드를 추가합니다.  
   - **Value**: `bucket-name.s3.amazonaws.com`  
2. **CloudFront** 배포 시, **Route 53**의 `A record`를 사용해 **CNAME**을 설정합니다.  
3. **Global Accelerator**와 연동해 **DNS 해결 속도**를 개선합니다.  

> **💡 Tip:** S3 버킷과 Route 53의 연동 시 **DNS Propagation** 시간(약 5~30분)을 고려하세요.

## ⌨️ AWS CLI로 Route 53 사용하기

### 사전 준비
```bash
# AWS CLI 버전 확인
aws --version

# AWS 자격 증명 확인
aws sts get-caller-identity

# 현재 리전 확인
aws configure get region
```

**설명:**  
AWS CLI를 사용하기 전에 버전을 확인하고, IAM 자격 증명이 정상적으로 설정되었는지 확인하세요. 리전은 Route 53 리소스의 위치를 결정합니다.  
**팁:** AWS CLI를 설치하지 않았다면 [https://aws.amazon.com/cli/](https://aws.amazon.com/cli/)에서 다운로드하세요.

---

### 예제 1: Route 53 호스티드 존 목록 조회
```bash
# 호스티드 존 목록 조회
aws route53 list-hosted-zones --query "HostedZones[].Id" --output table
```

**옵션 설명:**
| 옵션 | 설명 | 예시 값 |
|-----|------|--------|
| `--query` | JSON 결과 필터링 | `"HostedZones[].Id"` |
| `--output` | 출력 형식 | `json`, `table`, `text` |

**예상 출력:**
```
|     Id      |
|-------------|
| /hostedzone/ID1234567890 |
| /hostedzone/ID0987654321 |
```

**참고:** 호스티드 존 ID는 `/hostedzone/` 형식으로 반환됩니다.

---

### 예제 2: Route 53 호스티드 존 생성
```bash
# 호스티드 존 생성
aws route53 create-hosted-zone \
    --name "example.com" \
    --vpc VPC_ID \
    --caller-reference "1234567890"
```

**필수 옵션:**
- `--name`: 도메인 이름 (예: `example.com`)
- `--vpc`: VPC ID (VPC를 사용할 경우)
- `--caller-reference`: 유일한 참조 ID (예: `1234567890`)

**예상 출력:**
```json
{
    "HostedZone": {
        "Id": "/hostedzone/ID1234567890",
        "Arn": "arn:aws:route53::1234567890:zone/ID1234567890",
        "Name": "example.com"
    }
}
```

**참고:** VPC를 사용하지 않을 경우 `--vpc` 옵션을 생략할 수 있습니다.

---

### 예제 3: Route 53 호스티드 존 수정
```bash
# 호스티드 존 주석 수정
aws route53 update-hosted-zone \
    --id "/hostedzone/ID1234567890" \
    --comment "Updated on 2023-10-01"
```

**필수 옵션:**
- `--id`: 수정할 호스티드 존 ID
- `--comment`: 새로운 주석 내용

**참고:** 호스티드 존의 기본 설정(예: DNSSEC, 정책)은 수정이 불가능합니다.

---

### 예제 4: Route 53 호스티드 존 삭제
```bash
# 호스티드 존 삭제
aws route53 delete-hosted-zone --id "/hostedzone/ID1234567890"

# 삭제 확인
aws route53 list-hosted-zones --query "HostedZones[].Id" --output table
```

> **⚠️ 주의:** 호스티드 존 삭제는 되돌릴 수 없습니다. 삭제 전에 모든 리소스(레코드, 정책 등)를 확인하세요.

---

### 자주 사용하는 명령어 정리
```bash
# 호스티드 존 관리
aws route53 list-hosted-zones
aws route53 create-hosted-zone --name "example.com" --caller-reference "123456"
aws route53 update-hosted-zone --id "/hostedzone/ID1234567890" --comment "New comment"
aws route53 delete-hosted-zone --id "/hostedzone/ID1234567890"

# DNS 레코드 관리
aws route53 list-resource-record-sets --zone-id "/hostedzone/ID1234567890"
aws route53 change-resource-record-sets --zone-id "/hostedzone/ID1234567890" --change-batch file://changes.json
```

**팁:** DNS 레코드 수정 시 `change-resource-record-sets` 명령어를 사용하며, 변경 사항은 AWS 콘솔에서 확인할 수 있습니다.

## 🎯 SAA-C03 시험 핵심 포인트

### 시험에서 자주 출제되는 Route 53 포인트

#### 📌 핵심 출제 포인트 TOP 5
1. **포인트 1: 라우팅 정책(Routing Policy)**  
   - 설명: Route 53의 핵심 기능 중 하나로, **latency-based**, **weighted**, **failover**, **multivalue** 등 다양한 라우팅 전략을 통해 트래픽을 최적화하는 방식입니다. 시험에서는 특정 라우팅 전략이 어떤 시나리오에 적합한지 묻는 문제가 자주 출제됩니다.  
   - 키워드: `라우팅 정책`, `latency-based`, `failover`

2. **포인트 2: 헬스 체크(Health Check)**  
   - 설명: Route 53이 인스턴스의 상태를 실시간으로 모니터링하며, 문제가 발생할 경우 트래픽을 다른 엔드포인트로 전환하는 기능입니다. 시험에서는 헬스 체크 설정 방법이나 **HTTP/HTTPS** 테스트 방법 등이 주요 검토 항목입니다.  
   - 키워드: `헬스 체크`, `HTTP/HTTPS`, `자동 복구`

3. **포인트 3: 지연 시간 기반 라우팅(Latency-Based Routing)**  
   - 설명: 사용자의 지역에 따라 가까운 서버로 트래픽을 분배하는 방식으로, **지연 시간 최소화**를 목표로 합니다. 이는 글로벌 웹 서비스에서 성능 최적화에 필수적입니다.  
   - 키워드: `지연 시간`, `지리적 분산`, `DNS 라우팅`

4. **포인트 4: 가중치 라우팅(Weighted Routing)**  
   - 설명: 트래픽을 여러 엔드포인트에 비례적으로 분배하는 방식으로, **A/B 테스트**나 **트래픽 분산** 시나리오에 적합합니다. 시험에서는 가중치 설정 방법이나 트래픽 비율 조절 방식이 주요 출제 포인트입니다.  
   - 키워드: `가중치`, `트래픽 분산`, `A/B 테스트`

5. **포인트 5: 실패 대비 라우팅(Failover Routing)**  
   - 설명: 주 엔드포인트가 다운될 경우 자동으로 대체 엔드포인트로 트래픽을 전환하는 방식입니다. **고가용성(High Availability)**을 요구하는 시나리오에서 필수적인 기능입니다.  
   - 키워드: `고가용성`, `자동 전환`, `장애 대응`

#### ⚠️ 시험에서 자주 나오는 함정
| 함정 유형 | 설명 | 올바른 답변 |
|----------|------|------------|
| 함정 1 | 라우팅 정책 중 **latency-based**와 **weighted**의 차이를 헷갈리는 경우 | `latency-based`는 지연 시간 최소화, `weighted`는 트래픽 비율 조절 |
| 함정 2 | 헬스 체크에서 **HTTP/HTTPS** 테스트 대상이 아닌 경우 | 헬스 체크는 **DNS 엔드포인트**에 대한 상태만 모니터링 |
| 함정 3 | **Failover Routing**에서 주 엔드포인트가 다운되면 자동 전환되지 않는 경우 | `Failover`는 **자동 전환** 기능이 포함되어 있음 |

#### 🔄 Route 53 vs 비슷한 서비스 비교 (시험 단골!)
| 비교 항목 | Route 53 | Global Accelerator | 선택 기준 |
|----------|----------|------------------|----------|
| 용도 | **DNS 라우팅 및 트래픽 관리** | **글로벌 트래픽 가속화** | DNS 설정이 필요한 경우 |
| 확장성 | **무한 DNS 레코드** 지원 | **100개의 액세스 포인트** 제한 | 대규모 DNS 관리가 필요한 경우 |
| 비용 | **DNS 요청 기반 요금** | **전송량 기반 요금** | 트래픽량이 많은 경우 |
| 지연 시간 | **DNS 레코드 기반 최적화** | **지역별 가속화** | 실시간 지연 최소화가 필요한 경우 |

#### 📝 시험 대비 체크리스트
- [ ] Route 53의 핵심 목적을 한 문장으로 설명할 수 있는가?  
  *예: "DNS 라우팅을 통해 트래픽을 최적화하는 서비스입니다."*
- [ ] Route 53를 선택해야 하는 시나리오를 알고 있는가?  
  *예: "글로벌 사용자에게 빠른 응답을 제공해야 할 때"*
- [ ] Route 53의 제한사항/한계를 알고 있는가?  
  *예: "트래픽이 많은 경우 추가 비용 발생"*
- [ ] Route 53와 Global Accelerator의 차이점을 설명할 수 있는가?  
  *예: "Route 53는 DNS 설정, Global Accelerator는 트래픽 가속화"*
- [ ] Route 53의 비용 구조를 이해하고 있는가?  
  *예: "DNS 요청 수에 따라 요금이 발생하며, 프리티어는 100,000 건 제공"*

#### 💡 시험 팁
> **키워드 매칭:** 문제에서 이런 키워드가 나오면 Route 53를 떠올리세요:  
> - **DNS 라우팅**  
> - **지연 시간 최소화**  
> - **트래픽 분산**  
> - **헬스 체크**  
> - **고가용성**

---

| [⬅️ CloudFront](./CloudFront.md) | [📑 Day 5 목차](./README.md) | [🏠 Week 2](../README.md) | [Global Accelerator ➡️](./Global-Accelerator.md) |

---
